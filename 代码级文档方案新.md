# 项目代码级文档生成 Agent 提示词与工程流水线

> 本文档可直接下载，作为指导 Agent 为整个项目生成代码级文档的完整方案。  
> 核心思想：**清单驱动生成，文件夹镜像阅读，符号级锚点，行级源码链接，中间表示 + 模板渲染 + 自动校验**。

---

## 0. 使用说明

- 将本文件放在项目根目录，或作为 Agent 的系统指令/任务说明书。
- 建议按阶段逐步执行，每完成一个阶段检查产物。
- 所有提示词均用 ` ```markdown ` 包裹，可整段复制给 Agent。
- 若项目较大，务必让 Agent 一次只处理一个文件或一个小批次，避免上下文溢出。
- 最终文档输出到 `docs/` 目录，**不修改源码结构**。

---

## 1. 总体设计原则

1. **不直接写长 Markdown**  
   先让 Agent 产出中间 JSON（`docs/_intermediate/`），再由模板渲染成 Markdown。  
   好处：可校验、可重跑、可增量、避免幻觉。

2. **文件清单驱动**  
   先生成 `docs/_manifest.json`，记录每个源码文件的状态、hash、文档路径、符号表。  
   每处理完一个文件就更新状态，支持中断续跑。

3. **行级解释必须有证据**  
   每条解释必须带行号范围（如 `L12-L18`）和原文片段/符号名。  
   不确定就写 `TODO`，禁止编造。

4. **优先静态解析，模型补语义**  
   Python 用 `ast`，JS/TS 用 tree-sitter 或正则，HTML 按 DOM 区块，JSON/YAML 按 key 解释。  
   先提取 imports、classes、functions、params、returns、globals，再让模型补充业务含义。

5. **文档输出到独立 `docs/`，镜像源码目录**  
   - `docs/README.md`：总览  
   - `docs/<folder>/README.md`：模块文档  
   - `docs/files/<path>.md`：单文件级文档  
   - `docs/_manifest.json`：进度清单  
   - `docs/_intermediate/...`：中间 JSON  
   - `docs/_index/source-map.md`：源码 → 文档索引  
   - `docs/_index/by-symbol.md`：符号 → 源码/文档索引

6. **质量门禁**  
   最后必须校验：源码覆盖率 100%、链接有效、行号不越界、无遗漏函数/类、Markdown 格式规范。

---

## 2. 目标目录结构

```text
project/
├── src/
│   ├── core/
│   │   ├── main.py
│   │   └── utils.py
│   └── api/
│       └── server.py
├── docs/
│   ├── README.md                  # 总览
│   ├── _manifest.json             # 文件清单与状态
│   ├── _intermediate/             # 中间 JSON
│   │   └── src/
│   │       └── core/
│   │           └── main.py.json
│   ├── _index/
│   │   ├── source-map.md          # 源码路径 -> 文档
│   │   └── by-symbol.md           # 符号 -> 源码/文档
│   ├── core/
│   │   └── README.md              # 模块文档
│   ├── api/
│   │   └── README.md
│   └── files/
│       └── src/
│           ├── core/
│           │   ├── main.py.md
│           │   └── utils.py.md
│           └── api/
│               └── server.py.md
└── ... 其他源码
```

阅读路径：

```text
docs/README.md
  → docs/core/README.md
    → docs/files/src/core/main.py.md
      → 点击源码链接跳回 src/core/main.py#L12-L48
```

---

## 3. 中间清单 manifest 设计

`docs/_manifest.json` 示例：

```json
{
  "version": "1.0",
  "generated_at": "2026-09-11T12:00:00Z",
  "root": ".",
  "exclude": [".git", "venv", "__pycache__", "node_modules", ".idea"],
  "files": {
    "src/core/main.py": {
      "language": "python",
      "hash": "a1b2c3d4",
      "status": "parsed",
      "doc_path": "docs/files/src/core/main.py.md",
      "folder_doc": "docs/core/README.md",
      "intermediate_path": "docs/_intermediate/src/core/main.py.json",
      "symbols": {
        "parse_config": {
          "type": "function",
          "start_line": 12,
          "end_line": 48,
          "source_url": "../../../../src/core/main.py#L12-L48",
          "doc_anchor": "#parse_config"
        },
        "run": {
          "type": "function",
          "start_line": 50,
          "end_line": 80,
          "source_url": "../../../../src/core/main.py#L50-L80",
          "doc_anchor": "#run"
        }
      }
    },
    "src/api/server.py": {
      "language": "python",
      "hash": "e5f6g7h8",
      "status": "pending",
      "doc_path": "docs/files/src/api/server.py.md",
      "folder_doc": "docs/api/README.md",
      "intermediate_path": "docs/_intermediate/src/api/server.py.json",
      "symbols": {}
    }
  }
}
```

中间 JSON 示例（`docs/_intermediate/src/core/main.py.json`）：

```json
{
  "file": "src/core/main.py",
  "language": "python",
  "hash": "a1b2c3d4",
  "imports": [
    {"module": "os", "alias": null, "lineno": 1},
    {"module": "json", "alias": null, "lineno": 2}
  ],
  "globals": [
    {"name": "DEFAULT_CONFIG", "lineno": 5, "type": "dict", "desc": "默认配置字典"}
  ],
  "symbols": [
    {
      "name": "parse_config",
      "type": "function",
      "lineno": 12,
      "end_lineno": 48,
      "params": [{"name": "path", "type": "str", "desc": "配置文件路径"}],
      "returns": {"type": "dict", "desc": "解析后的配置字典"},
      "logic": [
        {"lines": "12-18", "desc": "读取配置文件并校验路径", "evidence": "open(path, 'r')"},
        {"lines": "20-30", "desc": "解析 JSON 并合并默认值", "evidence": "json.load(f)"},
        {"lines": "32-48", "desc": "处理异常并返回配置字典", "evidence": "try/except"}
      ],
      "uncertainties": []
    },
    {
      "name": "run",
      "type": "function",
      "lineno": 50,
      "end_lineno": 80,
      "params": [],
      "returns": {"type": "None", "desc": "无返回值"},
      "logic": [
        {"lines": "50-60", "desc": "初始化应用", "evidence": "app = create_app()"},
        {"lines": "62-80", "desc": "启动主循环", "evidence": "app.run()"}
      ],
      "uncertainties": []
    }
  ]
}
```

---

## 4. 分阶段提示词（可直接复制给 Agent）

### 阶段 0：初始化与扫描

```markdown
# Role: 高级代码文档架构师

# Task: 初始化项目文档体系
请扫描当前根目录下的所有一级子文件夹（排除 __pycache__, .git, .idea, node_modules, venv, .env 等环境/缓存目录）。

请执行以下操作：
1. 生成一个完整的项目目录树（Tree Structure），标记出所有包含 `.py`, `.json`, `.html`, `.js`, `.css`, `.yaml`, `.xml` 文件的文件夹。
2. 在根目录下创建 `docs/` 目录。
3. 创建 `docs/_manifest.json`，包含所有源码文件的清单，字段：language, hash, status(pending), doc_path, folder_doc, intermediate_path, symbols(空对象)。
4. 创建 `docs/README.md` 骨架，包含：
   - 项目名称与一句话简介
   - 技术栈列表（根据文件后缀和内容初步推断）
   - 核心架构分层（例如：API层、业务逻辑层、数据层、静态资源层）
   - 子模块索引表格（列名：子文件夹名称 | 核心职责 | 详细文档链接），链接暂时留空。
5. 创建 `docs/_index/` 目录，以及空的 `source-map.md` 和 `by-symbol.md`。
```

---

### 阶段 1：逐文件解析（生成中间 JSON）

```markdown
# Role: 代码逆向解析专家

# Task: 深度解析源码文件 [请替换为具体文件路径，如 src/core/main.py]
当前处理目标：`./[文件路径]`

请读取该文件，输出中间 JSON 到 `docs/_intermediate/<文件路径>.json`。
JSON 必须包含：
- file: 仓库相对路径
- language: 语言
- hash: 文件内容 hash（可用 md5）
- imports: 列表，每项 {module, alias, lineno}
- globals: 列表，每项 {name, lineno, type, desc}
- symbols: 列表，每个类/函数/方法包含：
  - name
  - type: function | class | method | dom_block | config_key
  - lineno, end_lineno
  - params: [{name, type, desc}]
  - returns: {type, desc}
  - logic: [{lines, desc, evidence}]
  - uncertainties: []
- 对于 HTML：按 DOM 区块提取，type 为 dom_block，记录区块功能。
- 对于 JSON/YAML：按 key 提取，type 为 config_key，解释每个 key 的用途和取值范围。

**重要约束**：
1. 必须引用具体行号（例如 L12-L35）。
2. 每条解释必须有 evidence（原文片段或符号名）。
3. 不确定写 TODO，禁止编造。
4. 完成该文件后，更新 `docs/_manifest.json` 中对应文件的 status 为 `parsed`，并填入 symbols 的行号与链接信息。
5. 然后暂停，等待下一个文件路径。
```

---

### 阶段 2：渲染单文件文档

```markdown
# Role: 文档渲染工程师

# Task: 根据中间 JSON 生成单文件 Markdown 文档
输入：`docs/_intermediate/<文件路径>.json`
输出：`docs/files/<文件路径>.md`

文档必须包含以下内容：

---
source_path: <源码相对路径>
doc_path: <文档相对路径>
---

# <文件路径>

[打开源码](由脚本生成的相对链接)
[打开源码 Lx-Ly](由脚本生成的相对链接#Lx-Ly)

## 文件职责
用一两句话概括该文件的宏观作用。

## 导入与依赖
列出 imports，说明每个模块的作用。

## 全局变量/常量
解释文件顶部定义的全局变量或配置项的作用。

## 符号详细说明
按逻辑顺序列出所有 Class 和 Function。

### `函数名` — [源码 Lx-Ly](相对链接#Lx-Ly)

- **类型**：function / class / method
- **输入参数**：
  - `参数名` (类型)：含义
- **返回值**：(类型) 含义
- **核心逻辑拆解**：

| 源码行 | 说明 | 源码 |
|---|---|---|
| L12-L18 | 读取配置文件并校验路径 | [查看](相对链接#L12-L18) |
| L20-L30 | 解析 JSON 并合并默认值 | [查看](相对链接#L20-L30) |
| L32-L48 | 处理异常并返回配置字典 | [查看](相对链接#L32-L48) |

- **不确定性**：如有 TODO 列出。

## 执行逻辑流
用文字描述数据进入该文件后，是如何在各个函数之间流转的。

**重要约束**：
1. 所有链接从 `docs/_manifest.json` 渲染，禁止手写相对路径。
2. 必须使用正确的 Markdown 代码块高亮。
3. 完成后更新 manifest 中该文件 status 为 `rendered`。
```

---

### 阶段 3：生成模块/文件夹 README

```markdown
# Role: 模块文档整合官

# Task: 为子文件夹 [请替换为具体文件夹名，如 src/core] 生成 README
读取该文件夹下所有已渲染的单文件文档（`docs/files/<文件夹>/...`），生成 `docs/<文件夹>/README.md`。

内容要求：

## 1. 模块概述
- 该模块在项目中的定位
- 模块间的依赖关系（调用了哪些其他兄弟模块）

## 2. 文件导航表

| 源码文件 | 详细文档 | 核心符号 |
|---|---|---|
| [src/core/main.py](../../src/core/main.py) | [main.py 文档](../files/src/core/main.py.md) | `parse_config`, `run` |
| [src/core/utils.py](../../src/core/utils.py) | [utils.py 文档](../files/src/core/utils.py.md) | `format_output` |

## 3. 模块执行逻辑流
用文字描述数据进入该文件夹后，是如何在各个文件之间流转的。

**重要约束**：
1. 链接必须由脚本生成，确保相对路径正确。
2. 完成后更新 manifest 中该文件夹的状态。
```

---

### 阶段 4：生成总览与全局索引

```markdown
# Role: 文档整合官

# Task: 汇总并建立知识图谱
请回顾所有已生成的子文件夹 `README.md`。

更新 `docs/README.md`：

1. **填充子模块索引表格**：将阶段 0 的表格中“详细文档链接”替换为正确的相对路径。
2. **绘制数据流向图（文字版）**：根据各个子文件夹 README 中的“执行逻辑流”，整理出一份从用户请求到服务器响应的全局调用链路图（使用 Markdown 的 ```mermaid 或纯文本缩进流程图）。
3. **环境与配置总览**：汇总根目录下的 `requirements.txt`、`package.json` 或 `docker-compose.yml`，解释启动步骤（Install -> Build -> Run）。
4. **快速导航**：在顶部生成“快速导航”，列出所有核心类或入口函数，并链接到对应子文件夹 README 中的锚点。

同时更新 `docs/_index/source-map.md` 和 `docs/_index/by-symbol.md`：

- source-map.md：| 源码路径 | 文档 | 主要符号 |
- by-symbol.md：| 符号 | 源码位置 | 文档 |

所有链接从 `docs/_manifest.json` 渲染。
```

---

### 阶段 5：校验与补漏

```markdown
# Role: QA 文档测试员

# Task: 查漏补缺与格式规范化
请对所有生成的 `.md` 文件执行最终审查：

1. **覆盖率检查**：对照 `docs/_manifest.json`，检查是否每个源码文件都被至少一个文档提及。如果有遗漏，补充对应文档。
2. **链接有效性检查**：验证 `docs/README.md` 指向子文件夹的链接、单文件文档中的源码链接、行号锚点是否正确。
3. **行号校验**：检查所有引用的行号是否越界。
4. **格式美化**：
   - 确保所有代码块标记了正确的语言。
   - 确保所有函数名、类名使用反引号包裹。
5. **生成报告**：在 `docs/` 下创建 `DOCUMENTATION_SUMMARY.md`，列出总共解析了多少个文件夹、多少个代码文件，以及文档覆盖率。

**校验脚本建议**（可由 Agent 生成并执行）：
- 遍历 manifest，检查 status 是否全部为 rendered 或 parsed。
- 检查每个 doc_path 是否存在。
- 检查每个 source_url 指向的文件是否存在，行号是否在文件总行数内。
- 检查 Markdown 内部链接是否可解析。
```

---

## 5. 文档模板示例

### 单文件文档模板

```markdown
---
source_path: src/core/main.py
doc_path: docs/files/src/core/main.py.md
---

# src/core/main.py

[打开源码](../../../../src/core/main.py)
[打开源码 L12-L48](../../../../src/core/main.py#L12-L48)

## 文件职责
该文件是核心入口，负责解析配置并启动应用。

## 导入与依赖
- `os`：操作系统接口
- `json`：JSON 解析

## 全局变量/常量
- `DEFAULT_CONFIG` (dict, L5)：默认配置字典。

## 符号详细说明

### `parse_config` — [源码 L12-L48](../../../../src/core/main.py#L12-L48)

- **类型**：function
- **输入参数**：
  - `path` (str)：配置文件路径
- **返回值**：(dict) 解析后的配置字典
- **核心逻辑拆解**：

| 源码行 | 说明 | 源码 |
|---|---|---|
| L12-L18 | 读取配置文件并校验路径 | [查看](../../../../src/core/main.py#L12-L18) |
| L20-L30 | 解析 JSON 并合并默认值 | [查看](../../../../src/core/main.py#L20-L30) |
| L32-L48 | 处理异常并返回配置字典 | [查看](../../../../src/core/main.py#L32-L48) |

- **不确定性**：无

### `run` — [源码 L50-L80](../../../../src/core/main.py#L50-L80)

- **类型**：function
- **输入参数**：无
- **返回值**：(None) 无返回值
- **核心逻辑拆解**：

| 源码行 | 说明 | 源码 |
|---|---|---|
| L50-L60 | 初始化应用 | [查看](../../../../src/core/main.py#L50-L60) |
| L62-L80 | 启动主循环 | [查看](../../../../src/core/main.py#L62-L80) |

## 执行逻辑流
`run` 调用 `parse_config` 获取配置，然后初始化应用并启动主循环。
```

### 模块 README 模板

```markdown
# core 模块

## 1. 模块概述
核心业务逻辑层，包含配置解析和应用启动。

## 2. 文件导航表

| 源码文件 | 详细文档 | 核心符号 |
|---|---|---|
| [src/core/main.py](../../src/core/main.py) | [main.py 文档](../files/src/core/main.py.md) | `parse_config`, `run` |
| [src/core/utils.py](../../src/core/utils.py) | [utils.py 文档](../files/src/core/utils.py.md) | `format_output` |

## 3. 模块执行逻辑流
`main.py` 的 `run` 函数调用 `parse_config`，后者使用 `utils.py` 中的 `format_output` 格式化输出。
```

### 总览 README 模板

```markdown
# 项目总览

## 项目名称与简介
XXX 项目：一个用于 XXX 的工具。

## 技术栈
- Python 3.10
- FastAPI
- Vue 3

## 核心架构分层
- API 层：`src/api/`
- 业务逻辑层：`src/core/`
- 数据层：`src/db/`
- 静态资源：`static/`

## 子模块索引

| 子文件夹名称 | 核心职责 | 详细文档链接 |
|---|---|---|
| `src/core/` | 核心业务逻辑 | [查看详情](./core/README.md) |
| `src/api/` | HTTP 接口 | [查看详情](./api/README.md) |

## 快速导航
- [核心认证类](./core/README.md#核心类)
- [配置解析函数](./files/src/core/main.py.md#parse_config)

## 数据流向图
```mermaid
graph LR
A[用户请求] --> B[src/api/server.py]
B --> C[src/core/main.py]
C --> D[src/core/utils.py]
```

## 环境与配置
启动步骤：
1. `pip install -r requirements.txt`
2. `python src/core/main.py`
```

---

## 6. 源码定位与链接生成规则

- 每个文件文档必须包含 frontmatter：`source_path` 和 `doc_path`。
- 文件文档顶部必须有：
  - `[打开源码](相对链接)`
  - `[打开源码 Lx-Ly](相对链接#Lx-Ly)`
- 每个函数/类标题必须写成：`### \`name\` — [源码 Lx-Ly](相对链接#Lx-Ly)`
- 每个行级解释表格必须包含“源码”列：`| 源码行 | 说明 | 源码 |`
- 所有相对链接由脚本根据 `docs/_manifest.json` 生成，禁止手写。
- 生成规则：
  - 从 `docs/files/src/core/main.py.md` 到 `src/core/main.py` 的相对路径为 `../../../../src/core/main.py`。
  - 行号锚点格式：`#L12-L48`。
- 必须生成两个全局索引：
  - `docs/_index/source-map.md`：从源码找文档。
  - `docs/_index/by-symbol.md`：从函数/类找源码和文档。

---

## 7. 校验清单

- [ ] `docs/_manifest.json` 中所有文件 status 为 `rendered`。
- [ ] 每个源码文件都有对应的 `docs/files/<path>.md`。
- [ ] 每个模块文件夹都有 `docs/<folder>/README.md`。
- [ ] `docs/README.md` 包含所有模块索引和快速导航。
- [ ] 所有源码链接指向真实文件，行号不越界。
- [ ] 所有 Markdown 内部链接有效。
- [ ] 没有未解释的函数/类/全局变量。
- [ ] 所有代码块标记了正确语言。
- [ ] `DOCUMENTATION_SUMMARY.md` 已生成。

---

## 8. 给 Agent 的额外约束与提示

- **执行原则**：不要杜撰代码逻辑。所有解释必须严格基于读取到的实际代码内容。
- **分批处理**：一次只处理一个文件或一个小批次，避免上下文爆炸。
- **状态更新**：每完成一个文件，立即更新 `docs/_manifest.json`。
- **使用工具**：优先使用 `ast`、`tree-sitter`、正则等静态解析工具提取结构，再让模型补充语义。
- **不确定性**：遇到无法确定的地方，写 `TODO` 并说明原因，不要猜测。
- **最终目标**：生成一套可导航、可验证、与源码行号精确对应的代码级文档。

---

## 附录：完整提示词一键复制版

你可以按顺序将以下提示词复制给 Agent：

```markdown
# 阶段 0：初始化与扫描
（复制阶段 0 提示词）

# 阶段 1：逐文件解析
（复制阶段 1 提示词，每次替换文件路径）

# 阶段 2：渲染单文件文档
（复制阶段 2 提示词）

# 阶段 3：生成模块 README
（复制阶段 3 提示词，每次替换文件夹名）

# 阶段 4：生成总览与全局索引
（复制阶段 4 提示词）

# 阶段 5：校验与补漏
（复制阶段 5 提示词）
```

> 提示：若 Agent 支持系统指令，可将上述所有内容作为系统提示，并让它自行规划执行顺序。若为对话式，建议按阶段逐步推进。

---

**文档结束。**