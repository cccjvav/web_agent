# 文档约定（仓库级）

功能改完必须同步改说明书。本文件不是产品指南，是给后续提交用的检查清单。

对照的行级模板仍是第一阶段：子文件夹 `README.md` 写（1）概述与兄弟依赖（2）每文件职责、全部 Class/Function 参数/返回、**Lxx–Lyy**、常量（3）仅该夹执行流。JSON 每个 Key。解释必须能对上磁盘，不杜撰。

---

## 什么时候改哪一份

| 你改了 | 必须同步 |
|---|---|
| `shuncode-core/agent-host/src/<夹>/*.js` | 该夹 [`README.md`](./shuncode-core/agent-host/src/tools/README.md) 里对应 `### 📄 文件名`：函数行号、分支、返回值 |
| 新增 `.js` / `.html` / `.css` / 根 `.cmd` | 所在夹 README 增加一节；根脚本走 [`启动脚本说明.md`](./启动脚本说明.md)。然后改 [`DOCUMENTATION_SUMMARY.md`](./DOCUMENTATION_SUMMARY.md) 的文件数 |
| MCP 工具名、参数、错误码、`initialize.instructions` | [`src/tools/README.md`](./shuncode-core/agent-host/src/tools/README.md)、[`src/mcp/README.md`](./shuncode-core/agent-host/src/mcp/README.md)；工作流变了再改 [`总览.md`](./总览.md) §2 |
| `src/api/routes.js` 新路由 | [`src/api/README.md`](./shuncode-core/agent-host/src/api/README.md) 路由表 |
| `workbench/index.html` 或 `app.js` 按钮/统计 | [`workbench/README.md`](./shuncode-core/workbench/README.md) |
| `agent-host/tests/*.test.js` 新断言或新文件 | [`tests/README.md`](./shuncode-core/agent-host/tests/README.md)；`package.json` 的 `scripts.test` 若加文件也要写进去 |
| 用户请求 → 响应的全局链路（Chat / Bridge / 网页 VS Code） | [`总览.md`](./总览.md) §2，不要只改子 README |
| 根 `README.md` | **不要**改成行级模板（第四阶段约束） |

`shuncode-repro/` 冻结：不在那里再拆一套子 README，也不要为了文档去改它的 JS。

---

## 行号

- 用磁盘行号（`nl -ba` 或按换行计数），**不要**用编辑器/工具回传的偏一行的 Lxx。
- 函数写闭区间 **Lxx–Lyy**（含最后的 `}`）。
- 改完函数后，同文件后面所有函数的 Lxx 都要重数，不要只改动过的那一段。

---

## 禁止

- 写源码里没有的调用、路由、环境变量、工具名。
- 把指南口吻（「应该会开隧道」）写成实现（`POST /bridge/start` **没有** `startQuickTunnel`）。
- 为了对齐文档去改 `CONNECT_LINE` 或其它测试锁死的字符串，除非测试一起改。
- 提交 `bin/code-server-dist/`、`node_modules/`、`image-search/`。

---

## 提交前

1. `cd shuncode-core/agent-host && npm test`（或仓库根 `run-tests.cmd`）绿。
2. 新代码文件能在至少一份 README 里搜到文件名。
3. [`总览.md`](./总览.md) 若新增了「查看详情」链接，必须是 `./` 相对路径且目标存在。
