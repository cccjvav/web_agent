# 双向连接核对使用指南

## 先说清楚：核对连接，不鉴定后台模型

你的 `arena-model-probe` 已从分支提交 `73b46ae` 合入。原型保留为独立研究代码，本次没有自动运行它的抓包、登录、行为提问、后台轨迹查询或模型解析脚本。

本次接入的是一个独立、可选、最小采集的用户脚本：`arena-model-probe/webagent-connection.user.js`。它可以与原型分开使用，不依赖原型的网络钩子、模型名单或CDP。

两条证据各自有边界：

1. **外部网页→本机**：你主动导出页面来源、类型、路径SHA-256摘要、时间，作为浏览器自报上下文；任何人都能伪造这类JSON，所以它不是身份认证。
2. **本机→外部工具连接→本机**：本机生成两分钟有效、一次使用的随机挑战；你让已有认证MCP会话调用confirm_connection回传。主机记录会话指纹，并返回主机身份与摘要hash供双方比对。

成功只说明**拥有该挑战的已认证MCP会话完成了一次回传**；不证明执行者的人类身份、网页来源的真实性或后台模型供应商/具体版本。挑战可被转发，因此也不是“不可转交”的浏览器证明。不会新增权限，不替代OAuth/配对或工具审批。

## 第一次使用（无需改Conda环境）

1. 更新本分支并按你原来的CMD流程重启本机工作台。启动后确认Bridge已连接外部AI。
2. 如果你已使用Tampermonkey/Violentmonkey，在它里面新建脚本，复制 **webagent-connection.user.js** 的全部内容保存。只启用这份最小脚本即可；不是要求运行原型README中的全量verify/CDP脚本。
3. 在你自己已登录的 `https://arena.ai` 页面刷新，右下角展开 **WebAgent 连接核对（非模型鉴定）**。
4. 点 **生成页面摘要（不发送）**。输出框会选中JSON，手动Ctrl+C复制。脚本不读取Cookie、localStorage、请求头或会话正文，不主动访问网络/本机地址，也不自动发送聊天。
5. 回到本机工作台，打开Bridge的 **主机身份与能力诊断**，把JSON粘贴到“页面最小摘要”，点 **创建两分钟一次性核对**。摘要超过十分钟会拒绝，请重新导出。
6. 结果中有 `toolRequest`。把这部分发给你当前外部AI，要求它真实调用MCP工具，而不是仅用文字说“匹配”：

   ```json
   {"name":"confirm_connection","arguments":{"challenge":"用本机刚生成的64位十六进制值替换"}}
   ```

   不发送登录令牌、Cookie、GitHub PAT或完整抓包。该挑战本身不能当MCP登录凭据使用；连接仍需原有认证和initialize会话。
7. 点本机 **查询核对结果**。状态应为 `echo-confirmed`；核对外部工具结果和本机结果里的 `checkId`、`observationHash`、`identity.hostInstanceId`。返回值会明确写 `modelIdentityVerified:false`、`permissionsChanged:false`。
8. 完成后点 **清除核对记录**。这不注销MCP，也不清除Bridge调用统计。

## 失败或中断时

- 未initialize、没有有效认证远程会话：拒绝；本机Chat不能冒充远程回传。
- 过期/重复使用：拒绝；不自动重放，明确创建新核对。
- 原始probe dump、额外token/model字段、错误网站、异常日期：拒绝；只接受最小格式。
- 最多同时保留八个核对，两分钟后失效并按访问清理；已成功的记录也到期回收。所有记录只在内存。
- 刷新页面会丢掉当前核对编号；重新创建即可，旧记录仍按TTL失效。主机重启会清空全部核对。
- 摘要中的页面路径只做SHA-256，不含查询参数/锚点或原始路径；**hash不是加密，低熵路径仍可能被猜到**。不愿分享时不要生成/导入。

## 原型为什么没有整套自动启用？

原型涉及网络拦截、登录/运行访问令牌、下载目录和运行轨迹查询，并有“真实模型”“零侵入”等较强承诺。请求的model字段、协议格式、行为相似度和服务端标签都是各自来源的声明/线索，不是独立的模型身份认证；格式相同的兼容网关尤其不能据此断定供应商。

本轮不把这些机制变成WebAgent授权依据，不去查询或披露本会话底层模型身份。保留原代码不等于审阅背书或建议直接运行；逐模块评估仍在队列中。

## 上传资料的隐私处理

静态检查在recon目录发现两个文件含JWT形态字符串；未使用、未验证时效，也未复述值。原始取证、下载的网站代码和Python缓存已移出后续Git跟踪并加入忽略，本地文件保留，不随产品打包。

**历史提交可能仍有这些内容，移出跟踪不能撤销凭据。**如果这些来自你的登录/运行会话且仍有效，应使用对应平台提供的退出/撤销方式处理；不要把实际令牌发到聊天里。本轮没有重写Git历史或替你撤销账户凭据。

更新提醒：当前工作区仅移出Git跟踪，原始recon仍留在本地；但你在其他克隆中拉取该删除提交时，Git可能删除原先跟踪的对应文件。若需保留自己的原始取证，请先在仓库外妥善备份，勿重新提交敏感数据。
