# 测试说明

## 结论（先看这个）

**做过测试。** 现行产品的测试在：

```
shuncode-core/agent-host/tests/
```

这就是专门的测试目录。**不必再在仓库根另建一个 `tests/`**：根目录再放一份只会和 `agent-host` 脱节。Node 项目惯例是测试跟它所测的包放在一起。

Windows 在仓库根运行：

```bat
run-tests.cmd
```

其它环境：

```bash
cd shuncode-core/agent-host
npm test
```

本次在本机实际跑过，`npm test` 全部通过（含新加的 HTTP 冒烟测试）。

---

## 测的是产品，不是演示计算器

| 位置 | 是不是 ShunCode 的测试 | 测什么 |
|---|---|---|
| **`shuncode-core/agent-host/tests/`** | **是** | MCP、补丁、工具、隧道解析、真起 HTTP 进程 |
| `workspace/tests/` | 否 | 演示项目 `calculator` 的单元测试，给 Agent 练手用 |
| `shuncode-repro/tests/` | 旧原型 | 第一代 Bridge 的补丁引擎；现行启动不跑它 |

---

## `agent-host/tests/` 里有什么

| 文件 | 类型 | 覆盖的实现 |
|---|---|---|
| `patchEngine.test.js` | 单测 | `apply_patch` 写入、哈希过期 `STALE_FILE`、SEARCH 冲突、grep |
| `mcpProtocol.test.js` | 单测 | `initialize.instructions`、`shuncode://` 资源、**正好 24 个工具**、危险命令要 `confirm_dangerous`、`remember`/`recall`、剪贴板那句 **原文** |
| `workspaceTools.test.js` | 单测 | `git_status`/`git_diff`、skills、删/改名、Ask 模式锁死写入、路径逃不出工作区、`start_command` |
| `tunnel.test.js` | 单测 | 从 cloudflared 日志里解析 `https://….trycloudflare.com`（**不**真的联网开隧道） |
| `httpSmoke.test.js` | 冒烟 | **真的 fork 一份 agent-host**：`/health`、工作台 HTML 含 ShunCode、错误密钥 401、MCP `initialize` / `tools/list` / `ping` |
| `codeServerNotRunnable.test.js` | 守卫 | 断言 `bin/code-server-dist` **缺 `out/`、`node_modules`、捆绑 node**，且 `run-shuncode.cmd` **不**启动它 |

没有 Jest / Mocha，每个文件是 `node tests/xxx.test.js`，失败 `process.exit(1)`。

---

## 有意不测的（避免假绿灯）

- 工作台点击、拖拽等浏览器 UI（没有 Playwright）
- 真 Cloudflare Quick Tunnel（需要本机 `cloudflared` 和外网）
- ChatGPT / Arena 网页客户端
- `bin/code-server-dist` 作为 IDE 启动（那份树 **不能运行**，见下一节）
- `shuncode-repro`（已冻结的原型）

这些要靠 [使用指南.md](./使用指南.md) 在 Windows 上按步骤点一遍。

---

## 要不要根目录 `tests/`？

**没必要。**

- 产品代码在 `shuncode-core/agent-host`，测试已经在它旁边的 `tests/`。
- 根目录 `run-tests.cmd` 只负责「找到 Node → npm install → npm test」，方便 Windows 双击。
- 若再复制一套到根 `tests/`，改工具时容易漏改。

---

## 和 code-server 的关系

`codeServerNotRunnable.test.js` 会失败，如果有人把 `bin/code-server-dist` 补成可启动、或把启动脚本改去调它。  
当前它 **不是** 第二种运行方式，所以没有、也不会写「如何用 code-server 跑本项目」的使用文档。理由见 [组件说明.md](./组件说明.md) 第 1.2 节。
