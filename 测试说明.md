# 测试说明：怎么运行、结果证明什么

本篇是现行测试入口，不再保留一张已经过时的“52个文件/没有Playwright”清单。完整测试文件和逐fixture说明见[测试目录](webagent-core/agent-host/tests/README.md)，精确提交/CI结果见[管理索引](manager/CONTEXT.md)。历史失败证据保留，不用旧绿灯代签新提交。

## 1. Windows新手运行顺序

使用**桌面VSCode集成CMD、原有Conda环境、系统Node.js/npm**。修改PATH后完全退出并重新打开VSCode，不只是新建终端。具体环境与日志保存见[Conda说明](Conda环境说明.md)。

在仓库根运行：

```bat
check-env.cmd
run-tests.cmd
```

run-tests.cmd先检查Node与后端express/Acorn入口，缺少时运行`npm ci --include=dev --no-audit --no-fund`；安装失败就停，不继续假测。随后执行npm test并保留失败退出码。依赖已存在不会每次重装；若怀疑依赖损坏，主动重装并保留原错误日志。

不要在根目录新建平行tests，也不要把源码测试当作安装载荷必备运行步骤。发布载荷可不含开发测试依赖。

## 2. 各平台显式命令

从源码仓库根执行（CMD、常规Shell均可）：

```text
npm ci --prefix webagent-core/agent-host --include=dev --no-audit --no-fund
npm test --prefix webagent-core/agent-host
```

直接npm test不会自动安装依赖；runner缺express/Acorn入口会先退出2并给安装指引。生产启动的omit=dev依赖集不够跑文档测试。存在package.json检查不等于依赖树完整性认证。

筛选定位某类问题：

```text
npm test --prefix webagent-core/agent-host -- --filter=oauth
```

filter是区分大小写的文件名子串，不是正则；多次设置以最后一次为准。筛选通过不代表全量通过。未知参数/空filter/无匹配/非法超时配置退出2；基线必需测试丢失退出1，不能通过filter绕过。

每文件默认120秒，WEBAGENT_TEST_TIMEOUT_MS接受1000–600000毫秒整数。每个测试用独立Node子进程顺序运行，一个失败后继续汇总；总失败退出1。超时按失败处理，不自动重试到绿，也不保证runner能清掉测试遗留的所有孙进程。详见[运行器](webagent-core/agent-host/scripts/运行器详解.md)。

## 3. 真实浏览器是独立测试入口

已有Playwright真实Chromium回归，不再列为“有意不测”。旧文“没有Playwright”是错误说明，删除的是旧说法，不是依赖。当前Node开发依赖为playwright 1.63.0；开发/浏览器回归需要保留，普通产品运行和Arena连接不要求安装它。它也不是自动开放给Arena的浏览器工具。它不属于自动扫描的`.test.js`文件集合。

```text
npm ci --prefix webagent-core/agent-host --include=dev --no-audit --no-fund
cd webagent-core/agent-host
npx playwright install chromium
npm run test:browser
```

Linux还可能需要系统依赖；CI使用`npx playwright install --with-deps chromium`。安装失败、缺系统依赖或网络ECONNRESET都不能记为浏览器通过；不要关闭TLS验证来凑成功。本会话此前沙箱浏览器下载失败时，真实Chromium证据来自对应CI，不伪称本地已运行。

浏览器回归包含经典工作台真实点击、MCP/本机HTTP与磁盘检查、审批、权限/模式、刷新恢复、草稿保存/回退、跨文件检查点等。并非每个测试都访问真实公网：公网登记UI有HTTP拦截夹具，TLS传输另用隔离服务。具体断言以workbench.browser.js及其详解为准。

## 4. CI到底跑了什么

以当前`.github/workflows/test.yml`为准：

| 任务 | 内容 | 不代表什么 |
|---|---|---|
| agent-host七组 | Ubuntu/Windows各Node20/22/24，加Ubuntu18兼容；清单检查、npm test | 不是code-server或模型供应商兼容矩阵；Node18只是最低声明兼容验证，不建议新装过期运行时 |
| Windows额外生命周期 | 各Windows任务额外5轮ptyLifecycle、2轮stdioMcp，失败即失败 | 不是重试直到通过或用户桌面PTY实测 |
| windows-installer | 实际载荷/manifest、Inno编译、C#/PowerShell检查、探针两类包构建验证 | 不是UAC安装/升级/卸载、真实鼠标键盘或账户验收 |
| workbench-browser | Ubuntu Node22，安装Chromium后运行独立浏览器脚本 | 不是桌面VSCode/code-server全部UI，也不是手机全部工具通过 |

生产依赖npm audit在CI中是非阻断公告检查；CI绿色不保证零漏洞。Action自身Node运行时警告与被测试主机Node矩阵不是同一件事。查看head SHA与各job conclusion，不能只看命令退出或另一提交的run。

## 5. 如何选择测试并理解证据

| 范围 | 主要入口示例 | 证据边界 |
|---|---|---|
| 文件/恢复 | patchEngine、apiFiles、fileCheckpoints、editorReview | 真实临时磁盘、版本与部分失败；editorReview注入VSCode API，不代签原生撤销栈 |
| MCP/认证/模式 | mcpProtocol、oauth、oauthClientAuth、mcpCancellation、executionControl | 真实模块/HTTP与受控夹具；不自动证明厂商当前连接器兼容 |
| 命令/PTY/stdio | workspaceTools、ptyLifecycle、stdioMcp | 进程/状态/取消回归；Windows实际结果看Windows矩阵，不由Linux代签 |
| 公网MCP | publicHttps、externalDiscovery、approvedOperations | 真实隔离TLS/HTTP及审批；测试路由覆盖不是公网供应商验收 |
| Chat/规划 | runChat、modelLifecycle、planRound | 明确模型配置和失败语义，模型替身不是实际供应商调用 |
| 文档/发行 | docsSite、documentationLinks、documentationLearning、installerPackaging | 结构、生成漂移、部分导航/函数关联、载荷；不是全仓语义正确率 |
| UI/扩展 | editorRuntime、webviewRuntime、workbenchRuntime及独立浏览器入口 | VM/DOM替身与真实Chromium必须分开记 |

表中是选择入口，不是全部文件或覆盖率分母。完整文件列表由测试目录自动发现/索引，逐文件详解保留正常、错误、清理与未覆盖范围，不在多个根文档复制巨大易过期表格。

## 6. 人工验收和失败处理

手机Arena连接已按用户实测通过；不要重新列成完全未验，也不扩大为特定OS/蜂窝/所有工具通过。11.3协作说明已关闭，不作为同一阻塞项重新打开。

仍需独立证据的包括：真实桌面VSCode/code-server窗口、原生diff/Ctrl+Z、安装升级卸载/UAC、DPI/焦点/剪贴板/OCR、实际公网供应商及探针账户流程。按[Windows线性验收](Windows新手逐步验收.md)和[人工清单](review/CHECKLIST_WINDOWS.md)执行；读过步骤不等于做过。

失败时保留提交号、命令、退出码和首个错误；先区分缺依赖、断言失败、超时、网络和实际产品问题。不要删除失败测试、扩大超时来掩盖问题、按旧PID盲杀进程或用reset --hard丢弃用户修改。模型失败后由用户选择后续，不自动重放修改。

可选计算器测试位于examples/calculator，不是产品回归；webagent-repro为冻结原型，不冒充现行测试。测试只提供其声明场景的证据，没有已测得的全仓行覆盖率或“所有功能100%通过”结论。
