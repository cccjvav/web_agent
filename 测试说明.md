# 测试说明

## 结论（先看这个）

**做过测试。** 现行产品的测试在：

```
shuncode-core/agent-host/tests/
```

这就是专门的测试目录。**不必再在仓库根另建一个 `tests/`**：根目录再放一份只会和 `agent-host` 脱节。Node 项目惯例是测试跟它所测的包放在一起。

Windows 在仓库根运行：

```bat
run-tests.cmd
```

其它环境：

```bash
cd shuncode-core/agent-host
npm test
```

`run-tests.cmd` / `npm test` 应全部通过。

---

## 测的是产品，不是演示计算器

| 位置 | 是不是 ShunCode 的测试 | 测什么 |
|---|---|---|
| **`shuncode-core/agent-host/tests/`** | **是** | MCP、补丁、工具、隧道解析、真起 HTTP 进程 |
| `workspace/tests/` | 否 | 演示项目 `calculator` 的单元测试，给 Agent 练手用 |
| `shuncode-repro/tests/` | 旧原型 | 第一代 Bridge 的补丁引擎；现行启动不跑它 |

---

## `agent-host/tests/` 里有什么

| 文件 | 类型 | 覆盖的实现 |
|---|---|---|
| `patchEngine.test.js` | 单测 | `apply_patch` 写入、哈希过期 `STALE_FILE`、读后可省略 hash、从未 read 的文件 `HASH_REQUIRED`+`currentHash`、SEARCH 冲突、grep |
| `mcpProtocol.test.js` | 单测 | `initialize.instructions`、`shuncode://` 资源、**正好 25 个工具**、危险命令（含 `git reset --hard`）要 `confirm_dangerous`、未知名含 `Available:`、`cat`/`path` 别名、`tools/call` 失败 `isError:true`、`remember`/`recall`、剪贴板那句 **原文**、多客户端配方 |
| `workspaceTools.test.js` | 单测 | 无仓 `available:false`、skills、`delete_file` 须 `confirm`、覆盖须 `confirm_overwrite`、Ask 锁、路径逃逸、敏感文件、`path`/`confirm:'true'`/`bash`/`ls`、`start_command` |
| `tunnel.test.js` | 单测 | 从 cloudflared 日志里解析 `https://….trycloudflare.com`（**不**真的联网开隧道） |
| `bridgeTunnel.test.js` | 单测 | stub `startQuickTunnel`/`stopTunnel`：cloudflare 启动后 mcpUrl 走 trycloudflare；无二进制仍 200 且走当前 Host；未登录 403。**不**真 spawn |
| `httpSmoke.test.js` | 冒烟 | **真的 fork 一份 agent-host**：`/health`、工作台 HTML 含 ShunCode / Add API、错误密钥 401、MCP `initialize` / `tools/list` / `ping`、内置 Ask Chat 不写死 calculator |
| `skipWorkbench.test.js` | 守卫 | `SHUNCODE_SKIP_WORKBENCH=1` 不占用工作台端口 |
| `runChat.test.js` | 单测 | 任意工作区搜-读-再测；不依赖 calculator.js；不调用 get_diagnostics |
| `chatMode.test.js` | 单测 | 原生 Chat `@shuncode` 默认 Agent（code）；`/ask` `/plan` 切换 |
| `profile.test.js` | 单测 | 环境偏好、技术栈探测与写入；指令里带 Skills 目录 |
| `oauth.test.js` | 单测 | OAuth 发现文档、401 `WWW-Authenticate`、PKCE、Bearer `/mcp`、SSE |
| `codeServerNotRunnable.test.js` | 守卫 | Git **没有** `code-server-dist`；`run-shuncode.cmd` 不启动 code-server；vscode 入口走 npm runtime |

没有 Jest / Mocha，每个文件是 `node tests/xxx.test.js`，失败 `process.exit(1)`。

---

## 有意不测的（避免假绿灯）

- 工作台点击、拖拽等浏览器 UI（没有 Playwright）
- 真 Cloudflare Quick Tunnel（需要本机 `cloudflared` 和外网）
- ChatGPT / Arena / DeepSeek++ 网页客户端（配方在 `clients.js` 单测里覆盖；真浏览器扩展不测）
- 完整 code-server 工作台点击（太大；用 `run-shuncode-vscode.cmd` 手工验）
- `shuncode-repro`（已冻结的原型）

这些要靠 [使用指南.md](./使用指南.md) 在 Windows 上按步骤点一遍。

---

## 要不要根目录 `tests/`？

**没必要。**

- 产品代码在 `shuncode-core/agent-host`，测试已经在它旁边的 `tests/`。
- 根目录 `run-tests.cmd` 只负责「找到 Node → npm install → npm test」，方便 Windows 双击。
- 若再复制一套到根 `tests/`，改工具时容易漏改。

---

## 和 code-server 的关系

网页 VS Code 走 `run-shuncode-vscode.cmd` → npm `code-server@4.135.0` → `bin/code-server-runtime/`。  
`codeServerNotRunnable.test.js` 会失败，如果有人再把残缺切片提交进 Git，或让 `run-shuncode.cmd` 去启动 code-server。步骤见 [网页VSCode使用指南.md](./网页VSCode使用指南.md)。
