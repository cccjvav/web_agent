# Web Agent Windows 使用指南

第三方客户端是否可连接取决于当前版本、协议和授权；本指南不认证其兼容性。探测施工已暂停待外部助手交接，现有功能保留，见[阶段8](manager/stages/s8-probe-integration.md)。存量探针实装范围见[入口矩阵](探针入口与实际可用范围.md)。


新手从零照做：[Windows逐步验收手册](docs/guides/Windows新手逐步验收.md)（VS Code集成CMD、逐条命令与可选MCP辅助）。


使用 Conda 的读者先看[Windows + Conda 环境专项说明](docs/guides/Conda环境说明.md)：不要求改用 venv，包含运行、维护、产品测试及解释器验收。实际人工验收按[当前清单](review/CHECKLIST_WINDOWS.md)，代码学习从[复盘指南](docs/development/代码复盘指南.md)进入。

这份指南只写 **Windows + CMD**（不要用 Git Bash / WSL 当主路径）。目标有两件：

1. 本机打开工作台，用右侧 Chat 改磁盘上的代码。
2. 启动 Bridge，让**具备兼容 MCP 工具通道并完成认证的客户端**操作本机仓库。普通聊天文本里的 URL 不会自行建立工具通道。

---

## 1. 需要事先安装的软件

全部装完后，**完全退出并重新打开桌面 VS Code，再新建集成 CMD**，使终端继承新的 PATH；只新建终端可能仍使用旧环境。

| 软件 | 是否必须 | 安装 |
|---|---|---|
| **Node.js LTS**（带 npm） | 必须 | https://nodejs.org/ 选 LTS，安装时勾选 npm |
| **Git for Windows** | 建议 | https://git-scm.com/download/win （`git_status` / `git_diff` 需要） |
| **cloudflared** | Cloudflare 隧道时必须 | 见下一节。本地 Chat 不需要 |
| **ngrok** | 只在工作台选 ngrok 时必须 | 见 [隧道使用指南.md](docs/guides/隧道使用指南.md) |
| 浏览器 | 必须 | Edge / Chrome 打开 http://127.0.0.1:3000 |

检查是否装好（在仓库根目录双击，或 CMD 里运行）：

```bat
check-env.cmd
```

应看到 Node.js、npm 的版本号。cloudflared / ngrok 一行若是「未安装」，本地 Chat 仍能用。选对应隧道时再装。

### 安装 cloudflared（给网页 Agent 用）

Quick Tunnel不需要Cloudflare账号；Named需要单独配置。在普通CMD尝试安装，仅安装器明确需要时再提升权限：

```bat
winget install --id Cloudflare.cloudflared
```

若winget不可用，从官方发布页下载适合Windows架构的程序，放入自己管理的工具目录；不要为此写入System32。显式路径配置、完整重启VSCode后的核对与提供商选择，统一按[隧道使用指南](docs/guides/隧道使用指南.md)第2节执行。check-env只看PATH，不能用它否定主机支持的显式EXE路径。

第一次启动 Node 时，Windows 防火墙可能弹出「是否允许访问网络」。先确认提示对应自己启动的 Node；回环页面打不开应先查服务、端口和错误日志，不要直接开放所有网络或关闭防火墙。

---

## 2. 拿到这份代码

GitHub 默认分支 `main` 可能仍是更早的原型。请以你检出的分支为准：根目录能看到 `run-webagent.cmd`，并且 `webagent-core\\agent-host` 是正在维护的程序。

若你已经用 Git 克隆了本仓库，CMD 进入根目录即可（能看到 `run-webagent.cmd` 的那一层）：

```bat
cd /d D:\code\web_agent
```

把 `D:\code\web_agent` 换成你实际的路径。

克隆下来以后，根目录里会看到 `webagent-repro\`。它 **不是** 你要运行的程序：

- **`webagent-repro`**：更早一版 Bridge 原型，启动脚本不会进这里。
- **你的实际主路径：** 桌面VSCode＋核心扩展，使用run-webagent.cmd启动主机和经典工作台。网页code-server是单独平台路径，不因为存在启动脚本就证明Windows本机可运行；先看[网页VSCode说明](docs/guides/网页VSCode使用指南.md)的支持边界。两种主机入口都可能占3000，不能同时启动。
- **网页里要真 VS Code：** 用 `run-webagent-vscode.cmd`（从 npm 下载完整 code-server 到 `bin\code-server-runtime\`）。步骤见 [网页VSCode使用指南.md](docs/guides/网页VSCode使用指南.md)。
- **本机已装的桌面 VS Code：** 双击 `install-vscode-extension.cmd` 侧载插件，再用 `run-webagent.cmd` 挂工作区。步骤见第 5 节。桌面VSCode窗口本身不占3000，但同启的经典工作台仍占3000。
- 你真正跑的是 **`webagent-core\agent-host`**，浏览器界面是 **`webagent-core\workbench`**。

整条工作流、为什么这样实现、以及源码根 `.webagent` 与安装版用户数据的区别，写在 **[组件说明.md](docs/development/组件说明.md)**。人话 + 比喻 + 文件落在哪，见 **[架构导读.md](docs/development/架构导读.md)**。对着源码逐步直译（函数拆步骤、不杜撰）见 **[技术实现.md](docs/development/技术实现.md)**。Skills 放哪、怎么被读到，见 **[技能使用指南.md](docs/guides/技能使用指南.md)**。

---

## 3. 第一次启动（源码仓库）

源码默认工作区为仓库根目录，计算器示例另放在`examples/calculator`。在**仓库根目录**：

```bat
run-webagent.cmd
```

也可以资源管理器里双击 `run-webagent.cmd`。

缺express依赖时自动执行 `npm ci --omit=dev`（不含文档校验所需Acorn；测试前需 `npm ci --include=dev`）。看到类似：

```text
UI        http://127.0.0.1:3000
MCP       http://127.0.0.1:48271/mcp/……
Workspace D:\code\web_agent
```

用浏览器打开：**http://127.0.0.1:3000**

这个黑色窗口要一直开着。停止：在该窗口按 **Ctrl+C**。

只使用本机 Chat 不必安装 cloudflared；默认操作本仓库，测试修改前先检查 Git 状态。

---

## 4. 指向你自己的仓库（重点）

源码默认改的是本项目根目录；要修改其他业务项目仍需显式指定其目录。

**做法 A（推荐）——启动时带路径：**

```bat
cd /d D:\code\web_agent
run-webagent.cmd D:\code\my-app
```

`D:\code\my-app` 必须已经存在。文件工具限制在工作区内；命令以该目录为初始 cwd，但进程仍有当前 OS 用户的权限，不是目录沙箱。

路径有空格时加引号：

```bat
run-webagent.cmd "D:\My Projects\foo"
```

**做法 B——环境变量：**

```bat
cd /d D:\code\web_agent
set WORKSPACE_ROOT=D:\code\my-app
run-webagent.cmd
```

窗口标题下会打印 `Workspace ...`，确认是你的仓库再开始改。

启动时尝试维护工作区`.webagent/.gitignore`与仓库本地排除文件`.git/info/exclude`（F63起**不再改写受版本控制的根`.gitignore`**），并尽可能限制配置文件权限；这是尽力保护，不是自动撤销已提交的秘密。当前规则除config/read-hashes外还覆盖usage、board、memory、自定义配置与指令/偏好/技术栈等用户状态；具体以实际生成内容为准，不再称为“只追加两行”。

源码根确有受版本管理的示例规则/Skill；新忽略规则不会让已跟踪文件自动消失。提交或备份前检查Git状态与内容，确认业务规则可公开；不要用强制add提交私人状态。已泄露的凭据应在来源撤销/轮换，删除文件或gitignore不清除历史。详细边界见[安全说明](SECURITY.md)。


---

## 5. 本机已安装的 VS Code（桌面版）

已经在 Windows 上装了微软商店 / 官网的 **VS Code 1.90+**，不必再开浏览器里的 code-server。引擎仍是 `run-webagent.cmd` 起的 agent-host（**:48271**）。桌面 VS Code **不占用 3000**，可以和自制工作台同时开。

打开的文件夹必须与启动窗口打印的 `Workspace` 一致；默认启动时就是 `web_agent` 源码根，显式指定其他项目时才打开那个项目。

### 5.1 侧载插件（拷文件夹，不要 vsix）

`code --install-extension` 要 `.vsix` 包。本仓库没有做打包，而是把 `webagent-core\extension` 拷到：

`%USERPROFILE%\.vscode\extensions\webagent.webagent-core-<版本>`

（有 VS Code Insiders 时也会拷一份到 `.vscode-insiders\extensions`。）

仓库根：

```bat
install-vscode-extension.cmd
```

或：

```bat
node webagent-core\scripts\install-desktop-extension.js
```

然后**完全退出 VS Code 再打开**（或命令面板 `Developer: Reload Window`）。活动栏应出现 **Web Agent**；Chat 里输入 `@webagent`。

桌面扩展host地址优先用VS Code设置`webagent.agentHostUrl`；该设置本身已有本机48271默认值，通常会先命中。自定端口优先改此设置；仅设置为空时才退到启动宿主前提供的WEBAGENT_AGENT_HOST_URL，再退默认地址。修改环境后要完整重启VS Code，不仅新建终端。

### 5.2 启动顺序

1. `run-webagent.cmd D:\code\my-app`（黑色窗口一直开着；看到 MCP :48271）
2. 桌面 VS Code：**文件 → 打开文件夹** = `D:\code\my-app`（和窗口里 `Workspace` 同一路径）
3. 状态栏应是「Web Agent」或「Bridge 运行中」。若写「未连接 48271」，引擎没起来。若写「工作区不一致」，打开的文件夹和引擎改的不是同一个目录。

网页 Agent 改磁盘仍走第 7 节 Bridge，和界面是工作台还是桌面 VS Code 无关。

### 5.3 和 `run-webagent-vscode.cmd` 的差别

| | 桌面 VS Code | `run-webagent-vscode.cmd` |
|---|---|---|
| 界面 | 本机已装的 VS Code | 浏览器里 code-server |
| 占 3000？ | 否 | 是 |
| 插件装哪 | `%USERPROFILE%\.vscode\extensions` | `webagent-core\extensions-installed` |
| 引擎 | 同一套 agent-host :48271 | 同一套（脚本会自己拉起） |

不要同时开 `run-webagent.cmd` 和 `run-webagent-vscode.cmd`（抢 3000）。桌面 VS Code 可以和 `run-webagent.cmd` 一起用。

---

## 6. 本机 Chat（不经过 ChatGPT）

1. 打开 http://127.0.0.1:3000
2. 右侧保持 **CHAT**
3. 输入框下 **Agent ▾** 默认就是 **Web Agent Code**（允许本机修改与执行，仍有工具模式/版本/危险命令确认等策略；Bridge四项权限不等价于本机Chat的沙箱）
   - Ask：普通项目文件只读、不执行任意命令；仍可按工具策略更新待办/记忆/协作等元数据。
   - Plan：普通项目文件只读、不执行任意命令，元数据规则同上。设置里打开「多模型博弈」后：选模型+思考发任务；换模型再空发 = 新分支；满额徽章如 `分支 3/4`；≥2 支可点「总结」。只有明确选择内置模型才使用本机草案/拼接；非内置配置缺失、模型失败会停止，不自动回退或重放，不把agreementRate当真实模型投票
   - Code / Agent：搜 → 读 → 补丁 → 再测
4. 输入任务，例如：`分析当前项目实现了什么功能`（对你挂载的任意仓库，不限于演示计算器）

Chat **不需要** cloudflared，也 **不需要** 登录 GitHub。

---

## 7. 让网页端 Agent 改本机仓库（Bridge）

**不绑死 ChatGPT，也不需要 Plus。** ChatGPT 聊天栏贴链接不行。先看你用哪一种客户端：

| 客户端 | 要 Plus？ | 要隧道？ | 怎么连 |
|---|---|---|---|
| **本机 Chat**（右侧 CHAT） | 否 | 否 | 直接说话，见第 6 节 |
| **Arena** 等具备 MCP 工具通道的客户端 | 依实际服务 | 云端请求需要公网地址 | 使用完整连接提示并检查真实工具调用 |
| **DeepSeek第三方候选** | 依实际服务 | 依请求位置 | 先核对版本、权限与认证；见[候选说明](docs/guides/网页DeepSeek使用指南.md)，未完成当前兼容性验收 |
| **Chat Plus第三方候选** | 依实际服务 | 依请求位置 | 先核对扩展协议、认证与站点；见[候选说明](docs/guides/网页ChatPlus使用指南.md)，不保证只填URL即可使用 |
| **ChatGPT 聊天栏** | — | — | **贴 MCP 链接不会登录、也不会调工具**（免费和更高级账号都这样）。不要把 URL 当第一句 |
| **支持 MCP/OAuth 的外部客户端** | 依实际服务 | 云端请求需要公网地址 | 按其当前连接器界面填规范 `/mcp`，使用 OAuth 配对；不保证特定菜单名或订阅条件 |

1. 当前是用 `run-webagent.cmd` 跑着服务。选 Cloudflare 隧道则已装 **cloudflared**；选 ngrok 则已装 **ngrok**。逐步见 [隧道使用指南.md](docs/guides/隧道使用指南.md)。
2. 浏览器打开工作台 → 左下角齿轮 → **智能体自定义设置** → 左侧 **Bridge**。
3. 在「怎么连到本机仓库」里点选客户端（默认 Arena）。
4. 隧道保持默认 **Cloudflare Quick Tunnel**，或改选 Named Tunnel / ngrok。三种逐步填法见 [隧道使用指南.md](docs/guides/隧道使用指南.md)（Windows CMD）。
5. 点 **启动 Bridge**。选中的那种会 `spawn` 对应程序，隧道默认等约 25 秒就绪（页面启动POST等待期限45秒，含此前停止清理）。启动中按钮变为“停止启动”，仍可请求停止。确认启动且当前地址核对后，手动复制 MCP 地址：

   `https://公网主机名/mcp/一串密钥`

   才给云上 Agent 用。本机没有二进制、缺 Token/Authtoken，或 25 秒没解析到域名时，HTTP仍可能返回200但success:false（未就绪；页面独立结果区显示未确认，不回显含凭据的原始错误），此时仅本机MCP端口可用，不是3000工作台替代公网；不要向云端发送localhost地址。缺字段**不会**偷偷改走 Quick Tunnel。
6. **Arena类**按实际客户端的MCP连接界面接入；如果支持连接提示词，可使用工作台“复制提示词”，内容例如：

   ```text
   https://xxxx.trycloudflare.com/mcp/……

   快速连接这个 MCP（URL），明确使用规则，熟悉可用工具，做好处理接下来一系列工作的准备。
   ```

7. **Arena类：** 确认当前会话真的出现WebAgent工具，先调用ping/workspace_info核对主机与工作区，再发任务。普通聊天中有URL不等于完成连接；最终在本项目根验收的步骤见[新手手册](docs/guides/Windows新手逐步验收.md)。
   **DeepSeek第三方候选：** 按[现行说明](docs/guides/网页DeepSeek使用指南.md)先做只读认证/工具调用核对；旧安装步骤不作为当前保证。
   **Chat Plus第三方候选：** 按[现行说明](docs/guides/网页ChatPlus使用指南.md)核对实际版本。是否注入规则、是否需要隧道取决于客户端，不以旧上游行为作保证。
8. 回到 Web Agent，右侧切到 **BRIDGE**，应能看到工具调用。磁盘上的文件会变。底部 **MCP session** 卡显示调用次数、平均耗时、失败、成功率；**Clear log**（清除本轮统计）会清除MCP展示/HTTP会话并重置本轮计数/摘要，同时清当前进程及持久读取hash缓存；原会话ID可能需重新initialize/重连，长期密钥/OAuth并未因此轮换；需要版本依据的后续修改应重新读取，不能因此跳过hash保护。它不清除磁盘任务板、记忆或已上报计划，也不停止命令。**Health** 把探活结果写在同一张卡里「Streamable HTTP · …」那一行（工作台 / Bridge / 隧道），不弹到页面底。下次工具调用或停 Bridge 后，那一行恢复成调用统计。网页 Agent 做完这件任务后点 **Stop Bridge**——一次任务挂几小时可以，不必通宵开着。标题栏右侧 **浅色 / 深色** 只换这页的颜色（记在浏览器里），不是安装包皮肤。Bridge计数/摘要每3秒从主机同步，WS只触发提前刷新；其他界面仍使用本机事件流：断线时会显示「事件流重连中」并自动再连（1 秒起、最长 30 秒再试），一般不用刷新页面；网页 Agent 改磁盘不走这条连接。

支持 MCP/OAuth 的客户端应使用其当前连接器配置界面，服务器 URL 填规范 `https://公网主机/mcp`，完成本机配对；不把普通聊天框当连接器。

文件工具会阻止敏感路径，例如 `.env`、SSH 与 `.webagent/config.json`；但命令可能读取这些内容，因此不能承诺它们绝不会经 MCP 返回。可在工作区根放 `.webagentignore` 追加规则。

### 每次重启都要注意

- **Quick Tunnel（以及未预留域名的 ngrok）每次「启动 Bridge」域名都可能变。** 必须重新复制提示词或 MCP 地址。Named Tunnel 和预留 ngrok 域名固定；Token / Authtoken 写在工作区 `.webagent\\config.json`（已 gitignore），不要发到聊天里。
- VS Code 里执行 `Web Agent: Reset MCP Secret`（或侧栏 Bridge 的 Reset）会先弹模态确认，再带工作区/主机与旧密钥比较后轮换；失败只提示结果未确认，不会自动重试。停止 Bridge 同样先核对主机绑定。
- MCP 密钥存在工作区 `.webagent\\config.json`，一般不会每次换。经典工作台点「重置 MCP 地址」需再次确认，成功会换密钥并写回该文件，旧凭据不再接受新请求；不会自动停止已经接受的任务或隧道。结果未知时旧显示地址可能过期，请先重新读取状态，不要重复重置。若显示轮换已确认但地址未核对，只刷新读取，不再轮换；重启后沿用已保存的新密钥。

### 不要做的事

不要把 `trycloudflare.com/mcp/...` 发到群里、Issue、截图网盘。拿到的人可以改你的文件、在你电脑上跑命令。

默认只听 **本机 127.0.0.1**。不要为了「手机连电脑」去设 `WEBAGENT_BIND=0.0.0.0`，除非你清楚局域网里谁都能碰到这些端口。

本机`/api`要求回环连接和明确的本机Host（localhost、127.0.0.1或[::1]），外站Origin/Referer请求会404。`/ws`也在握手时拒绝外站Origin。不要用陌生域名反向代理本机控制面；这不影响独立的MCP网页白名单。扩展后台能否调用 `/mcp` 仍取决于其实现、有效凭据及实际协议。若某个聊天站要从**页面自己**跨域打 MCP，再设 `WEBAGENT_CORS_ORIGINS`。CORS 不是门卡，URL 里的密钥仍要保管好。

隧道只把 **MCP 工单**转到 48271。带 Cloudflare 头的人打 `/api/status`、`/api/chat` 会看到「没有这个地址」（404），拿不到密钥。本机右侧 Chat 走的是 3000，不经过隧道。

---

## 8. 常用 CMD 一览

在仓库根目录打开 CMD（`cd /d` 到有 `run-webagent.cmd` 的目录）。

| 目的 | 命令 |
|---|---|
| 检查 Node / Git / cloudflared / ngrok | `check-env.cmd` |
| 跑产品测试 | `run-tests.cmd` |
| 安装 cloudflared | `winget install --id Cloudflare.cloudflared` |
| 安装 ngrok | `winget install Ngrok.Ngrok` |
| 启动（默认源码根工作区） | `run-webagent.cmd` |
| 启动网页 VS Code | `run-webagent-vscode.cmd` 或带仓库路径 |
| 侧载插件到本机桌面 VS Code | `install-vscode-extension.cmd` |
| 启动并挂你的仓库 | `run-webagent.cmd D:\code\my-app` |
| 同上（环境变量） | `set WORKSPACE_ROOT=D:\code\my-app` 然后 `run-webagent.cmd` |
| 指定 cloudflared 路径 | `set CLOUDFLARED_PATH=C:\工具\cloudflared.exe` 然后照常启动 |
| 指定 ngrok 路径 / Authtoken | `set NGROK_PATH=C:\工具\ngrok.exe`；`set NGROK_AUTHTOKEN=…` |
| 停止 | 启动窗口里 **Ctrl+C** |
| 换端口（少用） | `set WORKBENCH_PORT=3000` 与 `set AGENT_HOST_PORT=48271` |
| 手机连接 | 使用认证 Bridge 公网 MCP，不开放远程工作台 UI |
| 额外允许跨域打 MCP 的网页 | `set WEBAGENT_CORS_ORIGINS=https://某站.com`（逗号分隔）。扩展 `chrome-extension://` 已默认放行 |
| 网页 VS Code 自定登录口令 | `set CODE_SERVER_PASSWORD=…` 然后 `run-webagent-vscode.cmd` |
| 网页 VS Code 不要登录页 | `set CODE_SERVER_AUTH=none`（只应本机） |

没有 Linux 的 `./run-webagent.sh` 步骤；Windows 请只用上面的 `.cmd`。

---

## 9. 界面里和仓库有关的设置

经典工作台只保留轻量文件编辑、搜索、Chat、模型配置和Bridge；完整IDE功能由桌面VS Code扩展承担。未实现的IDE仿制菜单、附件、浏览器前进后退、仿站登录/发送入口已移除；挂钩/外部MCP地址/插件清单移入“兼容资料（不执行）”，不删除原数据。外部客户端页仅提供真实网站链接与连接说明，不替你登录或提交任务。

完整操作与界面区别见 [内置探索Agent使用指南](docs/guides/内置探索Agent使用指南.md)；近期已落地变化见 [借鉴优化说明（新手版）](docs/development/借鉴优化说明（新手版）.md)。

聊天模型按钮会列出**内置探索 Agent，无需API Key**；选择Ask可扫描/读取当前工作区，展开Read工具卡核对正文。它是确定性本机探索器，不是通用语言模型；外部API仅在需要真正的大模型推理时配置。菜单按视口空间上下展开、长列表内部滚动，Escape或窗口resize关闭。 切换模型须等主机确认；等待时仍显示/使用最近确认的模型，不因一次点击就宣布切换成功。状态刷新失败会保留旧快照并在状态栏提示；若已提示设置保存成功但刷新失败，先核对主机，不要重复保存。

- **环境偏好**（自定义设置 → 环境偏好）：操作系统、Shell、回复语言、提交说明。写入 `.webagent\preference.md`。Windows 上会提示 Agent 用 PowerShell，不要假设 bash。
- **技术栈申明**（自定义设置 → 技术栈）：语言、框架、包管理、测试命令。写入 `.webagent\tech-stack.md`。可点「从工作区探测」。Code 模式跑测试时优先用这里填的命令。
- **指令**（自定义设置 → 指令）：写入工作区 `.webagent\instructions.md`。本地 Chat 和 Bridge 的 `initialize.instructions` 都会带上，例如「提交说明用中文」。第三方客户端是否转交 initialize.instructions 需实际验证；不能仅凭复制 URL 假定模型已收到规则。
- **技能引导**（自定义设置 → 技能）：逐步见 [技能使用指南.md](docs/guides/技能使用指南.md)。Skills 就是当前工作区 `.webagent\skills\名字\SKILL.md`。按「名称 → 何时使用 → 步骤」三步建，或点模板。对话里说「按 fix-tests 做」，或让模型 `load_skill`。源码根已有fix-tests、review、docs-sync、commit-now、evidence-check等示例。挂你自己的仓库时不会自动带上，需要拷过去或在设置里新建。普通Skill不能随意读工作区外路径；三个固定bundled Skill是明确例外。新建同名Skill会拒绝，不自动覆盖。
- **API Provider**：本地 Chat 要接真模型时再填 Endpoint + Key，Test只读取模型列表，不证明Chat/工具/视觉兼容；Add API仅追加，不覆盖旧模型/Key、不改当前模型，添加后请明确选择。重复的Endpoint/modelId会拒绝，不能用Add更新旧密钥。填写手动模型ID后再点Add，只登记该ID并跳过发现；发现失败不会自动保存。Endpoint不接受URL内凭据、查询参数或片段，超出100个模型请用手动ID选择所需模型。和 Bridge、cloudflared、ChatGPT 登录无关。可明确选择内置探索Agent；这不意味着配置失效的外部模型会自动改用内置。本机内置流程仍受工具模式、版本与危险命令等策略约束；Read/Edit/Execute/Capture是Bridge远端工具权限，不等于对本机Chat或任意程序的OS隔离。
- **本机授权**（自定义设置 → Bridge）：按钮写「本机演示授权」，**不是**「使用 GitHub 登录」。默认已经打开。关掉后启动 Bridge 会 403，Chat 仍可用。可选「GitHub 验证 / 验证令牌」只向 GitHub 问用户名，令牌**不会**写入 `config.json`。设备码需要本机环境变量 `WEBAGENT_GITHUB_CLIENT_ID`。使用统计后台是另一进程：`run-admin.cmd`（默认 4174），主窗口不会自动打开。
- **多模型博弈（Plan）**：设置页可开启用、选合并主模型（已配置列表或当前对话模型）、合并时只读验证、每回合最大分支（默认 4，夹到 2–8）。Plan 输入框发任务开第一支；不重输任务只换模型再发 = 新分支；不必跑满即可总结。选择内置模型时可生成本机草案；非builtin分支缺配置会停止。总结模型不存在或非builtin配置不足也会停止；仅明确选择内置模型才本机拼接；不把agreementRate冒充真实共识百分比。
- 工作区根目录就是你传入的 `WORKSPACE_ROOT`，文件工具不能写到这个目录外面；命令、stdio 程序与桌面操作不受等价的 OS 沙箱限制。

---

## 10. 故障排除

**CMD 提示找不到 Node.js**  
核对安装与实际PATH；VSCode用户完整退出并重开父进程，再新建集成CMD。用check-env和Conda环境说明核对，不急着重复安装。

**端口 3000 / 48271 已被占用**  
先核对占用者与所属工作区；仅在确认是本次管理的实例时正常停止，或为新实例选其它端口，不按端口/进程名批量终止未知程序。

**侧栏没有 Web Agent / Chat 里没有 @webagent**  
先跑 `install-vscode-extension.cmd`，完全退出 VS Code 再开。需要 1.90 或更新。不要用 `code --install-extension` 指文件夹（那条命令要 vsix）。

**状态栏写「未连接 48271」**  
`run-webagent.cmd`（或 `run-webagent-vscode.cmd`）没在跑。桌面 VS Code 自己不会起引擎。

**状态栏写「工作区不一致」**  
VS Code 打开的文件夹必须等于黑色窗口里的 `Workspace`。不要打开 `web_agent` 源码仓去改 `D:\code\my-app`。

**浏览器打不开 127.0.0.1:3000**  
看防火墙是否拦截了 Node；启动窗口是否还在跑、有没有报错。

**启动 Bridge 报未找到 cloudflared / ngrok**  
按[隧道使用指南](docs/guides/隧道使用指南.md)安装，修改PATH后完整重开VSCode再新建集成CMD。Windows 上故意不把 `localhost` 交给云上 Agent。

**cloudflared / ngrok 启动了但 25 秒没有公网地址**  
先看具体tunnelError和程序状态。Named注册日志不验证Public Hostname是否正确，映射错可能显示就绪却无法连接；缺ngrok Authtoken会直接拒绝，不一定等25秒。失败只保留本机MCP入口，不自动切换提供商。逐项按隧道指南排障，不能只凭日志就绪代签公网请求。

**网页 Agent 没改到文件**  
- 地址必须是 `https://` 公网门牌（trycloudflare / Named 域名 / ngrok），不是 localhost。点了「启动 Bridge」若仍是 `127.0.0.1`：本机没有对应二进制、PATH 不对，缺 Token/Authtoken，或 25 秒没解析到域名。看 toast / `check-env.cmd`，必要时设 `CLOUDFLARED_PATH` / `NGROK_PATH`。  
- 该聊天栏必须真能调 MCP/工具。ChatGPT **聊天栏贴链接不行**（任何档位）；要对接着设置里的自制 MCP 插件（服务器 URL + OAuth），或本机 Chat、Arena、DeepSeek++、Chat Plus。  
- 以客户端当前MCP接入界面、认证和真实工具列表为准。第三方候选未经当前兼容性认证，不照搬旧菜单或假设粘贴即可连接。
- 重启Bridge后重新读取并核对当前地址；Quick/非预留ngrok地址可能变化，Named或预留域名未必变化。只有经过验证的客户端才更新其连接配置。
- 确认 `Workspace` 打印的是你以为的那个仓库。  
- 工具回了 `HASH_REQUIRED` / `STALE_FILE`：不要把错误里的 `currentHash` 直接套到旧补丁重试；先重读并核对改动意图，解决冲突后重新生成操作，不自动重放。这是工具结果，不是 MCP 掉线。
- 工具回了 `matched N times`：SEARCH 在文件里出现了不止一次，默认不改。把 SEARCH 写得更独特，或带 `occurrence`（从 1 数第几处）。Windows 文件的换行不会被补丁改成 LF。  
- 网页端若调用 `bash` / `cat` / `ls`：主机会收成 `run_command` / `read_files` / `list_directory`。命令字符串在 Windows 上仍走 PowerShell。

**git_status 显示没有 git**  
不是 git 仓库、或没装 Git 时，工具返回 `available: false`，不抛失败。要看分支请安装 Git for Windows，并只在你确实要版本库时 `git init`。

**git_diff与终端显示不同**
产品只读Git工具禁用外部diff/textconv/fsmonitor及自定义filter，文件路径按字面值处理，*.txt不是批量匹配。LFS或其它转换驱动的差异可能与终端不同；遇到预算错误不自动改走任意命令。git_status现在保留中文路径、重命名的原/目标路径；有换行的文件名不会拆成多个状态项。不带路径的 git_diff 不再是"整仓差异"：它会逐个路径套用与单文件相同的敏感规则，`.env` 一类文件即使早已被 Git 跟踪也不会经由整体差异回流，被排除的路径名会列在 `excludedSensitivePaths`。若把工作区设在某个大仓库的子目录，status/diff 只报告该子目录内的改动。但这只是规则一致化，**不是脱敏导出**：规则没覆盖到的文件里若写了密钥，差异照样会返回，公开前仍要自己过目。

**文件编码或差异超预算**
文本读取与已有文件的写前检查只接受合法 UTF-8，并**保留 BOM 和 CRLF**：解码出的文本重新编码后必须与磁盘字节一致，hash 才能真正代表那个文件。`E_ENCODING` 表示拒绝损坏或其它编码，不会自动改码。需要交给 `JSON.parse` 的配置读取会单独剥掉前导 BOM，所以用记事本或 PowerShell 存过的配置仍能正常加载。
差异展示另有预算：每侧 1MiB／20000 行、算法 1500ms／20000 次编辑、输出 256KiB，超出报 `E_DIFF_BUDGET`，**发生在写入之前**，文件保持原样。请缩小改动范围重新审阅，或改用本机编辑器；不要为了绕过它而移除 hash 或自动重放写入。普通 `read_files`/`write_file` 仍是 8MiB 合同，检查点另有更小限制。
**npm test / 命令在 Windows 上的行为**  
本机命令走 PowerShell。在 Chat 里下发的应是 Windows 能跑的命令（例如 `npm test`）。工具名可以叫 `bash`，执行器仍是 PowerShell。

---

## 11. 源码运行与安装版

仓库已有 Windows 安装器，但程序仍由界面壳与 agent-host 组成，不是重做一套 VS Code 内核；布局和卸载保留策略见 [安装器说明](installer/README.md)。对「网页 Agent 改本机仓库」这条主路径：Windows 上用 `run-webagent.cmd` + cloudflared 即可。

已经装了桌面 VS Code：侧载插件见第 5 节（`install-vscode-extension.cmd`），引擎仍是 `run-webagent.cmd`。

Git 不再内嵌 code-server。网页里要真 VS Code，用 `run-webagent-vscode.cmd`（npm 包装到 `bin\code-server-runtime\`）。

可选编辑器启动时，下载/依赖准备各有120或180秒预算；旧后端依赖补装现在也限120秒，不计入之后15秒健康检查。取消/超时不会继续启动编辑器，也不自动重装或回滚部分node_modules；提示“直接子进程退出未确认”时请在本机核对，不凭旧PID或进程名批量终止。该边界是Node启动编排，不概括所有手写Shell或全部后代进程。

---

## 12. 怎么确认实现被测试过

仓库根：

```bat
run-tests.cmd
```

看运行器最终汇总和退出码，不以几行历史PASS文案作为全量通过。文件数以当前结果为准；普通npm test与Playwright的test:browser分开执行，浏览器包/Chromium安装和CI证据见[测试说明](docs/development/测试说明.md)。

测试代码在 `webagent-core\agent-host\tests\`，**根目录没有、也不需要再复制一个 `tests\`。** 细节见 [测试说明.md](docs/development/测试说明.md)。

## 工作台编辑器保存与冲突

文件标签后的圆点表示未保存修改。切换文件会保留编辑，关闭未保存文件需确认；保存中请等待完成。刷新/关闭页面会请求浏览器提示，但强制退出或崩溃不保证恢复，请及时保存。

保存会核对打开时的文件版本。如果磁盘已被外部编辑器或Agent修改，工作台拒绝覆盖并保留当前编辑。请先复制保留当前内容，核对磁盘版本后再重新打开、合并和保存；目前没有自动合并或强制覆盖按钮。网络/服务器错误不会显示“已保存”。

手机等设备上的兼容MCP客户端可按Bridge路线使用公网MCP地址和有效授权连接本机；需要电脑及隧道持续在线。这不等于远程访问工作台UI，后者仍仅本机开放。2026-09-16用户已实测手机浏览器登录Arena后，用本产品MCP链接成功连接，报告用法与电脑浏览器相同；手机连接项已通过。未提供具体OS/浏览器/网络条件，其他工具及生命周期项目不因此全部通过。

## 安装版可写数据（2026-09-11）

Windows安装版首次启动将校验过的运行时代码准备到LocalAppData/WebAgent/releases，再安装依赖；不向Program Files写入。默认工作区、code-server设置及管理统计分别在LocalAppData/WebAgent的workspace、code-server、admin下，升级/卸载保留。旧版安装目录里的工作区不自动迁移，请先备份、复制到用户可写目录并显式指定新工作区。不要把未提交的用户数据放在Program Files中。源码checkout现在默认仓库根目录，不使用根workspace子目录。

### 帮助与Bridge工具计数（0.7.0）
顶部“帮助”和欢迎首卡打开使用帮助。Bridge栏每3秒读取本机主机的累计工具完成数与最近100条摘要，即使WS断开也可同步；网页刷新不清零，主机重启/清除本轮会清零。不统计本地Chat、其他MCP、初始化或仅列工具。若没有增长，核对真实工具调用及host/工作区，而不是只看外部AI的文字声明。当前发行副本为0.7.2（含工作区绑定与Bridge Tasks）；VS Code和安装器必需的合法版本字段仍保留，原始审计日志中的历史版本不改写。

### Skills渐进加载与只读查看
技能页现可搜索来源ID/用途、查看正文、hash绑定续页以及只读资源。填入Ask不发送；只有完整读取的小型workflow.json能转到现有工作流预览，提交和本机批准仍独立。说明型Skill不能让内置固定探索器自动变成大模型，也不授予脚本/依赖安装权限。完整例子、参数和复制步骤见技能使用指南.md。

### 按需检索已保存记忆
已连接的模型或外部AI可调用recall，参数例如`{"query":"中文验收 Windows","limit":10}`（工具参数，不是在CMD执行）。只搜索当前工作区已有日记，按字面词匹配并附文件行号；不自动记笔记、不联网计算向量。看到truncated/warnings代表扫描不完整，可以指定day缩小到某天；无匹配不证明所有历史都没有。记忆可能过期或写错，做决定前仍需核对当前文件/真实工具。

外部MCP接入现支持最多10页/100工具的有界发现，合计目录256KiB，整个登记流程30秒；超限或错误目录明确失败，不只悄悄显示第一页。HTTP(S)方式默认限回环；公网仅支持另行明确启用的HTTPS（见下文“公网出站MCP”）。另支持下节经本机明确审阅的stdio启动，三种方式的每次工具调用均需本机批准。

### 工作流：检查批准前文件有没有变化
在“工具接入与审批”中，步骤可加`before`，在该步骤执行前检查；`expect`仍是执行后的核验。比如只在目标不存在时创建：

```json
{"steps":[{"id":"create","tool":"write_file","arguments":{"filePath":"review-note.txt","content":"已审阅"},"before":{"path":"review-note.txt","exists":false}}]}
```

预览不读写文件；提交后仍需另行批准。如果别的程序在批准前创建了目标，步骤会以E_PRECONDITION停止，不写目标、不运行后续步骤；不要因为失败就换key自动重试。已有文件修改应先读取hash，同时在before.sha256和工具arguments.expectedHash中使用所审阅的hash。

writeChecks会提示哪些写步骤没有显式保护，但不自动补条件。条件可能检查另一文件，应核对路径；contains只是内容检查，不是版本保护。前置检查不是文件锁/事务，不能消除所有并发竞态；前面已经执行的步骤不会自动撤销。

切换请求、预览新草稿或提交新审批时，旧批准按钮立即失效，必须重新读取并审阅完整参数。提交在途不能连点创建第二条；响应丢失请先刷新列表核对，不要换key再提交。批准/停止响应未确认时查询同一requestId；如批准仍在途，可从列表重新读取该请求后请求停止。unknown可能已经写入，应先核对文件；取消只阻止后步，不撤回已完成效果，cancelRequested也不等于已经停止。

检查点与审批列表分别刷新，一侧失败不代表另一侧没有请求。检查点恢复必须先取得当前ID的完整差异；工作区绑定变化应重新审阅。恢复响应缺失或成功标记与逐文件状态矛盾时，只能视为未确认，先查询原检查点记录和实际文件，不要再次发送恢复。

创建检查点时，按钮在本次请求及列表核对完成前禁用，防止连点产生多条备份；等待期间修改的路径草稿会保留。显示“创建未确认”时，先刷新原工作区列表核对，不要当成失败重建。若创建已确认、只是列表刷新失败，保留的ID仍是核对入口；再次明确确认创建会产生新的时间点快照，不是重试原请求。

HTTP外部工具登记在途不能重复提交，但仍可从列表移除正在连接的接入。同一ID移除在途不能重复发送；未知结果先刷新列表核对，不重放旧按钮。登记/移除的独立结果区不随列表统计清空：移除登记不表示外部HTTP服务停止，stdio显示“已请求停止”也不表示已经退出。登记失败提示不回显响应异常中的Token；只清本次实际发送的旧Token，保留后写的新草稿。

### stdio MCP：一次完成启动和调用的正确顺序

只有取得完整且未过期的启动预览，确认按钮才可用。编辑配置或切换绑定后必须重新预览，环境值发送后会从输入框清除，新的预览需重新填写所需env。在途不能重复预览或启动；编辑草稿不停止已发起进程。若启动结果未确认，先查询接入列表/实际进程，不要重新预览后盲目重启；确认启动但列表刷新失败时保留的ID仍可核对。


此方式会启动你指定的本机程序，**不是OS沙箱**；程序本身有当前系统用户的文件/网络权限，也可能访问本机管理API。不要启动不可信包。我们的主机不自动运行npx/uvx安装、不自动重启或重放；不保证你选择的程序自己不会安装东西。

1. **准备已安装并审阅的服务器**。先看它的来源、版本、许可证、启动参数及依赖。不要把“解释器叫node/python”当安全证明。
2. 在桌面VS Code集成**CMD**执行`where node`；Python服务器则先用原来的Conda环境，再执行`where python`。这里只查路径，不启动MCP服务器；不要把Conda命令或完整Shell字符串填进program。
3. 打开本机工作台 → Bridge → 工具接入与审批 → stdio MCP。以Node服务为例填JSON（替换为你机器的实际路径，server.js须是已审阅的真实服务器，不是随意新建一个空文件）：

```json
{
  "name": "Reviewed local MCP",
  "program": "C:\\Program Files\\nodejs\\node.exe",
  "args": ["server.js"],
  "cwd": ".",
  "reviewFiles": ["server.js"]
}
```

Python可把program改成`where python`得到的绝对python.exe路径，args改为`["-u","server.py"]`，reviewFiles改为`["server.py"]`。本机若需要UTF8环境可显式传`"env":{"PYTHONUTF8":"1"}`；需要API密钥也只能按该服务器文档显式提供所需env，不要放在args里。客户端不自行激活Conda、不安装Python包。

4. 点**只预览启动配置**：核对解析后的真实程序路径、参数、工作目录、程序hash、显式文件hash与env键名。此时不启动进程。可选env的值发送后从输入框清除，重新预览需重填；不会保存到磁盘/localStorage。reviewFiles只保护列出的文件，不覆盖导入的依赖。
5. 确认可信后点**确认并启动**，再次确认系统权限提示。预览两分钟有效、只能用一次；程序/显式文件变化后拒绝。启动本身就可能有副作用，即使尚未调用工具。
6. 看到discovered才表示初始化与工具发现完成。外部AI用external_servers查serverId，再经认证MCP会话提交external_request。你在本机核对完整参数并批准后，才发送该工具调用。**启动确认不等于所有工具永久获准。**
7. 完成后点**移除此接入**。超时/取消会停止整个stdio进程，其他在途调用也失败；unknown表示可能已有副作用，先核查，不要换requestKey自动重试。停止后重新启动需重新预览、确认。

Windows要求.exe，拒绝直接.cmd/.bat及常见Shell/包管理器启动器；包装器的C#编译或Job Object失败就拒绝，不裸跑。POSIX进程组能回收普通子孙，但恶意自行脱离进程组的程序不在此保证内。累计输出8MiB、stderr1MiB、4096帧或单帧256KiB上限到达会停止，长期使用要安排明确的人工重审启动；不隐藏失败。

开发者回归命令：`npm test --prefix webagent-core/agent-host -- --filter=stdioMcp`。测试运行我们自己的隔离夹具，不是下载第三方服务器，也不替代你的具体第三方包验收。

### Bridge刷新后计数异常
只刷新页面不应清除本轮计数。未取得服务端快照显示“—”，同步失败保留错误说明而非假装归零；可点“重新同步统计（不清零）”。主机ID/启动时间/本轮起点用来区分页面刷新、主机重启和操作者清除。具体CMD复验和两种统计区别见 [Bridge统计与刷新排查](docs/guides/Bridge统计与刷新排查.md)。

### 可选诊断：核对已认证MCP会话

探测专项暂停待外部交接；以下仅保留存量版本操作，不是新整合项目的安装或验收说明，本轮不改造探测代码。

这不是接入Arena的必经步骤，也不是模型鉴定。手机Arena已经能调用Bridge时，不需要为了正常使用再安装诊断脚本。只有需要核对“这次外部工具回传是否来自我连接的主机”时，才使用以下流程；主机须处于Bridge模式，不能由本机Chat代替远程回传。

1. 若浏览器已安装Tampermonkey/Violentmonkey，可新建脚本并复制[webagent-connection.user.js](arena-model-probe/webagent-connection.user.js)全文保存。在自己已登录的 `https://arena.ai` 刷新，展开右下角“WebAgent 连接核对（非模型鉴定）”。这份脚本独立于完整浏览器探针，不用运行原型main、CDP或抓包脚本。
2. 点“生成页面摘要（不发送）”，手动复制输出JSON。脚本只在点击后输出schema、origin、observedAt、pageKind、pageDigest五字段；不读取Cookie/localStorage、网络响应或会话正文，不主动联网、不发送聊天。路径只取pathname的SHA-256，不含查询参数/锚点；**hash不是加密，低熵路径仍可能被猜到**。
3. 回到经典工作台Bridge区域，点“核对主机与能力”，粘贴到“页面最小摘要”，点“创建两分钟一次性核对”。输入限2KiB，摘要超过十分钟或时间超前超过30秒会拒绝；不要粘贴原始dump、模型观测或登录令牌。
4. 将结果中的toolRequest交给**已有认证且initialize过的外部MCP客户端**，要求真实调用，不是用文字回答“匹配”：

   ```json
   {"name":"confirm_connection","arguments":{"challenge":"替换为刚生成的64位十六进制值"}}
   ```

   挑战不是MCP登录凭据；不需要发送Cookie、PAT、配对令牌或抓包。挑战可转交，拿到它的其他已认证会话也可能先回传，不能当作浏览器不可伪造证明。
5. 点“查询核对结果”，检查status为echo-confirmed，比较外部工具与本机显示的checkId、observationHash、identity.hostInstanceId。结果明确modelIdentityVerified:false、permissionsChanged:false；只证明已认证会话完成一次挑战回传，不证明人类账户、页面真实性或后台模型。
6. 完成后点“清除核对记录”：清除主机上的全部核对记录，不注销MCP、不清除Bridge统计。最多保留八项，成功或未成功记录都在两分钟到期后按访问清理；主机重启丢失，页面刷新丢失当前编号。超时/失败不自动重放，需要时明确重建，不据旧成功判断新请求。

桌面VSCode也可用独立Probe Companion的“导入最小页面摘要”→“复制一次性核对请求”→外部MCP调用→“查询核对结果”；详见[Companion当前指南](webagent-core/probe-extension/README.md)。它不自我确认，复制需明确批准且只能复制一次；“丢弃当前核对记录”仅清扩展当前核对/分析显示，**不删除主机记录或已保存历史**，主机记录仍按TTL回收。此入口不等于实时浏览器配对，更不代表code-server支持Companion。

连接摘要、模型观测、实时配对凭据是不同格式和用途。完整参考能力、实时连接与历史操作见[探针入口与实际可用范围](探针入口与实际可用范围.md)，不能再把早期“仅连接核对”当产品当前范围。验证覆盖connectionCheck、probeCompanion及浏览器隔离回归；不等于用户真实账号或模型身份认证。

## 新增：互斥工作模式与Bridge权限

手机Arena固定走Bridge；本机Chat与Bridge不能同时执行，同类型仍可多模型/多Agent并行。Bridge卡片的主人权限可保存Read/Edit/Execute/Capture并明确切换主机模式；这些权限约束远端Bridge工具，不是本机Chat/编辑器或任意程序的OS沙箱。旧配置默认全开，不是自动只读；Execute要求四项全开，不是OS沙箱。先结束任务/后台程序、停止隧道再切Chat。详细按钮、撤权和失败处理见[Bridge权限与工作模式](docs/guides/Bridge权限与工作模式.md)。新控件实机验收单列，不改写此前手机连接通过证据。

## 经典工作台：预览当前文件修改

在文件树打开已有文本文件并编辑，选择顶部“文件”→“预览当前文件修改”。diff页显示磁盘版本与本次草稿的差异，**此时没有保存**。核对后点“确认保存这份预览”；只保存你刚审阅的快照，保存时再次核对磁盘hash。普通Ctrl+S仍是直接保存，不强制预览。

预览过程中换标签/编辑、预览后改稿或关闭源文件，会要求重新预览；磁盘被别的Agent或程序修改则拒绝覆盖，保留草稿供人工协调。超时/丢失保存响应不要盲目重放，先检查磁盘；确认按钮一次消费。每份文本预览限64KiB/2000行，diff限256KiB并有计算预算，超限停止预览，可缩小修改或使用桌面编辑器。

这是经典工作台已有文本文件入口，不是多文件事务、受保护回退或新建文件预览。VS Code原生编辑器自带的比较/保存是另一条路径，不把本入口宣称为原生集成。

### 受保护回退：只撤销本页面记录的一次保存

在经典工作台打开已有文件，带版本号保存后，如需撤销这一次保存：回到该文件标签，选择“文件”→“预览回退本次保存”，核对diff后点“确认回退这次保存”。预览不会写盘，确认后仍检查磁盘hash；别人改了文件会拒绝，不提供强制覆盖。有未保存草稿时先自行保留并处理，不能借回退丢弃草稿；请求期间产生的新草稿也会保留。

记录仅在主机内存保留15分钟、最多16项，旧正文≤64KiB；双方预览≤2000行，diff≤256KiB。满额淘汰最早项，到期按访问清理。只记录有正确版本号的已有文件且正文确有变化、两份都在预算内的已验证保存；新建/超大/无变化保存可能成功但无回退句柄。页面刷新、关闭标签或重启主机后不能依赖此入口找回记录；**它不是持久备份**，重要修改请使用Git/备份。

确认请求发起后句柄一次消费；响应未知先检查磁盘，不重复提交。不提供redo，不回退任意Agent补丁、命令、删除、改名或多文件事务。只恢复正文，不恢复权限/时间戳；hash只比较当下内容，不检测曾改动后又恢复相同内容的历史。

## 公网出站MCP：不是手机连接本机的那条链路

### 先分清谁是client、谁是server

你现有的理解是对的：Arena等外部AI使用本机提供的MCP连接地址，本机WebAgent提供工具，是server；Arena的MCP连接端是client。通常由平台后端连接，不据此认定手机浏览器直接发出所有MCP请求。链接是服务器地址/认证入口，真正让WebAgent成为server的是它运行了接收请求并提供工具的服务，不是“生成一个链接”这个动作本身。

```text
现有必需链路（对本机而言：入站）
Arena的MCP client ──认证Bridge/隧道──→ 本机WebAgent MCP server
                                          │
                                          └─→ 本机文件/命令/已授权桌面工具

可选扩展链路（对本机而言：出站）
本机WebAgent内的MCP client ──HTTPS──→ 第三方MCP server
```

两条链可以串起来，但不是必须串联。WebAgent对Arena是server，对第三方则是client；同一程序可以在不同连接中承担不同角色。启用出站不新开本机入站端口/隧道，也不开放远程工作台。它不是模型API、不是控制Arena网页、也不是让Chat和Bridge同时运行；手机Arena仍固定Bridge。

**只让Arena操作你本机已有文件、命令、桌面工具，不需要开启公网出站MCP。** 第三方远程工具服务才需要它；本地服务可用回环HTTP(S)或经确认的stdio。现有Arena连接的OAuth/链接密钥与第三方Bearer令牌是两组不同凭据，不会自动转发Arena登录态或本机连接密钥给第三方。

### 例子：额外接入一个第三方资料查询工具

这是假设场景，不是已经验收某个供应商：

1. 你在本机登记可信第三方MCP端点，确认初始化/工具发现与可选凭据外发。不要填Arena聊天网页地址，也不要把本机Bridge链接填回这里。
2. Arena通过现有Bridge调用`external_servers`，看到已登记服务的ID和不可信工具描述。第三方工具不是直接改名合并成Arena的原生工具列表。
3. Arena调用`external_request`，指定服务、工具、参数及稳定requestKey。此时只是排队，`waiting-approval`不是执行成功。
4. 你在本机核对并批准，WebAgent才向第三方发送`tools/call`及该次参数；第三方可能处理/保存这些数据，不能因HTTPS而认定数据不外泄。
5. Arena用同一会话调用`operation_result`读取该requestId的结果。结果标记`external-reported`，只是第三方自报，不是本机独立验证了第三方效果；失败/未知不自动重放。

没有默认把整个工作区上传给第三方：出站发送协议信息、所填凭据及获批调用参数。不过参数可以包含文件正文；若批准发送了正文，它确实会离开本机，不能把“逐次审批”误解为“永不上传文件”。第三方结果也可能返回给Arena，应同时考虑这两个服务的数据接收范围。

### 本机登记与限制

在“工具接入与审批”勾选“明确启用公网HTTPS”，输入可信服务的https地址（不能含URL账号、查询串或片段）及可选Bearer令牌，确认外发提示再登记。需刷新取得正确工作区/主机身份。登记本身发送初始化及令牌，工具发现后仍须每次本机审批；远端请求还受Bridge所有者权限限制。

默认不开公网；只接受DNS全部结果为允许公网地址的HTTPS，连接固定到检查过的地址，证书正常验证，不跟随跳转、不自动重试。不能借此访问内网、元数据IP或本机隧道。服务器需要重定向、OAuth自动登录、Cookie或自定义头时，本实现不自动兼容；不要把令牌塞进URL绕过。失败/unknown先核查服务端效果，勿重复提交修改。

注册/令牌只存内存，移除接入中止本机请求，重启重新登记；中止不撤回对方已经收到的数据或产生的效果。端点路径本身也可能敏感，本机列表会展示，请勿贴到公开日志。逐函数与验证边界见[公网出站详解](webagent-core/agent-host/src/mcp/公网出站详解.md)。真实第三方服务未因隔离TLS测试而自动验收。

## 原生VS Code：草稿差异与恢复

核心扩展现提供两条命令：Ctrl+Shift+P输入“Web Agent: 预览当前草稿与磁盘差异”，只读查看磁盘/草稿快照；返回原文件后运行“Web Agent: 预览并恢复当前草稿为磁盘版本”，关闭自动保存、核对差异并明确确认，版本未变才修改草稿，可用原生Ctrl+Z。不会主动保存，不回滚已经保存的Agent修改或跨文件任务。限可信本地工作区、64KiB以内无BOM UTF-8已有文件；详细步骤和未完成实机验收见[扩展说明](webagent-core/extension/入口与Webview详解.md#editorreviewjs原生单文件草稿预览与恢复)。

## 跨文件内容检查点：任务前备份，任务后恢复

这解决“准备让Arena/Agent修改几个已有文件，事后想把它们恢复到任务前内容”的场景。**必须在任务之前建立**；不能在修改之后才建立检查点，并期待它知道更早的正文。它与上面的单次保存回退、VS Code未保存草稿恢复是三种不同入口。

1. 在经典工作台的Bridge→“工具接入与审批”找到“跨文件内容检查点”。先保存要纳入的内容，另行保留未保存草稿。
2. 每行输入一个工作区内已有文件路径，例如临时测试文件`test-a.txt`和`test-b.txt`；点击“确认建立任务前检查点”。原文只进本机内存，不自动上传给外部AI。列表只显示路径/状态。
3. 让Agent或其他程序执行所需任务。检查点不会自动记录其他文件，也不会随任务完成自动恢复。
4. 如需恢复，先保留仍需的修改，关闭或另存编辑器未保存草稿；点击“只预览此检查点恢复差异”。逐个核对current→checkpoint的diff，再明确确认“恢复一次”。
5. 系统先核对全部文件是否仍等于预览版本，然后逐文件恢复并读回验证。任一前置漂移则不开始写入；执行过程中漂移/失败则停止，**可能部分完成**，不能把它当原子事务。
6. 看逐文件状态：`restored`已恢复、`unchanged`无变化、`unknown`已发起但效果未确认、`not-started`未发起。失败后先人工查盘；已执行检查点显示`consumed`，不能再次恢复或自动补偿。刷新可找回未过期记录；不要换ID盲目重试。

最多8个检查点，每个1–12个文件；单文件64KiB、备份正文合计256KiB、15分钟有效，重启/过期/移除就丢失。仅无BOM的UTF-8普通文本，拒绝敏感/越界路径、二进制、目标消失和超预算。它不是长期备份，也不恢复新文件/删除/重命名、权限/目录结构、数据库或第三方服务效果。已建立检查点的已有文件，即使由shell改写，也只能恢复这些文件正文，不能撤销命令其余副作用。

这是本机人工控制，不新开Chat模型、不增加远程恢复工具。详细函数、状态机、并发边界和验证见[编辑回退详解](webagent-core/agent-host/src/utils/编辑回退详解.md#filecheckpointsjs任务前检查点与任务后跨文件内容恢复)。新控件人工Windows验收仍待；自动测试不代签。

经典工作台启停在设置Bridge页和右栏显示独立结果：写入确认而后续读取失败时，保留“原主机已确认、当前状态未核对”；请求超时/坏回包是结果未知，请先读取状态，不要反复启动。页面不自动重试或复制地址。停止不自动取消已接受的工具任务，已发送但未被服务器受理的启动或其他页面启动仍可能改变最终状态；停止确认不是所有后代进程退出证明。原生扩展重置/停止的绑定、确认和未知结果消费已补回归；真实用户IDE/窗口及进程结果仍单列验收。
