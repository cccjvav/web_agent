# ShunCode Windows 使用指南

这份指南只写 **Windows + CMD**（不要用 Git Bash / WSL 当主路径）。目标有两件：

1. 本机打开工作台，用右侧 Chat 改磁盘上的代码。
2. 点 Bridge，让 **Arena / DeepSeek 网页 / 其它网页 Agent** 改**你自己电脑上的仓库**（不需要 ChatGPT Plus）。

---

## 1. 需要事先安装的软件

全部装完后，**关掉旧的 CMD，新开一个**，否则 PATH 里还没有新程序。

| 软件 | 是否必须 | 安装 |
|---|---|---|
| **Node.js LTS**（带 npm） | 必须 | https://nodejs.org/ 选 LTS，安装时勾选 npm |
| **Git for Windows** | 建议 | https://git-scm.com/download/win （`git_status` / `git_diff` 需要） |
| **cloudflared** | 要用**网页** Agent 改本机仓库时必须 | 见下一节。本地 Chat 不需要 |
| 浏览器 | 必须 | Edge / Chrome 打开 http://127.0.0.1:3000 |

检查是否装好（在仓库根目录双击，或 CMD 里运行）：

```bat
check-env.cmd
```

应看到 Node.js、npm 的版本号。cloudflared 一行若是「未安装」，本地 Chat 仍能用。

### 安装 cloudflared（给网页 Agent 用）

不需要 Cloudflare 账号。管理员 CMD 里：

```bat
winget install --id Cloudflare.cloudflared
```

若 winget 不可用：到 https://github.com/cloudflare/cloudflared/releases 下载 `cloudflared-windows-amd64.exe`，改名为 `cloudflared.exe`，放到已在 PATH 里的目录（例如 `C:\Windows\System32`），或记住完整路径，启动前执行：

```bat
set CLOUDFLARED_PATH=C:\工具\cloudflared.exe
```

装完**新开 CMD**，再运行 `check-env.cmd`，应显示 `cloudflared 已在 PATH`。

第一次启动 Node 时，Windows 防火墙可能弹出「是否允许访问网络」。**专用网络请勾选允许**，否则本机浏览器也打不开 3000 端口。

---

## 2. 拿到这份代码

若你已经用 Git 克隆了本仓库，CMD 进入根目录即可（能看到 `run-shuncode.cmd` 的那一层）：

```bat
cd /d D:\code\web_agent
```

把 `D:\code\web_agent` 换成你实际的路径。

克隆下来以后，根目录里会看到 `shuncode-repro\`。它 **不是** 你要运行的程序：

- **`shuncode-repro`**：更早一版 Bridge 原型，启动脚本不会进这里。
- **网页里要真 VS Code：** 用 `run-shuncode-vscode.cmd`（从 npm 下载完整 code-server 到 `bin\code-server-runtime\`）。步骤见 [网页VSCode使用指南.md](./网页VSCode使用指南.md)。
- 你真正跑的是 **`shuncode-core\agent-host`**，浏览器界面是 **`shuncode-core\workbench`**。

整条工作流、为什么这样实现、以及为什么 GitHub 根上没有 `.shuncode`，写在 **[组件说明.md](./组件说明.md)**。对着源码逐步直译（函数拆步骤、不杜撰）见 **[技术实现.md](./技术实现.md)**。

---

## 3. 第一次启动（演示工作区）

仓库自带演示项目 `workspace\`（里面有计算器示例）。在**仓库根目录**：

```bat
run-shuncode.cmd
```

也可以资源管理器里双击 `run-shuncode.cmd`。

第一次会自动执行 `npm install`（只需一次）。看到类似：

```
UI        http://127.0.0.1:3000
MCP       http://127.0.0.1:48271/mcp/……
Workspace D:\code\web_agent\workspace
```

用浏览器打开：**http://127.0.0.1:3000**

这个黑色窗口要一直开着。停止：在该窗口按 **Ctrl+C**。

只想在本机右侧 Chat 里改演示代码：到这里就可以了，不必装 cloudflared。

---

## 4. 指向你自己的仓库（重点）

默认改的是本项目里的 `workspace\`，不是你平时干活的那个 Git 仓库。

**做法 A（推荐）——启动时带路径：**

```bat
cd /d D:\code\web_agent
run-shuncode.cmd D:\code\my-app
```

`D:\code\my-app` 必须已经存在。ShunCode 只会在这个目录里读文件、打补丁、跑命令。

路径有空格时加引号：

```bat
run-shuncode.cmd "D:\My Projects\foo"
```

**做法 B——环境变量：**

```bat
cd /d D:\code\web_agent
set WORKSPACE_ROOT=D:\code\my-app
run-shuncode.cmd
```

窗口标题下会打印 `Workspace ...`，确认是你的仓库再开始改。

建议在你的仓库 `.gitignore` 里加上（避免把 MCP 密钥提交上去）：

```
.shuncode/config.json
.shuncode/read-hashes.json
```

`.shuncode\` 里还会有 `instructions.md`、`preference.md`、`tech-stack.md`、`skills\`、`memory\`，这些可以按需提交。

---

## 5. 本机 Chat（不经过 ChatGPT）

1. 打开 http://127.0.0.1:3000
2. 右侧保持 **CHAT**
3. 输入框下 **Agent ▾** 默认就是 **ShunCode Code**（和 VS Code 里 Copilot 侧栏的 Agent 一样：直接动手）
   - Ask：只读
   - Plan：只读 + 多模型博弈
   - Code / Agent：搜 → 读 → 补丁 → 再测
4. 输入任务，例如：`分析当前项目实现了什么功能`（对你挂载的任意仓库，不限于演示计算器）

Chat **不需要** cloudflared，也 **不需要** 登录 GitHub。

---

## 6. 让网页端 Agent 改本机仓库（Bridge）

**不绑死 ChatGPT，也不需要 Plus。** 先看你用哪一种客户端：

| 客户端 | 要 Plus？ | 要隧道？ | 怎么连 |
|---|---|---|---|
| **本机 Chat**（右侧 CHAT） | 否 | 否 | 直接说话，见第 5 节 |
| **Arena** 等支持 MCP 的网页栏 | 否 | 是 | 复制提示词，整段当第一句 |
| **DeepSeek 网页** | 否 | 是 | 网页自己调不了 MCP：Chrome/Edge 装 DeepSeek++，把 MCP 地址填进扩展侧边栏。见 [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md) |
| **ChatGPT 免费普通聊天** | — | — | **通常调不了 MCP**，请用上面几行 |
| **ChatGPT Plus 开发者模式** | 是 | 是 | 可选：连接器填规范地址 `/mcp`，OAuth 配对码在 Bridge 页 |

1. 已安装 **cloudflared**，并且当前是用 `run-shuncode.cmd` 跑着服务。
2. 浏览器打开工作台 → 左下角齿轮 → **智能体自定义设置** → 左侧 **Bridge**。
3. 在「怎么连到本机仓库」里点选客户端（默认 Arena）。
4. 隧道保持默认 **Cloudflare Quick Tunnel**。
5. 点 **启动 Bridge**。默认 **Cloudflare Quick Tunnel** 会 `spawn` cloudflared，最多等约 25 秒。成功后 MCP 地址变成：

   `https://xxxx.trycloudflare.com/mcp/一串密钥`

   才给云上 Agent 用。本机没有 cloudflared 或 25 秒没解析到域名时，按钮仍成功（toast 会提示安装），MCP 走当前页面源——本机预览可用，**云端连的是它自己那台机器，不是你家电脑**。Named / ngrok 下拉目前不会另起进程。
6. **Arena 类**点 **复制提示词**（不要只复制 URL），剪贴板里应是两段：

   ```
   https://xxxx.trycloudflare.com/mcp/……

   快速连接这个 MCP（URL），明确使用规则，熟悉可用工具，做好处理接下来一系列工作的准备。
   ```

7. **Arena 类：** 打开该网页 Agent 的**新对话**，把上面整段当作**第一句**发出，再说任务，例如：`修复测试并运行 npm test`。  
   **DeepSeek 网页：** 不要把两段说明贴进聊天。用本机 Chrome/Edge 打开 https://chat.deepseek.com/ ，在 DeepSeek++ 侧边栏 MCP 里添加 **Streamable HTTP**，URL 只填那一行 `https://….trycloudflare.com/mcp/…`。工作台可点 **打开 DeepSeek** 复制地址。逐步截图级说明见 [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md)。**不要**再装 `deepseek-pp-shell-host`。
8. 回到 ShunCode，右侧切到 **BRIDGE**，应能看到工具调用。磁盘上的文件会变。本轮调用次数 / 失败率可点 **清除本轮统计**（也会清掉主机里「上次读文件的哈希」缓存）。

ChatGPT Plus 若要用官方连接器：MCP URL 填 **规范地址**（`https://….trycloudflare.com/mcp`，不要带密钥），授权走 OAuth，在本机输入 Bridge 页上的配对码。免费账号没有连接器这一步。

`.env`、密钥、SSH、`.shuncode/config.json` 不会经 MCP 读出。可在工作区根放 `.shuncodeignore` 追加规则。

### 每次重启都要注意

- **Quick Tunnel 的域名每次「启动 Bridge」都会变。** 必须重新复制提示词或 MCP 地址，并在网页 Agent / DeepSeek++ 里替换旧地址。
- MCP 密钥存在工作区 `.shuncode\\config.json`，一般不会每次换。点「重置 MCP 地址」会换密钥并写回该文件，旧链接立刻失效，重启后仍是新密钥。

### 不要做的事

不要把 `trycloudflare.com/mcp/...` 发到群里、Issue、截图网盘。拿到的人可以改你的文件、在你电脑上跑命令。

---

## 7. 常用 CMD 一览

在仓库根目录打开 CMD（`cd /d` 到有 `run-shuncode.cmd` 的目录）。

| 目的 | 命令 |
|---|---|
| 检查 Node / Git / cloudflared | `check-env.cmd` |
| 跑产品测试 | `run-tests.cmd` |
| 安装 cloudflared | `winget install --id Cloudflare.cloudflared` |
| 启动（演示 workspace） | `run-shuncode.cmd` |
| 启动网页 VS Code | `run-shuncode-vscode.cmd` 或带仓库路径 |
| 启动并挂你的仓库 | `run-shuncode.cmd D:\code\my-app` |
| 同上（环境变量） | `set WORKSPACE_ROOT=D:\code\my-app` 然后 `run-shuncode.cmd` |
| 指定 cloudflared 路径 | `set CLOUDFLARED_PATH=C:\工具\cloudflared.exe` 然后照常启动 |
| 停止 | 启动窗口里 **Ctrl+C** |
| 换端口（少用） | `set WORKBENCH_PORT=3000` 与 `set AGENT_HOST_PORT=48271` |

没有 Linux 的 `./run-shuncode.sh` 步骤；Windows 请只用上面的 `.cmd`。

---

## 8. 界面里和仓库有关的设置

- **环境偏好**（自定义设置 → 环境偏好）：操作系统、Shell、回复语言、提交说明。写入 `.shuncode\preference.md`。Windows 上会提示 Agent 用 PowerShell，不要假设 bash。
- **技术栈申明**（自定义设置 → 技术栈）：语言、框架、包管理、测试命令。写入 `.shuncode\tech-stack.md`。可点「从工作区探测」。Code 模式跑测试时优先用这里填的命令。
- **指令**（自定义设置 → 指令）：写入工作区 `.shuncode\instructions.md`。本地 Chat 和 Bridge 的 `initialize.instructions` 都会带上，例如「提交说明用中文」。
- **技能引导**（自定义设置 → 技能）：Skills 就是文件夹 `.shuncode\skills\名字\SKILL.md`。按「名称 → 何时使用 → 步骤」三步建，或点「修测试 / 代码审查 / 发版」模板。对话里说「按 fix-tests 做」，或让模型 `load_skill`。演示工作区已有 `fix-tests`、`review`、`docs-sync`（改功能必须同步改说明书）。挂你自己的仓库时不会自动带上，需要拷过去或在设置里新建。
- **API Provider**：本地 Chat 要接真模型时再填 Endpoint + Key，点 Test / Add API。和 Bridge、cloudflared、ChatGPT 登录无关。不填则用内置探索 Agent（对当前工作区搜、读、跑测试；消息里带补丁才会改文件）。
- **多模型博弈**：Plan 模式三分支独立作答，意见一致再行动。可设合并主模型、思考强度、只读验证、每回合最大分支数。
- 工作区根目录就是你传入的 `WORKSPACE_ROOT`，Agent 不能写到这个目录外面。

---

## 9. 故障排除

**CMD 提示找不到 Node.js**  
没装，或装完没新开 CMD。用 `check-env.cmd` 确认。

**端口 3000 / 48271 已被占用**  
关掉另一份 ShunCode，或结束占用端口的程序后重开 `run-shuncode.cmd`。

**浏览器打不开 127.0.0.1:3000**  
看防火墙是否拦截了 Node；启动窗口是否还在跑、有没有报错。

**启动 Bridge 报未找到 cloudflared**  
`winget install --id Cloudflare.cloudflared` 后新开 CMD。Windows 上故意不把 `localhost` 交给 ChatGPT。

**cloudflared 启动了但 25 秒没有 trycloudflare.com**  
检查能否访问外网；公司网可能拦隧道。可设置 `CLOUDFLARED_PATH` 指向真正的 `cloudflared.exe`。

**网页 Agent 没改到文件**  
- 地址必须是 `https://….trycloudflare.com/mcp/…`，不是 localhost。点了「启动 Bridge」若仍是 `127.0.0.1`：本机没有 cloudflared、PATH 不对，或 25 秒没解析到 trycloudflare.com。看 toast / `check-env.cmd`，必要时设 `CLOUDFLARED_PATH`。  
- 该聊天栏必须真能调 MCP/工具；免费 ChatGPT 普通对话通常不行，改用本机 Chat、Arena，或 DeepSeek++。  
- Arena 类：提示词要整段当第一句。DeepSeek：URL 填进扩展侧边栏，不是贴进聊天框。  
- 重新启动过 Bridge 就要换新地址（DeepSeek++ 里也要改）。  
- 确认 `Workspace` 打印的是你以为的那个仓库。  
- 工具回了 `HASH_REQUIRED` / `STALE_FILE`：正文里有 `currentHash`，让模型再读一次或带上这个哈希重试。这是工具结果，不是 MCP 掉线。  
- 网页端若调用 `bash` / `cat` / `ls`：主机会收成 `run_command` / `read_files` / `list_directory`。命令字符串在 Windows 上仍走 PowerShell。

**git_status 显示没有 git**  
不是 git 仓库、或没装 Git 时，工具返回 `available: false`，不抛失败。要看分支请安装 Git for Windows，并只在你确实要版本库时 `git init`。

**npm test / 命令在 Windows 上的行为**  
本机命令走 PowerShell。在 Chat 里下发的应是 Windows 能跑的命令（例如 `npm test`）。工具名可以叫 `bash`，执行器仍是 PowerShell。

---

## 10. 和「官方 ShunCode 安装包」的差别

这是可运行的复现工作台（浏览器 UI + 独立 agent-host），不是官方 exe 安装器。对「网页 Agent 改本机仓库」这条主路径：Windows 上用 `run-shuncode.cmd` + cloudflared 即可。

Git 不再内嵌 code-server。网页里要真 VS Code，用 `run-shuncode-vscode.cmd`（npm 包装到 `bin\code-server-runtime\`）。

---

## 11. 怎么确认实现被测试过

仓库根：

```bat
run-tests.cmd
```

应打印 `patchEngine tests passed`、`mcp protocol tests passed`、`workspace tool tests passed`、`host persist tests passed`、`tunnel tests passed`、`bridge tunnel tests passed`、`http smoke tests passed`、`vscode launcher uses npm runtime, not a vendored dist`、`skip workbench tests passed`、`runChat tests passed`、`chatMode tests passed`、`profile tests passed`、`oauth / streamable http tests passed`。

测试代码在 `shuncode-core\agent-host\tests\`，**根目录没有、也不需要再复制一个 `tests\`。** 细节见 [测试说明.md](./测试说明.md)。
