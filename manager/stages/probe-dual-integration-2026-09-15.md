# 双探针整合阶段

用户新增上传arena-trace-inspector 0.9.0，提交a8f7d86，在同分支保留。已有arena-model-probe视为用户所称arena-ai-probe；没有发现另一份该名称目录。

选择Inspector作为浏览器采集/归属/历史基础，Probe作为参考算法基础，不并行安装两套网络拦截器。0.3浏览器包已组合单采集器与标签参考；Companion实际接收Inspector原生单run证据，不是假接口。其余完整Probe能力、实时Bridge和统一写操作审批仍在实施；Chat API验证后置。

用户新上传原测试74项本地通过；本轮增加字节读取/取消用例，原源码与解包整合版均76项本地通过。VSIX解包运行两套引擎合成计算通过。完整WebAgent测试和精确SHA CI另核对，不代签真实Windows/浏览器验收。

后续同轮：增加同CDP字节流的Probe字段/协议/行为解析，每请求独立采样，只输出有限摘要；不会为了弱参考另开网络钩子。候选按来源去重避免finish重复协议计票；历史选中时不混入现场响应参考。原型及解包测试增为77项（新增解析错误片段不得进入HUD），WebAgent全65文件本地已通过此前版本，后续改动继续复测。

已推送d031262，精确提交CI35011315422九项成功，含Windows包内计算与浏览器整合测试。随后同流双解析与专用整合断言：本地WebAgent65文件、解包浏览器78项通过；这一后续版本CI仍须独立核对。

fe641d2的Windows浏览器构建步骤失败；日志下载暂不可用。已在本地确定复现CRLF不能匹配多行构建接缝的缺陷并修复，保持唯一接缝检查，追加固定回归和有界CI失败诊断。不能单凭此复现断言远端失败只有这一原因，修复后再次检查精确提交。

用户要求继续未完成项：0.4优先补VSCode参考历史闭环和真实取消入口。保存默认关闭且逐次确认；按workspaceState隔离，仅白名单摘要。保留50条/64KiB单条/1MiB总预算，满额拒绝而非删除旧记录。还未完成实时Bridge及统一浏览器写操作审批，不将本批称完整交付。

本批还增加显式WebAgent /ask草稿交接（16KiB白名单摘要、用户另按发送），不自动调用API。当前本地66测试文件通过，独立VSIX包内标准分析/trace分析/历史重开均通过；精确SHA CI待推送后核对。
