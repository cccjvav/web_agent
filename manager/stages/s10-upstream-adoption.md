# 阶段10：继续上游借鉴与文档整顿

2026-09-16。继续阶段7的26类队列，不把“发现/候选”当作全部实施；阶段8探针仍有界面接入缺口。

## 本轮实际落地

- 11限流：OAuth JSON/HTML 429都返回Retry-After；超额请求不增加计数/延长窗口；1000-key容量有恢复提示且旧key剩余额度不被挤掉；来源不直接回退不可信转发头。原固定窗口本来不会被连续拒绝无限延长，此处不虚报修复了不存在的问题。
- 18生成测试的一小部分：2000步固定种子时序与独立参考模型、容量/过期边界、真实HTTP伪造头/端点预算回归。不是完整性质/变异门禁。
- 文档：11.3按用户确认完成/关闭；探针三入口矩阵核对；历史根审计输入移入review/archive并修链接；重写两个未经当前验证的第三方扩展安装保证；旧计数标记历史。
- 本地74测试文件通过。代码9dfa0ad2853fd60f256e3ae47cf29145663e4ed4的CI35031845316九项成功（Ubuntu/Windows、安装器/探针包解包验证、真实Chromium）：https://github.com/cccjvav/web_agent/actions/runs/35031845316 。不是实机账户验收。

## 继续实施的具体缺口（未完成）

1. 探针经典工作台完整入口和code-server适配：先设计浏览器/主机位置、认证与生命周期，不直接移除Desktop保护。
2. 04/12公网MCP客户端：HTTPS、DNS实际连接固定/私网拒绝、重定向与凭据边界、发现及调用全链路。
3. 08/09可读编辑预览、版本保护回退、显式文件变化通知；不自动覆盖用户后续修改。
4. 11可信代理/认证身份下的会话公平性；当前仍可能共享代理/NAT预算，不能说已完成用户级公平调度。
5. 13–15 OCR/图片/桌面目标约束，18全面性质/变异测试，19来源/签名，以及其余待深入模块；逐项沿用借鉴映射，不自动申请密钥或放开新权限。

11.3是协作说明，已关闭，不再用它占位。真实探针/第三方/桌面等具体未验证功能仍按各自条目保留，但不得把11.3换个名字重新作为同一阻塞项。
