# Web Agent 修复任务书

**交付对象**：负责本项目的开发助手
**目标分支**：`arena/01a05d84-web-agent`
**基线提交**：`638d800`（Record deferred-feature tradeoffs; warn if secrets are git-tracked.）
**任务书生成日期**：2026-09-06

本文件自成上下文，不需要阅读其它报告。全部结论均在 `638d800` 上实测验证，非静态推测。

---

## 第 0 步：开工前必读（否则你会审错代码）

### 0.1 先确认你看到的是正确的分支

本沙箱环境的 `remote.origin.fetch` 可能被限制为只抓 `main`：

```
+refs/heads/main:refs/remotes/origin/main
```

在这种配置下，`git fetch origin` **不会**取到目标分支，`git branch -r` 里也**不会**出现它。已经有两个助手因此误判"该分支不存在"，进而对着落后 26 个提交的旧代码写了报告。

**开工前必须执行：**

```bash
# 直接问服务器，这是唯一可靠的方式
git ls-remote --heads origin

# 显式拉取目标分支
git fetch origin '+refs/heads/arena/01a05d84-web-agent:refs/remotes/origin/arena/01a05d84-web-agent'

# 确认基线
git log --oneline -1 origin/arena/01a05d84-web-agent   # 应为 638d800 或更新
```

**禁止**根据 `git branch -r` 或 `git branch -a` 的输出断言某分支不存在。

如果本会话被 Arena 固定在其它工作分支上无法切换，请用独立 worktree 审阅真实代码：

```bash
git worktree add /tmp/real origin/arena/01a05d84-web-agent --detach
```

### 0.2 不要"修"这些——它们是有意取舍

`SECURITY.md` 与《合并版项目问题清单与修复计划.md》已明确声明**不做**以下事项。请勿把它们当作缺陷提交修复：

- 操作系统级命令沙箱（`run_command` 可以访问工作区外，这是已声明的设计边界）
- 系统钥匙串 / OAuth 落盘
- 把一把 URL 密钥拆成多把
- 按客户端隔离全部全局状态
- 交互式 PTY
- Playwright 点工作台
- 把演示按钮改回"使用 GitHub 登录"、把 Plan 改回假的 97% 一致率
- 改 `webagent-repro/` 里的 JS（已冻结的旧原型）

Named Tunnel / ngrok **已经**实现 spawn，不要再写成"未实现"。

### 0.3 已经修好的，不要重复劳动

以下问题在 `638d800` 上**已确认修复**（我逐条实测过）：

| 项 | 实测证据 |
|---|---|
| `/api` 随隧道暴露公网 | 带 `X-Forwarded-For` + 外部 Host 的请求返回 `{"error":"not found"}` |
| CORS 通配 `*` | `Origin: https://evil.com` 打 `/api` 返回 404 |
| 符号链接逃逸沙箱 | `read_files` 经文件/目录符号链接均返回 `Security error: outside workspace root` |
| 命令超时失效 + 孙进程残留 | `timeoutSec:2` 实测 **2.0s 返回**，`status=timeout`，残留进程 **0** |
| 前端 `bind()` 崩溃致按钮失效 | jsdom 实跑 `ui.bind()` 无异常，关键按钮全部有 handler |
| JSON-RPC `id:0` 被吞成 null | 实测原样返回 `id:0` |
| `write_file` 跨重启无确认覆盖 | 已用内存态 `sessionHash` 正确隔离，新进程直接写**被拒绝** |
| 服务默认监听范围 | 默认绑定 `127.0.0.1`，需 `WEBAGENT_BIND=0.0.0.0` 才对外 |
| 缺 LICENSE / SECURITY.md / CI | 均已补齐，`.github/workflows/test.yml` 存在 |

`npm test` 在基线上全绿（28 个测试文件，含 `sandbox.test.js`、`corsAllow.test.js`、`localControl.test.js`、`workbenchHtml.test.js` 等）。

---

## 通用要求

- 每组任务**单独 commit**，commit message 用祈使句概括实际改动。
- 每组结束后运行，必须保持全绿：
  ```bash
  cd webagent-core/agent-host && npm install && npm test
  ```
- 新增的测试必须**在修复前失败、修复后通过**（先写测试确认它能捕获问题，再改实现）。
- 不做与任务无关的重构。
- 若你判断某项不该按本文件的方式修，**先说明理由再动手**，不要沉默地跳过。

---

## 任务一（P0）· 远程 MCP 危险命令拦截可被绕过

### 问题

`SECURITY.md` 第 12 行声称「已经做的：……**远程危险命令拒绝**」，第 21 行进一步承诺：

> `rm -rf` / `git push` / `curl | sh` 一类即使带 `confirm_dangerous` 也会被远程拒绝，只能在本机 Chat 确认

`src/mcp/instructions.js` 第 48 行把同样的承诺**发给了远程模型**：

> Destructive shell (rm -rf, mkfs, dd, shutdown, git reset --hard, Remove-Item -Recurse) needs confirm_dangerous=true on local Chat. **Remote MCP rejects those commands even with that flag (E_FORBIDDEN).**

但拦截基于字面量正则（`src/tools/index.js` 的 `DANGEROUS_RE`），换个等价写法就绕过了。

### 实测结果（基线 638d800）

远程 MCP 路径（`POST /mcp/<secret>` → `tools/call` → `run_command`）：

| 命令 | 结果 |
|---|---|
| `rm -rf /path` | 拦截 |
| `rm -r -f /path` | **放行** |

本机 `/api` 路径：

| 命令 | 结果 |
|---|---|
| `rm -rf /path` | 拦截 |
| `rm -r -f /path` | **放行** |
| `find /path -delete` | **放行** |

### 为什么这是 P0

其它几项只是功能缺陷，而这一项是**文档和 instructions 向远程模型明确承诺了一道护栏，实际却不存在**。模型会据此认为"我删不掉东西"，从而放心执行。这类"承诺了不存在的安全边界"比单纯没有边界更危险。

### 修复要求

1. 重写 `src/tools/index.js` 的危险命令判定。**不要只往正则里加几个词**——黑名单穷举 shell 语义是做不到的。建议采用分层策略：
   - 先做**词法归一化**：折叠连续空白、剥离引号（`r""m` → `rm`）、把 `-r -f` / `--recursive --force` 等短选项组合归一。
   - 在归一化后的 token 序列上判定，而非在原始字符串上做正则匹配。
   - 至少覆盖：`rm` 带任意形式的递归/强制选项、`find ... -delete`、`shred`、`truncate`、`dd`、`mkfs`、`> /dev/sd*`、`git push`、`curl|wget ... | sh|bash`。
2. 远程路径（MCP）与本机路径（`/api`）的策略差异要在**同一处**表达，避免两条路径各判一次而漂移。
3. 若最终判断某些变体确实无法可靠识别，**必须回头改文档**：把 `SECURITY.md` 第 12/21 行和 `instructions.js` 第 48 行的措辞从"拒绝"降级为"尽力拦截常见形式，不构成安全边界"。**代码与承诺必须一致，二选一，不允许留着不一致。**

### 验收

新增 `tests/dangerousCommands.test.js`，对**远程 MCP 与本机 `/api` 两条路径**分别断言下列命令全部被拒（远程）/ 需确认（本机）：

```
rm -rf X
rm -r -f X
rm --recursive --force X
rm -fr X
r""m -rf X
find X -delete
```

---

## 任务二（P1）· 模型列表表格永远显示空白

### 问题

`webagent-core/workbench/js/settings.js` 的 `paintProviderTable()` 依赖四个字段渲染表格：

```js
// 第 93、98、102、103、104 行
const g = m.group || 'custom';
const caps = (m.caps || []).map(...)
<td>${escapeHtml(m.contextSize || '—')}</td>
<td>${escapeHtml(m.pricing || '')}</td>
```

数据源是 `state.status.models`，来自 `GET /api/status`。但 `src/api/routes.js` 第 99-106 行做了白名单投影，把这四个字段全剥掉了：

```js
models: cfg.models.map((m) => ({
  id: m.id, name: m.name, protocol: m.protocol,
  modelId: m.modelId, baseUrl: m.baseUrl, hasKey: Boolean(m.apiKey)
})),
```

同一份数据在 `GET /api/models`（第 372 行）里是**完整**的，含 `caps` / `contextSize` / `group` / `pricing`，前端却没用这个端点。

### 实测结果

存入带完整字段的模型后：

```
GET /api/status  → models[0] keys: id,name,protocol,modelId,baseUrl,hasKey
```

用户看到的表格：「上下文大小」恒为 `—`，「功能」恒为 `—`，「定价」恒空，分组恒为 `custom`。

### 修复要求

二选一，并在代码注释中写明哪个是唯一数据源：

- **方案 A**：`/api/status` 的投影补回 `group` / `contextSize` / `caps` / `pricing`（这四个非敏感），继续不下发 `apiKey`、保留 `hasKey`。
- **方案 B**：`paintProviderTable()` 改从 `/api/models` 取数。

### 验收

新增测试断言：存入含 `caps`/`contextSize`/`group`/`pricing` 的模型后，前端所用的那个端点能拿到这四个字段。

---

## 任务三（P1）· MCP 的 Origin 校验不阻断执行

### 问题

`src/utils/corsAllow.js` 的 `mcpCors()` 使用：

```js
cors({ origin(origin, cb) { cb(null, isAllowedMcpOrigin(origin)); } })
```

`cors` 中间件在 `false` 时只是**不写 CORS 响应头**，请求照常进入路由并执行。

### 实测结果

```
POST /mcp/<正确secret>  Origin: https://evil.com
  method=tools/call  name=run_command  command="echo EVIL_EXEC"
→ 命令被执行，输出 EVIL_EXEC
```

浏览器读不到响应（CORS 头缺失），所以**不能窃取数据**；但恶意页面可以**盲打触发副作用**（前提是它已知道 secret）。

### 修复要求

在 `/mcp` 路由前增加一道 Origin 守卫中间件：

- 请求**带 Origin** 且不在 `isAllowedMcpOrigin` 白名单内 → 直接返回 **403**，不进入 `handleRpc`。
- 请求**不带 Origin**（服务端客户端、CLI、cloudflared 转发）→ 放行，不受影响。
- 保留现有 `mcpCors()` 负责正常的响应头协商。

注意：白名单已包含回环地址、浏览器扩展协议（`chrome-extension:` 等）、以及 `PAGE_ORIGINS` 里的站点，改动不要破坏这些。

### 验收

扩展 `tests/corsAllow.test.js`：断言 `Origin: https://evil.com` + 正确 secret 时，工具**不被执行**且返回 403；断言无 Origin 的请求仍能正常调用工具。

---

## 任务四（P2）· 不支持 JSON-RPC 批量请求

### 问题

`src/mcp/server.js` 的 `handlePost` 直接读 `req.body.jsonrpc`。当客户端提交请求数组时取不到该字段，误报 Invalid Request。

### 实测结果

```
POST /mcp/<secret>
[{"jsonrpc":"2.0","id":1,"method":"ping"},{"jsonrpc":"2.0","id":2,"method":"tools/list"}]

→ {"jsonrpc":"2.0","error":{"code":-32600,"message":"Invalid Request: jsonrpc must be \"2.0\""},"id":null}
```

会做批处理的 MCP 客户端直接连不上。

### 修复要求

`handlePost` 支持数组：

- `Array.isArray(req.body)` 时逐条走 `handleRpc`，把结果收集成数组返回。
- 数组中的通知类（无 `id`）不产生响应条目；若整批都是通知，返回 204 无响应体。
- 空数组按 JSON-RPC 2.0 规范返回 `-32600`。
- 单条请求的现有行为保持不变。

### 验收

扩展 `tests/mcpProtocol.test.js`：断言批量请求返回等长（去掉通知后）的结果数组、id 一一对应、空数组回 `-32600`。

---

## 任务五（P2）· 文档与实现不一致

以下三处是**代码已改、文档没跟上**，会直接误导读者和模型。

### 5.1 `架构导读.md` 第 52 行

现文：

> 工作区根写在进程配置 `webagent-core/agent-host/src/config.js` 的 `workspaceRoot`（环境变量 `WORKSPACE_ROOT`）。所有读/写/**命令**都先经过 `src/tools/patchEngine.js` 的 `resolveSafePath`：路径逃出工作区就拒绝。

实际：命令**不经过** `resolveSafePath`（只有 `cwd` 参数经过）。实测 `run_command "cat /tmp/outside.txt"` 可以读到工作区外文件——这本身是 `SECURITY.md` 声明的有意取舍，但这句话与之直接矛盾。

**改法**：删去"命令"，改为"文件工具（读/写/补丁）经过 realpath 校验；命令执行的边界见 `SECURITY.md` 威胁模型一节"。

### 5.2 `src/mcp/instructions.js` 第 51 行

现文（这段会发给远程模型）：

```
- Stay inside the workspace; the host rejects path escape.
```

实际：路径逃逸确实被拒（文件工具），但模型可用 `run_command` 访问工作区外。措辞应精确到"文件工具"，避免让模型以为命令也受限。

### 5.3 `SECURITY.md` 补充敏感文件保护的实际范围

`sensitive.js` 的黑名单（`.env`、`*.pem`、`.ssh/` 等）**只作用于文件工具**。实测 `read_files ".env"` 被拒，但 `run_command "cat .env"` 可以读出内容。

这在本机信任域内是已知行为，但应在 `SECURITY.md` 明确写一句，避免用户误以为"敏感文件保护"覆盖所有路径。

### 验收

三处文档改完后，全仓搜索其它对"沙箱/边界/拒绝越界/敏感文件"的表述（`README.md`、`组件说明.md`、`技术实现.md`、`docs-site/`），逐条核对一致性。若改动了 docs-site 的源 Markdown，需重跑 `node docs-site/build.js` 同步产物（仓库已有 `docsSite.test.js` 会校验漂移）。

---

## 任务六（P3，可选）· 两项风险缓解

这两项当前风险已较低，是否做取决于你对 `SECURITY.md` 既有取舍的态度。**如果决定不做，请在 `SECURITY.md` 里补一句说明残留风险，而不是沉默跳过。**

### 6.1 API Key 明文落工作区

实测 `sk-XXX` 明文写入 `<workspace>/.webagent/config.json`。

已有缓解：`.gitignore` 已收紧为 `**/.webagent/config.json`（不再是写死单一路径），且启动时会检测 Git 是否已跟踪该文件并警告。

残留风险：非 Git 场景——打包、备份、网盘同步、把工作区目录整个发给别人。

可选修法：迁到用户级目录（Windows `%APPDATA%/webagent/`，其它 `~/.config/webagent/`），做一次性迁移并删除原值。注意 `SECURITY.md` 已声明不做系统钥匙串，此处只是换目录，不涉及加密。

### 6.2 `/api/status` 仍下发 `secretKey`

实测 `('secretKey' in status) === true`。因 `/api` 已限制为仅回环 + 同源，风险大幅下降，但工作台前端持有长期密钥并非必要。

可选修法：`/api/status` 停止下发，改为按需的 `GET /api/bridge/secret`（仅回环可达）。注意 `js/bridge.js` 的 `arenaConnect()` 目前直接用 `state.status.secretKey` 拼 `/mcp/<secret>`，改动时需一并调整。

---

## 建议执行顺序

| 顺序 | 任务 | 理由 |
|---|---|---|
| 1 | 任务一（危险命令 + 文档一致） | 唯一的 P0；涉及对模型的虚假安全承诺 |
| 2 | 任务二（模型表格空白） | 用户可见的功能缺陷，配不了模型 |
| 3 | 任务三（Origin 阻断） | 安全加固，改动集中 |
| 4 | 任务四（批量请求） | 兼容性 |
| 5 | 任务五（文档一致性） | 可与任务一的文档部分合并做 |
| 6 | 任务六（可选） | 按你的判断 |

---

## 完成后请提供

1. 每组任务对应的 commit hash 与一句话说明。
2. `npm test` 的完整输出（确认全绿，且新增测试确实在跑）。
3. 逐条对应本任务书编号的说明：做了什么 / 跳过了什么 / 为什么。
4. 若任务一最终选择了"降级文档措辞"而非"加强拦截"，请明确说明这个决定，因为它改变了产品对外的安全承诺。
