/* generated by docs-site/build.js — do not edit by hand */
window.DOCS = {"builtAt":"2026-09-05","guide":{"introHtml":"<h1 id=\"架构导读-给完全没写过这种程序的人\">架构导读（给完全没写过这种程序的人）</h1>\n<p>这篇是「为什么电脑要这样装、不这样会出什么事」。按键步骤看 <a href=\"./使用指南.md\">使用指南.md</a>；文件夹点名看 <a href=\"#/workflow\">组件说明.md</a>；函数逐步直译看 <a href=\"#/impl\">技术实现.md</a>。</p>\n<p>每一节都按四层写：</p>\n<ol>\n<li><strong>人话</strong> — 不先甩缩写。</li>\n<li><strong>比喻</strong> — 换一个日常场景，帮助记住「为什么」。</li>\n<li><strong>落在仓库哪</strong> — 磁盘上哪个文件在干这件事（以当前源码为准，不写没实现的）。</li>\n<li><strong>行业里管这叫什么</strong> — 你以后读别人文档时能对上号。</li>\n</ol>\n<hr />","sections":[{"id":"1.-这个项目在解决什么麻烦","title":"1. 这个项目在解决什么麻烦","talkHtml":"<p>云上的聊天 AI（Arena、DeepSeek、ChatGPT…）跑在别人的机房里。它默认只能看见你打的字，<strong>进不了你硬盘上的 <code>D:\\code</code></strong>。这个仓库把「动手改文件、跑测试」放在<strong>你自己的电脑</strong>上；网页那边只负责想，想完填一张工单过来。</p>","metaphorHtml":"<p>远方的师傅再聪明，也摸不到你家车库里的车。你不会把整台车寄去机房，而是在自家院子开一间小车间：师傅打电话下工序（「把这个螺丝拧紧，再打火试一下」），车间里的人动手。车间始终在你家。</p>","filesHtml":"<ul>\n<li>车间本体：<code>webagent-core/agent-host/</code>（进程入口 <code>src/index.js</code>）</li>\n<li>院子墙上的按钮面板：<code>webagent-core/workbench/</code>（浏览器 http://127.0.0.1:3000）</li>\n<li>真正改磁盘的扳手：<code>webagent-core/agent-host/src/tools/</code></li>\n<li>网页师傅填的工单入口：<code>src/mcp/server.js</code>（端口 48271）</li>\n</ul>","jargonHtml":"<p>本地工具宿主（agent-host）+ 远端模型。网页侧走 <strong>MCP</strong>（Model Context Protocol，模型调用工具的一套填表规矩）。同类产品介绍可参考 <a href=\"https://docs.shuncode.top/docs/intro/\" target=\"_blank\" rel=\"noopener\">docs.shuncode.top</a>。本仓库是本机工作台 + agent-host，不是 exe 安装包。</p>\n<p>不这样会怎样：若把「改文件」也放到云上，你就得把整份代码上传给别人的服务器。本设计刻意反过来——代码不出门，工单进门。</p>","html":"<p><strong>人话</strong> 云上的聊天 AI（Arena、DeepSeek、ChatGPT…）跑在别人的机房里。它默认只能看见你打的字，<strong>进不了你硬盘上的 <code>D:\\code</code></strong>。这个仓库把「动手改文件、跑测试」放在<strong>你自己的电脑</strong>上；网页那边只负责想，想完填一张工单过来。</p>\n<p><strong>比喻</strong> 远方的师傅再聪明，也摸不到你家车库里的车。你不会把整台车寄去机房，而是在自家院子开一间小车间：师傅打电话下工序（「把这个螺丝拧紧，再打火试一下」），车间里的人动手。车间始终在你家。</p>\n<p><strong>落在仓库哪</strong></p>\n<ul>\n<li>车间本体：<code>webagent-core/agent-host/</code>（进程入口 <code>src/index.js</code>）</li>\n<li>院子墙上的按钮面板：<code>webagent-core/workbench/</code>（浏览器 http://127.0.0.1:3000）</li>\n<li>真正改磁盘的扳手：<code>webagent-core/agent-host/src/tools/</code></li>\n<li>网页师傅填的工单入口：<code>src/mcp/server.js</code>（端口 48271）</li>\n</ul>\n<p><strong>行业里管这叫什么</strong> 本地工具宿主（agent-host）+ 远端模型。网页侧走 <strong>MCP</strong>（Model Context Protocol，模型调用工具的一套填表规矩）。同类产品介绍可参考 <a href=\"https://docs.shuncode.top/docs/intro/\" target=\"_blank\" rel=\"noopener\">docs.shuncode.top</a>。本仓库是本机工作台 + agent-host，不是 exe 安装包。</p>\n<p>不这样会怎样：若把「改文件」也放到云上，你就得把整份代码上传给别人的服务器。本设计刻意反过来——代码不出门，工单进门。</p>\n<hr />"},{"id":"2.-三个绝不能混的东西","title":"2. 三个绝不能混的东西","talkHtml":"<div class=\"table-wrap\"><table><thead><tr>\n<th>东西</th>\n<th>它是谁</th>\n</tr></thead><tbody>\n<tr>\n<td>Git 仓库 <code>web_agent</code></td>\n<td>Web Agent <strong>这套程序</strong>的源码（工具箱）</td>\n</tr>\n<tr>\n<td>工作区</td>\n<td>Agent <strong>允许读写的那一个目录</strong>（默认 <code>workspace/</code>，也可以是 <code>D:\\code\\my-app</code>）</td>\n</tr>\n<tr>\n<td>网页上的 AI</td>\n<td>只会聊天、下工单，看不见你的硬盘</td>\n</tr>\n</tbody></table></div>\n<p>双击启动时，工作区是仓库里那个带测试的小计算器。<strong>它不是引擎。</strong> 你平时干活的项目，要用 <code>run-webagent.cmd D:\\code\\my-app</code> 指过去。</p>","metaphorHtml":"<p>工具箱、正在修的那台车、远方师傅，是三样东西。把工具箱本身当「正在修的车」，扳手可能改到启动脚本和说明书上。</p>","filesHtml":"<p>工作区根写在进程配置 <code>webagent-core/agent-host/src/config.js</code> 的 <code>workspaceRoot</code>（环境变量 <code>WORKSPACE_ROOT</code>）。所有读/写/命令都先经过 <code>src/tools/patchEngine.js</code> 的 <code>resolveSafePath</code>：路径逃出工作区就拒绝。</p>","jargonHtml":"<p>工作区沙箱（workspace sandbox）。和编辑器里「打开了一个文件夹」是同一类边界。</p>","html":"<p><strong>人话</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>东西</th>\n<th>它是谁</th>\n</tr></thead><tbody>\n<tr>\n<td>Git 仓库 <code>web_agent</code></td>\n<td>Web Agent <strong>这套程序</strong>的源码（工具箱）</td>\n</tr>\n<tr>\n<td>工作区</td>\n<td>Agent <strong>允许读写的那一个目录</strong>（默认 <code>workspace/</code>，也可以是 <code>D:\\code\\my-app</code>）</td>\n</tr>\n<tr>\n<td>网页上的 AI</td>\n<td>只会聊天、下工单，看不见你的硬盘</td>\n</tr>\n</tbody></table></div>\n<p>双击启动时，工作区是仓库里那个带测试的小计算器。<strong>它不是引擎。</strong> 你平时干活的项目，要用 <code>run-webagent.cmd D:\\code\\my-app</code> 指过去。</p>\n<p><strong>比喻</strong> 工具箱、正在修的那台车、远方师傅，是三样东西。把工具箱本身当「正在修的车」，扳手可能改到启动脚本和说明书上。</p>\n<p><strong>落在仓库哪</strong> 工作区根写在进程配置 <code>webagent-core/agent-host/src/config.js</code> 的 <code>workspaceRoot</code>（环境变量 <code>WORKSPACE_ROOT</code>）。所有读/写/命令都先经过 <code>src/tools/patchEngine.js</code> 的 <code>resolveSafePath</code>：路径逃出工作区就拒绝。</p>\n<p><strong>行业里管这叫什么</strong> 工作区沙箱（workspace sandbox）。和编辑器里「打开了一个文件夹」是同一类边界。</p>\n<hr />"},{"id":"3.-你电脑上同时活着谁","title":"3. 你电脑上同时活着谁","talkHtml":"<p>黑色 CMD 窗口必须一直开着。关了，网页和工作台一起停。同一套程序开了<strong>两扇门</strong>，而且<strong>两套菜单</strong>（两套 Express，不是两个进程）：</p>\n<ul>\n<li><strong>3000</strong>：给人点的页面（工作台）、本机 Chat、WebSocket。这扇门<strong>不</strong>办临时公网门牌。</li>\n<li><strong>48271</strong>：给网页工单用的门（<code>/mcp</code> + 登录发现文档）。本机的 VS Code 插件也会敲这扇门的 <code>/api</code>。<strong>云上经隧道进来时，<code>/api</code> 会当成没这道菜（404）</strong>，免得把车间钥匙和改文件按钮送到公网。</li>\n</ul>\n<p>只有「让云上的网页改你家文件」时，才再请一个叫 cloudflared 的程序，把 48271 临时映射成 <code>https://随机名.trycloudflare.com</code>。本机右侧 Chat <strong>不需要</strong>这个。</p>","metaphorHtml":"<p>饭店的<strong>店堂大门</strong>（3000，客人进来点菜）和<strong>后厨送货口</strong>（48271，供应商送原料）不是同一扇门。把两扇门焊成一扇，装修店堂时供应商就进不来——第一代原型 <code>webagent-repro/</code> 就是把网页和工单挤在同一个 3000 上，后来换「网页里真 VS Code」会抢门。所以拆开。临时门牌只贴在后厨口；后厨口对外只收工单，不把收银台密码贴到门上。</p>","filesHtml":"<pre class=\"lang-text\"><code>run-webagent.cmd\n    → node webagent-core/agent-host/src/index.js\n        uiApp  :3000  静态 workbench/ + /api + /ws（无 /mcp）\n        mcpApp :48271 /mcp + OAuth；/api 先过 src/utils/localControl.js</code></pre>\n<p>网页 VS Code 那种跑法会设 <code>WEBAGENT_SKIP_WORKBENCH=1</code>：本进程不再占 3000，把店堂让给真 VS Code，后厨 48271 照常。插件打的是本机 <code>127.0.0.1</code>，没有 Cloudflare 头，所以 <code>/api</code> 仍通。</p>","jargonHtml":"<p>双端口、同一进程。不是两套微服务，只是一扇给人、一扇给机器。</p>","html":"<p><strong>人话</strong> 黑色 CMD 窗口必须一直开着。关了，网页和工作台一起停。同一套程序开了<strong>两扇门</strong>，而且<strong>两套菜单</strong>（两套 Express，不是两个进程）：</p>\n<ul>\n<li><strong>3000</strong>：给人点的页面（工作台）、本机 Chat、WebSocket。这扇门<strong>不</strong>办临时公网门牌。</li>\n<li><strong>48271</strong>：给网页工单用的门（<code>/mcp</code> + 登录发现文档）。本机的 VS Code 插件也会敲这扇门的 <code>/api</code>。<strong>云上经隧道进来时，<code>/api</code> 会当成没这道菜（404）</strong>，免得把车间钥匙和改文件按钮送到公网。</li>\n</ul>\n<p>只有「让云上的网页改你家文件」时，才再请一个叫 cloudflared 的程序，把 48271 临时映射成 <code>https://随机名.trycloudflare.com</code>。本机右侧 Chat <strong>不需要</strong>这个。</p>\n<p><strong>比喻</strong> 饭店的<strong>店堂大门</strong>（3000，客人进来点菜）和<strong>后厨送货口</strong>（48271，供应商送原料）不是同一扇门。把两扇门焊成一扇，装修店堂时供应商就进不来——第一代原型 <code>webagent-repro/</code> 就是把网页和工单挤在同一个 3000 上，后来换「网页里真 VS Code」会抢门。所以拆开。临时门牌只贴在后厨口；后厨口对外只收工单，不把收银台密码贴到门上。</p>\n<p><strong>落在仓库哪</strong></p>\n<pre class=\"lang-text\"><code>run-webagent.cmd\n    → node webagent-core/agent-host/src/index.js\n        uiApp  :3000  静态 workbench/ + /api + /ws（无 /mcp）\n        mcpApp :48271 /mcp + OAuth；/api 先过 src/utils/localControl.js</code></pre>\n<p>网页 VS Code 那种跑法会设 <code>WEBAGENT_SKIP_WORKBENCH=1</code>：本进程不再占 3000，把店堂让给真 VS Code，后厨 48271 照常。插件打的是本机 <code>127.0.0.1</code>，没有 Cloudflare 头，所以 <code>/api</code> 仍通。</p>\n<p><strong>行业里管这叫什么</strong> 双端口、同一进程。不是两套微服务，只是一扇给人、一扇给机器。</p>\n<hr />"},{"id":"4.-三条路-一把扳手","title":"4. 三条路，一把扳手","talkHtml":"<p>谁下指令可以不同，<strong>改文件的引擎只有一套</strong>（<code>src/tools/index.js</code> 的 <code>callTool</code>）。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>路径</th>\n<th>谁在下指令</th>\n<th>要不要隧道</th>\n</tr></thead><tbody>\n<tr>\n<td>A 本机 Chat</td>\n<td>你在 3000 右侧说话</td>\n<td>不要</td>\n</tr>\n<tr>\n<td>B 网页 Agent</td>\n<td>云上聊天栏填工单</td>\n<td>要（Cloudflare Quick Tunnel）</td>\n</tr>\n<tr>\n<td>C 网页 VS Code</td>\n<td>侧栏 / <code>@webagent</code>，其实还是打本机 <code>/api/chat</code></td>\n<td>网页改磁盘仍走 B</td>\n</tr>\n</tbody></table></div>","metaphorHtml":"<ul>\n<li>A：你自己站在车间里喊「拧那颗螺丝」。</li>\n<li>B：师傅打电话，电话线必须先接到你家门口的<strong>临时门牌</strong>（公网 https），否则他拨 <code>127.0.0.1</code> 拨到的是<strong>他自己那台电脑的客厅</strong>。</li>\n<li>C：把店堂装修成更像 VS Code 的样子，后厨还是那一间。</li>\n</ul>","filesHtml":"<ul>\n<li>A：<code>workbench/app.js</code> → <code>js/chat.js</code> <code>sendChat</code> → <strong>3000</strong> 的 <code>POST /api/chat</code> → <code>src/agent/runChat.js</code>（认第二参或 <code>payload.emit</code>）→ <code>callTool(..., ask|plan|code)</code></li>\n<li>B：云上 HTTPS → 只进 <code>/mcp</code>（<code>src/mcp/server.js</code>）→ <code>callTool(..., remoteToolMode)</code>（默认 code）。隧道请求打 <code>/api</code> 得 404。</li>\n<li>C：<code>run-webagent-vscode.cmd</code> → <code>scripts/run-code-oss.js</code> 拉 code-server；插件 <code>extension/extension.js</code> 仍 POST 本机 <code>127.0.0.1:48271/api/chat</code></li>\n</ul>","jargonHtml":"<p>同一工具层、多种前端（local chat / MCP client / editor extension）。编辑器只是载体，推理和工具在独立 host。</p>\n<p>不这样会怎样：每换一个界面就重写一遍补丁引擎，哈希预检和路径沙箱会各写各的、必漏。</p>","html":"<p><strong>人话</strong> 谁下指令可以不同，<strong>改文件的引擎只有一套</strong>（<code>src/tools/index.js</code> 的 <code>callTool</code>）。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>路径</th>\n<th>谁在下指令</th>\n<th>要不要隧道</th>\n</tr></thead><tbody>\n<tr>\n<td>A 本机 Chat</td>\n<td>你在 3000 右侧说话</td>\n<td>不要</td>\n</tr>\n<tr>\n<td>B 网页 Agent</td>\n<td>云上聊天栏填工单</td>\n<td>要（Cloudflare Quick Tunnel）</td>\n</tr>\n<tr>\n<td>C 网页 VS Code</td>\n<td>侧栏 / <code>@webagent</code>，其实还是打本机 <code>/api/chat</code></td>\n<td>网页改磁盘仍走 B</td>\n</tr>\n</tbody></table></div>\n<p><strong>比喻</strong></p>\n<ul>\n<li>A：你自己站在车间里喊「拧那颗螺丝」。</li>\n<li>B：师傅打电话，电话线必须先接到你家门口的<strong>临时门牌</strong>（公网 https），否则他拨 <code>127.0.0.1</code> 拨到的是<strong>他自己那台电脑的客厅</strong>。</li>\n<li>C：把店堂装修成更像 VS Code 的样子，后厨还是那一间。</li>\n</ul>\n<p><strong>落在仓库哪</strong></p>\n<ul>\n<li>A：<code>workbench/app.js</code> → <code>js/chat.js</code> <code>sendChat</code> → <strong>3000</strong> 的 <code>POST /api/chat</code> → <code>src/agent/runChat.js</code>（认第二参或 <code>payload.emit</code>）→ <code>callTool(..., ask|plan|code)</code></li>\n<li>B：云上 HTTPS → 只进 <code>/mcp</code>（<code>src/mcp/server.js</code>）→ <code>callTool(..., remoteToolMode)</code>（默认 code）。隧道请求打 <code>/api</code> 得 404。</li>\n<li>C：<code>run-webagent-vscode.cmd</code> → <code>scripts/run-code-oss.js</code> 拉 code-server；插件 <code>extension/extension.js</code> 仍 POST 本机 <code>127.0.0.1:48271/api/chat</code></li>\n</ul>\n<p><strong>行业里管这叫什么</strong> 同一工具层、多种前端（local chat / MCP client / editor extension）。编辑器只是载体，推理和工具在独立 host。</p>\n<p>不这样会怎样：每换一个界面就重写一遍补丁引擎，哈希预检和路径沙箱会各写各的、必漏。</p>\n<hr />"},{"id":"5.-工单-长什么样-mcp","title":"5. 「工单」长什么样（MCP）","talkHtml":"<p>网页 AI 不能直接 <code>fs.writeFile</code>。它只能发一种填好的表格：「请用某某工具、参数是这些」。本机核对钥匙后执行，把结果（或失败原因）写回同一张表。</p>\n<p>失败通常<strong>不是断线</strong>。工具说「缺哈希」「命令太危险」会作为表格里的错误正文返回，让模型改参数再试。</p>","metaphorHtml":"<p>不是让师傅闯进厨房乱翻，而是给一本<strong>点菜单</strong>：只能点菜单上的菜。点到「红烧狮子头」厨房没有，不会把整家店的电话挂断，而是回一句「没有这道菜，有宫保鸡丁、鱼香肉丝…」。网页端看到的 <code>isError</code> 就是这张回条，不是店关门。</p>","filesHtml":"<ul>\n<li>收表：<code>src/mcp/server.js</code> 的 <code>handleRpc</code>（<code>initialize</code> / <code>tools/list</code> / <code>tools/call</code>）</li>\n<li>钥匙：URL 里的 <code>/mcp/&lt;密钥&gt;</code>，或 ChatGPT Plus 的登录令牌（<code>src/mcp/oauth.js</code>）</li>\n<li>工具失败：<code>tools/call</code> <strong>始终</strong>回 MCP <code>isError:true</code> 文本；只有「根本不是一张合法工单」（坏协议、未知方法）才走协议级错误</li>\n<li>第一句连接语（测试锁死原文）：<code>src/mcp/instructions.js</code> 的 <code>CONNECT_LINE</code></li>\n</ul>","jargonHtml":"<p>MCP、Streamable HTTP、JSON-RPC 2.0。Paste-URL 客户端把密钥放在路径里；OAuth 客户端用 Bearer 打规范地址 <code>/mcp</code>。</p>","html":"<p><strong>人话</strong> 网页 AI 不能直接 <code>fs.writeFile</code>。它只能发一种填好的表格：「请用某某工具、参数是这些」。本机核对钥匙后执行，把结果（或失败原因）写回同一张表。</p>\n<p>失败通常<strong>不是断线</strong>。工具说「缺哈希」「命令太危险」会作为表格里的错误正文返回，让模型改参数再试。</p>\n<p><strong>比喻</strong> 不是让师傅闯进厨房乱翻，而是给一本<strong>点菜单</strong>：只能点菜单上的菜。点到「红烧狮子头」厨房没有，不会把整家店的电话挂断，而是回一句「没有这道菜，有宫保鸡丁、鱼香肉丝…」。网页端看到的 <code>isError</code> 就是这张回条，不是店关门。</p>\n<p><strong>落在仓库哪</strong></p>\n<ul>\n<li>收表：<code>src/mcp/server.js</code> 的 <code>handleRpc</code>（<code>initialize</code> / <code>tools/list</code> / <code>tools/call</code>）</li>\n<li>钥匙：URL 里的 <code>/mcp/&lt;密钥&gt;</code>，或 ChatGPT Plus 的登录令牌（<code>src/mcp/oauth.js</code>）</li>\n<li>工具失败：<code>tools/call</code> <strong>始终</strong>回 MCP <code>isError:true</code> 文本；只有「根本不是一张合法工单」（坏协议、未知方法）才走协议级错误</li>\n<li>第一句连接语（测试锁死原文）：<code>src/mcp/instructions.js</code> 的 <code>CONNECT_LINE</code></li>\n</ul>\n<p><strong>行业里管这叫什么</strong> MCP、Streamable HTTP、JSON-RPC 2.0。Paste-URL 客户端把密钥放在路径里；OAuth 客户端用 Bearer 打规范地址 <code>/mcp</code>。</p>\n<hr />"},{"id":"6.-大门钥匙-长期门卡-vs-一次性访客码","title":"6. 大门钥匙：长期门卡 vs 一次性访客码","talkHtml":"<ul>\n<li>Arena 一类：地址里带着一串密钥，等于门卡。拿到完整地址的人可以读文件、在你电脑跑命令。<strong>不要发到群里。</strong></li>\n<li>ChatGPT Plus 连接器：给它看<strong>不带密钥</strong>的 <code>/mcp</code>，本机 Bridge 页出现一串几分钟有效的配对码，用过作废。</li>\n<li>点「重置 MCP 地址」：换新门卡，<strong>写进</strong>工作区 <code>.webagent/config.json</code>。旧链接立刻失效，重启也不会把旧卡读回来。</li>\n</ul>","metaphorHtml":"<p>小区门卡可以一直刷；快递柜取件码用一次就作废。给「只会贴网址、没有登录框」的客户端发门卡；给「官方连接器、能弹登录」的发取件码，避免长期密钥出现在聊天记录里。</p>","filesHtml":"<ul>\n<li>门卡：<code>config.secretKey</code>，启动时 <code>persistIdentity</code> 从 <code>config.json</code> 读回</li>\n<li>换卡：<code>POST /api/bridge/reset-secret</code> → <code>generateNewSecret()</code>（会 <code>store.patch</code>）</li>\n<li>取件码：<code>src/mcp/oauth.js</code> 的 pairing（内存，进程关了就没）</li>\n</ul>","jargonHtml":"<p>URL secret、OAuth 2.1 授权码 + PKCE。本仓库只实现连接器要用的那一小截，不是完整账号系统。</p>","html":"<p><strong>人话</strong></p>\n<ul>\n<li>Arena 一类：地址里带着一串密钥，等于门卡。拿到完整地址的人可以读文件、在你电脑跑命令。<strong>不要发到群里。</strong></li>\n<li>ChatGPT Plus 连接器：给它看<strong>不带密钥</strong>的 <code>/mcp</code>，本机 Bridge 页出现一串几分钟有效的配对码，用过作废。</li>\n<li>点「重置 MCP 地址」：换新门卡，<strong>写进</strong>工作区 <code>.webagent/config.json</code>。旧链接立刻失效，重启也不会把旧卡读回来。</li>\n</ul>\n<p><strong>比喻</strong> 小区门卡可以一直刷；快递柜取件码用一次就作废。给「只会贴网址、没有登录框」的客户端发门卡；给「官方连接器、能弹登录」的发取件码，避免长期密钥出现在聊天记录里。</p>\n<p><strong>落在仓库哪</strong></p>\n<ul>\n<li>门卡：<code>config.secretKey</code>，启动时 <code>persistIdentity</code> 从 <code>config.json</code> 读回</li>\n<li>换卡：<code>POST /api/bridge/reset-secret</code> → <code>generateNewSecret()</code>（会 <code>store.patch</code>）</li>\n<li>取件码：<code>src/mcp/oauth.js</code> 的 pairing（内存，进程关了就没）</li>\n</ul>\n<p><strong>行业里管这叫什么</strong> URL secret、OAuth 2.1 授权码 + PKCE。本仓库只实现连接器要用的那一小截，不是完整账号系统。</p>\n<hr />"},{"id":"7.-临时门牌-隧道","title":"7. 临时门牌（隧道）","talkHtml":"<p><code>127.0.0.1</code> 的意思永远是「<strong>这台正在执行的电脑自己</strong>」。云上的 ChatGPT 去连 127.0.0.1，连的是机房那台机器，不是你家。所以要一条公网 https，把请求转发进 48271。</p>\n<p>点「启动 Bridge」（默认 Cloudflare）会拉起 Quick Tunnel，最多等约 25 秒。成功则 MCP 变成 <code>https://….trycloudflare.com/mcp/…</code>。本机没装 cloudflared 时按钮仍成功，地址停在当前页面——本机预览可用，云上连不上。Named / ngrok 下拉<strong>还不会</strong>真的开那些隧道。</p>\n<p>域名<strong>每次启动都会变</strong>，必须重新复制。</p>","metaphorHtml":"<p>在信封上写「送到客厅」——每个房子都有客厅。你要的是「送到北京市某某临时门牌」。Quick Tunnel 像物业每天换一次的临时门牌：方便，但朋友得拿新地址。不要假装已经办了固定域名（Named）或另一家快递（ngrok），源码里没有对应实现。</p>","filesHtml":"<ul>\n<li>拉隧道：<code>src/api/routes.js</code> 的 <code>POST /bridge/start</code> → <code>src/tunnel/cloudflared.js</code> 的 <code>startQuickTunnel</code></li>\n<li>停：<code>/bridge/stop</code> 与登出都 <code>stopTunnel</code></li>\n<li>工作台只负责按钮和 toast：<code>workbench/js/bridge.js</code> 的 <code>startBridge</code> / <code>paintBridge</code>（入口仍是 <code>workbench/app.js</code>）</li>\n</ul>","jargonHtml":"<p>Cloudflare Quick Tunnel（无需 Cloudflare 账号的临时 HTTPS 反代）。不是 VPN，也不是把整个电脑暴露成远程桌面，只转发你指定的那一个本地端口。本仓库再加一层：<strong>看 Cloudflare 头和 Host 名</strong>，公网来的请求不能用未鉴权的 <code>/api</code>（<code>src/utils/localControl.js</code>）。不能只看「是不是 127.0.0.1」——cloudflared 连的就是本机回环。</p>","html":"<p><strong>人话</strong> <code>127.0.0.1</code> 的意思永远是「<strong>这台正在执行的电脑自己</strong>」。云上的 ChatGPT 去连 127.0.0.1，连的是机房那台机器，不是你家。所以要一条公网 https，把请求转发进 48271。</p>\n<p>点「启动 Bridge」（默认 Cloudflare）会拉起 Quick Tunnel，最多等约 25 秒。成功则 MCP 变成 <code>https://….trycloudflare.com/mcp/…</code>。本机没装 cloudflared 时按钮仍成功，地址停在当前页面——本机预览可用，云上连不上。Named / ngrok 下拉<strong>还不会</strong>真的开那些隧道。</p>\n<p>域名<strong>每次启动都会变</strong>，必须重新复制。</p>\n<p><strong>比喻</strong> 在信封上写「送到客厅」——每个房子都有客厅。你要的是「送到北京市某某临时门牌」。Quick Tunnel 像物业每天换一次的临时门牌：方便，但朋友得拿新地址。不要假装已经办了固定域名（Named）或另一家快递（ngrok），源码里没有对应实现。</p>\n<p><strong>落在仓库哪</strong></p>\n<ul>\n<li>拉隧道：<code>src/api/routes.js</code> 的 <code>POST /bridge/start</code> → <code>src/tunnel/cloudflared.js</code> 的 <code>startQuickTunnel</code></li>\n<li>停：<code>/bridge/stop</code> 与登出都 <code>stopTunnel</code></li>\n<li>工作台只负责按钮和 toast：<code>workbench/js/bridge.js</code> 的 <code>startBridge</code> / <code>paintBridge</code>（入口仍是 <code>workbench/app.js</code>）</li>\n</ul>\n<p><strong>行业里管这叫什么</strong> Cloudflare Quick Tunnel（无需 Cloudflare 账号的临时 HTTPS 反代）。不是 VPN，也不是把整个电脑暴露成远程桌面，只转发你指定的那一个本地端口。本仓库再加一层：<strong>看 Cloudflare 头和 Host 名</strong>，公网来的请求不能用未鉴权的 <code>/api</code>（<code>src/utils/localControl.js</code>）。不能只看「是不是 127.0.0.1」——cloudflared 连的就是本机回环。</p>\n<hr />"},{"id":"8.-扳手箱-能干什么-故意不让干什么","title":"8. 扳手箱：能干什么、故意不让干什么","talkHtml":"<p>大约 25 个正式工具名（读目录、搜、读文件、打补丁、跑命令…）。网页若喊 <code>bash</code> / <code>cat</code> / <code>ls</code>，主机会翻译成正式名，但 <strong>Windows 上命令字符串仍走 PowerShell</strong>。</p>\n<p>三档工牌：</p>\n<ul>\n<li><strong>Ask / Plan</strong>：只能看。喊「改文件」会被拒。</li>\n<li><strong>Code</strong>（界面上 Agent 默认就是它）：可以补丁、可以跑测试。</li>\n</ul>\n<p>本机 Chat 看你点的 Agent ▾。网页工单默认按 Code；若对方在工单里带 <code>params._meta.mode=ask</code>，也可以改成只读。</p>\n<p>改已有文件优先「只换这一段」（补丁），并核对该段是否还是刚才读到的版本（哈希）。从未读过会要你带哈希再试；读完磁盘又被别人改了会说过期。读过的哈希记在 <code>.webagent/read-hashes.json</code>，关掉程序再开还在；点「清除本轮统计」会删掉。</p>\n<p><code>.env</code>、密钥、SSH、<code>.webagent/config.json</code> 读不出来。<code>rm -rf</code> 一类要额外确认。</p>","metaphorHtml":"<ul>\n<li>Ask 是参观证，Code 是施工证。</li>\n<li>哈希像你复印文件时盖的「此页指纹」：回去改的时候先对指纹，对不上说明有人动过这一页，禁止凭记忆整页覆盖。</li>\n<li>敏感文件像保险柜：参观证、施工证都打不开。</li>\n</ul>","filesHtml":"<div class=\"table-wrap\"><table><thead><tr>\n<th>事</th>\n<th>文件</th>\n</tr></thead><tbody>\n<tr>\n<td>名单、工牌锁、危险命令闸</td>\n<td><code>src/tools/index.js</code></td>\n</tr>\n<tr>\n<td>别名 <code>bash</code>→<code>run_command</code></td>\n<td><code>src/tools/normalize.js</code></td>\n</tr>\n<tr>\n<td>补丁 + 路径沙箱</td>\n<td><code>src/tools/patchEngine.js</code></td>\n</tr>\n<tr>\n<td>读哈希落盘</td>\n<td><code>src/tools/readCache.js</code></td>\n</tr>\n<tr>\n<td>敏感路径</td>\n<td><code>src/tools/sensitive.js</code></td>\n</tr>\n<tr>\n<td>跑命令（Windows=PowerShell）</td>\n<td><code>src/tools/executor.js</code></td>\n</tr>\n<tr>\n<td>网页默认 Code</td>\n<td><code>src/mcp/server.js</code> 的 <code>remoteToolMode</code></td>\n</tr>\n</tbody></table></div>","jargonHtml":"<p>工具调用（tool calling）、内容寻址预检（hash / optimistic concurrency）、密钥扫描（secret redaction）。本机 Chat 的 Plan「三模型博弈」在源码里<strong>不请求任何模型 HTTP</strong>，三份答案是写死的字符串——不要把它写成已经接了三家大模型。</p>","html":"<p><strong>人话</strong> 大约 25 个正式工具名（读目录、搜、读文件、打补丁、跑命令…）。网页若喊 <code>bash</code> / <code>cat</code> / <code>ls</code>，主机会翻译成正式名，但 <strong>Windows 上命令字符串仍走 PowerShell</strong>。</p>\n<p>三档工牌：</p>\n<ul>\n<li><strong>Ask / Plan</strong>：只能看。喊「改文件」会被拒。</li>\n<li><strong>Code</strong>（界面上 Agent 默认就是它）：可以补丁、可以跑测试。</li>\n</ul>\n<p>本机 Chat 看你点的 Agent ▾。网页工单默认按 Code；若对方在工单里带 <code>params._meta.mode=ask</code>，也可以改成只读。</p>\n<p>改已有文件优先「只换这一段」（补丁），并核对该段是否还是刚才读到的版本（哈希）。从未读过会要你带哈希再试；读完磁盘又被别人改了会说过期。读过的哈希记在 <code>.webagent/read-hashes.json</code>，关掉程序再开还在；点「清除本轮统计」会删掉。</p>\n<p><code>.env</code>、密钥、SSH、<code>.webagent/config.json</code> 读不出来。<code>rm -rf</code> 一类要额外确认。</p>\n<p><strong>比喻</strong></p>\n<ul>\n<li>Ask 是参观证，Code 是施工证。</li>\n<li>哈希像你复印文件时盖的「此页指纹」：回去改的时候先对指纹，对不上说明有人动过这一页，禁止凭记忆整页覆盖。</li>\n<li>敏感文件像保险柜：参观证、施工证都打不开。</li>\n</ul>\n<p><strong>落在仓库哪</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>事</th>\n<th>文件</th>\n</tr></thead><tbody>\n<tr>\n<td>名单、工牌锁、危险命令闸</td>\n<td><code>src/tools/index.js</code></td>\n</tr>\n<tr>\n<td>别名 <code>bash</code>→<code>run_command</code></td>\n<td><code>src/tools/normalize.js</code></td>\n</tr>\n<tr>\n<td>补丁 + 路径沙箱</td>\n<td><code>src/tools/patchEngine.js</code></td>\n</tr>\n<tr>\n<td>读哈希落盘</td>\n<td><code>src/tools/readCache.js</code></td>\n</tr>\n<tr>\n<td>敏感路径</td>\n<td><code>src/tools/sensitive.js</code></td>\n</tr>\n<tr>\n<td>跑命令（Windows=PowerShell）</td>\n<td><code>src/tools/executor.js</code></td>\n</tr>\n<tr>\n<td>网页默认 Code</td>\n<td><code>src/mcp/server.js</code> 的 <code>remoteToolMode</code></td>\n</tr>\n</tbody></table></div>\n<p><strong>行业里管这叫什么</strong> 工具调用（tool calling）、内容寻址预检（hash / optimistic concurrency）、密钥扫描（secret redaction）。本机 Chat 的 Plan「三模型博弈」在源码里<strong>不请求任何模型 HTTP</strong>，三份答案是写死的字符串——不要把它写成已经接了三家大模型。</p>\n<hr />"},{"id":"9.-为什么文件夹要拆开-不是为了好看","title":"9. 为什么文件夹要拆开（不是为了好看）","talkHtml":"<div class=\"table-wrap\"><table><thead><tr>\n<th>拆法</th>\n<th>人话</th>\n<th>不拆的隐患</th>\n</tr></thead><tbody>\n<tr>\n<td>面板 <code>workbench/</code> vs 车间 <code>agent-host/</code></td>\n<td>按钮改不了磁盘，所有动手都进 host</td>\n<td>换皮（真 VS Code）时要把补丁引擎再写一遍</td>\n</tr>\n<tr>\n<td>插件 <code>extension/</code> 只打 HTTP</td>\n<td>VS Code 侧栏是另一块面板，扳手还在 host</td>\n<td>把工具写进编辑器内核，换皮时就要重写扳手</td>\n</tr>\n<tr>\n<td>默认工作区不是仓库根</td>\n<td>计算器 ≠ Web Agent 程序</td>\n<td>Agent 可能改掉 <code>run-webagent.cmd</code> 和说明书</td>\n</tr>\n<tr>\n<td><code>.webagent</code> 在工作区里</td>\n<td>密钥、技能属于「正在修的那台车」</td>\n<td>本仓库 gitignore 用 <code>**/.webagent/config.json</code>；挂别人的仓库时启动会给那份 git 补 ignore，技能仍可提交</td>\n</tr>\n<tr>\n<td><code>webagent-repro/</code> 冻结</td>\n<td>最早那版 Bridge，对照用</td>\n<td><code>npm start</code> 端口和工具都对不上现在的指南</td>\n</tr>\n<tr>\n<td>不 vendor DeepSeek++</td>\n<td>扩展有自己的商店更新</td>\n<td>拷源码进仓库会过期，还多一个无关的本机 Shell</td>\n</tr>\n<tr>\n<td>不 vendor Chat Plus</td>\n<td>GPL v3；从 GitHub 自己编译</td>\n<td>拷进本仓库会传染许可证；也不要为连 Bridge 再装 MCP-Gateway</td>\n</tr>\n</tbody></table></div>","metaphorHtml":"","filesHtml":"<p>对照表见 <a href=\"#/workflow\">组件说明.md</a> 第 5–6 节。启动脚本<strong>不会</strong>进入 <code>webagent-repro/</code>。</p>","jargonHtml":"<p>关注点分离、可替换 UI、工作区与工具链隔离。</p>","html":"<p><strong>人话 + 隐患</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>拆法</th>\n<th>人话</th>\n<th>不拆的隐患</th>\n</tr></thead><tbody>\n<tr>\n<td>面板 <code>workbench/</code> vs 车间 <code>agent-host/</code></td>\n<td>按钮改不了磁盘，所有动手都进 host</td>\n<td>换皮（真 VS Code）时要把补丁引擎再写一遍</td>\n</tr>\n<tr>\n<td>插件 <code>extension/</code> 只打 HTTP</td>\n<td>VS Code 侧栏是另一块面板，扳手还在 host</td>\n<td>把工具写进编辑器内核，换皮时就要重写扳手</td>\n</tr>\n<tr>\n<td>默认工作区不是仓库根</td>\n<td>计算器 ≠ Web Agent 程序</td>\n<td>Agent 可能改掉 <code>run-webagent.cmd</code> 和说明书</td>\n</tr>\n<tr>\n<td><code>.webagent</code> 在工作区里</td>\n<td>密钥、技能属于「正在修的那台车」</td>\n<td>本仓库 gitignore 用 <code>**/.webagent/config.json</code>；挂别人的仓库时启动会给那份 git 补 ignore，技能仍可提交</td>\n</tr>\n<tr>\n<td><code>webagent-repro/</code> 冻结</td>\n<td>最早那版 Bridge，对照用</td>\n<td><code>npm start</code> 端口和工具都对不上现在的指南</td>\n</tr>\n<tr>\n<td>不 vendor DeepSeek++</td>\n<td>扩展有自己的商店更新</td>\n<td>拷源码进仓库会过期，还多一个无关的本机 Shell</td>\n</tr>\n<tr>\n<td>不 vendor Chat Plus</td>\n<td>GPL v3；从 GitHub 自己编译</td>\n<td>拷进本仓库会传染许可证；也不要为连 Bridge 再装 MCP-Gateway</td>\n</tr>\n</tbody></table></div>\n<p><strong>落在仓库哪</strong> 对照表见 <a href=\"#/workflow\">组件说明.md</a> 第 5–6 节。启动脚本<strong>不会</strong>进入 <code>webagent-repro/</code>。</p>\n<p><strong>行业里管这叫什么</strong> 关注点分离、可替换 UI、工作区与工具链隔离。</p>\n<hr />"},{"id":"10.-一次-网页让你修测试-在机器里怎么走","title":"10. 一次「网页让你修测试」在机器里怎么走","talkHtml":"<p>你说话 → 扩展替模型填工单 → 公网门牌转到你家 48271 → 核对钥匙 → 读文件拿指纹 → 打补丁 → 跑测试 → 工作台右侧刷出卡片 → 磁盘已经变了。</p>","metaphorHtml":"<p>师傅口述「刹车异响」→ 监工（DeepSeek++）写成标准工序单 → 快递按临时门牌送到你家后厨 → 核对门卡 → 复印相关页并盖指纹 → 只改那一段 → 打火试车 → 店堂屏幕上出现「刚干了哪一步」。</p>","filesHtml":"<ol>\n<li>浏览器 <code>chat.deepseek.com</code>（扩展不是本仓库的代码）。</li>\n<li>HTTPS <code>https://….trycloudflare.com/mcp/&lt;密钥&gt;</code>。</li>\n<li><code>src/mcp/server.js</code>：<code>extractToken</code> → 与 <code>config.secretKey</code> 比对。</li>\n<li><code>initialize</code> 把 <code>src/mcp/instructions.js</code> 的规则塞给对方。</li>\n<li><code>tools/call</code> → <code>callTool(name, args, 'code')</code>（除非 <code>_meta.mode</code> 另写）。</li>\n<li><code>read_files</code> → sha256 写入 <code>read-hashes.json</code>。</li>\n<li><code>apply_patch</code>：对指纹；写 <code>.tmp.*</code> 再改名，避免写到一半停电留下半份文件。</li>\n<li><code>run_command</code>：工作区里跑，Windows 是 PowerShell。</li>\n<li><code>src/utils/eventBus.js</code> 经 <strong>3000</strong> 的 <code>/ws</code> 推到 <code>workbench/app.js</code>，BRIDGE 出卡片。48271 不挂 WebSocket。</li>\n</ol>\n<p>本机 Chat 从第 3 步换成 <strong>3000</strong> 的 <code>POST /api/chat</code> → <code>runChat.js</code>，后面相同。</p>","jargonHtml":"<p>远程 tool-calling 闭环、原子替换（temp + rename）、事件总线刷新 UI。</p>","html":"<p>以 DeepSeek 网页为例（Arena 把提示词当第一句，后面同一套扳手）。</p>\n<p><strong>人话</strong> 你说话 → 扩展替模型填工单 → 公网门牌转到你家 48271 → 核对钥匙 → 读文件拿指纹 → 打补丁 → 跑测试 → 工作台右侧刷出卡片 → 磁盘已经变了。</p>\n<p><strong>比喻</strong> 师傅口述「刹车异响」→ 监工（DeepSeek++）写成标准工序单 → 快递按临时门牌送到你家后厨 → 核对门卡 → 复印相关页并盖指纹 → 只改那一段 → 打火试车 → 店堂屏幕上出现「刚干了哪一步」。</p>\n<p><strong>落地闭环（源码路径）</strong></p>\n<ol>\n<li>浏览器 <code>chat.deepseek.com</code>（扩展不是本仓库的代码）。</li>\n<li>HTTPS <code>https://….trycloudflare.com/mcp/&lt;密钥&gt;</code>。</li>\n<li><code>src/mcp/server.js</code>：<code>extractToken</code> → 与 <code>config.secretKey</code> 比对。</li>\n<li><code>initialize</code> 把 <code>src/mcp/instructions.js</code> 的规则塞给对方。</li>\n<li><code>tools/call</code> → <code>callTool(name, args, 'code')</code>（除非 <code>_meta.mode</code> 另写）。</li>\n<li><code>read_files</code> → sha256 写入 <code>read-hashes.json</code>。</li>\n<li><code>apply_patch</code>：对指纹；写 <code>.tmp.*</code> 再改名，避免写到一半停电留下半份文件。</li>\n<li><code>run_command</code>：工作区里跑，Windows 是 PowerShell。</li>\n<li><code>src/utils/eventBus.js</code> 经 <strong>3000</strong> 的 <code>/ws</code> 推到 <code>workbench/app.js</code>，BRIDGE 出卡片。48271 不挂 WebSocket。</li>\n</ol>\n<p>本机 Chat 从第 3 步换成 <strong>3000</strong> 的 <code>POST /api/chat</code> → <code>runChat.js</code>，后面相同。</p>\n<p><strong>行业里管这叫什么</strong> 远程 tool-calling 闭环、原子替换（temp + rename）、事件总线刷新 UI。</p>\n<hr />"},{"id":"11.-已经补上的隐患-和仍不要写成-已经接上-的","title":"11. 已经补上的隐患，和仍不要写成「已经接上」的","talkHtml":"","metaphorHtml":"","filesHtml":"","jargonHtml":"","html":"<p>下面表格里的做法都已经在源码里，不是愿望清单。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>隐患（不这样会怎样）</th>\n<th>现在的做法</th>\n<th>文件</th>\n</tr></thead><tbody>\n<tr>\n<td>点「启动 Bridge」只亮灯、云上仍连 127.0.0.1</td>\n<td>Cloudflare 时 <code>await startQuickTunnel</code>；失败仍 200，本机预览走当前页面</td>\n<td><code>src/api/routes.js</code>、<code>src/tunnel/cloudflared.js</code></td>\n</tr>\n<tr>\n<td>重置密钥后重启，旧门卡又活了</td>\n<td><code>generateNewSecret</code> 写入 <code>config.json</code></td>\n<td><code>src/config.js</code></td>\n</tr>\n<tr>\n<td>网页 Agent 断线再来，不带哈希就打补丁失败</td>\n<td>哈希落盘 <code>read-hashes.json</code>；「清除本轮统计」删除</td>\n<td><code>src/tools/readCache.js</code></td>\n</tr>\n<tr>\n<td>网页工单完全不上工牌锁</td>\n<td>远程默认 Code；可 <code>_meta.mode</code></td>\n<td><code>src/mcp/server.js</code></td>\n</tr>\n<tr>\n<td>隧道把未鉴权 <code>/api</code> 送到公网</td>\n<td>mcp/工作台 <code>/api</code> 见隧道头或公网 Host 则 404；<code>/ws</code> 非本机直接关掉</td>\n<td><code>src/utils/localControl.js</code>、<code>src/index.js</code></td>\n</tr>\n<tr>\n<td>默认听所有网卡，局域网可打 <code>/api</code></td>\n<td>默认 <code>WEBAGENT_BIND=127.0.0.1</code></td>\n<td><code>src/config.js</code></td>\n</tr>\n<tr>\n<td>符号链接指到工作区外</td>\n<td><code>realpath</code> 后必须仍在工作区；列目录不跟随外链</td>\n<td><code>src/tools/patchEngine.js</code></td>\n</tr>\n<tr>\n<td>远程 <code>run_command</code> 几乎任意 Shell</td>\n<td>危险命令（含 <code>git push</code>、`curl … \\</td>\n<td>sh`）要确认；Unix 杀进程组</td>\n<td><code>src/tools/index.js</code>、<code>executor.js</code></td>\n</tr>\n<tr>\n<td>任意网页跨域打本机未鉴权 <code>/api</code></td>\n<td>MCP CORS 白名单；<code>/api</code> 非本机 Origin/Referer 则 404。密钥仍管 <code>/mcp</code></td>\n<td><code>src/utils/corsAllow.js</code></td>\n</tr>\n<tr>\n<td>网页 VS Code 无登录</td>\n<td>默认 password；口令在 <code>.local/share/code-server/webagent-password</code>；trusted-origins 不再是 <code>*</code></td>\n<td><code>scripts/codeServerAuth.js</code></td>\n</tr>\n<tr>\n<td>工作台保存绕过补丁/敏感闸</td>\n<td><code>PUT /api/files/content</code> 与新建 Skill 走 <code>write_file</code>（原子替换）；<code>.env</code> 写不进去</td>\n<td><code>src/api/routes.js</code>、<code>fileOps.js</code></td>\n</tr>\n<tr>\n<td>挂 <code>D:\\\\code\\\\my-app</code> 时密钥被 Git 提交</td>\n<td>启动写入 <code>.webagent/.gitignore</code>；若该目录是 git 仓库再补仓库根 ignore；<code>config.json</code> chmod 0600</td>\n<td><code>src/models/store.js</code></td>\n</tr>\n<tr>\n<td><code>review</code> Skill 只有半句话，模型不知道怎么审</td>\n<td>完整只读审查流程</td>\n<td><code>workspace/.webagent/skills/review/SKILL.md</code></td>\n</tr>\n</tbody></table></div>\n<p>仍不要写成已经接上的：Named / ngrok 不 spawn；Plan 三模型不打真实 HTTP；没有浏览器自动点选测试（Playwright）。</p>\n<hr />"},{"id":"12.-术语对照-先人话","title":"12. 术语对照（先人话）","talkHtml":"","metaphorHtml":"","filesHtml":"","jargonHtml":"","html":"<p>读到缩写时回这里。解释以本仓库行为为准。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你听到的词</th>\n<th>人话</th>\n</tr></thead><tbody>\n<tr>\n<td>Agent</td>\n<td>会自己决定下一步、去调工具的助手，不是只会聊天</td>\n</tr>\n<tr>\n<td>MCP</td>\n<td>网页 AI 给本机填的标准工单格式</td>\n</tr>\n<tr>\n<td>Bridge</td>\n<td>工作台上「开门让网页进来」的那一页</td>\n</tr>\n<tr>\n<td>隧道 / Quick Tunnel</td>\n<td>给家里 48271 办的临时公网门牌</td>\n</tr>\n<tr>\n<td><code>127.0.0.1</code></td>\n<td>「这台电脑自己」，每台机器都有一个，不能当你家地址给云用</td>\n</tr>\n<tr>\n<td>工作区</td>\n<td>Agent 能改的那一个文件夹</td>\n</tr>\n<tr>\n<td>sha256 / 哈希</td>\n<td>文件内容的指纹，用来发现「你读完之后有人改过」</td>\n</tr>\n<tr>\n<td>Ask / Plan / Code</td>\n<td>参观证 / 开会不许动手 / 施工证</td>\n</tr>\n<tr>\n<td>OAuth / 配对码</td>\n<td>给会弹登录的客户端用的一次性访客码</td>\n</tr>\n<tr>\n<td>Streamable HTTP</td>\n<td>工单用普通网页那种请求来回传，不是另接一套专用线</td>\n</tr>\n<tr>\n<td>WebSocket <code>/ws</code></td>\n<td>车间往店堂屏幕推「刚干完哪一步」</td>\n</tr>\n<tr>\n<td>Skill</td>\n<td>放在工作区里的一篇作业指导书，模型用 <code>load_skill</code> 读</td>\n</tr>\n<tr>\n<td>CORS</td>\n<td>浏览器问「那个网页能不能看这次回答」。本仓库只给扩展和名单里的聊天站点头；<strong>不是</strong>门卡</td>\n</tr>\n</tbody></table></div>\n<hr />"},{"id":"13.-读完这篇之后","title":"13. 读完这篇之后","talkHtml":"","metaphorHtml":"","filesHtml":"","jargonHtml":"","html":"<div class=\"table-wrap\"><table><thead><tr>\n<th>你想做的事</th>\n<th>去哪</th>\n</tr></thead><tbody>\n<tr>\n<td>从安装点到改文件</td>\n<td><a href=\"./使用指南.md\">使用指南.md</a></td>\n</tr>\n<tr>\n<td>DeepSeek 网页 + 扩展</td>\n<td><a href=\"./网页DeepSeek使用指南.md\">网页DeepSeek使用指南.md</a></td>\n</tr>\n<tr>\n<td>多个网页 AI + Chat Plus</td>\n<td><a href=\"./网页ChatPlus使用指南.md\">网页ChatPlus使用指南.md</a></td>\n</tr>\n<tr>\n<td>浏览器里真 VS Code</td>\n<td><a href=\"./网页VSCode使用指南.md\">网页VSCode使用指南.md</a></td>\n</tr>\n<tr>\n<td>GitHub 上每个目录是谁</td>\n<td><a href=\"#/workflow\">组件说明.md</a></td>\n</tr>\n<tr>\n<td>函数逐步、分支不漏</td>\n<td><a href=\"#/impl\">技术实现.md</a></td>\n</tr>\n<tr>\n<td>导读/直译做成可点的图</td>\n<td><a href=\"./docs-site/README.md\">docs-site/</a></td>\n</tr>\n<tr>\n<td>子文件夹说明书索引</td>\n<td><a href=\"#/graph\">总览.md</a></td>\n</tr>\n<tr>\n<td>测了什么</td>\n<td><a href=\"./测试说明.md\">测试说明.md</a></td>\n</tr>\n</tbody></table></div>\n<p>以后若架构、数据流或「为什么这样设计」变了，应改<strong>本文件对应小节</strong>（约定在 <code>workspace/.webagent/skills/docs-sync/SKILL.md</code>），不要只改代码不改这篇。</p>"}]},"impl":{"title":"代码直译技术实现","toc":[{"level":2,"text":"0. 总链条（文件名穿起来）","id":"0.-总链条-文件名穿起来"},{"level":2,"text":"1. 启动脚本","id":"1.-启动脚本"},{"level":3,"text":"1.1 run-webagent.cmd","id":"1.1-run-webagent.cmd"},{"level":3,"text":"1.2 run-webagent.sh","id":"1.2-run-webagent.sh"},{"level":3,"text":"1.3 run-webagent-vscode.cmd","id":"1.3-run-webagent-vscode.cmd"},{"level":3,"text":"1.4 webagent-core/scripts/ensure-code-server.js","id":"1.4-webagent-core/scripts/ensure-code-server.js"},{"level":4,"text":"npmCmd","id":"npmcmd"},{"level":4,"text":"runNpm(args, cwd)","id":"runnpm-args-cwd"},{"level":4,"text":"findEntry","id":"findentry"},{"level":4,"text":"ensureVscodeDeps(entry)","id":"ensurevscodedeps-entry"},{"level":4,"text":"ensure","id":"ensure"},{"level":4,"text":"syncExtension","id":"syncextension"},{"level":3,"text":"1.5 webagent-core/scripts/run-code-oss.js","id":"1.5-webagent-core/scripts/run-code-oss.js"},{"level":4,"text":"顶层","id":"顶层"},{"level":4,"text":"waitHealth(url, timeoutMs)","id":"waithealth-url-timeoutms"},{"level":4,"text":"run(command, args, opts)","id":"run-command-args-opts"},{"level":4,"text":"main（async）","id":"main-async"},{"level":2,"text":"2. 进程心脏","id":"2.-进程心脏"},{"level":3,"text":"2.1 src/config.js（47 行）","id":"2.1-src/config.js-47-行"},{"level":4,"text":"模块加载时","id":"模块加载时"},{"level":4,"text":"generateNewSecret","id":"generatenewsecret"},{"level":4,"text":"persistIdentity(store)","id":"persistidentity-store"},{"level":3,"text":"2.2 src/index.js（136 行）","id":"2.2-src/index.js-136-行"},{"level":3,"text":"2.3 src/utils/localControl.js（55 行）","id":"2.3-src/utils/localcontrol.js-55-行"},{"level":3,"text":"2.4 src/utils/corsAllow.js（115 行）","id":"2.4-src/utils/corsallow.js-115-行"},{"level":2,"text":"3. 磁盘上的工作区状态","id":"3.-磁盘上的工作区状态"},{"level":3,"text":"3.1 src/models/store.js","id":"3.1-src/models/store.js"},{"level":4,"text":"dir / storePath","id":"dir-/-storepath"},{"level":4,"text":"defaults","id":"defaults"},{"level":4,"text":"load","id":"load"},{"level":4,"text":"save(next)","id":"save-next"},{"level":4,"text":"protectWorkspaceSecrets","id":"protectworkspacesecrets"},{"level":4,"text":"patch(partial)","id":"patch-partial"},{"level":3,"text":"3.2 src/models/customizations.js","id":"3.2-src/models/customizations.js"},{"level":4,"text":"file","id":"file"},{"level":4,"text":"defaults","id":"defaults"},{"level":4,"text":"loadCustom","id":"loadcustom"},{"level":4,"text":"saveCustom(next)","id":"savecustom-next"},{"level":4,"text":"patchCustom(partial)","id":"patchcustom-partial"},{"level":3,"text":"3.3 src/models/profile.js","id":"3.3-src/models/profile.js"},{"level":4,"text":"detectEnvironment","id":"detectenvironment"},{"level":4,"text":"readJson(file)","id":"readjson-file"},{"level":4,"text":"exists(root, rel)","id":"exists-root-rel"},{"level":4,"text":"detectTechStack(workspaceRoot)","id":"detecttechstack-workspaceroot"},{"level":4,"text":"resolveEnvironment(custom)","id":"resolveenvironment-custom"},{"level":4,"text":"resolveTechStack(custom, workspaceRoot)","id":"resolvetechstack-custom-workspaceroot"},{"level":4,"text":"languageLabel(code)","id":"languagelabel-code"},{"level":4,"text":"osLabel(os)","id":"oslabel-os"},{"level":4,"text":"formatWorkspaceContext(custom, skills)","id":"formatworkspacecontext-custom-skills"},{"level":4,"text":"markdownPreference / markdownTechStack","id":"markdownpreference-/-markdowntechstack"},{"level":3,"text":"3.4 src/models/memory.js","id":"3.4-src/models/memory.js"},{"level":4,"text":"memoryDir / dayFile","id":"memorydir-/-dayfile"},{"level":4,"text":"remember({ text, day })","id":"remember-text-day"},{"level":4,"text":"recall({ limit = 40, day })","id":"recall-limit-40-day"},{"level":2,"text":"4. MCP 层（网页 Agent 进门）","id":"4.-mcp-层-网页-agent-进门"},{"level":3,"text":"4.1 src/mcp/errors.js","id":"4.1-src/mcp/errors.js"},{"level":4,"text":"类 ProtocolError / ExecutionError","id":"类-protocolerror-/-executionerror"},{"level":4,"text":"classifyToolError(err)","id":"classifytoolerror-err"},{"level":4,"text":"publicError(err)","id":"publicerror-err"},{"level":3,"text":"4.2 src/mcp/session.js","id":"4.2-src/mcp/session.js"},{"level":4,"text":"sessionKey(req)","id":"sessionkey-req"},{"level":4,"text":"touch(req, extra = {})","id":"touch-req-extra"},{"level":4,"text":"snapshot","id":"snapshot"},{"level":4,"text":"reset","id":"reset"},{"level":3,"text":"4.3 src/mcp/budget.js","id":"4.3-src/mcp/budget.js"},{"level":4,"text":"estimateTokens(text)","id":"estimatetokens-text"},{"level":4,"text":"clipText(text, maxChars)","id":"cliptext-text-maxchars"},{"level":4,"text":"clipJson(value, maxChars)","id":"clipjson-value-maxchars"},{"level":3,"text":"4.4 src/mcp/instructions.js","id":"4.4-src/mcp/instructions.js"},{"level":4,"text":"常量 CONNECT_LINE","id":"常量-connect_line"},{"level":4,"text":"getBootstrapPrompt(mcpUrl)","id":"getbootstrapprompt-mcpurl"},{"level":4,"text":"SERVER_INSTRUCTIONS","id":"server_instructions"},{"level":4,"text":"getInstructions","id":"getinstructions"},{"level":3,"text":"4.5 src/mcp/clients.js","id":"4.5-src/mcp/clients.js"},{"level":4,"text":"hydrateClient(client, urls)","id":"hydrateclient-client-urls"},{"level":4,"text":"listClients(urls)","id":"listclients-urls"},{"level":4,"text":"getClient(id, urls)","id":"getclient-id-urls"},{"level":3,"text":"4.6 src/mcp/resources.js","id":"4.6-src/mcp/resources.js"},{"level":4,"text":"listResources","id":"listresources"},{"level":4,"text":"readResource(uri) — switch","id":"readresource-uri-switch"},{"level":3,"text":"4.7 src/mcp/oauth.js（379 行）","id":"4.7-src/mcp/oauth.js-379-行"},{"level":4,"text":"now / randomToken(prefix, bytes=24) / randomPairingCode","id":"now-/-randomtoken-prefix-bytes-24-/-randompairingcode"},{"level":4,"text":"requestOrigin(req)","id":"requestorigin-req"},{"level":4,"text":"issuePairing / snapshotPairing / ensurePairing","id":"issuepairing-/-snapshotpairing-/-ensurepairing"},{"level":4,"text":"consumePairing(code)","id":"consumepairing-code"},{"level":4,"text":"authorizationServerMetadata(origin) / protectedResourceMetadata(origin) / wwwAuthenticate(origin)","id":"authorizationservermetadata-origin-/-protectedresourcemetadata-origin-/-wwwauthe"},{"level":4,"text":"registerClient(body)","id":"registerclient-body"},{"level":4,"text":"s256(verifier)","id":"s256-verifier"},{"level":4,"text":"issueAccess(clientId)","id":"issueaccess-clientid"},{"level":4,"text":"verifyAccessToken(token)","id":"verifyaccesstoken-token"},{"level":4,"text":"revokeAll","id":"revokeall"},{"level":4,"text":"escapeHtml / authorizeHtml(query, error)","id":"escapehtml-/-authorizehtml-query-error"},{"level":4,"text":"completeAuthorize(body)","id":"completeauthorize-body"},{"level":4,"text":"handleToken(body)","id":"handletoken-body"},{"level":4,"text":"tokenResponse / sendError","id":"tokenresponse-/-senderror"},{"level":4,"text":"路由（全部挂在 oauth.router 上，由 index 挂到 app 根）","id":"路由-全部挂在-oauth.router-上-由-index-挂到-app-根"},{"level":3,"text":"4.8 src/mcp/server.js（277 行）","id":"4.8-src/mcp/server.js-277-行"},{"level":4,"text":"extractToken(req)","id":"extracttoken-req"},{"level":4,"text":"isAuthorized / rejectUnauthorized / requireAuth","id":"isauthorized-/-rejectunauthorized-/-requireauth"},{"level":4,"text":"wantsSse(req)","id":"wantssse-req"},{"level":4,"text":"sessionIdFor(req)","id":"sessionidfor-req"},{"level":4,"text":"sendJsonRpc(req, res, payload, httpStatus=200)","id":"sendjsonrpc-req-res-payload-httpstatus-200"},{"level":4,"text":"builtinPrompts / promptsFromCustom","id":"builtinprompts-/-promptsfromcustom"},{"level":4,"text":"pickProtocol(params)","id":"pickprotocol-params"},{"level":4,"text":"handleRpc(req) — switch (method)","id":"handlerpc-req-switch-method"},{"level":4,"text":"hostStatus","id":"hoststatus"},{"level":4,"text":"handlePost","id":"handlepost"},{"level":4,"text":"handleGet","id":"handleget"},{"level":2,"text":"5. 工具层（真正改磁盘）","id":"5.-工具层-真正改磁盘"},{"level":3,"text":"5.1 src/utils/eventBus.js","id":"5.1-src/utils/eventbus.js"},{"level":3,"text":"5.2 src/utils/diff.js","id":"5.2-src/utils/diff.js"},{"level":3,"text":"5.3 src/tools/sensitive.js","id":"5.3-src/tools/sensitive.js"},{"level":4,"text":"toPosix / globMatch","id":"toposix-/-globmatch"},{"level":4,"text":"loadCustomPatterns","id":"loadcustompatterns"},{"level":4,"text":"isSensitive(relPath)","id":"issensitive-relpath"},{"level":4,"text":"isNoise / isHidden / assertNotSensitive","id":"isnoise-/-ishidden-/-assertnotsensitive"},{"level":3,"text":"5.4 src/tools/patchEngine.js","id":"5.4-src/tools/patchengine.js"},{"level":4,"text":"computeHash(content)","id":"computehash-content"},{"level":4,"text":"toPosixRel / isInsideWorkspace / resolveSafePath(relPath)","id":"toposixrel-/-isinsideworkspace-/-resolvesafepath-relpath"},{"level":4,"text":"parseSearchReplaceBlocks(patchText)","id":"parsesearchreplaceblocks-patchtext"},{"level":4,"text":"applyPatch({ filePath, patch, expectedHash=null, dryRun=false })","id":"applypatch-filepath-patch-expectedhash-null-dryrun-false"},{"level":3,"text":"5.5 src/tools/fileOps.js","id":"5.5-src/tools/fileops.js"},{"level":4,"text":"readFiles({ filePath, paths, offset=1, limit=400 })","id":"readfiles-filepath-paths-offset-1-limit-400"},{"level":4,"text":"readFile({ filePath, offset=1, limit=400 })","id":"readfile-filepath-offset-1-limit-400"},{"level":4,"text":"deleteFile({ filePath, confirm=false })","id":"deletefile-filepath-confirm-false"},{"level":4,"text":"renameFile({ from, to, filePath, dest })","id":"renamefile-from-to-filepath-dest"},{"level":4,"text":"writeFile({ filePath, content, expectedHash, confirmOverwrite=false, confirm_overwrite=false })","id":"writefile-filepath-content-expectedhash-confirmoverwrite-false-confirm_overwrite"},{"level":4,"text":"listDir({ dirPath='.', recursive=false, maxDepth=3 })","id":"listdir-dirpath-.-recursive-false-maxdepth-3"},{"level":4,"text":"grepSearch({ query, searchPath='.', isRegex=false, caseSensitive=false, limit=20, cursor=0 })","id":"grepsearch-query-searchpath-.-isregex-false-casesensitive-false-limit-20-cursor-"},{"level":3,"text":"5.6 src/tools/executor.js","id":"5.6-src/tools/executor.js"},{"level":4,"text":"killChild(child, force=false)","id":"killchild-child-force-false"},{"level":4,"text":"workingDirFrom(cwd)","id":"workingdirfrom-cwd"},{"level":4,"text":"publicRecord(rec, tail)","id":"publicrecord-rec-tail"},{"level":4,"text":"startProcess({ command, cwd='.', timeoutSec=30 })","id":"startprocess-command-cwd-.-timeoutsec-30"},{"level":4,"text":"executeCommand / startCommand","id":"executecommand-/-startcommand"},{"level":4,"text":"getCommandOutput({ execId, commandId, tail })","id":"getcommandoutput-execid-commandid-tail"},{"level":4,"text":"cancelCommand({ execId })","id":"cancelcommand-execid"},{"level":4,"text":"sendCommandInput","id":"sendcommandinput"},{"level":4,"text":"wait({ ms=800 })","id":"wait-ms-800"},{"level":3,"text":"5.7 src/tools/gitOps.js","id":"5.7-src/tools/gitops.js"},{"level":4,"text":"notGitResult(extra={})","id":"notgitresult-extra"},{"level":4,"text":"git(args, timeoutMs=8000)","id":"git-args-timeoutms-8000"},{"level":4,"text":"gitStatus","id":"gitstatus"},{"level":4,"text":"gitDiff({ filePath, staged=false, stat=false })","id":"gitdiff-filepath-staged-false-stat-false"},{"level":3,"text":"5.8 src/tools/findFiles.js","id":"5.8-src/tools/findfiles.js"},{"level":4,"text":"globToRegExp(glob)","id":"globtoregexp-glob"},{"level":4,"text":"findFiles({ glob='**/*', searchPath='.', maxResults=40 })","id":"findfiles-glob-/-searchpath-.-maxresults-40"},{"level":3,"text":"5.9 src/tools/skills.js","id":"5.9-src/tools/skills.js"},{"level":4,"text":"skillRoots","id":"skillroots"},{"level":4,"text":"listSkills","id":"listskills"},{"level":4,"text":"loadSkill({ name })","id":"loadskill-name"},{"level":3,"text":"5.10 src/tools/workspaceInfo.js","id":"5.10-src/tools/workspaceinfo.js"},{"level":4,"text":"workspaceInfo","id":"workspaceinfo"},{"level":3,"text":"5.11 src/tools/progressTracker.js","id":"5.11-src/tools/progresstracker.js"},{"level":4,"text":"reportProgress({ message, percentage=0, stepName='' })","id":"reportprogress-message-percentage-0-stepname"},{"level":4,"text":"setTodos({ todos=[] })","id":"settodos-todos"},{"level":4,"text":"getTaskState / resetTaskState","id":"gettaskstate-/-resettaskstate"},{"level":3,"text":"5.12 src/tools/consensusEngine.js","id":"5.12-src/tools/consensusengine.js"},{"level":4,"text":"runMultiModelConsensus({ taskDescription, facts={}, emit })","id":"runmultimodelconsensus-taskdescription-facts-emit"},{"level":3,"text":"5.13 src/tools/index.js（435 行）","id":"5.13-src/tools/index.js-435-行"},{"level":4,"text":"getToolList(currentMode=null)","id":"gettoollist-currentmode-null"},{"level":4,"text":"callTool(name, args={}, currentMode=null)","id":"calltool-name-args-currentmode-null"},{"level":2,"text":"6. 本机 Chat 与 REST","id":"6.-本机-chat-与-rest"},{"level":3,"text":"6.1 src/agent/providers.js","id":"6.1-src/agent/providers.js"},{"level":4,"text":"normalizeBase(url)","id":"normalizebase-url"},{"level":4,"text":"guessCaps / guessContext","id":"guesscaps-/-guesscontext"},{"level":4,"text":"listRemoteModels(baseUrl, apiKey)","id":"listremotemodels-baseurl-apikey"},{"level":3,"text":"6.2 src/agent/openai.js","id":"6.2-src/agent/openai.js"},{"level":4,"text":"toolLabel / systemPrompt(mode)","id":"toollabel-/-systemprompt-mode"},{"level":4,"text":"runOpenAI({ mode, message, history=[], emit, model })","id":"runopenai-mode-message-history-emit-model"},{"level":3,"text":"6.3 src/agent/runChat.js（387 行）","id":"6.3-src/agent/runchat.js-387-行"},{"level":4,"text":"辅助","id":"辅助"},{"level":4,"text":"explore(emit, mode, message)","id":"explore-emit-mode-message"},{"level":4,"text":"summarizeAsk(message, facts)","id":"summarizeask-message-facts"},{"level":4,"text":"runBuiltin(payload, emit)","id":"runbuiltin-payload-emit"},{"level":4,"text":"runChat(payload = {}, emit)","id":"runchat-payload-emit"},{"level":3,"text":"6.4 src/api/routes.js（378 行）","id":"6.4-src/api/routes.js-378-行"},{"level":4,"text":"publicOrigin / mcpOrigin / mcpInfo","id":"publicorigin-/-mcporigin-/-mcpinfo"},{"level":4,"text":"各路由（逐步）","id":"各路由-逐步"},{"level":2,"text":"7. 隧道 src/tunnel/cloudflared.js","id":"7.-隧道-src/tunnel/cloudflared.js"},{"level":4,"text":"parseTunnelUrl(chunk)","id":"parsetunnelurl-chunk"},{"level":4,"text":"findCloudflared","id":"findcloudflared"},{"level":4,"text":"installHint","id":"installhint"},{"level":4,"text":"stopTunnel","id":"stoptunnel"},{"level":4,"text":"startQuickTunnel({ port=config.port, timeoutMs=25000 })","id":"startquicktunnel-port-config.port-timeoutms-25000"},{"level":4,"text":"snapshot","id":"snapshot"},{"level":2,"text":"8. 工作台 UI webagent-core/workbench/","id":"8.-工作台-ui-webagent-core/workbench/"},{"level":4,"text":"toast / escapeHtml / renderMd","id":"toast-/-escapehtml-/-rendermd"},{"level":4,"text":"termLine / openModal / closeModal / showPage / setRight","id":"termline-/-openmodal-/-closemodal-/-showpage-/-setright"},{"level":4,"text":"paintTabs / activateTab / closeTab","id":"painttabs-/-activatetab-/-closetab"},{"level":4,"text":"openAgentWindow / openDiff / paintDiff / ensureWelcome","id":"openagentwindow-/-opendiff-/-paintdiff-/-ensurewelcome"},{"level":4,"text":"openFile(filePath)（async）","id":"openfile-filepath-async"},{"level":4,"text":"langFor / applyEditor","id":"langfor-/-applyeditor"},{"level":4,"text":"treeHtml / loadTree","id":"treehtml-/-loadtree"},{"level":4,"text":"emptyChat / paintChat / summarizeTool / renderMsg / pushMsg","id":"emptychat-/-paintchat-/-summarizetool-/-rendermsg-/-pushmsg"},{"level":4,"text":"sendChat(text, opts={})（async）","id":"sendchat-text-opts-async"},{"level":4,"text":"handleEvent(ev)","id":"handleevent-ev"},{"level":4,"text":"logBridgeTool / paintTodos / agentLabel / setAgentMode / paintStats","id":"logbridgetool-/-painttodos-/-agentlabel-/-setagentmode-/-paintstats"},{"level":4,"text":"selectedClientInfo / promptText / paintClients","id":"selectedclientinfo-/-prompttext-/-paintclients"},{"level":4,"text":"renderBrowser(tab)","id":"renderbrowser-tab"},{"level":4,"text":"arenaConnect(text)（async）","id":"arenaconnect-text-async"},{"level":4,"text":"openSite(key)（async）","id":"opensite-key-async"},{"level":4,"text":"startBridge / stopBridge / paintBridge / refreshStatus","id":"startbridge-/-stopbridge-/-paintbridge-/-refreshstatus"},{"level":4,"text":"paintCustom / paintProviderTable / loadCustomizations / saveCustom / loadSkills","id":"paintcustom-/-paintprovidertable-/-loadcustomizations-/-savecustom-/-loadskills"},{"level":4,"text":"bind","id":"bind"},{"level":4,"text":"saveActive / connectWs / loadMonaco / boot","id":"saveactive-/-connectws-/-loadmonaco-/-boot"},{"level":2,"text":"9. 网页 VS Code 插件 webagent-core/extension/extension.js（492 行）","id":"9.-网页-vs-code-插件-webagent-core/extension/extension.js-492-行"},{"level":4,"text":"agentHostUrl","id":"agenthosturl"},{"level":4,"text":"requestJson(method, url, body)","id":"requestjson-method-url-body"},{"level":4,"text":"postNdjson(url, body, onEvent)","id":"postndjson-url-body-onevent"},{"level":4,"text":"modeFromChatRequest(request)","id":"modefromchatrequest-request"},{"level":4,"text":"historyFromChatContext / revealWorkspaceFile","id":"historyfromchatcontext-/-revealworkspacefile"},{"level":4,"text":"registerChatParticipant","id":"registerchatparticipant"},{"level":4,"text":"activate(context)","id":"activate-context"},{"level":4,"text":"ChatView","id":"chatview"},{"level":4,"text":"BridgeView","id":"bridgeview"},{"level":2,"text":"10. 三条路径的函数调用栈（逐步，对照源码）","id":"10.-三条路径的函数调用栈-逐步-对照源码"},{"level":3,"text":"10.1 本机 Chat，无 API Key，Code 模式","id":"10.1-本机-chat-无-api-key-code-模式"},{"level":3,"text":"10.2 DeepSeek++ 改磁盘（源码能证明的部分）","id":"10.2-deepseek-改磁盘-源码能证明的部分"},{"level":3,"text":"10.3 Chat Plus 改磁盘（源码能证明的部分）","id":"10.3-chat-plus-改磁盘-源码能证明的部分"},{"level":3,"text":"10.4 ChatGPT Plus 连接器","id":"10.4-chatgpt-plus-连接器"},{"level":2,"text":"11. 源码现状里不要写成「已经接上」的点","id":"11.-源码现状里不要写成-已经接上-的点"},{"level":2,"text":"12. 改代码时建议从哪下手","id":"12.-改代码时建议从哪下手"}],"html":"<h1 id=\"代码直译技术实现\">代码直译技术实现</h1>\n<p>本文只描述<strong>当前仓库里读到的源码</strong>。没有写进代码的行为标「源码未实现」，不补愿望、不杜撰调用。</p>\n<p>读法：第 0 节总链 → 按文件跳到对应小节。每个函数拆成「步骤 1、2、3…」；<code>if</code> / <code>try</code> 分支全部列出。<strong>闭区间行号以各子文件夹 README / <a href=\"#/graph\">总览.md</a> 为准</strong>（四阶段文档）；本文偏行为直译，会随提交微调。</p>\n<p>小白工作流、怎么点按钮，仍看 <a href=\"#/workflow\">组件说明.md</a>。人话架构看 <a href=\"#/guide\">架构导读.md</a>。本文是对着源码的直译。</p>\n<hr />\n<h2 id=\"0.-总链条-文件名穿起来\">0. 总链条（文件名穿起来）</h2>\n<pre><code>run-webagent.cmd\n  └─ node webagent-core/agent-host/src/index.js\n        persistIdentity(store)\n        两套 Express（同一进程）\n        ├─ uiApp :3000  静态 workbench/ + /api + /ws + SPA 回退（无 /mcp）\n        │     workbench/app.js  POST /api/chat → agent/runChat.js\n        │     workbench/app.js  POST /api/bridge/start → api/routes.js\n        └─ mcpApp :48271  /mcp + OAuth；/api 先过 utils/localControl.js\n              隧道头或公网 Host → /api 404；本机回环仍通\n              mcp/server.js requireAuth → handleRpc\n                tools/call → tools/index.js callTool\n                  fileOps / patchEngine / executor / gitOps …\n                  eventBus.broadcast → 工作台 :3000 /ws（mcp 口不挂 WSS）</code></pre>\n<p>三条「谁下指令」最终都进 <strong>同一套 <code>callTool</code></strong>：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>入口</th>\n<th>第一个文件</th>\n<th>源码里<strong>不经过</strong></th>\n</tr></thead><tbody>\n<tr>\n<td>本机 CHAT</td>\n<td><code>workbench/app.js</code> → <strong>3000</strong> <code>POST /api/chat</code> → <code>runChat.js</code></td>\n<td>隧道、<code>/mcp</code></td>\n</tr>\n<tr>\n<td>网页 VS Code 侧栏 / <code>@webagent</code></td>\n<td><code>extension/extension.js</code> → 本机 48271 <code>POST /api/chat</code>（无 CF 头）</td>\n<td>工作台 HTML</td>\n</tr>\n<tr>\n<td>Arena / DeepSeek++ / Chat Plus / ChatGPT 连接器</td>\n<td><code>POST /mcp/&lt;secret&gt;</code> 或 <code>POST /mcp</code> + Bearer → <code>mcp/server.js</code></td>\n<td><code>/api/chat</code></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"1.-启动脚本\">1. 启动脚本</h2>\n<h3 id=\"1.1-run-webagent.cmd\">1.1 <code>run-webagent.cmd</code></h3>\n<p><strong>用途：</strong> Windows 主入口。不启动 code-server，不进 <code>webagent-repro</code>。 <strong>结果：</strong> 一个一直开着的 Node 进程（<code>node src\\index.js</code>）。 <strong>链条位置：</strong> 用户手 → 操作系统进程 → <code>index.js</code>。</p>\n<p>步骤：</p>\n<ol>\n<li><code>@echo off</code>、<code>chcp 65001</code>、<code>setlocal EnableExtensions</code>、窗口标题 <code>Web Agent</code>、<code>cd /d &quot;%~dp0&quot;</code> 进仓库根。</li>\n<li><code>if not &quot;%~1&quot;==&quot;&quot;</code> → 把第一个参数写成 <code>WORKSPACE_ROOT</code>；未定义则 <code>%~dp0workspace</code>。</li>\n<li>写死 <code>AGENT_HOST_PORT=48271</code>、<code>WORKBENCH_PORT=3000</code>。</li>\n<li><code>where node</code>：<code>errorlevel 1</code> 则打印安装提示、<code>pause</code>、<code>exit /b 1</code>。</li>\n<li><code>where npm</code>：失败同样退出。</li>\n<li>工作区不存在时：</li>\n</ol>\n<ul>\n<li><code>if &quot;%~1&quot;==&quot;&quot;</code>（没传参数）→ <code>mkdir</code> 默认 <code>workspace</code>。</li>\n<li><strong>否则</strong>打印「工作区不存在」并退出（不会替你建 <code>D:\\code\\my-app</code>）。</li>\n</ul>\n<ol>\n<li>打印 <code>node -v</code>、<code>npm -v</code>。<code>where git</code> 失败只打印「未安装」，<strong>不退出</strong>。<code>where cloudflared</code> 失败只打印提示，<strong>不退出</strong>。</li>\n<li><code>cd /d &quot;%~dp0webagent-core\\agent-host&quot;</code>。若不存在 <code>node_modules\\express</code> 则 <code>call npm install --no-audit --no-fund</code>；失败则退出。</li>\n<li><code>node src\\index.js</code>。若 <code>ERRORLEVEL</code> 非 0：打印端口占用提示、<code>pause</code>，再 <code>exit /b %ERR%</code>。</li>\n</ol>\n<h3 id=\"1.2-run-webagent.sh\">1.2 <code>run-webagent.sh</code></h3>\n<p><strong>用途：</strong> Linux/macOS 等价入口。 <strong>结果：</strong> <code>exec node src/index.js</code> 替换当前 shell。</p>\n<p>步骤：<code>set -e</code>；<code>ROOT</code> 为脚本所在目录；<code>WORKSPACE_ROOT</code> 默认 <code>$ROOT/workspace</code>；端口默认 48271 / 3000；<code>cd agent-host</code>；没有 <code>node_modules/express</code> 就 <code>npm install</code>；<code>exec node src/index.js</code>。没有 Windows 那套 <code>where</code> / <code>pause</code>。</p>\n<h3 id=\"1.3-run-webagent-vscode.cmd\">1.3 <code>run-webagent-vscode.cmd</code></h3>\n<p><strong>用途：</strong> 第二种跑法：网页 VS Code。 <strong>结果：</strong> 调用 <code>node webagent-core\\scripts\\run-code-oss.js</code>，把工作区当参数传进去。</p>\n<p>步骤：同样设 <code>WORKSPACE_ROOT</code>（默认 <code>%~dp0workspace</code>）。缺 Node/npm 退出。工作区不存在时<strong>直接退出</strong>（即使没传参数也不 <code>mkdir</code>，与 <code>run-webagent.cmd</code> 不同）。成功则 <code>node …\\run-code-oss.js &quot;%WORKSPACE_ROOT%&quot;</code>。</p>\n<h3 id=\"1.4-webagent-core/scripts/ensure-code-server.js\">1.4 <code>webagent-core/scripts/ensure-code-server.js</code></h3>\n<p><strong>用途：</strong> 第一次把 <code>code-server@4.135.0</code> 装到 <code>bin/code-server-runtime/</code>，并把插件拷到 <code>extensions-installed/</code>。 <strong>结果：</strong> 返回 <code>out/node/entry.js</code> 的绝对路径。 <strong>链条位置：</strong> 只被 <code>run-code-oss.js</code>（以及本文件作为 CLI 时）调用。<code>run-webagent.cmd</code> <strong>不经过</strong>这里。</p>\n<h4 id=\"npmcmd\"><code>npmCmd</code></h4>\n<p><code>win32</code> → <code>npm.cmd</code>，否则 <code>npm</code>。</p>\n<h4 id=\"runnpm-args-cwd\"><code>runNpm(args, cwd)</code></h4>\n<p>步骤 1：<code>spawnSync</code> 跑 npm，<code>stdio: inherit</code>，Windows 上 <code>shell: true</code>。 步骤 2：<code>env</code> 里加 <code>FORCE_NODE_VERSION</code> 为当前 Node 主版本号。 步骤 3：<code>if (r.status !== 0)</code> 抛 <code>Error</code>。</p>\n<h4 id=\"findentry\"><code>findEntry</code></h4>\n<p>按顺序查三个候选路径，<code>find</code> 第一个 <code>existsSync</code> 的；都没有返回 <code>null</code>。</p>\n<h4 id=\"ensurevscodedeps-entry\"><code>ensureVscodeDeps(entry)</code></h4>\n<p>步骤 1：从 entry 推出 <code>lib/vscode</code>。 步骤 2：若已有 <code>node_modules/@microsoft/1ds-core-js</code> → <strong>直接 return</strong>。 步骤 3：若该目录没有 <code>package.json</code> → 抛「包不完整」。 步骤 4：否则 <code>npm install --omit=dev --ignore-scripts</code>。</p>\n<h4 id=\"ensure\"><code>ensure</code></h4>\n<p>步骤 1：<code>mkdirSync(runtimeRoot, { recursive: true })</code>。 步骤 2：<code>findEntry()</code>。若没有：</p>\n<ul>\n<li>没有 <code>package.json</code> 则写入一个只依赖 <code>code-server</code> 的私有包。</li>\n<li><code>npm install code-server@4.135.0 --omit=dev --ignore-scripts</code>。</li>\n<li>再 <code>findEntry()</code>。</li>\n</ul>\n<p>步骤 3：仍没有 entry → 抛错。 步骤 4：<code>ensureVscodeDeps(entry)</code>，<strong>返回 entry</strong>。</p>\n<h4 id=\"syncextension\"><code>syncExtension</code></h4>\n<p>步骤 1：把 <code>extension/package.json</code>、<code>extension.js</code> 拷到 <code>extensions-installed/webagent.webagent-core-0.6.9/</code>。 步骤 2：若存在 <code>resources/icon.svg</code> 也拷。 步骤 3：写 <code>extensions.json</code>，<code>location.path</code> 是<strong>本机绝对路径</strong>（正斜杠）。</p>\n<p>文件末尾：<code>require.main === module</code> 时自己跑 <code>ensure</code> + <code>syncExtension</code>；失败 <code>process.exit(1)</code>。</p>\n<h3 id=\"1.5-webagent-core/scripts/run-code-oss.js\">1.5 <code>webagent-core/scripts/run-code-oss.js</code></h3>\n<p><strong>用途：</strong> 先起 agent-host（跳过 3000），再起 code-server 占 3000。 <strong>结果：</strong> 两个子进程；Ctrl+C 时 Windows 用 <code>taskkill /t /f</code>。</p>\n<h4 id=\"顶层\">顶层</h4>\n<p><code>workspace</code> = <code>argv[2]</code> 或 <code>WORKSPACE_ROOT</code> 或仓库 <code>workspace</code>。不存在则 <code>console.error</code> + <code>process.exit(1)</code>。 <code>mcpPort</code> 默认 48271，<code>codePort</code> 默认 3000。</p>\n<h4 id=\"waithealth-url-timeoutms\"><code>waitHealth(url, timeoutMs)</code></h4>\n<p>循环 <code>http.get</code>：状态码 200 则 resolve；超时 reject「agent-host 未在时限内就绪」；<code>error</code> 事件同样按超时判断，否则 200ms 后再试。</p>\n<h4 id=\"run-command-args-opts\"><code>run(command, args, opts)</code></h4>\n<p><code>spawn</code>，<code>stdio: inherit</code>，<code>windowsHide: true</code>。<code>child.on('error')</code> 打印后 <code>process.exit(1)</code>。返回 child。</p>\n<h4 id=\"main-async\"><code>main</code>（async）</h4>\n<p>步骤 1：<code>ensure()</code> 得到 code-server entry，<code>syncExtension()</code>。 步骤 2：agent-host 没有 <code>node_modules/express</code> 则在该目录 <code>npm install</code>；子进程 exit 非 0 则 reject。 步骤 3：注册 <code>SIGINT</code>/<code>SIGTERM</code>：对每个 child，Windows <code>taskkill /pid /t /f</code>，否则 <code>SIGTERM</code>。 步骤 4：<code>run(process.execPath, ['src/index.js'], { cwd: agentHostDir, env: { WORKSPACE_ROOT, AGENT_HOST_PORT, WEBAGENT_SKIP_WORKBENCH: '1' } })</code>。agent <code>exit</code> 若 <code>code</code> 真值：stop 后 <code>process.exit(code)</code>。 步骤 5：<code>await waitHealth(http://127.0.0.1:${mcpPort}/health, 15000)</code>。 步骤 6：确保 <code>.local/share/code-server</code> 目录存在。<code>resolveAuth</code>：默认 <code>--auth password</code>，口令来自 <code>CODE_SERVER_PASSWORD</code> 或该目录 <code>webagent-password</code>（没有则生成并写入）；<code>CODE_SERVER_AUTH=none</code> 才无登录。拼参数：<code>--auth</code>、<code>--bind-addr ${WEBAGENT_BIND||127.0.0.1}:${codePort}</code>、关遥测/更新/workspace-trust、<code>--trusted-origins</code> 仅本机 http 源、<code>--app-name Web Agent</code>、user-data-dir、extensions-dir、config yaml、最后一项是 <strong>workspace 路径</strong>。 步骤 7：<code>run(process.execPath, csArgs, { cwd: dirname(entry), env: password 时 PASSWORD })</code>。code-server 退出则 stop 并 <code>process.exit(code || 0)</code>。</p>\n<p><code>main().catch</code>：打印 <code>err.message</code>，<code>exit(1)</code>。</p>\n<hr />\n<h2 id=\"2.-进程心脏\">2. 进程心脏</h2>\n<h3 id=\"2.1-src/config.js-47-行\">2.1 <code>src/config.js</code>（47 行）</h3>\n<p><strong>用途：</strong> 进程内单例：端口、工作区、secret、隧道 URL。 <strong>结果：</strong> 任何 <code>require('../config')</code> 拿到<strong>同一块对象</strong>。 <strong>链条位置：</strong> index / mcp / tools / tunnel / routes 都读它。</p>\n<h4 id=\"模块加载时\">模块加载时</h4>\n<p>步骤 1：<code>workspaceRoot = path.resolve(process.env.WORKSPACE_ROOT || path.join(__dirname, '../../../workspace'))</code>。 步骤 2：构造 <code>config</code>：</p>\n<ul>\n<li><code>port</code>：<code>parseInt(process.env.AGENT_HOST_PORT || '48271', 10)</code></li>\n<li><code>workbenchPort</code>：<code>parseInt(process.env.WORKBENCH_PORT || '3000', 10)</code></li>\n<li><code>host</code>：<code>process.env.WEBAGENT_BIND || '127.0.0.1'</code></li>\n<li><code>secretKey</code>：<code>crypto.randomBytes(12).toString('hex')</code>（先随机）</li>\n<li><code>version: '0.6.9'</code>，<code>serverName: 'WebAgent-AgentHost'</code>，<code>productName: 'Web Agent'</code></li>\n<li><code>tunnelProvider: 'cloudflare'</code>，<code>publicTunnelUrl: null</code>，<code>bridgeRunning: false</code></li>\n<li><code>installId</code>：8 字节 hex</li>\n</ul>\n<h4 id=\"generatenewsecret\"><code>generateNewSecret</code></h4>\n<p>步骤 1：<code>config.secretKey = crypto.randomBytes(12).toString('hex')</code>。 步骤 2：lazy <code>require('./models/store').patch({ secretKey })</code> 写入工作区 <code>.webagent/config.json</code>；失败 catch 空。 步骤 3：返回新值。</p>\n<h4 id=\"persistidentity-store\"><code>persistIdentity(store)</code></h4>\n<p>步骤 1：<code>saved = store.load()</code>。 步骤 2：<code>if (saved.secretKey)</code> → 用磁盘值覆盖内存；<strong>else</strong> <code>store.patch({ secretKey: config.secretKey })</code> 把随机值落盘。 步骤 3：<code>installId</code> 同样：有则覆盖，无则 patch。 步骤 4：若 <code>store.protectWorkspaceSecrets</code> 是函数则调用（gitignore + chmod）；失败 catch 空。</p>\n<h3 id=\"2.2-src/index.js-136-行\">2.2 <code>src/index.js</code>（136 行）</h3>\n<p><strong>用途：</strong> 组装 <strong>uiApp / mcpApp</strong> 两套 Express，开两个 <code>http.Server</code>。 <strong>结果：</strong> 默认 <code>:3000</code> 给人看（工作台 + <code>/api</code> + <code>/ws</code>），<code>:48271</code> 给 MCP/OAuth；mcp 口 <code>/api</code> 只对本机回环开放。<code>WEBAGENT_SKIP_WORKBENCH=1</code> 时只开 48271。</p>\n<p>步骤 1：<code>persistIdentity(store)</code>。 步骤 2：工作区目录：</p>\n<ul>\n<li>不存在 <strong>且</strong> 设了 <code>process.env.WORKSPACE_ROOT</code> → 打印错误、<code>process.exit(1)</code>（不替你建用户指定的路径）。</li>\n<li>不存在 <strong>且没有</strong> 该环境变量 → <code>mkdirSync(..., { recursive: true })</code>。</li>\n</ul>\n<p>步骤 3：<code>applyCommon(app)</code>：关 x-powered-by；所有响应 <code>Cache-Control: no-store</code>；<code>express.json({ limit: '20mb' })</code>；<code>urlencoded({ extended: false })</code>。<strong>不</strong>在这里挂 cors。 步骤 4：<code>mountHealth</code>：<code>GET /health</code> 返回 <code>{ ok, product, version }</code>。 步骤 5：<code>mountWorkbench</code>：静态目录 <code>path.resolve(__dirname, '../../workbench')</code>；SPA 回退：</p>\n<ul>\n<li><code>method !== 'GET'</code> → <code>next()</code>。</li>\n<li>path 以 <code>/api</code> <code>/mcp</code> <code>/ws</code> <code>/oauth</code> <code>/.well-known</code> 开头，或恰好是 <code>/register</code> → <code>next()</code>。</li>\n<li><code>path.extname(req.path)</code> 真值（带扩展名）→ <code>next()</code>。</li>\n<li>否则 <code>sendFile(workbench/index.html)</code>。</li>\n</ul>\n<p>步骤 6：<code>uiApp</code>：applyCommon、health、<code>/api</code> 先 <code>rejectUnlessLocalControl</code> 再 <code>rejectCrossSiteApi</code>、工作台静态 + SPA。无 cors、无 <code>/mcp</code>。 步骤 7：<code>mcpApp</code>：applyCommon、<strong><code>mcpCors()</code></strong>（白名单，不是无条件 <code>cors()</code>）、health、<code>oauth.router</code>、<code>/mcp</code>、<code>/api</code> 先 <code>rejectUnlessLocalControl</code> 再 <code>rejectCrossSiteApi</code>。无静态工作台。 步骤 8：<code>attachWss(server)</code>：<code>WebSocketServer({ server, path: '/ws' })</code>；<strong>非本机控制面 <code>ws.close(1008)</code></strong>；否则 <code>eventBus.addWsClient(ws)</code>，并立刻 <code>ws.send</code> 一条 <code>type:'connected'</code>，payload 只含 <code>serverName</code>、<code>version</code>（<strong>不含 secretKey</strong>）。 步骤 9：<code>uiServer = createServer(uiApp)</code>，<code>mcpServer = createServer(mcpApp)</code>；<strong>只</strong>给 uiServer attachWss。 步骤 10：<code>listenOrExit</code>：<code>error</code> 且 <code>err.code === 'EADDRINUSE'</code> 打印占用并 <code>exit(1)</code>；其它 error 也 <code>exit(1)</code>；<code>server.listen(port, config.host)</code>。 步骤 11：<code>skipWorkbench = process.env.WEBAGENT_SKIP_WORKBENCH === '1'</code>。</p>\n<ul>\n<li><strong>不是</strong> skip：<code>listenOrExit(uiServer, workbenchPort)</code>，listening 时打印 UI / MCP / Workspace，以及「公网只收 /mcp 与 OAuth」。</li>\n<li><strong>是</strong> skip：打印「本进程不占用 3000」，并说明 <code>/api</code> 仅本机回环。</li>\n</ul>\n<p>步骤 12：无论 skip 与否，都 <code>listenOrExit(mcpServer, config.port)</code>。</p>\n<p>导出 <code>{ uiApp, mcpApp, uiServer, mcpServer }</code>。</p>\n<h3 id=\"2.3-src/utils/localcontrol.js-55-行\">2.3 <code>src/utils/localControl.js</code>（55 行）</h3>\n<p><strong>用途：</strong> 判断请求是不是本机控制面。 <strong>结果：</strong> 隧道/公网 Host 打 48271 的 <code>/api</code> 得 404 <code>{ error:'not found' }</code>。</p>\n<ul>\n<li><code>isLoopbackAddress</code>：<code>127.0.0.1</code> / <code>::1</code> / <code>::ffff:127.0.0.1</code> / <code>localhost</code>。</li>\n<li><code>isTunnelRequest</code>：头 <code>cf-ray</code> / <code>cf-connecting-ip</code> / <code>cf-visitor</code> / <code>cf-ew-via</code> / <code>cdn-loop</code>。</li>\n<li><code>isPublicHost</code>：Host 为 <code>*.trycloudflare.com</code> 或含 <code>ngrok</code>。</li>\n<li><code>isLocalControlPlane</code>：隧道头或公网 Host → 假；否则看 <code>socket.remoteAddress</code> 是否回环。</li>\n<li><code>rejectUnlessLocalControl</code>：本机 <code>next()</code>，否则 404。不能只看「连的是 127.0.0.1」——cloudflared 连的就是回环。</li>\n</ul>\n<h3 id=\"2.4-src/utils/corsallow.js-115-行\">2.4 <code>src/utils/corsAllow.js</code>（115 行）</h3>\n<p><strong>用途：</strong> MCP 口浏览器跨域白名单；<code>/api</code> 拒绝外站 Origin。 <strong>结果：</strong> <code>https://evil.example</code> 预检拿不到 <code>Access-Control-Allow-Origin</code>；打 <code>/api</code> 得 404。DeepSeek++ / Chat Plus 后台是 <code>chrome-extension://…</code>，仍放行。无 Origin 的 curl / 云上服务器 / VS Code 插件仍通。 <strong>不是认证：</strong> <code>/mcp</code> 仍要 URL 密钥或 Bearer。</p>\n<ul>\n<li>无 Origin → MCP CORS 放行。</li>\n<li>本机 <code>http(s)://127.0.0.1|localhost|::1</code> → 放行。</li>\n<li><code>chrome-extension:</code> / <code>moz-extension:</code> / <code>safari-web-extension:</code> → 放行。</li>\n<li>文档点名的页面：<code>chat.deepseek.com</code>、<code>chatgpt.com</code>、<code>chat.openai.com</code>、<code>gemini.google.com</code>、<code>aistudio.google.com</code>、<code>arena.ai</code>。</li>\n<li><code>WEBAGENT_CORS_ORIGINS</code> 逗号追加。</li>\n<li><code>/api</code> 只接受无 Origin 或本机 Origin；外站 Origin / Referer → 404。</li>\n</ul>\n<hr />\n<h2 id=\"3.-磁盘上的工作区状态\">3. 磁盘上的工作区状态</h2>\n<h3 id=\"3.1-src/models/store.js\">3.1 <code>src/models/store.js</code></h3>\n<p><strong>用途：</strong> 读写 <code>&lt;工作区&gt;/.webagent/config.json</code>。 <strong>结果：</strong> 模型列表、Bridge 登录标记、secret、installId 跨重启还在。 <strong>链条位置：</strong> 启动 persistIdentity；工作台改 Provider / 多模型时 save。</p>\n<h4 id=\"dir-/-storepath\"><code>dir</code> / <code>storePath</code></h4>\n<p><code>.webagent</code> 目录；其下 <code>config.json</code>。</p>\n<h4 id=\"defaults\"><code>defaults</code></h4>\n<p>返回对象，要点：</p>\n<ul>\n<li><code>activeModelId: 'builtin'</code>，一条 builtin 模型。</li>\n<li><code>multiModel.enabled: true</code>，<code>maxBranches: 3</code>，<code>mergeAllowsRead: true</code>。</li>\n<li><code>bridge.loggedIn: true</code>，<code>deviceAuthorized: true</code>，<code>username: 'demo'</code>，<code>license: '永久顺'</code>，<code>tunnelProvider: 'cloudflare'</code>，<code>persistentMode: false</code>。</li>\n</ul>\n<h4 id=\"load\"><code>load</code></h4>\n<p><code>try</code>：读 JSON，与 defaults 浅合并；<code>models</code> 若不是非空数组则用 defaults.models；<code>bridge</code> / <code>multiModel</code> 各自再与 defaults 合并。 <code>catch</code>（文件不存在或坏 JSON）：<strong>返回 defaults，不抛</strong>。</p>\n<h4 id=\"save-next\"><code>save(next)</code></h4>\n<p><code>mkdirSync</code> 后 <code>writeFileSync</code> 美化 JSON，<code>chmod 0600</code>，再 <code>protectWorkspaceSecrets()</code>，返回 <code>next</code>。</p>\n<h4 id=\"protectworkspacesecrets\"><code>protectWorkspaceSecrets</code></h4>\n<p>写 <code>.webagent/.gitignore</code>（<code>config.json</code>、<code>read-hashes.json</code>）。工作区根有 <code>.git</code> 时，若仓库 <code>.gitignore</code> 还没有这两行则追加。不是 git 仓库不改用户根 gitignore。已有 <code>config.json</code> 再 chmod。失败 catch 空。密钥<strong>仍在工作区</strong>（每台「车」一把），不搬到 APPDATA。</p>\n<h4 id=\"patch-partial\"><code>patch(partial)</code></h4>\n<p><code>load</code> → 浅合并；<code>bridge</code> / <code>multiModel</code> 再深一层；<code>models</code> 用 <code>partial.models || current.models</code>（传了才换）。然后 <code>save</code>。</p>\n<h3 id=\"3.2-src/models/customizations.js\">3.2 <code>src/models/customizations.js</code></h3>\n<p><strong>用途：</strong> <code>&lt;工作区&gt;/.webagent/customizations.json</code>，并同步写出 md。 <strong>结果：</strong> 指令、环境、技术栈、agents/prompts 等。 <strong>链条位置：</strong> MCP <code>initialize.instructions</code>、本机 Chat system、工作台设置页。</p>\n<h4 id=\"file\"><code>file</code></h4>\n<p><code>…/.webagent/customizations.json</code>。</p>\n<h4 id=\"defaults\"><code>defaults</code></h4>\n<p>含 <code>preference: ''</code>；<code>environment</code> 全 <code>auto</code> / <code>zh-CN</code>；<code>techStack</code> 空字符串；默认 <code>instructions</code> 文案；一个 default agent；一个 diagnose prompt；空数组 hooks/mcpServers/plugins/quickLinks；<code>codex: { loggedIn: false, account: '' }</code>。</p>\n<h4 id=\"loadcustom\"><code>loadCustom</code></h4>\n<p><code>try</code> 读 JSON，与 defaults 合并 environment/techStack。 <code>catch</code> 返回 defaults。</p>\n<h4 id=\"savecustom-next\"><code>saveCustom(next)</code></h4>\n<p>步骤 1：<code>merged = { ...defaults(), ...next }</code>。 步骤 2：<code>if (next &amp;&amp; next.environment)</code> 再合并 environment。 步骤 3：<code>if (next &amp;&amp; next.techStack)</code> 再合并 techStack。 步骤 4：确保目录存在，写 <code>customizations.json</code>。 步骤 5：同目录再写 <code>instructions.md</code>（<code>merged.instructions || ''</code>）、<code>preference.md</code>（<code>markdownPreference(merged)</code>）、<code>tech-stack.md</code>（<code>markdownTechStack(merged)</code>）。 步骤 6：返回 merged。</p>\n<h4 id=\"patchcustom-partial\"><code>patchCustom(partial)</code></h4>\n<p><code>saveCustom({ ...loadCustom(), ...partial })</code>。</p>\n<h3 id=\"3.3-src/models/profile.js\">3.3 <code>src/models/profile.js</code></h3>\n<p><strong>用途：</strong> 探测 OS/技术栈，拼进给模型的上下文；生成两份 markdown。 <strong>结果：</strong> 纯函数，<strong>不写磁盘</strong>（写磁盘是 customizations.saveCustom）。</p>\n<h4 id=\"detectenvironment\"><code>detectEnvironment</code></h4>\n<p><code>process.platform</code>：<code>win32</code> → os <code>windows</code>；<code>darwin</code> → <code>macos</code>；否则 <code>linux</code>。 shell：windows → <code>powershell</code>，否则 <code>bash</code>。 回复/提交语言写死 <code>zh-CN</code>。<code>notes: ''</code>。</p>\n<h4 id=\"readjson-file\"><code>readJson(file)</code></h4>\n<p><code>try</code> <code>JSON.parse(readFileSync)</code>；<code>catch</code> 返回 <code>null</code>。</p>\n<h4 id=\"exists-root-rel\"><code>exists(root, rel)</code></h4>\n<p><code>fs.existsSync(path.join(root, rel))</code>。</p>\n<h4 id=\"detecttechstack-workspaceroot\"><code>detectTechStack(workspaceRoot)</code></h4>\n<p>步骤 1：languages / frameworks 数组，packageManager / testCommand 空串，notes 空数组（后面 <code>notes.join</code> 但代码从未 <code>notes.push</code>，所以 notes 恒为空串）。 步骤 2：有 <code>package.json</code>：</p>\n<ul>\n<li>languages 加 <code>JavaScript</code>。</li>\n<li>有 <code>tsconfig.json</code> 或 <code>jsconfig.json</code> 再加 <code>TypeScript</code>。</li>\n<li>deps 合并 dependencies + devDependencies：<code>next</code> → Next.js，否则 <code>react</code> → React；<code>nuxt</code> → Nuxt，否则 <code>vue</code> → Vue；<code>express</code> → Express。</li>\n<li>lock：<code>pnpm-lock.yaml</code> → pnpm；else <code>yarn.lock</code> → yarn；else <code>npm</code>。</li>\n<li><code>pkg.scripts.test</code> 存在 → <code>testCommand = ${packageManager} test</code>。</li>\n</ul>\n<p>步骤 3：有 <code>pyproject.toml</code> 或 <code>requirements.txt</code> 或 <code>pytest.ini</code>：加 Python；若还没有 packageManager → <code>pip</code>；若还没有 testCommand → <code>python -m pytest -q</code>。 步骤 4：<code>Cargo.toml</code>：Rust / cargo / <code>cargo test</code>（仅当 packageManager / testCommand 仍空）。 步骤 5：<code>go.mod</code>：Go / go / <code>go test ./...</code>（同样仅当仍空）。 步骤 6：有 <code>index.html</code> 且 languages 不含 HTML → 加 HTML。 步骤 7：<code>uniq</code> 去重后 join。</p>\n<h4 id=\"resolveenvironment-custom\"><code>resolveEnvironment(custom)</code></h4>\n<p>探测值作底。<code>env.os &amp;&amp; env.os !== 'auto'</code> 才用用户值；shell 同理。<code>replyLanguage</code> / <code>commitLanguage</code> 用 <code>||</code>（空串会回落到探测值）。<code>notes</code> 用 <code>env.notes || ''</code>。</p>\n<h4 id=\"resolvetechstack-custom-workspaceroot\"><code>resolveTechStack(custom, workspaceRoot)</code></h4>\n<p>各字段 <code>stack.xxx || detected.xxx</code>。</p>\n<h4 id=\"languagelabel-code\"><code>languageLabel(code)</code></h4>\n<p><code>zh-CN</code> 或 <code>zh</code> → 中文；<code>en</code> → English；<code>follow-user</code> → 跟随用户提问的语言；否则原样或空。</p>\n<h4 id=\"oslabel-os\"><code>osLabel(os)</code></h4>\n<p>windows / macos / linux 映射；其它原样。</p>\n<h4 id=\"formatworkspacecontext-custom-skills\"><code>formatWorkspaceContext(custom, skills)</code></h4>\n<p>拼 markdown 三段：Environment preference、Tech stack、Skills。 tech stack 若 languages 和 testCommand 都空，加一行 <code>(not declared; inspect the workspace)</code>。 skills 空：提示放到 <code>.webagent/skills/</code>。非空：对每个 skill 取 preview 里第一行非空且不以 <code>#</code> 开头的，截 80 字。</p>\n<h4 id=\"markdownpreference-/-markdowntechstack\"><code>markdownPreference</code> / <code>markdownTechStack</code></h4>\n<p>生成中文 md。<code>filter((l) =&gt; l !== null)</code>：空字符串会保留（用来空行）。</p>\n<h3 id=\"3.4-src/models/memory.js\">3.4 <code>src/models/memory.js</code></h3>\n<p><strong>用途：</strong> <code>.webagent/memory/YYYY-MM-DD.md</code> 追加备忘。 <strong>结果：</strong> 工具 <code>remember</code> / <code>recall</code>。</p>\n<h4 id=\"memorydir-/-dayfile\"><code>memoryDir</code> / <code>dayFile</code></h4>\n<p>目录；<code>day</code> 缺省用 <code>toISOString().slice(0, 10)</code>。</p>\n<h4 id=\"remember-text-day\"><code>remember({ text, day })</code></h4>\n<p>步骤 1：<code>body = String(text || '').trim()</code>；空则 <code>{ ok: false, error: 'text required' }</code>。 步骤 2：<code>mkdirSync</code>。 步骤 3：一行 <code>- &lt;ISO时间&gt; &lt;正文换行压成空格&gt;</code>。 步骤 4：文件已存在 → <code>appendFileSync(line)</code>；否则先写 <code># 日期</code> 再写 line。 步骤 5：返回 <code>{ ok: true, path: 相对工作区 }</code>。</p>\n<h4 id=\"recall-limit-40-day\"><code>recall({ limit = 40, day })</code></h4>\n<p>步骤 1：确保目录。 步骤 2：若传了 <code>day</code>，只看那一个文件；否则读目录所有 <code>.md</code>，<code>sort().reverse()</code>（新日期在前）。 步骤 3：逐文件：不存在 <code>continue</code>；push <code>## 文件名\\n内容</code>；若拼接长度 <code>&gt; 8000</code> <strong>break</strong>。 步骤 4：无内容则 <code>'(empty memory)'</code>。 步骤 5：按行 <code>slice(0, Math.max(5, limit))</code>。 返回 <code>{ files: 相对路径数组, text }</code>。</p>\n<hr />\n<h2 id=\"4.-mcp-层-网页-agent-进门\">4. MCP 层（网页 Agent 进门）</h2>\n<h3 id=\"4.1-src/mcp/errors.js\">4.1 <code>src/mcp/errors.js</code></h3>\n<h4 id=\"类-protocolerror-/-executionerror\">类 <code>ProtocolError</code> / <code>ExecutionError</code></h4>\n<p>都 <code>super(message)</code>；设 <code>name</code>、<code>layer</code>（<code>protocol</code> / <code>execution</code>）、<code>code</code>、<code>detail || {}</code>。</p>\n<h4 id=\"classifytoolerror-err\"><code>classifyToolError(err)</code></h4>\n<p>步骤 1：已经是上述两类 → 原样返回。 步骤 2：取 <code>err.message</code> 或 <code>err</code> 转字符串，按正则<strong>从上到下</strong>第一次命中：</p>\n<ul>\n<li><code>Unknown tool</code> → <code>ProtocolError E_UNKNOWN_CMD</code></li>\n<li><code>locked in</code> 或 <code>Ask/Plan are read-only</code> → <code>E_BAD_ARGS</code></li>\n<li><code>requires </code> 或 <code>required</code> → <code>E_BAD_ARGS</code></li>\n<li><code>HASH_REQUIRED</code> → <code>E_BAD_ARGS</code></li>\n<li><code>STALE_FILE</code> → <code>ExecutionError E_STALE_FILE</code></li>\n<li><code>Patch conflict</code> → <code>E_CONFLICT</code></li>\n<li><code>GIT_UNAVAILABLE</code> 或 <code>not a git repository</code> → <code>E_NOT_READY</code>（现行 <code>git_status</code>/<code>git_diff</code> 多数已收成 <code>available:false</code>，不一定抛到这里）</li>\n<li><code>not found</code> 或 <code>No such file</code> → <code>E_NOT_FOUND</code></li>\n<li><code>timeout</code> 或 <code>isTimeout</code> → <code>E_TIMEOUT</code></li>\n<li><code>ACCESS_DENIED_SENSITIVE_FILE</code> 或 <code>E_FORBIDDEN</code> → <code>E_FORBIDDEN</code></li>\n<li><code>confirm_dangerous</code> / <code>confirm_overwrite</code> / <code>confirm=true</code> → <code>ProtocolError E_BAD_ARGS</code></li>\n<li>其它 → <code>ExecutionError E_INTERNAL</code></li>\n</ul>\n<h4 id=\"publicerror-err\"><code>publicError(err)</code></h4>\n<p>classify 后返回 <code>{ layer, code, msg: classified.message, detail }</code>。</p>\n<h3 id=\"4.2-src/mcp/session.js\">4.2 <code>src/mcp/session.js</code></h3>\n<p>内存 <code>Map</code>，进程关了就没。</p>\n<h4 id=\"sessionkey-req\"><code>sessionKey(req)</code></h4>\n<p>ip：<code>req.ip</code> 或 <code>x-forwarded-for</code>，都没有 <code>'local'</code>。 client：<code>req.body.params.clientInfo.name</code>，没有 <code>'mcp'</code>。 返回 <code></code> <code>${client}@${ip}</code> <code></code>。</p>\n<h4 id=\"touch-req-extra\"><code>touch(req, extra = {})</code></h4>\n<p>步骤 1：key = <code>extra.key || sessionKey(req)</code>。 步骤 2：已有记录或新建 <code>{ key, connectedAt, calls:0, fail:0 }</code>。 步骤 3：合并 extra；<code>lastSeen</code> 现时 ISO；<code>calls</code> / <code>fail</code> 仅当 <code>extra.incCall</code> / <code>incFail</code> 为真才 +1；<code>busy = Boolean(extra.busy)</code>。 步骤 4：写回 Map，返回 next。</p>\n<h4 id=\"snapshot\"><code>snapshot</code></h4>\n<p>按 <code>lastSeen</code> 字符串降序。<code>alive</code>：最新一条的年龄 <code>&lt; 10000</code> ms。返回 <code>clients</code> 数、<code>staleAfterMs: 10000</code>、<code>ageMs</code>、<code>latest</code>、最多 8 条 <code>sessions</code>。</p>\n<h4 id=\"reset\"><code>reset</code></h4>\n<p><code>sessions.clear()</code> 后返回 <code>snapshot()</code>。被 <code>POST /api/bridge/reset-round</code> 调用。</p>\n<h3 id=\"4.3-src/mcp/budget.js\">4.3 <code>src/mcp/budget.js</code></h3>\n<p>常量 <code>MAX_CHARS = 16000</code>（注释：约 4k tokens）。</p>\n<h4 id=\"estimatetokens-text\"><code>estimateTokens(text)</code></h4>\n<p><code>ceil(length / 4)</code>。</p>\n<h4 id=\"cliptext-text-maxchars\"><code>clipText(text, maxChars)</code></h4>\n<p>空当 <code>''</code>。长度 ≤ max → <code>{ text, truncated: false }</code>。 否则切 <code>maxChars-80</code>，后缀说明 truncated 字数，并提示用 offset/limit/cursor/<code>get_command_output</code>。</p>\n<h4 id=\"clipjson-value-maxchars\"><code>clipJson(value, maxChars)</code></h4>\n<ul>\n<li><code>null/undefined</code> 原样。</li>\n<li><strong>字符串</strong>：clipText；截断则 <code>{ text, _truncated, originalChars }</code>，否则原字符串。</li>\n<li>先 <code>JSON.stringify</code>；长度 ≤ max → 原对象。</li>\n<li>否则：对象（非数组）浅拷贝，数组则包成 <code>{ value }</code>。</li>\n<li>对键 <code>stdout|stderr|content|preview|text|diff</code>：若字符串长度 &gt; 2000，clipText 到 <code>min(4000, maxChars/3)</code>。</li>\n<li><code>matches</code> 数组长度 &gt; 20：留下 20，设 <code>nextCursor=20</code>，补 <code>totalMatches</code>。</li>\n<li><code>items</code> 数组长度 &gt; 40：留下 40，<code>nextCursor=40</code>。</li>\n<li>再 stringify：仍超长 → <code>{ _truncated, summary: clipText(...), originalChars, hint }</code>。</li>\n<li>否则给 copy 打 <code>_truncated</code> 和 <code>originalChars</code> 后返回 copy。</li>\n</ul>\n<h3 id=\"4.4-src/mcp/instructions.js\">4.4 <code>src/mcp/instructions.js</code></h3>\n<h4 id=\"常量-connect_line\">常量 <code>CONNECT_LINE</code></h4>\n<p>固定一句：<code>快速连接这个 MCP（URL），明确使用规则，熟悉可用工具，做好处理接下来一系列工作的准备。</code> （测试锁原文，改一字会红。）</p>\n<h4 id=\"getbootstrapprompt-mcpurl\"><code>getBootstrapPrompt(mcpUrl)</code></h4>\n<p><code></code> <code>${mcpUrl || ''}\\n\\n${CONNECT_LINE}</code> <code></code>。</p>\n<h4 id=\"server_instructions\"><code>SERVER_INSTRUCTIONS</code></h4>\n<p>大段 markdown：工具改磁盘、不要杜撰、Ask/Plan 只读、工作流 1–7、输出预算、错误分层、安全、memory。源码里就是这一整段字符串。</p>\n<h4 id=\"getinstructions\"><code>getInstructions</code></h4>\n<p>步骤 1：<code>loadCustom()</code>。 步骤 2：若 <code>custom.instructions</code> 真值，push <code>## Workspace instructions\\n…</code>。 步骤 3：push <code>formatWorkspaceContext(custom, listSkills())</code>。 步骤 4：push <code>## Workspace root\\n${config.workspaceRoot}</code>。 步骤 5：<code>[SERVER_INSTRUCTIONS.trim(), ...extra].join('\\n\\n')</code>。</p>\n<h3 id=\"4.5-src/mcp/clients.js\">4.5 <code>src/mcp/clients.js</code></h3>\n<p><strong>用途：</strong> 工作台 Bridge 卡片数据。<strong>不是</strong> MCP 协议的一部分。 <strong>结果：</strong> <code>/api/status.clients[]</code>。</p>\n<p><code>CLIENTS</code> 数组 <strong>7</strong> 项，字段以源码为准：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>id</th>\n<th>connectMode</th>\n<th>needsPlus</th>\n<th>needsTunnel</th>\n<th>supportsMcp</th>\n</tr></thead><tbody>\n<tr>\n<td><code>chat</code></td>\n<td><code>local-chat</code></td>\n<td>false</td>\n<td>false</td>\n<td>false</td>\n</tr>\n<tr>\n<td><code>arena</code></td>\n<td><code>paste-url</code></td>\n<td>false</td>\n<td>true</td>\n<td>true</td>\n</tr>\n<tr>\n<td><code>deepseek</code></td>\n<td><code>extension-http</code></td>\n<td>false</td>\n<td>true</td>\n<td>true</td>\n</tr>\n<tr>\n<td><code>chat-plus</code></td>\n<td><code>extension-http</code></td>\n<td>false</td>\n<td>true</td>\n<td>true</td>\n</tr>\n<tr>\n<td><code>generic</code></td>\n<td><code>paste-url</code></td>\n<td>false</td>\n<td>true</td>\n<td>true</td>\n</tr>\n<tr>\n<td><code>chatgpt-free</code></td>\n<td><code>unsupported-mcp</code></td>\n<td>false</td>\n<td>true</td>\n<td>false</td>\n</tr>\n<tr>\n<td><code>chatgpt-plus</code></td>\n<td><code>oauth-connector</code></td>\n<td>true</td>\n<td>true</td>\n<td>true</td>\n</tr>\n</tbody></table></div>\n<p>DeepSeek 项还带 <code>extensionId</code>、<code>storeUrl</code>。Chat Plus 项带 <code>repoUrl</code>（GitHub，无商店 ID）。每项有 <code>summary</code> 和 <code>steps[]</code>（UI 步骤文案）。</p>\n<h4 id=\"hydrateclient-client-urls\"><code>hydrateClient(client, urls)</code></h4>\n<p>步骤 1：<code>mcpUrl = urls.mcpUrl || ''</code>。 步骤 2：<code>canonical = urls.mcpCanonicalUrl || mcpUrl.replace(/\\/mcp\\/[^/]+$/, '/mcp')</code>。 步骤 3：按 <code>connectMode</code> 设 <code>prompt</code>：</p>\n<ul>\n<li><code>paste-url</code> → <code>getBootstrapPrompt(mcpUrl)</code>（URL + 空行 + CONNECT_LINE）</li>\n<li><code>oauth-connector</code> → 两行：规范地址 + 「认证：OAuth…」</li>\n<li><code>extension-http</code> → <strong>只有</strong> <code>mcpUrl</code>（一行，给扩展 URL 框）</li>\n<li><code>unsupported-mcp</code> → <code>''</code></li>\n<li>其它（含 <code>local-chat</code>）→ <code>prompt</code> 保持 <code>''</code></li>\n</ul>\n<p>步骤 4：返回展开后的 client；<code>mcpUrl</code> 在 oauth 模式下改成 canonical；附 <code>connectLine</code>。</p>\n<h4 id=\"listclients-urls\"><code>listClients(urls)</code></h4>\n<p><code>CLIENTS.map(hydrateClient)</code>。</p>\n<h4 id=\"getclient-id-urls\"><code>getClient(id, urls)</code></h4>\n<p><code>find</code> 按 id；找不到则 <strong><code>CLIENTS[1]</code>（arena）</strong>。再 hydrate。</p>\n<h3 id=\"4.6-src/mcp/resources.js\">4.6 <code>src/mcp/resources.js</code></h3>\n<p><code>RESOURCE_DEFS</code> 8 个 URI：instructions / protocol / capabilities / config / workspace / memory / profile / clients。</p>\n<h4 id=\"listresources\"><code>listResources</code></h4>\n<p>直接返回 <code>RESOURCE_DEFS</code>。</p>\n<h4 id=\"readresource-uri-switch\"><code>readResource(uri)</code> — <code>switch</code></h4>\n<ul>\n<li><code>webagent://instructions</code> → <code>getInstructions()</code>，markdown。</li>\n<li><code>webagent://profile</code> → <code>formatWorkspaceContext(loadCustom(), listSkills())</code>。</li>\n<li><code>webagent://protocol</code> → 源码里写死的一组要点（传输、<code>tools/call</code> 失败是 <code>isError</code>、心跳 10s、apply_patch 可复用上次 read 的 sha256、<code>bash</code>/<code>cat</code>/<code>path</code> 别名）。</li>\n<li><code>webagent://capabilities</code> → <code>tools N</code> + 每行 <code>- name: description</code>。</li>\n<li><code>webagent://config</code> → server/version/workspace/<code>bridgeRunning</code>/tunnel/<code>installId</code>/mcpClients；<strong>明文写 <code>secret omitted</code></strong>，不输出密钥。</li>\n<li><code>webagent://workspace</code> → root、instructions、task 状态、最近 5 条 event 的 type。</li>\n<li><code>webagent://memory</code> → <code>recall({ limit: 80 }).text</code>。</li>\n<li><code>webagent://clients</code> → 用占位 <code>(mcp url)</code> hydrate 后列出每张卡的 summary。</li>\n<li><strong>default</strong> → <code>null</code>。</li>\n</ul>\n<h3 id=\"4.7-src/mcp/oauth.js-379-行\">4.7 <code>src/mcp/oauth.js</code>（379 行）</h3>\n<p><strong>用途：</strong> OAuth 2.1 子集：动态注册 + 授权码 + PKCE S256 + 本机配对码。 <strong>结果：</strong> Bearer，给 <code>POST /mcp</code>（路径无密钥）。 <strong>链条位置：</strong> 仅 ChatGPT Plus 连接器。Arena / DeepSeek++ / Chat Plus <strong>不走这里</strong>。 存储：四个内存 <code>Map</code> + 模块级 <code>pairing</code>。进程退出全丢。</p>\n<p>TTL：配对 5 分钟、授权码 5 分钟、access 1 小时、refresh 7 天。</p>\n<h4 id=\"now-/-randomtoken-prefix-bytes-24-/-randompairingcode\"><code>now</code> / <code>randomToken(prefix, bytes=24)</code> / <code>randomPairingCode</code></h4>\n<p>配对码字母表去掉 0/O/1/I：<code>ABCDEFGHJKLMNPQRSTUVWXYZ23456789</code>，8 字节映射成长度 8 的码。</p>\n<h4 id=\"requestorigin-req\"><code>requestOrigin(req)</code></h4>\n<p>若 <code>config.publicTunnelUrl</code> 真值 → 去掉末尾 <code>/</code> 后返回。 否则 proto：<code>x-forwarded-proto</code> 或 <code>req.protocol</code> 或 <code>http</code>，取逗号前第一段。 host：<code>x-forwarded-host</code> 或 <code>host</code> 或 <code>127.0.0.1:${config.port}</code>。 返回 <code></code> <code>${proto}://${host}</code> <code></code>。</p>\n<h4 id=\"issuepairing-/-snapshotpairing-/-ensurepairing\"><code>issuePairing</code> / <code>snapshotPairing</code> / <code>ensurePairing</code></h4>\n<p>issue：写新 pairing（attempts:0）。 snapshot：没有或过期 → <code>{ code:null, expiresInSec:0, expired:true }</code>。 ensure：过期或无 code 则 issue，否则返回当前 snap。</p>\n<h4 id=\"consumepairing-code\"><code>consumePairing(code)</code></h4>\n<p>步骤 1：过期或无 pairing → 抛，<code>status=400</code>，「配对码已过期…」。 步骤 2：<code>attempts += 1</code>；<code>&gt; 5</code> → <code>pairing=null</code>，抛 429「尝试次数过多」。 步骤 3：trim + toUpperCase 不等于 <code>pairing.code</code> → 抛 400「不正确」。 步骤 4：<code>pairing=null</code>（用过即废），返回 <code>true</code>。</p>\n<h4 id=\"authorizationservermetadata-origin-/-protectedresourcemetadata-origin-/-wwwauthe\"><code>authorizationServerMetadata(origin)</code> / <code>protectedResourceMetadata(origin)</code> / <code>wwwAuthenticate(origin)</code></h4>\n<p>返回发现文档对象；WWW-Authenticate 指向 <code>/.well-known/oauth-protected-resource</code>。</p>\n<h4 id=\"registerclient-body\"><code>registerClient(body)</code></h4>\n<p>步骤 1：<code>redirect_uris</code> 必须是非空数组，否则 400。 步骤 2：生成 <code>sccid_</code> / <code>sccsec_</code>，存入 <code>clients</code> Map。 步骤 3：返回 client_id/secret、grant_types、response_types。</p>\n<h4 id=\"s256-verifier\"><code>s256(verifier)</code></h4>\n<p>SHA-256，<code>digest('base64url')</code>。</p>\n<h4 id=\"issueaccess-clientid\"><code>issueAccess(clientId)</code></h4>\n<p>生成 <code>scat_</code> / <code>scrt_</code>，写入两个 Map，返回 rec。</p>\n<h4 id=\"verifyaccesstoken-token\"><code>verifyAccessToken(token)</code></h4>\n<p>步骤 1：假值 → <code>null</code>。 步骤 2：<code>token === config.secretKey</code> → <code>{ kind:'secret', clientId:'url-secret' }</code>（<strong>URL 密钥和 OAuth 走同一扇门</strong>）。 步骤 3：Map 里没有 → <code>null</code>。 步骤 4：<code>accessExp &lt; now()</code> → delete，返回 <code>null</code>。 步骤 5：<code>{ kind:'oauth', clientId }</code>。</p>\n<h4 id=\"revokeall\"><code>revokeAll</code></h4>\n<p>四个 Map <code>clear()</code>，<code>pairing=null</code>。</p>\n<h4 id=\"escapehtml-/-authorizehtml-query-error\"><code>escapeHtml</code> / <code>authorizeHtml(query, error)</code></h4>\n<p>HTML 转义；返回配对页 HTML（含隐藏字段 client_id / redirect_uri / state / code_challenge）。</p>\n<h4 id=\"completeauthorize-body\"><code>completeAuthorize(body)</code></h4>\n<p>步骤 1：未知 client_id → 400。 步骤 2：<code>redirect_uri</code> 不在注册列表 → 400。 步骤 3：<code>code_challenge_method</code> 缺省 S256；不是 S256 → 400。 步骤 4：<code>consumePairing(body.pairing_code)</code>。 步骤 5：发 <code>sccode_</code>，存 challenge 与过期。 步骤 6：拼 redirect URL，带 <code>code</code>；若有 <code>state</code> 也带上。返回该字符串。</p>\n<h4 id=\"handletoken-body\"><code>handleToken(body)</code></h4>\n<ul>\n<li><code>grant_type === 'authorization_code'</code>：</li>\n</ul>\n<ol>\n<li>code 不存在或过期 → 400，并不会发 token。</li>\n<li><strong>先 delete 该 code</strong>（一次性）。</li>\n<li>client_id 不符 → 400。</li>\n<li>redirect_uri 不符 → 400。</li>\n<li>无 <code>code_verifier</code> 或 <code>s256(verifier) !== rec.challenge</code> → 400「PKCE verification failed」。</li>\n<li><code>issueAccess</code>，<code>tokenResponse</code>。</li>\n</ol>\n<ul>\n<li><code>grant_type === 'refresh_token'</code>：</li>\n</ul>\n<ol>\n<li>无效或 refresh 过期 → 400。</li>\n<li>删旧 access 和 refresh。</li>\n<li>再 <code>issueAccess</code>。</li>\n</ol>\n<ul>\n<li>其它 grant → 400 <code>unsupported grant_type</code>。</li>\n</ul>\n<h4 id=\"tokenresponse-/-senderror\"><code>tokenResponse</code> / <code>sendError</code></h4>\n<p>标准 token JSON；<code>err.status || 500</code>，400 时 <code>error: invalid_request</code>，否则 <code>server_error</code>。</p>\n<h4 id=\"路由-全部挂在-oauth.router-上-由-index-挂到-app-根\">路由（全部挂在 oauth.router 上，由 index 挂到 app 根）</h4>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>方法</th>\n<th>路径</th>\n<th>行为</th>\n</tr></thead><tbody>\n<tr>\n<td>GET</td>\n<td><code>/.well-known/oauth-authorization-server</code></td>\n<td>metadata</td>\n</tr>\n<tr>\n<td>GET</td>\n<td><code>/.well-known/oauth-protected-resource</code></td>\n<td>资源元数据</td>\n</tr>\n<tr>\n<td>GET</td>\n<td><code>/.well-known/oauth-protected-resource/mcp</code></td>\n<td>同上</td>\n</tr>\n<tr>\n<td>POST</td>\n<td><code>/oauth/register</code> 与 <code>/register</code></td>\n<td><code>registerClient</code>，201；catch → sendError</td>\n</tr>\n<tr>\n<td>GET</td>\n<td><code>/oauth/authorize</code></td>\n<td><code>ensurePairing()</code>，返回 HTML</td>\n</tr>\n<tr>\n<td>POST</td>\n<td><code>/oauth/authorize</code></td>\n<td>try <code>completeAuthorize</code> → 302；catch 用错误信息重绘 HTML</td>\n</tr>\n<tr>\n<td>POST</td>\n<td><code>/oauth/token</code></td>\n<td><code>handleToken</code></td>\n</tr>\n<tr>\n<td>POST</td>\n<td><code>/oauth/revoke</code></td>\n<td>从两个 token Map delete `body.token</td>\n<td></td>\n<td>body.access_token<code>，始终 200 </code>{ revoked:true }`</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"4.8-src/mcp/server.js-277-行\">4.8 <code>src/mcp/server.js</code>（277 行）</h3>\n<p><strong>用途：</strong> Streamable HTTP JSON-RPC 2.0。 <strong>结果：</strong> <code>GET/POST /mcp</code> 与 <code>GET/POST /mcp/:secret</code>。 <strong>链条位置：</strong> 云上客户端 →（隧道，若已有 publicTunnelUrl）→ 这里 → <code>callTool</code>。</p>\n<p>常量 <code>SUPPORTED_PROTOCOL = ['2024-11-05','2025-03-26','2025-06-18']</code>。</p>\n<h4 id=\"extracttoken-req\"><code>extractToken(req)</code></h4>\n<p>顺序：</p>\n<ol>\n<li><code>Authorization</code> 匹配 <code>/^Bearer\\s+/i</code> → 去掉前缀 trim。</li>\n<li><code>req.params.secret</code>。</li>\n<li>头 <code>x-mcp-secret</code>。</li>\n<li><code>req.query.secret</code>。</li>\n<li>否则 <code>''</code>。</li>\n</ol>\n<h4 id=\"isauthorized-/-rejectunauthorized-/-requireauth\"><code>isAuthorized</code> / <code>rejectUnauthorized</code> / <code>requireAuth</code></h4>\n<p><code>oauth.verifyAccessToken(extractToken(req))</code> 真值才过。 拒绝：设 <code>WWW-Authenticate</code>，401 JSON-RPC <code>error.code = -32000</code>。</p>\n<h4 id=\"wantssse-req\"><code>wantsSse(req)</code></h4>\n<p><code>Accept</code> 头包含 <code>text/event-stream</code>。</p>\n<h4 id=\"sessionidfor-req\"><code>sessionIdFor(req)</code></h4>\n<p>头 <code>mcp-session-id</code>，否则随机 8 字节 hex。</p>\n<h4 id=\"sendjsonrpc-req-res-payload-httpstatus-200\"><code>sendJsonRpc(req, res, payload, httpStatus=200)</code></h4>\n<p>设 <code>Mcp-Session-Id</code>。若 SSE：写 <code>event: message\\ndata: …\\n\\n</code> 后 end。否则 <code>res.status().json</code>。</p>\n<h4 id=\"builtinprompts-/-promptsfromcustom\"><code>builtinPrompts</code> / <code>promptsFromCustom</code></h4>\n<p>内置一个 <code>connect</code>。再拼 <code>custom.prompts</code> 的 id/name/description（content 截 120）。</p>\n<h4 id=\"pickprotocol-params\"><code>pickProtocol(params)</code></h4>\n<p><code>protocolVersion</code> 在支持列表里就用它，否则 <code>'2025-03-26'</code>。</p>\n<h4 id=\"handlerpc-req-switch-method\"><code>handleRpc(req)</code> — <code>switch (method)</code></h4>\n<p>从 <code>req.body</code> 取 <code>id, method, params</code>。</p>\n<p><strong><code>initialize</code></strong></p>\n<ol>\n<li><code>clientInfo = params.clientInfo || { name:'External-Agent' }</code>。</li>\n<li><code>touch(req, { clientInfo, key: name@ip })</code>。</li>\n<li><code>eventBus.broadcast('agent_connected', { clientInfo, ip })</code>。</li>\n<li>返回 <code>protocolVersion</code>、capabilities（tools/resources/prompts listChanged、logging 空对象）、<code>serverInfo</code>、<strong><code>instructions: getInstructions()</code></strong>。</li>\n</ol>\n<p><strong><code>notifications/initialized</code> / <code>notifications/cancelled</code> / <code>logging/setLevel</code></strong> 返回 <code>{}</code>。</p>\n<p><strong><code>ping</code></strong> <code>touch(..., { incCall:true })</code>，返回 ok/ts/<code>busy:false</code>/session 摘要/host snapshot。</p>\n<p><strong><code>tools/list</code></strong> <code>touch(req)</code>，返回 <code>{ tools: getToolList() }</code>（<strong>不传 mode</strong>，列表含全部工具含 Code-only）。</p>\n<p><strong><code>tools/call</code></strong></p>\n<ol>\n<li>无 <code>params.name</code> → 抛 <code>ProtocolError E_BAD_ARGS</code>。</li>\n<li>broadcast <code>tool_call_start</code>，source <code>'Bridge-Remote'</code>。</li>\n<li><code>try</code>：<code>callTool(name, toolArgs || {}, remoteToolMode(params))</code> —— 默认 <code>'code'</code>；<code>params._meta.mode</code> 为 ask|plan|code 时用之。</li>\n<li>再 <code>clipJson</code>；<code>touch incCall</code>；broadcast 成功。</li>\n<li>把结果变成 MCP <code>content: [{ type:'text', text: clipText(...).text }]</code>，<code>isError:false</code>。</li>\n<li><code>catch</code>：<code>publicError</code>；<code>touch incCall+incFail</code>；broadcast 失败。<strong>始终</strong> <code>return { content:[{type:'text', text: JSON.stringify(info)}], isError:true }</code>。未知工具名、HASH_REQUIRED、STALE_FILE 都走这条。JSON-RPC <code>error</code> 只留给坏 jsonrpc / 未知 <strong>method</strong> / 缺 <code>params.name</code>。</li>\n</ol>\n<p><strong><code>resources/list</code></strong> → <code>listResources()</code>。 <strong><code>resources/read</code></strong> → 未知 uri 抛 <code>E_NOT_FOUND</code>。 <strong><code>prompts/list</code></strong> → <code>promptsFromCustom()</code>。 <strong><code>prompts/get</code></strong></p>\n<ul>\n<li><code>name === 'connect'</code>：返回一条 user 消息，含 <code>getBootstrapPrompt('(this MCP server)')</code> + 让它读 instructions。</li>\n<li>否则在 custom.prompts 里按 id 或 name find；没有抛 <code>E_NOT_FOUND</code>；有则把 <code>prompt.content</code> 当 user 文本。</li>\n</ul>\n<p><strong>default</strong> → <code>ProtocolError E_UNKNOWN_CMD</code>。</p>\n<h4 id=\"hoststatus\"><code>hostStatus</code></h4>\n<p>给 GET 非 SSE 用：online、tools 名列表、resources URI、完整 instructions、session、transports <code>['streamable-http','sse']</code>、auth <code>['url-secret','bearer','oauth']</code>。</p>\n<h4 id=\"handlepost\"><code>handlePost</code></h4>\n<p>步骤 1：<code>jsonrpc !== '2.0'</code> → 400，RPC <code>-32600</code>。 步骤 2：<code>try handleRpc</code>。若 method 以 <code>notifications/</code> 开头 → <strong>HTTP 204 无 body</strong>。否则 <code>sendJsonRpc</code> result。 步骤 3：<code>catch</code>：<code>E_UNKNOWN_CMD</code> → HTTP 404 且 rpc <code>-32601</code>；其它协议 → <code>-32602</code>；否则 <code>-32603</code>（或 <code>err.rpcCode</code>）。HTTP 默认 200。</p>\n<h4 id=\"handleget\"><code>handleGet</code></h4>\n<p>若 SSE：写 <code>event: endpoint\\ndata: /mcp\\n\\n</code>，每 15s 写 <code>: ping\\n\\n</code>，<code>req.on('close')</code> 清 interval。 否则 <code>res.json(hostStatus())</code>。</p>\n<p>路由：<code>GET/POST /</code> 与 <code>GET/POST /:secret</code> 都 <code>requireAuth</code> 后进上述 handler。</p>\n<hr />\n<h2 id=\"5.-工具层-真正改磁盘\">5. 工具层（真正改磁盘）</h2>\n<h3 id=\"5.1-src/utils/eventbus.js\">5.1 <code>src/utils/eventBus.js</code></h3>\n<p>单例 <code>BridgeEventBus extends EventEmitter</code>。</p>\n<ul>\n<li><code>addWsClient(ws)</code>：加入 Set；<code>close</code> 时 delete。</li>\n<li><code>broadcast(type, payload={})</code>：</li>\n</ul>\n<ol>\n<li>造 <code>{ type, timestamp ISO, payload }</code>。</li>\n<li><code>logs.unshift</code>；超过 <code>maxLogs=500</code> 则 <code>pop</code>。</li>\n<li>对每个 ws，<code>readyState === 1</code> 才 <code>try ws.send</code>，catch 空。</li>\n<li><code>this.emit(type, payload)</code>。</li>\n</ol>\n<ul>\n<li><code>getRecentLogs(limit=50)</code>：<code>logs.slice(0, limit)</code>。</li>\n</ul>\n<h3 id=\"5.2-src/utils/diff.js\">5.2 <code>src/utils/diff.js</code></h3>\n<p><code>createUnifiedDiff(filePath, oldContent, newContent)</code>：</p>\n<ol>\n<li><code>jsdiff.createTwoFilesPatch</code> 路径 <code>a/</code> <code>b/</code>。</li>\n<li><code>diffLines</code> 累计 added/removed 行数（<code>split('\\n').filter(Boolean).length</code>）。</li>\n<li>返回 <code>{ patch, additions, deletions, changes }</code>。</li>\n</ol>\n<h3 id=\"5.3-src/tools/sensitive.js\">5.3 <code>src/tools/sensitive.js</code></h3>\n<p>常量：<code>SENSITIVE_PATTERNS</code>（<code>.env</code>、密钥、<code>.ssh/</code>、<code>.webagent/config.json</code> 等）、<code>SENSITIVE_EXCEPTIONS</code>（<code>.env.example</code> 等）、<code>NOISE_NAMES</code>（<code>node_modules</code>、<code>.git</code>、<code>dist</code>…）。</p>\n<h4 id=\"toposix-/-globmatch\"><code>toPosix</code> / <code>globMatch</code></h4>\n<p>路径统一 <code>/</code>，去掉前导 <code>./</code>。 pattern 以 <code>/</code> 结尾：匹配该目录或前缀。 否则把 <code>*</code> / <code>**</code> 编成正则，对整路径或 basename 测试。</p>\n<h4 id=\"loadcustompatterns\"><code>loadCustomPatterns</code></h4>\n<p>读工作区 <code>.webagentignore</code>。不存在或 catch → <code>[]</code>。按行 trim，空行和 <code>#</code> 开头丢掉。</p>\n<h4 id=\"issensitive-relpath\"><code>isSensitive(relPath)</code></h4>\n<p>basename 在例外列表 → <strong>false</strong>。 先扫内置 patterns，命中 true。 再扫自定义：以 <code>!</code> 开头命中则 <strong>false</strong>（放行）；否则命中 true。 都没有 → false。</p>\n<h4 id=\"isnoise-/-ishidden-/-assertnotsensitive\"><code>isNoise</code> / <code>isHidden</code> / <code>assertNotSensitive</code></h4>\n<p>noise：路径任一段在 <code>NOISE_NAMES</code>。空或 <code>.</code> 不是 noise。 hidden = sensitive <strong>或</strong> noise。 assert：sensitive 则抛 <code>ACCESS_DENIED_SENSITIVE_FILE</code>，<code>err.code='E_FORBIDDEN'</code>。</p>\n<h3 id=\"5.4-src/tools/patchengine.js\">5.4 <code>src/tools/patchEngine.js</code></h3>\n<p><strong>用途：</strong> 路径沙箱 + sha256 + SEARCH/REPLACE。 <strong>链条位置：</strong> Code 模式改文件的首选。</p>\n<h4 id=\"computehash-content\"><code>computeHash(content)</code></h4>\n<p>sha256 hex，utf8。</p>\n<h4 id=\"toposixrel-/-isinsideworkspace-/-resolvesafepath-relpath\"><code>toPosixRel</code> / <code>isInsideWorkspace</code> / <code>resolveSafePath(relPath)</code></h4>\n<p>步骤 1：root 绝对化；incoming 把 <code>/</code> <code>\\</code> 换成 <code>path.sep</code>；<code>path.resolve(root, incoming)</code>。 步骤 2：<code>rel = path.relative(root, resolved)</code>，再 posix。 步骤 3：若 posix 是 <code>..</code>、以 <code>../</code> 开头、或 <code>path.isAbsolute(rel)</code> → 抛 <code>Security error: path &quot;…&quot; is outside workspace root.</code> 步骤 4：对 resolved 做 <code>realpath</code>（缺文件则对已存在的祖先 realpath 再拼回去）。真实路径仍必须落在工作区 realpath 之内，否则同样抛。这样 <strong>symlink / junction</strong> 指到工作区外会被拦住。 步骤 5：posix 非空且不是 <code>.</code> → <code>assertNotSensitive(posix)</code>。 步骤 6：返回逻辑绝对路径。</p>\n<h4 id=\"parsesearchreplaceblocks-patchtext\"><code>parseSearchReplaceBlocks(patchText)</code></h4>\n<p>正则 <code>&lt;&lt;&lt;&lt;&lt; SEARCH</code> … <code>=====</code> … <code>&gt;&gt;&gt;&gt;&gt; REPLACE</code>（5 个或更多 <code>&lt;</code>/<code>=</code>/<code>&gt;</code>），全局 exec，收集 <code>{ search, replace }</code>。</p>\n<h4 id=\"applypatch-filepath-patch-expectedhash-null-dryrun-false\"><code>applyPatch({ filePath, patch, expectedHash=null, dryRun=false })</code></h4>\n<p>步骤 1：<code>fullPath = resolveSafePath(filePath)</code>。</p>\n<p><strong>文件还不存在：</strong></p>\n<ol>\n<li>解析 blocks。默认 <code>newContent = patch</code>。若有 block 且 <strong>第一个 search trim 为空</strong> → <code>newContent = blocks[0].replace</code>（空 SEARCH 当新建）。</li>\n<li><code>dryRun</code> → 返回成功、<code>isNewFile:true</code>，不写盘。</li>\n<li><code>mkdirSync</code> 父目录，<code>writeFileSync</code>。</li>\n<li>对空旧内容做 unified diff，broadcast <code>file_patched</code>（含 newHash）；<code>rememberHash</code>。</li>\n<li>返回 success / isNewFile / newHash / <code>diffSummary: +N -0</code>。</li>\n</ol>\n<p><strong>文件已存在：</strong></p>\n<ol>\n<li>读全文，算 <code>currentHash</code>。没传 <code>expectedHash</code> 则用 <code>recalledHash(filePath)</code>（上次 <code>read_files</code> / 成功补丁 / 写入记下的 sha256）。</li>\n<li>仍无 hash 且非 <code>dryRun</code> → <code>ProtocolError E_BAD_ARGS</code>，消息 <code>HASH_REQUIRED</code>，<code>detail.currentHash</code>。</li>\n<li>若有 <code>expectedHash</code>，且既不等于也不当前缀 → <code>ExecutionError E_STALE_FILE</code>，detail 含 currentHash。</li>\n<li>解析 blocks。</li>\n<li><strong>有 blocks：</strong> 对每个 block：</li>\n</ol>\n<ul>\n<li>当前内容、search、replace 都把 <code>\\r\\n</code> 换成 <code>\\n</code>。</li>\n<li>若 <code>includes(normalizedSearch)</code> → <code>replace</code> 一次（String.replace 默认只换第一处）。</li>\n<li><strong>否则</strong> search <code>trim</code> 后非空且 includes(trimmed) → 用 trimmed 去换 <code>normalizedReplace.trim()</code>。</li>\n<li>再否则抛 <code>Patch conflict: SEARCH block #i could not be found</code>。</li>\n</ul>\n<ol>\n<li><strong>无 blocks：</strong></li>\n</ol>\n<ul>\n<li>若 <code>patch.startsWith('--- ')</code> 且 <code>includes('@@')</code> → <code>jsdiff.applyPatch</code>；<code>applied === false</code> 抛 unified diff 失败。</li>\n<li>否则 <strong>整段 patch 当作新文件内容</strong>（覆盖）。</li>\n</ul>\n<ol>\n<li>算 diff。<code>dryRun</code> → 返回 summary + diff，不写。</li>\n<li>写 <code>${fullPath}.tmp.${Date.now()}</code>，再 <code>renameSync</code> 到目标（同盘原子替换）。</li>\n<li><code>rememberHash</code>；broadcast <code>file_patched</code>，返回 newHash、diffSummary、diff。</li>\n</ol>\n<h3 id=\"5.5-src/tools/fileops.js\">5.5 <code>src/tools/fileOps.js</code></h3>\n<h4 id=\"readfiles-filepath-paths-offset-1-limit-400\"><code>readFiles({ filePath, paths, offset=1, limit=400 })</code></h4>\n<p>步骤 1：paths 数组先推进 list，再 push 单个 filePath。 步骤 2：list 空 → 抛 <code>read_files requires filePath or paths[]</code>。 步骤 3：恰好 1 个 → 直接 <code>readFile</code>（单对象，不是 <code>{ files:[] }</code>）。 步骤 4：多个：逐个 try <code>readFile</code>；catch 成 <code>{ filePath, error: err.message }</code>。返回 <code>{ files: [...] }</code>。</p>\n<h4 id=\"readfile-filepath-offset-1-limit-400\"><code>readFile({ filePath, offset=1, limit=400 })</code></h4>\n<p>步骤 1：resolveSafePath（含敏感拦截）。 步骤 2：不存在 → 抛 File not found。 步骤 3：是目录 → 抛去用 list_dir。 步骤 4：读 utf8，hash <strong>全文</strong>，按 <code>\\r?\\n</code> 分行。 步骤 5：窗口：<code>startIdx = max(0, offset-1)</code>，<code>endIdx = min(len, start+limit)</code>。 步骤 6：每行格式 <code></code> <code>${行号}: ${内容}</code> <code></code>（行号 1-based）。 步骤 7：broadcast <code>file_read</code>；<code>rememberHash(filePath, hash)</code>。返回 filePath/totalLines/offset/limit/hash/content。</p>\n<h4 id=\"deletefile-filepath-confirm-false\"><code>deleteFile({ filePath, confirm=false })</code></h4>\n<p>无 filePath 抛。resolve。相对路径空或 <code>.</code> → 拒绝删工作区根。<code>confirm</code> 假 → <code>E_BAD_ARGS</code>（须 <code>confirm=true</code>）。不存在抛。 是目录：<code>readdir</code> 非空 → 抛「not empty」；空则 <code>rmdirSync</code>。 是文件：<code>unlinkSync</code>。 broadcast <code>file_deleted</code>；<code>forgetHash</code>。返回 type directory/file。</p>\n<h4 id=\"renamefile-from-to-filepath-dest\"><code>renameFile({ from, to, filePath, dest })</code></h4>\n<p><code>srcRel = from || filePath</code>，<code>destRel = to || dest</code>。缺一抛。 两边都 resolveSafePath。源不存在抛。目标已存在抛。 <code>mkdirSync</code> 目标父目录，<code>renameSync</code>。broadcast <code>file_renamed</code>。</p>\n<h4 id=\"writefile-filepath-content-expectedhash-confirmoverwrite-false-confirm_overwrite\"><code>writeFile({ filePath, content, expectedHash, confirmOverwrite=false, confirm_overwrite=false })</code></h4>\n<p>已存在且未 <code>confirm_overwrite</code>：若 <code>expectedHash</code> 或 <code>recalledHash</code> 仍等于当前 sha256 则放行；否则 <code>E_BAD_ARGS</code>（detail.currentHash）。若传了 expectedHash 但不匹配 → <code>E_STALE_FILE</code>。然后 mkdir，写 <code>.tmp.${Date.now()}</code> 再 <code>renameSync</code>（失败则删 tmp）；broadcast <code>file_written</code>；<code>rememberHash</code>。</p>\n<h4 id=\"listdir-dirpath-.-recursive-false-maxdepth-3\"><code>listDir({ dirPath='.', recursive=false, maxDepth=3 })</code></h4>\n<p>不存在抛。内嵌 <code>scan(currentPath, currentDepth)</code>：</p>\n<ul>\n<li><code>currentDepth &gt; maxDepth</code> → <code>[]</code>。</li>\n<li>每个 entry：hidden 则 <code>continue</code>。</li>\n<li>目录：若 <code>recursive &amp;&amp; currentDepth &lt; maxDepth</code> 才扫 children。</li>\n<li>文件：带 size、mtime。  </li>\n</ul>\n<p>从 depth=1 开始 scan。返回 <code>{ dirPath, items }</code>。</p>\n<h4 id=\"grepsearch-query-searchpath-.-isregex-false-casesensitive-false-limit-20-cursor-\"><code>grepSearch({ query, searchPath='.', isRegex=false, caseSensitive=false, limit=20, cursor=0 })</code></h4>\n<p>步骤 1：resolve 起点。 步骤 2：编正则：<code>caseSensitive</code> 则 <code>'g'</code> 否则 <code>'gi'</code>；<code>isRegex</code> 假则先转义 query。<code>catch</code> → 抛 Invalid regex。 步骤 3：目录则递归 <code>searchInDir</code>：hidden skip；文件 try 读，按行 <code>pattern.test</code>，命中 push <code>{ file, line, content: trim }</code>，并 <code>pattern.lastIndex=0</code>；读失败 catch 空（跳过二进制等）。 步骤 4：起点是文件则只搜该文件。 步骤 5：分页：<code>pageSize = min(100, max(1, Number(limit)||20))</code>，<code>start = max(0, Number(cursor)||0)</code>，<code>nextCursor</code> 还有剩余则为 <code>start+page.length</code> 否则 <code>null</code>。 返回 <code>{ query, totalMatches, matches: page, cursor, nextCursor }</code>。</p>\n<h3 id=\"5.6-src/tools/executor.js\">5.6 <code>src/tools/executor.js</code></h3>\n<p>模块状态：<code>commandSequence</code>、<code>commandStore</code> Map、<code>children</code> Map、<code>MAX_CAPTURE = 200*1024</code>。</p>\n<h4 id=\"killchild-child-force-false\"><code>killChild(child, force=false)</code></h4>\n<p>无 pid return。 win32：<code>taskkill /pid /t /f</code>。catch 空。 非 Windows：先 <code>process.kill(-pid)</code> 杀进程组，失败再 <code>child.kill</code>。</p>\n<h4 id=\"workingdirfrom-cwd\"><code>workingDirFrom(cwd)</code></h4>\n<p>走 <code>resolveSafePath</code>（含真实路径）。逃出工作区抛 cwd outside。</p>\n<h4 id=\"publicrecord-rec-tail\"><code>publicRecord(rec, tail)</code></h4>\n<p>stdout/stderr 各 <code>slice(-limit)</code>，limit 在 500 与 MAX_CAPTURE 之间，默认 8000。 running 时 <code>suggestedWaitMs</code> 保留，hint 让去 poll；否则 suggestedWaitMs=0，hint undefined。</p>\n<h4 id=\"startprocess-command-cwd-.-timeoutsec-30\"><code>startProcess({ command, cwd='.', timeoutSec=30 })</code></h4>\n<p>步骤 1：<code>execId = ++commandSequence</code>，timeout 至少 1000ms。 步骤 2：rec 入 store，status <code>running</code>，<code>suggestedWaitMs = min(4000, max(800, round(timeoutMs/8)))</code>。 步骤 3：broadcast <code>command_started</code>。 步骤 4：win32 → <code>powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command</code>；否则 <code>/bin/bash -c</code>。非 Windows <code>detached:true</code>。env 加 <code>CI=true</code>、<code>TERM</code>、<code>FORCE_COLOR=1</code>。 步骤 5：timer：到点 <code>isTimeout=true</code>，<code>killChild</code>；再 2s 后若还在 children 里 <code>killChild(force true)</code>。 步骤 6：stdout/stderr <code>data</code>：追加，超 MAX_CAPTURE 截尾，broadcast <code>command_output</code>。 步骤 7：<code>error</code>：清 timer，status <code>error</code>，reject <code>Failed to start command</code>。 步骤 8：<code>close</code>：清 timer；若当前不是 <code>cancelled</code> 则 status 为 timeout 或 <code>done</code>；broadcast <code>command_finished</code>；resolve <code>publicRecord</code>。 返回 <code>{ rec, done }</code>（done 是 Promise）。</p>\n<h4 id=\"executecommand-/-startcommand\"><code>executeCommand</code> / <code>startCommand</code></h4>\n<p>execute：返回 <code>done</code>（等到结束）。 start：<code>done.catch</code> 把 status 设 error 并拼 stderr；<strong>立即</strong>返回 <code>{ execId, status:'running', command, suggestedWaitMs, hint }</code>。</p>\n<h4 id=\"getcommandoutput-execid-commandid-tail\"><code>getCommandOutput({ execId, commandId, tail })</code></h4>\n<p>id = <code>String(execId || commandId || commandSequence)</code>。没有 rec → <code>{ found:false, message }</code>。否则 publicRecord。</p>\n<h4 id=\"cancelcommand-execid\"><code>cancelCommand({ execId })</code></h4>\n<p>没有 rec → found false。 <code>status !== 'running'</code> 或没有 child → cancelled false。 否则 force kill，status <code>cancelled</code>。</p>\n<h4 id=\"sendcommandinput\"><code>sendCommandInput</code></h4>\n<p><strong>恒定</strong>返回 <code>{ ok:false, message: 'Interactive PTY is not enabled…' }</code>，不写 stdin。</p>\n<h4 id=\"wait-ms-800\"><code>wait({ ms=800 })</code></h4>\n<p>delay clamp 到 0–15000，<code>setTimeout</code> 后 <code>{ waitedMs }</code>。</p>\n<h3 id=\"5.7-src/tools/gitops.js\">5.7 <code>src/tools/gitOps.js</code></h3>\n<h4 id=\"notgitresult-extra\"><code>notGitResult(extra={})</code></h4>\n<p>返回 <code>{ ok:true, available:false, git:false, ... hint 不要擅自 git init }</code>。</p>\n<h4 id=\"git-args-timeoutms-8000\"><code>git(args, timeoutMs=8000)</code></h4>\n<p><code>spawnSync('git', ['-c','color.ui=never', ...args], { cwd: workspaceRoot, encoding utf8, timeout, maxBuffer 512KB })</code>。 启动失败：ENOENT/<code>not found</code> → <code>Error</code> <code>code='GIT_UNAVAILABLE'</code>，其它 <code>E_INTERNAL</code>。 <code>status !== 0</code>：127 / not a git repository / command not found → <code>GIT_UNAVAILABLE</code>；否则 <code>E_INTERNAL</code> 截 800 字。 成功返回 stdout。</p>\n<h4 id=\"gitstatus\"><code>gitStatus</code></h4>\n<p>try porcelain v1 -b；成功 <code>{ ok, available:true, git:true, branch, dirty, summary, files≤80, truncated }</code>。catch 仅 <code>GIT_UNAVAILABLE</code> → <code>notGitResult()</code>，其它再抛。</p>\n<h4 id=\"gitdiff-filepath-staged-false-stat-false\"><code>gitDiff({ filePath, staged=false, stat=false })</code></h4>\n<p>args 从 <code>diff</code> 起；staged 加 <code>--cached</code>；stat 加 <code>--stat</code>；然后 <code>--</code>；有 filePath 则 <code>resolveSafePath</code> 后相对路径（空则 <code>.</code>）。 输出行数 cap：stat 80 否则 200。同样把 <code>GIT_UNAVAILABLE</code> 收成 <code>notGitResult</code>。</p>\n<h3 id=\"5.8-src/tools/findfiles.js\">5.8 <code>src/tools/findFiles.js</code></h3>\n<h4 id=\"globtoregexp-glob\"><code>globToRegExp(glob)</code></h4>\n<p>默认 <code>**/*</code>。反斜杠改 <code>/</code>。先转义正则特殊字符，再 <code>**</code>→<code>.*</code>，<code>*</code>→<code>[^/]*</code>，<code>?</code>→<code>[^/]</code>。整串 <code>^…$</code>。</p>\n<h4 id=\"findfiles-glob-/-searchpath-.-maxresults-40\"><code>findFiles({ glob='**/*', searchPath='.', maxResults=40 })</code></h4>\n<p>起点不存在抛。内嵌 <code>walk</code>：readdir 失败 catch 直接 return。真实路径在工作区外或是符号链接则 skip。hidden skip。目录递归。文件：<code>re.test(rel) || re.test(name) || glob==='**/*'</code> 再套一层相同条件才 push。满 maxResults 停止。 起点是文件则只 push 它自己。返回 <code>{ glob, searchPath, total, files }</code>。</p>\n<h3 id=\"5.9-src/tools/skills.js\">5.9 <code>src/tools/skills.js</code></h3>\n<h4 id=\"skillroots\"><code>skillRoots</code></h4>\n<p>两个：<code>.webagent/skills</code> 与工作区根下 <code>skills/</code>。</p>\n<h4 id=\"listskills\"><code>listSkills</code></h4>\n<p>每个 root 不存在 continue。对每个名字：必须是目录且有 <code>SKILL.md</code> 才 push <code>{ name, path 相对, preview: 前 240 字 }</code>。</p>\n<h4 id=\"loadskill-name\"><code>loadSkill({ name })</code></h4>\n<p>无 name → <code>{ skills, hint }</code>（有/无 skill 两套 hint）。 有 name 找不到 → <code>{ found:false, available, hint }</code>。 找到 → 读 SKILL.md <strong>最多 8000 字</strong>，<code>{ found:true, name, path, content }</code>。 演示工作区有 <code>fix-tests</code> / <code>review</code> / <code>docs-sync</code>（约定：改功能必须同步改说明书；动到设计理由时改 <code>架构导读.md</code>）。</p>\n<h3 id=\"5.10-src/tools/workspaceinfo.js\">5.10 <code>src/tools/workspaceInfo.js</code></h3>\n<h4 id=\"workspaceinfo\"><code>workspaceInfo</code></h4>\n<ol>\n<li>loadCustom，resolveEnvironment / resolveTechStack。  </li>\n<li><code>try gitStatus</code> → <code>{ available:true, ... }</code>；<code>catch</code> → <code>{ available:false, error: err.message }</code>。  </li>\n<li><code>try listDir({ dirPath:'.', recursive:false, maxDepth:1 })</code> 映射 name/type/path；catch → <code>[]</code>。  </li>\n<li><code>try</code> 读工作区 <code>package.json</code> 的 <code>name</code>；不存在或坏 JSON → <code>null</code>。  </li>\n<li>返回 root、packageName、git（只把 available/branch/error 放出）、environment、techStack、skills 名列表、topLevel、hint 不要 dump 整树。</li>\n</ol>\n<h3 id=\"5.11-src/tools/progresstracker.js\">5.11 <code>src/tools/progressTracker.js</code></h3>\n<p>模块级 <code>currentTaskState</code> 初始 idle。</p>\n<h4 id=\"reportprogress-message-percentage-0-stepname\"><code>reportProgress({ message, percentage=0, stepName='' })</code></h4>\n<p><code>status</code>：percentage≥100 → <code>completed</code> 否则 <code>in_progress</code>。progress clamp 0–100。stepName 空则保留旧值。broadcast <code>progress_updated</code>。返回 success/progress/stepName/message。</p>\n<h4 id=\"settodos-todos\"><code>setTodos({ todos=[] })</code></h4>\n<p>每项补 id（缺省 <code>todo-N</code>）、title（<code>title||text||description||'Task item'</code>）、status 默认 pending。broadcast <code>todos_updated</code>。</p>\n<h4 id=\"gettaskstate-/-resettaskstate\"><code>getTaskState</code> / <code>resetTaskState</code></h4>\n<p>直接返回；reset 回到 idle 并 broadcast。</p>\n<h3 id=\"5.12-src/tools/consensusengine.js\">5.12 <code>src/tools/consensusEngine.js</code></h3>\n<p><strong>用途：</strong> Plan 模式三分支。 <strong>源码事实：</strong> 不调用任何 LLM API。三份 <code>planA/B/C</code> 是函数里写死的字符串，中间 <code>sleep(200)</code> / <code>sleep(180)</code>。<code>consensusReached: true</code>、<code>agreementRate: '97%'</code>、<code>disagreements: []</code> 也是写死的。</p>\n<h4 id=\"runmultimodelconsensus-taskdescription-facts-emit\"><code>runMultiModelConsensus({ taskDescription, facts={}, emit })</code></h4>\n<p>步骤 1：task 默认「评估当前工作区…」；从 facts 取 files/readme/pkg/testOutput/testCmd。 步骤 2：broadcast <code>consensus_started</code>，models 三个名字。 步骤 3：若有 emit，发 status；sleep 200；构造 planA（架构），emit <code>branch</code>。 步骤 4：同样 planB（安全）、planC（编码）。 步骤 5：emit 合并中，sleep 180。 步骤 6：<code>unifiedActionPlan</code> 三条 pending。 步骤 7：broadcast <code>consensus_finished</code>，返回 result。</p>\n<h3 id=\"5.13-src/tools/index.js-435-行\">5.13 <code>src/tools/index.js</code>（435 行）</h3>\n<p><strong>用途：</strong> 注册表 + 模式锁 + 危险命令闸。 <strong>结果：</strong> <code>getToolList</code> / <code>callTool</code>。 <strong>链条位置：</strong> MCP 与 Chat 的唯一执行入口。</p>\n<p><code>tool(def)</code> 原样返回。另有 <code>normalize.js</code>（<code>bash</code>→<code>run_command</code>、<code>cat</code>→<code>read_files</code>、<code>path</code>→<code>filePath</code>、<code>&quot;true&quot;</code>→布尔）和 <code>readCache.js</code>（路径→sha256，落盘 <code>.webagent/read-hashes.json</code>；<code>reset-round</code> 会 <code>resetHashes</code>）。<code>DANGEROUS_RE</code> 还匹配 <code>git push</code> / <code>git reset --hard</code> / <code>git checkout --</code> / <code>git clean -f</code> / <code>format x:</code> / <code>del /s</code> / <code>rd /s</code> / <code>Remove-Item -Recurse</code> / <code>drop database</code> / <code>iex</code> / <code>iwr</code> / <code>curl|wget</code> 管道进 shell / <code>powershell -enc</code> / <code>certutil -urlcache</code> / <code>bitsadmin</code> / <code>reg add</code> / <code>net user</code> / <code>schtasks</code>。</p>\n<p><code>TOOLS</code> 现登记 <strong>25</strong> 个名字（测试锁长度）。摘要：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>name</th>\n<th>aliases</th>\n<th>mode</th>\n<th>handler</th>\n</tr></thead><tbody>\n<tr>\n<td>ping</td>\n<td></td>\n<td>ask,plan,code</td>\n<td>pingHost</td>\n</tr>\n<tr>\n<td>workspace_info</td>\n<td></td>\n<td>同上</td>\n<td>workspaceInfo</td>\n</tr>\n<tr>\n<td>get_capabilities</td>\n<td></td>\n<td>同上</td>\n<td>getCapabilities</td>\n</tr>\n<tr>\n<td>get_logs</td>\n<td></td>\n<td>同上</td>\n<td>getLogs（maxLines clamp 1–200）</td>\n</tr>\n<tr>\n<td>get_task_status</td>\n<td></td>\n<td>同上</td>\n<td>getTaskStatus</td>\n</tr>\n<tr>\n<td>remember</td>\n<td></td>\n<td>同上</td>\n<td>remember</td>\n</tr>\n<tr>\n<td>recall</td>\n<td></td>\n<td>同上</td>\n<td>recall</td>\n</tr>\n<tr>\n<td>list_directory</td>\n<td>list_dir</td>\n<td>同上</td>\n<td>listDir</td>\n</tr>\n<tr>\n<td>find_files</td>\n<td></td>\n<td>同上</td>\n<td>findFiles</td>\n</tr>\n<tr>\n<td>search_files</td>\n<td>grep_search</td>\n<td>同上</td>\n<td>grepSearch</td>\n</tr>\n<tr>\n<td>read_files</td>\n<td>read_file</td>\n<td>同上</td>\n<td>readFiles</td>\n</tr>\n<tr>\n<td>git_status</td>\n<td></td>\n<td>同上</td>\n<td>gitStatus</td>\n</tr>\n<tr>\n<td>git_diff</td>\n<td></td>\n<td>同上</td>\n<td>gitDiff</td>\n</tr>\n<tr>\n<td>load_skill</td>\n<td></td>\n<td>同上</td>\n<td>loadSkill</td>\n</tr>\n<tr>\n<td>apply_patch</td>\n<td></td>\n<td><strong>code</strong></td>\n<td>applyPatch</td>\n</tr>\n<tr>\n<td>write_file</td>\n<td></td>\n<td><strong>code</strong></td>\n<td>writeFile</td>\n</tr>\n<tr>\n<td>delete_file</td>\n<td></td>\n<td><strong>code</strong></td>\n<td>deleteFile</td>\n</tr>\n<tr>\n<td>rename_file</td>\n<td>move_file</td>\n<td><strong>code</strong></td>\n<td>renameFile</td>\n</tr>\n<tr>\n<td>run_command</td>\n<td>execute_command</td>\n<td><strong>code</strong></td>\n<td>executeCommand</td>\n</tr>\n<tr>\n<td>start_command</td>\n<td></td>\n<td><strong>code</strong></td>\n<td>startCommand</td>\n</tr>\n<tr>\n<td>get_command_output</td>\n<td></td>\n<td>ask,plan,code</td>\n<td>getCommandOutput</td>\n</tr>\n<tr>\n<td>cancel_command</td>\n<td></td>\n<td><strong>code</strong></td>\n<td>cancelCommand</td>\n</tr>\n<tr>\n<td>wait</td>\n<td></td>\n<td>ask,plan,code</td>\n<td>wait</td>\n</tr>\n<tr>\n<td>report_progress</td>\n<td></td>\n<td><strong>plan,code</strong></td>\n<td>reportProgress</td>\n</tr>\n<tr>\n<td>set_todos</td>\n<td></td>\n<td>ask,plan,code</td>\n<td>setTodos</td>\n</tr>\n</tbody></table></div>\n<p>加载时把 name 和 aliases 都放进 <code>toolRegistry</code> Map。</p>\n<h4 id=\"gettoollist-currentmode-null\"><code>getToolList(currentMode=null)</code></h4>\n<p><code>currentMode</code> 假 → 全部；真则 <code>t.mode.includes(currentMode)</code>。映射为 <code>{ name, description, inputSchema }</code>（不含 handler/mode）。</p>\n<h4 id=\"calltool-name-args-currentmode-null\"><code>callTool(name, args={}, currentMode=null)</code></h4>\n<p>步骤 1：<code>resolveToolName(name)</code>；registry 先查 resolved 再查原名。没有 → <code>E_UNKNOWN_CMD</code>，消息含 <code>Available:</code> 列表。 步骤 2：<code>currentMode</code> 真 <strong>且</strong> 该工具 mode 列表不含它 → <code>E_BAD_ARGS</code>，文案让切 CODE。远程 MCP 默认传入 <code>'code'</code>；仅当第三参为 <code>null</code>/<code>''</code> 才跳过锁。 步骤 3：<code>normalizeToolArgs</code>。若工具名是 <code>run_command</code> 或 <code>start_command</code>，且命令匹配 <code>DANGEROUS_RE</code>，且没有 <code>confirm_dangerous</code> → <code>E_BAD_ARGS</code>。 步骤 4：<code>await toolDef.handler(input)</code>。 步骤 5：若 <code>result &amp;&amp; result.isTimeout</code>：打 <code>code='E_TIMEOUT'</code>，<code>suggestedWaitMs=0</code>。 步骤 6：<code>return clipJson(result)</code>。</p>\n<p><code>getTaskStatus</code>：running 则 suggestedWaitMs=2000，<code>etaSeconds = max(1, round((100-progress)/10))</code>；否则 0。</p>\n<hr />\n<h2 id=\"6.-本机-chat-与-rest\">6. 本机 Chat 与 REST</h2>\n<h3 id=\"6.1-src/agent/providers.js\">6.1 <code>src/agent/providers.js</code></h3>\n<h4 id=\"normalizebase-url\"><code>normalizeBase(url)</code></h4>\n<p>trim，去掉末尾 <code>/</code>，再去掉末尾 <code>/chat/completions</code>（i 标志）。</p>\n<h4 id=\"guesscaps-/-guesscontext\"><code>guessCaps</code> / <code>guessContext</code></h4>\n<p>id 小写。caps 恒含 <code>'工具'</code>；匹配 vision|image|gpt-4o|flash|pro 加视觉；video 加视频。 context：video|image → <code>1.3M</code>；mini|haiku|lite → <code>128K</code>；否则 <code>1.3M</code>。</p>\n<h4 id=\"listremotemodels-baseurl-apikey\"><code>listRemoteModels(baseUrl, apiKey)</code></h4>\n<p>无 base → 抛「API Endpoint URL 不能为空」。无 key → 「API Key 不能为空」。 <code>GET ${base}/models</code> Bearer。<code>!resp.ok</code> 抛 HTTP 状态 + 正文前 200。 JSON.parse 失败 → 「不是 JSON」。 <code>data.data</code> 数组，否则 <code>data</code> 本身是数组，否则 <code>[]</code>；空则抛。 hostname 去掉 <code>api.</code> 取第一段当 group。每项 <code>{ id, name, group, contextSize, caps, pricing }</code>。</p>\n<h3 id=\"6.2-src/agent/openai.js\">6.2 <code>src/agent/openai.js</code></h3>\n<h4 id=\"toollabel-/-systemprompt-mode\"><code>toolLabel</code> / <code>systemPrompt(mode)</code></h4>\n<p>Ask/Plan：只读锁文案。Code：允许 patch/命令。 回复语言：follow-user / en / 默认中文。 Windows 或 powershell → 提示 Prefer PowerShell。 plan 再加一句不要改仓库。 拼 custom.instructions 与 <code>formatWorkspaceContext</code>。</p>\n<h4 id=\"runopenai-mode-message-history-emit-model\"><code>runOpenAI({ mode, message, history=[], emit, model })</code></h4>\n<p>步骤 1：baseUrl 去尾 <code>/</code>，空则抛。 步骤 2：<code>getToolList(mode)</code> 转成 OpenAI function tools（Ask 时列表里<strong>没有</strong> apply_patch）。 步骤 3：messages = system + history 里 user/assistant 且有 content 的最后 12 条 + 当前 user。 步骤 4：最多 <strong>10</strong> 轮：</p>\n<ul>\n<li>emit status。</li>\n<li><code>POST ${base}/chat/completions</code>，<code>tool_choice:'auto'</code>，<code>temperature:0.2</code>。</li>\n<li><code>!resp.ok</code> 抛 HTTP + 正文前 240。</li>\n<li>JSON 失败抛。无 <code>choices[0].message</code> 抛。</li>\n<li>把 msg push 进 messages。</li>\n<li><strong>若有 tool_calls</strong>：最多 8 个；arguments JSON.parse 失败当 <code>{}</code>；<code>try callTool(name, args, mode)</code> 成功则 role tool 内容 <code>JSON.stringify(result).slice(0,12000)</code>；catch 则 <code>ERROR: message</code>。然后 <code>continue</code> 下一轮。</li>\n<li><strong>无 tool_calls</strong>：emit <code>message</code>（content 空则「（无文本输出）」），<strong>return</strong>。</li>\n</ul>\n<p>步骤 5：10 轮用尽：emit「已达到最大工具轮次。」</p>\n<h3 id=\"6.3-src/agent/runchat.js-387-行\">6.3 <code>src/agent/runChat.js</code>（387 行）</h3>\n<p><strong>用途：</strong> <code>POST /api/chat</code> 的实现。 <strong>结果：</strong> 通过 <code>emit(type, data)</code> 写 NDJSON。</p>\n<h4 id=\"辅助\">辅助</h4>\n<ul>\n<li><code>flattenDir</code> / <code>countFiles</code>：递归摊平。</li>\n<li><code>toolLabel</code>：给 UI 的短标签（Explored / Found N files / Read path / …）。失败时 list/read 也给固定英文标签。</li>\n<li><code>timedTool(emit, mode, name, args)</code>：计时 <code>callTool</code>；成功/失败都 emit <code>tool</code>；返回 <code>{ ok, result|error, durationMs }</code>。<strong>不把异常抛出</strong>。</li>\n<li><code>keywordsFrom(message)</code>：按空白和中文标点切，去掉停用词，长度≥2，最多 6 个。</li>\n<li><code>pickExisting(relPaths)</code>：工作区里存在且是文件的留下。</li>\n<li><code>detectTestCommand</code>：先 <code>resolveTechStack(loadCustom()).testCommand</code>；否则 package.json scripts.test → <code>npm test</code>（这里<strong>写死 npm</strong>，不用探测到的包管理器）；再 pytest / cargo / go；再若存在 <code>tests/</code> 目录 → <code>{ cmd:'npm test', kind:'guess' }</code>；都没有 → <code>null</code>。</li>\n<li><code>extractPatch</code>：消息里第一段 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; SEARCH</code> … <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt; REPLACE</code>。</li>\n<li><code>extractWriteIntent</code>：同时有代码围栏和「写入|创建|write|create + 带扩展名路径」才返回 <code>{ filePath, content }</code>。</li>\n<li><code>clip</code> / <code>stripLineNumbers</code>：去 <code>^\\d+:\\s?</code>。</li>\n</ul>\n<h4 id=\"explore-emit-mode-message\"><code>explore(emit, mode, message)</code></h4>\n<ol>\n<li><code>list_directory</code> recursive maxDepth 3，最多收 80 个文件路径。  </li>\n<li><code>git_status</code>。  </li>\n<li><code>find_files **/*</code> maxResults 100，合并进 files。  </li>\n<li>若 keywords 非空：<code>search_files</code> query 为前 3 个 keyword 用 <code>|</code> 拼，<code>isRegex:true</code>，limit 30。  </li>\n<li>候选 README/package.json/pyproject/Cargo/go.mod 里存在的先读；再从 files 补到最多 6 个（跳过 <code>SKIP_DIRS</code> 顶层）。  </li>\n<li><code>read_files</code> limit 120；readme / package.json 填进 facts。  </li>\n<li><code>detectTestCommand</code> 写入 facts.testCmd（默认已是 <code>npm test</code>）。  </li>\n</ol>\n<p>返回 facts。<strong>explore 不写文件。</strong></p>\n<h4 id=\"summarizeask-message-facts\"><code>summarizeAsk(message, facts)</code></h4>\n<p>拼工作区路径、用户问题、文件列表（最多 30）、README/pkg 摘要、测试命令，结尾声明这是只读 Ask。</p>\n<h4 id=\"runbuiltin-payload-emit\"><code>runBuiltin(payload, emit)</code></h4>\n<p>步骤 1：mode 默认 <code>ask</code>。先 <code>set_todos</code> 三条。 步骤 2：<code>facts = await explore(...)</code>。再更新 todos。 步骤 3：<code>mode === 'plan'</code>：</p>\n<ul>\n<li><code>store.load().multiModel.enabled === false</code> → emit 一段「未开多模型博弈」文本，<strong>return</strong>（不跑 consensusEngine）。</li>\n<li>否则 <code>runMultiModelConsensus({ taskDescription: message, facts, emit })</code>，把 unifiedActionPlan 写入 todos，emit consensus + message，<strong>return</strong>。</li>\n</ul>\n<p>步骤 4：<code>mode === 'ask'</code>：todos 全 completed，emit summarizeAsk，<strong>return</strong>。 步骤 5：到这里视为 <strong>code</strong>：</p>\n<ul>\n<li>若 <code>extractWriteIntent</code> 有值 → <code>write_file</code>。</li>\n<li>若 <code>extractPatch</code> 有值：从消息匹配 <code>file|文件</code> 路径，否则 <code>facts.files[0]</code>；有 target 则先 <code>read_files</code> 取 hash，成功才 <code>apply_patch</code>。</li>\n<li><code>detectTestCommand()</code> 有则 <code>run_command</code> timeoutSec 60，stdout+stderr 进 facts.testOutput。</li>\n<li>再 set_todos completed。</li>\n<li>emit 摘要：是否写入、是否尝试补丁、测试输出 clip 1200；并写明「内置循环没有大模型」。</li>\n</ul>\n<h4 id=\"runchat-payload-emit\"><code>runChat(payload = {}, emit)</code></h4>\n<p>步骤 1：<code>send = typeof emit === 'function' ? emit : payload.emit</code>（HTTP <code>POST /api/chat</code> 把函数放进对象；单测传第二参）。 步骤 2：<code>store.load()</code> 找 <code>activeModelId</code> 对应模型。 步骤 3：<strong>若</strong> <code>active.apiKey &amp;&amp; active.baseUrl &amp;&amp; active.modelId</code> 都真 → <code>runOpenAI({ ...payload, model: active, emit: send })</code>。 步骤 4：<strong>否则</strong> <code>runBuiltin(payload, send)</code>。</p>\n<h3 id=\"6.4-src/api/routes.js-378-行\">6.4 <code>src/api/routes.js</code>（378 行）</h3>\n<p><strong>用途：</strong> 工作台 / 插件 REST，挂在 <code>/api</code>。</p>\n<h4 id=\"publicorigin-/-mcporigin-/-mcpinfo\"><code>publicOrigin</code> / <code>mcpOrigin</code> / <code>mcpInfo</code></h4>\n<p>publicOrigin：跟 oauth.requestOrigin 类似，但 fallback host 用 <code>workbenchPort</code>。 mcpOrigin：有 <code>config.publicTunnelUrl</code> 用它，否则 publicOrigin。 mcpInfo：拼 <code>/mcp/${secretKey}</code>、canonical <code>/mcp</code>、prompt、clients、pairing、<code>tunnel.snapshot()</code>。</p>\n<h4 id=\"各路由-逐步\">各路由（逐步）</h4>\n<p><strong>GET <code>/status</code></strong> 拼 online、端口、workspace、tools、taskState、recentLogs 40 条、bridgeRunning、mcpInfo 展开、models（apiKey 变成 <code>hasKey</code> 布尔）、activeModelId、multiModel、bridgeAccount、mcpSession。</p>\n<p><strong>POST <code>/bridge/reset-secret</code></strong> 记下 oldSecret → <code>generateNewSecret()</code>（内存 + <code>config.json</code>）→ <code>oauth.revokeAll()</code> → broadcast <code>secret_rotated</code> → json success + mcpInfo。</p>\n<p><strong>POST <code>/bridge/start</code></strong>（async）</p>\n<ol>\n<li><code>store.load()</code>；<code>!loggedIn || !deviceAuthorized</code> → <strong>403</strong>，Chat 不受影响。</li>\n<li>provider = body.tunnelProvider 或 cfg 或 <code>'cloudflare'</code>。</li>\n<li><code>store.patch({ bridge: { tunnelProvider: provider } })</code>。</li>\n<li><code>config.bridgeRunning = true</code>，<code>config.tunnelProvider = provider</code>。</li>\n<li><code>oauth.ensurePairing()</code>。</li>\n<li><code>provider === 'cloudflare'</code> 时 <strong><code>await tunnel.startQuickTunnel({ port: config.port })</code></strong>。catch 记下 <code>tunnelError</code>（无二进制、超时、spawn 错），<strong>不</strong> 500。</li>\n<li><code>mcpInfo</code>：有 <code>tunnel.url</code> 则 note「Quick Tunnel 已就绪」；有 <code>tunnelError</code> 则 note 带错误 +「MCP 暂走当前页面源」；否则 note 写未启动 Quick Tunnel（Named / ngrok 不 spawn）。</li>\n<li>broadcast <code>bridge_started</code>（含 tunnelUrl / tunnelError），json success、running、provider、tunnelError、note、mcpInfo。</li>\n</ol>\n<p><strong>POST <code>/bridge/stop</code></strong> <strong><code>tunnel.stopTunnel()</code></strong>；<code>config.bridgeRunning = false</code>；broadcast；json 带 mcpInfo。</p>\n<p><strong>POST <code>/bridge/reset-round</code></strong> <code>session.reset()</code> + <code>resetHashes()</code> + broadcast <code>bridge_round_reset</code>。</p>\n<p><strong>POST <code>/consensus/run</code></strong> <code>runMultiModelConsensus({ taskDescription })</code>；失败 500。</p>\n<p><strong>POST <code>/tool/call</code></strong> body <code>{ name, arguments, mode='code' }</code>。broadcast start source <code>Chat-${mode}</code> → <code>callTool(name, toolArgs, mode)</code> → 成功 200；catch 400 <code>{ error: err.message }</code>。</p>\n<p><strong>POST <code>/chat</code></strong> 设 NDJSON、<code>X-Accel-Buffering: no</code>，flushHeaders。 <code>emit</code> = <code>res.write(JSON.stringify({ type, ...data })+'\\n')</code>。 try <code>runChat({ mode, message, history, emit })</code>（emit 在对象里，由 <code>runChat</code> 取出）然后 emit <code>done</code>；catch emit <code>error</code>。最后 <code>res.end()</code>。</p>\n<p><strong>POST <code>/tasks/reset</code></strong> → <code>resetTaskState()</code>。</p>\n<p><strong>GET <code>/files/tree</code></strong> → <code>callTool('list_directory', { dirPath:'.', recursive:true, maxDepth:5 }, 'ask')</code>。</p>\n<p><strong>GET <code>/files/content</code></strong> query.path → resolveSafePath。不存在或是目录 → 404。否则全文 + hash + size，并 <code>rememberHash</code>。</p>\n<p><strong>PUT <code>/files/content</code></strong> body.path 与 content 必须；content 必须是 string。<code>callTool('write_file', { confirm_overwrite:true, expectedHash }, 'code')</code>。敏感/越界 400；<code>STALE_FILE</code> 409。工作台保存走这一条，不再直接 <code>writeFileSync</code>。</p>\n<p><strong>GET <code>/skills</code></strong> 扫两个 skills 根目录，有 SKILL.md 则 preview 前 400 字。 （与 <code>tools/skills.listSkills</code> 类似，但这里不要求 <code>statSync(dir).isDirectory()</code>。）</p>\n<p><strong>POST <code>/providers/probe</code></strong> → <code>listRemoteModels</code>，失败 400。</p>\n<p><strong>GET <code>/models</code></strong> → models 的 apiKey 显示成 <code>••••</code> 或 <code>''</code>。</p>\n<p><strong>POST <code>/models</code></strong> 可改 activeModelId；可整表替换 models；可 upsert 单个 <code>body.model</code>；可合并 multiModel。然后 <code>store.save(cfg)</code>（<strong>整份 cfg</strong>，不是 patch）。</p>\n<p><strong>GET <code>/logs</code></strong> → 80 条。 <strong>GET <code>/profile/detect</code></strong> → detectEnvironment + detectTechStack + listSkills。 <strong>GET/PUT <code>/customizations</code></strong> → load / patchCustom。</p>\n<p><strong>POST <code>/skills</code></strong> name trim，非单词字符变 <code>-</code>，去首尾 <code>-</code>，最长 40；空则 400。content 默认一个中文模板。<code>callTool('write_file', { confirm_overwrite:true }, 'code')</code> 写 <code>.webagent/skills/&lt;name&gt;/SKILL.md</code>。</p>\n<p><strong>POST <code>/bridge/login</code></strong> 默认 github / demo，patch loggedIn true、永久顺、deviceAuthorized true。</p>\n<p><strong>POST <code>/bridge/logout</code></strong> patch loggedIn false；<strong><code>tunnel.stopTunnel()</code></strong>；<code>config.bridgeRunning=false</code>。</p>\n<hr />\n<h2 id=\"7.-隧道-src/tunnel/cloudflared.js\">7. 隧道 <code>src/tunnel/cloudflared.js</code></h2>\n<p><strong>用途：</strong> 找二进制、拉 Quick Tunnel、从日志解析 <code>https://….trycloudflare.com</code>。 <strong>结果：</strong> 成功则 <code>config.publicTunnelUrl = url</code>。 <strong>链条位置：</strong> 模块被 <code>routes.js</code> require。<code>/api/status</code> 暴露 <code>snapshot()</code>；<strong><code>POST /bridge/start</code>（cloudflare）<code>await startQuickTunnel</code></strong>；<code>/bridge/stop</code> 与 <code>/bridge/logout</code> 调 <code>stopTunnel</code>。Named / ngrok 不进本文件。</p>\n<p>模块状态：<code>child</code>、<code>quickUrl</code>。<code>URL_RE</code> 匹配 trycloudflare.com。</p>\n<h4 id=\"parsetunnelurl-chunk\"><code>parseTunnelUrl(chunk)</code></h4>\n<p>正则命中则去尾 <code>/</code>，否则 null。</p>\n<h4 id=\"findcloudflared\"><code>findCloudflared</code></h4>\n<ol>\n<li><code>CLOUDFLARED_PATH</code> 存在则用。  </li>\n<li><code>where</code>（win）或 <code>which</code>，stdout 按行找 <code>existsSync</code>。  </li>\n<li>再猜常见安装路径（Windows LOCALAPPDATA / Program Files；Unix brew/usr）。  </li>\n<li>都没有 <code>null</code>。</li>\n</ol>\n<h4 id=\"installhint\"><code>installHint</code></h4>\n<p>按平台返回安装说明字符串（含 winget / GitHub releases）。</p>\n<h4 id=\"stoptunnel\"><code>stopTunnel</code></h4>\n<p>child 未 killed → SIGTERM；1.5s 后再 SIGKILL。清空 child、quickUrl、<code>config.publicTunnelUrl</code>。</p>\n<h4 id=\"startquicktunnel-port-config.port-timeoutms-25000\"><code>startQuickTunnel({ port=config.port, timeoutMs=25000 })</code></h4>\n<ol>\n<li>先 <code>stopTunnel()</code>。  </li>\n<li>找不到二进制 → <strong>Promise.reject</strong>，<code>err.code='E_NO_CLOUDFLARED'</code>，message=installHint。  </li>\n<li>spawn <code>cloudflared tunnel --url http://127.0.0.1:${port} --no-autoupdate</code>。Windows 上若二进制是 <code>.cmd/.bat</code> 才 <code>shell:true</code>。  </li>\n<li>25s 内未解析到 URL：stopTunnel，reject 超时文案。  </li>\n<li>stdout <strong>和</strong> stderr 都进 <code>onData</code>：broadcast <code>tunnel_log</code>（chunk 截 400），<code>parseTunnelUrl(buf)</code> 成功则 settled、设 quickUrl 与 <code>config.publicTunnelUrl</code>、broadcast <code>tunnel_ready</code>、resolve <code>{ url, binary, target }</code>。  </li>\n<li><code>error</code>：未 settled 则 reject「无法启动」。  </li>\n<li><code>exit</code>：未 settled 则 reject 退出码 + installHint。</li>\n</ol>\n<h4 id=\"snapshot\"><code>snapshot</code></h4>\n<p><code>{ binary: findCloudflared(), url: quickUrl || config.publicTunnelUrl, running: Boolean(child &amp;&amp; !child.killed) }</code>。</p>\n<p>进程 <code>exit</code>/<code>SIGINT</code>/<code>SIGTERM</code> 会 stopTunnel（SIGINT/TERM 再 <code>process.exit(0)</code>）。</p>\n<hr />\n<h2 id=\"8.-工作台-ui-webagent-core/workbench/\">8. 工作台 UI <code>webagent-core/workbench/</code></h2>\n<p><strong>用途：</strong> 浏览器里的壳。<strong>不直接 fs。</strong> <strong>结果：</strong> CHAT 流、BRIDGE 卡片、复制 MCP、内置假浏览器页。 <strong>链条位置：</strong> 路径 A 前端；路径 B 的「复制地址 / 看工具卡片」。</p>\n<p>入口 <code>app.js</code>（ES module）import <code>js/state.js</code>、<code>js/dom.js</code>、<code>js/tabs.js</code>、<code>js/chat.js</code>、<code>js/bridge.js</code>、<code>js/settings.js</code>、<code>js/bind.js</code>。跨文件调用走 <code>ui</code> 袋，行为与拆开前同一套函数。行号以 <a href=\"#/files/workbench\">workbench/README.md</a> 为准。</p>\n<p><code>$</code> / <code>$$</code> 是 querySelector。 <code>SITES</code>：chatgpt / arena / deepseek / workbuddy / trae / qwen / manus / shunova 的外链。 <code>state</code> 初值：<code>mode:'code'</code>，一条 welcome tab，<code>loggedIn:true</code>，<code>selectedClient:'arena'</code>，<code>stayOnBridge:false</code>。</p>\n<p>下面按函数直译（HTML 模板不逐字抄，分支写全）。函数现分文件，名称与分支未改。</p>\n<h4 id=\"toast-/-escapehtml-/-rendermd\"><code>toast</code> / <code>escapeHtml</code> / <code>renderMd</code></h4>\n<p>toast 2.2s 隐藏。escape 五字符。renderMd：先 escape 再极简替换 code fence / inline / bold / 标题 / <code>- </code> 列表 / 换行变 <code>&lt;br&gt;</code>（不是完整 markdown）。</p>\n<h4 id=\"termline-/-openmodal-/-closemodal-/-showpage-/-setright\"><code>termLine</code> / <code>openModal</code> / <code>closeModal</code> / <code>showPage</code> / <code>setRight</code></h4>\n<p>终端追加一行。模态显示某设置页。右侧切 chat/bridge（CSS class <code>hidden</code> / <code>on</code>）。</p>\n<h4 id=\"painttabs-/-activatetab-/-closetab\"><code>paintTabs</code> / <code>activateTab</code> / <code>closeTab</code></h4>\n<p>画标签。activate：按 kind 显示 welcome / browser / agent / diff / file（Monaco 或 fallback textarea）。只剩 1 个 tab 时 close 直接 return。</p>\n<h4 id=\"openagentwindow-/-opendiff-/-paintdiff-/-ensurewelcome\"><code>openAgentWindow</code> / <code>openDiff</code> / <code>paintDiff</code> / <code>ensureWelcome</code></h4>\n<p>没有 agent tab 就加。diff：<code>+</code> 且非 <code>+++</code> 绿色，<code>-</code> 且非 <code>---</code> 红色，<code>@@</code> hunk。</p>\n<h4 id=\"openfile-filepath-async\"><code>openFile(filePath)</code>（async）</h4>\n<p>已有 tab 则激活。否则 <code>GET /api/files/content?path=</code>；<code>!res.ok</code> toast 后 return；否则新 tab。</p>\n<h4 id=\"langfor-/-applyeditor\"><code>langFor</code> / <code>applyEditor</code></h4>\n<p>按扩展名；有 monaco 则 createModel，否则写 textarea。</p>\n<h4 id=\"treehtml-/-loadtree\"><code>treeHtml</code> / <code>loadTree</code></h4>\n<p>递归 HTML。点击目录折叠（chev ▾/▸）；点击文件 <code>openFile</code>。recent-list 最多 6 个文件。</p>\n<h4 id=\"emptychat-/-paintchat-/-summarizetool-/-rendermsg-/-pushmsg\"><code>emptyChat</code> / <code>paintChat</code> / <code>summarizeTool</code> / <code>renderMsg</code> / <code>pushMsg</code></h4>\n<p>无消息显示空态四个设置链接。 renderMsg 分支：<code>user</code> / <code>status</code> / <code>tool</code>（点 header 切换 pre 显示）/ <code>consensus</code>（合并 + 各分支 tab；「采纳共识」会 <code>setAgentMode('code')</code> 并 <code>sendChat('按已对齐方案执行：…')</code>）/ 默认 assistant（renderMd）。</p>\n<h4 id=\"sendchat-text-opts-async\"><code>sendChat(text, opts={})</code>（async）</h4>\n<ol>\n<li><code>state.sending</code> 真 → return。  </li>\n<li>text 为 null/undefined 才从输入框取值并清空。  </li>\n<li>若智能体窗可见则用 <code>#agent-mode</code>，否则 <code>#mode-select</code>。  </li>\n<li><code>stayOnBridge = !!opts.stayOnBridge</code>；非 stay 则切到 chat。  </li>\n<li>history 取最后 12。有 message 才当 user；空则推「（空输入 · 分支作答）」。  </li>\n<li><code>POST /api/chat</code> JSON <code>{ mode: state.mode, message, history }</code>。  </li>\n<li><code>ReadableStream</code> 按 <code>\\n</code> 拆；<code>JSON.parse</code> 失败 <code>continue</code>；<code>handleEvent</code>；累积 assistantText。  </li>\n<li>catch → 推「请求失败」。  </li>\n<li>finally：<code>sending=false</code>，<code>refreshStatus()</code>，<code>loadTree()</code>。</li>\n</ol>\n<h4 id=\"handleevent-ev\"><code>handleEvent(ev)</code></h4>\n<ul>\n<li>status → 状态行。</li>\n<li>tool → 工具卡；stayOnBridge 则 <code>logBridgeTool</code>；<code>set_todos</code> 画任务；<code>run_command</code>/<code>execute_command</code> 把 stdout/stderr 进终端；<code>apply_patch</code> 成功则刷新已打开 tab，有 diff 则 <code>openDiff</code>。</li>\n<li>consensus / message / error。</li>\n</ul>\n<h4 id=\"logbridgetool-/-painttodos-/-agentlabel-/-setagentmode-/-paintstats\"><code>logBridgeTool</code> / <code>paintTodos</code> / <code>agentLabel</code> / <code>setAgentMode</code> / <code>paintStats</code></h4>\n<p>统计 calls/fail/totalMs（成功率 = <code>calls ? round((1-fail/calls)*100) : 100</code>）。<code>resetRound</code> POST <code>/api/bridge/reset-round</code> 后清本地统计和 bridge-log。todos 同时画 chat 与 bridge 两处。</p>\n<h4 id=\"selectedclientinfo-/-prompttext-/-paintclients\"><code>selectedClientInfo</code> / <code>promptText</code> / <code>paintClients</code></h4>\n<p>选中卡的 <code>c.prompt</code>；没有则 status.prompt 或自己拼 CONNECT_LINE。 配对码：<code>pair.code &amp;&amp; bridgeRunning</code> 才显示码，否则说明文案。</p>\n<h4 id=\"renderbrowser-tab\"><code>renderBrowser(tab)</code></h4>\n<ul>\n<li><code>arena</code>：内置仿 Arena 页，发送走 <code>arenaConnect</code>。</li>\n<li><code>chatgpt</code>：仿 ChatGPT，发送同样 <code>arenaConnect</code>。</li>\n<li><code>deepseek</code>：<strong>不</strong>调 MCP。展示商店链接、说明内置浏览器跑不了扩展、只显示 mcpUrl。</li>\n<li>其它：外链 + prompt 文本。</li>\n</ul>\n<h4 id=\"arenaconnect-text-async\"><code>arenaConnect(text)</code>（async）</h4>\n<ol>\n<li>切到 bridge，toast「已用提示词连接本机 MCP」。  </li>\n<li>try：对 <code>/mcp/${secret}</code> 连发 initialize、tools/list、resources/read instructions（clientInfo name <code>'Arena'</code>）。失败 toast。  </li>\n<li>从 text 去掉 <code>promptText()</code> 后的剩余当任务；空则默认「搜相关文件…」。  </li>\n<li><code>setAgentMode('code')</code>，<code>sendChat(task, { stayOnBridge:true })</code>。  </li>\n</ol>\n<p><strong>这是工作台内置演示，不是云上 Arena。</strong> 云上 Arena 自己对隧道发 MCP。</p>\n<h4 id=\"opensite-key-async\"><code>openSite(key)</code>（async）</h4>\n<p>未知 key return。bridge 未运行则 <code>await startBridge()</code>。try 复制 promptText。打开 browser tab，切右侧 bridge。</p>\n<h4 id=\"startbridge-/-stopbridge-/-paintbridge-/-refreshstatus\"><code>startBridge</code> / <code>stopBridge</code> / <code>paintBridge</code> / <code>refreshStatus</code></h4>\n<p>start：读 radio <code>tunnel</code>，默认 cloudflare；<code>POST /api/bridge/start</code>；<code>!data.success</code> toast 返回 false；有 note toast 前 180 字；refresh；复制 mcpUrl。 stop：POST stop，去 sess-dot。 paintBridge：按 <code>s.tunnel.url</code> 显示 Quick Tunnel 或「未找到 cloudflared 时，MCP 走当前页面源」。登录文案在 <code>state.loggedIn</code> 分支写死 demo。 refreshStatus：GET /status，重画模型下拉、provider 表、todos。</p>\n<h4 id=\"paintcustom-/-paintprovidertable-/-loadcustomizations-/-savecustom-/-loadskills\"><code>paintCustom</code> / <code>paintProviderTable</code> / <code>loadCustomizations</code> / <code>saveCustom</code> / <code>loadSkills</code></h4>\n<p>设置页表单。provider 表过滤掉 protocol===builtin。点 radio → POST <code>/api/models</code> <code>{ activeModelId }</code>。</p>\n<h4 id=\"bind\"><code>bind</code></h4>\n<p>先 <code>onClick(id, handler)</code>：节点不存在则跳过。绑定活动栏、菜单、发送、Enter（Shift+Enter 不发送）、Bridge 启停、复制 URL/提示词（extension-http 时 toast 不同文案）、重置密钥、GitHub 登录演示、各种 saveCustom、探测 env/stack（HTML 有 <code>#page-env</code> / <code>#page-stack</code>）、Add API（probe 失败且填了手动 id 则造一条 custom 模型；排除 modelId 匹配 video|image 的当默认）、终端 <code>POST /api/tool/call</code> run_command mode code、搜索 <code>search_files</code> mode ask、Ctrl/Cmd+S 保存。</p>\n<p><code>skillMarkdown</code> / <code>SKILL_TPL</code>（fix-tests / review / release）只在 bind 闭包内。<code>fillSkillPreview</code>：body 已 dirty 则不覆盖。</p>\n<h4 id=\"saveactive-/-connectws-/-loadmonaco-/-boot\"><code>saveActive</code> / <code>connectWs</code> / <code>loadMonaco</code> / <code>boot</code></h4>\n<p>仅 file tab 才保存 PUT <code>/api/files/content</code>。 WS：<code>wss</code> 或 <code>ws</code> + <code>location.host</code> + <code>/ws</code>。消息：command_output 进终端；file_patched 刷新树；todos_updated；tool_call_end → logBridgeTool。parse 失败 return。 Monaco 从 jsDelivr 拉 0.52.2；7s 超时或 onerror → resolve false/是否已有 editor。 boot：<code>try ui.bind()</code>（失败 <code>console.error</code>，后面仍跑）、默认 code、并行 refresh/tree/skills/custom/monaco，再 WS，激活 welcome。<code>boot().catch(console.error)</code>。</p>\n<hr />\n<h2 id=\"9.-网页-vs-code-插件-webagent-core/extension/extension.js-492-行\">9. 网页 VS Code 插件 <code>webagent-core/extension/extension.js</code>（492 行）</h2>\n<p><strong>用途：</strong> code-server 侧栏 + 原生 Chat <code>@webagent</code>。 <strong>结果：</strong> 同样打 <code>agentHostUrl()/api/chat</code>。 <strong>链条位置：</strong> 路径 C 的 UI。插件<strong>不实现</strong> patch 引擎。</p>\n<h4 id=\"agenthosturl\"><code>agentHostUrl</code></h4>\n<p>配置 <code>webagent.agentHostUrl</code> 或 env <code>WEBAGENT_AGENT_HOST_URL</code> 或 <code>'http://127.0.0.1:48271'</code>，去尾 <code>/</code>。</p>\n<h4 id=\"requestjson-method-url-body\"><code>requestJson(method, url, body)</code></h4>\n<p>http/https 按协议。body 有则 JSON。结束时 try parse；失败 <code>{ json:null, raw }</code>。<code>req.on('error', reject)</code>。</p>\n<h4 id=\"postndjson-url-body-onevent\"><code>postNdjson(url, body, onEvent)</code></h4>\n<p><strong>只用 <code>http.request</code></strong>（https URL 不会走 https 模块）。按行 JSON.parse，失败忽略。结束时残余 buf 再试一次。</p>\n<h4 id=\"modefromchatrequest-request\"><code>modeFromChatRequest(request)</code></h4>\n<p><code>request.command</code> 小写是 ask/plan/code 则用。否则 prompt 以 <code>/ask</code> <code>/plan</code> <code>/code</code> 开头。<strong>都不匹配 → <code>'code'</code></strong>。</p>\n<h4 id=\"historyfromchatcontext-/-revealworkspacefile\"><code>historyFromChatContext</code> / <code>revealWorkspaceFile</code></h4>\n<p>history 最多 12 轮。reveal：没有 workspaceFolder 或 rel 则 return；打开失败 catch 空。</p>\n<h4 id=\"registerchatparticipant\"><code>registerChatParticipant</code></h4>\n<p>没有 <code>vscode.chat.createChatParticipant</code> → return。 handler：去掉 slash 后 message 空 → 输出模式说明 markdown，return。 否则 progress 文案，<code>postNdjson /api/chat</code>。事件：status→progress；tool→markdown 一行，apply_patch 成功则 reveal + <code>stream.reference</code>；message；error；consensus。 catch：提示连不上 48271。 外层 try/catch：注册失败 <code>console.warn</code>，不抛给 activate。</p>\n<h4 id=\"activate-context\"><code>activate(context)</code></h4>\n<p>注册 ChatView、BridgeView。<code>registerChatParticipant</code>。状态栏每 5s GET <code>/api/status</code>：bridgeRunning 则「Bridge 运行中」，失败「未连接 48271」。 命令：<code>openBridge</code> 打开侧栏；<code>openAgentChat</code> 试 <code>workbench.action.chat.open</code> query <code>@webagent </code>，失败则侧栏；<code>resetSecret</code> POST reset-secret。</p>\n<h4 id=\"chatview\"><code>ChatView</code></h4>\n<p>webview 脚本开。消息 <code>openNative</code> → 命令。<code>send</code>：本地 history 12，<code>postNdjson</code>，事件转给 webview；apply_patch reveal；assistantText 非空才推进 history。catch 发 error 事件。</p>\n<p>webview 内脚本：默认 <code>mode='code'</code>；Agent 菜单切 ask/plan/code；Enter 发送；set_todos 画任务。</p>\n<h4 id=\"bridgeview\"><code>BridgeView</code></h4>\n<p>start → POST <code>/api/bridge/start</code> <code>{ tunnelProvider:'cloudflare' }</code>。stop / copy / reset。<code>refresh</code> GET status。catch 弹 ErrorMessage。 webview：4s 一次 refresh；copy 用 <code>status.prompt</code> 或 mcpUrl+CONNECT。</p>\n<p><code>deactivate</code> 空函数。导出 <code>{ activate, deactivate, modeFromChatRequest }</code>。</p>\n<hr />\n<h2 id=\"10.-三条路径的函数调用栈-逐步-对照源码\">10. 三条路径的函数调用栈（逐步，对照源码）</h2>\n<h3 id=\"10.1-本机-chat-无-api-key-code-模式\">10.1 本机 Chat，无 API Key，Code 模式</h3>\n<ol>\n<li>用户点发送 → <code>app.js sendChat</code>。  </li>\n<li><code>fetch POST</code> <strong>3000</strong> <code>/api/chat</code> <code>{ mode:'code', message, history }</code>。  </li>\n<li><code>routes.js</code> 打开 NDJSON，<code>emit = res.write</code>，调用 <code>runChat({ mode, message, history, emit })</code>。  </li>\n<li><code>runChat</code> 取出 <code>send</code>；active 模型缺 apiKey/baseUrl/modelId → <code>runBuiltin(payload, send)</code>。  </li>\n<li><code>timedTool(set_todos)</code> → <code>callTool(..., 'code')</code>。  </li>\n<li><code>explore</code>：list_directory → git_status → find_files → 或有 search_files → read_files。均 <code>callTool</code>，失败不抛。  </li>\n<li>若消息含围栏+「写入文件」→ <code>write_file</code>。  </li>\n<li>若含 SEARCH/REPLACE → 读 hash → <code>apply_patch</code>（resolveSafePath → 写 tmp → rename）。  </li>\n<li><code>detectTestCommand</code> 有则 <code>run_command</code>（Windows powershell.exe）。  </li>\n<li>emit message 摘要。<code>handleEvent</code> 画卡；有 diff 开 diff tab。</li>\n</ol>\n<h3 id=\"10.2-deepseek-改磁盘-源码能证明的部分\">10.2 DeepSeek++ 改磁盘（源码能证明的部分）</h3>\n<p>本仓库<strong>没有</strong> DeepSeek++ 源码。能直译的是我们这一侧：</p>\n<ol>\n<li>工作台 <code>clients.js</code> hydrate：<code>connectMode==='extension-http'</code> → <code>prompt = mcpUrl</code> 一行。  </li>\n<li>用户把这一行填进扩展（文档步骤，不是本仓库代码）。  </li>\n<li>扩展应对 <code>POST /mcp/&lt;secret&gt;</code>（或隧道 URL 同路径）。  </li>\n<li><code>server.js</code> <code>extractToken</code> 从 <code>params.secret</code> 取出 → <code>verifyAccessToken</code> 与 <code>config.secretKey</code> 相等 → 过。  </li>\n<li><code>handleRpc</code> <code>tools/call</code> → <code>callTool(name, args, remoteToolMode)</code>：默认 <code>'code'</code>；<code>params._meta.mode</code> 为 ask|plan|code 时用之。  </li>\n<li><code>clipJson</code> 后 MCP text；broadcast <code>tool_call_end</code>。  </li>\n<li>工作台 <code>connectWs</code> 收到后 <code>logBridgeTool</code>。</li>\n</ol>\n<p>隧道：<code>POST /bridge/start</code> 在 cloudflare 下 <code>await startQuickTunnel</code>，成功则 <code>publicTunnelUrl</code> 为 trycloudflare.com。无二进制或超时 catch 后 <code>mcpOrigin</code> 退回当前页面 Host（本机预览可用）。Named / ngrok 不 spawn。</p>\n<h3 id=\"10.3-chat-plus-改磁盘-源码能证明的部分\">10.3 Chat Plus 改磁盘（源码能证明的部分）</h3>\n<p>本仓库<strong>没有</strong> Chat Plus 源码（GPL，不 vendor）。能直译的是我们这一侧，与 10.2 同一条 MCP：</p>\n<ol>\n<li><code>clients.js</code> 里 <code>id:'chat-plus'</code> 同样 <code>connectMode==='extension-http'</code> → <code>prompt = mcpUrl</code> 一行。  </li>\n<li>用户按 <a href=\"./网页ChatPlus使用指南.md\">网页ChatPlus使用指南.md</a> 从 GitHub 编译扩展，把这一行填进侧边栏（传输 Streamable HTTP）。<strong>不要</strong>再跑 <code>aiguicai/MCP-Gateway</code>。  </li>\n<li>之后 <code>POST /mcp/&lt;secret&gt;</code> 与 10.2 第 3–7 步相同。</li>\n</ol>\n<h3 id=\"10.4-chatgpt-plus-连接器\">10.4 ChatGPT Plus 连接器</h3>\n<ol>\n<li>GET <code>/.well-known/oauth-authorization-server</code>（匿名）。  </li>\n<li>POST <code>/oauth/register</code> → client_id/secret。  </li>\n<li>浏览器 GET <code>/oauth/authorize</code> → <code>ensurePairing</code> 出码（码在工作台 <code>/api/status.pairing</code>）。  </li>\n<li>POST 表单 → <code>completeAuthorize</code> → <code>consumePairing</code> → 302 带 code。  </li>\n<li>POST <code>/oauth/token</code> + PKCE → Bearer。  </li>\n<li>之后 <code>POST /mcp</code>（无密钥路径）<code>Authorization: Bearer</code>。  </li>\n<li><code>handleRpc</code> 与 10.2 相同。</li>\n</ol>\n<hr />\n<h2 id=\"11.-源码现状里不要写成-已经接上-的点\">11. 源码现状里不要写成「已经接上」的点</h2>\n<p>技术文档必须写实际代码。</p>\n<ol>\n<li><strong>Named / ngrok 不 spawn。</strong> <code>/bridge/start</code> 只在 <code>provider==='cloudflare'</code> 时 <code>await startQuickTunnel</code>。另外两个下拉不会拉起对应隧道。无 cloudflared 时 start 仍 200，MCP 走当前 Host。</li>\n</ol>\n<ol>\n<li><strong>读哈希文件不要提交。</strong> <code>.webagent/read-hashes.json</code> 是运行时缓存（最多 400 条）；「清除本轮统计」会删。文件在进程外被改仍会 <code>STALE_FILE</code>。</li>\n</ol>\n<ol>\n<li><strong>远程 <code>tools/call</code> 默认 Code。</strong> <code>remoteToolMode</code> 读 <code>params._meta.mode</code>（ask|plan|code），缺省 <code>'code'</code>。本机 Chat 仍由 <code>/api/chat</code> 传入 UI 模式。</li>\n</ol>\n<ol>\n<li><strong><code>consensusEngine</code> 不请求任何模型 HTTP。</strong> 三份答案是源码字符串。<code>runBuiltin</code> 的 plan 在 <code>multiModel.enabled === false</code> 时连这个函数都不进。</li>\n</ol>\n<ol>\n<li><strong><code>sendCommandInput</code> 恒定 ok:false。</strong> 没有交互式 PTY。</li>\n</ol>\n<ol>\n<li><strong>工作台 <code>arenaConnect</code> 打的是本机 <code>/mcp/${secret}</code></strong>，再转去本机 <code>/api/chat</code>。不是 Cloudflare 上的 Arena 产品。</li>\n</ol>\n<ol>\n<li><strong><code>read_files</code> 的敏感拦截在 <code>resolveSafePath</code>→<code>assertNotSensitive</code>；列表/grep 靠 <code>isHidden</code>。</strong> 两条都在，删一条会在「读」或「列」上漏。</li>\n</ol>\n<ol>\n<li><strong>隧道上的 <code>/api</code> 不是「有密钥就过」。</strong> mcp 口见 Cloudflare 头或 <code>*.trycloudflare.com</code> Host 则 404。本机 Chat 走 3000；VS Code 插件走无 CF 头的 <code>127.0.0.1:48271</code>。WebSocket 只挂 3000，非本机连接直接关掉，连接消息不下发 secretKey。默认 <code>WEBAGENT_BIND=127.0.0.1</code>。符号链接必须 <code>realpath</code> 仍在工作区。<code>git push</code> / <code>curl|sh</code> 等要 <code>confirm_dangerous</code>。mcp 口不再 <code>cors()</code> 全开：只给扩展和已知聊天站回 CORS 头。带外站 Origin 的 <code>/api</code> 404。CORS <strong>不是</strong>认证。</li>\n</ol>\n<hr />\n<h2 id=\"12.-改代码时建议从哪下手\">12. 改代码时建议从哪下手</h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你想改的行为</th>\n<th>先打开</th>\n</tr></thead><tbody>\n<tr>\n<td>新 MCP 工具</td>\n<td><code>tools/index.js</code> 注册 + 新文件；<code>mcpProtocol.test.js</code> 的 25 改数量</td>\n</tr>\n<tr>\n<td>补丁匹配 / 哈希</td>\n<td><code>patchEngine.js</code> + <code>tests/patchEngine.test.js</code></td>\n</tr>\n<tr>\n<td>网页第一句提示词</td>\n<td><code>instructions.js</code> 的 <code>CONNECT_LINE</code>（有测试锁原文）</td>\n</tr>\n<tr>\n<td>DeepSeek / Chat Plus / Arena 卡片步骤</td>\n<td><code>mcp/clients.js</code></td>\n</tr>\n<tr>\n<td>本机无 Key 时的探索策略</td>\n<td><code>agent/runChat.js</code> <code>explore</code> / <code>runBuiltin</code></td>\n</tr>\n<tr>\n<td>有 Key 时模型怎么被管</td>\n<td><code>agent/openai.js</code> <code>systemPrompt</code></td>\n</tr>\n<tr>\n<td>工作台按钮</td>\n<td><code>workbench/js/bind.js</code> + <code>index.html</code></td>\n</tr>\n<tr>\n<td>隧道上未鉴权 <code>/api</code></td>\n<td><code>src/utils/localControl.js</code> + <code>src/index.js</code></td>\n</tr>\n<tr>\n<td>MCP CORS / 外站 Origin 打 <code>/api</code></td>\n<td><code>src/utils/corsAllow.js</code></td>\n</tr>\n<tr>\n<td>VS Code Chat 默认 Agent</td>\n<td><code>extension/extension.js</code> <code>modeFromChatRequest</code></td>\n</tr>\n<tr>\n<td>网页 VS Code 登录口令</td>\n<td><code>scripts/codeServerAuth.js</code> + <code>run-code-oss.js</code></td>\n</tr>\n<tr>\n<td>外部工作区密钥进 Git</td>\n<td><code>models/store.js</code> <code>protectWorkspaceSecrets</code></td>\n</tr>\n<tr>\n<td>工作台保存 / 新建 Skill</td>\n<td><code>api/routes.js</code> PUT <code>/files/content</code>、POST <code>/skills</code>（均 <code>write_file</code>）</td>\n</tr>\n<tr>\n<td>Named / ngrok 隧道</td>\n<td><code>api/routes.js</code> <code>/bridge/start</code>（目前只接 cloudflare Quick Tunnel）</td>\n</tr>\n<tr>\n<td>密钥持久化</td>\n<td><code>config.js</code> + <code>store.js</code> + reset-secret 路由</td>\n</tr>\n</tbody></table></div>\n<p>测试入口：<code>webagent-core/agent-host/tests/</code>，根目录 <code>run-tests.cmd</code> 只是调用它。说明见 <a href=\"./测试说明.md\">测试说明.md</a>。 调用它。说明见 <a href=\"./测试说明.md\">测试说明.md</a>。</p>"},"overview":{"title":"总览（知识图谱）","toc":[{"level":2,"text":"快速导航","id":"快速导航"},{"level":2,"text":"1. 子模块索引","id":"1.-子模块索引"},{"level":2,"text":"2. 数据流向图（用户请求 → 响应）","id":"2.-数据流向图-用户请求-响应"},{"level":3,"text":"2.1 总图","id":"2.1-总图"},{"level":3,"text":"2.2 路径 A：本机 Chat（不需要隧道）","id":"2.2-路径-a-本机-chat-不需要隧道"},{"level":3,"text":"2.3 路径 B：网页 Agent → MCP → 本机仓库","id":"2.3-路径-b-网页-agent-mcp-本机仓库"},{"level":3,"text":"2.4 路径 C：网页 VS Code","id":"2.4-路径-c-网页-vs-code"},{"level":3,"text":"2.5 一次 apply_patch 在文件夹之间怎么走","id":"2.5-一次-apply_patch-在文件夹之间怎么走"},{"level":2,"text":"3. 环境与配置总览","id":"3.-环境与配置总览"},{"level":3,"text":"3.1 仓库里实际有的清单","id":"3.1-仓库里实际有的清单"},{"level":3,"text":"3.2 Install → Build → Run","id":"3.2-install-build-run"},{"level":3,"text":"3.3 不要当成启动方式的东西","id":"3.3-不要当成启动方式的东西"},{"level":2,"text":"4. 和其它根文档的分工","id":"4.-和其它根文档的分工"}],"html":"<h1 id=\"总览-知识图谱\">总览（知识图谱）</h1>\n<p>第一、二阶段在各子文件夹生成的 <code>README.md</code> 汇总。本文件只做索引与全局链路，<strong>不替代</strong>根目录产品首页 <a href=\"./README.md\">README.md</a>（Windows 怎么跑请仍看 <a href=\"./使用指南.md\">使用指南.md</a>；为什么这样装的人话见 <a href=\"#/guide\">架构导读.md</a>）。</p>\n<p>仓库根 <strong>没有</strong> <code>requirements.txt</code>、<code>docker-compose.yml</code>、根 <code>package.json</code>。依赖与启动见第 4 节。</p>\n<hr />\n<h2 id=\"快速导航\">快速导航</h2>\n<p>入口与核心函数 → 对应说明书锚点（GitHub 对 <code>### 📄 文件名：\\</code>xxx.js\\<code></code> 生成的 slug 多为 <code>文件名xxxjs</code>）。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你要找的</th>\n<th>文件</th>\n<th>说明书</th>\n</tr></thead><tbody>\n<tr>\n<td>Windows 主入口</td>\n<td><code>run-webagent.cmd</code></td>\n<td><a href=\"#/files/launchers/文件名run-webagentcmd\">启动脚本说明 · run-webagent.cmd</a></td>\n</tr>\n<tr>\n<td>进程入口 <code>persistIdentity</code> / <code>listenOrExit</code></td>\n<td><code>src/index.js</code>、<code>config.js</code></td>\n<td><a href=\"#/files/src/文件名indexjs\">src · index.js</a> · <a href=\"#/files/src/文件名configjs\">config.js</a></td>\n</tr>\n<tr>\n<td>MCP <code>handleRpc</code> / <code>requireAuth</code></td>\n<td><code>src/mcp/server.js</code></td>\n<td><a href=\"#/files/mcp/文件名serverjs\">mcp · server.js</a></td>\n</tr>\n<tr>\n<td>OAuth / PKCE <code>verifyAccessToken</code></td>\n<td><code>src/mcp/oauth.js</code></td>\n<td><a href=\"#/files/mcp/文件名oauthjs\">mcp · oauth.js</a></td>\n</tr>\n<tr>\n<td>连接提示词 <code>getBootstrapPrompt</code></td>\n<td><code>src/mcp/instructions.js</code></td>\n<td><a href=\"#/files/mcp/文件名instructionsjs\">mcp · instructions.js</a></td>\n</tr>\n<tr>\n<td>客户端配方 <code>listClients</code></td>\n<td><code>src/mcp/clients.js</code></td>\n<td><a href=\"#/files/mcp/文件名clientsjs\">mcp · clients.js</a></td>\n</tr>\n<tr>\n<td>工具总闸 <code>callTool</code> / <code>getToolList</code></td>\n<td><code>src/tools/index.js</code></td>\n<td><a href=\"#/files/tools/文件名indexjs\">tools · index.js</a></td>\n</tr>\n<tr>\n<td>别名 <code>resolveToolName</code> / <code>normalizeToolArgs</code></td>\n<td><code>src/tools/normalize.js</code></td>\n<td><a href=\"#/files/tools/文件名normalizejs\">tools · normalize.js</a></td>\n</tr>\n<tr>\n<td>读哈希缓存 <code>rememberHash</code></td>\n<td><code>src/tools/readCache.js</code></td>\n<td><a href=\"#/files/tools/文件名readcachejs\">tools · readCache.js</a></td>\n</tr>\n<tr>\n<td>打补丁 <code>applyPatch</code> / <code>resolveSafePath</code></td>\n<td><code>src/tools/patchEngine.js</code></td>\n<td><a href=\"#/files/tools/文件名patchenginejs\">tools · patchEngine.js</a></td>\n</tr>\n<tr>\n<td>跑命令 <code>executeCommand</code>（Windows=PowerShell）</td>\n<td><code>src/tools/executor.js</code></td>\n<td><a href=\"#/files/tools/文件名executorjs\">tools · executor.js</a></td>\n</tr>\n<tr>\n<td>本机 Chat <code>runChat</code> / <code>runBuiltin</code></td>\n<td><code>src/agent/runChat.js</code></td>\n<td><a href=\"#/files/agent/文件名runchatjs\">agent · runChat.js</a></td>\n</tr>\n<tr>\n<td>有 Key 时工具循环 <code>runOpenAI</code></td>\n<td><code>src/agent/openai.js</code></td>\n<td><a href=\"#/files/agent/文件名openaijs\">agent · openai.js</a></td>\n</tr>\n<tr>\n<td>工作台 REST <code>POST /chat</code>、<code>/bridge/*</code>、<code>/bridge/reset-round</code></td>\n<td><code>src/api/routes.js</code></td>\n<td><a href=\"#/files/api/文件名routesjs\">api · routes.js</a></td>\n</tr>\n<tr>\n<td>隧道日志解析 <code>parseTunnelUrl</code></td>\n<td><code>src/tunnel/cloudflared.js</code></td>\n<td><a href=\"#/files/tunnel/文件名cloudflaredjs\">tunnel · cloudflared.js</a></td>\n</tr>\n<tr>\n<td>事件推送 <code>broadcast</code></td>\n<td><code>src/utils/eventBus.js</code></td>\n<td><a href=\"#/files/utils/文件名eventbusjs\">utils · eventBus.js</a></td>\n</tr>\n<tr>\n<td>本机控制面闸 <code>rejectUnlessLocalControl</code></td>\n<td><code>src/utils/localControl.js</code></td>\n<td><a href=\"#/files/utils/文件名localcontroljs\">utils · localControl.js</a></td>\n</tr>\n<tr>\n<td>MCP CORS 白名单 <code>mcpCors</code> / <code>rejectCrossSiteApi</code></td>\n<td><code>src/utils/corsAllow.js</code></td>\n<td><a href=\"#/files/utils/文件名corsallowjs\">utils · corsAllow.js</a></td>\n</tr>\n<tr>\n<td>工作台 UI</td>\n<td><code>workbench/app.js</code> + <code>js/*.js</code></td>\n<td><a href=\"#/files/workbench/文件名appjs\">workbench · app.js</a></td>\n</tr>\n<tr>\n<td>VS Code <code>activate</code> / <code>modeFromChatRequest</code></td>\n<td><code>extension/extension.js</code></td>\n<td><a href=\"#/files/extension/文件名extensionjs\">extension · extension.js</a></td>\n</tr>\n<tr>\n<td>网页 VS Code <code>main</code> / <code>ensure</code></td>\n<td><code>scripts/run-code-oss.js</code></td>\n<td><a href=\"#/files/scripts/文件名run-code-ossjs\">scripts · run-code-oss.js</a></td>\n</tr>\n<tr>\n<td>网页 VS Code 登录 <code>resolveAuth</code></td>\n<td><code>scripts/codeServerAuth.js</code></td>\n<td><a href=\"#/files/scripts/文件名codeserverauthjs\">scripts · codeServerAuth.js</a></td>\n</tr>\n<tr>\n<td>默认演示 <code>divide</code></td>\n<td><code>workspace/src/calculator.js</code></td>\n<td><a href=\"#/files/workspace/文件名srccalculatorjs\">workspace · calculator.js</a></td>\n</tr>\n<tr>\n<td>文档必须随功能改</td>\n<td><code>workspace/.webagent/skills/docs-sync/SKILL.md</code></td>\n<td><a href=\"#/files/workspace/文件名webagentskillsdocs-syncskillmd\">workspace · docs-sync</a></td>\n</tr>\n<tr>\n<td>人话架构（四层写法）</td>\n<td><code>架构导读.md</code></td>\n<td><a href=\"#/guide\">架构导读.md</a></td>\n</tr>\n</tbody></table></div>\n<p>类（源码里明确 <code>class</code> 的很少）：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>类</th>\n<th>说明书</th>\n</tr></thead><tbody>\n<tr>\n<td><code>ProtocolError</code> / <code>ExecutionError</code></td>\n<td><a href=\"#/files/mcp/文件名errorsjs\">mcp · errors.js</a></td>\n</tr>\n<tr>\n<td><code>ChatView</code> / <code>BridgeView</code></td>\n<td><a href=\"#/files/extension/文件名extensionjs\">extension · extension.js</a></td>\n</tr>\n<tr>\n<td>原型 <code>TunnelManager</code> / <code>BridgeEventBus</code>（冻结）</td>\n<td><a href=\"#/files/repro/文件名srctunneltunnelmanagerjs\">webagent-repro</a></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"1.-子模块索引\">1. 子模块索引</h2>\n<p>「详细文档」一律指向该文件夹内部 README（相对本文件）。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>子模块</th>\n<th>路径</th>\n<th>定位</th>\n<th>详细文档</th>\n</tr></thead><tbody>\n<tr>\n<td>产品代码根</td>\n<td><code>webagent-core/</code></td>\n<td>现行程序；本层只有 <code>start-webagent.sh</code></td>\n<td><a href=\"#/files/webagent-core\">查看详情</a></td>\n</tr>\n<tr>\n<td>agent-host 包</td>\n<td><code>webagent-core/agent-host/</code></td>\n<td>npm 包根：<code>package.json</code> 的 start/test</td>\n<td><a href=\"#/files/agent-host\">查看详情</a></td>\n</tr>\n<tr>\n<td>进程入口</td>\n<td><code>…/src/</code></td>\n<td><code>config.js</code> + <code>index.js</code> 两套 Express、双端口</td>\n<td><a href=\"#/files/src\">查看详情</a></td>\n</tr>\n<tr>\n<td>MCP</td>\n<td><code>…/src/mcp/</code></td>\n<td>Streamable HTTP、OAuth、instructions、clients</td>\n<td><a href=\"#/files/mcp\">查看详情</a></td>\n</tr>\n<tr>\n<td>工具</td>\n<td><code>…/src/tools/</code></td>\n<td>约 25 正式名；<code>normalize.js</code> 收 <code>bash</code>/<code>cat</code>/<code>ls</code> 等别名；<code>readCache.js</code> 记 sha256</td>\n<td><a href=\"#/files/tools\">查看详情</a></td>\n</tr>\n<tr>\n<td>本机 Chat</td>\n<td><code>…/src/agent/</code></td>\n<td><code>runChat</code>；网页 Agent <strong>不进</strong>这里</td>\n<td><a href=\"#/files/agent\">查看详情</a></td>\n</tr>\n<tr>\n<td>模型/工作区配置</td>\n<td><code>…/src/models/</code></td>\n<td><code>.webagent</code> store / profile / memory</td>\n<td><a href=\"#/files/models\">查看详情</a></td>\n</tr>\n<tr>\n<td>REST</td>\n<td><code>…/src/api/</code></td>\n<td><code>/api/chat</code>、<code>/api/bridge/*</code>、文件树</td>\n<td><a href=\"#/files/api\">查看详情</a></td>\n</tr>\n<tr>\n<td>隧道</td>\n<td><code>…/src/tunnel/</code></td>\n<td>解析 cloudflared 日志</td>\n<td><a href=\"#/files/tunnel\">查看详情</a></td>\n</tr>\n<tr>\n<td>工具函数</td>\n<td><code>…/src/utils/</code></td>\n<td>eventBus、diff、localControl、corsAllow</td>\n<td><a href=\"#/files/utils\">查看详情</a></td>\n</tr>\n<tr>\n<td>产品测试</td>\n<td><code>…/tests/</code></td>\n<td>Node assert，无 Jest</td>\n<td><a href=\"#/files/tests\">查看详情</a></td>\n</tr>\n<tr>\n<td>工作台 UI</td>\n<td><code>webagent-core/workbench/</code></td>\n<td>浏览器 3000</td>\n<td><a href=\"#/files/workbench\">查看详情</a></td>\n</tr>\n<tr>\n<td>VS Code 插件</td>\n<td><code>webagent-core/extension/</code></td>\n<td><code>@webagent</code> 默认 Agent=code</td>\n<td><a href=\"#/files/extension\">查看详情</a></td>\n</tr>\n<tr>\n<td>插件安装副本</td>\n<td><code>webagent-core/extensions-installed/</code></td>\n<td>不要在这里改 JS</td>\n<td><a href=\"#/files/ext-installed\">查看详情</a></td>\n</tr>\n<tr>\n<td>网页 VS Code 脚本</td>\n<td><code>webagent-core/scripts/</code></td>\n<td>npm 拉 code-server 4.135.0</td>\n<td><a href=\"#/files/scripts\">查看详情</a></td>\n</tr>\n<tr>\n<td>默认工作区</td>\n<td><code>workspace/</code></td>\n<td>演示计算器 + <code>.webagent</code>（含 <code>docs-sync</code> Skill）</td>\n<td><a href=\"#/files/workspace\">查看详情</a></td>\n</tr>\n<tr>\n<td>运行时目录</td>\n<td><code>bin/</code></td>\n<td><code>code-server-runtime</code> 不进 Git</td>\n<td><a href=\"#/files/bin\">查看详情</a></td>\n</tr>\n<tr>\n<td>冻结原型</td>\n<td><code>webagent-repro/</code></td>\n<td><strong>不要运行</strong>；单端口 3000、8 工具</td>\n<td><a href=\"#/files/repro\">查看详情</a></td>\n</tr>\n<tr>\n<td>根启动脚本</td>\n<td>仓库根 <code>.cmd</code> / <code>.sh</code> / <code>.gitignore</code> / <code>.gitattributes</code></td>\n<td>不是子文件夹 README</td>\n<td><a href=\"#/files/launchers\">启动脚本说明.md</a></td>\n</tr>\n<tr>\n<td>code-server 配置</td>\n<td><code>.config/code-server/</code></td>\n<td>网页 VS Code 的 <code>--config</code> yaml</td>\n<td><a href=\"#/files/code-server-cfg\">查看详情</a></td>\n</tr>\n<tr>\n<td>可视化导览</td>\n<td><code>docs-site/</code></td>\n<td>导读 / 直译 / 行级 README 的 HTML；不改磁盘逻辑</td>\n<td><a href=\"./docs-site/README.md\">查看详情</a></td>\n</tr>\n</tbody></table></div>\n<p>依赖方向（产品路径，不含 repro）：</p>\n<pre class=\"lang-text\"><code>run-webagent.cmd\n    → agent-host/src/index.js\n        → workbench/（静态）\n        → mcp/oauth.js + mcp/server.js\n        → api/routes.js\n        → tunnel/（POST /bridge/start 且 provider=cloudflare 时 spawn）\n        → tools/* ← agent/runChat.js\n                 ← mcp/server.js tools/call\n        → models/*、utils/eventBus</code></pre>\n<hr />\n<h2 id=\"2.-数据流向图-用户请求-响应\">2. 数据流向图（用户请求 → 响应）</h2>\n<p>三条路径 <strong>改盘引擎都是</strong> <code>tools/index.js</code> 的 <code>callTool</code>。差别只是谁下指令。</p>\n<h3 id=\"2.1-总图\">2.1 总图</h3>\n<pre class=\"mermaid-src\"><code>flowchart TB\n  subgraph user [用户]\n    CMD[run-webagent.cmd]\n    Browser[浏览器 127.0.0.1:3000]\n    WebAI[云上 Arena / DeepSeek++ / Chat Plus / ChatGPT Plus]\n    VscCmd[run-webagent-vscode.cmd]\n  end\n\n  subgraph host [本机 agent-host]\n    Index[src/index.js uiApp+mcpApp]\n    UI[workbench :3000]\n    MCP[mcp/server.js :48271]\n    Gate[utils/localControl.js]\n    API[api/routes.js /api]\n    Chat[agent/runChat.js]\n    Tools[tools/callTool]\n    Disk[workspace 磁盘]\n    WS[utils/eventBus /ws]\n  end\n\n  CMD --&gt; Index\n  Index --&gt; UI\n  Index --&gt; MCP\n  Index --&gt; Gate\n  Gate --&gt;|本机回环才放行 48271 /api| API\n  Browser --&gt; UI\n  UI --&gt;|POST :3000 /api/chat| API\n  API --&gt; Chat\n  Chat --&gt; Tools\n  WebAI --&gt;|HTTPS JSON-RPC /mcp/密钥 或 Bearer| MCP\n  MCP --&gt; Tools\n  Tools --&gt; Disk\n  Tools --&gt; WS\n  WS --&gt; UI\n  VscCmd --&gt; Scripts[scripts/run-code-oss.js]\n  Scripts --&gt; Index\n  Scripts --&gt; CS[code-server :3000]\n  CS --&gt; Ext[extension.js]\n  Ext --&gt;|同一 /api/chat| API</code></pre>\n<h3 id=\"2.2-路径-a-本机-chat-不需要隧道\">2.2 路径 A：本机 Chat（不需要隧道）</h3>\n<pre class=\"lang-text\"><code>用户在工作台 CHAT 回车\n  → workbench/app.js fetch POST :3000 /api/chat\n  → api/routes.js L178–L200 NDJSON\n  → agent/runChat.js（第二参或 payload.emit）\n       有 apiKey+baseUrl+modelId → openai.runOpenAI（最多 10 轮 tool_calls）\n       否则 → runBuiltin：explore 只读 → Ask 摘要 / Plan consensusEngine / Code 补丁或围栏再 npm test\n  → tools/callTool(name, args, mode)   ← 本机传 ask|plan|code，模式锁生效\n  → patchEngine / fileOps / executor（Windows PowerShell）\n  → 工作区文件变化\n  → eventBus.broadcast → /ws → 右侧工具卡</code></pre>\n<p>VS Code 侧栏 / <code>@webagent</code>：<code>extension.js</code> 的 <code>postNdjson</code> 打本机 <code>127.0.0.1:48271/api/chat</code>（无 Cloudflare 头，闸门放行），后面不变。</p>\n<h3 id=\"2.3-路径-b-网页-agent-mcp-本机仓库\">2.3 路径 B：网页 Agent → MCP → 本机仓库</h3>\n<pre class=\"lang-text\"><code>云上聊天栏\n  → HTTPS https://*.trycloudflare.com/mcp/&lt;secret&gt;\n       或 ChatGPT Plus：canonical /mcp + Bearer（oauth.js PKCE）\n  → Cloudflare 转到本机 :48271（只应收 /mcp；带 CF 头的 /api 404）\n  → mcp/server.js requireAuth\n       extractToken：Bearer → 路径密钥 → x-mcp-secret → query\n  → handleRpc\n       initialize → instructions.getInstructions()\n       tools/list → getToolList()（不按 mode 过滤）\n        tools/call → callTool(name, args, remoteToolMode)   ← 默认 code；_meta.mode 可切 ask/plan\n  → 与路径 A 同一套 tools/*\n  → 工作台 BRIDGE 经 WebSocket 看到卡片</code></pre>\n<p>身份对照：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>客户端</th>\n<th>凭证</th>\n<th>说明书</th>\n</tr></thead><tbody>\n<tr>\n<td>Arena 等 paste-url</td>\n<td>URL 含 secret + CONNECT_LINE</td>\n<td><a href=\"#/files/mcp/文件名clientsjs\">clients.js</a></td>\n</tr>\n<tr>\n<td>DeepSeek++</td>\n<td>Streamable HTTP，prompt <strong>只有 URL</strong></td>\n<td>同上；扩展不进本仓库</td>\n</tr>\n<tr>\n<td>Chat Plus</td>\n<td>Streamable HTTP，prompt <strong>只有 URL</strong></td>\n<td>同上；GPL 不 vendor；不要装 MCP-Gateway</td>\n</tr>\n<tr>\n<td>ChatGPT 免费聊天</td>\n<td>不装扩展时通常调不了 MCP</td>\n<td><code>chatgpt-free</code> = unsupported-mcp</td>\n</tr>\n<tr>\n<td>ChatGPT Plus 连接器</td>\n<td>OAuth <code>scat_</code> + <code>/mcp</code> 无长期密钥</td>\n<td><a href=\"#/files/mcp/文件名oauthjs\">oauth.js</a></td>\n</tr>\n</tbody></table></div>\n<p><strong>源码事实：</strong> <code>POST /api/bridge/start</code>（<a href=\"#/files/api/文件名routesjs\">routes.js</a>）在登录校验后置 <code>bridgeRunning</code>、写 pairing；<strong><code>tunnelProvider==='cloudflare'</code> 时 <code>await tunnel.startQuickTunnel({ port })</code></strong>。成功则 <code>mcpOrigin</code> 用 <code>https://*.trycloudflare.com</code>；无 cloudflared / 超时仍 200，MCP 走当前 Host。<code>POST /bridge/stop</code> 调 <code>stopTunnel</code>。Named / ngrok <strong>不</strong> spawn。隧道模块见 <a href=\"#/files/tunnel\">tunnel/README.md</a>。</p>\n<h3 id=\"2.4-路径-c-网页-vs-code\">2.4 路径 C：网页 VS Code</h3>\n<pre class=\"lang-text\"><code>run-webagent-vscode.cmd\n  → scripts/run-code-oss.js\n       ensure() 可能 npm 安装 code-server@4.135.0 → bin/code-server-runtime/\n       syncExtension() 拷 extension/ → extensions-installed/\n       spawn agent-host WEBAGENT_SKIP_WORKBENCH=1（不占 3000）\n       wait /health :48271\n       spawn code-server :3000\n  → 插件 activate → 仍 POST http://127.0.0.1:48271/api/chat</code></pre>\n<p>不要与 <code>run-webagent.cmd</code> 同时开（抢 3000）。</p>\n<h3 id=\"2.5-一次-apply_patch-在文件夹之间怎么走\">2.5 一次 apply_patch 在文件夹之间怎么走</h3>\n<pre class=\"lang-text\"><code>MCP tools/call 或 Chat timedTool\n  → tools/index.js callTool\n       resolveToolName（bash→run_command 等）\n       normalizeToolArgs（path→filePath，&quot;true&quot;→布尔）\n  → patchEngine.applyPatch\n       resolveSafePath → sensitive.assertNotSensitive\n       没传 expectedHash → recalledHash（上次 read_files / 成功补丁）\n       仍无 hash（文件从未被本进程读过）→ HASH_REQUIRED + detail.currentHash\n       expectedHash 不符 → STALE_FILE + detail.currentHash\n       SEARCH/REPLACE 或 unified diff\n       写 .tmp.* 再 renameSync\n       rememberHash\n  → eventBus.broadcast('file_patched')\n  → utils/diff.createUnifiedDiff 给 UI\n  → 磁盘 = config.workspaceRoot（默认仓库 workspace/）</code></pre>\n<hr />\n<h2 id=\"3.-环境与配置总览\">3. 环境与配置总览</h2>\n<h3 id=\"3.1-仓库里实际有的清单\">3.1 仓库里实际有的清单</h3>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>常见文件</th>\n<th>本仓库</th>\n</tr></thead><tbody>\n<tr>\n<td>根 <code>package.json</code></td>\n<td><strong>无</strong></td>\n</tr>\n<tr>\n<td><code>requirements.txt</code></td>\n<td><strong>无</strong>（无 Python 服务）</td>\n</tr>\n<tr>\n<td><code>docker-compose.yml</code></td>\n<td><strong>无</strong></td>\n</tr>\n<tr>\n<td><code>webagent-core/agent-host/package.json</code></td>\n<td><strong>有</strong>：运行时 <code>express</code> <code>ws</code> <code>cors</code> <code>diff</code>；<code>scripts.start</code> / <code>scripts.test</code></td>\n</tr>\n<tr>\n<td><code>workspace/package.json</code></td>\n<td>演示计算器，<code>npm test</code> → <code>tests/calculator.test.js</code></td>\n</tr>\n<tr>\n<td><code>webagent-repro/package.json</code></td>\n<td>冻结原型，<strong>不要 npm start</strong></td>\n</tr>\n<tr>\n<td><code>bin/code-server-runtime/package.json</code></td>\n<td>占位，依赖恰好 <code>code-server@4.135.0</code></td>\n</tr>\n</tbody></table></div>\n<p>agent-host 依赖 Key 见 <a href=\"#/files/agent-host/文件名packagejson\">agent-host/README.md</a>。</p>\n<h3 id=\"3.2-install-build-run\">3.2 Install → Build → Run</h3>\n<p><strong>没有单独 Build 步。</strong> 自绘工作台是静态 <code>workbench/*.html|js|css</code>，由 Express 直接托管。</p>\n<p><strong>Install</strong></p>\n<ol>\n<li>安装 <a href=\"https://nodejs.org/\" target=\"_blank\" rel=\"noopener\">Node.js LTS</a>（勾选 npm）。Windows 用 <strong>CMD</strong>。</li>\n<li>仓库根：</li>\n</ol>\n<pre class=\"lang-bat\"><code>check-env.cmd</code></pre>\n<p>检查 PATH 里的 node / npm / git / cloudflared。git、cloudflared 不是启动的硬前置（缺 git 则 git 工具不可用；缺 cloudflared 则本机 Chat 仍可跑）。</p>\n<ol>\n<li>依赖：第一次 <code>run-webagent.cmd</code> 若没有 <code>webagent-core\\agent-host\\node_modules\\express</code> 会执行 <code>npm install --no-audit --no-fund</code>。不必在仓库根 <code>npm install</code>。</li>\n</ol>\n<p><strong>Build</strong></p>\n<p>无。不要找 <code>npm run build</code>。网页 VS Code 第一次会从 npm <strong>下载</strong> code-server（约 50MB+），那是安装运行时，不是编译本仓库。</p>\n<p><strong>Run</strong></p>\n<pre class=\"lang-bat\"><code>run-webagent.cmd</code></pre>\n<p>默认工作区 <code>.\\workspace</code>。改自己的仓库（路径必须已存在）：</p>\n<pre class=\"lang-bat\"><code>run-webagent.cmd D:\\code\\my-app</code></pre>\n<p>浏览器：<strong>http://127.0.0.1:3000</strong>（工作台）。MCP：<strong>http://127.0.0.1:48271</strong>（见黑窗口打印的 <code>/mcp/&lt;密钥&gt;</code>）。</p>\n<p>停：CMD 窗口 <strong>Ctrl+C</strong>。</p>\n<p>其它：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>目的</th>\n<th>命令</th>\n</tr></thead><tbody>\n<tr>\n<td>网页真 VS Code</td>\n<td><code>run-webagent-vscode.cmd</code>（工作区必须已存在）</td>\n</tr>\n<tr>\n<td>跑产品测试</td>\n<td><code>run-tests.cmd</code> → <code>agent-host</code> 的 <code>npm test</code></td>\n</tr>\n<tr>\n<td>Linux/macOS 对照</td>\n<td><code>./run-webagent.sh</code>（指南主体仍是 CMD）</td>\n</tr>\n<tr>\n<td>架构/源码可视化 HTML</td>\n<td><code>node docs-site/serve.js</code> → http://127.0.0.1:4173/</td>\n</tr>\n</tbody></table></div>\n<p>环境变量（<code>src/config.js</code> 与根 <code>.cmd</code> 一致）：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>变量</th>\n<th>默认</th>\n<th>含义</th>\n</tr></thead><tbody>\n<tr>\n<td><code>WORKSPACE_ROOT</code></td>\n<td>仓库 <code>workspace/</code></td>\n<td>工具允许读写的根</td>\n</tr>\n<tr>\n<td><code>WORKBENCH_PORT</code></td>\n<td><code>3000</code></td>\n<td>工作台 UI</td>\n</tr>\n<tr>\n<td><code>AGENT_HOST_PORT</code></td>\n<td><code>48271</code></td>\n<td>MCP；本机 <code>/api</code>（隧道上来则 404）</td>\n</tr>\n<tr>\n<td><code>WEBAGENT_BIND</code></td>\n<td><code>127.0.0.1</code></td>\n<td>工作台 / MCP / 网页 VS Code 监听地址。只有要听局域网时才设 <code>0.0.0.0</code></td>\n</tr>\n<tr>\n<td><code>WEBAGENT_SKIP_WORKBENCH</code></td>\n<td>未设</td>\n<td><code>1</code> 时不听 3000（vscode 入口会设）</td>\n</tr>\n<tr>\n<td><code>WEBAGENT_CORS_ORIGINS</code></td>\n<td>空</td>\n<td>额外允许跨域打 <code>/mcp</code> 的 Origin，逗号分隔。扩展协议与文档点名的聊天站已内置</td>\n</tr>\n<tr>\n<td><code>CODE_SERVER_PASSWORD</code></td>\n<td>随机并落盘</td>\n<td>网页 VS Code 登录口令</td>\n</tr>\n<tr>\n<td><code>CODE_SERVER_AUTH</code></td>\n<td><code>password</code></td>\n<td><code>none</code> 时 code-server 不要登录页</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"3.3-不要当成启动方式的东西\">3.3 不要当成启动方式的东西</h3>\n<ul>\n<li><code>cd webagent-repro &amp;&amp; npm start</code> — 冻结原型，端口/工具/密钥都对不上。</li>\n<li>根目录再造 <code>tests/</code> — 测试已在 <code>webagent-core/agent-host/tests/</code>。</li>\n<li><code>bin/code-server-dist</code> — 已删除；测试禁止再 vendor。</li>\n<li>为连 Bridge 去装 <code>deepseek-pp-shell-host</code> — 不需要。</li>\n<li>为连 Bridge 去装 <code>aiguicai/MCP-Gateway</code> 或把 Chat Plus 源码拷进本仓库 — 不需要。</li>\n</ul>\n<hr />\n<h2 id=\"4.-和其它根文档的分工\">4. 和其它根文档的分工</h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>文档</th>\n<th>角色</th>\n</tr></thead><tbody>\n<tr>\n<td><a href=\"./README.md\">README.md</a></td>\n<td>GitHub 首页：最短开始</td>\n</tr>\n<tr>\n<td><a href=\"#/guide\">架构导读.md</a></td>\n<td>人话 → 比喻 → 文件落地 → 行业叫法</td>\n</tr>\n<tr>\n<td><a href=\"./docs-site/README.md\">docs-site/</a></td>\n<td>把导读、技术实现、总览、行级 README 收成可点的 HTML；<code>node docs-site/serve.js</code></td>\n</tr>\n<tr>\n<td><a href=\"#/workflow\">组件说明.md</a></td>\n<td>小白工作流、为什么这样装</td>\n</tr>\n<tr>\n<td><a href=\"#/impl\">技术实现.md</a></td>\n<td>代码逐步直译</td>\n</tr>\n<tr>\n<td><a href=\"./使用指南.md\">使用指南.md</a></td>\n<td>Windows 按键</td>\n</tr>\n<tr>\n<td><strong>本文件 总览.md</strong></td>\n<td>子 README 索引 + 全局调用链 + 环境</td>\n</tr>\n</tbody></table></div>\n<p>第四阶段未提供正文，不在此自拟。</p>"},"workflow":{"title":"组件说明","toc":[{"level":2,"text":"1. 三个必须先分清的东西","id":"1.-三个必须先分清的东西"},{"level":2,"text":"2. 整条工作流（从双击到文件被改）","id":"2.-整条工作流-从双击到文件被改"},{"level":3,"text":"路径 A：本机 Chat（最简单，不必装 cloudflared）","id":"路径-a-本机-chat-最简单-不必装-cloudflared"},{"level":3,"text":"路径 B：网页 Agent 改本机仓库（这个项目存在的理由）","id":"路径-b-网页-agent-改本机仓库-这个项目存在的理由"},{"level":3,"text":"路径 C：网页里打开真正的 VS Code（换皮，不换引擎）","id":"路径-c-网页里打开真正的-vs-code-换皮-不换引擎"},{"level":2,"text":"3. 具体怎么运行","id":"3.-具体怎么运行"},{"level":3,"text":"3.1 最短路径（演示计算器）","id":"3.1-最短路径-演示计算器"},{"level":3,"text":"3.2 改你自己的仓库（重点）","id":"3.2-改你自己的仓库-重点"},{"level":3,"text":"3.3 让网页 Agent 动手","id":"3.3-让网页-agent-动手"},{"level":3,"text":"3.4 验证实现不是空文档","id":"3.4-验证实现不是空文档"},{"level":2,"text":"4. 一次「打补丁」在代码里怎么走","id":"4.-一次-打补丁-在代码里怎么走"},{"level":2,"text":"5. 为什么要这样实现","id":"5.-为什么要这样实现"},{"level":2,"text":"6. 打开 GitHub 时这些文件夹是谁","id":"6.-打开-github-时这些文件夹是谁"},{"level":3,"text":"6.1 webagent-core/workbench/ —— 给人看的壳","id":"6.1-webagent-core/workbench/-给人看的壳"},{"level":3,"text":"6.2 webagent-core/agent-host/ —— 真正干活的进程","id":"6.2-webagent-core/agent-host/-真正干活的进程"},{"level":3,"text":"6.3 extension/ 和网页 VS Code","id":"6.3-extension/-和网页-vs-code"},{"level":3,"text":"6.4 工作区里的 .webagent/","id":"6.4-工作区里的-.webagent/"},{"level":2,"text":"7. 常见误读","id":"7.-常见误读"},{"level":2,"text":"8. 还想看细节时读哪篇","id":"8.-还想看细节时读哪篇"}],"html":"<h1 id=\"这个仓库是干什么的\">这个仓库是干什么的</h1>\n<p>先不用记文件夹名字。用一句话：</p>\n<blockquote><p><strong>程序跑在你自己的电脑上，真正改硬盘上的代码；网页上的 AI 只负责「想」，想完通过一条加密地址来调用你电脑上的工具。</strong></p></blockquote>\n<p>同类产品公开文档可参考：<a href=\"https://docs.shuncode.top/docs/intro/\" target=\"_blank\" rel=\"noopener\">docs.shuncode.top</a>。本仓库是本机工作台 + 独立 agent-host，不是 exe 安装包。</p>\n<ul>\n<li><strong>按按钮、装软件、CMD 命令：</strong> 请跟 <a href=\"./使用指南.md\">使用指南.md</a>（Windows）。</li>\n<li><strong>DeepSeek 网页怎么装扩展：</strong> <a href=\"./网页DeepSeek使用指南.md\">网页DeepSeek使用指南.md</a>。</li>\n<li><strong>多个网页 AI 用 Chat Plus：</strong> <a href=\"./网页ChatPlus使用指南.md\">网页ChatPlus使用指南.md</a>（不 fork、不装 MCP-Gateway）。</li>\n<li><strong>浏览器里要真正的 VS Code：</strong> <a href=\"./网页VSCode使用指南.md\">网页VSCode使用指南.md</a>。</li>\n<li><strong>完全没写过这种程序、先要人话+比喻：</strong> <a href=\"#/guide\">架构导读.md</a>。</li>\n<li><strong>对着源码逐步直译（函数拆步骤、分支不漏）：</strong> <a href=\"#/impl\">技术实现.md</a>。</li>\n<li><strong>本文：</strong> 整条工作流、为什么这样装、打开 GitHub 时每个目录是谁。过时的交接稿已经删掉，以本文为准。</li>\n</ul>\n<hr />\n<h2 id=\"1.-三个必须先分清的东西\">1. 三个必须先分清的东西</h2>\n<p>很多人第一次晕，是因为把「程序」和「被改的项目」看成同一个文件夹。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>名字</th>\n<th>它是什么</th>\n<th>打个比方</th>\n</tr></thead><tbody>\n<tr>\n<td><strong>本 Git 仓库</strong> <code>web_agent</code></td>\n<td>Web Agent 这套<strong>程序</strong>的源码</td>\n<td>工具箱</td>\n</tr>\n<tr>\n<td><strong>工作区</strong>（默认 <code>workspace/</code>，也可以是 <code>D:\\code\\my-app</code>）</td>\n<td>Agent <strong>允许读写的那一个目录</strong></td>\n<td>正在修的那台机器</td>\n</tr>\n<tr>\n<td><strong>网页上的 AI</strong>（Arena / DeepSeek / ChatGPT…）</td>\n<td>只会聊天、下指令</td>\n<td>远方的师傅，看不见你的车库，除非你把门开一条缝</td>\n</tr>\n</tbody></table></div>\n<p>默认双击启动时，工作区就是仓库里的 <code>workspace\\</code>（一个带测试的小计算器）。<strong>它不是 Web Agent 引擎。</strong> 你平时干活的项目，要用启动参数指过去，见第 4 节。</p>\n<p>再记三个运行时名词：</p>\n<ol>\n<li><strong>agent-host</strong>  </li>\n</ol>\n<p> 真正动手的 Node 进程。读文件、打补丁、跑 <code>npm test</code>，全是它。源码在 <code>webagent-core/agent-host/</code>。</p>\n<ol>\n<li><strong>工作台</strong>  </li>\n</ol>\n<p> 浏览器打开 http://127.0.0.1:3000 看到的黑底界面（欢迎页、CHAT、BRIDGE）。源码在 <code>webagent-core/workbench/</code>。它自己<strong>改不了磁盘</strong>，按钮都会打到 agent-host。</p>\n<ol>\n<li><strong>Bridge / MCP / 隧道</strong>  </li>\n</ol>\n<ul>\n<li><strong>MCP</strong>：一套「AI 调工具」的协议。本仓库把工作区暴露成 MCP 服务。  </li>\n<li><strong>Bridge</strong>：工作台里「启动 Bridge」那一页，等于打开大门。  </li>\n<li><strong>隧道（cloudflared）</strong>：云上的网页访问不到你电脑的 <code>127.0.0.1</code>，所以要一条临时的 <code>https://xxxx.trycloudflare.com</code> 把请求转发进家。本机 Chat <strong>不需要</strong>隧道。</li>\n</ul>\n<hr />\n<h2 id=\"2.-整条工作流-从双击到文件被改\">2. 整条工作流（从双击到文件被改）</h2>\n<p>电脑上同时活着的，其实是这样：</p>\n<pre><code>你的 Windows 电脑\n│\n├── 黑色 CMD 窗口          必须一直开着 = agent-host 还在跑\n│     端口 3000  →  工作台网页（给你点）\n│     端口 48271 →  工具真正改磁盘的地方\n│\n├── 浏览器 http://127.0.0.1:3000\n│     右侧 CHAT   = 本机说话，不经过互联网\n│     右侧 BRIDGE = 看「外面的 AI」有没有在调工具\n│\n└──（只有网页 Agent 改盘时才需要）\n      cloudflared  把 48271 映射成 https://xxxx.trycloudflare.com</code></pre>\n<p>下面三条路径，<strong>改文件的引擎是同一套</strong>。差别只是「谁下指令」。</p>\n<h3 id=\"路径-a-本机-chat-最简单-不必装-cloudflared\">路径 A：本机 Chat（最简单，不必装 cloudflared）</h3>\n<p>适合：先确认程序能跑、先改演示计算器、或不想把电脑暴露到公网。</p>\n<pre><code>你在右侧 CHAT 打字\n        │\n        ▼\n工作台 app.js  POST 本机 :3000 /api/chat\n        │\n        ▼\nagent-host 的 runChat.js（认第二参或 payload.emit）\n   没填 API Key → 内置探索 Agent（会搜、读、必要时打补丁并跑测试）\n   填了 Key     → 按 OpenAI 兼容接口让模型循环调工具（最多约 10 步）\n        │\n        ▼\ntools/index.js  按模式上锁\n   Ask / Plan = 只读\n   Code / Agent = 可以 apply_patch、run_command\n        │\n        ▼\n工作区磁盘上的文件变了</code></pre>\n<p>输入框下 <strong>Agent ▾</strong> 默认就是 <strong>Web Agent Code</strong>（和 Copilot 侧栏的 Agent 一样：直接动手）。</p>\n<ul>\n<li>Ask：只读，分析项目、查原因。  </li>\n<li>Plan：只读 + 多模型博弈（意见一致再行动）。  </li>\n<li>Code / Agent：搜 → 读 → 补丁 → 再测。</li>\n</ul>\n<p><strong>为什么本机 Chat 不走隧道：</strong> 浏览器和工作台、agent-host 都在你这台电脑上，<code>127.0.0.1</code> 互相找得到。把隧道开给本机 Chat 只会多一个故障点。</p>\n<h3 id=\"路径-b-网页-agent-改本机仓库-这个项目存在的理由\">路径 B：网页 Agent 改本机仓库（这个项目存在的理由）</h3>\n<p>适合：你想用 Arena、DeepSeek 网页、Chat Plus 覆盖的其它网页、或其它能调工具的聊天栏，改<strong>你自己硬盘</strong>上的代码。</p>\n<p><strong>为什么必须绕这么一圈：</strong></p>\n<p>云上的 ChatGPT / Arena / DeepSeek 运行在别人的机房。它们默认只能看到对话文字，<strong>进不了你的 <code>D:\\code</code></strong>。如果你把 MCP 地址写成 <code>http://127.0.0.1:48271/...</code>，云端去连的是「它自己那台机器的 127.0.0.1」，不是你家电脑。所以：</p>\n<ol>\n<li>本机 agent-host 在 48271 上提供 MCP。  </li>\n<li>cloudflared 开一条 <strong>Quick Tunnel</strong>（不用 Cloudflare 账号），得到  </li>\n</ol>\n<p> <code>https://随机名.trycloudflare.com</code>。 </p>\n<ol>\n<li>网页 Agent 访问的是这个 https 地址；Cloudflare 再转到你电脑的 48271。  </li>\n<li>URL 里带一串<strong>密钥</strong>。拿到完整地址的人可以读你的文件、在你电脑上跑命令。所以<strong>不要发到群里、Issue、截图网盘</strong>。</li>\n</ol>\n<pre><code>云上的网页 Agent  （Arena / DeepSeek / 其它）\n        │  HTTPS\n        ▼\nhttps://xxxx.trycloudflare.com/mcp/&lt;密钥&gt;\n        │  Cloudflare 转到你家\n        ▼\n本机 agent-host :48271  只收 /mcp（和登录发现文档）\n        │  带 Cloudflare 头的 /api 会 404，不会把密钥送到公网\n        │  tools/call\n        ▼\nD:\\code\\my-app  里的文件被改\n        │  WebSocket 只在 :3000 /ws\n        ▼\n工作台右侧 BRIDGE 出现工具卡片</code></pre>\n<p>几种接法（都不绑死 ChatGPT，<strong>不必 Plus</strong> 也能走扩展或贴 URL）：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你用的聊天栏</th>\n<th>它会不会真的调 MCP</th>\n<th>你要做的事</th>\n</tr></thead><tbody>\n<tr>\n<td><strong>Arena</strong> 等能把 URL 当工具后端的网页栏</td>\n<td>会（把第一句里的 URL 拿去连）</td>\n<td>Bridge 里<strong>复制提示词</strong>（URL + 那句连接说明），新对话整段当第一句</td>\n</tr>\n<tr>\n<td><strong>DeepSeek 网页</strong></td>\n<td>网页自己<strong>不会</strong>。要靠第三方扩展 DeepSeek++ 当手</td>\n<td>Chrome/Edge 装扩展，侧边栏 MCP 填 <strong>Streamable HTTP</strong> 和那一行 URL。见 <a href=\"./网页DeepSeek使用指南.md\">网页DeepSeek使用指南.md</a>。不要装它的 Shell Native Host</td>\n</tr>\n<tr>\n<td><strong>Chat Plus 扩展（多网站）</strong></td>\n<td>网页自己<strong>不会</strong>。扩展当手</td>\n<td>从 GitHub 编译 Chat Plus，侧边栏填 <strong>Streamable HTTP</strong> 和那一行 URL。见 <a href=\"./网页ChatPlus使用指南.md\">网页ChatPlus使用指南.md</a>。不要装 MCP-Gateway，也不要把 GPL 源码拷进本仓库</td>\n</tr>\n<tr>\n<td><strong>ChatGPT 免费普通聊天</strong></td>\n<td><strong>不装扩展时通常不会</strong></td>\n<td>改用本机 Chat、Arena、DeepSeek++，或 Chat Plus</td>\n</tr>\n<tr>\n<td><strong>ChatGPT Plus 开发者模式</strong>（可选）</td>\n<td>会（官方连接器）</td>\n<td>填规范地址 <code>https://….trycloudflare.com/mcp</code>（<strong>不要</strong>带密钥），OAuth 配对码只出现在本机 Bridge 页</td>\n</tr>\n</tbody></table></div>\n<p><strong>为什么 DeepSeek / ChatGPT 网页要扩展、Arena 只要贴 URL：</strong> Arena 这类产品聊天栏本身就会按 MCP 去调工具。DeepSeek、免费 ChatGPT 网页模型只会打字，不会开 HTTP 调你的电脑；DeepSeek++ 或 Chat Plus 识别它打出的工具标记，替它执行，再把结果塞回同一条对话。本仓库<strong>不 fork 扩展源码</strong>，只写安装步骤。Chat Plus 源码里就能填任意 Streamable HTTP，连我们不必再跑他们的 MCP-Gateway。</p>\n<p><strong>为什么密钥放在 URL 里，ChatGPT Plus 却用另一套：</strong> 贴 URL 的客户端没有登录框，密钥等于「门卡」。ChatGPT 连接器支持 OAuth，所以给它看不带密钥的 <code>/mcp</code>，在本机输入一次性配对码，避免把长期密钥贴进聊天。</p>\n<p>Quick Tunnel 的<strong>域名每次「启动 Bridge」都会变</strong>，必须重新复制。密钥存在工作区 <code>.webagent\\config.json</code>，一般不换；点「重置 MCP 地址」才会换，旧链接立刻失效。</p>\n<h3 id=\"路径-c-网页里打开真正的-vs-code-换皮-不换引擎\">路径 C：网页里打开真正的 VS Code（换皮，不换引擎）</h3>\n<p>适合：想要语法高亮、多文件标签、VS Code 插件生态，同时还要同一套 Chat / Bridge。</p>\n<pre><code>run-webagent-vscode.cmd\n        │\n        ├── agent-host 仍在 :48271（环境变量 WEBAGENT_SKIP_WORKBENCH=1，不再占用 3000）\n        └── 从 npm 下载完整 code-server 到 bin/code-server-runtime/（不进 Git）\n              监听 :3000 = 真 VS Code Web\n              侧栏插件来自 webagent-core/extension/</code></pre>\n<p><strong>不要和 <code>run-webagent.cmd</code> 同时开</strong>（抢 3000 端口）。 网页 Agent 改磁盘仍然走路径 B，和界面是自绘工作台还是 VS Code <strong>无关</strong>。</p>\n<p>Git 里曾经放过一份残缺的 code-server 切片，缺关键 <code>out/</code>，<strong>已经删掉</strong>。现在第一次跑 vscode 脚本会从 npm 拉完整包。</p>\n<hr />\n<h2 id=\"3.-具体怎么运行\">3. 具体怎么运行</h2>\n<p>Windows 用 <strong>CMD</strong>（不要把 Git Bash / WSL 当主路径）。命令的逐条说明、防火墙、排错见 <a href=\"./使用指南.md\">使用指南.md</a>。这里只把「按下去之后发生了什么」讲清楚。</p>\n<h3 id=\"3.1-最短路径-演示计算器\">3.1 最短路径（演示计算器）</h3>\n<ol>\n<li>安装 <a href=\"https://nodejs.org/\" target=\"_blank\" rel=\"noopener\">Node.js LTS</a>（勾选 npm）。装完<strong>新开</strong> CMD。  </li>\n<li>仓库根目录（能看见 <code>run-webagent.cmd</code> 的那一层）运行：</li>\n</ol>\n<pre class=\"lang-bat\"><code>check-env.cmd\nrun-webagent.cmd</code></pre>\n<ol>\n<li>黑色窗口应出现类似：</li>\n</ol>\n<pre><code>UI        http://127.0.0.1:3000\nMCP       http://127.0.0.1:48271/mcp/……\nWorkspace D:\\code\\web_agent\\workspace</code></pre>\n<ol>\n<li>浏览器打开 <strong>http://127.0.0.1:3000</strong>，右侧 CHAT 说一句：<code>分析当前项目实现了什么功能</code>。  </li>\n<li>停止：在黑色窗口按 <strong>Ctrl+C</strong>。窗口关了，工作台和工具就都停了。</li>\n</ol>\n<p><code>run-webagent.cmd</code> 实际做的事：检查 Node → 若没装过依赖就 <code>npm install</code> → <code>node webagent-core\\agent-host\\src\\index.js</code>。没有第二套隐藏服务。</p>\n<h3 id=\"3.2-改你自己的仓库-重点\">3.2 改你自己的仓库（重点）</h3>\n<p>默认改的是本仓库的 <code>workspace\\</code>。要改 <code>D:\\code\\my-app</code>：</p>\n<pre class=\"lang-bat\"><code>cd /d D:\\code\\web_agent\nrun-webagent.cmd D:\\code\\my-app</code></pre>\n<p><code>D:\\code\\my-app</code> 必须已经存在。agent-host 会把<strong>工作区根</strong>设成它，工具禁止写到这个目录外面。看黑色窗口的 <code>Workspace</code> 那一行，对上了再开始改。</p>\n<h3 id=\"3.3-让网页-agent-动手\">3.3 让网页 Agent 动手</h3>\n<ol>\n<li>安装 cloudflared（<code>winget install --id Cloudflare.cloudflared</code>，然后<strong>新开</strong> CMD）。  </li>\n<li>仍然用 <code>run-webagent.cmd</code> 挂你的仓库。  </li>\n<li>工作台左下角齿轮 → <strong>Bridge</strong> → <strong>启动 Bridge</strong>。默认 Cloudflare 会拉起 Quick Tunnel；成功后地址是 <code>https://….trycloudflare.com/mcp/…</code>。没有 cloudflared 时按钮仍成功，MCP 走当前页面源（本机预览可用，云上连不上）。Named / ngrok 目前不会 spawn。  </li>\n<li>按第 2 节表格连接 Arena、DeepSeek++ 或 Chat Plus。  </li>\n<li>右侧切到 <strong>BRIDGE</strong>，应出现工具调用；资源管理器里文件会变。</li>\n</ol>\n<h3 id=\"3.4-验证实现不是空文档\">3.4 验证实现不是空文档</h3>\n<pre class=\"lang-bat\"><code>run-tests.cmd</code></pre>\n<p>测试在 <code>webagent-core\\agent-host\\tests\\</code>，根目录不必再放一份 <code>tests\\</code>。说明见 <a href=\"./测试说明.md\">测试说明.md</a>。</p>\n<p>Linux / macOS 对应 <code>./run-webagent.sh</code>。网页 VS Code：<code>run-webagent-vscode.cmd</code>。</p>\n<hr />\n<h2 id=\"4.-一次-打补丁-在代码里怎么走\">4. 一次「打补丁」在代码里怎么走</h2>\n<p>以「DeepSeek 网页让你修测试」为例，方便对照源码：</p>\n<ol>\n<li>你在 <code>chat.deepseek.com</code> 说：修测试并跑 <code>npm test</code>。  </li>\n<li>DeepSeek++ 拦截模型的工具标记，对隧道地址发 MCP JSON-RPC：<code>initialize</code> → <code>tools/list</code> → <code>tools/call</code>。  </li>\n<li><code>webagent-core/agent-host/src/mcp/server.js</code> 校验 URL 里的密钥（或 OAuth Bearer）。  </li>\n<li><code>src/mcp/instructions.js</code> 在 <code>initialize</code> 时把规则塞给客户端（Ask 只读、要先 <code>read_files</code> 拿哈希、再 <code>apply_patch</code>…）。客户端若没吃到，还可以读资源 <code>webagent://instructions</code>。  </li>\n<li><code>src/tools/index.js</code> 按当前模式决定能不能写。网页这边默认按 Code 执行。  </li>\n<li><code>read_files</code>（<code>fileOps.js</code>）读文件并返回 <strong>sha256</strong>，主机写入 <code>.webagent/read-hashes.json</code>（重启后还在，直到点「清除本轮统计」）。  </li>\n<li><code>apply_patch</code>（<code>patchEngine.js</code>）用 SEARCH/REPLACE。刚读过同一路径可以不带 <code>expectedHash</code>；从未读过会 <code>HASH_REQUIRED</code>（带 <code>currentHash</code>）；哈希过期 <code>STALE_FILE</code>。网页端工具失败是 MCP <code>isError</code> 文本，不是掉线。  </li>\n<li><code>run_command</code> / <code>start_command</code>（<code>executor.js</code>）在工作区跑命令。<strong>Windows 走 PowerShell</strong>。网页若调用工具名 <code>bash</code>/<code>cat</code>/<code>ls</code>，主机会收成正式名，但命令字符串仍是 PowerShell。  </li>\n<li><code>eventBus.js</code> 通过 WebSocket <code>/ws</code> 推到工作台，右侧 BRIDGE 画出工具卡片。  </li>\n<li>磁盘上的文件已经变了。密钥、<code>.env</code>、SSH 私钥、<code>.webagent/config.json</code> 会被 <code>sensitive.js</code> 拦住，读不出来。</li>\n</ol>\n<p>本机 Chat 从第 3 步换成 <strong>3000</strong> 的 <code>POST /api/chat</code> → <code>runChat.js</code>，后面同一套工具。</p>\n<hr />\n<h2 id=\"5.-为什么要这样实现\">5. 为什么要这样实现</h2>\n<p>这些选择都是故意的，不是随便拆目录。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>做法</th>\n<th>为什么</th>\n</tr></thead><tbody>\n<tr>\n<td><strong>UI 和工作台在 3000，MCP 在 48271</strong></td>\n<td>两个端口两件事。网页 VS Code 要占用 3000 时，agent-host 可以只留 48271（<code>WEBAGENT_SKIP_WORKBENCH=1</code>）。第一代原型 <code>webagent-repro</code> 把网页和 MCP 挤在同一个 3000，换皮时会打架。隧道只转发 48271 的 <code>/mcp</code>；公网打 <code>/api</code> 得 404。</td>\n</tr>\n<tr>\n<td><strong>模型循环不写进 VS Code 内核</strong></td>\n<td>编辑器只是外壳，推理和工具在独立 agent-host。换自绘工作台或 code-server，改盘逻辑不用重写。同类产品也是这个拆法（见 <a href=\"https://docs.shuncode.top/docs/intro/\" target=\"_blank\" rel=\"noopener\">docs.shuncode.top</a>）。</td>\n</tr>\n<tr>\n<td><strong>Git 不内嵌 code-server</strong></td>\n<td>上游不发布 Windows 安装包；内嵌切片缺 <code>out/</code>，启动即报错。改为第一次运行时 npm 下载完整包到 <code>bin/code-server-runtime/</code>（Git 忽略内容）。</td>\n</tr>\n<tr>\n<td><strong>不把 DeepSeek++ 源码拷进仓库</strong></td>\n<td>它是社区扩展，有自己的商店更新。本仓库只需要它当「手」；改磁盘仍走我们的 agent-host。再装 <code>deepseek-pp-shell-host</code> 会多一个本机命令通道，和 MCP 无关。</td>\n</tr>\n<tr>\n<td><strong>不把 Chat Plus 源码拷进仓库</strong></td>\n<td>GPL v3 会传染许可证；上游自己更新。本仓库只写编译加载步骤。连 Bridge <strong>不要</strong>再装 <code>aiguicai/MCP-Gateway</code>（我们的 agent-host 已经是 MCP）。</td>\n</tr>\n<tr>\n<td><strong>默认工作区是 <code>workspace/</code>，不是仓库根</strong></td>\n<td>仓库根是 Web Agent 程序。若把根当工作区，Agent 可能改到 <code>run-webagent.cmd</code>、指南、测试。工作区边界把「被修的项目」和「修它的工具」分开。</td>\n</tr>\n<tr>\n<td><strong><code>.webagent</code> 建在工作区里，不在 GitHub 仓库根</strong></td>\n<td>类似项目里的 <code>.vscode</code>：设置、密钥、Skills 属于<strong>正在编辑的那个项目</strong>。所以 GitHub 打开 <code>web_agent</code> 根目录看不见 <code>.webagent</code>，要进 <code>workspace/</code>。你自己的 <code>D:\\code\\my-app</code> 跑过一次、保存过设置后才会出现。</td>\n</tr>\n<tr>\n<td><strong>Ask / Plan / Code 三档锁工具</strong></td>\n<td>防止「我只是问一句」却被改文件。Ask/Plan 调用 <code>apply_patch</code> 会被拒。</td>\n</tr>\n<tr>\n<td><strong>改文件优先 <code>apply_patch</code>，而不是整文件覆盖</strong></td>\n<td>带哈希预检，避免模型凭记忆覆盖你刚改的内容。</td>\n</tr>\n<tr>\n<td><strong>命令在 Windows 上走 PowerShell</strong></td>\n<td>用户主路径是 Windows CMD。假设 bash 会在别人电脑上直接失败。</td>\n</tr>\n<tr>\n<td><strong>留下 <code>webagent-repro/</code> 但冻结</strong></td>\n<td>方便对照「最早那版 Bridge」。启动脚本<strong>不会</strong>进去。不要 <code>npm start</code>。</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"6.-打开-github-时这些文件夹是谁\">6. 打开 GitHub 时这些文件夹是谁</h2>\n<pre><code>web_agent/                         ← 你克隆下来的 Git 仓库 = 工具箱\n├── run-webagent.cmd               ★ Windows 主入口（自绘工作台）\n├── run-webagent-vscode.cmd        可选：浏览器里真 VS Code\n├── check-env.cmd / run-tests.cmd\n├── 使用指南.md                    按键说明书\n├── 组件说明.md                    本文\n├── 架构导读.md                    人话 + 比喻 + 文件落地 + 行业叫法\n├── 网页DeepSeek使用指南.md\n├── 网页ChatPlus使用指南.md\n├── 网页VSCode使用指南.md\n├── 技术实现.md                    代码级实现\n├── 测试说明.md\n├── webagent-core/                 ★ 正在维护的程序\n│   ├── workbench/                 你在 3000 端口看到的网页\n│   ├── agent-host/                改磁盘、MCP、Chat、隧道\n│   ├── extension/                 VS Code 插件源码\n│   └── extensions-installed/      给 code-server 的已安装副本\n├── workspace/                     默认演示项目（计算器），不是引擎\n├── webagent-repro/                第一代原型，勿运行\n├── bin/                           网页 VS Code 下载到这里（内容不进 Git）\n└── .config/code-server/           code-server 的 yaml（要密码、默认 127.0.0.1:3000）</code></pre>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你在 GitHub 看到的</th>\n<th><code>run-webagent.cmd</code> 会不会用它</th>\n<th>一句话</th>\n</tr></thead><tbody>\n<tr>\n<td><strong><code>webagent-core/</code></strong></td>\n<td><strong>会</strong></td>\n<td>现行产品</td>\n</tr>\n<tr>\n<td><strong><code>workspace/</code></strong></td>\n<td>当作默认工作区挂上</td>\n<td>演示计算器</td>\n</tr>\n<tr>\n<td><strong><code>webagent-repro/</code></strong></td>\n<td><strong>不会</strong></td>\n<td>旧原型</td>\n</tr>\n<tr>\n<td><strong><code>bin/code-server-runtime/</code></strong></td>\n<td>只有 vscode 脚本会</td>\n<td>npm 下的完整 code-server</td>\n</tr>\n<tr>\n<td><strong><code>image-search/</code></strong></td>\n<td>不会</td>\n<td>本地截图，已 gitignore</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"6.1-webagent-core/workbench/-给人看的壳\">6.1 <code>webagent-core/workbench/</code> —— 给人看的壳</h3>\n<p><code>index.html</code> + <code>app.js</code>（入口）+ <code>js/*.js</code>（按职责拆开，无打包）+ <code>styles.css</code>。欢迎页「Web Agent / 编辑进化」、右侧 CHAT / BRIDGE、齿轮里的自定义设置（含 <strong><code>#page-env</code> 环境偏好</strong>、<strong><code>#page-stack</code> 技术栈</strong>）、<strong>打开 DeepSeek</strong> 等按钮，都在这里。它通过 <strong>3000</strong> 的 <code>/api/*</code> 和 <code>/ws</code> 找 agent-host。<code>bind.js</code> 用 <code>onClick</code> 包一层：按钮节点不存在时不抛，避免整页 Chat 绑不上。</p>\n<h3 id=\"6.2-webagent-core/agent-host/-真正干活的进程\">6.2 <code>webagent-core/agent-host/</code> —— 真正干活的进程</h3>\n<p>入口 <code>src/index.js</code>：<strong>两套</strong> Express（<code>uiApp</code> 3000、<code>mcpApp</code> 48271）。网页 VS Code 模式下只听 48271。mcp 口 <code>/api</code> 先过 <code>src/utils/localControl.js</code>：隧道头或公网 Host 得 404；本机回环仍通。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>目录</th>\n<th>干什么</th>\n</tr></thead><tbody>\n<tr>\n<td><code>src/mcp/</code></td>\n<td>MCP 协议：<code>server.js</code> 收 JSON-RPC；<code>clients.js</code> 里是本机 Chat / Arena / DeepSeek / Chat Plus / ChatGPT 各怎么连；<code>oauth.js</code> 给 Plus 连接器；<code>instructions.js</code> 是给模型的规则</td>\n</tr>\n<tr>\n<td><code>src/tools/</code></td>\n<td>约 25 个正式名（另收 <code>bash</code>/<code>cat</code>/<code>ls</code> 等别名）。Ask/Plan 只读，Code 可写。危险命令要 <code>confirm_dangerous</code>；删文件要 <code>confirm</code>；覆盖 <code>write_file</code> 要 <code>confirm_overwrite</code> 或哈希仍匹配。不是 git 仓库时 <code>git_status</code> 返回 <code>available:false</code></td>\n</tr>\n<tr>\n<td><code>src/agent/</code></td>\n<td>本机 Chat：<code>runChat.js</code>、接 API 的 <code>openai.js</code></td>\n</tr>\n<tr>\n<td><code>src/tunnel/</code></td>\n<td>启动 <code>cloudflared tunnel --url http://127.0.0.1:48271</code>，从日志里抠 <code>*.trycloudflare.com</code></td>\n</tr>\n<tr>\n<td><code>src/api/routes.js</code></td>\n<td>工作台用的 REST：状态、启停 Bridge、Chat、文件树、自定义设置。工作台走 3000；VS Code 插件走本机 48271</td>\n</tr>\n<tr>\n<td><code>src/utils/localControl.js</code></td>\n<td>判断是不是本机控制面；挡住隧道上的未鉴权 <code>/api</code></td>\n</tr>\n<tr>\n<td><code>src/models/</code></td>\n<td>工作区 <code>.webagent/</code> 里的配置、指令、记忆</td>\n</tr>\n<tr>\n<td><code>tests/</code></td>\n<td>产品测试，<code>run-tests.cmd</code> 跑这里</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"6.3-extension/-和网页-vs-code\">6.3 <code>extension/</code> 和网页 VS Code</h3>\n<p>只在 code-server / VS Code 加载插件时生效。侧栏 Chat、原生 Chat <code>@webagent</code>（默认 Agent）打的仍是本机 <code>http://127.0.0.1:48271/api/chat</code>。<code>extensions.json</code> 含本机绝对路径，启动时重写，<strong>不进 Git</strong>。</p>\n<p>自绘工作台不走这套插件 API，但同样有 <strong>Agent ▾</strong>。</p>\n<h3 id=\"6.4-工作区里的-.webagent/\">6.4 工作区里的 <code>.webagent/</code></h3>\n<p>建在<strong>被编辑的项目根</strong>下，不是 <code>web_agent</code> 根下。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>路径</th>\n<th>提交到 Git？</th>\n<th>干什么</th>\n</tr></thead><tbody>\n<tr>\n<td><code>.webagent/config.json</code></td>\n<td><strong>不要</strong>（含 MCP 密钥）</td>\n<td>模型列表、Bridge、secret</td>\n</tr>\n<tr>\n<td><code>.webagent/instructions.md</code></td>\n<td>可以</td>\n<td>始终生效的指令，会进 MCP <code>initialize.instructions</code></td>\n</tr>\n<tr>\n<td><code>.webagent/preference.md</code></td>\n<td>可以</td>\n<td>OS / Shell / 回复语言</td>\n</tr>\n<tr>\n<td><code>.webagent/tech-stack.md</code></td>\n<td>可以</td>\n<td>语言、测试命令；Code 模式跑测试时优先用</td>\n</tr>\n<tr>\n<td><code>.webagent/skills/&lt;名&gt;/SKILL.md</code></td>\n<td>可以</td>\n<td>技能；界面「技能引导」三步生成</td>\n</tr>\n<tr>\n<td><code>.webagent/memory/</code></td>\n<td>按需</td>\n<td>Agent 跨对话备忘</td>\n</tr>\n<tr>\n<td><code>.webagent/customizations.json</code></td>\n<td>按需</td>\n<td>智能体 / 挂钩 / 提示等 UI 配置</td>\n</tr>\n</tbody></table></div>\n<p>演示区提交了 <code>workspace/.webagent/instructions.md</code> 和三个 skill（<code>fix-tests</code>、<code>review</code>、<code>docs-sync</code>）。<code>config.json</code> 与 <code>read-hashes.json</code> 被 <code>.gitignore</code> 排除。<code>review</code> 是只读审查流程；<code>docs-sync</code> 是给 <code>load_skill</code> 读的约定：改功能必须同步改说明书，动到「为什么这样装」时还要按四层写法改 <a href=\"#/guide\">架构导读.md</a>。</p>\n<hr />\n<h2 id=\"7.-常见误读\">7. 常见误读</h2>\n<ol>\n<li><strong>「应该 <code>cd webagent-repro &amp;&amp; npm start</code>」</strong>  </li>\n</ol>\n<p> 那是旧原型，端口和工具都对不上现在的指南。用根目录 <code>run-webagent.cmd</code>。</p>\n<ol>\n<li><strong>「Git 里没有 code-server 就开不了 VS Code」</strong>  </li>\n</ol>\n<p> 用 <code>run-webagent-vscode.cmd</code>，它会从 npm 装到 <code>bin/code-server-runtime/</code>。</p>\n<ol>\n<li><strong>「<code>.webagent</code> 应该出现在 web_agent 仓库根上」</strong>  </li>\n</ol>\n<p> 不会。它属于被编辑的项目。</p>\n<ol>\n<li><strong>「把这个 GitHub 发给别人，别人就能改我电脑」</strong>  </li>\n</ol>\n<p> 不会。程序必须跑在<strong>你的</strong>电脑上，再用隧道让云上的网页连进来。MCP 地址不能公开。</p>\n<ol>\n<li><strong>「要把 DeepSeek++ 源码拷进本仓库才能连 DeepSeek」</strong>  </li>\n</ol>\n<p> 不要。商店安装即可。也不要为连 Bridge 去装 <code>deepseek-pp-shell-host</code>。</p>\n<ol>\n<li><strong>「免费 ChatGPT 聊天栏贴了 URL 就会改磁盘」</strong>  </li>\n</ol>\n<p> 通常不会。它没有自定义 MCP。用本机 Chat、Arena、DeepSeek++，或 Chat Plus 扩展当手。</p>\n<ol>\n<li><strong>「要把 Chat Plus 源码拷进本仓库，或先装 MCP-Gateway」</strong>  </li>\n</ol>\n<p> 不要。GPL，不 vendor。扩展侧填我们的 Streamable HTTP 即可。</p>\n<ol>\n<li><strong>「工作台里点「打开 DeepSeek」就会连上」</strong>  </li>\n</ol>\n<p> 那一页只复制地址、给步骤。扩展只能加载在本机 Chrome/Edge 的 <code>chat.deepseek.com</code> 上，内置假浏览器跑不了扩展。</p>\n<hr />\n<h2 id=\"8.-还想看细节时读哪篇\">8. 还想看细节时读哪篇</h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>问题</th>\n<th>文档</th>\n</tr></thead><tbody>\n<tr>\n<td>没装过、要从零点到改文件</td>\n<td><a href=\"./使用指南.md\">使用指南.md</a></td>\n</tr>\n<tr>\n<td>为什么这样装（人话、比喻、磁盘上长什么样）</td>\n<td><a href=\"#/guide\">架构导读.md</a></td>\n</tr>\n<tr>\n<td>同一套导读做成可点的图和四层卡片</td>\n<td><a href=\"./docs-site/README.md\">docs-site/</a>（<code>node docs-site/serve.js</code>）</td>\n</tr>\n<tr>\n<td>DeepSeek 网页 + 扩展</td>\n<td><a href=\"./网页DeepSeek使用指南.md\">网页DeepSeek使用指南.md</a></td>\n</tr>\n<tr>\n<td>多个网页 AI + Chat Plus</td>\n<td><a href=\"./网页ChatPlus使用指南.md\">网页ChatPlus使用指南.md</a></td>\n</tr>\n<tr>\n<td>浏览器里真 VS Code</td>\n<td><a href=\"./网页VSCode使用指南.md\">网页VSCode使用指南.md</a></td>\n</tr>\n<tr>\n<td>测试覆盖了什么</td>\n<td><a href=\"./测试说明.md\">测试说明.md</a></td>\n</tr>\n<tr>\n<td><strong>函数逐步直译、if/try 分支、文件在链条上的位置</strong></td>\n<td><strong><a href=\"#/impl\">技术实现.md</a></strong></td>\n</tr>\n<tr>\n<td>同类产品公开文档</td>\n<td>https://docs.shuncode.top/docs/intro/</td>\n</tr>\n</tbody></table></div>"},"terms":[{"term":"Agent","meaning":"会自己决定下一步、去调工具的助手，不是只会聊天"},{"term":"MCP","meaning":"网页 AI 给本机填的标准工单格式"},{"term":"Bridge","meaning":"工作台上「开门让网页进来」的那一页"},{"term":"隧道 / Quick Tunnel","meaning":"给家里 48271 办的临时公网门牌"},{"term":"`127.0.0.1`","meaning":"「这台电脑自己」，每台机器都有一个，不能当你家地址给云用"},{"term":"工作区","meaning":"Agent 能改的那一个文件夹"},{"term":"sha256 / 哈希","meaning":"文件内容的指纹，用来发现「你读完之后有人改过」"},{"term":"Ask / Plan / Code","meaning":"参观证 / 开会不许动手 / 施工证"},{"term":"OAuth / 配对码","meaning":"给会弹登录的客户端用的一次性访客码"},{"term":"Streamable HTTP","meaning":"工单用普通网页那种请求来回传，不是另接一套专用线"},{"term":"WebSocket `/ws`","meaning":"车间往店堂屏幕推「刚干完哪一步」"},{"term":"Skill","meaning":"放在工作区里的一篇作业指导书，模型用 `load_skill` 读"},{"term":"CORS","meaning":"浏览器问「那个网页能不能看这次回答」。本仓库只给扩展和名单里的聊天站点头；**不是**门卡"}],"files":{"webagent-core":{"id":"webagent-core","path":"webagent-core/README.md","group":"产品","title":"webagent-core 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：start-webagent.sh","id":"文件名-start-webagent.sh"},{"level":2,"text":"3. 执行逻辑流（仅本层）","id":"3.-执行逻辑流-仅本层"}],"html":"<h1 id=\"webagent-core-模块说明书\">webagent-core 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/</code></p>\n<p>这是本仓库 <strong>正在维护的产品代码</strong>。<code>run-webagent.cmd</code> / <code>run-webagent.sh</code> 只启动这里的 <code>agent-host</code>。</p>\n<p>本层直接文件只有 <code>start-webagent.sh</code> 与本 README。子目录源码的行级说明在各自内部 README，这里不把 <code>agent-host/src/**</code> 再抄一遍。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 产品代码根：工作台、MCP 进程、VS Code 插件、code-server 启动脚本。</li>\n<li><strong>兄弟依赖：</strong> 被仓库根启动脚本调用；默认工作区是仓库根 <code>workspace/</code>（不在本文件夹内）。</li>\n<li><strong>谁调用：</strong> 根目录 <code>run-webagent*.cmd/.sh</code>、<code>run-tests.cmd</code>。</li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>子目录</th>\n<th>职责</th>\n<th>行级 README</th>\n</tr></thead><tbody>\n<tr>\n<td><code>workbench/</code></td>\n<td>浏览器工作台（http://127.0.0.1:3000）</td>\n<td><code>workbench/README.md</code></td>\n</tr>\n<tr>\n<td><code>agent-host/</code></td>\n<td>MCP <code>:48271</code>、本机 Chat、隧道、磁盘工具</td>\n<td><code>agent-host/README.md</code> 与 <code>src/*/README.md</code></td>\n</tr>\n<tr>\n<td><code>extension/</code></td>\n<td>VS Code 插件：侧栏 + <code>@webagent</code></td>\n<td><code>extension/README.md</code></td>\n</tr>\n<tr>\n<td><code>scripts/</code></td>\n<td>网页 VS Code：下载 code-server 并双进程启动</td>\n<td><code>scripts/README.md</code></td>\n</tr>\n<tr>\n<td><code>extensions-installed/</code></td>\n<td><code>code-server --extensions-dir</code> 的已安装副本</td>\n<td><code>extensions-installed/README.md</code></td>\n</tr>\n</tbody></table></div>\n<p>Windows 操作见根目录 <a href=\"../使用指南.md\">使用指南.md</a>。工作流见 <a href=\"../组件说明.md\">组件说明.md</a>。人话架构见 <a href=\"../架构导读.md\">架构导读.md</a>。测试见 <code>agent-host/tests/</code> 与 <a href=\"../测试说明.md\">测试说明.md</a>。</p>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-start-webagent.sh\">📄 文件名：<code>start-webagent.sh</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 从 <code>webagent-core/</code> 跳回仓库根，转调根目录 <code>run-webagent.sh</code>。不是 MCP 实现。</li>\n<li><strong>核心逻辑拆解（文件共 4 行）：</strong></li>\n<li>L1：shebang <code>#!/bin/bash</code></li>\n<li>L2：<code>set -e</code>，命令失败即退出</li>\n<li>L3：<code>ROOT</code> = 本脚本所在目录的上一级（仓库根）</li>\n<li>L4：<code>exec &quot;$ROOT/run-webagent.sh&quot;</code> 替换当前进程，参数不转发（本文件无 <code>&quot;$@&quot;</code>）</li>\n<li><strong>关键变量：</strong> 无配置常量。无函数。</li>\n</ul>\n<p>无 <code>.py</code> / <code>.js</code> / <code>.html</code> / <code>.json</code> 位于本层。</p>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本层\">3. 执行逻辑流（仅本层）</h2>\n<ol>\n<li>用户通常 <strong>不</strong> 先进入本目录，而是双击仓库根 <code>run-webagent.cmd</code> → <code>agent-host/src/index.js</code>。</li>\n<li>若有人执行本目录 <code>start-webagent.sh</code>：算出仓库根 → <code>exec run-webagent.sh</code> → 与根 bash 入口相同。</li>\n<li>网页 VS Code：根 <code>run-webagent-vscode.cmd</code> → <code>scripts/run-code-oss.js</code> → 拷 <code>extension/</code> 到 <code>extensions-installed/</code>，agent-host 跳过 3000。</li>\n</ol>"},"agent-host":{"id":"agent-host","path":"webagent-core/agent-host/README.md","group":"产品","title":"agent-host 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：package.json","id":"文件名-package.json"},{"level":2,"text":"3. 执行逻辑流（仅本层）","id":"3.-执行逻辑流-仅本层"}],"html":"<h1 id=\"agent-host-模块说明书\">agent-host 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/</code></p>\n<p>本目录是 <strong>现行 Bridge 进程</strong> 的 npm 包根。本层只有 <code>package.json</code>（无 <code>.js</code> / <code>.html</code>）。源码在 <code>src/</code>，测试在 <code>tests/</code>，各子目录已有第一阶段 README，这里不重复行级翻译那些 <code>.js</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 独立 Node 进程：工作台 UI（默认 3000）、MCP/API（默认 48271）、本机 Chat、磁盘工具、可选隧道。</li>\n<li><strong>兄弟依赖：</strong> 静态页来自 <code>../workbench/</code>（<code>src/index.js</code> 挂静态目录）。不依赖 <code>../extension/</code>（那是 code-server 插件）。不依赖 <code>../../webagent-repro/</code>。</li>\n<li><strong>谁调用：</strong> 仓库根 <code>run-webagent.cmd</code> / <code>.sh</code> 执行 <code>node src/index.js</code>；<code>../scripts/run-code-oss.js</code> 同样启动并设 <code>WEBAGENT_SKIP_WORKBENCH=1</code>；<code>run-tests.cmd</code> 跑 <code>npm test</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-package.json\">📄 文件名：<code>package.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> npm 清单：启动命令、测试顺序、运行时依赖。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>name</code></td>\n<td>包名</td>\n<td><code>agent-host</code></td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td>npm 版本</td>\n<td><code>1.0.0</code>（展示用产品版本在 <code>src/config.js</code> 的 <code>0.6.9</code>，不是这个字段）</td>\n</tr>\n<tr>\n<td><code>main</code></td>\n<td>Node 默认入口字段</td>\n<td><code>index.js</code>（<strong>本目录根没有该文件</strong>；真正启动走 <code>scripts.start</code>）</td>\n</tr>\n<tr>\n<td><code>scripts.start</code></td>\n<td><code>npm start</code></td>\n<td><code>node src/index.js</code></td>\n</tr>\n<tr>\n<td><code>scripts.test</code></td>\n<td><code>npm test</code></td>\n<td>18 个 <code>tests/*.test.js</code> 用 <code>&amp;&amp;</code> 串联，顺序见 <a href=\"./tests/README.md\"><code>tests/README.md</code></a>。任一非 0 即停。</td>\n</tr>\n<tr>\n<td><code>keywords</code></td>\n<td>npm 关键词</td>\n<td><code>[]</code></td>\n</tr>\n<tr>\n<td><code>author</code></td>\n<td>作者</td>\n<td><code>&quot;&quot;</code></td>\n</tr>\n<tr>\n<td><code>license</code></td>\n<td>许可证</td>\n<td><code>ISC</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td>简介</td>\n<td><code>&quot;&quot;</code></td>\n</tr>\n<tr>\n<td><code>dependencies.cors</code></td>\n<td>CORS 中间件</td>\n<td><code>^2.8.6</code>（<code>src/utils/corsAllow.js</code> 的白名单，不再全开）</td>\n</tr>\n<tr>\n<td><code>dependencies.diff</code></td>\n<td>jsdiff</td>\n<td><code>^9.0.0</code>（<code>src/utils/diff.js</code>、<code>tools/patchEngine.js</code>）</td>\n</tr>\n<tr>\n<td><code>dependencies.express</code></td>\n<td>HTTP</td>\n<td><code>^5.2.1</code></td>\n</tr>\n<tr>\n<td><code>dependencies.ws</code></td>\n<td>WebSocket</td>\n<td><code>^8.21.3</code>（<code>src/index.js</code> 的 <code>/ws</code>）</td>\n</tr>\n</tbody></table></div>\n<p>无 <code>devDependencies</code>。测试用 Node 自带 <code>assert</code>。</p>\n<p><code>package-lock.json</code> 是 lockfile，不在本说明书展开每个嵌套包。</p>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本层\">3. 执行逻辑流（仅本层）</h2>\n<ol>\n<li><code>cd webagent-core/agent-host</code>（或由根 <code>.cmd</code> <code>cd</code> 进来）。</li>\n<li>没有 <code>node_modules/express</code> 时 <code>npm install</code>（根脚本检查的是这个路径）。</li>\n<li><code>node src/index.js</code> → 见 <code>src/README.md</code>（双端口、OAuth、MCP）。</li>\n<li><code>npm test</code> → 见 <code>tests/README.md</code>。</li>\n</ol>"},"src":{"id":"src","path":"webagent-core/agent-host/src/README.md","group":"进程","title":"src 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：config.js","id":"文件名-config.js"},{"level":3,"text":"📄 文件名：index.js","id":"文件名-index.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"src-模块说明书\">src 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/</code></p>\n<p>本 README 只覆盖<strong>本目录直接文件</strong> <code>config.js</code>、<code>index.js</code>。子目录各有自己的 README：<code>mcp/</code>（已完成）、<code>tools/</code>、<code>agent/</code>、<code>models/</code>、<code>api/</code>、<code>tunnel/</code>、<code>utils/</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 进程入口与全局单例配置。<code>index.js</code> 组装<strong>两套</strong> Express（店堂 UI 与后厨 MCP）、开两个 HTTP 端口。</li>\n<li><strong>依赖：</strong> 本目录几乎所有子模块；npm：<code>express</code>、<code>ws</code>、<code>cors</code>。</li>\n<li><strong>谁调用：</strong> <code>run-webagent.cmd</code> / <code>.sh</code> 执行 <code>node src/index.js</code>；<code>scripts/run-code-oss.js</code> 同样启动并设 <code>WEBAGENT_SKIP_WORKBENCH=1</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-config.js\">📄 文件名：<code>config.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 进程内单例：端口、工作区根、secret、隧道 URL。任何 <code>require('../config')</code> 拿到同一块对象。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>模块加载（L1–L21）</strong></li>\n<li>L4–L6：<code>workspaceRoot = path.resolve(process.env.WORKSPACE_ROOT || …/workspace)</code>。</li>\n<li>L8–L21 <code>config</code> 对象，见 Key 表。</li>\n<li><strong>Function <code>generateNewSecret()</code>（L23–L29）</strong> — 12 字节 hex 写入 <code>config.secretKey</code>；lazy <code>require('./models/store').patch({ secretKey })</code> 落盘（失败 catch 空）。返回新值。</li>\n<li><strong>Function <code>persistIdentity(store)</code>（L31–L37）</strong></li>\n<li>L32：<code>store.load()</code>。</li>\n<li>L33–L34：有 <code>saved.secretKey</code> 则覆盖内存，否则 <code>store.patch({ secretKey })</code>。</li>\n<li>L35–L36：<code>installId</code> 同样。</li>\n</ul>\n<ul>\n<li><strong>关键变量 <code>config</code> Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>含义</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>port</code></td>\n<td>MCP/API 端口</td>\n<td><code>AGENT_HOST_PORT</code> 或 <code>48271</code></td>\n</tr>\n<tr>\n<td><code>workbenchPort</code></td>\n<td>UI 端口</td>\n<td><code>WORKBENCH_PORT</code> 或 <code>3000</code></td>\n</tr>\n<tr>\n<td><code>host</code></td>\n<td>listen 地址</td>\n<td><code>WEBAGENT_BIND</code> 或 <strong><code>'127.0.0.1'</code></strong>（不再默认听所有网卡）</td>\n</tr>\n<tr>\n<td><code>workspaceRoot</code></td>\n<td>工具允许读写的根</td>\n<td>环境变量或仓库 <code>workspace/</code></td>\n</tr>\n<tr>\n<td><code>secretKey</code></td>\n<td>URL 密钥</td>\n<td>先随机 12 字节 hex，随即可能被 persistIdentity 换成磁盘值</td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td>展示版本</td>\n<td><code>'0.6.9'</code></td>\n</tr>\n<tr>\n<td><code>serverName</code> / <code>productName</code></td>\n<td>MCP serverInfo / 日志</td>\n<td><code>WebAgent-AgentHost</code> / <code>Web Agent</code></td>\n</tr>\n<tr>\n<td><code>tunnelProvider</code></td>\n<td>隧道种类标签</td>\n<td>默认 <code>'cloudflare'</code></td>\n</tr>\n<tr>\n<td><code>publicTunnelUrl</code></td>\n<td>Quick Tunnel URL</td>\n<td>默认 <code>null</code>，由 tunnel 模块成功时写入</td>\n</tr>\n<tr>\n<td><code>bridgeRunning</code></td>\n<td>Bridge 开关</td>\n<td>默认 <code>false</code>，由 <code>/api/bridge/start</code> 置 true</td>\n</tr>\n<tr>\n<td><code>installId</code></td>\n<td>安装 ID</td>\n<td>8 字节 hex，可被磁盘覆盖</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h3 id=\"文件名-index.js\">📄 文件名：<code>index.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 创建 <strong>uiApp / mcpApp</strong> 两套 Express、两个 <code>http.Server</code>、可选跳过 3000。隧道打在 48271 时公网只应碰到 MCP/OAuth；<code>/api</code> 在 mcpApp 上要过 <code>rejectUnlessLocalControl</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>顶层启动（L1–L23）</strong></li>\n<li>L13：<code>require('./utils/corsAllow')</code> 的 <code>mcpCors</code> / <code>rejectCrossSiteApi</code>。</li>\n<li>L15：<code>persistIdentity(store)</code>。</li>\n<li>L17–L23：工作区不存在且设了 <code>WORKSPACE_ROOT</code> → 打印错误 <code>exit(1)</code>；未设环境变量 → <code>mkdirSync</code>。</li>\n<li><strong>Function <code>applyCommon(app)</code>（L25–33）</strong> — 关 x-powered-by、所有响应 <code>Cache-Control: no-store</code>、json 20mb、urlencoded。<strong>不再</strong>给两套 app 共用无条件 <code>cors()</code>。</li>\n<li><strong>Function <code>mountHealth(app)</code>（L35–39）</strong> — <code>GET /health</code> → <code>{ ok, product, version }</code>。</li>\n<li><strong>Function <code>mountWorkbench(app)</code>（L43–57）</strong> — 静态 <code>../../workbench</code>；SPA 回退：非 GET → next；path 以 <code>/api</code> <code>/mcp</code> <code>/ws</code> <code>/oauth</code> <code>/.well-known</code> 开头或恰好 <code>/register</code> → next；有扩展名 → next；否则 <code>index.html</code>。</li>\n<li><strong>两套 app（L60–72）</strong></li>\n<li>L60–64 <code>uiApp</code>：health、<code>/api</code> 先 <code>rejectUnlessLocalControl</code> 再 <code>rejectCrossSiteApi</code>、工作台静态 + SPA。<strong>没有</strong> cors、<strong>没有</strong> <code>/mcp</code>。</li>\n<li>L66–72 <code>mcpApp</code>：<code>mcpCors()</code>（白名单，不是 <code>cors()</code> 全开）、health、<code>oauth.router</code>、<code>/mcp</code>、<code>/api</code> 先 <code>rejectUnlessLocalControl</code> 再 <code>rejectCrossSiteApi</code>。<strong>没有</strong>静态工作台、<strong>没有</strong> SPA。</li>\n<li><strong>Function <code>attachWss(server)</code>（L74–94）</strong> — <code>WebSocketServer</code> path <code>/ws</code>；<strong>非本机控制面直接 close(1008)</strong>；否则 <code>addWsClient</code>，立刻 send <code>type:'connected'</code>，payload 只含 <code>serverName</code>、<code>version</code>（<strong>不含 secretKey</strong>）。</li>\n<li><strong>Function <code>listenOrExit(server, port, label)</code>（L100–110）</strong> — <code>error.code==='EADDRINUSE'</code> 打印占用后 <code>exit(1)</code>；其它 error 同样退出；<code>listen(port, config.host)</code>（默认 127.0.0.1）。</li>\n<li><strong>双服务器（L96–139）</strong></li>\n<li>L96–98：<code>uiServer = createServer(uiApp)</code>，<code>mcpServer = createServer(mcpApp)</code>；<strong>只</strong>给 uiServer attachWss。</li>\n<li>L112：<code>skipWorkbench = WEBAGENT_SKIP_WORKBENCH === '1'</code>。</li>\n<li>L114–126：非 skip 则 listen 3000 并打印 UI/MCP/<strong>Bind</strong>，以及「公网只收 /mcp 与 OAuth」。</li>\n<li>L127–134：skip 则打印「不占用 3000」，并说明 <code>/api</code> 仅本机回环。</li>\n<li>L136–139：无论 skip 都 listen <code>config.port</code>（48271）。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L41 <code>workbenchDir</code>；L60 <code>uiApp</code>；L66 <code>mcpApp</code>；L96–97 两个 Server。导出 <code>{ uiApp, mcpApp, uiServer, mcpServer }</code>（L141）。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>CMD/<code>node src/index.js</code> 加载 <code>config</code>（随机 secret）→ <code>persistIdentity</code> 可能换成磁盘密钥。</li>\n<li>确保工作区目录。</li>\n<li>两套 Express：<code>uiApp</code>（3000：工作台 + <code>/api</code> + <code>/ws</code>）与 <code>mcpApp</code>（48271：<code>/mcp</code> + OAuth + 本机才能打的 <code>/api</code>）。</li>\n<li>两个 listen：给人看的 3000（可跳过）与 MCP 的 48271。</li>\n<li>浏览器打开 3000 拿到 workbench；Chat 走 3000 的 <code>/api</code>；网页 Agent 走 <code>/mcp</code>（可能经隧道）。VS Code 插件仍打本机 <code>127.0.0.1:48271/api</code>（无 Cloudflare 头）。</li>\n<li><code>/ws</code> 只挂在 3000，把工具事件推回 UI；连接消息<strong>不下发</strong> secretKey。</li>\n</ol>"},"mcp":{"id":"mcp","path":"webagent-core/agent-host/src/mcp/README.md","group":"进程","title":"MCP 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：errors.js","id":"文件名-errors.js"},{"level":3,"text":"📄 文件名：session.js","id":"文件名-session.js"},{"level":3,"text":"📄 文件名：budget.js","id":"文件名-budget.js"},{"level":3,"text":"📄 文件名：instructions.js","id":"文件名-instructions.js"},{"level":3,"text":"📄 文件名：clients.js","id":"文件名-clients.js"},{"level":3,"text":"📄 文件名：resources.js","id":"文件名-resources.js"},{"level":3,"text":"📄 文件名：oauth.js","id":"文件名-oauth.js"},{"level":3,"text":"📄 文件名：server.js","id":"文件名-server.js"},{"level":2,"text":"3. 执行逻辑流（仅针对该子文件夹）","id":"3.-执行逻辑流-仅针对该子文件夹"}],"html":"<h1 id=\"mcp-模块说明书\">MCP 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/mcp/</code></p>\n<p>本文件只描述该目录内 8 个 <code>.js</code> 源码（无 <code>.json</code> / <code>.yaml</code> / 独立 <code>.html</code>）。行号以当前文件为准；解释不补源码里没有的调用。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> agent-host 的 <strong>MCP 协议门面</strong>。把本机工具暴露成 Streamable HTTP JSON-RPC 2.0（兼 SSE），给 Arena / DeepSeek++ / Chat Plus / ChatGPT 连接器等网页端调用。本目录<strong>不改磁盘</strong>：改文件发生在兄弟模块 <code>../tools/</code>。</li>\n<li><strong>在进程中的挂载（由 <code>../index.js</code> 完成，不在本目录）：</strong> <code>app.use(oauth.router)</code> 在前（匿名发现文档 + 配对页），然后 <code>app.use('/mcp', mcpRouter)</code>（<code>server.js</code> 导出的 Express Router）。</li>\n</ul>\n<p><strong>它调用的兄弟模块：</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>本目录文件</th>\n<th>require 的本目录文件</th>\n<th>require 的目录外模块</th>\n</tr></thead><tbody>\n<tr>\n<td><code>server.js</code></td>\n<td><code>instructions</code> <code>resources</code> <code>budget</code> <code>errors</code> <code>session</code> <code>oauth</code></td>\n<td><code>../tools</code>（<code>getToolList</code>/<code>callTool</code>）、<code>../config</code>、<code>../models/customizations</code>、<code>../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>oauth.js</code></td>\n<td>（无）</td>\n<td><code>../config</code></td>\n</tr>\n<tr>\n<td><code>resources.js</code></td>\n<td><code>session</code> <code>instructions</code> <code>clients</code></td>\n<td><code>../config</code>、<code>../tools</code>、<code>../tools/skills</code>、<code>../tools/progressTracker</code>、<code>../models/customizations</code>、<code>../models/profile</code>、<code>../models/memory</code>、<code>../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>instructions.js</code></td>\n<td>（无）</td>\n<td><code>../models/customizations</code>、<code>../config</code>、<code>../models/profile</code>、<code>../tools/skills</code></td>\n</tr>\n<tr>\n<td><code>clients.js</code></td>\n<td><code>instructions</code></td>\n<td>（无）</td>\n</tr>\n<tr>\n<td><code>budget.js</code> / <code>errors.js</code> / <code>session.js</code></td>\n<td>（无）</td>\n<td>（无）</td>\n</tr>\n</tbody></table></div>\n<p><strong>谁调用本模块：</strong> <code>../index.js</code> 挂路由；<code>../api/routes.js</code> 用 <code>oauth.snapshotPairing</code> / <code>ensurePairing</code> / <code>revokeAll</code>、<code>clients.listClients</code>、<code>instructions.getBootstrapPrompt</code>（工作台 Bridge 页，不走 JSON-RPC）。</p>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-errors.js\">📄 文件名：<code>errors.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 把工具失败分成「协议层」和「执行层」，供 <code>server.js</code> 决定回 JSON-RPC <code>error</code> 还是 MCP <code>isError: true</code>。</li>\n<li><strong>核心类/函数清单（代码行级注释）：</strong></li>\n</ul>\n<ul>\n<li><strong>Class <code>ProtocolError</code>（L1–L9）</strong></li>\n<li>输入：<code>code</code>（字符串错误码）、<code>message</code>、<code>detail</code>（可选对象）。</li>\n<li>行为：L3 <code>super(message)</code>；L4–L7 设 <code>name='ProtocolError'</code>、<code>layer='protocol'</code>、<code>code</code>、<code>detail || {}</code>。</li>\n<li><strong>Class <code>ExecutionError</code>（L11–L19）</strong></li>\n<li>与上相同，但 L14–L15 <code>name='ExecutionError'</code>、<code>layer='execution'</code>。</li>\n<li><strong>Function <code>classifyToolError(err)</code>（L21–L36）</strong></li>\n<li>输入：任意 thrown 值。返回：已分类的 Error。</li>\n<li>L22：已经是上述两类 → 原样返回。</li>\n<li>L23：取出 <code>err.message</code>，否则把 <code>err</code> 转字符串。</li>\n<li>L24–L35：<strong>按顺序第一次命中即返回</strong>（后面的正则不会再跑）：</li>\n<li>L24 <code>Unknown tool</code> → <code>E_UNKNOWN_CMD</code>（协议）</li>\n<li>L25 <code>locked in</code> 或 <code>Ask/Plan are read-only</code> → <code>E_BAD_ARGS</code></li>\n<li>L26 <code>requires </code> 或 <code>required</code> → <code>E_BAD_ARGS</code></li>\n<li>L27 <code>HASH_REQUIRED</code> → <code>E_BAD_ARGS</code>（协议；<code>apply_patch</code> 没 hash 时已是 <code>ProtocolError</code>，这条兜底裸 Error）</li>\n<li>L28 <code>STALE_FILE</code> → <code>E_STALE_FILE</code>（执行）</li>\n<li>L29 <code>Patch conflict</code> → <code>E_CONFLICT</code></li>\n<li>L30 <code>GIT_UNAVAILABLE</code> 或 <code>not a git repository</code> → <code>E_NOT_READY</code>（执行；现行 <code>git_status</code>/<code>git_diff</code> 多数情况已收成 <code>available:false</code>，不会走到这里）</li>\n<li>L31 <code>not found</code> / <code>No such file</code> → <code>E_NOT_FOUND</code></li>\n<li>L32 <code>timeout</code> / <code>isTimeout</code> → <code>E_TIMEOUT</code></li>\n<li>L33 <code>ACCESS_DENIED_SENSITIVE_FILE</code> / <code>E_FORBIDDEN</code> → <code>E_FORBIDDEN</code></li>\n<li>L34 <code>confirm_dangerous</code> / <code>confirm_overwrite</code> / <code>confirm=true</code> → <code>E_BAD_ARGS</code>（协议）</li>\n<li>L35 其它 → <code>E_INTERNAL</code>（执行）</li>\n<li><strong>Function <code>publicError(err)</code>（L38–L46）</strong></li>\n<li>返回 <code>{ layer, code, msg, detail }</code>，给 MCP 正文或 JSON-RPC <code>data</code>。<code>detail</code> 常含 <code>currentHash</code> / <code>retryHint</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量/常量：</strong> 无模块级配置。导出见 L48。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-session.js\">📄 文件名：<code>session.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 进程内 MCP 客户端心跳表（内存 <code>Map</code>，重启清空）。工作台用它显示「对面还在不在」。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>sessionKey(req)</code>（L3–L7）</strong></li>\n<li>输入：Express <code>req</code>（可空）。</li>\n<li>L4：ip = <code>req.ip</code> 或头 <code>x-forwarded-for</code>，都没有则 <code>'local'</code>。</li>\n<li>L5：client = <code>req.body.params.clientInfo.name</code>，没有则 <code>'mcp'</code>。</li>\n<li>L6：返回 <code></code> <code>${client}@${ip}</code> <code></code>。</li>\n<li><strong>Function <code>touch(req, extra = {})</code>（L9–L27）</strong></li>\n<li>输入：<code>req</code>；<code>extra</code> 可含 <code>key</code>、<code>incCall</code>、<code>incFail</code>、<code>busy</code>、以及要合并的其它字段。</li>\n<li>L10：key 优先 <code>extra.key</code>。</li>\n<li>L11–L16：没有旧记录则新建 <code>connectedAt</code>、<code>calls:0</code>、<code>fail:0</code>。</li>\n<li>L17–L24：覆盖 extra；<strong>始终</strong>刷新 <code>lastSeen</code>；仅当 <code>incCall</code>/<code>incFail</code> 为真才 +1；<code>busy = Boolean(extra.busy)</code>（没传则为 false）。</li>\n<li>L25–L26：写回 Map，返回 <code>next</code>。</li>\n<li><strong>Function <code>snapshot()</code>（L29–L41）</strong></li>\n<li>无输入。L30 按 <code>lastSeen</code> 字符串降序。</li>\n<li>L31–L32：最新一条的年龄毫秒；没有 latest 则 <code>ageMs=null</code>。</li>\n<li>L33–L40：<code>staleAfterMs</code> 写死 10000；<code>alive</code> 当 latest 存在且年龄 &amp;lt; 10s；<code>sessions</code> 最多 8 条。</li>\n<li><strong>Function <code>reset()</code>（L43–L46）</strong> — <code>sessions.clear()</code> 后返回 <code>snapshot()</code>。被 <code>POST /api/bridge/reset-round</code> 调用。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L1 <code>sessions = new Map()</code>，整个进程一份。导出 L48：<code>touch</code> / <code>snapshot</code> / <code>sessionKey</code> / <code>reset</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-budget.js\">📄 文件名：<code>budget.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 把工具结果截到约 16k 字符，避免网页 Agent 上下文被一次日志撑爆。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>estimateTokens(text)</code>（L4–L6）</strong></li>\n<li>输入：任意。返回 <code>ceil(length/4)</code>。源码里 MCP 主路径主要用 <code>clipText</code>/<code>clipJson</code>，本函数被导出供测试或其它模块。</li>\n<li><strong>Function <code>clipText(text, maxChars = MAX_CHARS)</code>（L8–L17）</strong></li>\n<li>L9：<code>null</code>/<code>undefined</code> 当空串。</li>\n<li>L10：未超长 → <code>{ text, truncated:false }</code>。</li>\n<li>L11–L16：保留 <code>maxChars-80</code>，后缀提示 truncated 字数以及用 offset/limit/cursor/<code>get_command_output</code>。</li>\n<li><strong>Function <code>clipJson(value, maxChars = MAX_CHARS)</code>（L19–L56）</strong></li>\n<li>L20：<code>null</code>/<code>undefined</code> 原样返回。</li>\n<li>L21–L26：<strong>字符串</strong>：截断则包成 <code>{ text, _truncated, originalChars }</code>，否则返回原字符串。</li>\n<li>L27–L28：stringify 后未超长 → 原对象。</li>\n<li>L29：非数组对象浅拷贝，否则包 <code>{ value }</code>。</li>\n<li>L30–L35：键 <code>stdout|stderr|content|preview|text|diff</code> 若字符串 &amp;gt;2000，再 clip 到 <code>min(4000, maxChars/3)</code>。</li>\n<li>L36–L40：<code>matches</code> 多于 20 条则切到 20，设 <code>nextCursor=20</code>。</li>\n<li>L41–L44：<code>items</code> 多于 40 条则切到 40，<code>nextCursor=40</code>。</li>\n<li>L45–L52：仍超长 → 只留 <code>_truncated</code> + <code>summary</code> + <code>hint</code>。</li>\n<li>L53–L55：否则给 copy 打 <code>_truncated</code> 和 <code>originalChars</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L1–L2 注释写明 ~4k tokens ≈ 16k chars；<code>MAX_CHARS = 16000</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-instructions.js\">📄 文件名：<code>instructions.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 给模型的「怎么用这台 MCP」说明书，以及剪贴板第一句连接语。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>getBootstrapPrompt(mcpUrl)</code>（L8–L10）</strong></li>\n<li>输入：<code>mcpUrl</code> 字符串（可空）。</li>\n<li>返回：URL + 空行 + <code>CONNECT_LINE</code>。Arena 等 paste-url 客户端当第一句。</li>\n<li><strong>Function <code>getInstructions()</code>（L59–L66）</strong></li>\n<li>无参数。L58 <code>loadCustom()</code>。</li>\n<li>L60：若 <code>custom.instructions</code> 真值，追加 <code>## Workspace instructions</code>。</li>\n<li>L61：追加 <code>formatWorkspaceContext</code>（环境/技术栈/skills）。</li>\n<li>L62：追加工作区根路径。</li>\n<li>L63：<code>SERVER_INSTRUCTIONS.trim()</code> 与 extra 用 <code>\\n\\n</code> 拼接。</li>\n</ul>\n<ul>\n<li><strong>关键变量/常量：</strong></li>\n<li>L6 <code>CONNECT_LINE</code>：固定一句中文（测试锁原文，改一字会红）。</li>\n<li>L12–L55 <code>SERVER_INSTRUCTIONS</code>：Ask/Plan 只读、Code 可写、工作流 1–7（含 git <code>available:false</code>、读后可省略 <code>expectedHash</code>、HASH_REQUIRED 带 <code>currentHash</code>）、输出预算、<code>tools/call</code> 失败是 MCP <code>isError</code> 文本（不是传输崩溃）、别名 <code>bash</code>/<code>cat</code>/<code>path</code>、安全（<code>confirm_dangerous</code> / <code>confirm_overwrite</code> / <code>confirm=true</code>）、memory。这是 <code>initialize.instructions</code> 的主体。模板字符串里<strong>不能</strong>写 <code>{layer,code,msg,detail}</code> 这种花括号，会当成 JS 插值炸掉。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-clients.js\">📄 文件名：<code>clients.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 工作台 Bridge 页「怎么连」卡片的<strong>数据</strong>，不是 MCP 协议实现。<code>server.js</code> 的 JSON-RPC <strong>不读取</strong>本文件。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>hydrateClient(client, urls)</code>（L136–L158）</strong></li>\n<li>输入：<code>CLIENTS</code> 里的一项；<code>urls.mcpUrl</code> / <code>urls.mcpCanonicalUrl</code>。</li>\n<li>L137–L138：canonical 缺省把 <code>/mcp/&lt;secret&gt;</code> 收成 <code>/mcp</code>。</li>\n<li>L140–L150 按 <code>connectMode</code> 设 <code>prompt</code>：</li>\n<li><code>paste-url</code> → <code>getBootstrapPrompt(mcpUrl)</code>（URL + CONNECT_LINE）</li>\n<li><code>oauth-connector</code> → 规范地址两行，不含长期密钥</li>\n<li><code>extension-http</code> → <strong>只有</strong> <code>mcpUrl</code> 一行（DeepSeek++ / Chat Plus URL 框）</li>\n<li><code>unsupported-mcp</code> → 空串</li>\n<li>其它（含 <code>local-chat</code>）→ <code>prompt</code> 保持 <code>''</code></li>\n<li>L151–L157：oauth 模式对外 <code>mcpUrl</code> 改成 canonical；附 <code>connectLine</code>。</li>\n<li><strong>Function <code>listClients(urls = {})</code>（L160–L162）</strong></li>\n<li>返回 <strong>7</strong> 张卡全部 hydrate。</li>\n<li><strong>Function <code>getClient(id, urls = {})</code>（L164–L167）</strong></li>\n<li>找不到 id 则 <strong>回落到 <code>CLIENTS[1]</code>（arena）</strong>。</li>\n</ul>\n<ul>\n<li><strong>关键变量 <code>CLIENTS</code>（L3–L133）——每项 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>含义</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>id</code></td>\n<td>卡片主键</td>\n<td><code>chat</code> / <code>arena</code> / <code>deepseek</code> / <code>chat-plus</code> / <code>generic</code> / <code>chatgpt-free</code> / <code>chatgpt-plus</code></td>\n</tr>\n<tr>\n<td><code>name</code></td>\n<td>UI 标题</td>\n<td>中文名</td>\n</tr>\n<tr>\n<td><code>url</code></td>\n<td>打开的网站；本机 Chat / generic 为 <code>null</code></td>\n<td>URL 或 null</td>\n</tr>\n<tr>\n<td><code>needsPlus</code></td>\n<td>是否必须付费档</td>\n<td>仅 <code>chatgpt-plus</code> 为 <code>true</code></td>\n</tr>\n<tr>\n<td><code>needsTunnel</code></td>\n<td>是否需要公网隧道</td>\n<td>仅 <code>chat</code> 为 <code>false</code></td>\n</tr>\n<tr>\n<td><code>supportsMcp</code></td>\n<td>该端会不会真调 MCP</td>\n<td><code>chat</code> 与 <code>chatgpt-free</code> 为 <code>false</code></td>\n</tr>\n<tr>\n<td><code>connectMode</code></td>\n<td>hydrate 分支</td>\n<td>见上</td>\n</tr>\n<tr>\n<td><code>summary</code> / <code>steps</code></td>\n<td>UI 文案</td>\n<td>字符串 / 字符串数组</td>\n</tr>\n<tr>\n<td><code>extensionId</code> / <code>storeUrl</code></td>\n<td>仅 DeepSeek 项</td>\n<td>CWS 扩展 ID 与商店 URL</td>\n</tr>\n<tr>\n<td><code>repoUrl</code></td>\n<td>仅 Chat Plus 项</td>\n<td><code>https://github.com/aiguicai/Chat-Plus</code></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h3 id=\"文件名-resources.js\">📄 文件名：<code>resources.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> MCP <code>resources/list</code> 与 <code>resources/read</code>。当网页客户端丢掉 <code>initialize.instructions</code> 时的第二条规则通道。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>listResources()</code>（L25–L27）</strong></li>\n<li>无输入。直接返回 <code>RESOURCE_DEFS</code>（L13–L23 的 8 项）。</li>\n<li><strong>Function <code>readResource(uri)</code>（L29–L112）</strong></li>\n<li>输入：字符串 URI。未知 → L110–L111 返回 <code>null</code>（由 <code>server.js</code> 转成 <code>E_NOT_FOUND</code>）。</li>\n<li>L32–L33 <code>webagent://instructions</code>：<code>getInstructions()</code>。</li>\n<li>L34–L39 <code>profile</code>：<code>formatWorkspaceContext(loadCustom(), listSkills())</code>。</li>\n<li>L38–L58 <code>protocol</code>：写死的传输/错误/心跳/补丁要点（markdown 数组 join）。含：<code>tools/call</code> 失败是 <code>isError: true</code>；JSON-RPC <code>error</code> 只给坏 jsonrpc / 未知 method / 缺 <code>params.name</code>；<code>git_status</code> 在普通文件夹返回 <code>available:false</code>；<code>apply_patch</code> 复用上次 <code>read_files</code> 的 sha256；<code>path</code>/<code>bash</code>/<code>cat</code>/<code>grep</code>/<code>ls</code> 别名。</li>\n<li>L58–L65 <code>capabilities</code>：<code>getToolList()</code> 拼 <code>tools N</code> + 每行 name/description。</li>\n<li>L66–L80 <code>config</code>：server/version/workspace/<code>bridgeRunning</code>/tunnel/<code>installId</code>/客户端数；L78 <strong>明文 <code>secret omitted</code>，不输出密钥</strong>。</li>\n<li>L81–L93 <code>workspace</code>：root、用户 instructions、任务状态、最近 5 条 event 的 type。</li>\n<li>L94–L97 <code>memory</code>：<code>recall({ limit: 80 }).text</code>。</li>\n<li>L98–L109 <code>clients</code>：用占位 <code>(mcp url)</code> 调用 <code>listClients</code>，列出每张卡 summary。</li>\n</ul>\n<ul>\n<li><strong>关键变量 <code>RESOURCE_DEFS</code>（L13–L23）：</strong> 每项 <code>uri</code> / <code>name</code> / <code>mimeType</code> / <code>description</code>。uri 取值仅上表 8 个 <code>webagent://…</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-oauth.js\">📄 文件名：<code>oauth.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> OAuth 2.1 子集（动态注册 + 授权码 + PKCE S256 + 本机配对码）。给 ChatGPT Plus 连接器。Arena / DeepSeek++ / Chat Plus <strong>不走本文件的授权码流程</strong>，但 <code>verifyAccessToken</code> 同时认 URL 密钥。</li>\n<li><strong>存储：</strong> L7–L10 四个内存 <code>Map</code>；L17 <code>pairing</code>。进程退出全丢。</li>\n</ul>\n<ul>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>now()</code>（L19–L21）</strong> — 返回 <code>Date.now()</code>。</li>\n<li><strong>Function <code>randomToken(prefix, bytes = 24)</code>（L23–L25）</strong> — <code></code> <code>${prefix}</code> + hex <code></code>。</li>\n<li><strong>Function <code>randomPairingCode()</code>（L27–L31）</strong></li>\n<li>L28 字母表去掉 0/O/1/I。L29–L30 用 8 字节映射成长度 8 的码。</li>\n<li><strong>Function <code>requestOrigin(req)</code>（L33–L38）</strong></li>\n<li>L34：若 <code>config.publicTunnelUrl</code> 真 → 去尾 <code>/</code> 返回（隧道域名）。</li>\n<li>L35–L37：否则用 <code>x-forwarded-proto</code> / <code>req.protocol</code> / <code>http</code> 与 host 头拼 origin。</li>\n<li><strong>Function <code>issuePairing()</code>（L40–L48）</strong> — 写入新 pairing（attempts:0，TTL 见常量），返回 snapshot。</li>\n<li><strong>Function <code>snapshotPairing()</code>（L50–L59）</strong></li>\n<li>L51：没有或过期 → <code>{ code:null, expiresInSec:0, expired:true }</code>。</li>\n<li>L55–L58：否则给出剩余秒数。</li>\n<li><strong>Function <code>ensurePairing()</code>（L61–L65）</strong> — 过期或无 code 则 issue，否则返回当前 snap。</li>\n<li><strong>Function <code>consumePairing(code)</code>（L67–L88）</strong></li>\n<li>L69–L73：过期 → 抛，<code>status=400</code>。</li>\n<li>L74–L80：<code>attempts &gt; 5</code> → <code>pairing=null</code>，抛 429。</li>\n<li>L81–L85：大小写不敏感比较失败 → 400。</li>\n<li>L86–L87：成功则 <code>pairing=null</code>（用过即废），返回 <code>true</code>。</li>\n<li><strong>Function <code>authorizationServerMetadata(origin)</code>（L90–L102）</strong> — 发现文档对象（issuer、authorize/token/register/revoke、S256、grant 类型）。</li>\n<li><strong>Function <code>protectedResourceMetadata(origin)</code>（L104–L110）</strong> — <code>resource</code> 为 <code>${origin}/mcp</code>。</li>\n<li><strong>Function <code>wwwAuthenticate(origin)</code>（L112–L114）</strong> — <code>Bearer realm=…</code> 指向 protected-resource 元数据。</li>\n<li><strong>Function <code>registerClient(body = {})</code>（L116–L142）</strong></li>\n<li>L117–L121：<code>redirect_uris</code> 必须是非空数组，否则 400。</li>\n<li>L122–L141：生成 <code>sccid_</code> / <code>sccsec_</code>，存 Map，返回注册结果。</li>\n<li><strong>Function <code>s256(verifier)</code>（L144–L146）</strong> — SHA-256 <code>base64url</code>。</li>\n<li><strong>Function <code>issueAccess(clientId)</code>（L148–L162）</strong> — 发 <code>scat_</code> / <code>scrt_</code>，写入两个 token Map。</li>\n<li><strong>Function <code>verifyAccessToken(token)</code>（L164–L175）</strong></li>\n<li>L165：假值 → <code>null</code>。</li>\n<li>L166：<code>token === config.secretKey</code> → <code>{ kind:'secret', clientId:'url-secret' }</code>（<strong>贴 URL 的密钥走这里</strong>）。</li>\n<li>L167–L172：Map 没有或过期（过期会 delete）→ <code>null</code>。</li>\n<li>L174：<code>{ kind:'oauth', clientId }</code>。</li>\n<li><strong>Function <code>revokeAll()</code>（L177–L183）</strong> — 四个 Map clear，<code>pairing=null</code>。被 <code>POST /api/bridge/reset-secret</code> 调用。</li>\n<li><strong>Function <code>authorizeHtml(query, error)</code>（L185–L218）</strong> — 返回完整 HTML 字符串（见下方 DOM）。</li>\n<li><strong>Function <code>escapeHtml(s)</code>（L220–L224）</strong> — <code>&amp; &lt; &gt; &quot; '</code>。</li>\n<li><strong>Function <code>completeAuthorize(body)</code>（L226–L257）</strong></li>\n<li>L227–L231：未知 client_id → 400。</li>\n<li>L232–L236：redirect_uri 不在注册列表 → 400。</li>\n<li>L237–L241：challenge method 缺省 S256；不是 S256 → 400。</li>\n<li>L242：<code>consumePairing</code>。</li>\n<li>L243–L250：发一次性 <code>sccode_</code>。</li>\n<li>L251–L256：302 目标 URL 带 <code>code</code>，有 <code>state</code> 则带上。</li>\n<li><strong>Function <code>handleToken(body = {})</code>（L259–L297）</strong></li>\n<li>L261–L283 <code>authorization_code</code>：code 无效/过期 400；<strong>先 delete code</strong>；client_id / redirect_uri 不符 400；无 verifier 或 PKCE 失败 400；然后 <code>issueAccess</code>。</li>\n<li>L284–L293 <code>refresh_token</code>：无效/过期 400；删旧双 token；再发一对。</li>\n<li>L294–L296 其它 grant → 400。</li>\n<li><strong>Function <code>tokenResponse(issued)</code>（L299–L306）</strong> — Bearer、expires_in 秒、refresh、scope <code>mcp</code>。</li>\n<li><strong>Function <code>sendError(res, err)</code>（L308–L311）</strong> — <code>status || 500</code>；400 时 <code>error=invalid_request</code> 否则 <code>server_error</code>。</li>\n<li><strong>Function <code>registerHandler</code>（L325–L331）</strong> — try 201 + <code>registerClient</code>；catch <code>sendError</code>。</li>\n</ul>\n<ul>\n<li><strong>路由（L313–L365）：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>行</th>\n<th>方法</th>\n<th>路径</th>\n<th>行为</th>\n</tr></thead><tbody>\n<tr>\n<td>L313–L315</td>\n<td>GET</td>\n<td><code>/.well-known/oauth-authorization-server</code></td>\n<td>metadata（匿名）</td>\n</tr>\n<tr>\n<td>L316–L318</td>\n<td>GET</td>\n<td><code>/.well-known/oauth-protected-resource</code></td>\n<td>资源元数据</td>\n</tr>\n<tr>\n<td>L319–L321</td>\n<td>GET</td>\n<td><code>/.well-known/oauth-protected-resource/mcp</code></td>\n<td>同上</td>\n</tr>\n<tr>\n<td>L332–L333</td>\n<td>POST</td>\n<td><code>/oauth/register</code> 与 <code>/register</code></td>\n<td>动态注册</td>\n</tr>\n<tr>\n<td>L335–L339</td>\n<td>GET</td>\n<td><code>/oauth/authorize</code></td>\n<td><code>ensurePairing</code> + HTML</td>\n</tr>\n<tr>\n<td>L341–L349</td>\n<td>POST</td>\n<td><code>/oauth/authorize</code></td>\n<td>try 302；catch 用错误重绘 HTML</td>\n</tr>\n<tr>\n<td>L351–L356</td>\n<td>POST</td>\n<td><code>/oauth/token</code></td>\n<td><code>handleToken</code></td>\n</tr>\n<tr>\n<td>L358–L365</td>\n<td>POST</td>\n<td><code>/oauth/revoke</code></td>\n<td>从两个 token Map delete，<strong>始终 200</strong> <code>{ revoked:true }</code></td>\n</tr>\n</tbody></table></div>\n<ul>\n<li><strong>内嵌 HTML DOM（<code>authorizeHtml</code> L185–L218）：</strong></li>\n<li>L192–L201：全页居中深色样式。</li>\n<li>L202–L216：<code>&lt;form method=&quot;post&quot; action=&quot;/oauth/authorize&quot;&gt;</code>。</li>\n<li>L205–L206：说明配对码 5 分钟、用过即废。</li>\n<li>L207：可选错误 <code>&lt;p class=&quot;err&quot;&gt;</code>。</li>\n<li>L208：用户输入 <code>pairing_code</code>。</li>\n<li>L209–L214：隐藏域 <code>client_id</code> <code>redirect_uri</code> <code>state</code> <code>code_challenge</code> <code>code_challenge_method</code> <code>response_type=code</code>。</li>\n<li>L215：提交按钮「确认配对」。</li>\n</ul>\n<ul>\n<li><strong>关键常量（L12–L15）：</strong></li>\n<li><code>PAIRING_TTL_MS</code> = 5 分钟</li>\n<li><code>CODE_TTL_MS</code> = 5 分钟</li>\n<li><code>ACCESS_TTL_MS</code> = 1 小时</li>\n<li><code>REFRESH_TTL_MS</code> = 7 天</li>\n</ul>\n<hr />\n<h3 id=\"文件名-server.js\">📄 文件名：<code>server.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> MCP 的 HTTP 入口。校验身份后，把 JSON-RPC method 派到本目录其它文件或 <code>../tools.callTool</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>extractToken(req)</code>（L17–L24）</strong> — 顺序：Bearer 头 → <code>params.secret</code> → 头 <code>x-mcp-secret</code> → <code>query.secret</code> → <code>''</code>。</li>\n<li><strong>Function <code>isAuthorized(req)</code>（L26–L28）</strong> — <code>Boolean(oauth.verifyAccessToken(extractToken(req)))</code>。</li>\n<li><strong>Function <code>rejectUnauthorized(req, res)</code>（L30–L38）</strong> — 设 <code>WWW-Authenticate</code>，401 JSON-RPC <code>-32000</code>。</li>\n<li><strong>Function <code>requireAuth</code>（L40–L43）</strong> — 未授权则 reject，否则 <code>next()</code>。</li>\n<li><strong>Function <code>wantsSse(req)</code>（L45–L47）</strong> — <code>Accept</code> 含 <code>text/event-stream</code>。</li>\n<li><strong>Function <code>sessionIdFor(req)</code>（L49–L51）</strong> — 头 <code>mcp-session-id</code> 或随机 8 字节 hex。</li>\n<li><strong>Function <code>sendJsonRpc(req, res, payload, httpStatus=200)</code>（L53–L65）</strong></li>\n<li>L54–L55：回写 <code>Mcp-Session-Id</code>。</li>\n<li>L56–L62：SSE 则 <code>event: message</code> + data 后 end。</li>\n<li>L64：否则普通 JSON。</li>\n<li><strong>Function <code>builtinPrompts()</code>（L67–L75）</strong> — 仅一项 <code>name:'connect'</code>。</li>\n<li><strong>Function <code>promptsFromCustom()</code>（L77–L85）</strong> — 内置 + <code>custom.prompts</code>（description 截 120 字）。</li>\n<li><strong>Function <code>pickProtocol(params)</code>（L86–L90）</strong> — 客户端要的版本在支持列表里就用，否则 <code>'2025-03-26'</code>。</li>\n<li><strong>Function <code>remoteToolMode(params)</code>（L92–L97）</strong> — 读 <code>params._meta.mode</code> 或 <code>_meta.webagentMode</code>；仅 <code>ask|plan|code</code>；缺省 <strong><code>'code'</code></strong>。</li>\n<li><strong>Function <code>handleRpc(req)</code>（L99–L210）</strong> — 见下方 method 分支。</li>\n<li><strong>Function <code>hostStatus()</code>（L212–L225）</strong> — GET 非 SSE 的主机摘要（含完整 instructions、transports、auth 三种）。</li>\n<li><strong>Function <code>handlePost(req, res)</code>（L227–L253）</strong></li>\n<li>L229–L235：<code>jsonrpc !== '2.0'</code> → 400、RPC <code>-32600</code>。</li>\n<li>L237–L242：<code>handleRpc</code>；method 以 <code>notifications/</code> 开头 → <strong>HTTP 204 无 body</strong>。</li>\n<li>L243–L252：catch：<code>E_UNKNOWN_CMD</code> → HTTP 404 且 rpc <code>-32601</code>；其它协议 <code>-32602</code>；否则 <code>-32603</code> 或 <code>err.rpcCode</code>。<strong>工具失败不会进这里</strong>：<code>tools/call</code> 自己 <code>return { isError:true }</code>。</li>\n<li><strong>Function <code>handleGet(req, res)</code>（L255–L268）</strong></li>\n<li>L256–L265：SSE → 先写 <code>event: endpoint</code> <code>data: /mcp</code>，每 15s <code>: ping</code>，close 清 interval。</li>\n<li>L267：否则 <code>hostStatus()</code> JSON。</li>\n</ul>\n<p> <strong><code>handleRpc</code> 的 method 分支：</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>行</th>\n<th>method</th>\n<th>做什么</th>\n</tr></thead><tbody>\n<tr>\n<td>L102–L117</td>\n<td><code>initialize</code></td>\n<td>L103 默认 client 名 <code>External-Agent</code>；L104 <code>touch</code>；L105 broadcast <code>agent_connected</code>；返回 protocol、capabilities、serverInfo、<strong><code>instructions: getInstructions()</code></strong></td>\n</tr>\n<tr>\n<td>L119–L122</td>\n<td><code>notifications/initialized</code>、<code>notifications/cancelled</code>、<code>logging/setLevel</code></td>\n<td>返回 <code>{}</code></td>\n</tr>\n<tr>\n<td>L124–L133</td>\n<td><code>ping</code></td>\n<td><code>touch incCall</code>，busy 写死 <code>false</code></td>\n</tr>\n<tr>\n<td>L135–L137</td>\n<td><code>tools/list</code></td>\n<td><code>getToolList()</code> <strong>不传 mode</strong>（列表含 Code-only 工具）</td>\n</tr>\n<tr>\n<td>L139–L165</td>\n<td><code>tools/call</code></td>\n<td>见下</td>\n</tr>\n<tr>\n<td>L167–L168</td>\n<td><code>resources/list</code></td>\n<td><code>listResources()</code></td>\n</tr>\n<tr>\n<td>L170–L175</td>\n<td><code>resources/read</code></td>\n<td>未知 uri 抛 <code>E_NOT_FOUND</code></td>\n</tr>\n<tr>\n<td>L177–L178</td>\n<td><code>prompts/list</code></td>\n<td><code>promptsFromCustom()</code></td>\n</tr>\n<tr>\n<td>L180–L205</td>\n<td><code>prompts/get</code></td>\n<td><code>connect</code> 走 bootstrap；否则 custom.prompts；没有抛 <code>E_NOT_FOUND</code></td>\n</tr>\n<tr>\n<td>L207–L208</td>\n<td>default</td>\n<td><code>E_UNKNOWN_CMD</code>（未知 <strong>method</strong>，仍是 JSON-RPC error）</td>\n</tr>\n</tbody></table></div>\n<p> <strong><code>tools/call</code> 细节（L139–L165）：</strong></p>\n<ul>\n<li>L141：无 <code>name</code> → 抛 <code>E_BAD_ARGS</code>（这才会变成 JSON-RPC error）。</li>\n<li>L142：broadcast <code>tool_call_start</code>，source <code>'Bridge-Remote'</code>。</li>\n<li>L145：<strong><code>callTool(name, toolArgs || {}, remoteToolMode(params))</code></strong> — 默认 Code；<code>_meta.mode=ask|plan</code> 时模式锁生效。</li>\n<li>L146：再 <code>clipJson</code>。</li>\n<li>L151–L154：成功 → MCP <code>content[{type:text}]</code>，<code>isError:false</code>。</li>\n<li>L155–L163：<code>catch</code> → <code>publicError</code>；<code>incFail</code>；<strong>始终</strong> <code>return { content:[{type:text, text: JSON.stringify(info)}], isError:true }</code>。未知工具名、HASH_REQUIRED、STALE_FILE 都走这条，网页 Agent 把它当工具结果而不是传输崩溃。</li>\n</ul>\n<ul>\n<li><strong>路由（L270–L273）：</strong> <code>GET/POST /</code> 与 <code>GET/POST /:secret</code> 均 <code>requireAuth</code> 后进 handleGet/handlePost。挂到 app 上后即 <code>/mcp</code> 与 <code>/mcp/:secret</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L14 <code>router</code>；L15 <code>SUPPORTED_PROTOCOL = ['2024-11-05','2025-03-26','2025-06-18']</code>。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅针对该子文件夹\">3. 执行逻辑流（仅针对该子文件夹）</h2>\n<p>数据从 HTTP 进入本文件夹之后：</p>\n<ol>\n<li><strong>OAuth 发现 / 配对（匿名，不进 <code>server.js</code>）</strong>  </li>\n</ol>\n<p> 客户端 GET <code>oauth.js</code> 的 <code>/.well-known/…</code>（L313–L321）拿元数据 → POST <code>/oauth/register</code>（L325–L333）拿到 <code>client_id</code> → 浏览器 GET <code>/oauth/authorize</code>（L335–L339，<code>ensurePairing</code>）看到配对页 HTML → 用户填工作台配对码 → POST <code>completeAuthorize</code>（L341–L349）→ 302 带回一次性 code → POST <code>/oauth/token</code> + PKCE（L351–L356）得到 Bearer。</p>\n<ol>\n<li><strong>MCP 请求进 <code>server.js</code> 路由（L270–L273）</strong>  </li>\n</ol>\n<p> <code>requireAuth</code> → <code>extractToken</code>（路径密钥 / Bearer / 头 / query）→ <code>oauth.verifyAccessToken</code>（L166 认 URL 密钥，或认 access token）。失败则 401 + <code>WWW-Authenticate</code>。</p>\n<ol>\n<li><strong>GET</strong>  </li>\n</ol>\n<ul>\n<li><code>Accept: text/event-stream</code> → SSE 通道（L255–L265）。  </li>\n<li>否则 <code>hostStatus()</code>，其中 <code>instructions</code> 来自 <code>instructions.js</code>。</li>\n</ul>\n<ol>\n<li><strong>POST JSON-RPC</strong>  </li>\n</ol>\n<p> <code>handlePost</code> 校验 <code>jsonrpc==='2.0'</code> → <code>handleRpc</code>：</p>\n<ul>\n<li><code>initialize</code>：<code>session.touch</code> + <code>instructions.getInstructions()</code>（读 customizations / profile / skills）。</li>\n<li><code>tools/list</code>：出本目录，调 <code>../tools.getToolList()</code>。</li>\n<li><code>tools/call</code>：出本目录，调 <code>../tools.callTool</code>（真正改盘）；回来用 <code>budget.clipJson</code> / <code>clipText</code>；<strong>工具失败回 MCP <code>isError:true</code> 文本</strong>（<code>publicError</code> 的 layer/code/msg/detail），不升级成 JSON-RPC <code>error</code>；全程 <code>eventBus.broadcast</code>（目录外）给工作台。</li>\n<li><code>resources/*</code>：留在 <code>resources.js</code>（只读说明书 / 状态，不写盘）。</li>\n<li><code>prompts/*</code>：<code>instructions</code> + <code>customizations</code>。</li>\n<li><code>notifications/*</code>：空对象，HTTP 204。</li>\n</ul>\n<ol>\n<li><strong>工作台卡片（不经 JSON-RPC）</strong>  </li>\n</ol>\n<p> <code>../api/routes.js</code> 调 <code>clients.listClients</code> + <code>instructions.getBootstrapPrompt</code> + <code>oauth.snapshotPairing</code>，把 hydrate 后的 <code>prompt</code> 交给用户复制。DeepSeek 卡只有一行 URL；Arena 卡是 URL + <code>CONNECT_LINE</code>。</p>\n<p><strong>本目录没有的事（避免误读）：</strong> 不 spawn cloudflared；不实现 <code>apply_patch</code>。远程 <code>tools/call</code> 默认 Code，可用 <code>params._meta.mode</code> 切 Ask/Plan。</p>\n<hr />\n<p>第一阶段本文件夹已完成。按约束暂停。</p>\n<p>请输入下一个文件夹名称（例如 <code>webagent-core/agent-host/src/tools</code>）。全部文件夹处理完后再进入第三阶段。</p>"},"tools":{"id":"tools","path":"webagent-core/agent-host/src/tools/README.md","group":"进程","title":"tools 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：index.js","id":"文件名-index.js"},{"level":3,"text":"📄 文件名：patchEngine.js","id":"文件名-patchengine.js"},{"level":3,"text":"📄 文件名：fileOps.js","id":"文件名-fileops.js"},{"level":3,"text":"📄 文件名：normalize.js","id":"文件名-normalize.js"},{"level":3,"text":"📄 文件名：readCache.js","id":"文件名-readcache.js"},{"level":3,"text":"📄 文件名：sensitive.js","id":"文件名-sensitive.js"},{"level":3,"text":"📄 文件名：executor.js","id":"文件名-executor.js"},{"level":3,"text":"📄 文件名：gitOps.js","id":"文件名-gitops.js"},{"level":3,"text":"📄 文件名：findFiles.js","id":"文件名-findfiles.js"},{"level":3,"text":"📄 文件名：skills.js","id":"文件名-skills.js"},{"level":3,"text":"📄 文件名：workspaceInfo.js","id":"文件名-workspaceinfo.js"},{"level":3,"text":"📄 文件名：progressTracker.js","id":"文件名-progresstracker.js"},{"level":3,"text":"📄 文件名：consensusEngine.js","id":"文件名-consensusengine.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"tools-模块说明书\">tools 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/tools/</code></p>\n<p>本目录是 <strong>真正改磁盘 / 跑命令</strong> 的实现。MCP（<code>../mcp/server.js</code>）和本机 Chat（<code>../agent/runChat.js</code>、<code>../api/routes.js</code>）都只通过 <code>index.js</code> 的 <code>callTool</code> 进来。目录内无 <code>.json</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 工具注册表 + 路径沙箱 + 补丁 / 读写 / grep / git / 命令执行 / 进度 / Plan 博弈文案。<code>normalize.js</code> 把网页 Agent 的别名收成正式名；<code>readCache.js</code> 记住最近一次读/写的 sha256，让 <code>apply_patch</code> 可以不带 <code>expectedHash</code>。</li>\n<li><strong>依赖的兄弟模块：</strong></li>\n<li><code>../config</code>：工作区根。</li>\n<li><code>../utils/eventBus</code>、<code>../utils/diff</code>：广播与 unified diff。</li>\n<li><code>../mcp/errors</code>、<code>../mcp/budget</code>、<code>../mcp/session</code>：错误分类、截断、心跳快照。</li>\n<li><code>../models/memory</code>、<code>../models/customizations</code>、<code>../models/profile</code>：记忆与工作区画像。</li>\n<li><strong>谁调用本模块：</strong> <code>../mcp/server.js</code>（远程，<code>callTool</code> 第三参默认 <code>'code'</code>，或 <code>params._meta.mode</code>）、<code>../agent/*</code>、<code>../api/routes.js</code>（传 Ask/Plan/Code）。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-index.js\">📄 文件名：<code>index.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 登记约 25 个工具名，做模式锁和危险命令闸，然后把调用派到本目录其它文件。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>tool(def)</code>（L17–L19）</strong> — 输入工具定义对象，原样返回（无变换）。</li>\n<li><strong>Function <code>pingHost()</code>（L21–L23）</strong> — 无参。返回 <code>{ ok, ts, ...snapshot() }</code>。</li>\n<li><strong>Function <code>getLogs({ maxLines=50 })</code>（L25–L28）</strong> — <code>maxLines</code> clamp 到 1–200；返回 <code>{ logs, count }</code>。</li>\n<li><strong>Function <code>getCapabilities()</code>（L30–L35）</strong> — 工具名+描述 + session snapshot。</li>\n<li><strong>Function <code>getTaskStatus()</code>（L37–L45）</strong> — 展开 <code>getTaskState()</code>；<code>status==='in_progress'</code> 时 <code>suggestedWaitMs=2000</code>，<code>etaSeconds = max(1, round((100-progress)/10))</code>，否则两者为 0。</li>\n<li><strong>Const <code>TOOLS</code>（L50–L394）</strong> — 每项含 <code>name</code> / <code>aliases</code> / <code>description</code> / <code>mode</code> / <code>inputSchema</code> / <code>handler</code>。名称行号（<code>name:</code> 所在行）：</li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>行</th>\n<th>name</th>\n<th>mode</th>\n<th>handler</th>\n</tr></thead><tbody>\n<tr>\n<td>L52</td>\n<td>ping</td>\n<td>ask,plan,code</td>\n<td>pingHost</td>\n</tr>\n<tr>\n<td>L60</td>\n<td>workspace_info</td>\n<td>同上</td>\n<td>workspaceInfo</td>\n</tr>\n<tr>\n<td>L68</td>\n<td>get_capabilities</td>\n<td>同上</td>\n<td>getCapabilities</td>\n</tr>\n<tr>\n<td>L76</td>\n<td>get_logs</td>\n<td>同上</td>\n<td>getLogs</td>\n</tr>\n<tr>\n<td>L87</td>\n<td>get_task_status</td>\n<td>同上</td>\n<td>getTaskStatus</td>\n</tr>\n<tr>\n<td>L95</td>\n<td>remember</td>\n<td>同上</td>\n<td>remember</td>\n</tr>\n<tr>\n<td>L107</td>\n<td>recall</td>\n<td>同上</td>\n<td>recall</td>\n</tr>\n<tr>\n<td>L118</td>\n<td>list_directory（alias list_dir）</td>\n<td>同上</td>\n<td>listDir</td>\n</tr>\n<tr>\n<td>L133</td>\n<td>find_files</td>\n<td>同上</td>\n<td>findFiles</td>\n</tr>\n<tr>\n<td>L148</td>\n<td>search_files（alias grep_search）</td>\n<td>同上</td>\n<td>grepSearch</td>\n</tr>\n<tr>\n<td>L167</td>\n<td>read_files（alias read_file）</td>\n<td>同上</td>\n<td>readFiles</td>\n</tr>\n<tr>\n<td>L183</td>\n<td>git_status</td>\n<td>同上</td>\n<td>gitStatus</td>\n</tr>\n<tr>\n<td>L191</td>\n<td>git_diff</td>\n<td>同上</td>\n<td>gitDiff</td>\n</tr>\n<tr>\n<td>L206</td>\n<td>load_skill</td>\n<td>同上</td>\n<td>loadSkill</td>\n</tr>\n<tr>\n<td>L217</td>\n<td>apply_patch</td>\n<td><strong>code</strong></td>\n<td>applyPatch</td>\n</tr>\n<tr>\n<td>L234</td>\n<td>write_file</td>\n<td><strong>code</strong></td>\n<td>writeFile</td>\n</tr>\n<tr>\n<td>L254</td>\n<td>delete_file</td>\n<td><strong>code</strong></td>\n<td>deleteFile</td>\n</tr>\n<tr>\n<td>L269</td>\n<td>rename_file（alias move_file）</td>\n<td><strong>code</strong></td>\n<td>renameFile</td>\n</tr>\n<tr>\n<td>L284</td>\n<td>run_command（alias execute_command）</td>\n<td><strong>code</strong></td>\n<td>executeCommand</td>\n</tr>\n<tr>\n<td>L301</td>\n<td>start_command</td>\n<td><strong>code</strong></td>\n<td>startCommand</td>\n</tr>\n<tr>\n<td>L318</td>\n<td>get_command_output</td>\n<td>ask,plan,code</td>\n<td>getCommandOutput</td>\n</tr>\n<tr>\n<td>L333</td>\n<td>cancel_command</td>\n<td><strong>code</strong></td>\n<td>cancelCommand</td>\n</tr>\n<tr>\n<td>L345</td>\n<td>wait</td>\n<td>ask,plan,code</td>\n<td>wait</td>\n</tr>\n<tr>\n<td>L356</td>\n<td>report_progress</td>\n<td><strong>plan,code</strong></td>\n<td>reportProgress</td>\n</tr>\n<tr>\n<td>L372</td>\n<td>set_todos</td>\n<td>ask,plan,code</td>\n<td>setTodos</td>\n</tr>\n</tbody></table></div>\n<ul>\n<li><strong>L396–L402</strong> — 把 name 与 aliases 写入 <code>toolRegistry</code> Map。</li>\n<li><strong>Function <code>getToolList(currentMode=null)</code>（L406–L410）</strong> — mode 假则全部；真则 <code>t.mode.includes(currentMode)</code>。映射为 <code>{ name, description, inputSchema }</code>（不含 handler）。</li>\n<li><strong>Function <code>callTool(name, args={}, currentMode=null)</code>（L412–L444）</strong></li>\n<li>L413：<code>resolveToolName(name)</code>（<code>normalize.js</code>：<code>bash</code>→<code>run_command</code>、<code>cat</code>→<code>read_files</code> 等）。</li>\n<li>L414：registry 先查 resolved 再查原名。</li>\n<li>L415–L421：未知名 → <code>ProtocolError E_UNKNOWN_CMD</code>，消息含 Available 列表，<code>detail.retryHint</code> 提示可用别名。</li>\n<li>L422–L427：<code>currentMode</code> 真且不在该工具 mode 列表 → <code>E_BAD_ARGS</code>（Ask/Plan 只读文案）。远程 MCP 默认传入 <code>'code'</code>（见 <code>mcp/server.remoteToolMode</code>）；本机 Chat 传入 UI 模式。</li>\n<li>L428：<code>normalizeToolArgs(toolDef.name, args)</code>（snake_case、<code>path</code>→<code>filePath</code>、<code>&quot;true&quot;</code>→布尔）。</li>\n<li>L429–L437：工具名为 <code>run_command</code> 或 <code>start_command</code> 且命令匹配 <code>DANGEROUS_RE</code> 且无 <code>confirm_dangerous</code> → <code>E_BAD_ARGS</code> + retryHint。闸包括 <code>rm -rf</code>、<code>git push</code>、<code>curl … | sh</code>、<code>iex</code> / <code>iwr</code>、关机格式化等。</li>\n<li>L438：<code>await handler(input)</code>。</li>\n<li>L439–L442：<code>result.isTimeout</code> 则打 <code>E_TIMEOUT</code>、<code>suggestedWaitMs=0</code>。</li>\n<li>L443：<code>clipJson(result)</code> 后返回。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L47–L48 <code>DANGEROUS_RE</code>（i 标志）匹配 <code>rm -rf</code> / <code>mkfs</code> / <code>dd if=</code> / <code>shutdown</code> / <code>reboot</code> / <code>halt</code> / <code>poweroff</code> / <strong><code>git push</code></strong> / <code>git reset --hard</code> / <code>git checkout --</code> / <code>git clean -f</code> / <code>format x:</code> / <code>del /s</code> / <code>rd /s</code> / <code>Remove-Item -Recurse</code> / <code>drop database</code> / <code>Invoke-Expression</code> / <code>iex</code> / <code>iwr</code> / <code>Start-Process</code> / <strong><code>curl|wget</code> 管道进 shell</strong> / <code>powershell -enc</code> / <code>certutil -urlcache</code> / <code>bitsadmin</code> / <code>reg add</code> / <code>net user</code> / <code>schtasks</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-patchengine.js\">📄 文件名：<code>patchEngine.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 工作区路径沙箱、sha256、SEARCH/REPLACE（或 unified diff / 整文件覆盖）写盘。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>computeHash(content)</code>（L11–L13）</strong> — sha256 hex，utf8。</li>\n<li><strong>Function <code>toPosixRel(p)</code>（L15–L17）</strong> — 反斜杠改 <code>/</code>。</li>\n<li><strong>Function <code>existingAncestor</code> / <code>realPathOrJoin</code> / <code>isInsideWorkspace</code>（L19–L49）</strong> — 沿父目录找到已存在的节点再 <code>realpathSync</code>，挡住 <strong>symlink / junction</strong> 指到工作区外。</li>\n<li><strong>Function <code>resolveSafePath(relPath)</code>（L51–L65）</strong></li>\n<li>L53–L55：相对 <code>config.workspaceRoot</code> 解析。</li>\n<li>L57–L59：posix 为 <code>..</code>、以 <code>../</code> 开头、或 <code>rel</code> 绝对路径 → 抛 Security error。</li>\n<li>L60–L62：<code>isInsideWorkspace</code> 为假（真实路径跑出工作区）同样抛。</li>\n<li>L63：posix 非空且不是 <code>.</code> → <code>assertNotSensitive</code>。</li>\n<li>L64：返回<strong>逻辑</strong>绝对路径（写新文件用；读/写会再被真实路径检查拦住）。</li>\n<li><strong>Function <code>parseSearchReplaceBlocks(patchText)</code>（L32–L43）</strong> — 正则 <code>&lt;&lt;&lt;&lt;&lt; SEARCH</code> … <code>=====</code> … <code>&gt;&gt;&gt;&gt;&gt; REPLACE</code>，收集 <code>{ search, replace }</code>。</li>\n<li><strong>Function <code>applyPatch({ filePath, patch, expectedHash=null, dryRun=false })</code>（L80–L212）</strong></li>\n<li>L46：<code>resolveSafePath</code>。</li>\n<li><strong>文件不存在 L48–L81：</strong> 解析 blocks；若第一块 search trim 为空则用 replace 当新内容，否则整段 <code>patch</code>。<code>dryRun</code> 只返回成功。否则 mkdir + write；broadcast <code>file_patched</code>；<code>rememberHash</code>；返回 <code>isNewFile</code> + newHash。</li>\n<li><strong>文件存在 L83–L176：</strong> 读全文算 <code>currentHash</code>。L85–L88：没传 <code>expectedHash</code> 则用 <code>recalledHash(filePath)</code>（上次 <code>read_files</code> / 成功补丁记下的 sha256）。</li>\n<li>L90–L100：仍无 hash 且非 <code>dryRun</code> → <code>ProtocolError E_BAD_ARGS</code>，消息 <code>HASH_REQUIRED</code>，<code>detail.currentHash</code> + <code>retryHint</code>。</li>\n<li>L102–L108：传了 expectedHash 且既不等于也不当前缀 → <code>ExecutionError E_STALE_FILE</code>，detail 含 currentHash。</li>\n<li>L113–L130 有 blocks：统一 <code>\\n</code>；<code>includes(search)</code> 则 replace 第一处；否则 trim 后再试；再失败抛 Patch conflict。</li>\n<li>L131–L141 无 blocks：以 <code>--- </code> 开头且含 <code>@@</code> → <code>jsdiff.applyPatch</code>，<code>false</code> 则抛；否则整段覆盖。</li>\n<li>L146–L153 <code>dryRun</code> 返回 diff 不写盘。</li>\n<li>L155–L175：写 <code>.tmp.${Date.now()}</code> 再 <code>renameSync</code>；<code>rememberHash</code>；broadcast；返回 newHash + diff。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-fileops.js\">📄 文件名：<code>fileOps.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 读/写/删/改名/列目录/grep。全部先 <code>resolveSafePath</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>readFiles</code>（L8–L25）</strong> — 合并 <code>paths[]</code> 与 <code>filePath</code>。空 → 抛。恰好 1 个直接 <code>readFile</code>。多个则逐个 try，失败变成 <code>{ filePath, error }</code>。</li>\n<li><strong>Function <code>readFile</code>（L27–L60）</strong> — 不存在抛；目录抛去用 list_dir。全文 hash；默认 offset=1 limit=400；内容格式 <code>行号: 文本</code>。broadcast <code>file_read</code>。</li>\n<li><strong>Function <code>deleteFile</code>（L62–L84）</strong> — 无 path 抛。相对路径空或 <code>.</code> 拒绝删根。不存在抛。非空目录抛。目录 <code>rmdirSync</code>，文件 <code>unlinkSync</code>。broadcast <code>file_deleted</code>。</li>\n<li><strong>Function <code>renameFile</code>（L86–L100）</strong> — <code>from||filePath</code> 与 <code>to||dest</code> 缺一抛。源不存在 / 目标已存在抛。mkdir 父目录后 rename。broadcast <code>file_renamed</code>。</li>\n<li><strong>Function <code>writeFile</code>（L114–L164）</strong> — <code>resolveSafePath</code>（含敏感拦截）。已存在则要 <code>confirm_overwrite</code> 或 remembered/expected hash；hash 不符 → <code>E_STALE_FILE</code>。写 <code>.tmp.${Date.now()}</code> 再 <code>renameSync</code>。broadcast <code>file_written</code>；<code>rememberHash</code>。</li>\n<li><strong>Function <code>listDir</code>（L166–L200）</strong> — 内嵌 <code>scan</code>：depth 超 <code>maxDepth</code> 返回 []；真实路径在工作区外或 <strong>符号链接</strong> skip；<code>isHidden</code> skip；目录仅 <code>recursive &amp;&amp; currentDepth &lt; maxDepth</code> 才扫 children。</li>\n<li><strong>Function <code>grepSearch</code>（L202–L259）</strong> — 编正则（非 regex 则转义）；非法正则抛。目录递归时同样跳过工作区外与符号链接。分页 <code>limit</code> 1–100，<code>nextCursor</code> 或 null。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-normalize.js\">📄 文件名：<code>normalize.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 网页 Agent 常发的别名参数 / 工具名，收成 handler 认识的字段。只被 <code>index.js</code> <code>callTool</code> 调用。</li>\n<li><strong>核心类/函数清单：</strong></li>\n<li><strong>Const <code>TOOL_NAME_ALIASES</code>（L1–L19）</strong> — <code>bash|shell|exec|execute</code>→<code>run_command</code>；<code>str_replace|search_replace|replace_in_file|edit_file</code>→<code>apply_patch</code>；<code>cat|read</code>→<code>read_files</code>；<code>ls</code>→<code>list_directory</code>；<code>grep</code>→<code>search_files</code>；<code>glob</code>→<code>find_files</code>；<code>write|create_file</code>→<code>write_file</code>；<code>rm</code>→<code>delete_file</code>；<code>mv</code>→<code>rename_file</code>。</li>\n<li><strong>Function <code>isTruthy(v)</code>（L21–L25）</strong> — <code>true</code> / <code>1</code> / 字符串 <code>true|yes|1</code>（i）为真。</li>\n<li><strong>Function <code>firstDefined(obj, keys)</code>（L27–L32）</strong> — 第一个非 null 且非 <code>''</code> 的键。</li>\n<li><strong>Function <code>resolveToolName(name)</code>（L34–L41）</strong> — trim；查表（原样与小写）；否则原名。</li>\n<li><strong>Function <code>normalizeToolArgs(toolName, args)</code>（L43–L145）</strong> — 浅拷贝；先把 snake_case 填到 camelCase；再按工具名把 <code>path</code>/<code>file</code>/<code>cmd</code>/<code>pattern</code> 等收到 <code>filePath</code>/<code>dirPath</code>/<code>command</code>/<code>query</code>/<code>patch</code>；写/删/危险命令的 confirm 走 <code>isTruthy</code>。</li>\n</ul>\n<h3 id=\"文件名-readcache.js\">📄 文件名：<code>readCache.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> posix 路径 → 最近一次读/补丁/写入的 sha256。落盘 <code>.webagent/read-hashes.json</code>（最多 400 条）。<code>POST /api/bridge/reset-round</code> 会 <code>resetHashes()</code>（清 Map 并删文件）。</li>\n<li><strong>核心类/函数清单：</strong></li>\n<li><strong>Function <code>norm(filePath)</code>（L9–L11）</strong> — 反斜杠改 <code>/</code>，去掉前导 <code>./</code>。</li>\n<li><strong>Function <code>hashFile()</code>（L13–L15）</strong> — <code>&lt;workspace&gt;/.webagent/read-hashes.json</code>。</li>\n<li><strong>Function <code>ensureLoaded()</code>（L17–L29）</strong> — 按 <code>workspaceRoot</code> 懒加载；坏 JSON / 缺文件当空表。</li>\n<li><strong>Function <code>persist()</code>（L31–L38）</strong> — mkdir 后写 JSON；失败 catch 空。</li>\n<li><strong>Function <code>rememberHash(filePath, hash)</code>（L40–L51）</strong> — 空路径或空 hash return；更新后超过 400 删最老；<code>persist</code>。</li>\n<li><strong>Function <code>recalledHash(filePath)</code>（L53–L56）</strong> — 没有则 <code>null</code>。</li>\n<li><strong>Function <code>forgetHash(filePath)</code>（L58–L62）</strong> / <strong><code>resetHashes()</code>（L64–L68）</strong> — 删一条并 persist / 清空并 <code>unlinkSync</code>。</li>\n</ul>\n<h3 id=\"文件名-sensitive.js\">📄 文件名：<code>sensitive.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 敏感路径与噪声目录过滤。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>toPosix</code>（L58–L60）</strong> — <code>/</code> 化，去前导 <code>./</code>。</li>\n<li><strong>Function <code>globMatch</code>（L62–L79）</strong> — pattern 以 <code>/</code> 结尾匹配目录前缀；否则 <code>*</code>/<code>**</code> 编正则，测整路径或 basename。空或 <code>.</code> → false。</li>\n<li><strong>Function <code>loadCustomPatterns</code>（L81–L93）</strong> — 读工作区 <code>.webagentignore</code>；不存在或 catch → <code>[]</code>；空行与 <code>#</code> 丢掉。</li>\n<li><strong>Function <code>isSensitive</code>（L95–L110）</strong> — basename 在例外列表 → false；内置 patterns 命中 true；自定义以 <code>!</code> 开头命中则 false（放行）。</li>\n<li><strong>Function <code>isNoise</code>（L112–L117）</strong> — 路径任一段在 <code>NOISE_NAMES</code>。</li>\n<li><strong>Function <code>isHidden</code>（L119–L121）</strong> — sensitive 或 noise。</li>\n<li><strong>Function <code>assertNotSensitive</code>（L123–L129）</strong> — 敏感则抛 <code>ACCESS_DENIED_SENSITIVE_FILE</code>，<code>code='E_FORBIDDEN'</code>。</li>\n</ul>\n<ul>\n<li><strong>关键常量：</strong> L5–L35 <code>SENSITIVE_PATTERNS</code>（<code>.env</code>、密钥、<code>.webagent/config.json</code> 等）；L37 例外 <code>.env.example</code> 等；L39–L56 <code>NOISE_NAMES</code>（<code>node_modules</code>、<code>.git</code>、<code>dist</code>…）。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-executor.js\">📄 文件名：<code>executor.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 在工作区跑 shell。Windows 走 PowerShell，其它 bash。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>killChild</code>（L12–L26）</strong> — 无 pid return。win32 <code>taskkill /pid /t /f</code>。非 Windows 先 <code>process.kill(-pid)</code> 杀<strong>进程组</strong>，失败再 <code>child.kill</code>。</li>\n<li><strong>Function <code>workingDirFrom</code>（L28–L34）</strong> — 走 <code>resolveSafePath</code>（含真实路径），逃出工作区抛 outside workspace。</li>\n<li><strong>Function <code>publicRecord</code>（L31–L48）</strong> — stdout/stderr 截尾；running 时带 <code>suggestedWaitMs</code> 与 poll hint。</li>\n<li><strong>Function <code>startProcess</code>（L55–L140）</strong> — <code>execId</code> 自增；timeout 至少 1s；broadcast <code>command_started</code>；spawn PowerShell 或 bash；非 Windows <code>detached:true</code> 以便杀进程组；超时 kill 再 2s force；stdout/stderr 环形 200KB；error reject；close 时若不是 <code>cancelled</code> 则 status <code>timeout</code> 或 <code>done</code>。返回 <code>{ rec, done }</code>。</li>\n<li><strong>Function <code>executeCommand</code>（L134–L137）</strong> — 返回 <code>done</code>（等到结束）。</li>\n<li><strong>Function <code>startCommand</code>（L139–L152）</strong> — 不等待；<code>done.catch</code> 标 error；立即返回 execId + running。</li>\n<li><strong>Function <code>getCommandOutput</code>（L154–L161）</strong> — id = execId 或 commandId 或最新序号；没有 rec → found false。</li>\n<li><strong>Function <code>cancelCommand</code>（L163–L174）</strong> — 非 running → cancelled false；否则 force kill。</li>\n<li><strong>Function <code>sendCommandInput</code>（L176–L183）</strong> — <strong>恒定</strong> <code>{ ok:false }</code>，无 PTY。</li>\n<li><strong>Function <code>wait</code>（L185–L189）</strong> — ms clamp 0–15000。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L6–L9 <code>commandSequence</code>、<code>commandStore</code>、<code>children</code>、<code>MAX_CAPTURE=200*1024</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-gitops.js\">📄 文件名：<code>gitOps.js</code></h3>\n<ul>\n<li><strong>Function <code>notGitResult(extra={})</code>（L7–L20）</strong> — <code>{ ok:true, available:false, git:false, branch:null, dirty:false, files:[], summary:'', truncated:false, hint:… }</code> 再 spread extra。hint 写明不要擅自 <code>git init</code>。</li>\n<li><strong>Function <code>git</code>（L22–L57）</strong> — <code>spawnSync git -c color.ui=never</code>，cwd 工作区，timeout 默认 8s。启动失败：ENOENT/<code>not found</code> → <code>Error</code> <code>code='GIT_UNAVAILABLE'</code>，其它 <code>E_INTERNAL</code>。非 0：status 127 / not a git repository / command not found → <code>GIT_UNAVAILABLE</code>；否则 <code>E_INTERNAL</code> 截 800 字。</li>\n<li><strong>Function <code>gitStatus</code>（L59–L88）</strong> — try porcelain v1 -b；成功 <code>{ ok, available:true, git:true, branch, dirty, summary, files≤80, truncated }</code>。catch 仅 <code>GIT_UNAVAILABLE</code> → <code>notGitResult()</code>，其它再抛。</li>\n<li><strong>Function <code>gitDiff</code>（L90–L118）</strong> — staged → <code>--cached</code>；stat → <code>--stat</code>；有 filePath 则 resolveSafePath 后相对路径。输出 cap：stat 80 行否则 200。同样把 <code>GIT_UNAVAILABLE</code> 收成 <code>notGitResult</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-findfiles.js\">📄 文件名：<code>findFiles.js</code></h3>\n<ul>\n<li><strong>Function <code>globToRegExp</code>（L7–L16）</strong> — 默认 <code>**/*</code>；<code>**</code>→<code>.*</code>，<code>*</code>→<code>[^/]*</code>。</li>\n<li><strong>Function <code>findFiles</code>（L18–L54）</strong> — 起点不存在抛。内嵌 <code>walk</code>：readdir 失败 return；hidden skip；满 <code>maxResults</code>（默认 40）停止。<code>glob==='**/*'</code> 时文件都收。起点是文件则只 push 自己。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-skills.js\">📄 文件名：<code>skills.js</code></h3>\n<ul>\n<li><strong>Function <code>skillRoots</code>（L5–L10）</strong> — <code>.webagent/skills</code> 与工作区 <code>skills/</code>。</li>\n<li><strong>Function <code>listSkills</code>（L12–L29）</strong> — 必须是目录且有 <code>SKILL.md</code>；preview 前 240 字。</li>\n<li><strong>Function <code>loadSkill</code>（L31–L55）</strong> — 无 name 返回列表+hint。找不到 <code>{ found:false, available }</code>。找到读最多 8000 字。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-workspaceinfo.js\">📄 文件名：<code>workspaceInfo.js</code></h3>\n<ul>\n<li><strong>Function <code>workspaceInfo</code>（L10–L51）</strong> — loadCustom + resolve env/stack；try gitStatus，catch 记 error；try listDir 顶层 maxDepth 1；try 读 package.json <code>name</code>。返回 root、packageName、git 摘要、skills 名、topLevel、hint 不要 dump 整树。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-progresstracker.js\">📄 文件名：<code>progressTracker.js</code></h3>\n<ul>\n<li><strong>模块状态 L3–L10</strong> <code>currentTaskState</code> 初始 idle。</li>\n<li><strong>Function <code>reportProgress</code>（L12–L27）</strong> — percentage≥100 → completed 否则 in_progress；progress clamp 0–100；broadcast <code>progress_updated</code>。</li>\n<li><strong>Function <code>setTodos</code>（L29–L46）</strong> — 补 id/title/status；broadcast <code>todos_updated</code>。</li>\n<li><strong>Function <code>getTaskState</code>（L48–L50）</strong> / <strong><code>resetTaskState</code>（L52–L63）</strong> — reset 回到 idle 并 broadcast。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-consensusengine.js\">📄 文件名：<code>consensusEngine.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> Plan 模式三分支文案。<strong>不请求任何 LLM HTTP。</strong> <code>consensusReached</code> / <code>agreementRate:'97%'</code> / <code>disagreements:[]</code> 写死。</li>\n<li><strong>Function <code>sleep</code>（L3–L5）</strong> / <strong><code>clip</code>（L7–L10）</strong> — 截到 n（默认 900）。</li>\n<li><strong>Function <code>runMultiModelConsensus</code>（L16–L102）</strong> — 从 facts 取 files/readme/pkg/test；broadcast <code>consensus_started</code>；sleep 200 三次构造 planA/B/C（架构/安全/编码），有 emit 则发 status/branch；sleep 180；拼 unifiedActionPlan 三条 pending；broadcast <code>consensus_finished</code>。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>调用方 <code>callTool(name, args, mode?)</code>（<code>index.js</code> L412）。</li>\n<li><code>resolveToolName</code> → 查 registry → 可选模式锁 → <code>normalizeToolArgs</code> → 可选危险命令闸。</li>\n<li>handler 进入具体文件：读走 <code>fileOps</code>/<code>findFiles</code>/<code>gitOps</code>/<code>skills</code>/<code>workspaceInfo</code>/<code>memory</code>；写走 <code>patchEngine</code>/<code>fileOps.writeFile</code>；命令走 <code>executor</code>。读/补丁成功会 <code>rememberHash</code>。</li>\n<li>所有写路径先 <code>resolveSafePath</code> → <code>assertNotSensitive</code>。</li>\n<li>结果经 <code>clipJson</code> 返回；副作用经 <code>eventBus.broadcast</code> 到工作台。</li>\n<li>远程 MCP 不传 mode，锁不生效；本机 Chat 传 ask/plan/code。</li>\n<li>不是 git 仓库时 <code>git_status</code>/<code>git_diff</code> 返回 <code>available:false</code>，不抛。</li>\n</ol>"},"agent":{"id":"agent","path":"webagent-core/agent-host/src/agent/README.md","group":"进程","title":"agent 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：runChat.js","id":"文件名-runchat.js"},{"level":3,"text":"📄 文件名：openai.js","id":"文件名-openai.js"},{"level":3,"text":"📄 文件名：providers.js","id":"文件名-providers.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"agent-模块说明书\">agent 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/agent/</code></p>\n<p>本目录实现 <strong>本机 Chat</strong>（<code>POST /api/chat</code>）。网页 Agent 走 MCP，<strong>不进入本目录</strong>。无 <code>.json</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 有 API Key 时跑 OpenAI 兼容工具循环；没有 Key 时跑内置探索（搜-读-可选补丁-测）。</li>\n<li><strong>依赖：</strong> <code>../tools</code>（<code>callTool</code>/<code>getToolList</code>/<code>runMultiModelConsensus</code>）、<code>../models/store</code>、<code>../models/customizations</code>、<code>../models/profile</code>、<code>../tools/skills</code>、<code>../config</code>。</li>\n<li><strong>谁调用：</strong> 仅 <code>../api/routes.js</code> 的 <code>POST /chat</code>（以及测试）。VS Code 插件也打同一条 <code>/api/chat</code>，因此间接经过本目录。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-runchat.js\">📄 文件名：<code>runChat.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> Chat 入口。决定 builtin vs OpenAI；内置路径实现 Ask/Plan/Code。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>flattenDir</code>（L10–L16）</strong> — 递归摊平 <code>items</code>（含 children）到数组。</li>\n<li><strong>Function <code>countFiles</code>（L18–L20）</strong> — 其中 <code>type==='file'</code> 的个数。</li>\n<li><strong>Function <code>toolLabel</code>（L22–L52）</strong> — 给 UI 的短标签。失败时 list/read 给固定英文。成功时按工具名拼 Explored / Found N / Read path / command / Patched。</li>\n<li><strong>Function <code>timedTool</code>（L54–L84）</strong></li>\n<li>输入：<code>emit</code>、<code>mode</code>、工具名、args。</li>\n<li>try <code>callTool</code> 成功 emit <code>tool</code> ok true；catch emit ok false，<strong>不把异常抛出</strong>。返回 <code>{ ok, result|error, durationMs }</code>。</li>\n<li><strong>Function <code>keywordsFrom</code>（L86–L97）</strong> — 按空白与中文标点切词，去掉停用词，长度≥2，最多 6 个。</li>\n<li><strong>Function <code>pickExisting</code>（L99–L107）</strong> — 工作区存在且是文件的相对路径。</li>\n<li><strong>Function <code>detectTestCommand</code>（L109–L133）</strong></li>\n<li>L114–L115：先用 <code>resolveTechStack(loadCustom()).testCommand</code>。</li>\n<li>L117–L124：有 package.json <code>scripts.test</code> → <code>{ cmd:'npm test', kind:'npm' }</code>（<strong>写死 npm</strong>）。</li>\n<li>L125–L128：pytest / cargo / go 清单文件。</li>\n<li>L129–L130：存在 <code>tests/</code> 目录 → <code>{ cmd:'npm test', kind:'guess' }</code>。</li>\n<li>L131：都没有 → <code>null</code>。</li>\n<li><strong>Function <code>extractPatch</code>（L135–L138）</strong> — 第一段 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; SEARCH</code>…<code>&gt;&gt;&gt;&gt;&gt;&gt;&gt; REPLACE</code>，没有则 null。</li>\n<li><strong>Function <code>extractWriteIntent</code>（L140–L145）</strong> — 同时有代码围栏和「写入|创建|write|create + 带扩展名路径」才返回 <code>{ filePath, content }</code>。</li>\n<li><strong>Function <code>clip</code>（L147–L150）</strong> / <strong><code>stripLineNumbers</code>（L152–L157）</strong> — 去掉 <code>^\\d+:\\s?</code>。</li>\n<li><strong>Function <code>explore</code>（L159–L224）</strong> — <strong>只读</strong>：list_directory maxDepth 3 → git_status → find_files max 100 → 有 keyword 则 search_files（前 3 词 <code>|</code> 拼接、isRegex）→ 读 README/package 等最多 6 个。填 facts.files/readme/pkg/testCmd。不写文件。</li>\n<li><strong>Function <code>summarizeAsk</code>（L226–L246）</strong> — markdown 摘要，声明只读 Ask。</li>\n<li><strong>Function <code>runBuiltin</code>（L248–L375）</strong></li>\n<li>L249–L250：mode 默认 <code>ask</code>。</li>\n<li>L252–L260：set_todos 三条。</li>\n<li>L262–L263：<code>facts = explore(...)</code>。</li>\n<li>L265–L277：更新 todos。</li>\n<li>L279–L311 <strong><code>mode==='plan'</code>：</strong> <code>multiModel.enabled === false</code> → emit 未开博弈文本并 <strong>return</strong>；否则 <code>runMultiModelConsensus</code>，写 todos，emit consensus + message，return。</li>\n<li>L313–L325 <strong><code>mode==='ask'</code>：</strong> todos completed，emit summarizeAsk，return。</li>\n<li>L327 起视为 <strong>code</strong>：有 writeIntent → write_file；有 patch → 从消息匹配文件或 <code>facts.files[0]</code>，read_files 取 hash 再 apply_patch；detectTestCommand 有则 run_command timeout 60；最后 emit 摘要（写明内置没有大模型）。</li>\n<li><strong>Function <code>runChat</code>（L377–L385）</strong> — <code>emit</code> 可以是第二参，也可以是 <code>payload.emit</code>（HTTP <code>POST /api/chat</code> 把函数放进对象里）。<code>store.load()</code> 找 active 模型；<strong>若</strong> <code>apiKey &amp;&amp; baseUrl &amp;&amp; modelId</code> 都真 → <code>runOpenAI({ ...payload, emit: send })</code>；<strong>否则</strong> <code>runBuiltin(payload, send)</code>。两种调用都要把 <code>send</code> 传下去，否则工作台收不到 tool/message。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L8 <code>SKIP_DIRS</code> = node_modules/.git/.cache/dist/build/.local/bin。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-openai.js\">📄 文件名：<code>openai.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> OpenAI 兼容 <code>/chat/completions</code> 工具循环，最多 10 步。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>toolLabel</code>（L7–L20）</strong> — 与 runChat 类似的短标签。</li>\n<li><strong>Function <code>systemPrompt(mode)</code>（L22–L55）</strong></li>\n<li>L23–L26：code 允许 patch/命令；否则 READ-ONLY。</li>\n<li>L30–L34：follow-user / en / 默认中文。</li>\n<li>L35–L53：拼工作区根、循环规则、Windows PowerShell 提示、plan 不改仓库、custom.instructions、<code>formatWorkspaceContext</code>。</li>\n<li><strong>Function <code>runOpenAI({ mode, message, history=[], emit, model })</code>（L57–L144）</strong></li>\n<li>L58–L59：baseUrl 去尾 <code>/</code>，空则抛。</li>\n<li>L60–L68：<code>getToolList(mode)</code> 转 function tools（Ask 列表无 apply_patch）。</li>\n<li>L70–L77：system + history 中 user/assistant 且有 content 的最后 12 条 + 当前 user。</li>\n<li>L79–L141：最多 10 轮 POST <code>${base}/chat/completions</code>，<code>tool_choice:'auto'</code>，<code>temperature:0.2</code>。</li>\n<li>L91–L93：<code>!resp.ok</code> 抛 HTTP + 正文前 240。</li>\n<li>L96–L98：JSON 失败抛。</li>\n<li>L100：无 message 抛。</li>\n<li>L103–L133：有 tool_calls 最多 8 个；arguments parse 失败当 <code>{}</code>；try <code>callTool(name, args, mode)</code>，结果 <code>JSON.stringify.slice(0,12000)</code> 作为 role tool；catch 则 <code>ERROR: …</code>；然后 <code>continue</code>。</li>\n<li>L136–L137：无 tool_calls → emit message（空则「（无文本输出）」）并 return。</li>\n<li>L143：10 轮用尽 emit「已达到最大工具轮次。」</li>\n</ul>\n<hr />\n<h3 id=\"文件名-providers.js\">📄 文件名：<code>providers.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 探测远程 <code>/models</code>，给工作台 Add API 用。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>normalizeBase</code>（L1–L6）</strong> — trim，去尾 <code>/</code>，再去掉尾部 <code>/chat/completions</code>。</li>\n<li><strong>Function <code>guessCaps</code>（L8–L14）</strong> — 恒含 <code>'工具'</code>；vision|image|gpt-4o|flash|pro 加视觉；video 加视频。</li>\n<li><strong>Function <code>guessContext</code>（L16–L21）</strong> — video|image → 1.3M；mini|haiku|lite → 128K；否则 1.3M。</li>\n<li><strong>Function <code>listRemoteModels</code>（L23–L63）</strong></li>\n<li>L24–L26：无 base / 无 key 抛中文错误。</li>\n<li>L27–L35：GET <code>/models</code> Bearer；<code>!ok</code> 抛 HTTP + 正文前 200。</li>\n<li>L37–L41：非 JSON 抛。</li>\n<li>L42–L43：<code>data.data</code> 数组，否则 <code>data</code> 是数组，否则 <code>[]</code>；空则抛。</li>\n<li>L44–L50：hostname 去 <code>api.</code> 取第一段当 group。</li>\n<li>L51–L62：映射 id/name/group/contextSize/caps/pricing。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li><code>routes.js</code> <code>POST /chat</code> 打开 NDJSON，调用 <code>runChat({ mode, message, history, emit })</code>（emit 在对象里）。</li>\n<li><code>runChat</code> 取出 <code>send = 第二参或 payload.emit</code>，看 store 里当前模型是否同时有 Key、Endpoint、modelId。</li>\n<li>有 → <code>openai.runOpenAI</code>：systemPrompt（含 mode 锁）→ 最多 10 次 completions → 每次 tool_calls 进 <code>callTool(..., mode)</code>。</li>\n<li>无 → <code>runBuiltin</code>：explore 只读 → Ask 摘要 / Plan 进 consensusEngine（或关闭时跳过）/ Code 解析消息里的补丁或围栏再测。</li>\n<li>每步 <code>timedTool</code>/<code>emit('tool')</code> 被工作台或 VS Code 插件画成工具卡。</li>\n</ol>"},"models":{"id":"models","path":"webagent-core/agent-host/src/models/README.md","group":"进程","title":"models 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：store.js","id":"文件名-store.js"},{"level":3,"text":"📄 文件名：customizations.js","id":"文件名-customizations.js"},{"level":3,"text":"📄 文件名：profile.js","id":"文件名-profile.js"},{"level":3,"text":"📄 文件名：memory.js","id":"文件名-memory.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"models-模块说明书\">models 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/models/</code></p>\n<p>工作区磁盘上的配置、指令、记忆。文件都写在 <strong>被编辑项目</strong> 的 <code>.webagent/</code> 下，不是 Git 仓库根。无独立 <code>.html</code>。产出的 json/md 由本目录函数写入。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 持久化「这个工作区怎么干活」：模型列表与 Bridge 开关（<code>store.js</code>）、自定义指令/环境/技术栈（<code>customizations.js</code>）、OS/栈探测（<code>profile.js</code>）、跨对话备忘（<code>memory.js</code>）。</li>\n<li><strong>依赖：</strong> <code>../config</code>（workspaceRoot）。<code>profile</code> 被 customizations 与 mcp/instructions、agent/openai 调用。</li>\n<li><strong>谁调用：</strong> <code>../index.js</code> persistIdentity；<code>../api/routes.js</code> REST；<code>../mcp/*</code> 拼 instructions；工具 <code>remember</code>/<code>recall</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-store.js\">📄 文件名：<code>store.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 读写 <code>&lt;工作区&gt;/.webagent/config.json</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>dir</code>（L8–L10）</strong> / <strong><code>storePath</code>（L12–L14）</strong> — <code>.webagent</code> 与其中 <code>config.json</code>。</li>\n<li><strong>Function <code>defaults</code>（L16–L50）</strong> — 见下方 Key。</li>\n<li><strong>Function <code>load</code>（L52–L65）</strong> — try 读 JSON 与 defaults 浅合并；<code>models</code> 非非空数组则用默认；<code>bridge</code>/<code>multiModel</code> 再与默认合并。<strong>catch 返回 defaults，不抛。</strong></li>\n<li><strong>Function <code>restrictFileMode</code>（L67–L71）</strong> — <code>chmod 0600</code>；失败 catch 空（Windows 可能无效）。</li>\n<li><strong>Function <code>lineCovers</code> / <code>alreadyIgnored</code>（L77–L91）</strong> — 根或嵌套 <code>.gitignore</code> 是否已覆盖 <code>.webagent/config.json</code> 等。</li>\n<li><strong>Function <code>ensureNestedIgnore</code>（L97–L113）</strong> — 写 <code>.webagent/.gitignore</code>（<code>config.json</code>、<code>read-hashes.json</code>），已有则不重复。</li>\n<li><strong>Function <code>ensureWorkspaceGitignore</code>（L115–L133）</strong> — 仅当工作区根有 <code>.git</code> 时，往<strong>该仓库</strong> <code>.gitignore</code> 追加上述两行。不是 git 仓库则跳过。</li>\n<li><strong>Function <code>protectWorkspaceSecrets</code>（L135–L141）</strong> — 嵌套 ignore + 工作区 ignore + 已有 <code>config.json</code> 则 chmod。失败 catch 空。</li>\n<li><strong>Function <code>save</code>（L143–L149）</strong> — mkdir + 美化 JSON + chmod + <code>protectWorkspaceSecrets</code>。</li>\n<li><strong>Function <code>patch</code>（L151–L161）</strong> — load 后浅合并；bridge/multiModel 深一层；<code>models</code> 仅当 <code>partial.models</code> 真才替换。</li>\n</ul>\n<ul>\n<li><strong>关键变量 <code>defaults()</code> 的 JSON Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>含义</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>activeModelId</code></td>\n<td>当前 Chat 模型</td>\n<td>默认 <code>'builtin'</code></td>\n</tr>\n<tr>\n<td><code>models[]</code></td>\n<td>模型列表</td>\n<td>默认一条 builtin：<code>id/name/protocol/baseUrl/apiKey/modelId</code></td>\n</tr>\n<tr>\n<td><code>models[].protocol</code></td>\n<td>协议</td>\n<td><code>'builtin'</code> 或工作台写入的 <code>'chat.completions'</code></td>\n</tr>\n<tr>\n<td><code>multiModel.enabled</code></td>\n<td>Plan 是否走博弈</td>\n<td>默认 <code>true</code></td>\n</tr>\n<tr>\n<td><code>multiModel.mergeModel</code></td>\n<td>合并主模型</td>\n<td>默认 <code>'auto'</code></td>\n</tr>\n<tr>\n<td><code>multiModel.thinkLevel</code></td>\n<td>思考强度</td>\n<td>默认 <code>'high'</code></td>\n</tr>\n<tr>\n<td><code>multiModel.maxBranches</code></td>\n<td>最大分支</td>\n<td>默认 <code>3</code></td>\n</tr>\n<tr>\n<td><code>multiModel.mergeAllowsRead</code></td>\n<td>合并时只读验证</td>\n<td>默认 <code>true</code></td>\n</tr>\n<tr>\n<td><code>bridge.loggedIn</code></td>\n<td>演示登录</td>\n<td>默认 <code>true</code>（否则 <code>/bridge/start</code> 403）</td>\n</tr>\n<tr>\n<td><code>bridge.deviceAuthorized</code></td>\n<td>设备授权</td>\n<td>默认 <code>true</code></td>\n</tr>\n<tr>\n<td><code>bridge.provider</code> / <code>username</code> / <code>license</code></td>\n<td>账号展示</td>\n<td><code>'github'</code> / <code>'demo'</code> / <code>'永久顺'</code></td>\n</tr>\n<tr>\n<td><code>bridge.tunnelProvider</code></td>\n<td>隧道种类</td>\n<td>默认 <code>'cloudflare'</code></td>\n</tr>\n<tr>\n<td><code>bridge.persistentMode</code></td>\n<td>持久隧道标记</td>\n<td>默认 <code>false</code>（本目录不消费它去 spawn）</td>\n</tr>\n<tr>\n<td><code>bridge.ngrokDomain</code> / <code>namedDomain</code> / <code>namedPort</code> / <code>quickLinks</code></td>\n<td>UI 字段</td>\n<td>空串 / 48271 / <code>[]</code></td>\n</tr>\n</tbody></table></div>\n<p> 另外 <code>persistIdentity</code> 会往该文件写入 <code>secretKey</code>、<code>installId</code>（不在 defaults 函数里，由 config 补上）。</p>\n<hr />\n<h3 id=\"文件名-customizations.js\">📄 文件名：<code>customizations.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <code>customizations.json</code>，并同步写出 <code>instructions.md</code> / <code>preference.md</code> / <code>tech-stack.md</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>file</code>（L6–L8）</strong> — <code>…/.webagent/customizations.json</code>。</li>\n<li><strong>Function <code>defaults</code>（L10–L50）</strong> — 见 Key 表。</li>\n<li><strong>Function <code>loadCustom</code>（L52–L65）</strong> — try 合并 environment/techStack；catch 返回 defaults。</li>\n<li><strong>Function <code>saveCustom</code>（L67–L78）</strong> — 与 defaults 合并；有 next.environment / techStack 再深合并；写 json + 三份 md。</li>\n<li><strong>Function <code>patchCustom</code>（L80–L82）</strong> — <code>saveCustom({ ...loadCustom(), ...partial })</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量 <code>defaults()</code> Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>含义</th>\n<th>默认</th>\n</tr></thead><tbody>\n<tr>\n<td><code>preference</code></td>\n<td>概述页偏好句</td>\n<td><code>''</code></td>\n</tr>\n<tr>\n<td><code>environment.os/shell</code></td>\n<td>系统/壳</td>\n<td><code>'auto'</code>（由 profile 探测）</td>\n</tr>\n<tr>\n<td><code>environment.replyLanguage/commitLanguage</code></td>\n<td>回复/提交语言</td>\n<td><code>'zh-CN'</code></td>\n</tr>\n<tr>\n<td><code>environment.notes</code></td>\n<td>备注</td>\n<td><code>''</code></td>\n</tr>\n<tr>\n<td><code>techStack.languages/frameworks/packageManager/testCommand/notes</code></td>\n<td>技术栈</td>\n<td>全 <code>''</code></td>\n</tr>\n<tr>\n<td><code>instructions</code></td>\n<td>始终生效指令</td>\n<td>中文默认句（提交用中文、Ask 只读等）</td>\n</tr>\n<tr>\n<td><code>agents[]</code></td>\n<td>自定义智能体</td>\n<td>一条 default</td>\n</tr>\n<tr>\n<td><code>prompts[]</code></td>\n<td>可插入提示</td>\n<td>一条 diagnose</td>\n</tr>\n<tr>\n<td><code>hooks</code> / <code>mcpServers</code> / <code>plugins</code> / <code>quickLinks</code></td>\n<td>UI 列表</td>\n<td><code>[]</code>（hooks 无运行时执行器）</td>\n</tr>\n<tr>\n<td><code>voice</code> / <code>dictation</code></td>\n<td>占位</td>\n<td><code>''</code></td>\n</tr>\n<tr>\n<td><code>codex.loggedIn/account</code></td>\n<td>演示登录</td>\n<td>false / <code>''</code></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h3 id=\"文件名-profile.js\">📄 文件名：<code>profile.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 探测 OS 与仓库技术栈；拼给模型的上下文；生成两份 markdown。<strong>不写磁盘。</strong></li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>detectEnvironment</code>（L5–L16）</strong> — win32→windows/powershell；darwin→macos/bash；否则 linux/bash。语言写死 zh-CN。</li>\n<li><strong>Function <code>readJson</code>（L18–L24）</strong> — 失败 null。</li>\n<li><strong>Function <code>exists</code>（L26–L28）</strong> — <code>existsSync(join(root,rel))</code>。</li>\n<li><strong>Function <code>detectTechStack</code>（L30–L76）</strong></li>\n<li>L38–L50：有 package.json → JS；tsconfig/jsconfig → TS；deps 判 Next/React/Nuxt/Vue/Express；lock 判 pnpm/yarn/npm；<code>scripts.test</code> → <code>${packageManager} test</code>。</li>\n<li>L51–L55：Python 清单 → pytest 命令（仅当 testCommand 仍空）。</li>\n<li>L56–L64：Cargo.toml / go.mod 同样仅当字段仍空。</li>\n<li>L65：index.html 且不含 HTML → 加 HTML。</li>\n<li>L67–L75：uniq join。<code>notes</code> 数组从未 push，恒空串。</li>\n<li><strong>Function <code>resolveEnvironment</code>（L78–L88）</strong> — os/shell 仅当用户值存在且不是 <code>'auto'</code> 才覆盖；语言用 <code>||</code>。</li>\n<li><strong>Function <code>resolveTechStack</code>（L90–L100）</strong> — 各字段 <code>stack.xxx || detected.xxx</code>。</li>\n<li><strong>Function <code>languageLabel</code>（L102–L107）</strong> — zh-CN/zh→中文；en→English；follow-user→跟随用户。</li>\n<li><strong>Function <code>osLabel</code>（L109–L111）</strong> — windows/macos/linux 映射。</li>\n<li><strong>Function <code>formatWorkspaceContext</code>（L113–L147）</strong> — markdown 三段 Environment / Tech stack / Skills。栈全空时加 not declared。skills 空则提示目录。</li>\n<li><strong>Function <code>markdownPreference</code>（L149–L166）</strong> / <strong><code>markdownTechStack</code>（L168–L182）</strong> — 中文 md；<code>filter(l !== null)</code> 保留空字符串当空行。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-memory.js\">📄 文件名：<code>memory.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <code>.webagent/memory/YYYY-MM-DD.md</code> 追加备忘。</li>\n<li><strong>Function <code>memoryDir</code>（L5–L7）</strong> / <strong><code>dayFile</code>（L9–L12）</strong> — day 缺省 ISO 日期。</li>\n<li><strong>Function <code>remember</code>（L14–L22）</strong> — text trim 空 → <code>{ ok:false, error:'text required' }</code>。存在则 append；否则先写 <code># 日期</code>。换行压成空格。</li>\n<li><strong>Function <code>recall</code>（L24–L38）</strong> — 指定 day 只读一天；否则所有 <code>.md</code> sort reverse。拼接超 8000 字 break。再按行 <code>slice(0, max(5,limit))</code>。空则 <code>'(empty memory)'</code>。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>进程启动：<code>config.persistIdentity(store)</code> 从 <code>config.json</code> 取回 secret/installId，没有则 patch 进去；然后 <code>protectWorkspaceSecrets</code>（密钥仍在工作区，但 git 忽略）。</li>\n<li>工作台设置页 PUT <code>/api/customizations</code> → <code>saveCustom</code> 写 json+md。</li>\n<li>MCP <code>initialize</code> / Chat systemPrompt 读 <code>loadCustom</code> + <code>formatWorkspaceContext</code>。</li>\n<li>工具 remember/recall 只碰 <code>memory/</code>。</li>\n<li>Add API / 多模型开关走 <code>store.save</code> / <code>patch</code>，与 customizations 文件分开。</li>\n</ol>"},"api":{"id":"api","path":"webagent-core/agent-host/src/api/README.md","group":"进程","title":"api 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：routes.js","id":"文件名-routes.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"api-模块说明书\">api 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/api/</code></p>\n<p>本目录只有 <code>routes.js</code>：工作台和 VS Code 插件用的 REST，挂在 <code>/api</code>。无 <code>.json</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 给人点的按钮的后端：状态、Chat 流、文件树、自定义设置、启停 Bridge、探测模型。</li>\n<li><strong>依赖：</strong> <code>../config</code>、<code>../tools</code>、<code>../tools/readCache</code>（<code>resetHashes</code>、<code>rememberHash</code>）、<code>../agent/runChat</code>、<code>../agent/providers</code>、<code>../models/*</code>、<code>../mcp/session</code>（含 <code>reset</code>）、<code>../mcp/instructions</code>、<code>../mcp/clients</code>、<code>../mcp/oauth</code>、<code>../tunnel/cloudflared</code>、<code>../utils/eventBus</code>、<code>../tools/patchEngine</code>。</li>\n<li><strong>谁调用：</strong> <code>../index.js</code>：<code>uiApp.use('/api', apiRouter)</code>；<code>mcpApp.use('/api', rejectUnlessLocalControl, apiRouter)</code>（隧道/公网 Host 打 48271 的 <code>/api</code> 得 404）。浏览器工作台走 <strong>3000</strong>；VS Code 插件走本机 <code>127.0.0.1:48271</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-routes.js\">📄 文件名：<code>routes.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 注册全部 <code>/api/*</code> 路由。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>publicOrigin(req)</code>（L24–L28）</strong> — proto/host 来自转发头或 <code>req</code>，fallback host 用 <code>workbenchPort</code>。</li>\n<li><strong>Function <code>mcpOrigin(req)</code>（L30–L33）</strong> — 有 <code>config.publicTunnelUrl</code> 用它（去尾 <code>/</code>），否则 <code>publicOrigin</code>。</li>\n<li><strong>Function <code>mcpInfo(req)</code>（L35–L51）</strong> — 拼 <code>/mcp/${secretKey}</code>、canonical <code>/mcp</code>、bootstrap prompt、<code>listClients</code>、pairing、<code>tunnel.snapshot()</code>。</li>\n</ul>\n<ul>\n<li><strong>路由（逐步，含分支）：</strong></li>\n</ul>\n<ul>\n<li><strong>GET <code>/status</code>（L53–L89）</strong> — 拼 online、端口、workspace、tools、taskState、logs 40 条、bridgeRunning、mcpInfo 展开、models（apiKey 变成 <code>hasKey</code> 布尔）、activeModelId、multiModel、bridgeAccount、mcpSession。无鉴权。</li>\n<li><strong>POST <code>/bridge/reset-secret</code>（L91–L97）</strong> — <code>generateNewSecret()</code>（内存 + <code>.webagent/config.json</code>）→ <code>oauth.revokeAll()</code> → broadcast <code>secret_rotated</code>。</li>\n<li><strong>POST <code>/bridge/start</code>（L99–L139）</strong></li>\n<li>L101–L103：<code>!loggedIn || !deviceAuthorized</code> → <strong>403</strong>。</li>\n<li>L104–L108：记下 tunnelProvider，patch store，<code>config.bridgeRunning=true</code>。</li>\n<li>L108：<code>oauth.ensurePairing()</code>。</li>\n<li>L111–L117：<code>provider === 'cloudflare'</code> 时 <strong><code>await tunnel.startQuickTunnel({ port: config.port })</code></strong>。失败（无二进制、超时、spawn 错）记下 <code>tunnelError</code>，<strong>不</strong>把整个 Bridge 判失败。</li>\n<li>L119–L138：broadcast + json。有 <code>tunnel.url</code> → note「Quick Tunnel 已就绪」；否则 note 带错误或「走当前页面源」，<code>mcpOrigin</code> 仍用 Host。</li>\n<li>Named / ngrok <strong>不</strong> spawn（源码没有对应实现）。</li>\n<li><strong>POST <code>/bridge/stop</code>（L141–L146）</strong> — <strong><code>tunnel.stopTunnel()</code></strong>，<code>bridgeRunning=false</code>，broadcast，json 带 mcpInfo。</li>\n<li><strong>POST <code>/bridge/reset-round</code>（L148–L153）</strong> — <code>mcpReset()</code> + <code>resetHashes()</code> + broadcast <code>bridge_round_reset</code>。</li>\n<li><strong>POST <code>/consensus/run</code>（L155–L163）</strong> — <code>runMultiModelConsensus</code>；catch 500。</li>\n<li><strong>POST <code>/tool/call</code>（L165–L176）</strong> — body <code>{ name, arguments, mode='code' }</code>；broadcast 后 <code>callTool(..., mode)</code>；失败 400。</li>\n<li><strong>POST <code>/chat</code>（L178–L200）</strong> — NDJSON、<code>X-Accel-Buffering: no</code>、flushHeaders。emit 写一行 JSON。try <code>runChat({ mode, message, history, emit })</code> 后 emit <code>done</code>；catch emit <code>error</code>；最后 <code>res.end()</code>。<code>runChat</code> 必须从 payload 取出 emit，否则内置/OpenAI 路径都没有事件流。</li>\n<li><strong>POST <code>/tasks/reset</code>（L202–L204）</strong> — <code>resetTaskState()</code>。</li>\n<li><strong>GET <code>/files/tree</code>（L206–L213）</strong> — <code>callTool('list_directory', { recursive:true, maxDepth:5 }, 'ask')</code>。</li>\n<li><strong>GET <code>/files/content</code>（L215–L234）</strong> — query.path → resolveSafePath；不存在或目录 404；否则全文+hash，并 <code>rememberHash</code>。</li>\n<li><strong>PUT <code>/files/content</code>（L236–L258）</strong> — path 与 string content 必须；<code>callTool('write_file', { confirm_overwrite:true, expectedHash? }, 'code')</code>。敏感路径 / 逃出工作区 400；<code>STALE_FILE</code> 409。不直接 <code>writeFileSync</code>。</li>\n<li><strong>GET <code>/skills</code>（L260–L281）</strong> — 扫两个 skills 根，有 SKILL.md 则 preview 400 字（不要求 isDirectory 检查，与 <code>tools/skills.listSkills</code> 略不同）。</li>\n<li><strong>POST <code>/providers/probe</code>（L283–L291）</strong> — <code>listRemoteModels</code>；失败 400。</li>\n<li><strong>GET <code>/models</code>（L293–L300）</strong> — apiKey 显示 <code>••••</code> 或 <code>''</code>。</li>\n<li><strong>POST <code>/models</code>（L302–L315）</strong> — 可改 activeModelId；可整表 models；可 upsert <code>body.model</code>；可合并 multiModel；然后 <strong><code>store.save(cfg)</code> 整份</strong>。</li>\n<li><strong>GET <code>/logs</code>（L317–L319）</strong> — 80 条。</li>\n<li><strong>GET <code>/profile/detect</code>（L321–L327）</strong> — detectEnvironment + detectTechStack + listSkills。</li>\n<li><strong>GET <code>/customizations</code>（L329–L331）</strong> / <strong>PUT（L333–L337）</strong> — load / patchCustom。</li>\n<li><strong>POST <code>/skills</code>（L339–L354）</strong> — name 清洗：非单词变 <code>-</code>，去首尾 <code>-</code>，最长 40；空 400。默认 content 模板。<code>callTool('write_file')</code> 写 <code>.webagent/skills/&lt;name&gt;/SKILL.md</code>。</li>\n<li><strong>POST <code>/bridge/login</code>（L356–L369）</strong> — 默认 github/demo，patch loggedIn true、永久顺、deviceAuthorized true。</li>\n<li><strong>POST <code>/bridge/logout</code>（L371–L377）</strong> — loggedIn false；<strong><code>tunnel.stopTunnel()</code></strong>；<code>bridgeRunning=false</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L22 <code>router = express.Router()</code>。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>工作台 boot → GET <code>/status</code> 填 Bridge 卡与模型下拉。</li>\n<li>CHAT 发送 → POST <code>/chat</code> → <code>runChat</code> → 工具经 <code>callTool</code>。</li>\n<li>点启动 Bridge → POST <code>/bridge/start</code>：登录校验后置 <code>bridgeRunning</code>、配对码；<code>tunnelProvider==='cloudflare'</code> 时 <code>await startQuickTunnel</code>。成功则 <code>mcpOrigin</code> 用 trycloudflare 公网 URL；失败仍 200，MCP 走当前页面 Host。</li>\n<li>点停止 Bridge → POST <code>/bridge/stop</code> → <code>stopTunnel()</code> 清子进程与 <code>publicTunnelUrl</code>。</li>\n<li>点「清除本轮统计」→ POST <code>/bridge/reset-round</code> → 清 MCP session 计数与 <code>readCache</code> 哈希。</li>\n<li>设置页表单 → PUT <code>/customizations</code> 或 POST <code>/models</code>。</li>\n<li>插件侧栏与工作台打同一组路径。</li>\n</ol>"},"tunnel":{"id":"tunnel","path":"webagent-core/agent-host/src/tunnel/README.md","group":"进程","title":"tunnel 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：cloudflared.js","id":"文件名-cloudflared.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"tunnel-模块说明书\">tunnel 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/tunnel/</code></p>\n<p>本目录只有 <code>cloudflared.js</code>：找二进制、拉 Cloudflare Quick Tunnel、从日志解析 <code>*.trycloudflare.com</code>。无 <code>.json</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 把本机 <code>config.port</code>（默认 48271）映射成临时 HTTPS，让云上网页 Agent 能打进来。</li>\n<li><strong>依赖：</strong> <code>../config</code>、<code>../utils/eventBus</code>。</li>\n<li><strong>谁调用：</strong> <code>../api/routes.js</code>：<code>/status</code> 暴露 <code>tunnel.snapshot()</code>；<strong><code>POST /bridge/start</code> 在 <code>tunnelProvider==='cloudflare'</code> 时 <code>await startQuickTunnel({ port: config.port })</code></strong>（失败记下 <code>tunnelError</code>，Bridge 仍 200）；<code>/bridge/stop</code> 与 <code>/bridge/logout</code> 调 <code>stopTunnel</code>。Named / ngrok 不 spawn。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-cloudflared.js\">📄 文件名：<code>cloudflared.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> spawn <code>cloudflared tunnel --url http://127.0.0.1:&lt;port&gt;</code>，从 stdout/stderr 抠公网 URL，写入 <code>config.publicTunnelUrl</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>parseTunnelUrl(chunk)</code>（L12–L15）</strong> — 用 <code>URL_RE</code> 匹配；命中去尾 <code>/</code>，否则 null。</li>\n<li><strong>Function <code>findCloudflared()</code>（L17–L40）</strong></li>\n<li>L18–L20：<code>CLOUDFLARED_PATH</code> 存在则用。</li>\n<li>L21–L29：<code>where</code>（win）或 <code>which</code>，stdout 按行找 <code>existsSync</code>。</li>\n<li>L30–L39：再猜 LOCALAPPDATA / Program Files（Windows）或 brew/usr（Unix）。</li>\n<li>L39：都没有 <code>null</code>。</li>\n<li><strong>Function <code>installHint()</code>（L42–L47）</strong> — win32 返回 winget + GitHub releases 文案；否则 brew / Cloudflare 文档。</li>\n<li><strong>Function <code>stopTunnel()</code>（L49–L59）</strong> — child 未 killed → SIGTERM；1.5s 后再 SIGKILL。清空 child、quickUrl、<code>config.publicTunnelUrl</code>。</li>\n<li><strong>Function <code>startQuickTunnel({ port=config.port, timeoutMs=25000 })</code>（L61–L122）</strong></li>\n<li>L62：先 <code>stopTunnel()</code>。</li>\n<li>L63–L68：无二进制 → <strong>Promise.reject</strong>，<code>code='E_NO_CLOUDFLARED'</code>。</li>\n<li>L70–L122：Promise：spawn args <code>tunnel --url http://127.0.0.1:${port} --no-autoupdate</code>。Windows 上仅当二进制是 <code>.cmd/.bat</code> 才 <code>shell:true</code>。</li>\n<li>L83–L88：timeoutMs 内未解析 URL → stopTunnel + reject。</li>\n<li>L90–L103：stdout <strong>和</strong> stderr 都进 onData；broadcast <code>tunnel_log</code>（截 400 字）；解析成功则设 quickUrl 与 <code>config.publicTunnelUrl</code>，broadcast <code>tunnel_ready</code>，resolve <code>{ url, binary, target }</code>。</li>\n<li>L105–L111：<code>error</code> 未 settled → reject 无法启动。</li>\n<li>L112–L119：<code>exit</code> 未 settled → reject 退出码 + installHint。</li>\n<li><strong>Function <code>snapshot()</code>（L124–L130）</strong> — <code>{ binary, url: quickUrl||publicTunnelUrl, running: Boolean(child &amp;&amp; !killed) }</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong></li>\n<li>L8 <code>URL_RE</code> = <code>/https:\\/\\/[a-z0-9-]+\\.trycloudflare\\.com/i</code></li>\n<li>L10–L11 模块级 <code>child</code>、<code>quickUrl</code></li>\n<li>L132–L134：<code>process</code> 的 exit/SIGINT/SIGTERM 会 <code>stopTunnel</code>（后两者再 <code>process.exit(0)</code>）</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>有人调用 <code>startQuickTunnel</code> → 找二进制 → spawn → 扫日志 → 写下 <code>config.publicTunnelUrl</code>。</li>\n<li>之后 <code>mcp/oauth.requestOrigin</code> 与 <code>api/routes.mcpOrigin</code> 会优先用该 URL，MCP 地址变成 <code>https://….trycloudflare.com/mcp/&lt;secret&gt;</code>。</li>\n<li><code>stopTunnel</code> 或进程退出清掉 URL。</li>\n<li><strong>产品按钮路径：</strong> <code>POST /bridge/start</code>（cloudflare）进入第 1 步；<code>/bridge/stop</code> 与 logout 进入 <code>stopTunnel</code>。无二进制或超时 → 路由 catch，MCP 仍走当前 Host。</li>\n</ol>"},"utils":{"id":"utils","path":"webagent-core/agent-host/src/utils/README.md","group":"进程","title":"utils 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：eventBus.js","id":"文件名-eventbus.js"},{"level":3,"text":"📄 文件名：diff.js","id":"文件名-diff.js"},{"level":3,"text":"📄 文件名：localControl.js","id":"文件名-localcontrol.js"},{"level":3,"text":"📄 文件名：corsAllow.js","id":"文件名-corsallow.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"utils-模块说明书\">utils 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/src/utils/</code></p>\n<p>进程内事件总线、diff 辅助、本机控制面闸、以及 MCP CORS 白名单。无 <code>.json</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 跨 MCP / Chat / 命令执行的广播通道；补丁成功后生成 unified diff 给 UI；mcp 端口上的 <code>/api</code> 只允许本机回环；浏览器跨域只放行扩展和已知聊天站。</li>\n<li><strong>依赖：</strong> Node <code>events</code>；npm 包 <code>diff</code>、<code>cors</code>（仅 <code>corsAllow.js</code>）。<code>localControl.js</code> 无第三方依赖。</li>\n<li><strong>谁调用：</strong> <code>../index.js</code> 把 <code>/ws</code> 客户端交给 eventBus，并把 <code>rejectUnlessLocalControl</code> + <code>rejectCrossSiteApi</code> 挂在两套 app 的 <code>/api</code> 前；<code>mcpCors()</code> 挂在 mcpApp；几乎所有工具与 <code>mcp/server.js</code>、<code>api/routes.js</code> broadcast；<code>patchEngine</code> 调 <code>createUnifiedDiff</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-eventbus.js\">📄 文件名：<code>eventBus.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 单例 EventEmitter + WebSocket 扇出 + 环形日志。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Class <code>BridgeEventBus</code>（L3–L44）</strong></li>\n<li><strong>constructor（L4–L8）</strong> — <code>wsClients</code> Set；<code>logs=[]</code>；<code>maxLogs=500</code>。</li>\n<li><strong>Method <code>addWsClient(ws)</code>（L10–L15）</strong> — 加入 Set；<code>close</code> 时 delete。</li>\n<li><strong>Method <code>broadcast(type, payload={})</code>（L17–L40）</strong></li>\n<li>L18–L22：造 <code>{ type, timestamp ISO, payload }</code>。</li>\n<li>L24–L27：<code>logs.unshift</code>；超过 maxLogs 则 <code>pop</code>。</li>\n<li>L29–L37：对每个 ws，<code>readyState === 1</code> 才 <code>try send</code>，catch 空。</li>\n<li>L39：<code>this.emit(type, payload)</code> 给进程内监听者。</li>\n<li><strong>Method <code>getRecentLogs(limit=50)</code>（L42–L44）</strong> — <code>logs.slice(0, limit)</code>。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong> L46 <code>const eventBus = new BridgeEventBus()</code>，L47 <code>module.exports = eventBus</code>（单例，不是类）。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-diff.js\">📄 文件名：<code>diff.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 给 <code>apply_patch</code> 成功结果提供 patch 文本和加减行数。</li>\n<li><strong>Function <code>createUnifiedDiff(filePath, oldContent, newContent)</code>（L3–L29）</strong></li>\n<li>L4–L11：<code>jsdiff.createTwoFilesPatch</code>，路径 <code>a/</code> <code>b/</code>，头 <code>current</code>/<code>patched</code>。</li>\n<li>L13–L22：<code>diffLines</code> 累计 added/removed（按非空行计数）。</li>\n<li>L24–L29：返回 <code>{ patch, additions, deletions, changes }</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-localcontrol.js\">📄 文件名：<code>localControl.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 判断请求是不是本机控制面。隧道/公网 Host 打 <code>/api</code> 得 404。</li>\n<li><strong>Function <code>isLoopbackAddress(addr)</code>（L1–L7）</strong> — <code>127.0.0.1</code> / <code>::1</code> / <code>::ffff:127.0.0.1</code> / <code>localhost</code>。</li>\n<li><strong>Function <code>isTunnelRequest(req)</code>（L9–L18）</strong> — 头 <code>cf-ray</code> / <code>cf-connecting-ip</code> / <code>cf-visitor</code> / <code>cf-ew-via</code> / <code>cdn-loop</code>。</li>\n<li><strong>Function <code>hostName(req)</code>（L20–L26）</strong> — <code>Host</code> 去端口、去 IPv6 方括号。</li>\n<li><strong>Function <code>isPublicHost(req)</code>（L28–L35）</strong> — <code>*.trycloudflare.com</code> 或 ngrok 域名。</li>\n<li><strong>Function <code>isLocalControlPlane(req)</code>（L37–L42）</strong> — 隧道头或公网 Host → 假；否则看 <code>socket.remoteAddress</code> 是否回环。</li>\n<li><strong>Function <code>rejectUnlessLocalControl(req, res, next)</code>（L44–L47）</strong> — 本机 <code>next()</code>，否则 404 <code>{ error:'not found' }</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-corsallow.js\">📄 文件名：<code>corsAllow.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> MCP 口 CORS 白名单；<code>/api</code> 拒绝非本机浏览器 Origin（防网页 CSRF）。<strong>不是</strong>认证。</li>\n<li><strong>常量：</strong> L3–L7 <code>EXTENSION_PROTOCOLS</code>；L10–L18 <code>PAGE_ORIGINS</code>（DeepSeek / ChatGPT / Gemini / AI Studio / Arena，以本仓库文档点名为准）。</li>\n<li><strong>Function <code>extraOrigins()</code>（L20–L25）</strong> — <code>WEBAGENT_CORS_ORIGINS</code> 逗号分隔。</li>\n<li><strong>Function <code>parseOrigin(origin)</code>（L27–L33）</strong> — <code>new URL</code>，坏值 <code>null</code>。</li>\n<li><strong>Function <code>isLoopbackHostName(name)</code>（L35–L38）</strong> / <strong><code>isLoopbackOrigin(origin)</code>（L40–L45）</strong> — 仅 <code>http(s)</code> 的 127.0.0.1 / localhost / <code>::1</code>。</li>\n<li><strong>Function <code>isExtensionOrigin(origin)</code>（L47–L50）</strong> — <code>chrome-extension:</code> / <code>moz-extension:</code> / <code>safari-web-extension:</code>。</li>\n<li><strong>Function <code>extraOriginSet()</code>（L52–L59）</strong> — 环境变量解析成 origin 集合。</li>\n<li><strong>Function <code>isAllowedMcpOrigin(origin)</code>（L61–L70）</strong> — 无 Origin（curl / Node / 云上服务器）放行；本机、扩展、PAGE_ORIGINS、额外名单放行；其它假。</li>\n<li><strong>Function <code>isAllowedApiBrowserOrigin(origin)</code>（L72–L75）</strong> — 无 Origin 或本机 Origin 才真。聊天站 Origin <strong>不能</strong>打 <code>/api</code>。</li>\n<li><strong>Function <code>refererOrigin(req)</code>（L77–L82）</strong> — 从表单 CSRF 的 Referer 取 origin。</li>\n<li><strong>Function <code>rejectCrossSiteApi(req, res, next)</code>（L84–L96）</strong> — 有 Origin 且非本机 → 404；无 Origin 但 Referer 非本机 → 404；否则 <code>next()</code>。</li>\n<li><strong>Function <code>mcpCors()</code>（L98–L104）</strong> — <code>cors({ origin(cb) })</code>，回调 <code>isAllowedMcpOrigin</code>。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li><code>index.js</code> <code>attachWss</code>：浏览器连 3000 的 <code>/ws</code> → <code>addWsClient</code>，并立即收到 <code>connected</code>（<strong>不含</strong> secretKey）。mcp 端口不挂 WebSocket。</li>\n<li>mcpApp 先 <code>mcpCors()</code>：浏览器预检只给扩展和名单里的聊天站回 <code>Access-Control-Allow-Origin</code>。</li>\n<li>两套 app 的 <code>/api</code> 先过 <code>rejectUnlessLocalControl</code>：Cloudflare 头或 <code>*.trycloudflare.com</code> Host → 404；本机回环 <code>next()</code>。再过 <code>rejectCrossSiteApi</code>：<code>https://evil.example</code> 这类 Origin 同样 404。无 Origin 的 Node 插件 / 测试仍通。</li>\n<li>工具/MCP 调用 <code>broadcast</code> → 写入 logs + 推到所有打开的工作台。</li>\n<li>工作台 <code>connectWs</code> 根据 type 刷新终端、文件树、BRIDGE 工具卡、todos。</li>\n<li><code>patchEngine</code> 写盘后用 <code>createUnifiedDiff</code> 把 diff 放进 broadcast payload，工作台可开 diff 页。</li>\n</ol>"},"tests":{"id":"tests","path":"webagent-core/agent-host/tests/README.md","group":"产品","title":"tests 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：patchEngine.test.js","id":"文件名-patchengine.test.js"},{"level":3,"text":"📄 文件名：mcpProtocol.test.js","id":"文件名-mcpprotocol.test.js"},{"level":3,"text":"📄 文件名：workspaceTools.test.js","id":"文件名-workspacetools.test.js"},{"level":3,"text":"📄 文件名：sandbox.test.js","id":"文件名-sandbox.test.js"},{"level":3,"text":"📄 文件名：tunnel.test.js","id":"文件名-tunnel.test.js"},{"level":3,"text":"📄 文件名：hostPersist.test.js","id":"文件名-hostpersist.test.js"},{"level":3,"text":"📄 文件名：bridgeTunnel.test.js","id":"文件名-bridgetunnel.test.js"},{"level":3,"text":"📄 文件名：apiFiles.test.js","id":"文件名-apifiles.test.js"},{"level":3,"text":"📄 文件名：localControl.test.js","id":"文件名-localcontrol.test.js"},{"level":3,"text":"📄 文件名：corsAllow.test.js","id":"文件名-corsallow.test.js"},{"level":3,"text":"📄 文件名：httpSmoke.test.js","id":"文件名-httpsmoke.test.js"},{"level":3,"text":"📄 文件名：codeServerNotRunnable.test.js","id":"文件名-codeservernotrunnable.test.js"},{"level":3,"text":"📄 文件名：codeServerAuth.test.js","id":"文件名-codeserverauth.test.js"},{"level":3,"text":"📄 文件名：skipWorkbench.test.js","id":"文件名-skipworkbench.test.js"},{"level":3,"text":"📄 文件名：runChat.test.js","id":"文件名-runchat.test.js"},{"level":3,"text":"📄 文件名：chatMode.test.js","id":"文件名-chatmode.test.js"},{"level":3,"text":"📄 文件名：profile.test.js","id":"文件名-profile.test.js"},{"level":3,"text":"📄 文件名：oauth.test.js","id":"文件名-oauth.test.js"},{"level":2,"text":"3. 执行逻辑流（仅本目录）","id":"3.-执行逻辑流-仅本目录"}],"html":"<h1 id=\"tests-模块说明书\">tests 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/agent-host/tests/</code></p>\n<p>本目录是 <strong>现行产品测试集</strong>（Node 自带 <code>assert</code>，无 Jest）。仓库根不必再另建 <code>tests/</code>。无 <code>.html</code> / <code>.py</code>。</p>\n<p>跑法：Windows <code>run-tests.cmd</code>；其它 <code>cd webagent-core/agent-host &amp;&amp; npm test</code>。覆盖总表也见 <a href=\"../../../测试说明.md\">测试说明.md</a>。</p>\n<p><code>package.json</code> scripts.test 按下面顺序 <code>&amp;&amp;</code> 串联，任一失败即停。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>文件</th>\n<th>覆盖</th>\n</tr></thead><tbody>\n<tr>\n<td><code>patchEngine.test.js</code></td>\n<td><code>apply_patch</code> 成功、STALE_FILE、读缓存省略 hash、从未 read 的 orphan→<code>HASH_REQUIRED</code>+<code>currentHash</code>、冲突、grep</td>\n</tr>\n<tr>\n<td><code>mcpProtocol.test.js</code></td>\n<td>initialize.instructions、资源、<strong>25</strong> 工具、危险命令（含 <code>git reset --hard</code>）、<code>Available:</code>、<code>cat</code>/<code>path</code> 别名、<code>tools/call</code> <code>isError:true</code>、memory、connect 提示词、DeepSeek / Chat Plus 客户端配方</td>\n</tr>\n<tr>\n<td><code>workspaceTools.test.js</code></td>\n<td>无仓 <code>available:false</code>、skills、<code>delete_file</code> 须 <code>confirm</code>、覆盖须 <code>confirm_overwrite</code>、Ask 锁、路径逃逸、敏感文件、<code>path</code>/<code>confirm:'true'</code>/<code>bash</code>/<code>ls</code>、<code>start_command</code></td>\n</tr>\n<tr>\n<td><code>sandbox.test.js</code></td>\n<td>默认 <code>host=127.0.0.1</code>；symlink 指到工作区外时 read/cwd/list 拒绝</td>\n</tr>\n<tr>\n<td><code>hostPersist.test.js</code></td>\n<td><code>generateNewSecret</code> 写入 <code>config.json</code>；<code>read-hashes.json</code> 跨 require 仍能 recalledHash；<code>resetHashes</code> 删文件；<code>.webagent/.gitignore</code>；git 仓库补根 ignore</td>\n</tr>\n<tr>\n<td><code>tunnel.test.js</code></td>\n<td>从 cloudflared 日志解析 <code>*.trycloudflare.com</code></td>\n</tr>\n<tr>\n<td><code>bridgeTunnel.test.js</code></td>\n<td>stub <code>startQuickTunnel</code>/<code>stopTunnel</code>：cloudflare 启动后 mcpUrl 含 trycloudflare；<code>E_NO_CLOUDFLARED</code> 仍 200；未登录 403</td>\n</tr>\n<tr>\n<td><code>apiFiles.test.js</code></td>\n<td><code>PUT /api/files/content</code> 走 <code>write_file</code>：普通文件写入、<code>.env</code> 拒绝、越界拒绝、错 hash 409、<code>POST /api/skills</code></td>\n</tr>\n<tr>\n<td><code>localControl.test.js</code></td>\n<td>回环 / Cloudflare 头 / trycloudflare Host 是否算本机控制面</td>\n</tr>\n<tr>\n<td><code>corsAllow.test.js</code></td>\n<td>MCP Origin 白名单；外站 Origin/Referer 打 <code>/api</code> 拒绝</td>\n</tr>\n<tr>\n<td><code>httpSmoke.test.js</code></td>\n<td>真起进程：health、工作台 HTML（含 <code>#page-env</code>）、模块脚本、MCP 401、initialize、tools/list、ping、隧道头打 <code>/api</code> 得 404、外站 Origin 的 <code>/api</code> 404、DeepSeek/扩展 OPTIONS 有 CORS 头、本机 <code>POST /api/chat</code> NDJSON</td>\n</tr>\n<tr>\n<td><code>codeServerNotRunnable.test.js</code></td>\n<td>Git 不内嵌 <code>code-server-dist</code>；vscode 入口走 npm runtime；不写死 <code>--auth none</code> / <code>trusted-origins *</code></td>\n</tr>\n<tr>\n<td><code>codeServerAuth.test.js</code></td>\n<td>口令落盘复用；<code>CODE_SERVER_PASSWORD</code>；<code>CODE_SERVER_AUTH=none</code>；trusted-origins 仅本机</td>\n</tr>\n<tr>\n<td><code>skipWorkbench.test.js</code></td>\n<td><code>WEBAGENT_SKIP_WORKBENCH=1</code> 不占用工作台端口</td>\n</tr>\n<tr>\n<td><code>runChat.test.js</code></td>\n<td>内置 Chat 对任意工作区搜-读-再测；第二参 emit 与 <code>payload.emit</code> 两条路径</td>\n</tr>\n<tr>\n<td><code>chatMode.test.js</code></td>\n<td><code>@webagent</code> 默认 Agent=code；<code>/ask</code> <code>/plan</code></td>\n</tr>\n<tr>\n<td><code>profile.test.js</code></td>\n<td>环境偏好 / 技术栈写入 <code>.webagent</code>，进入指令</td>\n</tr>\n<tr>\n<td><code>oauth.test.js</code></td>\n<td>OAuth 发现、配对、PKCE、Bearer <code>/mcp</code>、SSE</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 锁协议、工具沙箱、Chat 循环、OAuth、HTTP 烟测、启动脚本约束。失败 <code>process.exit(1)</code>。</li>\n<li><strong>兄弟依赖：</strong> 全部 <code>../src/**</code>。<code>httpSmoke</code> / <code>skipWorkbench</code> 会 <code>spawn</code> <code>src/index.js</code>。多数测试把 <code>config.workspaceRoot</code> 指到 <code>os.tmpdir()</code>，结束 <code>rmSync</code>。</li>\n<li><strong>谁调用：</strong> <code>npm test</code> 或根目录 <code>run-tests.cmd</code>。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-patchengine.test.js\">📄 文件名：<code>patchEngine.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 临时工作区测补丁成功、过期 hash、读缓存、orphan <code>HASH_REQUIRED</code>、冲突、grep。</li>\n<li><strong>顶部：</strong> L1–L10 <code>assert</code>/<code>fs</code>/<code>os</code>/<code>path</code>；<code>config</code>；<code>applyPatch</code>/<code>computeHash</code>；<code>readFile</code>/<code>grepSearch</code>；<code>mkdtempSync</code> 后改 <code>config.workspaceRoot</code>。</li>\n<li><strong>Function <code>main</code>（L12–L99）</strong></li>\n<li>L13：写 <code>sample.js</code>，内容含 <code>return a + b</code>。</li>\n<li>L15–L17：<code>readFile</code> 必须有 <code>hash</code>，正文含原 return。</li>\n<li>L19–L30：带 <code>expectedHash</code> 的 SEARCH/REPLACE 改成 <code>Number(a)+Number(b)</code>，磁盘出现 <code>Number(a)</code>。</li>\n<li>L32–L46：再用<strong>旧</strong> hash 打补丁；catch 消息须匹配 <code>STALE_FILE</code>。</li>\n<li>L48–L56：不传 <code>expectedHash</code> 仍成功（复用上次 read/补丁的缓存）。</li>\n<li>L58–L75：磁盘直接写 <code>orphan.js</code>（<strong>从未</strong> <code>readFile</code>）再补丁 → <code>HASH_REQUIRED</code> 且 <code>err.detail.currentHash</code> 有值。</li>\n<li>L77–L92：SEARCH 不在文件中 → 消息匹配 <code>Patch conflict</code>。</li>\n<li>L94–L95：<code>grepSearch({ query:'function add', searchPath:'.' })</code>，<code>totalMatches &gt;= 1</code>。</li>\n<li>L97：删临时目录。L98：打印 passed。</li>\n<li>L101–L104：<code>main().catch</code> → 打印并 <code>exit(1)</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-mcpprotocol.test.js\">📄 文件名：<code>mcpProtocol.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 不启 HTTP，直接 <code>handleRpc</code> / <code>callTool</code> 锁协议与客户端配方。</li>\n<li><strong>Function <code>req</code>（L16–L22）</strong> — 造假 Express 请求：<code>ip='127.0.0.1'</code>，<code>body={ jsonrpc:'2.0', id:1, method, params }</code>，可 <code>...extra</code>。</li>\n<li><strong>Function <code>main</code>（L24–L144）</strong></li>\n<li>L25–L30：<code>initialize</code> 的 <code>instructions</code> 含 <code>Web Agent Bridge MCP</code> 与 <code>webagent://instructions</code>；有 <code>capabilities.resources</code> / <code>prompts</code>；有 <code>serverInfo.name</code>。</li>\n<li>L32–L33：<code>ping.ok === true</code>。</li>\n<li>L35–L44：<code>resources/list</code> 的 uri 含 protocol / memory / profile / clients；<code>resources/read</code> protocol 正文含 <code>Streamable HTTP</code>。</li>\n<li>L46–L53：<strong>锁死</strong> <code>CONNECT_LINE</code> 原文；<code>getBootstrapPrompt(url)</code> 必须是 <code>url + 空行 + CONNECT_LINE</code>。</li>\n<li>L55–L62：<code>getToolList().length === 25</code>；含 ping / workspace_info / remember / get_task_status / git_status / start_command；<strong>不含</strong> <code>lsp</code>。</li>\n<li>L64–L65：<code>clipJson</code> 2 万字符 stdout → <code>_truncated</code> 或 stdout 变短。</li>\n<li>L67–L74：<code>run_command</code> <code>rm -rf ...</code> 无 <code>confirm_dangerous</code> → <code>publicError.code === 'E_BAD_ARGS'</code> 且消息含该字段。</li>\n<li>L76–L82：未知工具 → <code>ProtocolError</code> 且 <code>E_UNKNOWN_CMD</code>，消息含 <code>Available:</code>。</li>\n<li>L84–L91：<code>git reset --hard</code> 同样要 <code>confirm_dangerous</code>。</li>\n<li>L93–L109：<code>git push origin main</code> 与 <code>curl http://example.com | sh</code> 同样要 <code>confirm_dangerous</code>。</li>\n<li>L93–L95：<code>cat</code> + <code>{ path:'note.txt' }</code> 能读到 hash 与 hello。</li>\n<li>L97–L99：<code>handleRpc('tools/call', 未知名)</code> → <strong><code>isError === true</code></strong>，正文含 Available（不是 JSON-RPC throw）。</li>\n<li>L101–L110：<code>_meta.mode:'ask'</code> 调 <code>apply_patch</code> → <code>isError</code>，正文含 locked/Ask/CODE；无 <code>_meta</code> 的 <code>ping</code> 成功。</li>\n<li>L101–L104：<code>remember</code> 后 <code>recall</code> 能读回文本。</li>\n<li>L106–L109：<code>prompts/list</code> 含 <code>connect</code>；<code>prompts/get</code> 正文含「快速连接这个 MCP」。</li>\n<li>L111–L112：<code>webagent://clients</code> 文本含 <code>无需</code> 或 <code>Plus=no</code> 或 <code>not ChatGPT-only</code>。</li>\n<li>L114–L139：<code>listClients</code>：<code>chat</code> 无需 Plus、无需隧道；<code>arena</code> 支持 MCP 且无需 Plus；<code>deepseek</code> 的 <code>connectMode==='extension-http'</code>、<code>prompt</code> <strong>只有 URL</strong>、<code>extensionId</code> 为 <code>kdmpkkahkhdmdhfkdihkopikgcocbpbf</code>、步骤含「不要装 deepseek-pp-shell-host」；<code>chat-plus</code> 同样 <code>extension-http</code>、<code>prompt</code> 只有 URL、<code>repoUrl</code> 为 <code>https://github.com/aiguicai/Chat-Plus</code>、步骤含「不要再装 aiguicai/MCP-Gateway」；<code>chatgpt-free</code> 为 <code>unsupported-mcp</code>；<code>chatgpt-plus</code> <code>needsPlus</code>。</li>\n<li>L141：删 tmp。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-workspacetools.test.js\">📄 文件名：<code>workspaceTools.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> git、skills、删/改名、Ask 锁、逃逸、敏感文件、异步命令。</li>\n<li><strong>Function <code>git</code>（L14–L17）</strong> — <code>spawnSync</code>，非 0 抛 stderr/stdout。</li>\n<li><strong>Function <code>pollOutput</code>（L19–L26）</strong> — 最多 20 次、间隔 50ms 调 <code>get_command_output</code>，直到 status 不是 <code>running</code>。</li>\n<li><strong>Function <code>main</code>（L28–L122）</strong></li>\n<li>L29–L37：名单含 git_status / start_command / delete_file / rename_file / load_skill；不含 lsp / get_diagnostics / <strong>send_command_input</strong>。</li>\n<li>L39–L50：<code>git init</code> + config + commit 后 <code>git_status</code>（ask）有 branch；改文件后 <code>git_diff</code>（plan）。</li>\n<li>L52–L57：写 <code>.webagent/skills/demo/SKILL.md</code>，<code>load_skill</code> 列表与全文。</li>\n<li>L59–L67：code 模式 <code>delete_file</code> / <code>rename_file</code> 成功。</li>\n<li>L69–L75：Ask 调 <code>delete_file</code> → <code>ProtocolError E_BAD_ARGS</code>。</li>\n<li>L77–L83：<code>../outside.txt</code> → 消息匹配 <code>outside workspace</code>。</li>\n<li>L85–L107：<code>.env</code> 与 <code>.webagent/config.json</code> 走 <code>read_files</code> 被拒（SENSITIVE|FORBIDDEN）；<code>.env.example</code> 可读；<code>list_directory</code> 不出现 <code>.env</code>。</li>\n<li>L109–L111：<code>workspace_info.root === tmp</code>。</li>\n<li>L113–L117：<code>start_command</code> <code>echo async-ok</code>，poll 后 status 为 done/timeout，stdout 含该字符串。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-sandbox.test.js\">📄 文件名：<code>sandbox.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 默认只听本机；符号链接不能读出工作区。</li>\n<li>L24：<code>config.host</code> 等于 <code>WEBAGENT_BIND</code> 或 <code>127.0.0.1</code>。</li>\n<li>L31–L58：若能 <code>symlinkSync</code> 把工作区 <code>leak</code> 指到临时目录外的 <code>secret.txt</code>，则 <code>read_files leak/secret.txt</code>、<code>run_command cwd=leak</code>、<code>resolveSafePath</code> 都须匹配 outside workspace；<code>list_directory</code> 正文不得出现 <code>secret.txt</code>。</li>\n<li>无法建符号链接的环境跳过那一段，仍测 host 默认值。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-tunnel.test.js\">📄 文件名：<code>tunnel.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 只测日志解析，不 spawn cloudflared。</li>\n<li>L4–L9：样例日志含 <code>https://random-words-ab12.trycloudflare.com</code>。</li>\n<li>L10：<code>parseTunnelUrl(sample)</code> 严格等于该 URL。</li>\n<li>L11：无 URL 文本 → <code>null</code>。</li>\n<li>L12：打印 passed。无 <code>main()</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-hostpersist.test.js\">📄 文件名：<code>hostPersist.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 密钥落盘与读哈希缓存跨重启。</li>\n<li><strong>Function <code>main</code>（L14–L71）</strong> — 同步。</li>\n<li>L15–L18：<code>generateNewSecret()</code> 后 <code>store.load().secretKey</code> 等于内存。</li>\n<li>L20–L23：把内存 secret 改成 <code>deadbeefdead</code> 再 <code>persistIdentity</code>，应回到磁盘值。</li>\n<li>L25–L30：<code>rememberHash</code> 写出 <code>.webagent/read-hashes.json</code>。</li>\n<li>L32–L34：<code>delete require.cache</code> 后再 require，仍能 <code>recalledHash</code>。</li>\n<li>L36–L43：<code>.webagent/.gitignore</code> 含 config.json 与 read-hashes.json；非 Windows 时 config.json mode <code>0600</code>。</li>\n<li>L45–L48：删掉嵌套 gitignore 后再 <code>persistIdentity</code> 会写回；无 <code>.git</code> 时不写工作区根 <code>.gitignore</code>。</li>\n<li>L50–L60：<code>git init</code> 后 <code>protectWorkspaceSecrets</code> 追加仓库根 ignore；<code>git check-ignore</code> 命中；再调一次不重复。</li>\n<li>L62–L63：本仓库根 <code>.gitignore</code> 含 <code>**/.webagent/config.json</code>。</li>\n<li>L65–L67：<code>resetHashes</code> 后内存与文件都空。</li>\n<li>L73：直接 <code>main()</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-bridgetunnel.test.js\">📄 文件名：<code>bridgeTunnel.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 测 REST 接线，<strong>不</strong> spawn cloudflared、不等 25s。替换 <code>cloudflared.js</code> 上同对象导出的 <code>startQuickTunnel</code> / <code>stopTunnel</code>（routes 已 require 该对象）。</li>\n<li><strong>Function <code>request</code>（L16–L45）</strong> — 对已 listen 的 server 发 HTTP，body 有则 JSON。</li>\n<li><strong>Function <code>main</code>（L47–L123）</strong></li>\n<li>L48–L61：保存原函数；stub start 写 <code>config.publicTunnelUrl</code>；stub stop 清 URL。</li>\n<li>L63–L68：express 挂 <code>/api</code>，<code>listen(0)</code>。</li>\n<li>L71–L79：<code>store.patch</code> 已登录；<code>POST /api/bridge/start</code> <code>{ tunnelProvider:'cloudflare' }</code> → 200、<code>startCalls===1</code>、mcpUrl 含 trycloudflare、note 含「Quick Tunnel 已就绪」、<code>tunnelError===null</code>。</li>\n<li>L81–L83：<code>GET /api/status</code> mcpUrl 仍含 trycloudflare，<code>bridgeRunning</code>。</li>\n<li>L85–L88：<code>POST /api/bridge/stop</code> → <code>stopCalls&gt;=1</code>、<code>publicTunnelUrl===null</code>。</li>\n<li>L90–L103：stub start throw <code>E_NO_CLOUDFLARED</code> → 仍 200、<code>success</code>、有 <code>tunnelError</code>、note 含「当前页面源」、mcpUrl <strong>不含</strong> trycloudflare。</li>\n<li>L105–L109：<code>tunnelProvider:'named'</code> → 200 且 <strong>不</strong>调 <code>startQuickTunnel</code>，note 含「未启动 Quick Tunnel」。</li>\n<li>L111–L113：未登录 → <strong>403</strong>。</li>\n<li>L114–L121：finally 还原导出、关 server、删 tmp。</li>\n<li>L125–L128：<code>main().catch</code> → <code>exit(1)</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-apifiles.test.js\">📄 文件名：<code>apiFiles.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 挂 <code>apiRouter</code>，测工作台写路径走 <code>write_file</code>（沙箱 / 敏感 / 原子写），不启完整 <code>index.js</code>。</li>\n<li><strong>Function <code>request</code>（L14–L45）</strong> — 对已 listen 的 server 发 HTTP，body 有则 JSON。</li>\n<li><strong>Function <code>main</code>（L46–L104）</strong></li>\n<li>L55–L62：<code>PUT /api/files/content</code> <code>notes.md</code> → 200、磁盘内容正确、无残留 <code>.tmp.</code>。</li>\n<li>L64–L69：写 <code>.env</code> 被拒，磁盘无该文件。</li>\n<li>L71–L75：<code>../outside.txt</code> 匹配 outside workspace。</li>\n<li>L77–L82：已有文件带错 <code>expectedHash</code> → 409 <code>STALE_FILE</code>，磁盘仍旧内容。</li>\n<li>L84–L88：<code>POST /api/skills</code> 写出 <code>.webagent/skills/demo-skill/SKILL.md</code>。</li>\n<li>L90–L93：<code>GET /api/files/content</code> 仍返回全文 + hash。</li>\n<li>L106–L109：<code>main().catch</code> → <code>exit(1)</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-localcontrol.test.js\">📄 文件名：<code>localControl.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 不启 HTTP，直接断言 <code>isLoopbackAddress</code> / <code>isTunnelRequest</code> / <code>isPublicHost</code> / <code>isLocalControlPlane</code>。</li>\n<li><strong>Function <code>req</code>（L9–15）</strong> — 造假 Express 请求：默认 <code>ip=127.0.0.1</code>、<code>Host=127.0.0.1:48271</code>，可叠 headers。</li>\n<li>L17–20：回环地址为真，<code>192.168.1.8</code> 为假。</li>\n<li>L22–24：<code>cf-ray</code> / <code>cf-connecting-ip</code> 为隧道；无头不是。</li>\n<li>L26–28：<code>*.trycloudflare.com</code> 为公网 Host；<code>127.0.0.1</code> / <code>localhost</code> 不是。</li>\n<li>L30–33：默认本机为真；带 Cloudflare 头、trycloudflare Host、或 <code>10.0.0.8</code> 为假。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-corsallow.test.js\">📄 文件名：<code>corsAllow.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 不启 HTTP，断言 Origin 白名单与 <code>/api</code> 跨站闸。</li>\n<li>L11–L15：本机 Origin / 扩展协议为真；聊天站不是 loopback。</li>\n<li>L17–L25：无 Origin、DeepSeek / ChatGPT / Gemini / Arena、扩展、本机 → MCP 放行；<code>https://evil.example</code> 拒绝。</li>\n<li>L28–L31：<code>/api</code> 只放行无 Origin 或本机 Origin。</li>\n<li>L33–L39：<code>WEBAGENT_CORS_ORIGINS</code> 追加 <code>doubao</code> / <code>tongyi</code> 后 MCP 放行，evil 仍拒绝。</li>\n<li>L54–L90：<code>rejectCrossSiteApi</code>：evil Origin / evil Referer → 404；本机 Origin 与无头 → <code>next()</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-httpsmoke.test.js\">📄 文件名：<code>httpSmoke.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 真起 <code>src/index.js</code>，打工作台 HTML、MCP status/401/initialize/tools/list/ping、OAuth 发现、隧道头挡 <code>/api</code>、工作台口 Chat NDJSON。</li>\n<li><strong>顶部 L8–L11：</strong> tmp；<code>hostDir</code>；随机 <code>workbenchPort</code>（18000+）、<code>mcpPort</code>（20000+）。</li>\n<li><strong>Function <code>request</code>（L13–49）</strong> — Node <code>http.request</code>，body 有则 JSON；第四参 <code>extraHeaders</code> 叠进请求头。</li>\n<li><strong>Function <code>waitHealth</code>（L51–68）</strong> — 轮询 GET 直到 200 或超时。</li>\n<li><strong>Function <code>stop</code>（L70–79）</strong> — win32 <code>taskkill /t /f</code>，否则 SIGTERM。</li>\n<li><strong>Function <code>main</code>（L81–257）</strong></li>\n<li>L82–89：spawn <code>src/index.js</code>，env 设 <code>WORKSPACE_ROOT=tmp</code>、<code>WORKBENCH_PORT</code>、<code>AGENT_HOST_PORT</code>。</li>\n<li>L111–113：health JSON <code>ok</code> 且 <code>product==='Web Agent'</code>。</li>\n<li>L115–135：GET <code>/</code> HTML 必须含：<code>Web Agent</code>；<code>编辑进化</code> 或 <code>CHAT</code>；<code>Add API</code>；<code>btn-agent-pick</code>；<code>agent-pick-menu</code>；<code>Web Agent Code</code>；<code>环境偏好</code>；<code>技术栈</code>；<code>技能引导</code>；<code>怎么连到本机仓库</code>；<code>无需 Plus</code> 或 <code>不需要 Plus</code>；<code>打开 DeepSeek</code>；<code>data-site=&quot;deepseek&quot;</code>；<code>id=&quot;page-env&quot;</code> / <code>btn-detect-env</code> / <code>page-stack</code> / <code>btn-detect-stack</code>；<code>type=&quot;module&quot;</code> 与 <code>/app.js</code>。</li>\n<li>L137–142：GET <code>/app.js</code> 含 <code>from './js/state.js'</code>；GET <code>/js/state.js</code> 含 <code>export const state</code>。</li>\n<li>L144–152：GET <strong>mcp 端口</strong> <code>/api/status</code>（本机无隧道头）：有 <code>secretKey</code>；<code>prompt</code> 含「快速连接这个 MCP…」整句；<code>tools.length===25</code>；clients 含 arena（无需 Plus）、deepseek（<code>extension-http</code>、支持 MCP、无需 Plus）与 chat-plus；<code>mcpCanonicalUrl</code> 以 <code>/mcp</code> 结尾。</li>\n<li>L154–160：错误 secret POST initialize → 401。</li>\n<li>L162–170：正确 secret initialize 200，instructions 含 Bridge MCP 与 <code>webagent://instructions</code>。</li>\n<li>L172–183：tools/list 25 个且含 apply_patch / start_command / workspace_info。</li>\n<li>L185–191：<code>POST /mcp</code> 无密钥 → 401。</li>\n<li>L193–195：GET <code>/.well-known/oauth-authorization-server</code> 200，有 <code>authorization_endpoint</code>。</li>\n<li>L197–205：tools/call ping 成功，<code>isError===false</code>。</li>\n<li>L207–211：本机 <code>POST /api/bridge/reset-round</code> 仍 200。</li>\n<li>L214–223：带 <code>cf-ray</code> 的 mcp 口 <code>/api/status</code>、<code>/api/chat</code>、<code>/api/tool/call</code> 以及 <code>Host: *.trycloudflare.com</code> 的 <code>/api/status</code> 都 <strong>404</strong>，正文不含 secret。</li>\n<li>L225–231：同一组隧道头 <code>POST /mcp/&lt;secret&gt;</code> initialize 仍 200。</li>\n<li>L233–270：<code>Origin: https://evil.example</code> 打 mcp <code>/api/status</code> 与工作台 <code>reset-round</code> 都 404；本机 Origin 的 <code>/api/status</code> 200；OPTIONS <code>/mcp</code> 对 evil 无 ACAO，对 <code>chat.deepseek.com</code> 与 DeepSeek++ 扩展 Origin 回相同 ACAO。</li>\n<li>L272–273：mcp 口 GET <code>/</code> 正文不得含 <code>btn-agent-pick</code>（没有工作台静态）。</li>\n<li>L236–249：<strong>工作台口</strong> <code>POST /api/chat</code> NDJSON 必须有 <code>tool</code>（<code>list_directory</code>）、<code>message</code>、<code>done</code>。</li>\n<li>L259–262：finally <code>stop</code> 子进程，等 300ms，删 tmp。</li>\n</ul>\n<h3 id=\"文件名-codeservernotrunnable.test.js\">📄 文件名：<code>codeServerNotRunnable.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> Git <strong>不得</strong>内嵌 <code>bin/code-server-dist</code>；主启动脚本不得拉 code-server。</li>\n<li>L5：<code>repoRoot</code> = tests 上三级（仓库根）。</li>\n<li>L6–L8：读 <code>ensure-code-server.js</code> 与 <code>run-code-oss.js</code> 原文。</li>\n<li>L10：<code>bin/code-server-dist</code> 不存在。</li>\n<li>L12–L17：拼接 <code>run-webagent.cmd</code> + <code>.sh</code>，正则 <strong>不得</strong> 匹配 <code>code-server</code>；必须匹配 <code>agent-host</code>。</li>\n<li>L19–L28：<code>run-webagent-vscode.cmd</code> 与 <code>run-code-oss.js</code> 存在；ensure 含 <code>bin/code-server-runtime</code>，且含 <code>code-server@4.135.0</code> 或 <code>'code-server': VERSION</code>；ensure/runner 都不含 <code>code-server-dist</code>；runner <code>require('./codeServerAuth')</code>；不得写死 <code>'--auth', 'none'</code> 与 <code>trusted-origins *</code>。</li>\n<li>无 async <code>main</code>。</li>\n</ul>\n<h3 id=\"文件名-codeserverauth.test.js\">📄 文件名：<code>codeServerAuth.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 测 <code>scripts/codeServerAuth.js</code>，不 spawn code-server。</li>\n<li>L10–L11：<code>trustedOrigins(3000/8080)</code> 只有 127.0.0.1 与 localhost。</li>\n<li>L13–L20：空 env 生成口令并写入 tmp/<code>webagent-password</code>；再调一次同一串。</li>\n<li>L22–L24：<code>CODE_SERVER_PASSWORD</code> 覆盖文件。</li>\n<li>L26–L28：<code>CODE_SERVER_AUTH=none</code> → <code>mode:'none'</code>、password null。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-skipworkbench.test.js\">📄 文件名：<code>skipWorkbench.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <code>WEBAGENT_SKIP_WORKBENCH=1</code> 时 MCP health 通，工作台端口无人听。</li>\n<li><strong>Function <code>get</code>（L12–L21）</strong> / <strong><code>waitOk</code>（L23–L39）</strong> — HTTP 轮询。</li>\n<li><strong>Function <code>main</code>（L41–L74）</strong></li>\n<li>L42–L52：spawn index.js，<code>AGENT_HOST_PORT=mcpPort</code>，<code>WORKBENCH_PORT='19999'</code>，<code>SKIP=1</code>。</li>\n<li>L54–L56：mcp 端口 <code>/health</code> 200。</li>\n<li>L57–L62：GET <code>127.0.0.1:19999/health</code> 若成功回调则 <strong>reject</strong>（端口应空闲）；<code>error</code> 事件才 resolve。</li>\n<li>L64–L71：finally taskkill/SIGTERM，删 tmp。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-runchat.test.js\">📄 文件名：<code>runChat.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 内置 Chat 对<strong>任意</strong>临时工作区搜-读-测；不依赖 calculator.js；不调 get_diagnostics；同时锁第二参 emit 与 <code>payload.emit</code>。</li>\n<li><strong>Function <code>collect</code>（L12–L16）</strong> — <code>{ events, emit }</code>，emit 把 <code>{type,...data}</code> 推进数组。</li>\n<li><strong>Function <code>main</code>（L18–L85）</strong></li>\n<li>L19–L34：写 README（标题 Widget）、<code>src/app.js</code> greet、<code>package.json</code> scripts.test、<code>tests/app.test.js</code>。</li>\n<li>L36–L46 <strong>ask「分析当前项目」：</strong> 第二参 <code>ask.emit</code>；工具含 list_directory / find_files / read_files；禁止 diagnostics / apply_patch；message 匹配 README|Widget|app.js；整段事件 JSON <strong>不含</strong> <code>calculator.js</code>；有 label 匹配 <code>Found N files</code>。</li>\n<li>L48–L53 <strong>plan：</strong> 有 <code>consensus.result.consensusReached</code>；无 calculator.js；有 <code>set_todos</code>。</li>\n<li>L55–L61 <strong>code「跑测试」：</strong> 有 <code>run_command</code> 且 <code>ok</code>；message 含 <code>npm test</code> 或 <code>ok</code>。</li>\n<li>L63–L70 <strong>payload.emit：</strong> <code>runChat({ mode:'ask', message, emit })</code> 无第二参，仍须有 <code>list_directory</code> 与 <code>message</code>。</li>\n<li>L72–L81 <strong>code 写入 notes.md + 围栏：</strong> 磁盘出现该文件且含 <code>hello from agent</code>。</li>\n</ul>\n<h3 id=\"文件名-chatmode.test.js\">📄 文件名：<code>chatMode.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <strong>复制</strong>插件里的 <code>modeFromChatRequest</code> 逻辑并断言默认 Agent=code（本文件不 <code>require</code> extension.js）。</li>\n<li><strong>Function <code>modeFromChatRequest</code>（L3–L11）</strong> — <code>request.command</code> 小写若为 ask/plan/code 则用之；否则看 <code>prompt</code> 是否 <code>/ask|/plan|/code</code> 前缀；否则 <code>'code'</code>。</li>\n<li>L13–L17：command ask/plan、<code>/ask 这是什么</code>、普通「修复测试」、空对象 → 分别 ask/plan/ask/code/code。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-profile.test.js\">📄 文件名：<code>profile.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 环境/技术栈写入 <code>.webagent</code>，并进入 instructions 与 <code>webagent://profile</code>。</li>\n<li><strong>Function <code>main</code>（L21–L68）</strong> — <strong>同步</strong>，末尾 L70 直接 <code>main()</code>（无 catch）。</li>\n<li>L22–L24：<code>detectEnvironment</code> 的 os ∈ windows/macos/linux；shell ∈ powershell/bash。</li>\n<li>L26–L34：临时 package.json + express → JS / Express / npm / <code>npm test</code>。</li>\n<li>L36–L44：<code>saveCustom</code> 后存在 <code>.webagent/preference.md</code> 与 <code>tech-stack.md</code>；markdown 含 PowerShell 与 <code>npm test</code>。</li>\n<li>L46–L62：skill <code>review</code> 出现在 <code>formatWorkspaceContext</code> 与 <code>getInstructions</code>。</li>\n<li>L64–L65：<code>readResource('webagent://profile')</code> 正文含 <code>Tech stack</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-oauth.test.js\">📄 文件名：<code>oauth.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 真 listen 随机端口：发现文档、401、URL 密钥 initialize、SSE ping、PKCE 发 token、Bearer tools/call、revoke 后 401。</li>\n<li><strong>Function <code>request</code>（L16–L47）</strong> — 相对已 listen 的 server；<code>json=true</code> 发 JSON，否则 urlencoded。</li>\n<li><strong>Function <code>main</code>（L49–L153）</strong></li>\n<li>L50–L57：express 挂 <code>oauth.router</code> + <code>/mcp</code>；<code>listen(0)</code>。</li>\n<li>L60–L63：GET <code>/.well-known/oauth-authorization-server</code> 200；<code>authorization_endpoint</code> 含 <code>/oauth/authorize</code>；<code>code_challenge_methods_supported</code> 含 <code>S256</code>。</li>\n<li>L65–L67：GET <code>/.well-known/oauth-protected-resource</code> 200；<code>resource</code> 以 <code>/mcp</code> 结尾。</li>\n<li>L69–L73：无凭证 POST <code>/mcp</code> → 401 且 <code>WWW-Authenticate</code> 含 <code>resource_metadata=</code>。</li>\n<li>L75–L79：<code>POST /mcp/${config.secretKey}</code> initialize 200，instructions 含 Bridge MCP。</li>\n<li>L81–L87：同一 URL 密钥 + <code>Accept: text/event-stream</code> 的 ping → content-type 含 event-stream，正文含 <code>event: message</code>。</li>\n<li>L89–L118：<code>s256</code> + <code>registerClient</code> + <code>issuePairing</code> + <code>completeAuthorize</code> + <code>handleToken</code>；access 以 <code>scat_</code> 开头。</li>\n<li>L120–L132：坏 code → catch，<code>pkceFailed</code> true。</li>\n<li>L134–L140：Bearer 调 <code>workspace_info</code> 成功，<code>isError===false</code>。</li>\n<li>L142–L147：<code>revokeAll</code> 后同一 Bearer ping → 401。</li>\n<li>L148–L151：close server，删 tmp。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录\">3. 执行逻辑流（仅本目录）</h2>\n<ol>\n<li><code>npm test</code> 按 <code>package.json</code> <code>scripts.test</code> 顺序 <code>&amp;&amp;</code>：patchEngine → mcpProtocol → workspaceTools → <strong>sandbox</strong> → <strong>hostPersist</strong> → tunnel → <strong>bridgeTunnel</strong> → <strong>apiFiles</strong> → <strong>localControl</strong> → <strong>corsAllow</strong> → httpSmoke → codeServerNotRunnable → <strong>codeServerAuth</strong> → skipWorkbench → runChat → chatMode → profile → oauth。</li>\n<li>单文件：改 <code>config.workspaceRoot</code> 指向 tmp → require 被测模块 → assert → 删 tmp。<code>tunnel</code> / <code>chatMode</code> / <code>codeServerNotRunnable</code> 不改工作区。<code>bridgeTunnel</code> 改 tmp 工作区并 stub 隧道导出。</li>\n<li>启进程的测试 spawn <code>src/index.js</code>，结束必须杀子进程。</li>\n<li>失败路径：有 <code>main()</code> 的文件走 <code>main().catch</code> → <code>exit(1)</code>；<code>profile.test.js</code> 同步抛错由 Node 非 0 退出；CMD 的 <code>run-tests.cmd</code> 据此 pause。</li>\n</ol>"},"workbench":{"id":"workbench","path":"webagent-core/workbench/README.md","group":"界面","title":"workbench 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：index.html","id":"文件名-index.html"},{"level":3,"text":"📄 文件名：app.js","id":"文件名-app.js"},{"level":3,"text":"📄 文件名：js/state.js","id":"文件名-js/state.js"},{"level":3,"text":"📄 文件名：js/dom.js","id":"文件名-js/dom.js"},{"level":3,"text":"📄 文件名：js/tabs.js","id":"文件名-js/tabs.js"},{"level":3,"text":"📄 文件名：js/chat.js","id":"文件名-js/chat.js"},{"level":3,"text":"📄 文件名：js/bridge.js","id":"文件名-js/bridge.js"},{"level":3,"text":"📄 文件名：js/settings.js","id":"文件名-js/settings.js"},{"level":3,"text":"📄 文件名：js/bind.js","id":"文件名-js/bind.js"},{"level":3,"text":"📄 文件名：styles.css","id":"文件名-styles.css"},{"level":3,"text":"📄 文件名：favicon.svg","id":"文件名-favicon.svg"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"workbench-模块说明书\">workbench 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/workbench/</code></p>\n<p>浏览器里的自绘工作台。静态文件由 <code>../agent-host/src/index.js</code> 用 <code>express.static</code> 挂出。本目录 <strong>不直接 fs</strong>；所有读写经 <code>/api/*</code> 与 <code>/ws</code>。</p>\n<p>文件：<code>index.html</code>、<code>app.js</code>（入口）、<code>js/*.js</code>（按职责拆开的交互）、<code>styles.css</code>、<code>favicon.svg</code>。无打包；浏览器原生 ES module。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> UI 组件壳（欢迎页、编辑器、CHAT、BRIDGE、设置模态）。真正改盘在 agent-host。</li>\n<li><strong>依赖的兄弟模块：</strong> 运行时 HTTP 依赖 <code>agent-host</code> 的 <code>/api</code>、<code>/mcp</code>（仅内置演示 <code>arenaConnect</code>）、<code>/ws</code>。源码上不 require Node 模块。</li>\n<li><strong>谁调用：</strong> 用户浏览器打开 <code>http://127.0.0.1:3000</code>。<code>run-webagent-vscode</code> 跳过本目录（<code>WEBAGENT_SKIP_WORKBENCH=1</code>）。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-index.html\">📄 文件名：<code>index.html</code></h3>\n<ul>\n<li><strong>文件职责：</strong> DOM 骨架。逻辑在 <code>app.js</code> + <code>js/*.js</code>。</li>\n<li><strong>DOM 结构区块：</strong></li>\n<li>L3–L8：charset、viewport、title、favicon、<code>/styles.css</code>。</li>\n<li>L11–L28 <strong>标题栏 <code>#titlebar</code>：</strong> 文件/编辑/选择/查看/转到/运行菜单（多数按钮无 JS）；<code>#menu-term</code> 终端、<code>#menu-help</code> 帮助；中间 <code>#window-title</code>。</li>\n<li>L31–L54 <strong>活动栏 <code>#activitybar</code>：</strong> <code>data-left=explorer|search</code> 有 JS；SCM/调试/扩展按钮无 handler；底栏 <code>#btn-account</code> 无登录实现、<code>#btn-manage</code> 打开管理菜单。</li>\n<li>L56–L70 <strong>左侧栏 <code>#sidebar</code>：</strong> 默认 <code>collapsed</code>。<code>#left-explorer</code> 文件树；<code>#left-search</code> 搜索框。</li>\n<li>L72–L157 <strong>中间 <code>#center</code>：</strong></li>\n<li>L73 <code>#tabs</code> 标签条。</li>\n<li>L75–L112 <code>#welcome</code> 欢迎页：新建/打开文件/打开文件夹（后两个只点开资源管理器）；「连接到」「生成新工作区」无 handler；<code>#recent-list</code>；<code>#walk-basics</code> 打开设置；<code>#btn-agent-window</code>。</li>\n<li>L113–L114 Monaco <code>#editor</code> 与 fallback textarea。</li>\n<li>L115–L123 <code>#browser</code> 内置假浏览器（地址栏、<code>#browser-page</code>）。</li>\n<li>L125–L140 <code>#agent-pane</code> 智能体窗口（独立输入框与 Ask/Plan/Code）。</li>\n<li>L142–L145 <code>#diff-pane</code> 补丁对比。</li>\n<li>L147–L157 <code>#panel</code> 终端，默认 hidden。</li>\n<li>L160–L223 <strong>右侧 <code>#rightbar</code>：</strong></li>\n<li>L161–L164 CHAT / BRIDGE 页签。</li>\n<li>L165–L192 <code>#right-chat</code>：流、Tasks、chips 快捷句、<code>#chat-input</code>、<code>#btn-agent-pick</code>、隐藏 <code>#mode-select</code>、<code>#model-select</code>、发送。</li>\n<li>L193–L222 <code>#right-bridge</code>：等待文案、任务、log、MCP session。L204–L221 <code>.mcp-session</code>：<code>#btn-reset-round</code>（清除本轮统计）、<code>#btn-stop-bridge-rb</code>、<code>#stat-calls</code> / <code>#stat-avg</code> / <code>#stat-fail</code> / <code>#stat-ok</code>。</li>\n<li>L226–L235 <code>#statusbar</code>。</li>\n<li>L238–L569 <strong><code>#modal</code> 设置：</strong> 左侧 nav 多页（概述/环境/技术栈/智能体/技能/指令/提示/挂钩/MCP/Bridge/插件/API/Codex/多模型）。<strong><code>#page-env</code> / <code>#page-stack</code> 有完整表单</strong>（<code>#btn-detect-env</code>、<code>#btn-save-env</code>、<code>#btn-detect-stack</code>、<code>#btn-save-stack</code>）。Bridge 页含客户端卡片、复制 URL/提示词、打开各站点、GitHub 登录演示、隧道 radio（cloudflare 默认；named/ngrok 输入框 <strong>无对应 JS 去 spawn</strong>）。警告文案写明隧道不转发 <code>/api</code>。<code>#btn-reset-secret</code> 在高级设置。</li>\n<li>L518–L537 下拉：<code>#file-menu</code>、<code>#manage-menu</code>、<code>#agent-pick-menu</code>。</li>\n<li>L538 <code>#toast</code>；L539 <code>&lt;script type=&quot;module&quot; src=&quot;/app.js&quot;&gt;</code>（原生 ES module，无打包）。</li>\n</ul>\n<p>跨模块调用走 <code>js/state.js</code> 的 <code>ui</code> 袋（避免 import 环），<strong>不改</strong> <code>/api</code> 与按钮行为。</p>\n<hr />\n<h3 id=\"文件名-app.js\">📄 文件名：<code>app.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 工作台入口。import <code>js/*.js</code> 后 <code>boot</code>。71 行。</li>\n<li><strong>Function <code>connectWs</code>（L9–L32）</strong> — <code>ws(s)://location.host/ws</code>；<code>command_output</code> → <code>ui.termLine</code>；<code>file_patched</code> → <code>ui.loadTree</code>；<code>todos_updated</code> → <code>ui.paintTodos</code>；<code>tool_call_end</code> → <code>ui.logBridgeTool</code>。</li>\n<li><strong>Function <code>loadMonaco</code>（L34–L58）</strong> — jsDelivr monaco 0.52.2；<code>window.monaco.editor.create</code>；onerror 或 7s 超时。</li>\n<li><strong>Function <code>boot</code>（L60–L69）</strong> — <code>ui.bind</code>、默认 code、并行 refresh/tree/skills/custom/monaco、WS、welcome。L71 <code>boot().catch(console.error)</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/state.js\">📄 文件名：<code>js/state.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <code>$</code> / <code>$$</code>、外链 <code>SITES</code>、共享 <code>state</code>、空对象 <code>ui</code>。无函数。L1–L35。</li>\n<li><strong><code>state</code> 初值：</strong> <code>mode:'code'</code>、<code>tabs</code> 仅 welcome、<code>stats</code>、<code>loggedIn:true</code>、<code>selectedClient:'arena'</code>、<code>stayOnBridge:false</code>。</li>\n<li><strong><code>SITES</code>：</strong> chatgpt/arena/deepseek/workbuddy/trae/qwen/manus/shunova 的外链。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/dom.js\">📄 文件名：<code>js/dom.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> toast / 转义 / 极简 markdown / 终端行 / 模态 / 右侧页签。</li>\n<li><strong>Function <code>toast</code>（L3–L9）</strong> — 显示 2.2s。</li>\n<li><strong>Function <code>escapeHtml</code>（L11–L15）</strong> — 五字符。</li>\n<li><strong>Function <code>renderMd</code>（L17–L27）</strong> — escape 后再 fence/inline/bold/标题/列表/<code>&lt;br&gt;</code>。</li>\n<li><strong>Function <code>termLine</code>（L29–L36）</strong>。</li>\n<li><strong>Function <code>openModal</code> / <code>closeModal</code> / <code>showPage</code>（L38–L47）</strong>。</li>\n<li><strong>Function <code>setRight</code>（L49–L54）</strong> — chat/bridge。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/tabs.js\">📄 文件名：<code>js/tabs.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 标签、文件树、打开文件、Monaco/fallback、保存。</li>\n<li><strong>Function <code>paintTabs</code>（L4–L19）</strong> / <strong><code>activateTab</code>（L21–L35）</strong> — kind=welcome/browser/agent/diff/file；browser 调 <code>ui.renderBrowser</code>。</li>\n<li><strong>Function <code>closeTab</code>（L37–L42）</strong> — 只剩 1 个则 return。</li>\n<li><strong>Function <code>openAgentWindow</code>（L44–L53）</strong> / <strong><code>openDiff</code>（L55–L65）</strong> / <strong><code>paintDiff</code>（L67–L76）</strong> — <code>+</code> 非 <code>+++</code> 绿。</li>\n<li><strong>Function <code>ensureWelcome</code>（L78–L83）</strong>。</li>\n<li><strong>Function <code>openFile</code>（L85–L95）</strong> — GET <code>/api/files/content</code>。</li>\n<li><strong>Function <code>langFor</code>（L97–L104）</strong> / <strong><code>applyEditor</code>（L106–L113）</strong> — <code>window.monaco</code>。</li>\n<li><strong>Function <code>treeHtml</code>（L115–L126）</strong> / <strong><code>loadTree</code>（L128–L162）</strong> — GET <code>/api/files/tree</code>；recent 最多 6。</li>\n<li><strong>Function <code>saveActive</code>（L163–L174）</strong> — 仅 file tab PUT。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/chat.js\">📄 文件名：<code>js/chat.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 本机 CHAT 流。</li>\n<li><strong>Function <code>emptyChat</code>（L4–L16）</strong> / <strong><code>paintChat</code>（L18–L34）</strong>。</li>\n<li><strong>Function <code>summarizeTool</code>（L36–L42）</strong> / <strong><code>renderMsg</code>（L44–L93）</strong> — user/status/tool/consensus（采纳则 <code>ui.setAgentMode('code')</code> 并 <code>ui.sendChat</code> 固定句）/assistant。</li>\n<li><strong>Function <code>pushMsg</code>（L95–L102）</strong>。</li>\n<li><strong>Function <code>sendChat</code>（L104–L152）</strong> — POST <code>/api/chat</code> NDJSON；parse 失败 continue；finally <code>ui.refreshStatus</code> + <code>ui.loadTree</code>。</li>\n<li><strong>Function <code>handleEvent</code>（L154–L188）</strong> — tool 可 <code>ui.logBridgeTool</code>；set_todos；run_command 进终端；apply_patch 刷新并 <code>ui.openDiff</code>。</li>\n<li><strong>Function <code>paintTodos</code>（L190–L207）</strong> / <strong><code>agentLabel</code>（L209–L212）</strong> / <strong><code>setAgentMode</code>（L214–L220）</strong>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/bridge.js\">📄 文件名：<code>js/bridge.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> Bridge 启停、客户端卡、内置假浏览器。<strong>不是云上 Arena。</strong></li>\n<li><strong>Function <code>logBridgeTool</code>（L4–L20）</strong> / <strong><code>paintStats</code>（L22–L29）</strong> / <strong><code>resetRound</code>（L31–L43）</strong> — POST <code>/api/bridge/reset-round</code>。</li>\n<li><strong>Function <code>selectedClientInfo</code>（L45–L48）</strong> / <strong><code>promptText</code>（L50–L55）</strong> / <strong><code>paintClients</code>（L57–L89）</strong> — 无 prompt 则拼 CONNECT_LINE；配对码仅 <code>pair.code &amp;&amp; bridgeRunning</code>。</li>\n<li><strong>Function <code>renderBrowser</code>（L91–L139）</strong> — arena/chatgpt 走 <code>arenaConnect</code>；deepseek <strong>不调 MCP</strong>。</li>\n<li><strong>Function <code>arenaConnect</code>（L141–L170）</strong> — 本机 <code>/mcp/${secret}</code> initialize/tools/list/resources/read，再 <code>ui.sendChat(..., { stayOnBridge:true })</code>。</li>\n<li><strong>Function <code>openSite</code>（L172–L190）</strong>。</li>\n<li><strong>Function <code>startBridge</code>（L192–L208）</strong> / <strong><code>stopBridge</code>（L210–L214）</strong> / <strong><code>paintBridge</code>（L216–L257）</strong> — POST start/stop；按 <code>s.tunnel.url</code> 显示隧道或「走当前页面源」。</li>\n<li><strong>Function <code>refreshStatus</code>（L259–L271）</strong> — GET <code>/api/status</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/settings.js\">📄 文件名：<code>js/settings.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 自定义设置、API Provider 表、skills 列表。</li>\n<li><strong>Function <code>rowList</code>（L4–L7）</strong> / <strong><code>paintCustom</code>（L9–L71）</strong>。</li>\n<li><strong>Function <code>paintProviderTable</code>（L73–L111）</strong> — radio 改 <code>activeModelId</code>。</li>\n<li><strong>Function <code>loadCustomizations</code>（L113–L117）</strong> / <strong><code>saveCustom</code>（L119–L129）</strong> — GET/PUT <code>/api/customizations</code>。</li>\n<li><strong>Function <code>loadSkills</code>（L131–L139）</strong> — GET <code>/api/skills</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-js/bind.js\">📄 文件名：<code>js/bind.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 全部 DOM 事件。闭包内 <code>skillMarkdown</code> / <code>SKILL_TPL</code> / <code>fillSkillPreview</code> / <code>probeProvider</code>（不导出）。</li>\n<li><strong>Function <code>onClick(id, handler)</code>（L4–8）</strong> — 节点不存在则跳过，避免 <code>null.onclick</code> 把整个 <code>boot</code> 打断。</li>\n<li><strong>Function <code>bind</code>（L10–L475）</strong> — 活动栏、菜单、发送、Enter、Bridge、复制（extension-http toast 不同）、reset-secret、GitHub 登录演示、各 <code>ui.saveCustom</code>、技能模板、环境/技术栈探测与保存（<code>onClick</code> 守卫）、probe/Add API（排除 modelId 匹配 video|image 当默认）、终端 <code>POST /api/tool/call</code> <code>run_command</code> mode code、搜索 <code>search_files</code> mode ask、Ctrl/Cmd+S。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-styles.css\">📄 文件名：<code>styles.css</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 深色 VS Code 风布局。无 JS。</li>\n<li><strong>区块：</strong></li>\n<li>L1–L24 <code>:root</code> 色板与尺寸（<code>--right:356px</code> 右侧栏宽）。</li>\n<li>L25–L29 全局；<code>.hidden { display:none !important }</code>。</li>\n<li>L31–L44 标题栏。</li>\n<li>L46–L72 活动栏与侧栏文件树。</li>\n<li>L74–L123 标签、编辑器、欢迎页。</li>\n<li>L125–L166 内置浏览器与 Arena/generic 仿页。</li>\n<li>L168–L177 终端面板。</li>\n<li>L179–L277 右侧 CHAT 消息/工具卡/共识/composer。</li>\n<li>L279–L291 BRIDGE 等待与统计。</li>\n<li>L293–L297 状态栏。</li>\n<li>L299–L352 设置模态与表单。</li>\n<li>L354–L387 Bridge 药丸、URL 盒、隧道卡片。</li>\n<li>L389–L424 菜单、toast、diff 色、下拉、客户端卡、ChatGPT 仿页、滚动条、<code>@media max-width 980px</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-favicon.svg\">📄 文件名：<code>favicon.svg</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 标签页图标。L1–L5 简单几何 SVG，无脚本。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>浏览器 GET <code>/</code> → SPA 回退 <code>index.html</code> → <code>type=module</code> 加载 <code>/app.js</code> → import <code>js/*.js</code>。</li>\n<li><code>boot</code> 拉 <code>/api/status</code>、文件树、skills、customizations，尝试 Monaco。</li>\n<li>用户 CHAT → <code>sendChat</code> → NDJSON <code>/api/chat</code> → <code>handleEvent</code> 画卡。</li>\n<li>启动 Bridge → POST <code>/api/bridge/start</code>（cloudflare 会 <code>await startQuickTunnel</code>）→ toast <code>note</code> → <code>paintBridge</code> 按 <code>s.tunnel.url</code> 显示 Quick Tunnel 或「走当前页面源」。</li>\n<li>复制提示词读 <code>clients[].prompt</code>（hydrate 在服务端）。</li>\n<li><code>/ws</code> 把远程 MCP 工具调用画到 BRIDGE。</li>\n<li>「清除本轮统计」→ POST <code>/api/bridge/reset-round</code>（清 session 计数 + 读哈希缓存）并清空右侧 log。</li>\n<li>内置「打开 Arena」只是本机演示：先打本机 <code>/mcp</code>，再走 <code>/api/chat</code>。</li>\n</ol>"},"extension":{"id":"extension","path":"webagent-core/extension/README.md","group":"界面","title":"extension 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：package.json","id":"文件名-package.json"},{"level":3,"text":"📄 文件名：extension.js","id":"文件名-extension.js"},{"level":3,"text":"📄 文件名：resources/icon.svg","id":"文件名-resources/icon.svg"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"extension-模块说明书\">extension 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/extension/</code></p>\n<p>VS Code / code-server 插件源码。侧栏 Chat、Bridge、原生 Chat <code>@webagent</code>。工具实现仍在 agent-host，本目录只做 HTTP 客户端 + webview HTML 字符串。</p>\n<p>文件：<code>extension.js</code>、<code>package.json</code>、<code>resources/icon.svg</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 网页 VS Code 的 UI 插件。<code>scripts/ensure-code-server.js</code> 的 <code>syncExtension</code> 会把本目录拷到 <code>extensions-installed/</code>。</li>\n<li><strong>依赖：</strong> VS Code API（<code>vscode</code>）；Node <code>http</code>/<code>https</code>/<code>path</code>。运行时打 <code>agentHostUrl()</code>（默认 <code>http://127.0.0.1:48271</code>）。</li>\n<li><strong>谁调用：</strong> code-server 加载插件后 <code>activate</code>。自绘工作台 <strong>不加载</strong> 本目录。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-package.json\">📄 文件名：<code>package.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 插件清单。code-server 用它注册 Chat 参与者、侧栏、命令。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>name</code></td>\n<td>npm/插件 id 段</td>\n<td><code>webagent-core</code></td>\n</tr>\n<tr>\n<td><code>displayName</code></td>\n<td>市场显示名</td>\n<td><code>Web Agent &amp; Bridge</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td>简介</td>\n<td>说明连本地 agent-host</td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td>版本</td>\n<td><code>0.6.9</code>（与 syncExtension 目标目录名一致）</td>\n</tr>\n<tr>\n<td><code>publisher</code></td>\n<td>发布者</td>\n<td><code>webagent</code></td>\n</tr>\n<tr>\n<td><code>engines.vscode</code></td>\n<td>最低 VS Code</td>\n<td><code>^1.90.0</code></td>\n</tr>\n<tr>\n<td><code>categories</code></td>\n<td>分类</td>\n<td>Other、Chat</td>\n</tr>\n<tr>\n<td><code>activationEvents</code></td>\n<td>何时激活</td>\n<td><code>*</code> 与 <code>onChatParticipant:webagent.agent</code></td>\n</tr>\n<tr>\n<td><code>main</code></td>\n<td>入口</td>\n<td><code>./extension.js</code></td>\n</tr>\n<tr>\n<td><code>contributes.configuration.properties.webagent.agentHostUrl</code></td>\n<td>可配置 host</td>\n<td>类型 string，默认 <code>http://127.0.0.1:48271</code></td>\n</tr>\n<tr>\n<td><code>contributes.chatParticipants[0].id</code></td>\n<td>参与者 id</td>\n<td><code>webagent.agent</code></td>\n</tr>\n<tr>\n<td><code>.fullName</code> / <code>.name</code></td>\n<td>UI 名</td>\n<td>Web Agent / webagent（<code>@webagent</code>）</td>\n</tr>\n<tr>\n<td><code>.isSticky</code> / <code>.isDefault</code></td>\n<td>粘滞、默认</td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>.commands</code></td>\n<td>slash</td>\n<td><code>ask</code> / <code>plan</code> / <code>code</code></td>\n</tr>\n<tr>\n<td><code>viewsContainers.activitybar</code></td>\n<td>活动栏容器</td>\n<td>id <code>webagent-sidebar</code>，icon <code>resources/icon.svg</code></td>\n</tr>\n<tr>\n<td><code>views.webagent-sidebar</code></td>\n<td>两个 webview</td>\n<td><code>webagent.chatView</code>、<code>webagent.bridgeView</code></td>\n</tr>\n<tr>\n<td><code>commands</code></td>\n<td>命令面板</td>\n<td><code>webagent.openBridge</code>、<code>openAgentChat</code>、<code>resetSecret</code></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h3 id=\"文件名-extension.js\">📄 文件名：<code>extension.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> activate 注册侧栏、Chat 参与者、状态栏；webview HTML 内嵌在本文件。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>agentHostUrl()</code>（L6–L9）</strong> — 配置 <code>webagent.agentHostUrl</code> 或 env <code>WEBAGENT_AGENT_HOST_URL</code> 或默认 48271，去尾 <code>/</code>。</li>\n<li><strong>Function <code>requestJson(method, url, body)</code>（L11–L43）</strong> — 按协议选 http/https。结束 try JSON.parse；失败 <code>{ json:null, raw }</code>。<code>req.on('error', reject)</code>。</li>\n<li><strong>Function <code>postNdjson(url, body, onEvent)</code>（L45–L85）</strong> — <strong>只用 <code>http.request</code></strong>（https URL 不会走 https 模块）。按行 parse，失败忽略；结束处理残余 buf。</li>\n<li><strong>Function <code>modeFromChatRequest(request)</code>（L87–L95）</strong> — <code>request.command</code> 小写 ask/plan/code；否则 prompt 以 <code>/ask|/plan|/code</code> 开头；<strong>都不匹配 → <code>'code'</code></strong>。</li>\n<li><strong>Function <code>historyFromChatContext</code>（L97–L113）</strong> — 最多 12 轮 user/assistant。</li>\n<li><strong>Function <code>revealWorkspaceFile(rel)</code>（L115–L123）</strong> — 无 folder 或 rel 则 return；打开失败 catch 空。</li>\n<li><strong>Function <code>registerChatParticipant</code>（L125–L174）</strong> — 无 <code>createChatParticipant</code> 则 return。handler：空 message 输出模式说明；否则 <code>postNdjson /api/chat</code>。status→progress；tool→markdown，apply_patch 成功 reveal + <code>stream.reference</code>；message/error/consensus。catch 提示连不上 48271。外层 try/catch warn，不抛给 activate。</li>\n<li><strong>Function <code>activate</code>（L176–L228）</strong> — 注册 ChatView、BridgeView；Chat 参与者；状态栏每 5s GET <code>/api/status</code>（运行中 / Agent / 未连接）。命令：打开侧栏；<code>openAgentChat</code> 试原生 Chat query <code>@webagent </code>，失败侧栏；resetSecret POST reset-secret。</li>\n<li><strong>Class <code>ChatView</code>（L230–L271）</strong> — webview scripts 开。<code>openNative</code> → 命令。<code>send</code>：history 12，postNdjson，事件转 webview；apply_patch reveal；assistantText 非空才进 history。</li>\n<li><strong>Class <code>BridgeView</code>（L273–L312）</strong> — start POST <code>{ tunnelProvider:'cloudflare' }</code>；stop/copy/reset；refresh GET status。catch 弹 ErrorMessage。</li>\n<li><strong>Function <code>chatHtml</code>（L314–L421）</strong> — 完整 HTML。内嵌脚本：默认 <code>mode='code'</code>；Agent 菜单切 ask/plan/code；Enter 发送；set_todos 画任务。DOM：<code>#log</code> 空态、<code>#tasks</code>、textarea <code>#q</code>、<code>#agent</code> 按钮、<code>#menu</code>、<code>#go</code>。</li>\n<li><strong>Function <code>bridgeHtml</code>（L423–L490）</strong> — 启动/停止/复制/重置。4s refresh。copy 用 <code>status.prompt</code> 或 mcpUrl+CONNECT。DOM：<code>#pill</code>、<code>#url</code>、按钮、<code>#tasks</code>、<code>#stream</code>。</li>\n</ul>\n<p> 内嵌 <code>chatHtml</code> 脚本函数：L385 <code>add</code>、L390 <code>paintTasks</code>。 内嵌 <code>bridgeHtml</code> 脚本：L463 <code>paintTasks</code>、L470 <code>paintLogs</code>（只画 <code>tool_call_end</code> 最多 12 条）。</p>\n<ul>\n<li><strong>导出（L492）：</strong> <code>{ activate, deactivate: () =&gt; {}, modeFromChatRequest }</code>。<code>deactivate</code> 空函数。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-resources/icon.svg\">📄 文件名：<code>resources/icon.svg</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 活动栏图标。L1–L5 描边 SVG（代码括号风格），无脚本。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li><code>run-code-oss.js</code> → <code>syncExtension</code> 拷贝本目录并写 <code>extensions.json</code>（绝对路径）。</li>\n<li>code-server 激活 → <code>activate</code> 注册侧栏与 <code>@webagent</code>。</li>\n<li>用户发消息 → <code>postNdjson('/api/chat')</code> → 与工作台同一套 <code>runChat</code>/<code>callTool</code>。</li>\n<li>Bridge 按钮 → <code>/api/bridge/start|stop</code>：start 在 cloudflare 下 <code>await startQuickTunnel</code>；失败仍 200，MCP 走当前 Host。stop 调 <code>stopTunnel</code>。</li>\n<li>默认模式 Code（Agent），与 Copilot 侧栏 Agent 对齐。</li>\n</ol>"},"ext-installed":{"id":"ext-installed","path":"webagent-core/extensions-installed/README.md","group":"界面","title":"extensions-installed 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：webagent.webagent-core-0.6.9/package.json","id":"文件名-webagent.webagent-core-0.6.9/package.json"},{"level":3,"text":"📄 文件名：webagent.webagent-core-0.6.9/extension.js","id":"文件名-webagent.webagent-core-0.6.9/extension.js"},{"level":3,"text":"📄 文件名：webagent.webagent-core-0.6.9/resources/icon.svg","id":"文件名-webagent.webagent-core-0.6.9/resources/icon.svg"},{"level":3,"text":"📄 文件名：extensions.json（启动时生成，不进 Git）","id":"文件名-extensions.json-启动时生成-不进-git"},{"level":2,"text":"3. 执行逻辑流（仅本目录）","id":"3.-执行逻辑流-仅本目录"}],"html":"<h1 id=\"extensions-installed-模块说明书\">extensions-installed 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/extensions-installed/</code></p>\n<p>这是 <strong>code-server 的插件安装目录</strong>，不是第二份源码。<code>scripts/ensure-code-server.js</code> 的 <code>syncExtension()</code> 把 <code>../extension/</code> 拷到这里。</p>\n<p><strong>不要在本目录改 JS。</strong> 改 <code>../extension/extension.js</code> / <code>package.json</code>，再跑 vscode 入口即可覆盖副本。</p>\n<p>无 Python。本层无手写源码；副本文件与 <code>../extension/</code> 相同（<code>extension.js</code>、<code>package.json</code>、<code>resources/icon.svg</code>）。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> <code>--extensions-dir</code> 指向的已安装树，让网页 VS Code 侧栏出现 Web Agent。</li>\n<li><strong>兄弟依赖：</strong> 源是 <code>../extension/</code>；被 <code>../scripts/ensure-code-server.js</code> 写入；<code>extensions.json</code> 含<strong>本机绝对路径</strong>，故 <code>.gitignore</code> 忽略该 json（见仓库根 <code>.gitignore</code> L28）。</li>\n<li><strong>谁调用：</strong> <code>run-code-oss.js</code> 把 code-server 的 <code>--extensions-dir</code> 指过来。自绘工作台 <strong>不读</strong> 本目录。</li>\n</ul>\n<p>行级函数说明见 <a href=\"../extension/README.md\">../extension/README.md</a>，本 README 不把 <code>extension.js</code> 再译一遍。</p>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-webagent.webagent-core-0.6.9/package.json\">📄 文件名：<code>webagent.webagent-core-0.6.9/package.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 安装后的插件清单副本。Key 与 <code>../extension/package.json</code> 相同（<code>name=webagent-core</code>，<code>version=0.6.9</code>，<code>publisher=webagent</code>，Chat 参与者 <code>webagent.agent</code> 等）。逐 Key 表见 extension 说明书，不在此重复。</li>\n</ul>\n<h3 id=\"文件名-webagent.webagent-core-0.6.9/extension.js\">📄 文件名：<code>webagent.webagent-core-0.6.9/extension.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> <code>activate</code> 等函数的副本。以 <code>../extension/extension.js</code> 为准。</li>\n</ul>\n<h3 id=\"文件名-webagent.webagent-core-0.6.9/resources/icon.svg\">📄 文件名：<code>webagent.webagent-core-0.6.9/resources/icon.svg</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 活动栏图标副本。</li>\n</ul>\n<h3 id=\"文件名-extensions.json-启动时生成-不进-git\">📄 文件名：<code>extensions.json</code>（启动时生成，不进 Git）</h3>\n<ul>\n<li><strong>文件职责：</strong> code-server 扩展列表。<code>syncExtension</code> 会写入 <code>location.path</code> 为机器绝对路径。不要提交。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录\">3. 执行逻辑流（仅本目录）</h2>\n<ol>\n<li><code>run-webagent-vscode.cmd</code> → <code>scripts/run-code-oss.js</code> → <code>syncExtension()</code>。</li>\n<li>覆盖拷贝 <code>extension/</code> → <code>webagent.webagent-core-0.6.9/</code>。</li>\n<li>写 <code>extensions.json</code>（gitignore）。</li>\n<li>code-server 从本目录加载插件，HTTP 打 agent-host <code>:48271</code>。</li>\n</ol>"},"scripts":{"id":"scripts","path":"webagent-core/scripts/README.md","group":"界面","title":"scripts 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：ensure-code-server.js","id":"文件名-ensure-code-server.js"},{"level":3,"text":"📄 文件名：codeServerAuth.js","id":"文件名-codeserverauth.js"},{"level":3,"text":"📄 文件名：run-code-oss.js","id":"文件名-run-code-oss.js"},{"level":2,"text":"3. 执行逻辑流","id":"3.-执行逻辑流"}],"html":"<h1 id=\"scripts-模块说明书\">scripts 模块说明书</h1>\n<p>当前处理目标：<code>webagent-core/scripts/</code></p>\n<p>网页 VS Code 第二种跑法的启动脚本。<code>run-webagent.cmd</code> <strong>不进入</strong>本目录。无 <code>.html</code>。</p>\n<p>文件：<code>ensure-code-server.js</code>、<code>run-code-oss.js</code>、<code>codeServerAuth.js</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 第一次从 npm 安装完整 <code>code-server@4.135.0</code> 到 <code>bin/code-server-runtime/</code>（Git 忽略内容），同步 Web Agent 插件，然后同时拉起 agent-host（跳过工作台 3000）与 code-server（占 3000）。</li>\n<li><strong>依赖：</strong> Node <code>fs</code>/<code>path</code>/<code>http</code>/<code>child_process</code>；本机 npm。</li>\n<li><strong>谁调用：</strong> 仓库根 <code>run-webagent-vscode.cmd</code> / <code>.sh</code> 执行 <code>node webagent-core/scripts/run-code-oss.js &lt;工作区&gt;</code>。<code>ensure-code-server.js</code> 也可作为 CLI（<code>require.main === module</code>）。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-ensure-code-server.js\">📄 文件名：<code>ensure-code-server.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 下载 code-server、补 VS Code 依赖、拷插件、写 <code>extensions.json</code>。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>npmCmd()</code>（L9–L11）</strong> — win32 → <code>npm.cmd</code>，否则 <code>npm</code>。</li>\n<li><strong>Function <code>runNpm(args, cwd)</code>（L13–L24）</strong> — spawnSync inherit；Windows <code>shell:true</code>；env 加 <code>FORCE_NODE_VERSION</code> 为当前 Node 主版本。<code>status !== 0</code> 抛 Error。</li>\n<li><strong>Function <code>findEntry()</code>（L26–L33）</strong> — 三个候选：<code>node_modules/code-server/out/node/entry.js</code>、<code>code-server-4.135.0/out/node/entry.js</code>、带 <code>-linux-amd64</code> 的同名。第一个 exists 或 null。</li>\n<li><strong>Function <code>vscodeDirFromEntry(entry)</code>（L35–L37）</strong> — <code>dirname(entry)/../../lib/vscode</code>。</li>\n<li><strong>Function <code>ensureVscodeDeps(entry)</code>（L39–L48）</strong> — 已有 <code>@microsoft/1ds-core-js</code> 则 return；无 package.json 抛「包不完整」；否则 npm install <code>--omit=dev --ignore-scripts</code>。</li>\n<li><strong>Function <code>ensure()</code>（L50–L81）</strong> — mkdir runtimeRoot；无 entry 则写私有 package.json（只依赖 code-server）并 npm install <code>code-server@VERSION</code>；仍无 entry 抛错；<code>ensureVscodeDeps</code>；返回 entry。</li>\n<li><strong>Function <code>syncExtension()</code>（L83–L117）</strong> — 拷 <code>package.json</code>、<code>extension.js</code> 到 <code>extensions-installed/webagent.webagent-core-0.6.9/</code>；有 icon 则拷；写 <code>extensions.json</code>，<code>location.path</code> 为本机绝对路径（正斜杠）。</li>\n</ul>\n<ul>\n<li><strong>关键变量：</strong></li>\n<li>L5 <code>VERSION = '4.135.0'</code></li>\n<li>L6 <code>repoRoot</code> = 本文件上两级（仓库根）</li>\n<li>L7 <code>runtimeRoot</code> = <code>bin/code-server-runtime</code></li>\n<li>L119–L126：直接运行时 try ensure+syncExtension，失败 exit 1。</li>\n<li>L128 导出 <code>{ ensure, syncExtension, findEntry, VERSION, runtimeRoot, repoRoot }</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-codeserverauth.js\">📄 文件名：<code>codeServerAuth.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 网页 VS Code 的登录口令与 CORS Origin 名单。不 spawn code-server。</li>\n<li><strong>Function <code>trustedOrigins(port)</code>（L5–L8）</strong> — <code>http://127.0.0.1:${port},http://localhost:${port}</code>。不是 <code>*</code>。</li>\n<li><strong>Function <code>resolveAuth({ userData, env })</code>（L10–L35）</strong></li>\n<li>L11–L14：<code>CODE_SERVER_AUTH=none</code> → <code>{ mode:'none', password:null }</code>。</li>\n<li>L16–L21：否则 <code>mode:'password'</code>。<code>CODE_SERVER_PASSWORD</code> 有值则用之。</li>\n<li>L23–L29：读 <code>userData/webagent-password</code>（已有则沿用）。</li>\n<li>L31–L34：没有则 <code>randomBytes(12)</code> 的 base64url，写入该文件 <code>mode 0o600</code>。</li>\n<li>L37 导出 <code>{ trustedOrigins, resolveAuth }</code>。</li>\n</ul>\n<h3 id=\"文件名-run-code-oss.js\">📄 文件名：<code>run-code-oss.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 编排：ensure → 装 agent-host 依赖 → 起 agent-host（skip workbench）→ 等 <code>/health</code> → 起 code-server。</li>\n<li><strong>顶层（L8–L17）：</strong> <code>workspace</code> = argv[2] 或 <code>WORKSPACE_ROOT</code> 或仓库 <code>workspace</code>。不存在 <code>exit(1)</code>。<code>mcpPort</code> 默认 48271，<code>codePort</code> 默认 3000。</li>\n<li><strong>核心类/函数清单：</strong></li>\n</ul>\n<ul>\n<li><strong>Function <code>waitHealth(url, timeoutMs)</code>（L19–L36）</strong> — 循环 http.get：200 resolve；超时或持续 error 直到超时 → reject「agent-host 未在时限内就绪」；否则 200ms 再试。</li>\n<li><strong>Function <code>run(command, args, opts)</code>（L38–L51）</strong> — spawn inherit、windowsHide；error 打印后 exit 1。<code>env</code> 与 <code>process.env</code> 合并。返回 child。</li>\n<li><strong>Function <code>main()</code>（L53–L166）</strong></li>\n<li>L58–L60：<code>ensure()</code> + <code>syncExtension()</code>。</li>\n<li>L62–L74：agent-host 无 <code>node_modules/express</code> 则在该目录 npm install；exit 非 0 reject。</li>\n<li>L76–L95：SIGINT/SIGTERM：Windows <code>taskkill /pid /t /f</code>，否则 SIGTERM。</li>\n<li>L97–L113：<code>run(process.execPath, ['src/index.js'], { cwd: agentHostDir, env: WORKSPACE_ROOT, AGENT_HOST_PORT, WEBAGENT_SKIP_WORKBENCH:'1' })</code>。agent exit 真值则 stop 并 exit。</li>\n<li>L115：<code>waitHealth(http://127.0.0.1:${mcpPort}/health, 15000)</code>。</li>\n<li>L117–L120：<code>userData</code> = 仓库根 <code>.local/share/code-server</code>（mkdir）；<code>configFile</code> = 仓库根 <a href=\"#/files/code-server-cfg\"><code>.config/code-server/config.yaml</code></a>；<code>resolveAuth</code>。</li>\n<li>L122–L148：code-server 参数：<code>--auth</code> 为 <code>password</code>（或 <code>CODE_SERVER_AUTH=none</code>）、<code>--bind-addr ${WEBAGENT_BIND||127.0.0.1}:${codePort}</code>、关遥测/更新/workspace-trust、<code>--trusted-origins</code> 仅本机 http 源、<code>--app-name Web Agent</code>、<code>--user-data-dir</code>、<code>--extensions-dir</code>、<code>--config</code>、最后一项 workspace 路径。</li>\n<li>L150–L162：打印 VS Code URL；password 模式打印口令与文件路径。</li>\n<li>L164–L168：spawn 时 password 模式把 <code>PASSWORD</code> 传给 code-server；退出则 stop 并 <code>exit(code||0)</code>。</li>\n<li>L168–L171：<code>main().catch</code> 打印 message，exit 1。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流\">3. 执行逻辑流</h2>\n<ol>\n<li>用户 <code>run-webagent-vscode.cmd D:\\code\\my-app</code>。</li>\n<li><code>run-code-oss.js</code> 检查工作区存在。</li>\n<li><code>ensure()</code> 可能下载 ~50MB code-server + VS Code deps。</li>\n<li><code>syncExtension()</code> 让侧栏出现 Web Agent。</li>\n<li>agent-host 只听 48271（不占 3000）。</li>\n<li><code>/health</code> 200 后 code-server 听 3000，打开的是真 VS Code Web，工作区即用户路径。</li>\n<li>插件打 48271 的 <code>/api/chat</code> 与 <code>/api/bridge/*</code>，与自绘工作台同一引擎。</li>\n<li>不要与 <code>run-webagent.cmd</code> 同时开（抢 3000）。</li>\n</ol>"},"workspace":{"id":"workspace","path":"workspace/README.md","group":"工作区","title":"workspace 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：package.json","id":"文件名-package.json"},{"level":3,"text":"📄 文件名：src/calculator.js","id":"文件名-src/calculator.js"},{"level":3,"text":"📄 文件名：tests/calculator.test.js","id":"文件名-tests/calculator.test.js"},{"level":3,"text":"📄 文件名：.webagent/customizations.json","id":"文件名-.webagent/customizations.json"},{"level":3,"text":"📄 文件名：.webagent/instructions.md","id":"文件名-.webagent/instructions.md"},{"level":3,"text":"📄 文件名：.webagent/skills/fix-tests/SKILL.md","id":"文件名-.webagent/skills/fix-tests/skill.md"},{"level":3,"text":"📄 文件名：.webagent/skills/review/SKILL.md","id":"文件名-.webagent/skills/review/skill.md"},{"level":3,"text":"📄 文件名：.webagent/skills/docs-sync/SKILL.md","id":"文件名-.webagent/skills/docs-sync/skill.md"},{"level":2,"text":"3. 执行逻辑流（仅本目录）","id":"3.-执行逻辑流-仅本目录"}],"html":"<h1 id=\"workspace-模块说明书\">workspace 模块说明书</h1>\n<p>当前处理目标：<code>workspace/</code></p>\n<p>这是 <code>run-webagent.cmd</code> <strong>默认</strong>挂上的本机项目（<code>WORKSPACE_ROOT</code> 未指定时 = 仓库根 <code>workspace\\</code>）。Chat / Bridge / 网页 Agent 改的就是这里的磁盘。不是 MCP 服务器源码。</p>\n<p>无 Python。代码：<code>src/calculator.js</code>、<code>tests/calculator.test.js</code>；配置：<code>package.json</code>、<code>.webagent/customizations.json</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 演示工作区（计算器）。用来验证搜-读-补丁-再测，不是产品进程。</li>\n<li><strong>兄弟依赖：</strong> <strong>没有 require 产品代码。</strong> 反过来：<code>agent-host</code> 的 <code>config.workspaceRoot</code> 默认指向本目录；<code>load_skill</code> 读 <code>.webagent/skills/</code>；<code>getInstructions</code> 会拼 <code>customizations.json</code> / <code>instructions.md</code>。</li>\n<li><strong>谁调用：</strong> 用户在工作台或 MCP 工具里读写；<code>npm test</code> 在本目录跑计算器测试。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-package.json\">📄 文件名：<code>package.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 演示项目 npm 清单。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>name</code></td>\n<td>包名</td>\n<td><code>webagent-workspace-target</code></td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td>版本</td>\n<td><code>1.0.0</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td>说明</td>\n<td><code>Live workspace inside Web Agent Editor</code></td>\n</tr>\n<tr>\n<td><code>main</code></td>\n<td>入口字段</td>\n<td><code>src/calculator.js</code></td>\n</tr>\n<tr>\n<td><code>scripts.test</code></td>\n<td><code>npm test</code></td>\n<td><code>node tests/calculator.test.js</code></td>\n</tr>\n</tbody></table></div>\n<p>无 <code>dependencies</code>。</p>\n<hr />\n<h3 id=\"文件名-src/calculator.js\">📄 文件名：<code>src/calculator.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 被 Agent 改的演示模块。</li>\n<li><strong>核心类/函数清单：</strong></li>\n<li><strong>Function <code>add(a, b)</code>（L5–L7）</strong> — 返回 <code>a + b</code>。</li>\n<li><strong>Function <code>subtract(a, b)</code>（L9–L11）</strong> — 返回 <code>a - b</code>。</li>\n<li><strong>Function <code>multiply(a, b)</code>（L13–L15）</strong> — 返回 <code>a * b</code>。</li>\n<li><strong>Function <code>divide(a, b)</code>（L17–L22）</strong> — L18–L20：<code>b === 0</code> 则 throw <code>Cannot divide by zero</code>；L21 返回 <code>a / b</code>。</li>\n<li><strong>Function <code>power(base, exponent)</code>（L24–L26）</strong> — <code>Math.pow</code>。</li>\n<li>L28–L34：<code>module.exports</code> 五函数。</li>\n<li><strong>关键变量：</strong> 无模块级配置。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-tests/calculator.test.js\">📄 文件名：<code>tests/calculator.test.js</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 手写 assert 套件，对应 <code>npm test</code>。</li>\n<li><strong>模块变量：</strong> L7–L8 <code>passed</code> / <code>failed</code> 计数。</li>\n<li><strong>Function <code>test(name, fn)</code>（L10–L19）</strong></li>\n<li>输入：<code>name</code> 字符串；<code>fn</code> 无参函数。</li>\n<li>L11–L14：<code>fn()</code> 成功则打印 PASS，<code>passed++</code>。</li>\n<li>L14–L18：catch 打印 FAIL 与 <code>err.message</code>，<code>failed++</code>。无返回值。</li>\n<li><strong>用例（L21–L40）：</strong></li>\n<li>L21–L23：<code>add(2,3)===5</code></li>\n<li>L25–L27：<code>subtract(10,4)===6</code></li>\n<li>L29–L31：<code>multiply(6,7)===42</code></li>\n<li>L33–L35：<code>divide(10,2)===5</code></li>\n<li>L37–L40：<code>divide(10,0)</code> 必须 throw，消息匹配 <code>/Cannot divide by zero/</code></li>\n<li>L42–L52：打印 Summary；<code>failed&gt;0</code> → <code>exit(1)</code>，否则 <code>exit(0)</code>。</li>\n<li><strong>没有测 <code>power</code>。</strong></li>\n</ul>\n<hr />\n<h3 id=\"文件名-.webagent/customizations.json\">📄 文件名：<code>.webagent/customizations.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 工作区自定义（<code>src/models/customizations.js</code> 会读）。Git <strong>跟踪</strong>本文件；<code>.gitignore</code> 只忽略 <code>workspace/.webagent/config.json</code>（MCP 密钥，本树当前无该文件）。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>当前取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>preference</code></td>\n<td>环境偏好自由文本</td>\n<td><code>&quot;&quot;</code></td>\n</tr>\n<tr>\n<td><code>instructions</code></td>\n<td>追加进 MCP <code>initialize.instructions</code></td>\n<td>中文：提交说明用中文；尽量 <code>apply_patch</code>；Ask/Plan 只读；改动带测试</td>\n</tr>\n<tr>\n<td><code>agents</code></td>\n<td>智能体列表</td>\n<td>一项 <code>id=default</code>，<code>name=默认编程智能体</code>，<code>role</code> 说明 Ask/Plan/Code</td>\n</tr>\n<tr>\n<td><code>agents[].id</code> / <code>name</code> / <code>role</code></td>\n<td>卡片主键、显示名、职责</td>\n<td>见上</td>\n</tr>\n<tr>\n<td><code>prompts</code></td>\n<td>自定义 MCP prompts</td>\n<td>一项 <code>id=diagnose</code>，<code>name=诊断测试失败</code>，<code>content</code> 只读探查、不要改文件</td>\n</tr>\n<tr>\n<td><code>hooks</code></td>\n<td>钩子</td>\n<td><code>[]</code>（产品代码若未读则无效果）</td>\n</tr>\n<tr>\n<td><code>mcpServers</code></td>\n<td>额外 MCP</td>\n<td><code>[]</code></td>\n</tr>\n<tr>\n<td><code>plugins</code></td>\n<td>插件</td>\n<td><code>[]</code></td>\n</tr>\n<tr>\n<td><code>quickLinks</code></td>\n<td>快捷链接</td>\n<td><code>[]</code></td>\n</tr>\n<tr>\n<td><code>voice</code> / <code>dictation</code></td>\n<td>语音</td>\n<td><code>&quot;&quot;</code></td>\n</tr>\n<tr>\n<td><code>codex.loggedIn</code></td>\n<td>登录标记</td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>codex.account</code></td>\n<td>账号</td>\n<td><code>&quot;&quot;</code></td>\n</tr>\n</tbody></table></div>\n<hr />\n<h3 id=\"文件名-.webagent/instructions.md\">📄 文件名：<code>.webagent/instructions.md</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 与 <code>customizations.instructions</code> 同一句话的 markdown 副本（1 行，无函数）。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-.webagent/skills/fix-tests/skill.md\">📄 文件名：<code>.webagent/skills/fix-tests/SKILL.md</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 给模型的 Skill 文本（<code>load_skill</code> 可读）。不是可执行 JS。</li>\n<li><strong>内容结构：</strong></li>\n<li>L1–L3：标题「修复失败的单元测试」；触发词：测试失败 / 除以零 / calculator。</li>\n<li>L5–L6 <strong>Ask：</strong> 只读；文中写了 <code>list_directory</code> → <code>search_files</code> → <code>read_files</code> → <code>get_diagnostics</code>；禁止 <code>apply_patch</code> / <code>run_command</code>。  </li>\n</ul>\n<p> （现行 <code>getToolList</code> <strong>没有</strong> <code>get_diagnostics</code> / <code>search_files</code> 这两个名字，测试锁的是 <code>find_files</code>。本文件是演示 Skill 原文，不以它为准改工具层。）</p>\n<ul>\n<li>L8–L9 <strong>Plan：</strong> 独立分支、写清共识、不改仓库。</li>\n<li>L11–L23 <strong>Code：</strong> 读 <code>calculator.js</code> hash → <code>apply_patch</code> 给 <code>divide</code> 加除 0 守卫 → <code>STALE_FILE</code> 则重读 → <code>npm test</code> 要 5/5。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-.webagent/skills/review/skill.md\">📄 文件名：<code>.webagent/skills/review/SKILL.md</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 代码审查 Skill（<code>load_skill</code> 可读）。默认只读。</li>\n<li><strong>内容结构：</strong></li>\n<li>L1–L3：标题与触发词（审查 / code review / 找风险 / 看 diff / 合并前检查）。</li>\n<li>L5–L7 <strong>Ask：</strong> <code>git_status</code> → <code>git_diff</code> → <code>list_directory</code> → <code>search_files</code> → <code>read_files</code>；禁止 patch / write / run_command。</li>\n<li>L9–L11 <strong>Plan：</strong> 列出风险，不改仓库。</li>\n<li>L13–L21 <strong>Code：</strong> 用户明确说要修才动手；<code>workspace_info</code> → git 或目录 → <code>read_files</code> → 按文件写严重/建议/风格 → 需要时 <code>apply_patch</code> 再跑测试。不是 git 仓库时 <code>available:false</code>，不要 <code>git init</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-.webagent/skills/docs-sync/skill.md\">📄 文件名：<code>.webagent/skills/docs-sync/SKILL.md</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 仓库级「功能改完必须同步改说明书」约定，以 Skill 形式给 <code>load_skill</code> 读。不是可执行 JS。不要再在仓库根放 <code>文档约定.md</code>。动到设计理由时还要改根目录 <code>架构导读.md</code>（四层：人话 → 比喻 → 文件落地 → 行业叫法）。</li>\n<li><strong>内容结构：</strong></li>\n<li>L1–L14：标题「文档同步」；触发词含新功能 / 改工具 MCP 路由 工作台 测试 / 说明书 / README / 四阶段 / 文档约定 / 架构 / 导读 / 为什么这样装。四层写法说明。默认 <code>workspace/</code> 沙箱进不去仓库其它目录。</li>\n<li>L16–L18 <strong>Ask：</strong> 只读 <code>list_directory</code> → <code>search_files</code> → <code>read_files</code>（含根 <code>架构导读.md</code>）；禁止 <code>apply_patch</code> / <code>write_file</code> / <code>run_command</code>。</li>\n<li>L20–L22 <strong>Plan：</strong> 列出要改的说明书路径（含是否动导读），不改仓库。</li>\n<li>L24–L33 <strong>Code：</strong> 按改动类型点名对应夹 README / <code>总览.md</code> / <code>DOCUMENTATION_SUMMARY.md</code>；第 8 条：架构/数据流/钥匙隧道/新工单入口改 <code>架构导读.md</code> 对应小节。根 <code>README.md</code> 不改成行级模板；<code>webagent-repro/</code> 冻结。</li>\n<li>L35：禁止杜撰、禁止为对齐文档改 <code>CONNECT_LINE</code>、禁止提交 dist。</li>\n<li>L37：提交前 <code>npm test</code> 绿；动过设计理由则导读能对上新代码。</li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录\">3. 执行逻辑流（仅本目录）</h2>\n<ol>\n<li>agent-host 启动时 <code>workspaceRoot</code> 默认为本目录。</li>\n<li>MCP / Chat 工具的相对路径都相对这里：<code>src/calculator.js</code>、<code>tests/</code>、<code>.webagent/</code>。</li>\n<li>用户或 Agent 执行 <code>npm test</code> → <code>tests/calculator.test.js</code> 调 <code>src/calculator.js</code>。</li>\n<li><code>load_skill</code> 扫描 <code>.webagent/skills/*/SKILL.md</code>；<code>getInstructions</code> 拼 <code>customizations.json</code> 的 instructions。</li>\n<li>换工作区：<code>run-webagent.cmd D:\\code\\my-repo</code>，就不再用本演示树。</li>\n</ol>"},"bin":{"id":"bin","path":"bin/README.md","group":"运行时","title":"bin 模块说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：code-server-runtime/package.json","id":"文件名-code-server-runtime/package.json"},{"level":2,"text":"3. 执行逻辑流（仅本目录）","id":"3.-执行逻辑流-仅本目录"}],"html":"<h1 id=\"bin-模块说明书\">bin 模块说明书</h1>\n<p>当前处理目标：<code>bin/</code></p>\n<p>Git <strong>不再内嵌</strong> code-server。以前那份残缺切片 <code>code-server-dist/</code> 已删除，不能当 VS Code 启动。本层无 <code>.js</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 网页 VS Code 运行时的落盘目录（下载结果）。不是 MCP 实现。</li>\n<li><strong>兄弟依赖：</strong> <code>webagent-core/scripts/ensure-code-server.js</code> 往 <code>code-server-runtime/</code> 写文件；<code>run-code-oss.js</code> 从这里找 <code>entry.js</code>。</li>\n<li><strong>谁调用：</strong> 仅第二种跑法 <code>run-webagent-vscode.cmd</code>。主路径 <code>run-webagent.cmd</code> <strong>不读</strong>本目录。</li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>路径</th>\n<th>进 Git？</th>\n<th>职责</th>\n</tr></thead><tbody>\n<tr>\n<td><code>code-server-runtime/</code></td>\n<td>内容忽略（保留 <code>package.json</code>）</td>\n<td>第一次从 npm 下载的完整 <code>code-server@4.135.0</code></td>\n</tr>\n<tr>\n<td><code>code-server-dist/</code></td>\n<td>禁止存在</td>\n<td>由 <code>codeServerNotRunnable.test.js</code> 断言不得出现</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-code-server-runtime/package.json\">📄 文件名：<code>code-server-runtime/package.json</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 给 <code>ensure()</code> 的占位清单；<code>.gitignore</code> 用 <code>!bin/code-server-runtime/package.json</code> 保留这一份。<code>npm install</code> 之后同目录会出现被忽略的 <code>node_modules/</code>。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>name</code></td>\n<td>包名</td>\n<td><code>webagent-code-server-runtime</code></td>\n</tr>\n<tr>\n<td><code>private</code></td>\n<td>禁止当公共包发布</td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td>说明</td>\n<td>写明下载官方 npm <code>code-server 4.135.0</code>；不要用残缺 git 切片 <code>lib/code-server-4.135.0</code></td>\n</tr>\n<tr>\n<td><code>dependencies.code-server</code></td>\n<td>要安装的版本</td>\n<td><strong>恰好</strong> <code>4.135.0</code>（与 <code>ensure-code-server.js</code> 的 <code>VERSION</code> 一致）</td>\n</tr>\n</tbody></table></div>\n<p>无 scripts、无 main。本文件被 <code>ensure()</code> 在缺失 entry 时也可能重写（见 <code>scripts/README.md</code>），以磁盘当时内容为准。</p>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录\">3. 执行逻辑流（仅本目录）</h2>\n<ol>\n<li>用户第一次跑 <code>run-webagent-vscode.cmd</code>。</li>\n<li><code>ensure-code-server.js</code> 在 <code>code-server-runtime/</code> 执行 npm，拉 <code>code-server@4.135.0</code>。</li>\n<li>找到 <code>node_modules/code-server/out/node/entry.js</code> 后由 <code>run-code-oss.js</code> spawn。</li>\n<li>这些文件不进 Git；删掉 <code>node_modules</code> 后下次会再下载。</li>\n<li>主路径工作台仍走 <code>run-webagent.cmd</code>，不经过本目录。</li>\n</ol>"},"repro":{"id":"repro","path":"webagent-repro/README.md","group":"冻结","title":"webagent-repro（已冻结，不要运行）","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：package.json","id":"文件名-package.json"},{"level":3,"text":"📄 文件名：src/config.js","id":"文件名-src/config.js"},{"level":3,"text":"📄 文件名：src/index.js","id":"文件名-src/index.js"},{"level":3,"text":"📄 文件名：src/mcp/auth.js","id":"文件名-src/mcp/auth.js"},{"level":3,"text":"📄 文件名：src/mcp/server.js","id":"文件名-src/mcp/server.js"},{"level":3,"text":"📄 文件名：src/mcp/tools/index.js","id":"文件名-src/mcp/tools/index.js"},{"level":3,"text":"📄 文件名：src/mcp/tools/fileOps.js","id":"文件名-src/mcp/tools/fileops.js"},{"level":3,"text":"📄 文件名：src/mcp/tools/patchEngine.js","id":"文件名-src/mcp/tools/patchengine.js"},{"level":3,"text":"📄 文件名：src/mcp/tools/executor.js","id":"文件名-src/mcp/tools/executor.js"},{"level":3,"text":"📄 文件名：src/mcp/tools/progressTracker.js","id":"文件名-src/mcp/tools/progresstracker.js"},{"level":3,"text":"📄 文件名：src/api/routes.js","id":"文件名-src/api/routes.js"},{"level":3,"text":"📄 文件名：src/tunnel/tunnelManager.js","id":"文件名-src/tunnel/tunnelmanager.js"},{"level":3,"text":"📄 文件名：src/utils/eventBus.js","id":"文件名-src/utils/eventbus.js"},{"level":3,"text":"📄 文件名：src/utils/diff.js","id":"文件名-src/utils/diff.js"},{"level":3,"text":"📄 文件名：public/index.html","id":"文件名-public/index.html"},{"level":3,"text":"📄 文件名：public/styles.css","id":"文件名-public/styles.css"},{"level":3,"text":"📄 文件名：public/app.js","id":"文件名-public/app.js"},{"level":3,"text":"📄 文件名：tests/patchEngine.test.js","id":"文件名-tests/patchengine.test.js"},{"level":3,"text":"📄 文件名：workspace_demo/package.json","id":"文件名-workspace_demo/package.json"},{"level":3,"text":"📄 文件名：workspace_demo/src/calculator.js","id":"文件名-workspace_demo/src/calculator.js"},{"level":3,"text":"📄 文件名：workspace_demo/tests/calculator.test.js","id":"文件名-workspace_demo/tests/calculator.test.js"},{"level":2,"text":"3. 执行逻辑流（仅本目录，历史行为）","id":"3.-执行逻辑流-仅本目录-历史行为"}],"html":"<h1 id=\"webagent-repro-已冻结-不要运行\">webagent-repro（已冻结，不要运行）</h1>\n<p>这是仓库里 <strong>第一代 Bridge 原型</strong>：一个 Node 进程同时提供简易网页和 MCP（默认端口 <strong>3000</strong>，工作区是本目录 <code>workspace_demo/</code>）。</p>\n<p><strong>现行产品不在这里。</strong> 启动脚本不会进入本目录。不要 <code>cd webagent-repro &amp;&amp; npm start</code>。端口、工具集、密钥存储都和现在的使用指南对不上。</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>你想做的事</th>\n<th>请用</th>\n</tr></thead><tbody>\n<tr>\n<td>Windows 改本机仓库</td>\n<td>仓库根 <code>run-webagent.cmd</code>，代码在 <code>../webagent-core/</code></td>\n</tr>\n<tr>\n<td>网页里真 VS Code</td>\n<td><code>run-webagent-vscode.cmd</code></td>\n</tr>\n<tr>\n<td>目录对照</td>\n<td><a href=\"../组件说明.md\">组件说明.md</a> 第 1.1 节</td>\n</tr>\n</tbody></table></div>\n<p>下面是第一阶段行级说明书（对照用）。<strong>不</strong>把本目录当成可启动产品，不要改这里的 JS。</p>\n<p>当前处理目标：<code>webagent-repro/</code>（整棵树：<code>src/</code>、<code>public/</code>、<code>tests/</code>、<code>workspace_demo/</code>）。无 Python。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> 单 Express + <code>ws</code> 进程、单端口 <code>3000</code>、路径密钥 MCP、<strong>8</strong> 个工具、内嵌 Studio 页面、可选 cloudflared。没有 OAuth、没有双端口、没有内置 Chat Agent、没有 25 工具。</li>\n<li><strong>兄弟依赖：</strong> <strong>没有。</strong> 不 import <code>webagent-core/</code>。</li>\n<li><strong>谁曾经调用：</strong> <code>npm start</code> → <code>node src/index.js</code>（<strong>现在不要跑</strong>）。<code>npm test</code> 只跑本目录 <code>tests/patchEngine.test.js</code>。</li>\n</ul>\n<p><strong>目录内 require 关系：</strong></p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>文件</th>\n<th>require 本目录</th>\n</tr></thead><tbody>\n<tr>\n<td><code>src/index.js</code></td>\n<td><code>config</code>、<code>mcp/server</code>、<code>api/routes</code>、<code>utils/eventBus</code>、<code>tunnel/tunnelManager</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/server.js</code></td>\n<td><code>./tools</code>、<code>./auth</code>、<code>../config</code>、<code>../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/auth.js</code></td>\n<td><code>../config</code>、<code>../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/tools/index.js</code></td>\n<td><code>./patchEngine</code> <code>./fileOps</code> <code>./executor</code> <code>./progressTracker</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/tools/fileOps.js</code></td>\n<td><code>./patchEngine</code>、<code>../../config</code>、<code>../../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/tools/patchEngine.js</code></td>\n<td><code>../../config</code>、<code>../../utils/eventBus</code>、<code>../../utils/diff</code></td>\n</tr>\n<tr>\n<td><code>src/mcp/tools/executor.js</code></td>\n<td><code>../../config</code>、<code>../../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>src/api/routes.js</code></td>\n<td><code>config</code>、<code>mcp/auth</code>、<code>tunnel/tunnelManager</code>、<code>mcp/tools</code>、<code>progressTracker</code>、<code>executor</code>、<code>eventBus</code></td>\n</tr>\n<tr>\n<td><code>src/tunnel/tunnelManager.js</code></td>\n<td><code>../config</code>、<code>../utils/eventBus</code></td>\n</tr>\n<tr>\n<td><code>public/app.js</code></td>\n<td>无 require（浏览器）</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-package.json\">📄 文件名：<code>package.json</code></h3>\n<ul>\n<li><strong>职责：</strong> 原型 npm 清单。</li>\n<li><strong>Key：</strong></li>\n<li><code>name</code>=<code>webagent-repro</code>，<code>version</code>=<code>1.0.0</code>，<code>main</code>=<code>index.js</code>（实际启动走 scripts，不是根 <code>index.js</code>）</li>\n<li><code>scripts.start</code>=<code>node src/index.js</code>；<code>scripts.test</code>=<code>node tests/patchEngine.test.js</code></li>\n<li><code>dependencies</code>：<code>cors ^2.8.6</code>、<code>diff ^9.0.0</code>、<code>express ^5.2.1</code>、<code>ws ^8.21.3</code></li>\n<li>无 <code>uuid</code>、无 <code>nodemon</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/config.js\">📄 文件名：<code>src/config.js</code></h3>\n<ul>\n<li><strong>职责：</strong> 进程内配置对象。密钥<strong>每次启动随机</strong>，不写 <code>.secret</code> 文件。</li>\n<li><strong>对象 <code>config</code>（L7–L21）：</strong></li>\n<li><code>port</code>：<code>PORT</code> 或 <strong>3000</strong></li>\n<li><code>host</code>：写死 <code>'0.0.0.0'</code></li>\n<li><code>workspaceRoot</code>：<code>WORKSPACE_ROOT</code> 或 <code>../workspace_demo</code></li>\n<li><code>secretKey</code>：<code>crypto.randomBytes(12).toString('hex')</code></li>\n<li><code>tunnelProvider</code>：写死 <code>'quick'</code>（注释写 Cloudflare / named / ngrok / local）</li>\n<li><code>tunnelUrl</code>：初始 <code>null</code>（隧道成功后由 TunnelManager 写回）</li>\n<li><code>version</code>：<code>'0.6.9'</code>；<code>serverName</code>：<code>'Web Agent-Bridge'</code></li>\n<li><code>timeoutMs</code> 30000；<code>maxCommandTimeout</code> 60000</li>\n<li><code>allowShellExecution</code> / <code>autoApprove</code> 均为 <code>true</code>（本目录其它文件<strong>没有读取</strong>这两项）</li>\n<li><strong>Function <code>generateNewSecret</code>（L22–L25）</strong> — 重新 randomBytes，返回新密钥。</li>\n<li><strong>Function <code>setWorkspaceRoot</code>（L27–L29）</strong> — <code>path.resolve</code> 后写入 <code>config.workspaceRoot</code>。本目录启动路径<strong>没有调用</strong>它。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/index.js\">📄 文件名：<code>src/index.js</code></h3>\n<ul>\n<li><strong>职责：</strong> 唯一入口：HTTP + 同端口 WebSocket + 静态页 + <code>/mcp</code> + <code>/api</code>。</li>\n<li>L12–L14：<code>express()</code>、<code>http.createServer</code>、<code>WebSocketServer({ server })</code>（同一端口升级 WS）。</li>\n<li><strong>WS <code>connection</code>（L17–L29）</strong> — <code>eventBus.addWsClient</code>；立刻 <code>send</code> <code>{ type:'connected', payload:{ serverName, version, secretKey } }</code>。</li>\n<li><strong>中间件 L32–L36：</strong> cors；json <strong>20mb</strong>；<code>public/</code> 静态。</li>\n<li><strong>路由 L39–L40：</strong> <code>app.use('/mcp', mcpRouter)</code>；<code>app.use('/api', apiRouter)</code>。</li>\n<li><strong>SPA fallback L43–L47：</strong> GET 且路径不以 <code>/api</code>、<code>/mcp</code> 开头 → 送 <code>public/index.html</code>。</li>\n<li><strong>listen L51–L61：</strong> <code>config.host:config.port</code>；打印 MCP <code>http://127.0.0.1:${port}/mcp/${secretKey}</code>；然后 <strong><code>tunnelManager.startTunnel('local')</code></strong>（.catch 只 <code>console.error</code>）。</li>\n<li>L64：导出 <code>{ app, server }</code>（文件共 64 行）。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/auth.js\">📄 文件名：<code>src/mcp/auth.js</code></h3>\n<ul>\n<li><strong>Function <code>validateSecret</code>（L4–L19）</strong> — 密钥来源顺序：<code>req.params.secret</code> <strong>或</strong> 头 <code>x-mcp-secret</code> <strong>或</strong> <code>query.secret</code>。不等或缺失 → 401 JSON-RPC <code>-32000</code>。无 Bearer、无 OAuth。</li>\n<li><strong>Function <code>rotateSecret</code>（L21–L29）</strong> — 调 <code>generateNewSecret</code>；<code>eventBus.broadcast('secret_rotated', { oldSecret, newSecret, timestamp })</code>；返回新密钥。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/server.js\">📄 文件名：<code>src/mcp/server.js</code></h3>\n<ul>\n<li><strong>职责：</strong> Express Router，挂到 <code>/mcp</code> 后路径为 <code>/mcp/:secret</code>。</li>\n<li><strong>GET <code>/:secret</code>（L12–L27）</strong> — 先 <code>validateSecret</code>；返回 status/online、server、version、workspace、<code>protocol:'mcp-streamable-http'</code>、endpoints 对象（jsonrpc / sse / <strong>messages</strong>）、toolsCount、tools 名列表。  </li>\n</ul>\n<p> <strong>源码里没有 <code>POST /:secret/messages</code> 路由</strong>，诊断 JSON 列出的 messages 地址本文件未实现。</p>\n<ul>\n<li><strong>POST <code>/:secret</code>（L32–L180）</strong></li>\n<li>L33：拆 <code>jsonrpc, id, method, params</code>。</li>\n<li>L35–L40：<code>jsonrpc !== '2.0'</code> → 400、<code>-32600</code>。</li>\n<li>L42：<code>callStartTime = Date.now()</code>。</li>\n<li><strong>switch method：</strong></li>\n<li><code>initialize</code> L47–L74：broadcast <code>agent_connected</code>；返回 protocol <code>2024-11-05</code>、capabilities（tools.listChanged、空 prompts/resources/logging）、serverInfo。<strong>没有 <code>instructions</code> 字段。</strong></li>\n<li><code>notifications/initialized</code> L76–L78：result <code>{}</code>（仍 200 JSON，不是产品的 204）。</li>\n<li><code>ping</code> L80–L82：result <code>{}</code>。</li>\n<li><code>tools/list</code> L84–L93：<code>getToolList()</code>。</li>\n<li><code>tools/call</code> L95–L157：无 <code>name</code> → 400 <code>-32602</code>；broadcast <code>tool_call_start</code>；<code>callTool</code>；成功 broadcast <code>tool_call_end</code> success；MCP <code>content[{type:text}]</code> <code>isError:false</code>；工具 throw → isError true，文本 <code>Tool Execution Error: …</code>。</li>\n<li>default L159–L164：404、<code>-32601</code>。</li>\n<li>L166–L170：外层 catch → 500 <code>-32603</code>。</li>\n<li><strong>GET <code>/:secret/sse</code>（L182–L199）</strong> — SSE 头；先写 <code>event: endpoint</code> <code>data: /mcp/${secret}/messages</code>；每 15s <code>: ping</code>；close 清 interval。同样没有对应 messages POST。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/tools/index.js\">📄 文件名：<code>src/mcp/tools/index.js</code></h3>\n<ul>\n<li><strong>职责：</strong> 8 个工具的 schema + 分发。</li>\n<li><strong>常量 <code>TOOLS</code>（L9–L201）</strong> 每项 <code>name</code> / <code>description</code> / <code>inputSchema</code> / <code>handler</code>：</li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>name</th>\n<th>handler</th>\n<th>required</th>\n</tr></thead><tbody>\n<tr>\n<td><code>read_file</code></td>\n<td><code>readFile</code></td>\n<td>filePath</td>\n</tr>\n<tr>\n<td><code>write_file</code></td>\n<td><code>writeFile</code></td>\n<td>filePath, content</td>\n</tr>\n<tr>\n<td><code>apply_patch</code></td>\n<td><code>applyPatch</code></td>\n<td>filePath, patch</td>\n</tr>\n<tr>\n<td><code>execute_command</code></td>\n<td><code>executeCommand</code></td>\n<td>command</td>\n</tr>\n<tr>\n<td><code>list_dir</code></td>\n<td><code>listDir</code></td>\n<td>（无 required）</td>\n</tr>\n<tr>\n<td><code>grep_search</code></td>\n<td><code>grepSearch</code></td>\n<td>query</td>\n</tr>\n<tr>\n<td><code>report_progress</code></td>\n<td><code>reportProgress</code></td>\n<td>message</td>\n</tr>\n<tr>\n<td><code>set_todos</code></td>\n<td><code>setTodos</code></td>\n<td>todos</td>\n</tr>\n</tbody></table></div>\n<ul>\n<li>L203–L204：<code>toolRegistry</code> Map（<code>TOOLS.forEach</code> 写入）。</li>\n<li><strong>Function <code>getToolList</code>（L206–L212）</strong> — 去掉 handler，只返回 name/description/inputSchema。</li>\n<li><strong>Function <code>callTool</code>（L214–L220）</strong> — 未知名 throw <code>Unknown tool: &quot;…&quot;</code>. Available tools: …；否则 <code>await tool.handler(args)</code>。无 Ask/Plan 模式参数。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/tools/fileops.js\">📄 文件名：<code>src/mcp/tools/fileOps.js</code></h3>\n<ul>\n<li><strong>Function <code>readFile</code>（L7–L48）</strong> — <code>resolveSafePath</code>；不存在 throw；是目录 throw 让用 list_dir；读 utf8；<code>computeHash</code>；按 1-based <code>offset</code>（默认 1）与 <code>limit</code>（默认 2000）切片；格式 <code>行号: 内容</code>；broadcast <code>file_read</code>；返回 filePath/totalLines/offset/limit/hash/content。</li>\n<li><strong>Function <code>writeFile</code>（L50–L68）</strong> — mkdir recursive；写盘；broadcast <code>file_written</code>；返回 success/filePath/size/hash。</li>\n<li><strong>Function <code>listDir</code>（L70–L123）</strong> — 内部 <code>scan(currentPath, currentDepth)</code>：depth &gt; maxDepth 返回 <code>[]</code>；跳过 <code>node_modules/.git/.cache/dist/build</code>；目录可 <code>children</code>（recursive 且 depth &lt; maxDepth）；文件带 size/mtime。默认 dirPath <code>.</code>、recursive false、maxDepth 3。</li>\n<li><strong>Function <code>grepSearch</code>（L121–L187）</strong> — 非法正则 throw；非 regex 则转义；目录递归 / 单文件；跳过 node_modules/.git/.cache/dist；读失败忽略；<code>matches</code> 最多 <strong>100</strong> 条；返回 query/totalMatches/matches。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/tools/patchengine.js\">📄 文件名：<code>src/mcp/tools/patchEngine.js</code></h3>\n<ul>\n<li><strong>Function <code>computeHash</code>（L8–L10）</strong> — SHA256 hex。</li>\n<li><strong>Function <code>resolveSafePath</code>（L12–L18）</strong> — resolve 后必须以 <code>workspaceRoot</code> 开头，否则 throw <code>Security error: path … is outside workspace root.</code></li>\n<li><strong>Function <code>parseSearchReplaceBlocks</code>（L28–L39）</strong> — 正则抓 <code>&lt;&lt;&lt;&lt;&lt; SEARCH</code> … <code>=====</code> … <code>&gt;&gt;&gt;&gt;&gt; REPLACE</code>；返回 <code>{search, replace}[]</code>。</li>\n<li><strong>Function <code>applyPatch</code>（L44–L167）</strong> 参数 <code>{ filePath, patch, expectedHash=null, dryRun=false }</code>：</li>\n</ul>\n<ol>\n<li>文件不存在：若第一块 search 为空则用 replace 当新内容，否则整份 patch 当新内容；dryRun 不写盘；否则 mkdir+write；broadcast <code>file_patched</code> isNewFile。</li>\n<li>文件存在：读盘算 hash；若提供 <code>expectedHash</code> 且既不等于全 hash 也不让全 hash <code>startsWith(expectedHash)</code> → throw <strong><code>Hash mismatch conflict</code></strong>（<strong>不是</strong>产品测试里的 <code>STALE_FILE</code> 字符串）。</li>\n<li>有 SEARCH 块：规范化换行；找不到则 trim 再试；再找不到 throw <code>Patch conflict in block #N</code>。</li>\n<li>无 SEARCH 块：若以 <code>--- </code> 开头且含 <code>@@</code> → <code>jsdiff.applyPatch</code>，失败 throw；否则整文件替换。</li>\n<li>dryRun 返回 diff 不写盘。</li>\n<li>否则写 <code>${fullPath}.tmp.${Date.now()}</code> 再 <code>renameSync</code>。</li>\n</ol>\n<hr />\n<h3 id=\"文件名-src/mcp/tools/executor.js\">📄 文件名：<code>src/mcp/tools/executor.js</code></h3>\n<ul>\n<li><strong>模块变量</strong> L6 <code>commandSequence = 0</code>。</li>\n<li><strong>Function <code>executeCommand</code>（L8–L105）</strong> — 返回 Promise。</li>\n<li>L10–L13：execId 自增；cwd resolve 到 workspace；timeout 默认 30s。</li>\n<li>L15–L20：broadcast <code>command_started</code>。</li>\n<li>L27–L28：win32 <code>powershell.exe -Command</code>，否则 <code>/bin/bash -c</code>。</li>\n<li>L38–L45：超时 SIGTERM，2s 后再 SIGKILL。</li>\n<li>stdout/stderr data → 累加并 broadcast <code>command_output</code>。</li>\n<li><code>error</code> → reject <code>Failed to start command</code>。</li>\n<li><code>close</code> → broadcast <code>command_finished</code>；超时也 <strong>resolve</strong>（带 <code>error</code> 字段），不 reject。</li>\n<li><strong>无命令黑名单</strong>（产品 <code>run_command</code> 有 confirm_dangerous）。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/mcp/tools/progresstracker.js\">📄 文件名：<code>src/mcp/tools/progressTracker.js</code></h3>\n<ul>\n<li><strong>模块状态 <code>currentTaskState</code>（L3–L10）</strong> — status/progress/stepName/lastMessage/lastUpdated/todos。</li>\n<li><strong>Function <code>reportProgress</code>（L12–L29）</strong> — percentage≥100 → status <code>completed</code> 否则 <code>in_progress</code>；clamp 0–100；broadcast <code>progress_updated</code>。</li>\n<li><strong>Function <code>setTodos</code>（L31–L48）</strong> — 每项 id 默认 <code>todo-N</code>；title 回落到 text/description/<code>Task item</code>；status 默认 pending；broadcast <code>todos_updated</code>。</li>\n<li><strong>Function <code>getTaskState</code>（L50–L52）</strong> / <strong><code>resetTaskState</code>（L54–L65）</strong> — 后者重置并 broadcast。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/api/routes.js\">📄 文件名：<code>src/api/routes.js</code></h3>\n<ul>\n<li><strong>职责：</strong> 给 Studio 页的 REST（无密钥校验）。</li>\n<li><strong>GET <code>/status</code>（L17–L35）</strong> — bridge=隧道 getStatus + port/version/serverName；workspace root/relRoot；tools 全列表；taskState；recentLogs 30。</li>\n<li><strong>POST <code>/bridge/start</code>（L39–L43）</strong> — body <code>provider</code> 默认 <code>'quick'</code>，<code>host</code>；<code>startTunnel</code>。</li>\n<li><strong>POST <code>/bridge/stop</code>（L45–L48）</strong> — <code>stopTunnel</code>。</li>\n<li><strong>POST <code>/bridge/rotate-secret</code>（L50–L57）</strong> — <code>rotateSecret</code> + 当前隧道状态。</li>\n<li><strong>POST <code>/task/reset</code>（L59–L62）</strong> — <code>resetTaskState</code>。</li>\n<li><strong>GET <code>/workspace/tree</code>（L67–L75）</strong> — <code>listDir({ dirPath:'.', recursive:true, maxDepth:4 })</code>。</li>\n<li><strong>GET <code>/workspace/file</code>（L77–L94）</strong> — query <code>path</code>；resolve 后必须 startsWith workspaceRoot 否则 403；不存在 404。</li>\n<li><strong>POST <code>/workspace/file</code>（L96–L115）</strong> — body <code>path</code>+<code>content</code>；同样 403 逃逸检查；mkdir+write。</li>\n<li><strong>POST <code>/workspace/run-tests</code>（L118–L129）</strong> — 固定 <code>executeCommand({ command:'npm test', cwd:'.', timeoutSec:15 })</code>。</li>\n<li><strong>POST <code>/simulator/call</code>（L134–L145）</strong> — body <code>tool</code> + <code>arguments</code>；broadcast <code>simulator_call</code>；<code>callTool</code>。</li>\n</ul>\n<p>挂到 app 后完整路径是 <code>/api/...</code>。</p>\n<hr />\n<h3 id=\"文件名-src/tunnel/tunnelmanager.js\">📄 文件名：<code>src/tunnel/tunnelManager.js</code></h3>\n<ul>\n<li><strong>Class <code>TunnelManager</code>（L5–L91）</strong></li>\n<li>实例字段：process、publicUrl、status（<code>offline|starting|online|error</code>）、provider（默认 <code>'quick'</code>）、error。</li>\n<li><strong>Method <code>isCloudflaredAvailable</code>（L14–L21）</strong> — <code>execSync('cloudflared --version')</code>，失败 false。</li>\n<li><strong>Method <code>startTunnel(provider='quick', customHost=null)</code>（L23–L75）</strong></li>\n<li>L24–L27：记下 provider，status=starting，broadcast。</li>\n<li>L29–L35：<code>provider==='local'</code> → publicUrl=<code>http://127.0.0.1:${port}</code>，online，写 <code>config.tunnelUrl</code>。</li>\n<li>L37–L64：若有 cloudflared → spawn <code>cloudflared tunnel --url http://127.0.0.1:${port}</code>（<strong>不看 named/ngrok，也不用 <code>customHost</code></strong>）；stderr 正则 <code>https://*.trycloudflare.com</code> 第一次命中则 online。</li>\n<li>L65–L73：无 cloudflared → 与 local 相同 fallback。</li>\n<li><strong>Method <code>stopTunnel</code>（L77–L85）</strong> — SIGTERM 子进程，status offline。</li>\n<li><strong>Method <code>getStatus</code>（L88–L97）</strong> — status/provider/publicUrl、<code>mcpEndpoint</code>=<code>${publicUrl}/mcp/${secretKey}</code>、secretKey、hasCloudflared。</li>\n<li>L100–L101：单例导出。</li>\n</ul>\n<p>入口 <code>index.js</code> 只调用 <code>startTunnel('local')</code>，默认<strong>不会</strong>自动开 Cloudflare。</p>\n<hr />\n<h3 id=\"文件名-src/utils/eventbus.js\">📄 文件名：<code>src/utils/eventBus.js</code></h3>\n<ul>\n<li><strong>Class <code>BridgeEventBus</code> extends EventEmitter（L3–L47）</strong></li>\n<li>L6–L8：<code>wsClients</code> Set；<code>logs</code> 数组；<code>maxLogs=500</code>。</li>\n<li><strong><code>addWsClient</code>（L11–L16）</strong> — add；close 时 delete。</li>\n<li><strong><code>broadcast(type, payload={})</code>（L18–L42）</strong> — 组 <code>{type,timestamp,payload}</code>；unshift 进 logs，超 500 则 pop；向 readyState===1 的 ws <code>send</code> JSON；再 <code>this.emit(type, payload)</code>。</li>\n<li><strong><code>getRecentLogs(limit=50)</code>（L44–L46）</strong> — <code>logs.slice(0, limit)</code>。</li>\n<li>L49–L50：单例导出。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-src/utils/diff.js\">📄 文件名：<code>src/utils/diff.js</code></h3>\n<ul>\n<li><strong>Function <code>createUnifiedDiff</code>（L3–L30）</strong> — <code>jsdiff.createTwoFilesPatch</code>；<code>diffLines</code> 统计 additions/deletions（空行 filter 掉）；返回 <code>{ patch, additions, deletions, changes }</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-public/index.html\">📄 文件名：<code>public/index.html</code></h3>\n<ul>\n<li><strong>职责：</strong> Studio 壳，无业务脚本（L237 <code>&lt;script src=&quot;app.js&quot;&gt;</code>）。</li>\n<li><strong>主要 DOM id：</strong> <code>bridge-status-pill</code>、<code>btn-rotate-secret</code>、<code>btn-copy-prompt</code>、<code>btn-run-tests</code>、<code>btn-copy-url</code>、<code>mcp-url-display</code>、<code>prompt-text-preview</code>、<code>file-tree-container</code>、<code>btn-refresh-files</code>、<code>tools-badge-container</code>、<code>tab-stream/editor/diff/simulator</code>、<code>activity-stream-list</code>、<code>code-editor-area</code>、<code>btn-save-file</code>、<code>diff-content-area</code>、<code>sim-tool-select</code>、<code>sim-args-input</code>、<code>btn-run-sim-call</code>、<code>sim-btn-diagnose/apply-fix/verify/full-flow</code>、<code>task-progress-bar</code>、<code>todo-list-container</code>、<code>terminal-stream-box</code>、<code>toast-notify</code>。</li>\n<li>标题写 <code>v0.6.9 (Core Repro)</code>。无 Chat 输入、无 <code>POST /api/chat</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-public/styles.css\">📄 文件名：<code>public/styles.css</code></h3>\n<ul>\n<li><strong>职责：</strong> 深色三栏布局，无 JS。</li>\n<li><strong><code>:root</code>（L1–L16）</strong> — <code>--bg-primary</code> <code>#181824</code> 等；<code>--accent-primary</code> <code>#6366f1</code>；<code>--font-mono</code> / <code>--font-sans</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-public/app.js\">📄 文件名：<code>public/app.js</code></h3>\n<ul>\n<li><strong>顶部变量 L3–L6：</strong> <code>ws</code>、<code>currentSecret</code>、<code>currentMcpUrl</code>、<code>currentSelectedFile</code>。</li>\n<li><strong>常量 <code>SIMULATOR_PRESETS</code>（L9–L62）</strong> — 各工具默认 JSON：read_file / apply_patch（针对 calculator divide）/ execute_command <code>npm test</code> / report_progress / set_todos / grep_search / list_dir。</li>\n<li><strong>启动 L63–L70：</strong> DOMContentLoaded → tabs、WS、status、文件树、bindEvents、simulator。</li>\n<li><strong>Function <code>showToast</code>（L72–L80）</strong> — <code>#toast-notify</code> 显示 2200ms。</li>\n<li><strong>Function <code>initTabs</code>（L82–L95）</strong> — <code>.tab-btn</code> 的 <code>data-tab</code> 切 <code>.tab-content</code>。</li>\n<li><strong>Function <code>initWebSocket</code>（L97–L119）</strong> — <code>ws(s)://location.host</code> 同端口；onmessage → <code>handleBridgeEvent</code>；onclose 3s 再连。</li>\n<li><strong>Function <code>handleBridgeEvent</code>（L122–L193）</strong> — switch <code>type</code>：</li>\n<li><code>tool_call_start/end</code> → 活动卡</li>\n<li><code>file_patched</code> → diff + 刷新树，若正在看该文件则重读</li>\n<li><code>command_output/started/finished</code> → 终端</li>\n<li><code>progress_updated</code> / <code>todos_updated</code> → 看板</li>\n<li><code>secret_rotated</code> → 重拉 status + toast</li>\n<li>其它 type（含服务端 <code>connected</code>）<strong>无 case，忽略</strong></li>\n<li><strong>Function <code>fetchInitialStatus</code>（L195–L223）</strong> — GET <code>/api/status</code>；拼 <code>origin/mcp/${secret}</code>；提示词 = URL + 空行 + 与产品相同的那句中文；渲染工具徽章与任务状态。</li>\n<li><strong>Function <code>renderToolsCatalog</code>（L225–L232）</strong></li>\n<li><strong>Function <code>loadFileTree</code>（L234–L275）</strong> — GET <code>/api/workspace/tree</code>；内部 <code>renderItems</code>（L241）；仅文件可 click 打开。</li>\n<li><strong>Function <code>loadFileContent</code>（L277–L291）</strong> / <strong><code>saveCurrentFile</code>（L293–L313）</strong> — GET/POST <code>/api/workspace/file</code>。</li>\n<li><strong>Function <code>addActivityCard</code>（L315–L346）</strong> / <strong><code>renderDiffView</code>（L348–L376）</strong> / <strong><code>appendTerminalLog</code>（L378–L386）</strong></li>\n<li><strong>Function <code>updateProgressUI</code>（L388–L394）</strong> / <strong><code>updateTodosUI</code>（L396–L424）</strong></li>\n<li><strong>Function <code>initSimulatorDefaults</code>（L426–L573）</strong></li>\n<li>下拉变化填 preset JSON；<code>btn-run-sim-call</code> POST <code>/api/simulator/call</code>。</li>\n<li>diagnose / apply-fix / verify 三钮改选中工具并 click 执行。</li>\n<li><code>sim-btn-full-flow</code>：依次 set_todos → progress 25% → npm test → read_file → progress 70% → apply_patch → 再 npm test → progress 100% → todos 全 completed。</li>\n<li><strong>Function <code>bindEvents</code>（L575–L621）</strong> — 复制 URL / 复制提示词 / rotate-secret / run-tests / 刷新树 / 保存 / 清活动 / 清终端 / reset task。</li>\n<li><strong>Function <code>escapeHtml</code>（L623–L631）</strong> — <code>&amp; &lt; &gt; &quot; '</code>。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-tests/patchengine.test.js\">📄 文件名：<code>tests/patchEngine.test.js</code></h3>\n<ul>\n<li><strong>职责：</strong> 原型自己的 5 步核心测试（不是产品 <code>agent-host/tests</code>）。</li>\n<li>L10–L13：在 <code>tests/temp_workspace</code> mkdir，改 <code>config.workspaceRoot</code>。</li>\n<li><strong>Function <code>runTests</code>（L15–L64）</strong></li>\n</ul>\n<ol>\n<li>写 <code>hello.txt</code></li>\n<li><code>readFile</code> 含 Hello World</li>\n<li>apply_patch 改 Line 2</li>\n<li>不存在的 SEARCH → 必须 throw 且消息含 <code>Patch conflict</code></li>\n<li>grep <code>Modified</code> totalMatches===1</li>\n<li><code>rmSync</code> 临时目录</li>\n</ol>\n<ul>\n<li>L66–L69：catch → exit(1)。<strong>不测</strong> hash mismatch。</li>\n</ul>\n<hr />\n<h3 id=\"文件名-workspace_demo/package.json\">📄 文件名：<code>workspace_demo/package.json</code></h3>\n<ul>\n<li><code>name</code>=<code>webagent-demo-project</code>；<code>main</code>=<code>src/calculator.js</code>；<code>scripts.test</code>=<code>node tests/calculator.test.js</code>。无 dependencies。</li>\n</ul>\n<h3 id=\"文件名-workspace_demo/src/calculator.js\">📄 文件名：<code>workspace_demo/src/calculator.js</code></h3>\n<ul>\n<li><strong>Function</strong> <code>add</code>/<code>subtract</code>/<code>multiply</code>（L5–L15）；<code>divide</code>（L17–L22）除 0 throw <code>Cannot divide by zero</code>；<code>power</code>（L24–L26）；L28–L34 <code>module.exports</code> 五函数。</li>\n</ul>\n<h3 id=\"文件名-workspace_demo/tests/calculator.test.js\">📄 文件名：<code>workspace_demo/tests/calculator.test.js</code></h3>\n<ul>\n<li>L10–L19 本地 <code>test(name, fn)</code> 计数 passed/failed。</li>\n<li>L21–L40：add/subtract/multiply/divide 正常；divide(10,0) <code>assert.throws</code>。</li>\n<li>L42–L52：failed&gt;0 则 exit 1。<strong>没有测 <code>power</code>。</strong></li>\n</ul>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录-历史行为\">3. 执行逻辑流（仅本目录，历史行为）</h2>\n<ol>\n<li><code>node src/index.js</code> 读 <code>config</code>（密钥内存随机）。</li>\n<li>同一端口：静态 Studio + WS + <code>POST /mcp/:secret</code> + <code>/api/*</code>。</li>\n<li>listen(3000) 后 <code>startTunnel('local')</code> → MCP URL 是 <code>http://127.0.0.1:3000/mcp/&lt;secret&gt;</code>。</li>\n<li>网页 Agent 若连上，只能调 8 工具；<code>initialize</code> <strong>不带</strong> instructions。</li>\n<li>Studio 用 REST 读文件、模拟器 <code>callTool</code>；页面里<strong>没有</strong>多步 Chat Agent。</li>\n<li>本目录 <code>npm test</code> 只验证 patch/read/grep，不启 HTTP。</li>\n</ol>\n<p><strong>现行对照（不要在本目录做）：</strong> 产品双端口（工作台 3000 / MCP 48271）、25 工具、OAuth、Chat、密钥可持久化。见 <code>webagent-core/agent-host/src/README.md</code>。</p>"},"code-server-cfg":{"id":"code-server-cfg","path":".config/code-server/README.md","group":"运行时","title":"code-server 配置说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：config.yaml","id":"文件名-config.yaml"},{"level":2,"text":"3. 执行逻辑流（仅本目录）","id":"3.-执行逻辑流-仅本目录"}],"html":"<h1 id=\"code-server-配置说明书\">code-server 配置说明书</h1>\n<p>当前处理目标：<code>.config/code-server/</code></p>\n<p>本目录只有 <code>config.yaml</code>。给第二种跑法（网页 VS Code）用，<strong>不是</strong> MCP 实现。无 <code>.js</code> / <code>.py</code> / <code>.html</code>。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> code-server 的 <code>--config</code> 文件。<code>webagent-core/scripts/run-code-oss.js</code> 拼出本路径，以 <code>--config</code> 传入。口令不写在本文件（避免进 Git），由 <code>codeServerAuth.js</code> 写到 <code>.local/share/code-server/webagent-password</code>。</li>\n<li><strong>兄弟依赖：</strong> 只被 <code>run-code-oss.js</code> 读取。自绘工作台 <code>run-webagent.cmd</code> <strong>不读</strong>本文件。</li>\n<li><strong>谁调用：</strong> <code>run-webagent-vscode.cmd</code> → <code>run-code-oss.js</code> → code-server 进程。</li>\n</ul>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-config.yaml\">📄 文件名：<code>config.yaml</code></h3>\n<ul>\n<li><strong>文件职责：</strong> 默认只听本机 3000，要登录，关掉遥测。共 5 行，无函数。明文密码不在这里。</li>\n<li><strong>每一个 Key：</strong></li>\n</ul>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Key</th>\n<th>用途</th>\n<th>取值</th>\n</tr></thead><tbody>\n<tr>\n<td><code>bind-addr</code></td>\n<td>监听地址</td>\n<td><code>127.0.0.1:3000</code>（与 <code>CODE_SERVER_PORT</code> 默认一致；进程参数里仍会再传 <code>--bind-addr</code>，可用 <code>WEBAGENT_BIND</code> 覆盖）</td>\n</tr>\n<tr>\n<td><code>auth</code></td>\n<td>登录</td>\n<td><code>password</code>（口令由环境变量 <code>PASSWORD</code> 传入，来自 <code>CODE_SERVER_PASSWORD</code> 或 <code>.local/share/code-server/webagent-password</code>）</td>\n</tr>\n<tr>\n<td><code>cert</code></td>\n<td>TLS 证书</td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>disable-telemetry</code></td>\n<td>关遥测</td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>disable-update-check</code></td>\n<td>关更新检查</td>\n<td><code>true</code></td>\n</tr>\n</tbody></table></div>\n<p>命令行还会加 <code>--disable-workspace-trust</code>、<code>--trusted-origins</code>（仅 <code>127.0.0.1</code> / <code>localhost</code>）、<code>--app-name Web Agent</code>（见 <code>run-code-oss.js</code>），不写在本 yaml 里。<code>CODE_SERVER_AUTH=none</code> 时命令行 <code>--auth none</code> 关掉登录。</p>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅本目录\">3. 执行逻辑流（仅本目录）</h2>\n<ol>\n<li>用户跑 vscode 入口。</li>\n<li><code>run-code-oss.js</code> 把 <code>--config</code> 指到本文件，并设置 <code>PASSWORD</code>。</li>\n<li>code-server 按 yaml + 命令行参数听 3000，浏览器先看到登录页。</li>\n<li>改本文件会影响网页 VS Code 的认证/绑定；<strong>不会</strong>改变 agent-host 的 48271。</li>\n</ol>"},"launchers":{"id":"launchers","path":"启动脚本说明.md","group":"入口","title":"仓库根启动脚本说明书","toc":[{"level":2,"text":"1. 模块概述","id":"1.-模块概述"},{"level":2,"text":"2. 文件级详细说明书","id":"2.-文件级详细说明书"},{"level":3,"text":"📄 文件名：run-webagent.cmd","id":"文件名-run-webagent.cmd"},{"level":3,"text":"📄 文件名：run-webagent.sh","id":"文件名-run-webagent.sh"},{"level":3,"text":"📄 文件名：run-webagent-vscode.cmd","id":"文件名-run-webagent-vscode.cmd"},{"level":3,"text":"📄 文件名：run-webagent-vscode.sh","id":"文件名-run-webagent-vscode.sh"},{"level":3,"text":"📄 文件名：check-env.cmd","id":"文件名-check-env.cmd"},{"level":3,"text":"📄 文件名：run-tests.cmd","id":"文件名-run-tests.cmd"},{"level":3,"text":"📄 文件名：.gitignore","id":"文件名-.gitignore"},{"level":3,"text":"📄 文件名：.gitattributes","id":"文件名-.gitattributes"},{"level":2,"text":"3. 执行逻辑流（仅这些脚本）","id":"3.-执行逻辑流-仅这些脚本"}],"html":"<h1 id=\"仓库根启动脚本说明书\">仓库根启动脚本说明书</h1>\n<p>当前处理目标：仓库根目录下的 <code>.cmd</code> / <code>.sh</code> / <code>.gitignore</code> / <code>.gitattributes</code>。</p>\n<blockquote><p><strong>为什么不写进根 <code>README.md</code>？</strong> GitHub 打开仓库首页就是根 <code>README.md</code>（产品说明、两种运行方式、Windows CMD）。第一阶段模板要求「在该文件夹内部建 README」，但根 README 已经是给人看的产品页，<strong>覆盖它会毁掉首页</strong>。根目录也不是一个「只含脚本的子文件夹」（还有 <code>webagent-core/</code>、<code>docs/</code>、<code>组件说明.md</code>），所以启动脚本单独放本文件。</p></blockquote>\n<p>无 Python / 无 HTML。脚本本身就是要说明的代码。行号按当前文件。</p>\n<hr />\n<h2 id=\"1.-模块概述\">1. 模块概述</h2>\n<ul>\n<li><strong>定位：</strong> Windows 双击入口 + 同功能 bash；环境检查；跑 agent-host 测试。不实现 MCP，只设环境变量并 <code>node</code>。</li>\n<li><strong>依赖：</strong> <code>webagent-core/agent-host/src/index.js</code>、<code>webagent-core/scripts/run-code-oss.js</code>、<code>agent-host/package.json</code> 的 <code>npm test</code>。</li>\n<li><strong>谁调用：</strong> 用户在资源管理器双击，或 CMD 里执行。</li>\n</ul>\n<p>端口以脚本里 <strong>echo 的为准</strong>（也与 <code>agent-host/src/config.js</code> 默认一致）：</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>环境变量</th>\n<th>默认</th>\n<th>用途</th>\n</tr></thead><tbody>\n<tr>\n<td><code>WORKSPACE_ROOT</code></td>\n<td>仓库根下的 <code>workspace\\</code></td>\n<td>要改的本机目录</td>\n</tr>\n<tr>\n<td><code>AGENT_HOST_PORT</code></td>\n<td><strong>48271</strong></td>\n<td>MCP / API</td>\n</tr>\n<tr>\n<td><code>WORKBENCH_PORT</code></td>\n<td><strong>3000</strong></td>\n<td>本机工作台 UI</td>\n</tr>\n</tbody></table></div>\n<hr />\n<h2 id=\"2.-文件级详细说明书\">2. 文件级详细说明书</h2>\n<h3 id=\"文件名-run-webagent.cmd\">📄 文件名：<code>run-webagent.cmd</code></h3>\n<ul>\n<li><strong>职责：</strong> 推荐入口：只起 Bridge（工作台 + MCP），不下载 VS Code。</li>\n<li><strong>步骤（91 行）：</strong></li>\n</ul>\n<ol>\n<li>L1–L5：<code>@echo off</code>；<code>chcp 65001</code>；<code>setlocal EnableExtensions</code>；窗口标题 <code>Web Agent</code>；<code>cd /d &quot;%~dp0&quot;</code>。</li>\n<li>L7–L8：若有第 1 个参数则 <code>WORKSPACE_ROOT=%~1</code>；否则 <code>%~dp0workspace</code>。</li>\n<li>L10–L11：<code>AGENT_HOST_PORT=48271</code>，<code>WORKBENCH_PORT=3000</code>。</li>\n<li>L18–L26：<code>where node</code> 失败 → 提示安装 LTS、<code>pause</code>、<code>exit /b 1</code>。</li>\n<li>L28–L33：有 node 无 npm → 提示重装并勾选 npm。</li>\n<li>L35–L47：工作区目录不存在时：无参数则 <code>mkdir</code>；<strong>有参数则报错退出</strong>（不替用户建任意路径）。</li>\n<li>L49–L62：打印 Node/npm 版本；Git / cloudflared 未安装只提示，不退出（Git 缺则 git 工具不可用；cloudflared 缺则本地 Chat 仍可用）。</li>\n<li>L65–L69：打印工作区、<code>http://127.0.0.1:3000</code>、MCP 端口 48271、Ctrl+C 停止。</li>\n<li>L72–L81：<code>cd webagent-core\\agent-host</code>；没有 <code>node_modules\\express</code> 则 <code>npm install --no-audit --no-fund</code>。</li>\n<li>L83：<code>node src\\index.js</code>（继承上面的环境变量）。</li>\n<li>L84–L91：非 0 退出则提示端口占用 3000/48271，<code>pause</code>，<code>exit /b %ERR%</code>。成功路径不 pause。</li>\n</ol>\n<h3 id=\"文件名-run-webagent.sh\">📄 文件名：<code>run-webagent.sh</code></h3>\n<ul>\n<li><strong>职责：</strong> 与 <code>.cmd</code> 同入口的 bash（指南主体仍是 CMD）。</li>\n<li>L1–L6：<code>set -e</code>；<code>ROOT</code>；<code>WORKSPACE_ROOT</code> 默认 <code>$ROOT/workspace</code>；两端口默认同上。</li>\n<li>L12–L16：进 agent-host；没有 <code>node_modules/express</code> 则 <code>npm install</code>。</li>\n<li>L18：<code>exec node src/index.js</code>。</li>\n<li><strong>不</strong>检查 node 是否安装、<strong>不</strong> mkdir 工作区。</li>\n</ul>\n<h3 id=\"文件名-run-webagent-vscode.cmd\">📄 文件名：<code>run-webagent-vscode.cmd</code></h3>\n<ul>\n<li><strong>职责：</strong> 备选：网页 VS Code。不在本文件里 npm install。</li>\n<li>L7–L8：工作区参数规则同主脚本。</li>\n<li>L15–L26：检查 node / npm。</li>\n<li>L28–L34：工作区<strong>必须已存在</strong>（不 mkdir），否则打印用法 <code>run-webagent-vscode.cmd D:\\code\\my-app</code>。</li>\n<li>L36–L38：提示第一次会从 npm 下载 code-server <strong>4.135.0</strong>；<strong>不要和 <code>run-webagent.cmd</code> 同时开</strong>（都要用 3000）；Ctrl+C 停止。</li>\n<li>L41：<code>node &quot;%~dp0webagent-core\\scripts\\run-code-oss.js&quot; &quot;%WORKSPACE_ROOT%&quot;</code>。</li>\n<li>L42–L49：非 0 则指向 <code>网页VSCode使用指南.md</code>。</li>\n<li><strong>不</strong>出现 <code>code-server-dist</code> 字符串（由 <code>codeServerNotRunnable.test.js</code> 锁住）。</li>\n</ul>\n<h3 id=\"文件名-run-webagent-vscode.sh\">📄 文件名：<code>run-webagent-vscode.sh</code></h3>\n<ul>\n<li>L4：工作区优先 <code>$1</code>，否则 <code>WORKSPACE_ROOT</code>，否则 <code>$ROOT/workspace</code>。</li>\n<li>L5–L6：<code>AGENT_HOST_PORT</code> 默认 48271；<code>CODE_SERVER_PORT</code> 默认 3000。</li>\n<li>L7：<code>exec node …/run-code-oss.js &quot;$WORKSPACE_ROOT&quot;</code>。</li>\n</ul>\n<h3 id=\"文件名-check-env.cmd\">📄 文件名：<code>check-env.cmd</code></h3>\n<ul>\n<li><strong>职责：</strong> 只读检查 PATH，不启动服务、不看仓库文件是否存在。</li>\n<li>L6：Node 版本或「未安装」+ nodejs.org。</li>\n<li>L7：npm 版本或未安装。</li>\n<li>L8：Git 版本或 git-scm 下载页。</li>\n<li>L9：cloudflared 已在 PATH，否则提示 <code>winget install --id Cloudflare.cloudflared</code>。</li>\n<li>L11–L13：说明缺什么按链接装，然后新开 CMD；<code>pause</code>。</li>\n</ul>\n<h3 id=\"文件名-run-tests.cmd\">📄 文件名：<code>run-tests.cmd</code></h3>\n<ul>\n<li><strong>职责：</strong> 一键跑现行产品测试（不是 <code>webagent-repro/tests</code>）。</li>\n<li>L12–L17：无 node 则让用户先跑 <code>check-env.cmd</code>。</li>\n<li>L19–L28：进 <code>webagent-core\\agent-host</code>；没有 express 则 npm install。</li>\n<li>L31–L33：打印测试目录后 <code>call npm test</code>。</li>\n<li>L36–L40：失败 → 指向仓库根 <code>测试说明.md</code>，pause，exit 该码。</li>\n<li>L42–L46：成功打印「仓库根不必再单独建 tests\\」，pause，exit 0。</li>\n</ul>\n<h3 id=\"文件名-.gitignore\">📄 文件名：<code>.gitignore</code></h3>\n<ul>\n<li><strong>职责：</strong> 不提交依赖、运行时、本机密钥、截图垃圾。</li>\n<li><strong>规则（按行，不是函数）：</strong></li>\n<li>L2–L3：<code>node_modules/</code>、<code>.npm/</code></li>\n<li>L6–L12：<code>code-server-app/</code>；<code>bin/code-server-runtime/*</code> 但 <strong>保留</strong> <code>bin/code-server-runtime/package.json</code>；<code>.cache/</code>、<code>.local/</code>、<code>*.tar.gz</code>、<code>*.tmp.*</code></li>\n<li>L15–L18：<code>dist/</code>、<code>build/</code>、<code>out/</code>、<code>*.log</code></li>\n<li>L21–L22：<code>.DS_Store</code>、<code>Thumbs.db</code></li>\n<li>L25–L26：<code>**/.webagent/config.json</code> 与 <code>**/.webagent/read-hashes.json</code>（任意工作区路径；不是整个 <code>.webagent/</code>，技能仍可提交）</li>\n<li>L29：<code>webagent-core/extensions-installed/extensions.json</code>（启动时会写成绝对路径）</li>\n<li>L32：<code>image-search/</code></li>\n</ul>\n<h3 id=\"文件名-.gitattributes\">📄 文件名：<code>.gitattributes</code></h3>\n<ul>\n<li><strong>职责：</strong> 强制 Windows 脚本在 Git 里用 CRLF，避免检出成 LF 后 CMD 解析异常。</li>\n<li><strong>规则（L1–L3，无函数）：</strong></li>\n<li>L1：<code>* text=auto</code> — 文本文件按平台转换换行。</li>\n<li>L2：<code>*.cmd text eol=crlf</code></li>\n<li>L3：<code>*.bat text eol=crlf</code></li>\n</ul>\n<p>根 <code>README.md</code>、<code>架构导读.md</code>、<code>组件说明.md</code>、<code>技术实现.md</code>、<code>测试说明.md</code> 是产品文档，不属于本「启动脚本」范围。</p>\n<hr />\n<h2 id=\"3.-执行逻辑流-仅这些脚本\">3. 执行逻辑流（仅这些脚本）</h2>\n<pre class=\"lang-text\"><code>用户双击 run-webagent.cmd [可选工作区路径]\n  → 有 Node 和 npm？否则退出\n  → 默认工作区不存在则 mkdir；自定义路径必须已存在\n  → 打印 UI :3000 与 MCP :48271\n  → cd agent-host，必要时 npm install\n  → node src/index.js   （实现不在本目录）\n\n用户双击 run-webagent-vscode.cmd &lt;已存在的工作区&gt;\n  → 检查 node/npm；工作区必须存在\n  → node webagent-core/scripts/run-code-oss.js\n      → 可能下载 gitignore 掉的 code-server-runtime\n\n用户双击 check-env.cmd → 只打印 PATH 里的 node/npm/git/cloudflared\n用户双击 run-tests.cmd → agent-host npm test</code></pre>\n<p>bash 文件与对应 <code>.cmd</code> 同流，供非 Windows 对照；bash 更短，少检查、少 pause。</p>"}},"fileIndex":[{"id":"webagent-core","path":"webagent-core/README.md","group":"产品"},{"id":"agent-host","path":"webagent-core/agent-host/README.md","group":"产品"},{"id":"src","path":"webagent-core/agent-host/src/README.md","group":"进程"},{"id":"mcp","path":"webagent-core/agent-host/src/mcp/README.md","group":"进程"},{"id":"tools","path":"webagent-core/agent-host/src/tools/README.md","group":"进程"},{"id":"agent","path":"webagent-core/agent-host/src/agent/README.md","group":"进程"},{"id":"models","path":"webagent-core/agent-host/src/models/README.md","group":"进程"},{"id":"api","path":"webagent-core/agent-host/src/api/README.md","group":"进程"},{"id":"tunnel","path":"webagent-core/agent-host/src/tunnel/README.md","group":"进程"},{"id":"utils","path":"webagent-core/agent-host/src/utils/README.md","group":"进程"},{"id":"tests","path":"webagent-core/agent-host/tests/README.md","group":"产品"},{"id":"workbench","path":"webagent-core/workbench/README.md","group":"界面"},{"id":"extension","path":"webagent-core/extension/README.md","group":"界面"},{"id":"ext-installed","path":"webagent-core/extensions-installed/README.md","group":"界面"},{"id":"scripts","path":"webagent-core/scripts/README.md","group":"界面"},{"id":"workspace","path":"workspace/README.md","group":"工作区"},{"id":"bin","path":"bin/README.md","group":"运行时"},{"id":"repro","path":"webagent-repro/README.md","group":"冻结"},{"id":"code-server-cfg","path":".config/code-server/README.md","group":"运行时"},{"id":"launchers","path":"启动脚本说明.md","group":"入口"}]};
