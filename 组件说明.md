# 这个仓库是干什么的

先不用记文件夹名字。用一句话：

> **程序跑在你自己的电脑上，真正改硬盘上的代码；网页上的 AI 只负责「想」，想完通过一条加密地址来调用你电脑上的工具。**

同类产品公开文档可参考：[docs.shuncode.top](https://docs.shuncode.top/docs/intro/)。本仓库是本机工作台 + 独立 agent-host，不是 exe 安装包。

- **按按钮、装软件、CMD 命令：** 请跟 [使用指南.md](./使用指南.md)（Windows）。
- **DeepSeek 网页怎么装扩展：** [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md)。
- **多个网页 AI 用 Chat Plus：** [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md)（不 fork、不装 MCP-Gateway）。
- **浏览器里要真正的 VS Code：** [网页VSCode使用指南.md](./网页VSCode使用指南.md)。
- **完全没写过这种程序、先要人话+比喻：** [架构导读.md](./架构导读.md)。
- **对着源码逐步直译（函数拆步骤、分支不漏）：** [技术实现.md](./技术实现.md)。
- **本文：** 整条工作流、为什么这样装、打开 GitHub 时每个目录是谁。过时的交接稿已经删掉，以本文为准。

---

## 1. 三个必须先分清的东西

很多人第一次晕，是因为把「程序」和「被改的项目」看成同一个文件夹。

| 名字 | 它是什么 | 打个比方 |
|---|---|---|
| **本 Git 仓库** `web_agent` | Web Agent 这套**程序**的源码 | 工具箱 |
| **工作区**（默认 `workspace/`，也可以是 `D:\code\my-app`） | Agent **允许读写的那一个目录** | 正在修的那台机器 |
| **网页上的 AI**（Arena / DeepSeek / ChatGPT…） | 只会聊天、下指令 | 远方的师傅，看不见你的车库，除非你把门开一条缝 |

默认双击启动时，工作区就是仓库里的 `workspace\`（一个带测试的小计算器）。**它不是 Web Agent 引擎。** 你平时干活的项目，要用启动参数指过去，见第 4 节。

再记三个运行时名词：

1. **agent-host**  
   真正动手的 Node 进程。读文件、打补丁、跑 `npm test`，全是它。源码在 `webagent-core/agent-host/`。

2. **工作台**  
   浏览器打开 http://127.0.0.1:3000 看到的黑底界面（欢迎页、CHAT、BRIDGE）。源码在 `webagent-core/workbench/`。它自己**改不了磁盘**，按钮都会打到 agent-host。

3. **Bridge / MCP / 隧道**  
   - **MCP**：一套「AI 调工具」的协议。本仓库把工作区暴露成 MCP 服务。  
   - **Bridge**：工作台里「启动 Bridge」那一页，等于打开大门。  
   - **隧道（cloudflared）**：云上的网页访问不到你电脑的 `127.0.0.1`，所以要一条临时的 `https://xxxx.trycloudflare.com` 把请求转发进家。本机 Chat **不需要**隧道。

---

## 2. 整条工作流（从双击到文件被改）

电脑上同时活着的，其实是这样：

```
你的 Windows 电脑
│
├── 黑色 CMD 窗口          必须一直开着 = agent-host 还在跑
│     端口 3000  →  工作台网页（给你点）
│     端口 48271 →  工具真正改磁盘的地方
│
├── 浏览器 http://127.0.0.1:3000
│     右侧 CHAT   = 本机说话，不经过互联网
│     右侧 BRIDGE = 看「外面的 AI」有没有在调工具
│
└──（只有网页 Agent 改盘时才需要）
      cloudflared  把 48271 映射成 https://xxxx.trycloudflare.com
```

下面三条路径，**改文件的引擎是同一套**。差别只是「谁下指令」。

### 路径 A：本机 Chat（最简单，不必装 cloudflared）

适合：先确认程序能跑、先改演示计算器、或不想把电脑暴露到公网。

```
你在右侧 CHAT 打字
        │
        ▼
工作台 app.js  POST /api/chat
        │
        ▼
agent-host 的 runChat.js
   没填 API Key → 内置探索 Agent（会搜、读、必要时打补丁并跑测试）
   填了 Key     → 按 OpenAI 兼容接口让模型循环调工具（最多约 10 步）
        │
        ▼
tools/index.js  按模式上锁
   Ask / Plan = 只读
   Code / Agent = 可以 apply_patch、run_command
        │
        ▼
工作区磁盘上的文件变了
```

输入框下 **Agent ▾** 默认就是 **Web Agent Code**（和 Copilot 侧栏的 Agent 一样：直接动手）。

- Ask：只读，分析项目、查原因。  
- Plan：只读 + 多模型博弈（意见一致再行动）。  
- Code / Agent：搜 → 读 → 补丁 → 再测。

**为什么本机 Chat 不走隧道：** 浏览器和工作台、agent-host 都在你这台电脑上，`127.0.0.1` 互相找得到。把隧道开给本机 Chat 只会多一个故障点。

### 路径 B：网页 Agent 改本机仓库（这个项目存在的理由）

适合：你想用 Arena、DeepSeek 网页、Chat Plus 覆盖的其它网页、或其它能调工具的聊天栏，改**你自己硬盘**上的代码。

**为什么必须绕这么一圈：**

云上的 ChatGPT / Arena / DeepSeek 运行在别人的机房。它们默认只能看到对话文字，**进不了你的 `D:\code`**。如果你把 MCP 地址写成 `http://127.0.0.1:48271/...`，云端去连的是「它自己那台机器的 127.0.0.1」，不是你家电脑。所以：

1. 本机 agent-host 在 48271 上提供 MCP。  
2. cloudflared 开一条 **Quick Tunnel**（不用 Cloudflare 账号），得到  
   `https://随机名.trycloudflare.com`。  
3. 网页 Agent 访问的是这个 https 地址；Cloudflare 再转到你电脑的 48271。  
4. URL 里带一串**密钥**。拿到完整地址的人可以读你的文件、在你电脑上跑命令。所以**不要发到群里、Issue、截图网盘**。

```
云上的网页 Agent  （Arena / DeepSeek / 其它）
        │  HTTPS
        ▼
https://xxxx.trycloudflare.com/mcp/<密钥>
        │  Cloudflare 转到你家
        ▼
本机 agent-host :48271
        │  tools/call
        ▼
D:\code\my-app  里的文件被改
        │  WebSocket 推送
        ▼
工作台右侧 BRIDGE 出现工具卡片
```

几种接法（都不绑死 ChatGPT，**不必 Plus** 也能走扩展或贴 URL）：

| 你用的聊天栏 | 它会不会真的调 MCP | 你要做的事 |
|---|---|---|
| **Arena** 等能把 URL 当工具后端的网页栏 | 会（把第一句里的 URL 拿去连） | Bridge 里**复制提示词**（URL + 那句连接说明），新对话整段当第一句 |
| **DeepSeek 网页** | 网页自己**不会**。要靠第三方扩展 DeepSeek++ 当手 | Chrome/Edge 装扩展，侧边栏 MCP 填 **Streamable HTTP** 和那一行 URL。见 [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md)。不要装它的 Shell Native Host |
| **Chat Plus 扩展（多网站）** | 网页自己**不会**。扩展当手 | 从 GitHub 编译 Chat Plus，侧边栏填 **Streamable HTTP** 和那一行 URL。见 [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md)。不要装 MCP-Gateway，也不要把 GPL 源码拷进本仓库 |
| **ChatGPT 免费普通聊天** | **不装扩展时通常不会** | 改用本机 Chat、Arena、DeepSeek++，或 Chat Plus |
| **ChatGPT Plus 开发者模式**（可选） | 会（官方连接器） | 填规范地址 `https://….trycloudflare.com/mcp`（**不要**带密钥），OAuth 配对码只出现在本机 Bridge 页 |

**为什么 DeepSeek / ChatGPT 网页要扩展、Arena 只要贴 URL：**  
Arena 这类产品聊天栏本身就会按 MCP 去调工具。DeepSeek、免费 ChatGPT 网页模型只会打字，不会开 HTTP 调你的电脑；DeepSeek++ 或 Chat Plus 识别它打出的工具标记，替它执行，再把结果塞回同一条对话。本仓库**不 fork 扩展源码**，只写安装步骤。Chat Plus 源码里就能填任意 Streamable HTTP，连我们不必再跑他们的 MCP-Gateway。

**为什么密钥放在 URL 里，ChatGPT Plus 却用另一套：**  
贴 URL 的客户端没有登录框，密钥等于「门卡」。ChatGPT 连接器支持 OAuth，所以给它看不带密钥的 `/mcp`，在本机输入一次性配对码，避免把长期密钥贴进聊天。

Quick Tunnel 的**域名每次「启动 Bridge」都会变**，必须重新复制。密钥存在工作区 `.webagent\config.json`，一般不换；点「重置 MCP 地址」才会换，旧链接立刻失效。

### 路径 C：网页里打开真正的 VS Code（换皮，不换引擎）

适合：想要语法高亮、多文件标签、VS Code 插件生态，同时还要同一套 Chat / Bridge。

```
run-webagent-vscode.cmd
        │
        ├── agent-host 仍在 :48271（环境变量 WEBAGENT_SKIP_WORKBENCH=1，不再占用 3000）
        └── 从 npm 下载完整 code-server 到 bin/code-server-runtime/（不进 Git）
              监听 :3000 = 真 VS Code Web
              侧栏插件来自 webagent-core/extension/
```

**不要和 `run-webagent.cmd` 同时开**（抢 3000 端口）。  
网页 Agent 改磁盘仍然走路径 B，和界面是自绘工作台还是 VS Code **无关**。

Git 里曾经放过一份残缺的 code-server 切片，缺关键 `out/`，**已经删掉**。现在第一次跑 vscode 脚本会从 npm 拉完整包。

---

## 3. 具体怎么运行

Windows 用 **CMD**（不要把 Git Bash / WSL 当主路径）。命令的逐条说明、防火墙、排错见 [使用指南.md](./使用指南.md)。这里只把「按下去之后发生了什么」讲清楚。

### 3.1 最短路径（演示计算器）

1. 安装 [Node.js LTS](https://nodejs.org/)（勾选 npm）。装完**新开** CMD。  
2. 仓库根目录（能看见 `run-webagent.cmd` 的那一层）运行：

```bat
check-env.cmd
run-webagent.cmd
```

3. 黑色窗口应出现类似：

```
UI        http://127.0.0.1:3000
MCP       http://127.0.0.1:48271/mcp/……
Workspace D:\code\web_agent\workspace
```

4. 浏览器打开 **http://127.0.0.1:3000**，右侧 CHAT 说一句：`分析当前项目实现了什么功能`。  
5. 停止：在黑色窗口按 **Ctrl+C**。窗口关了，工作台和工具就都停了。

`run-webagent.cmd` 实际做的事：检查 Node → 若没装过依赖就 `npm install` → `node webagent-core\agent-host\src\index.js`。没有第二套隐藏服务。

### 3.2 改你自己的仓库（重点）

默认改的是本仓库的 `workspace\`。要改 `D:\code\my-app`：

```bat
cd /d D:\code\web_agent
run-webagent.cmd D:\code\my-app
```

`D:\code\my-app` 必须已经存在。agent-host 会把**工作区根**设成它，工具禁止写到这个目录外面。看黑色窗口的 `Workspace` 那一行，对上了再开始改。

### 3.3 让网页 Agent 动手

1. 安装 cloudflared（`winget install --id Cloudflare.cloudflared`，然后**新开** CMD）。  
2. 仍然用 `run-webagent.cmd` 挂你的仓库。  
3. 工作台左下角齿轮 → **Bridge** → **启动 Bridge**。默认 Cloudflare 会拉起 Quick Tunnel；成功后地址是 `https://….trycloudflare.com/mcp/…`。没有 cloudflared 时按钮仍成功，MCP 走当前页面源（本机预览可用，云上连不上）。Named / ngrok 目前不会 spawn。  
4. 按第 2 节表格连接 Arena、DeepSeek++ 或 Chat Plus。  
5. 右侧切到 **BRIDGE**，应出现工具调用；资源管理器里文件会变。

### 3.4 验证实现不是空文档

```bat
run-tests.cmd
```

测试在 `webagent-core\agent-host\tests\`，根目录不必再放一份 `tests\`。说明见 [测试说明.md](./测试说明.md)。

Linux / macOS 对应 `./run-webagent.sh`。网页 VS Code：`run-webagent-vscode.cmd`。

---

## 4. 一次「打补丁」在代码里怎么走

以「DeepSeek 网页让你修测试」为例，方便对照源码：

1. 你在 `chat.deepseek.com` 说：修测试并跑 `npm test`。  
2. DeepSeek++ 拦截模型的工具标记，对隧道地址发 MCP JSON-RPC：`initialize` → `tools/list` → `tools/call`。  
3. `webagent-core/agent-host/src/mcp/server.js` 校验 URL 里的密钥（或 OAuth Bearer）。  
4. `src/mcp/instructions.js` 在 `initialize` 时把规则塞给客户端（Ask 只读、要先 `read_files` 拿哈希、再 `apply_patch`…）。客户端若没吃到，还可以读资源 `webagent://instructions`。  
5. `src/tools/index.js` 按当前模式决定能不能写。网页这边默认按 Code 执行。  
6. `read_files`（`fileOps.js`）读文件并返回 **sha256**，主机写入 `.webagent/read-hashes.json`（重启后还在，直到点「清除本轮统计」）。  
7. `apply_patch`（`patchEngine.js`）用 SEARCH/REPLACE。刚读过同一路径可以不带 `expectedHash`；从未读过会 `HASH_REQUIRED`（带 `currentHash`）；哈希过期 `STALE_FILE`。网页端工具失败是 MCP `isError` 文本，不是掉线。  
8. `run_command` / `start_command`（`executor.js`）在工作区跑命令。**Windows 走 PowerShell**。网页若调用工具名 `bash`/`cat`/`ls`，主机会收成正式名，但命令字符串仍是 PowerShell。  
9. `eventBus.js` 通过 WebSocket `/ws` 推到工作台，右侧 BRIDGE 画出工具卡片。  
10. 磁盘上的文件已经变了。密钥、`.env`、SSH 私钥、`.webagent/config.json` 会被 `sensitive.js` 拦住，读不出来。

本机 Chat 从第 3 步换成 `POST /api/chat` → `runChat.js`，后面同一套工具。

---

## 5. 为什么要这样实现

这些选择都是故意的，不是随便拆目录。

| 做法 | 为什么 |
|---|---|
| **UI 和工作台在 3000，MCP 在 48271** | 两个端口两件事。网页 VS Code 要占用 3000 时，agent-host 可以只留 48271（`WEBAGENT_SKIP_WORKBENCH=1`）。第一代原型 `webagent-repro` 把网页和 MCP 挤在同一个 3000，换皮时会打架。 |
| **模型循环不写进 VS Code 内核** | 编辑器只是外壳，推理和工具在独立 agent-host。换自绘工作台或 code-server，改盘逻辑不用重写。同类产品也是这个拆法（见 [docs.shuncode.top](https://docs.shuncode.top/docs/intro/)）。 |
| **Git 不内嵌 code-server** | 上游不发布 Windows 安装包；内嵌切片缺 `out/`，启动即报错。改为第一次运行时 npm 下载完整包到 `bin/code-server-runtime/`（Git 忽略内容）。 |
| **不把 DeepSeek++ 源码拷进仓库** | 它是社区扩展，有自己的商店更新。本仓库只需要它当「手」；改磁盘仍走我们的 agent-host。再装 `deepseek-pp-shell-host` 会多一个本机命令通道，和 MCP 无关。 |
| **不把 Chat Plus 源码拷进仓库** | GPL v3 会传染许可证；上游自己更新。本仓库只写编译加载步骤。连 Bridge **不要**再装 `aiguicai/MCP-Gateway`（我们的 agent-host 已经是 MCP）。 |
| **默认工作区是 `workspace/`，不是仓库根** | 仓库根是 Web Agent 程序。若把根当工作区，Agent 可能改到 `run-webagent.cmd`、指南、测试。工作区边界把「被修的项目」和「修它的工具」分开。 |
| **`.webagent` 建在工作区里，不在 GitHub 仓库根** | 类似项目里的 `.vscode`：设置、密钥、Skills 属于**正在编辑的那个项目**。所以 GitHub 打开 `web_agent` 根目录看不见 `.webagent`，要进 `workspace/`。你自己的 `D:\code\my-app` 跑过一次、保存过设置后才会出现。 |
| **Ask / Plan / Code 三档锁工具** | 防止「我只是问一句」却被改文件。Ask/Plan 调用 `apply_patch` 会被拒。 |
| **改文件优先 `apply_patch`，而不是整文件覆盖** | 带哈希预检，避免模型凭记忆覆盖你刚改的内容。 |
| **命令在 Windows 上走 PowerShell** | 用户主路径是 Windows CMD。假设 bash 会在别人电脑上直接失败。 |
| **留下 `webagent-repro/` 但冻结** | 方便对照「最早那版 Bridge」。启动脚本**不会**进去。不要 `npm start`。 |

---

## 6. 打开 GitHub 时这些文件夹是谁

```
web_agent/                         ← 你克隆下来的 Git 仓库 = 工具箱
├── run-webagent.cmd               ★ Windows 主入口（自绘工作台）
├── run-webagent-vscode.cmd        可选：浏览器里真 VS Code
├── check-env.cmd / run-tests.cmd
├── 使用指南.md                    按键说明书
├── 组件说明.md                    本文
├── 架构导读.md                    人话 + 比喻 + 文件落地 + 行业叫法
├── 网页DeepSeek使用指南.md
├── 网页ChatPlus使用指南.md
├── 网页VSCode使用指南.md
├── 技术实现.md                    代码级实现
├── 测试说明.md
├── webagent-core/                 ★ 正在维护的程序
│   ├── workbench/                 你在 3000 端口看到的网页
│   ├── agent-host/                改磁盘、MCP、Chat、隧道
│   ├── extension/                 VS Code 插件源码
│   └── extensions-installed/      给 code-server 的已安装副本
├── workspace/                     默认演示项目（计算器），不是引擎
├── webagent-repro/                第一代原型，勿运行
├── bin/                           网页 VS Code 下载到这里（内容不进 Git）
└── .config/code-server/           code-server 的 yaml（无密码、0.0.0.0:3000）
```

| 你在 GitHub 看到的 | `run-webagent.cmd` 会不会用它 | 一句话 |
|---|---|---|
| **`webagent-core/`** | **会** | 现行产品 |
| **`workspace/`** | 当作默认工作区挂上 | 演示计算器 |
| **`webagent-repro/`** | **不会** | 旧原型 |
| **`bin/code-server-runtime/`** | 只有 vscode 脚本会 | npm 下的完整 code-server |
| **`image-search/`** | 不会 | 本地截图，已 gitignore |

### 6.1 `webagent-core/workbench/` —— 给人看的壳

`index.html` + `app.js`（入口）+ `js/*.js`（按职责拆开，无打包）+ `styles.css`。欢迎页「Web Agent / 编辑进化」、右侧 CHAT / BRIDGE、齿轮里的自定义设置、**打开 DeepSeek** 等按钮，都在这里。它通过 `/api/*` 和 `/ws` 找 agent-host。

### 6.2 `webagent-core/agent-host/` —— 真正干活的进程

入口 `src/index.js`：同一套 Express 应用听**两个**端口（网页 VS Code 模式下只听 48271）。

| 目录 | 干什么 |
|---|---|
| `src/mcp/` | MCP 协议：`server.js` 收 JSON-RPC；`clients.js` 里是本机 Chat / Arena / DeepSeek / Chat Plus / ChatGPT 各怎么连；`oauth.js` 给 Plus 连接器；`instructions.js` 是给模型的规则 |
| `src/tools/` | 约 25 个正式名（另收 `bash`/`cat`/`ls` 等别名）。Ask/Plan 只读，Code 可写。危险命令要 `confirm_dangerous`；删文件要 `confirm`；覆盖 `write_file` 要 `confirm_overwrite` 或哈希仍匹配。不是 git 仓库时 `git_status` 返回 `available:false` |
| `src/agent/` | 本机 Chat：`runChat.js`、接 API 的 `openai.js` |
| `src/tunnel/` | 启动 `cloudflared tunnel --url http://127.0.0.1:48271`，从日志里抠 `*.trycloudflare.com` |
| `src/api/routes.js` | 工作台用的 REST：状态、启停 Bridge、Chat、文件树、自定义设置 |
| `src/models/` | 工作区 `.webagent/` 里的配置、指令、记忆 |
| `tests/` | 产品测试，`run-tests.cmd` 跑这里 |

### 6.3 `extension/` 和网页 VS Code

只在 code-server / VS Code 加载插件时生效。侧栏 Chat、原生 Chat `@webagent`（默认 Agent）打的仍是本机 `http://127.0.0.1:48271/api/chat`。`extensions.json` 含本机绝对路径，启动时重写，**不进 Git**。

自绘工作台不走这套插件 API，但同样有 **Agent ▾**。

### 6.4 工作区里的 `.webagent/`

建在**被编辑的项目根**下，不是 `web_agent` 根下。

| 路径 | 提交到 Git？ | 干什么 |
|---|---|---|
| `.webagent/config.json` | **不要**（含 MCP 密钥） | 模型列表、Bridge、secret |
| `.webagent/instructions.md` | 可以 | 始终生效的指令，会进 MCP `initialize.instructions` |
| `.webagent/preference.md` | 可以 | OS / Shell / 回复语言 |
| `.webagent/tech-stack.md` | 可以 | 语言、测试命令；Code 模式跑测试时优先用 |
| `.webagent/skills/<名>/SKILL.md` | 可以 | 技能；界面「技能引导」三步生成 |
| `.webagent/memory/` | 按需 | Agent 跨对话备忘 |
| `.webagent/customizations.json` | 按需 | 智能体 / 挂钩 / 提示等 UI 配置 |

演示区提交了 `workspace/.webagent/instructions.md` 和三个 skill（`fix-tests`、`review`、`docs-sync`）。`config.json` 与 `read-hashes.json` 被 `.gitignore` 排除。`review` 是只读审查流程；`docs-sync` 是给 `load_skill` 读的约定：改功能必须同步改说明书，动到「为什么这样装」时还要按四层写法改 [架构导读.md](./架构导读.md)。

---

## 7. 常见误读

1. **「应该 `cd webagent-repro && npm start`」**  
   那是旧原型，端口和工具都对不上现在的指南。用根目录 `run-webagent.cmd`。

2. **「Git 里没有 code-server 就开不了 VS Code」**  
   用 `run-webagent-vscode.cmd`，它会从 npm 装到 `bin/code-server-runtime/`。

3. **「`.webagent` 应该出现在 web_agent 仓库根上」**  
   不会。它属于被编辑的项目。

4. **「把这个 GitHub 发给别人，别人就能改我电脑」**  
   不会。程序必须跑在**你的**电脑上，再用隧道让云上的网页连进来。MCP 地址不能公开。

5. **「要把 DeepSeek++ 源码拷进本仓库才能连 DeepSeek」**  
   不要。商店安装即可。也不要为连 Bridge 去装 `deepseek-pp-shell-host`。

6. **「免费 ChatGPT 聊天栏贴了 URL 就会改磁盘」**  
   通常不会。它没有自定义 MCP。用本机 Chat、Arena、DeepSeek++，或 Chat Plus 扩展当手。

7. **「要把 Chat Plus 源码拷进本仓库，或先装 MCP-Gateway」**  
   不要。GPL，不 vendor。扩展侧填我们的 Streamable HTTP 即可。

8. **「工作台里点「打开 DeepSeek」就会连上」**  
   那一页只复制地址、给步骤。扩展只能加载在本机 Chrome/Edge 的 `chat.deepseek.com` 上，内置假浏览器跑不了扩展。

---

## 8. 还想看细节时读哪篇

| 问题 | 文档 |
|---|---|
| 没装过、要从零点到改文件 | [使用指南.md](./使用指南.md) |
| 为什么这样装（人话、比喻、磁盘上长什么样） | [架构导读.md](./架构导读.md) |
| DeepSeek 网页 + 扩展 | [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md) |
| 多个网页 AI + Chat Plus | [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md) |
| 浏览器里真 VS Code | [网页VSCode使用指南.md](./网页VSCode使用指南.md) |
| 测试覆盖了什么 | [测试说明.md](./测试说明.md) |
| **函数逐步直译、if/try 分支、文件在链条上的位置** | **[技术实现.md](./技术实现.md)** |
| 同类产品公开文档 | https://docs.shuncode.top/docs/intro/ |
