# 仓库组件说明

这份文档解释 **GitHub 上每一个顶层目录和主要文件是干什么的**。对照源码，不写口号。

官方产品文档：https://docs.shuncode.top/docs/intro/

Windows 怎么跑：请读 [使用指南.md](./使用指南.md)。本文只讲「仓库里这些文件夹分别负责什么」。

---

## 0. 先看结论

当前 **真正会启动、给 ChatGPT / Arena 改本机仓库用的路径** 只有这一条：

```
run-shuncode.cmd
        │
        ▼
shuncode-core/agent-host/src/index.js
        │
        ├── 端口 3000  →  shuncode-core/workbench/   浏览器工作台 UI
        ├── 端口 48271 →  MCP + REST API
        └── WORKSPACE_ROOT →  默认是本仓库的 workspace/，也可换成你自己的项目
```

因此：

| 你在 GitHub 上看到的目录 | 现在会不会被 `run-shuncode.cmd` 启动 | 一句话 |
|---|---|---|
| **`shuncode-core/`** | **会** | 现行产品：工作台 + 独立 agent-host + MCP |
| **`workspace/`** | 作为默认工作区被挂载 | 演示项目（计算器），**不是** ShunCode 程序本身 |
| **`shuncode-repro/`** | **不会** | 更早一版「纯 Bridge Host」原型，端口、工具集、UI 都更小 |
| **`bin/code-server-runtime/`** | **`run-shuncode-vscode.cmd` 会** | 从 npm 下载的**完整** code-server 4.135.0（不进 Git）。见第 1.2 节 |
| `.config/code-server/` | 不会（给 code-server 用的配置） | 若将来手动启动 code-server，监听 `0.0.0.0:3000`、无密码 |
| `image-search/` | 不会 | 本地截图素材，不应当进 Git |

`.shuncode/` **不在仓库根目录**。它出现在 **被编辑的那个工作区** 里（默认 `workspace/.shuncode/`），类似项目里的 `.vscode`。详见第 5 节。

---

## 1. 你点名的两个目录

### 1.1 `shuncode-repro/` —— 第一代 Bridge Host 原型

**职责：** 把「本机工作区 → Streamable HTTP MCP」做成一个 **独立小服务**：一个 Node 进程同时提供简易网页控制台和 MCP。

它 **不是** 现在 Windows 用户该跑的程序。`run-shuncode.cmd` / `run-shuncode.sh` **都不会** `cd` 进这里。

| 路径 | 职责 |
|---|---|
| `src/index.js` | 入口。Express + WebSocket。静态站 `public/`，`/mcp`、`/api` 挂在 **同一个端口**（默认 **3000**） |
| `src/config.js` | 端口、密钥、工作区（默认 `workspace_demo/`） |
| `src/mcp/server.js` | MCP JSON-RPC：`initialize` / `tools/list` / `tools/call` |
| `src/mcp/auth.js` | URL 里的 `/mcp/<secret>` 校验与重置 |
| `src/mcp/tools/` | **8 个工具**：`read_file`、`write_file`、`apply_patch`、`execute_command`、`list_dir`、`grep_search`、`report_progress`、`set_todos` |
| `src/tunnel/tunnelManager.js` | 探测 `cloudflared`，能找到就开 Quick Tunnel，否则退回 `127.0.0.1` |
| `src/api/routes.js` | 给网页控制台用的 REST（状态、模拟 Agent、调工具） |
| `src/utils/diff.js`、`eventBus.js` | 补丁 diff、WebSocket 广播工具调用 |
| `public/` | 简易 Host Studio：MCP 地址、工具流、Diff、TODO |
| `workspace_demo/` | 它自己的演示计算器，和根目录 `workspace/` 是两份拷贝 |
| `tests/patchEngine.test.js` | 补丁引擎单测 |

和现行 `shuncode-core` 的差别：

- 工作台不是 VS Code 外观，没有 Ask/Plan/Code 完整 UI、没有 Skills / 指令 / 内置浏览器。
- MCP 与 UI **抢同一个 3000 端口**；现行方案是 UI 3000、MCP 48271 分开。
- 工具大约 8 个；现行约 **25** 个（含 git、异步命令、memory、skills、workspace_info）。
- 密钥每次启动随机，不写进工作区 `config.json`。
- 没有 Windows CMD 启动脚本。

**什么时候才需要碰它：** 对照「最早那版 Bridge 长什么样」，或单独 `cd shuncode-repro && npm start` 做最小 MCP 实验。日常改本机仓库 **不要用它**。

---

### 1.2 网页 VS Code：npm runtime（Git 不内嵌 code-server）

官方 ShunCode：编辑器载体是 Code-OSS，模型循环在独立 agent-host。

Git **不再**放一份残缺的 code-server 切片。第二种跑法第一次启动时从 npm 拉取完整 `code-server@4.135.0` 到 `bin/code-server-runtime/`（Git 忽略）：

```
run-shuncode-vscode.cmd
        │
        ├── agent-host :48271     SHUNCODE_SKIP_WORKBENCH=1（不占 3000）
        └── code-server :3000     真 VS Code Web + shuncode-core 插件
```

插件源码 `shuncode-core/extension/`，运行前复制到 `extensions-installed/`。侧栏 Chat 打 `/api/chat`，Bridge 打 `/api/bridge/*`。原生 Chat 参与者 `@shuncode`（默认 Agent / Code，`/ask` `/plan` `/code`）走同一接口。

Windows 官方不发布 code-server 安装包；本方式用 **npm 包 + Node 22**（跳过原生模块编译）。VS Code 内置终端可能不可用，命令请走 Chat CODE 或本机 CMD。

完整步骤：[网页VSCode使用指南.md](./网页VSCode使用指南.md)

`.config/code-server/config.yaml`：`0.0.0.0:3000`、`auth: none`。

**和 ChatGPT 改磁盘：** 仍然是 agent-host 的 MCP + cloudflared。网页 VS Code 只换「本机编辑器外壳」。

---

## 2. 现行主路径：`shuncode-core/`

这是复现工作台的 **正在维护的代码**。

```
shuncode-core/
├── workbench/                 浏览器 UI（你打开 http://127.0.0.1:3000 看到的东西）
├── agent-host/                独立进程：MCP、工具、本机 Chat、隧道
├── extension/                 VS Code 插件源码（给 code-server 侧栏用）
├── extensions-installed/      已「安装」的插件目录（code-server --extensions-dir）
└── start-shuncode.sh          转到仓库根目录的 run-shuncode.sh
```

### 2.1 `workbench/` —— 自绘工作台 UI

对照官方截图做的网页：欢迎页「ShunCode / 编辑进化」、活动栏、右侧 CHAT / BRIDGE、齿轮里的「本地的智能体自定义设置」、Bridge 页（启动/复制提示词/打开 ChatGPT·Arena…）。

| 文件 | 职责 |
|---|---|
| `index.html` | 页面骨架：标题栏、活动栏、欢迎页、编辑区、右侧栏、设置模态、内置浏览器 |
| `styles.css` | Dark Modern 外观 |
| `app.js` | 调 `/api/*`、WebSocket `/ws`、Chat 发送、Bridge 启停、复制提示词、打开内置浏览器 |
| `favicon.svg` | 图标 |

它 **不能单独改文件**。所有读盘、打补丁、跑命令都打到 agent-host。

### 2.2 `agent-host/` —— 独立运行时（产品核心）

入口 `src/index.js`：起 **两个** HTTP 服务器，共用同一套 Express 应用。

- **3000** `WORKBENCH_PORT`：静态工作台 + 同源 `/api`、`/ws`
- **48271** `AGENT_HOST_PORT`：MCP `POST /mcp/<secret>` 或 OAuth `POST /mcp`，与同一套 `/api`

工作区根目录 `WORKSPACE_ROOT`（未设置则为仓库内 `workspace/`）。工具禁止写到这个目录外面。

#### 配置与身份

| 文件 | 职责 |
|---|---|
| `src/config.js` | 端口、工作区路径、进程内 `secretKey` / `installId` |
| `src/models/store.js` | 读写 `<工作区>/.shuncode/config.json`（模型、Bridge 选项、**MCP 密钥**） |
| `src/models/customizations.js` | 智能体/技能/指令/挂钩、环境偏好、技术栈，`<工作区>/.shuncode/customizations.json` |
| `src/models/profile.js` | 探测 OS/Shell 与 package.json 技术栈，拼进 Chat / MCP 指令 |
| `src/models/memory.js` | `remember` / `recall`，目录 `<工作区>/.shuncode/memory/` |

密钥存在工作区配置里，所以重启进程一般 **不换** MCP 密钥；换的是 Cloudflare Quick Tunnel 的域名。点「重置 MCP 地址」才会换密钥。

#### MCP 协议

| 文件 | 职责 |
|---|---|
| `src/mcp/server.js` | JSON-RPC 2.0 Streamable HTTP：`/mcp/<secret>` 或 Bearer `/mcp`；SSE 可选 |
| `src/mcp/oauth.js` | OAuth 2.1（PKCE S256、动态注册、配对码）。给 ChatGPT Plus 连接器；Arena 不需要 |
| `src/mcp/clients.js` | 客户端配方：本机 Chat / Arena / 其它网页栏 / ChatGPT 免费（不能连）/ Plus 连接器 |
| `src/mcp/instructions.js` | 服务端规则 + 剪贴板第一句 `getBootstrapPrompt`（截图 5 那两行，不要加长） |
| `src/mcp/resources.js` | `shuncode://instructions` 等资源，网页客户端没吃到 initialize 时可再读 |
| `src/mcp/session.js` | 会话计数、心跳快照 |
| `src/mcp/budget.js` | 单次工具结果截断，避免撑爆网页 Agent 上下文 |
| `src/mcp/errors.js` | 协议错误 vs 执行错误 |

网页 Agent 连上之后的规则通道：

1. `initialize.instructions`
2. 剪贴板：MCP URL +「快速连接这个 MCP（URL），明确使用规则…」
3. `tools/list`
4. 资源 `shuncode://instructions`
5. prompt 名 `connect`

#### 工具（约 25 个，Ask/Plan 只读，Code 可写）

| 文件 | 职责 |
|---|---|
| `src/tools/index.js` | 注册表、模式锁、危险命令要 `confirm_dangerous` |
| `src/tools/workspaceInfo.js` | 一次返回根目录 / git / 技术栈 / 顶层文件名，避免网页 Agent 整树列举 |
| `src/tools/sensitive.js` | 拦截 `.env`、密钥、SSH、`.shuncode/config.json`；`.shuncodeignore` 可追加 |
| `src/tools/fileOps.js` | 读/写/删/改名/列表/搜索，路径统一成 POSIX `/` |
| `src/tools/findFiles.js` | glob |
| `src/tools/patchEngine.js` | `apply_patch`：SEARCH/REPLACE、哈希预检、冲突 |
| `src/tools/executor.js` | `run_command` / `start_command`；**Windows 走 PowerShell**，结束用 `taskkill` |
| `src/tools/gitOps.js` | 只读 `git_status` / `git_diff` |
| `src/tools/skills.js` | `.shuncode/skills/<名>/SKILL.md` |
| `src/tools/progressTracker.js` | `report_progress`、`set_todos` |
| `src/tools/consensusEngine.js` | Plan 模式多模型博弈（本机 Chat 用） |
| `src/tools/lspStub.js` | 语言服务占位，未做成独立 MCP 工具 |

#### 本机 Chat、隧道、HTTP API

| 文件 | 职责 |
|---|---|
| `src/agent/runChat.js` | 右侧 CHAT：对**当前工作区**搜-读-补丁-再测；无 API Key 时走内置探索循环 |
| `src/agent/openai.js` | 配了 API Provider 后走 `/chat/completions` 工具循环（最多 10 步） |
| `src/agent/providers.js` | Test / Add API：GET `{endpoint}/models` |
| `src/tunnel/cloudflared.js` | 启动 Bridge：拉起 `cloudflared tunnel --url http://127.0.0.1:48271`，解析 `*.trycloudflare.com`。Windows 上若二进制是 `.cmd`/`.bat` 会 `shell: true` |
| `src/api/routes.js` | 工作台 REST：状态、启停 Bridge、复制提示词、文件树、Chat、自定义设置 |
| `src/utils/eventBus.js` | WebSocket 把工具调用推到右侧 BRIDGE 面板 |
| `src/utils/diff.js` | 统一 diff 文本 |
| `tests/` | 补丁、MCP、工作区工具、隧道探测 |

### 2.3 `extension/` 与 `extensions-installed/`

VS Code 插件（manifest 里活动栏「ShunCode」、Chat / Bridge 两个 webview，以及 `chatParticipants` `@shuncode`）。  
**只在 code-server / VS Code 加载扩展时生效。** 现行浏览器工作台不走这套 API，但同样有 **Agent ▾**（默认 Code）。界面逻辑在 `workbench/app.js`。

`extensions-installed/shuncode.shuncode-core-0.6.9/` 是给 `code-server --extensions-dir` 用的已安装副本，内容和 `extension/` 对应。

---

## 3. 工作区 `workspace/` 与 `.shuncode/`

`workspace/` 是 **默认被编辑的演示项目**（`src/calculator.js` + `tests/`），不是 ShunCode 引擎。

挂你自己的仓库：

```bat
run-shuncode.cmd D:\code\my-app
```

此时 agent-host 的根变成 `D:\code\my-app`，演示 `workspace/` 不再被改。

`.shuncode/` 建在 **工作区根** 下：

| 路径 | 提交到 Git？ | 职责 |
|---|---|---|
| `.shuncode/config.json` | **不要**（含 MCP 密钥） | 模型、Bridge、secret |
| `.shuncode/instructions.md` | 可以 | 始终生效的工作区指令，会拼进 MCP `initialize.instructions` |
| `.shuncode/preference.md` | 可以 | 环境偏好（OS / Shell / 回复语言） |
| `.shuncode/tech-stack.md` | 可以 | 技术栈申明（语言 / 测试命令） |
| `.shuncode/skills/<名>/SKILL.md` | 可以 | 技能；界面「技能引导」按名称→何时使用→步骤生成 |
| `.shuncode/memory/` | 按需 | Agent 长期备忘 |
| `.shuncode/customizations.json` | 按需 | 智能体/挂钩/提示等 UI 配置 |

本仓库演示区提交了 `workspace/.shuncode/instructions.md` 和两个 skill；`config.json` 被 `.gitignore` 排除。  
所以在 **GitHub 仓库根目录看不到 `.shuncode`**，要进 `workspace/`。你自己的项目则是跑过一次、保存过设置之后才会出现。

---

## 4. 启动脚本与根目录文件

| 路径 | 职责 |
|---|---|
| `run-shuncode.cmd` | **Windows 主入口**：检查 Node/npm，可选工作区参数，`npm install`，启动 agent-host |
| `check-env.cmd` | 打印 Node / npm / Git / cloudflared 是否在 PATH |
| `run-tests.cmd` | Windows 跑 `shuncode-core/agent-host/tests`（不必在仓库根再建 `tests/`） |
| `run-shuncode.sh` | Linux/macOS 等价入口，同样只启 agent-host |
| `使用指南.md` | Windows + CMD 从安装到 Bridge 的操作手册 |
| `测试说明.md` | 测了什么、怎么跑、为什么不单独建根目录 tests |
| `README.md` | 仓库首页，指向使用指南和本文 |
| `组件说明.md` | 本文 |
| `SHUNCODE_REPRODUCTION_OVERVIEW.md` | 早期「Code-OSS 载体」架构草稿，目录树已过时，以本文为准 |
| `AGENT_HANDOVER.md` | 给后续 Agent 的交接稿，启动方式同样过时（仍写着手动 `code-server`） |
| `.gitignore` | 忽略 `node_modules`、日志、`workspace/.shuncode/config.json` 等 |
| `.gitattributes` | `*.cmd` / `*.bat` 强制 CRLF，避免 Windows 脚本坏掉 |

---

## 5. 运行时数据流（网页 Agent 改你磁盘）

```
你的 Windows 电脑
├── 浏览器 http://127.0.0.1:3000     工作台（本机 Chat 不走隧道）
├── agent-host :48271                工具真正改 D:\code\my-app
└── cloudflared                      把 48271 映射成 https://xxxx.trycloudflare.com

云上的 Arena / 其它 MCP 网页栏（无需 Plus）
└── 第一句贴上 MCP URL + 连接说明

ChatGPT Plus 开发者模式（可选）
└── 连接器填 /mcp，OAuth 配对码只出现在本机
```

本机右侧 Chat **不走** 隧道，也 **不必** 登录 GitHub，也 **不必** ChatGPT Plus。

---

## 6. 常见误读

1. **「Git 里应该有一份 code-server 才能开 VS Code」**  
   没有。第二种跑法是 `run-shuncode-vscode.cmd`，它从 npm 装完整包到 `bin/code-server-runtime/`。

2. **「应该 `npm start` 在 shuncode-repro 里」**  
   那是旧原型，端口和工具都对不上现在的使用指南。

3. **「`.shuncode` 应该出现在 web_agent 仓库根上」**  
   不会。它属于被编辑的项目，默认在 `workspace/.shuncode`。

4. **「把这个 GitHub 仓库发给别人，别人就能改我电脑」**  
   不会。程序必须跑在 **你的** 电脑上，再用 Quick Tunnel 让云上的 ChatGPT 连进来。MCP 地址不能发到公开地方。

---

## 7. 目录速查（给扫仓库的人）

```
web_agent/                          ← 本 Git 仓库
├── 使用指南.md                     Windows 操作
├── 组件说明.md                     本文
├── run-shuncode.cmd / check-env.cmd
├── shuncode-core/                  ★ 现行产品
│   ├── workbench/                  浏览器 UI
│   └── agent-host/                 MCP + 工具 + Chat + 隧道
├── workspace/                      默认演示工作区（含 .shuncode 示例）
├── shuncode-repro/                 第一代 Bridge 原型（勿当主程序）
├── bin/README.md                   说明网页 VS Code 走 npm runtime
└── .config/code-server/            给 code-server 预备的 yaml
```
