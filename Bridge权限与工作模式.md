# Bridge权限与Chat/Bridge工作模式

## 先分清两层“模式”

**手机浏览器中的Arena连接本机MCP，固定属于Bridge，不属于本机Chat。**

- 主机工作模式只有Chat与Bridge，不能同时执行。Chat内部可多模型并行；Bridge内部可多个Arena Agent并行。不是每个窗口各选各的。
- Ask/Plan/Code是工具调用策略，不是主机工作模式，也不是远端能改的所有者权限。远端即使声明Code、remote:false或自行夹带permissions，仍受本机Bridge权限限制。
- 查看Chat/Bridge页签、编辑文件、查状态、批准/拒绝操作属于本机操作界面；打开两个页签不会启动两个Agent模式。
- 重启后工作模式为idle；首次Chat执行或首次认证MCP请求占用相应模式，之后不会因空闲自动释放/抢占。操作者也可以先明确选择模式。权限单独持久保存，重启不会自动恢复全开。

## 在哪里设置

经典工作台：打开Bridge区域，找到“工作模式与Bridge权限”。桌面VS Code和code-server核心扩展：打开Web Agent的Bridge侧栏，同名卡片使用同一个主机接口。更新源码后须按原安装流程更新扩展并重启主机；旧窗口不会自动换上新扩展代码。此卡片不是此前取消的浏览器探针入口。

1. 核对主机工作区和身份。没有工作区、错工作区或主机已重启时，设置会被拒绝；先刷新/重新连接正确主机。
2. 按“切换Chat”或“切换Bridge”。看主机回读，不以按钮点击或页签颜色为成功依据。
3. 勾选权限，点“保存权限”。仅勾选尚未保存不会生效；轮询不会覆盖尚未保存的草稿。“重新读取”放弃草稿并读取主机配置。
4. 多个本机窗口同时设置时，旧revision保存被拒绝，刷新核对后再决定。不能用旧窗口无声覆盖新权限。

## 四项具体限制

| 项目 | 实际范围 | 依赖/边界 |
|---|---|---|
| Read | 文件/搜索/git读取、记忆/任务/日志/技能读取、外部服务/探针报告、审批结果；MCP资源读取与提示词读取也检查 | 禁用不隐藏全部协议元信息，如主机身份、会话和工具描述；不是擦除远端已经读到的内容 |
| Edit | 写/补丁/删除/移动文件，写记忆、任务板创建/认领/更新 | 需要Read以支持版本核对和修改结果；不是每次修改都弹审批 |
| Execute | shell命令、输入、不透明外部工具/浏览器操作；未知工具保守要求全部权限 | 必须Read/Edit/Capture也开启。远端交互PTY仍禁止，既有命令限制和单次外部审批仍保留 |
| Capture | 允许命令结果识别出的截图作为MCP图片附件返回 | 是已有回图链的总开关，不是新截图/OCR工具；关掉Capture也必须关Execute。现有路径/格式/大小限制继续适用 |

没有既存配置时，四项默认全开，以延续旧版已有权限；这不是“默认只读”。建议接入前明确保存所需权限。**只读组合：Read开，其余三项关。** 全关仍允许ping、连接核对、等待、进度/todo报告、取消命令及不读文件的workflow_preview；这些不授予执行任意程序的能力。workflow_request需Read，执行时每个内置步骤仍分别校验，不能通过工作流绕过Edit。

任意程序能读取/修改文件、截屏，还能以同一OS用户访问本机控制面；应用层无法承诺“允许任意Execute但绝对不读/写/截图”。因此拒绝这类矛盾配置，而不是给出四个看似独立隔离的开关。全开只应给可信Agent/程序；权限不是OS沙箱、独立系统账户或每客户端ACL。本机操作者、既有外部进程和直接文件系统访问不受这个远端工具ACL隔离。

## 切换或改权限被拒绝怎么办

- Chat/Bridge请求正在执行：先让任务结束，或使用对应的明确取消入口。不会自动切模式、偷偷取消或重放修改。
- 后台start_command已经返回execId，程序可能仍运行：用get_command_output查看，用cancel_command明确停止。记录写着cancelled不等于子进程close已经到达。
- 等待审批的操作：批准完成或拒绝后再切模式。可以在等待审批时撤销权限；批准执行时重新检查，禁止的操作/步骤失败，不执行、不自动重提。
- 运行中的审批、命令、stdio程序和PTY未确认取消：先结束再改权限。撤销不是即时强杀；失败响应不会声称权限已保存。
- stdio已从列表移除仍可能等待进程关闭，期间继续阻止切换。PTY取消后要等原终端所有者回报；连接丢失不会仅因TTL过期就假定程序停止。必要时人工确认终端/进程已停止后重启主机，不能把重启主机当作所有外部程序已停止的证明。
- 切回Chat前停止Bridge隧道，再点切换Chat。停止隧道不等于取消全部任务，也不自动切回Chat；本机MCP在Bridge模式仍可用。隧道启动失败也保留Bridge模式，需明确切回。切到Chat后原有MCP连接不能执行，需切回Bridge才能继续。

后台检查针对主机管理的请求、子进程、stdio和PTY状态；不能检测/隔离任意shell自行脱离的系统进程、所有浏览器后台动作或重启前的程序。多Agent并发不代表全部共享文件/终端状态按Agent隔离。

## 验证与仍需实机确认

[executionControl.test.js](webagent-core/agent-host/tests/executionControl.test.js)覆盖真实HTTP权限保存、错绑定、别名/远端提升、资源/提示词限制、嵌套工作流撤权、同类型并发、后台命令与PTY/stdio屏障、隧道启动预占、重启持久化。既有混合模式测试改为明确切换，不放松主机门禁。

新的控件还须在用户真实Windows桌面和手机Arena上核验。手机此前“连接和用法与电脑相同”的通过结论保持有效，但不冒充新增开关已经实测。源码解释见[执行控制详解](webagent-core/agent-host/src/utils/执行控制详解.md)。
