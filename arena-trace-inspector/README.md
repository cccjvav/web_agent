# Arena Trace Inspector 0.9.0

## 0.9.0：安装教程与发布包

- 扩展版本统一更新为 0.9.0，完整安装步骤见同目录的《安装教程.md》。
- 发布包包含当前扩展源码、安装教程和测试文件；解压后选择直接包含 manifest.json 的 arena-trace-inspector 文件夹安装。
- 包含自动重命名、一键归档及本地记录清理，以及侧栏自动展开修复。下方保留历史版本说明。

## 0.8.2：一键归档聊天及删除记录

- 修复将聊天链接的提示气泡 `data-state=closed` 误判为不可见的问题；侧栏收起时自动展开并滚动定位当前会话，不再要求手动展开。

- 点击“归档聊天及删除记录”直接执行，不再弹出扩展二次确认；如官方页面出现可识别的归档确认框，自动完成确认。
- 保留当前会话校验、重复点击保护、停止监听及归档结果检查；归档成功后才删除对应本地记录。归档不等于永久删除聊天。
- 单独“删除记录”按钮仍保留确认提示。以下为历史版本说明。


## 0.8.1：浮层布局调整

- 开启／停止监听按钮恢复到内容区左上角，与监听状态并排显示。
- 操作区新增“归档聊天及删除记录”：确认后使用 Arena 官方 Archive 菜单，停止当前页监听；确认侧栏聊天条目消失后再删除本地记录。归档并非永久删除。若结果无法确认则保留本地记录；若归档后本地清理失败，可在扩展弹窗的会话列表重试删除记录。
- 模型卡片依次展示操作按钮行、运行 ID、记录时间与操作结果提示；不改变监听、自动重命名和删除逻辑。


## 0.8.0：自动重命名与本地记录管理

- 浮层卡片底部从左到右排列“自动重命名”勾选框、“重命名对话”和“删除记录”；移除“复制型号”和 run 的“复制 ID”按钮，保留 ID 文本。
- 自动重命名默认关闭，本地保存开关；开启后每个会话首次完成新的检测时最多自动尝试一次，历史恢复不触发。自动尝试失败可手动重命名；多型号使用当前展示的第一个型号。
- 浮层和弹窗会话列表均可确认删除单个会话的本地记录；不删除 Arena 对话，不清除其他会话或偏好。继续监听后新捕获的结果可能重新生成记录。
- 更新后重新加载扩展并刷新 Arena 页面；以下内容为历史版本说明。


## 0.7.1：修复工具栏弹窗反复缩放

- 移除 popup body 的 `max-width: 100vw`，使用固定 480px 宽度／最小宽度，避免浏览器弹窗自动尺寸计算与视口宽度约束形成循环。
- 保留点击插件图标打开弹窗的原有操作方式；修复后型号和标题不再因弹窗持续缩窄而异常换行，较长内容仍可纵向滚动。
- 不改变页面浮层、监听、重命名、历史搜索与导出逻辑；不增权限、不改历史记录。

## 0.7.0：按型号重命名当前对话

- 页面展开面板的每个“复制型号”按钮下新增“重命名对话”。点击后使用该按钮对应的完整模型名称作为当前 Arena 对话标题，不是修改浏览器标签页标题。
- 仅有已识别型号且属于当前会话／当前展示运行时可操作。多型号时点击哪一组就使用哪一个型号；不会批量修改其他对话。
- 通过当前会话侧栏菜单的 Rename 和官方 Rename chat 表单提交；未新增权限、私有接口调用或令牌读取。复制仍只复制，不触发重命名。
- 严格按当前 URL 的会话 ID 找到菜单，校验原名称和表单；导航／型号改变、超过 100 字符、已有对话框、菜单不明确时停止并提示。提交后等待侧栏名称更新且对话框关闭，不伪造页面文字。
- 原插件弹窗的历史浏览不加重命名按钮，避免查看别的历史记录时误改当前页面。启动／监听／捕获本身不会自动重命名，必须点击新按钮。
- 修正 SPA 切换会话后浏览器 sender.url 仍为旧文档 URL 时的状态读取与监听控制；请求携带当前页 URL，后台仍按实际发送标签页和实际地址校验。

## 0.6.0：进入页面即显示，浮层内开启监听

- 插件启用后，进入或刷新 Arena 页面自动显示展开浮层，无历史记录／未监听时也显示，不必先打开插件弹窗。
- 展开区新增“开启监听／停止监听”按钮。显示浮层、恢复历史、刷新和切换页面本身不会附加调试器、发消息或自动开启监听；只有点击按钮才改变监听状态。
- 每次新页面加载／切换路径默认展开，位置仍记忆；同页主动收起不会被普通状态更新或切回标签页重新展开。此规则取代 0.5.x 跨刷新保持收起的行为。
- 缩略和展开状态灯保持同步；停止后浮层保留，已有历史不删除。连接失败有重试入口，开启失败直接在浮层提示。
- 内容脚本控制限定为当前 Arena 顶层标签页，忽略请求中的其他 tabId，显式开启／停止请求串行、幂等处理；不增加权限。
- 原插件弹窗保留为历史搜索／导出的备用入口，不再是开启监听的前提。

## 0.5.2：精简状态灯与标题栏清理

- 缩略卡片标题左侧增加与展开界面同步的状态灯：监听中为绿色，本地历史／未监听为黄色；悬停和辅助技术可读取状态说明。
- 展开标题栏移除状态灯左侧的拖动图标和标题栏的拖动提示；仍可拖动标题栏，方向键／Home 键移动能力不变。
- 保留缩略界面的拖动、单行模型名、加粗指标与位置记忆；不改统计、历史记录或权限。

## 0.5.1：精简卡片样式调整

- 收起状态加回 `ARENA · TRACE INSPECTOR` 标题，使用接近旧版的深色纯色背景和浅绿色模型名。
- 模型名称保持单行，过长以省略号显示，悬停查看完整名称；相同型号文本不再重复展示，不合并调用记录或改变统计。
- Token、trace 费用、次数、状态的标签与数值加粗；保留缺失／部分用量和历史状态提示。
- 标题栏和模型名称区域均可拖动，保留展开按钮、位置记忆及完整详情。无新增权限，不修改历史记录。
- 更新：将新版文件覆盖到原扩展目录，在扩展管理页重新加载，再刷新 Arena 页面。监听中的会话建议等当前调用结束后再更新。

## 0.5.0：自由拖动与精简收起视图

- 页面浮层可拖动：展开时拖标题栏，收起时拖模型名称区域；支持鼠标、触摸和触控笔。按钮、明细与复制操作不会触发拖动。
- 收起后仅显示模型名称、Token、trace 费用、调用次数和状态；右上角箭头展开完整卡片／调用明细／证据区。历史结果在状态字段中注明“历史”，不伪装为本次捕获。
- 位置和收起状态通过后台保存在 `ati.ui.hud.v1`，刷新／切换会话后恢复，不保存认证数据。界面设置与会话记录分开；无新增权限，不改变 TRUSTED_CONTEXTS 存储限制。
- 位置以视口内可移动范围的比例保存，窗口变化时自动限制在可见区域，展开不会跑到屏幕外。键盘聚焦拖动区域后可用方向键微调、Shift 加速、Home 回到右下角。
- 只有拖动结束、键盘移动或切换收起状态时写入设置；不在每次鼠标移动时写盘。界面操作不会发起模型调用。

## 0.4.1：返回会话与刷新后的自动恢复

- 切换会话再返回、刷新 Arena 页面、重新加载扩展后，会按当前会话 ID 读取本机历史，恢复最近保存的型号、Token、trace 费用、调用明细及已有证据。
- 页面浮层在未开启监听时也可显示该会话的已保存结果，明确标注“本地历史 · 非重新验证”，指示灯为黄色。恢复数据不会自动发消息、查询旧令牌或开启调试监听。
- 新的运行令牌到达后切换为“本次捕获”。异步读取带导航校验与失效标记，旧会话的慢读取不能覆盖当前会话或新运行。
- 未保存的会话显示等待／无记录，不借用其他会话的数据；存储读取失败明确提示，不删除历史。
- 保留既有 storage key 和 schemaVersion，无新增权限。历史恢复与监听状态分离：重启后的新捕获仍需手动开启监听。

## 0.4.0：结果卡片、调用明细与证据抽屉

- 弹窗与页面浮层共享新结果卡片：完整服务端模型标签、供应商、复制型号／运行 ID。页面浮层支持收起。
- 指标仅展示 Token、trace 费用、调用次数、完成状态。完成状态仅指已捕获的模型调用，不宣称整个 Agent 工作流已经结束。未知、进行中、报错和取消分别展示。
- 调用明细保留每个 `ai.streamText.doStream` span，同型号多次调用不会被折叠成一次；按 runId + spanId 去重。
- 证据抽屉保存模型、供应商、Token、费用的原始标签、固定字段路径与各自的观测时间，以及三个调用状态布尔值。只保留白名单字段，不保存原始 trace、令牌、Cookie 或对话正文。
- 支持复制／下载脱敏证据 JSON。后续快照缺失的用量继续保留，但每个标签携带自己实际观测的时间，不伪装成新读取的值。
- 弹窗支持切换当前会话的不同运行，也可在历史列表点击“查看运行”。本次捕获与本地历史分别标记，切换运行不会混用指标。
- **旧记录兼容**：保留原有 storage key 与 schemaVersion，不清空既有记录。旧版没保存的原始标签显示“未保存原始标签”，不从解析数字伪造原始标签，不自动发消息补录。
- 不增加置信度、盲测猜测、代际映射、思考强度或未经测量的时延指标。无新增权限。
- **更新**：在 `edge://extensions` 重新加载原目录的扩展，再刷新 Arena 页并重新开启监听；不要卸载扩展，以免丢失本地历史。

## 新增：Token 与 trace 费用统计

- 浮层和弹窗显示本次运行、会话已捕获累计；历史列表显示各会话累计，弹窗另显示全部已保存记录汇总。JSON 导出包含每个运行的 span 用量和累计值。
- 读取已实测的 `ai.streamText.doStream` 标签：`tabler-hash`（例如 `6.6k`）与 `tabler-currency-dollar`（例如 `$0.0133`）。缩写 Token 标为约数；费用是 trace 展示值的汇总，不是用户账单，也不按公开价格另行估算。没有输入／输出细分时不推算。
- 按 `runId + spanId` 去重。同一运行新快照更新旧 span，新增 span 才累加；不同时累计父级 `ai.streamText`，避免翻倍。每次新收到运行令牌都允许刷新，支持一个运行跨多轮继续更新。
- Token／费用缺失显示“未提供”，不是 0。部分 span 缺失标注“部分缺失”；流尚未结束标注“进行中”。标签未齐时最多尝试 8 次。
- 只统计成功捕获的运行，未监听的对话／旧版未记录用量不会自动补全；累计不是账号总消费。原 0.2.0 会话记录继续保留，重新识别后补充用量。
- 在原目录更新后到 `edge://extensions` 点击扩展“重新加载”，刷新 Arena 页面，再开启监听。本版不增加权限。

## 新增：会话—模型本地存储

- 每次成功识别后，自动保存会话 URL、网页标题（可读时）、会话 ID、运行 ID、模型／提供方标签、首次与最近确认时间。链接由实际流会话 ID 生成，避免串到其他页面。
- 同一会话归为一条记录；同一运行及模型去重，不同运行或模型保留多条观测，不把历史结果当作当前重新验证的结论。
- 插件弹窗下半部分可搜索、查看和点击会话链接，支持导出 JSON。没有开启监听也能看历史；重启浏览器后仍保留。
- 数据存于扩展 `chrome.storage.local`，不云同步、不保存令牌、Cookie、正文或原始 trace。关闭监听只清除临时令牌，不删除记录。卸载扩展会丢失扩展本地数据，需保留时先导出。
- 保存失败时面板明确提示“已识别模型，但本地保存失败”，不会把失败显示成成功。
- **升级**：保留原安装目录，在 `edge://extensions` 对这个扩展点击“重新加载”，接受新增 storage 权限（若浏览器提示），刷新 Arena 页面，再重新开启监听。不要卸载重装。旧版没有保存的历史不会自动补录；需再次识别。

Edge / Chrome Manifest V3 扩展。当前标签页开启后，自动解析 Arena Agent 的 SSE 流记录，提取本次运行公开令牌，用只读 GET 查询 Trigger.dev trace，并显示 `ai.streamText.doStream` 中 `tabler-cube` 对应的模型标签。

## 安装（Edge）

1. 打开 `edge://extensions`，开启「开发人员模式」。
2. 点击「加载解压缩的扩展」，选择 **D:\SHUN\arena-trace-inspector**，不是 ZIP，也不是它的父目录。
3. 把扩展固定到工具栏。Chrome 对应入口为 `chrome://extensions`。
4. 刷新一个 Arena Agent 页面以加载悬浮面板。
5. 如果 Codex 正在控制这个标签页，先结束该标签页的控制；也不要同时开启这个页面的 DevTools。扩展不会强行夺取已有调试连接。
6. 点击扩展 →「开启当前页监听」→ 在 Arena 新建对话并发送普通测试消息。
7. 令牌可能到 `turn-complete` 才到达；等待模型回复结束后，悬浮面板应显示服务端模型标签。
8. 点击扩展 →「停止监听并清除令牌」可以退出。浏览器重启后需要手动重新开启。

## 自动执行的范围

- 只附加用户开启的 Arena 标签页，不监听其他标签页；离开 Arena 后自动解除附加。
- 只解析 `/ai-proxy/realtime/v1/sessions/<sessionId>/out` 的成功响应。
- 只接受流记录 `headers` 内的 `public-access-token`，不从对话文本读取令牌。
- JWT 必须包含预期 issuer/audience、`pub:true`、未过期，以及匹配当前流会话的读取 scope 和唯一具体运行 scope。
- 解码 JWT 不等于验证签名；令牌是否有效由 Trigger.dev 服务端验证。
- 令牌只发往固定 `https://api.trigger.dev/api/v1/runs/<runId>/events`；GET、禁止重定向、不携带 Cookie、不调用写接口。
- 最多读取 8 次 trace，每次间隔 3 秒，请求超时 10 秒。401/403/404/429 或网络错误停止，不无限重试。
- 不把 token、Cookie、对话内容或原始 trace 写入 storage、文件、日志或悬浮面板。成功后释放令牌引用；停止后清除内存状态。无遥测、无外部依赖、无远程代码。
- 不提交评价、不自动发消息、不修改模型或运行。

## 显示结果的含义

显示的是服务端 trace 中记录的模型 ID，不是对底层权重身份的独立证明。若 trace 返回多个模型标签，会全部列出；无标签时显示未确认，不通过文风猜测，不生成百分比置信度。

## 排错

- **无法附加调试器**：结束 Codex 对该标签页的控制或关闭该页 DevTools，再开启扩展。
- **一直等待令牌**：必须先开启，再发消息；旧消息不能保证回放。等回复结束后检查。Arena 流格式发生变化也会导致等待。
- **有工具栏状态但没有悬浮面板**：刷新 Arena 页面；安装前的页面没有自动加载 content script。
- **401 / 403**：当前令牌过期或服务端不允许该运行的 trace，扩展不会绕过；发送新消息可重新验证。
- **无模型标签**：服务端可能移除或改变标签；不会用排行榜名称填充。
- **调试提示条**：来自浏览器的 `debugger` API，可用来确认捕获正在进行；不要在测试期间关闭提示条。

## 验证

```powershell
Set-Location 'D:\SHUN\arena-trace-inspector'
node --test tests/*.test.mjs
```

已用真实 Edge 会话验证协议链：SSE token → 指定 run events HTTP 200 → `ai.streamText.doStream` 标签 `grok-4.6`。这是开发前的手动协议验证；扩展安装后的端到端行为需按上方步骤验证，不能把单元测试当作已完成安装实测。

Chrome 官方 debugger 文档：https://developer.chrome.com/docs/extensions/reference/api/debugger

## 文件

`background.js` 捕获及只读查询；`core.js` 解析与校验；`usage.js` 用量与调用记录；`evidence.js` 证据白名单；`view-model.js` 展示状态；`panel.js` 共享结果卡片／明细／证据抽屉；`popup.*` 控制面板；`hud.js` 页面浮层；`tests/` 无真实令牌的测试。
