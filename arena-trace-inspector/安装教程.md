# Arena Trace Inspector 安装教程

适用版本：0.9.0  
浏览器：Microsoft Edge / Google Chrome 桌面版

## 一、准备扩展文件

你需要的是包含扩展源码的 **`arena-trace-inspector` 文件夹**，不是整个项目父目录。

当前开发电脑上的路径为：

```text
D:\SHUN\arena-trace-inspector
```

打开这个文件夹，应当能直接看到：

```text
manifest.json
background.js
popup.html
hud.js
panel.js
conversation-rename.js
……
```

如果拿到的是 ZIP 压缩包，请先完整解压。选择安装目录时，以 **直接包含 `manifest.json` 的那一层文件夹** 为准。

> 请使用当前 0.9.0 的完整文件夹。项目父目录中已有的旧版本 ZIP 不会因为源码更新而自动更新，不要误装旧包。

## 二、在 Edge 中安装

1. 在地址栏输入并打开：
   ```text
   edge://extensions
   ```
2. 打开页面中的 **开发人员模式**。
3. 点击 **加载解压缩的扩展**。
4. 选择 `arena-trace-inspector` 文件夹。例如：
   ```text
   D:\SHUN\arena-trace-inspector
   ```
5. 确认扩展列表中出现 **Arena Trace Inspector**，版本为 **0.9.0**，并处于启用状态。
6. 点击浏览器工具栏上的“扩展”图标，将该扩展设置为 **在工具栏中显示**，方便打开控制面板。
7. 打开或刷新一个 `https://arena.ai/agent` 页面。

> 如果电脑由公司或学校管理，扩展安装可能受策略限制，需要联系管理员，不建议绕过限制。

## 三、在 Chrome 中安装

步骤与 Edge 基本相同，只需将扩展管理地址换成：

```text
chrome://extensions
```

打开 **开发者模式** → 点击 **加载已解压的扩展程序** → 选择包含 `manifest.json` 的 `arena-trace-inspector` 文件夹。

## 四、首次使用

### 1. 打开 Arena 页面

访问：

```text
https://arena.ai/agent
```

按 Arena 页面要求登录。安装扩展之前已经打开的 Arena 页面，需要刷新后才能加载悬浮面板。

### 2. 开启监听

在页面悬浮面板左上方点击 **开启监听**；也可以点击浏览器工具栏中的扩展图标，在弹窗中开启当前页监听。

看到“当前页监听中”等状态后，再发送消息。

> 扩展只监听你开启的 Arena 标签页。不要把“面板已出现”或“恢复了历史记录”理解为“已经开启监听”。

### 3. 等待捕获结果

开启监听后，在 Arena 中发送一条正常消息，等待响应与检测完成。成功后，面板会显示已捕获的服务端模型标签、运行 ID，以及可获取到的 Token、trace 费用和调用信息。

注意：

- 先开启监听，再发送消息；未监听的历史调用不会保证自动补录。
- 检测结果可能在回复结束后才出现。
- Token 或费用没有返回时，会显示“未提供”或缺失说明，不会补造数值。
- 服务端模型标签是 trace 中的记录，不是对底层模型身份的独立证明。
- trace 费用不是实际账单；累计值只包括扩展已捕获并保存的记录。

## 五、常用功能

| 功能 | 使用说明 |
| --- | --- |
| 开启／停止监听 | 控制当前 Arena 标签页的捕获。停止监听不会自动删除已保存记录。 |
| 收起／展开 | 在详细面板与精简面板之间切换。 |
| 拖动面板 | 按住面板标题栏拖动，调整显示位置。 |
| 自动重命名 | 默认关闭。勾选后保存设置，每个会话首次完成新的检测时最多自动尝试重命名一次；恢复历史记录不触发。 |
| 重命名对话 | 使用当前展示的第一个型号，通过 Arena 官方界面重命名当前聊天。 |
| 删除记录 | 确认后删除对应会话的扩展本地记录，不删除 Arena 网站上的聊天。 |
| 归档聊天及删除记录 | **点击后直接执行，没有扩展二次确认。** 停止当前页监听，自动定位当前聊天并使用官方 Archive 入口归档；确认成功后删除对应本地记录。 |
| 会话记录／导出记录 | 在工具栏扩展弹窗中搜索、查看或导出已保存的本地会话记录。 |

### 归档操作的重要说明

- **归档不等于永久删除聊天。**
- “归档聊天及删除记录”会真实操作当前 Arena 聊天，请确认目标后再点击。
- 侧栏收起时，扩展会尝试自动展开并滚动定位当前聊天，不需要先手动展开。
- 若归档结果无法确认，扩展会保留本地记录并显示错误提示。
- 若聊天已归档但本地记录清理失败，可打开扩展弹窗，在会话列表中单独删除记录。
- 本地记录删除不可撤销；需要保留时，请先导出。继续监听并捕获新结果后，仍可能重新生成记录。

## 六、已有用户如何更新

**不要先卸载扩展。** 卸载可能导致扩展本地数据丢失。

1. 建议先在扩展弹窗中点击 **导出记录**，备份需要保留的数据。
2. 等当前回复或任务结束，停止监听。
3. 将新版完整文件覆盖到 **原安装目录**，保持文件夹路径不变。
4. 打开 `edge://extensions` 或 `chrome://extensions`。
5. 找到 **Arena Trace Inspector**，点击扩展卡片上的 **重新加载／刷新** 按钮。
6. 检查版本号。当前版本为 **0.9.0**。
7. 刷新所有需要使用扩展的 Arena 页面。
8. 需要继续捕获时，重新开启监听。

> 即使修复前后都显示 0.9.0，也需要重新加载扩展并刷新页面，页面才会使用最新脚本。仅关闭再打开弹窗不能保证更新已有页面中的悬浮面板。

## 七、常见问题

### 1. 提示找不到清单文件，或无法加载扩展

检查所选文件夹是否直接包含 `manifest.json`。不要选择 ZIP，也不要选择 `D:\SHUN` 这样的项目父目录。

### 2. 安装后没有悬浮面板

- 确认扩展处于启用状态。
- 确认页面是 `https://arena.ai/` 下的页面。
- 刷新 Arena 页面；安装前已打开的页面不会自动获得新版脚本。
- 仍无面板时，重新加载扩展，再刷新页面。

### 3. 面板显示“本地历史 · 非重新验证”

这是恢复了之前保存的记录，并非本次重新检测。要获取新结果，需要开启监听，再发送新消息。

### 4. 显示扩展连接失败，或“Extension context invalidated”

常见于扩展更新后，旧 Arena 页面仍在运行旧脚本。刷新 Arena 页面，再重新开启监听。

### 5. 开启监听失败或提示调试器冲突

扩展使用浏览器 debugger API。请关闭当前标签页的开发者工具，并结束其他工具对该标签页的调试控制，再重试。不要同时让多个调试工具争用同一标签页。

### 6. 浏览器出现“正在调试此浏览器”的提示条

这是使用 debugger API 时浏览器显示的提示。需要监听时，不要通过关闭该提示条来中断调试连接；请使用扩展中的 **停止监听** 按钮结束捕获。

### 7. 一直等待模型标签

先确认是在开启监听之后发送的消息，并等待回复结束。若提示权限拒绝、令牌过期或 trace 不存在，不会自动绕过限制。可以停止监听后重试；Arena 页面或接口变化也可能导致扩展暂时失效。

### 8. 归档仍提示展开侧栏或找不到当前聊天

先确认使用的是包含最新归档修复的 0.9.0 源码，然后重新加载扩展并刷新 Arena 页面。修复版会处理侧栏自动展开，并避免把提示气泡关闭状态误判为聊天不可见。

若仍失败，不要反复操作其他聊天；保留错误提示和当前页面地址，便于排查页面结构变化。

### 9. 自动重命名没有再次触发

自动重命名不是每条消息执行一次，而是每个会话最多自动尝试一次。历史恢复不会触发，未完成的检测不会触发。需要再次修改名称时，可使用 **重命名对话** 按钮。

## 八、数据与权限说明

- 扩展需要 `activeTab`、`debugger` 和 `storage` 权限，并访问 Arena 与固定的 Trigger.dev API 域名。
- 会话记录保存在当前扩展的 `chrome.storage.local`，不会自动同步到其他浏览器、浏览器配置文件或电脑。
- 设计上不将令牌、Cookie、对话正文或原始 trace 全文保存为会话记录。
- 本地记录可能包含会话标题、URL、运行 ID、模型标签和用量数据，导出文件也应妥善保管。
- 重命名和归档使用 Arena 官方页面入口，会真实修改网站上的聊天名称或归档状态。
- **“停止监听”不删除记录；“删除记录”不删除网站聊天；“归档聊天及删除记录”会归档网站聊天并清理对应本地记录。**
