# 探针能力对照与迁移边界

## 当前用户决定（优先于下方旧版状态）

用户明确接受参考性探测、不要求绝对真实，要求完整原型能力。目标已改为完整整合，不再以“不能认证真实模型”为由长期排除功能；授权/隐私/未核验提示仍保留。Chat模式API模型的确切验证延后，待完整探针整合完成后再做。

0.2.0现已加入实际离线分析：复用原型解析/分类/协议/行为/命名/向量与tokenizer函数，支持输入内的UUID映射和冲突检查。不会自动执行原main或轨迹链。下方“连接版/VSCode未实现”的表述是**0.1.x交付历史**，不应作为0.2现状或最终范围。详见[完整实施与验收表](探针完整整合实施与验收.md)。

## 0.1.x交付范围回顾

**没有移植那些采集/分析功能，会影响模型线索探测，而且不是轻微影响。**目前接入WebAgent的网页摘要脚本和VS Code Companion都只做连接核对，未实现原型模型探测。它们不是完整模型探针的等价升级。先前只强调“不认证模型身份”不足以说明这种功能缺口；“没有认证”与“根本没有实现模型探测”必须分开说。

三者是不同入口，不能混称“整合版已经包含原型能力”：

- `arena-model-probe/src`及由它构建的原型：保留采集、分类、映射和运行标签相关代码，仍有待修问题，未全审。不随WebAgent产品自动启用。
- `arena-model-probe/webagent-connection.user.js` + WebAgent本机核对页面：只输出五字段最小摘要并核对MCP挑战。
- `webagent-core/probe-extension`：同一连接核对流程的桌面VS Code入口，可与WebAgent并装。不会凭VS Code安装身份自动获得网页流量。

## 0.1.x逐项对照（历史缺口，不是最终排除决定）

| 功能/证据 | 原型代码状态 | WebAgent连接版 / VS Code版 | 对探测的影响 |
|---|---|---|---|
| 请求model字段 | 保留，有长度/URL筛选 | 未接入 | 不知道页面请求的模型标识；该字段本身也不证明实际执行者 |
| 响应头、JSON、流中的model字段 | 保留，有界采样；SSE实现仍不完整 | 未接入 | 失去服务端/网关返回的显式名称线索 |
| UUID到公开目录名称映射 | 保留，尚未完整复核 | 未接入 | 无法把未知标识映射到目录候选；映射也不证明当前请求的实际执行者 |
| 协议格式指纹 | 保留，存在跨请求混用风险 | 未接入 | 缺少协议/网关候选推断；协议可兼容或被转译，不能直接等同模型 |
| 行为文本与tokenizer启发式 | 保留，规则未统计校准 | 未接入 | 匿名情况下少了一类弱线索；模板、上下文和分词计数口径都可能造成误判 |
| 运行标签/后台轨迹链 | 保留原型代码，凭据、访问范围及轮询竞态未全审 | 未接入 | 可能失去更具体的上游自报标签；不能因标签来自后台就断言不可改写或已认证 |
| CDP与账户相关驱动脚本 | 原文件保留，未在本轮执行 | 未接入 | 没有对应的浏览器附加/采集自动化；VS Code本身不是网页网络权限的替代品 |
| 指纹/观察记录与导出 | 原型仍有本地记录机制，隐私与数据归属未全审 | 不导入原始dump，只保留当前核对内存状态 | 无模型指纹历史积累；减少原始对话/凭据进入产品的风险 |
| 最小页面摘要+一次性挑战 | 独立连接脚本提供，不是上述分类算法 | 已接入 | 能说明认证MCP会话持有挑战，不能说明模型、人物或浏览器真实身份 |

“未接入”不是删掉原型源代码；但对当前使用连接版的人来说，这些探测能力确实不可用。没有做公平的同数据集、同站点、同版本测试，不能给出“只降低一点”“准确率没有变化”等数字结论。

## 已作修改也有取舍

保留原型功能并不意味着每个优化都无损：

- 旁路读现在限15秒、1MiB、4096块、最多4路。首字节晚于截止、长响应末尾才出现的字段，或高并发被跳过的响应可能漏采；页面自身请求不会因此被探针取消。
- 每行65536字符及4096行上限可能截掉大帧/很多小帧。文本只保留200000字符，usage只扫描最近8192字符，观察历史保留20条。这些不是完整流或完整账单。
- 请求体超过32768字符不解析，可能连同其中本来存在的model字段一起漏掉；URL筛选也可能漏掉自定义路径。
- 更新时禁止旧版热叠加，需要刷新。原main仍可能等DOMContentLoaded，早期请求可能漏采；“document-start”元数据不能代替实际安装时序。
- Response对象/构造器兼容修复旨在减少对页面的干扰，不提供“任意网站零影响”的保证。
- 同一完成观测的行为线索只记一次，会使过去因重复点击/重算而增长的证据条数减少。这是去掉重复计数，不是发现了更少的独立响应；结果分数可能变化，不能把旧分数的虚高当成真实探测能力。

因此需分别评估：**线索覆盖率、误报率、请求归属、资源成本、隐私风险**。采得更多不自动代表判得更准，有界采样更安全也不代表没有漏报。

## 后续应怎样恢复探测用途

这仍是尚未完成的迁移工作，不是当前产品功能表：

1. 先修原型采样协议、重复计数、同请求/并发归属和停止机制，使用合成/经授权的脱敏样本验证。
2. 独立定义“模型线索观察”契约，与当前五字段连接摘要区分；不能偷偷给现有摘要塞model或原始正文。它应携带来源、采样截断、请求关联和明确的未核验状态。
3. 让VS Code接收用户明确选择的脱敏线索并展示来源/候选/冲突/缺失，不把启发式评分写成身份认证概率。0.2.0已提供离线文件分析入口；浏览器自动接入/完整历史等仍在实施。
4. 自动网页采集需要专门浏览器端入口、明确启停与权限，而不是让VS Code后台附加用户浏览器。
5. 凭据/后台轨迹链需要另外核对明确授权、接口访问范围、存储、过期/取消和请求归属；不以“页面里有token”推定可以访问所有资源，不承诺获取平台隐藏身份信息。

以上不会通过自动执行真实账号的令牌/轨迹脚本来“补验收”。原型目前的模型线索仍只是候选证据；可靠认证还需要独立、可信且绑定到具体请求的服务端证据，当前项目没有建立这样的机制。
