# 探针能力对照与迁移边界

## 当前范围（0.5.2，替换旧版迁移表）

目标仍是保留原 Probe 的完整参考用途，与 Inspector 取长补短，再接入 WebAgent。模型字段、自报身份、协议、行为、目录映射都是参考，不是认证；不能以缺乏认证能力为由取消这些功能。Chat 模式 API 后端的确切验证仍后置。

本页旧版本曾以0.1连接版、0.2离线版描述“当前未实现”，再追加0.3更新，已不适合作为现行状态。历史经过见 [阶段时间线](manager/stages/probe-dual-integration-2026-09-15.md)，现行管理状态见 [阶段8](manager/stages/s8-probe-integration.md)。代码实现与真实账户验收分开记录。

## 现行能力对照

| 能力 | 当前入口与实现 | 限制及未验收项 |
|---|---|---|
| 请求/响应模型字段 | 单CDP所有者；独立请求SSETap；请求body/响应头/JSON/流字段 | 白名单/字节预算；标签可以被伪造；实际网站格式待验 |
| UUID公开目录 | 审批刷新固定公开leaderboard、缓存日期/冲突；当前UUID应用和目录导出 | 无Cookie，不跟随跳转；不能证明当前请求由此模型执行；沙箱匿名请求曾ECONNRESET |
| 协议与行为参考 | browserReference与离线Worker复用原分类器；响应与trace分开显示 | 网关协议不是模型家族；分数未校准，不是概率 |
| 主动题组/tokenizer | 查看全文、逐次审批、网页确认；完整基准声明和可比prompt计数才测量 | 拒答题安全替换；发送点击不等于回答完成，计费/计数口径须实机核对 |
| run/span/用量 | Inspector既有授权范围链、逐调用证据；VSCode原生证据分析 | 不混run、不读任意后台；缺失字段和总token不能冒充prompt计数 |
| 实时跨端 | 浏览器→独立/probe-link→主机→VSCode；15分钟单tab能力、显式订阅 | 只对已保存agent会话远控；MV3可能休眠，重启重配；标签可能敏感 |
| 操作审批 | probe_request/operatorQueue；监听、改名、归档、题目及目录刷新 | 逐次批准；网页写入另确认；已发生效果不保证回滚，未知不重放 |
| 历史与迁移 | 显式保存/比较/删除，整库导入导出，Inspector批量历史；旧Probe原生dump转换 | 摘要不是原文备份，不是云同步或跨进程事务 |
| 未知样本/回填 | 原指纹、完整链接相似度簇、精确UUID派生参考，保留原记录 | 不按相似度继承名字；只有时序/长度的样本不聚类 |
| 五字段连接挑战 | 独立userscript/Companion挑战；已认证MCP客户端完成核对 | 与模型观测不同契约，不是人物、浏览器或模型身份认证 |

## 三种文件不能混用

1. **连接摘要**：精确五字段，只走连接挑战；禁止粘贴旧dump或登录信息。
2. **模型观测/Inspector单run证据/旧Probe原生dump**：离线分析命令明确选本地JSON。原buildDump只保留当前响应前4000字符；转换忽略全局BUS证据、slots和缓存判定，避免跨请求污染。导出时间不伪装原请求时间，无当前观测则拒绝，丢失正文不能恢复。
3. **历史库/Inspector批量历史/公开目录**：各走历史导入或UUID追加映射命令，不改schema冒充实时观测。重复内容幂等、冲突拒绝、保存旧记录。

具体操作见 [Companion说明](webagent-core/probe-extension/README.md) 和 [完整实施与验收](探针完整整合实施与验收.md)。

## 采样与安全取舍

- 整合包只有一个debugger所有者，不另启原main的网络钩子。请停用单独的旧Probe/Inspector，避免重复采集。
- 普通响应支持标准/轻量预设，分别256KiB/8并发/15秒与64KiB/2并发/5秒。WS、流块和完整响应按UTF8字节计，二进制WS省略；原trace链另有预算。预算是采样取舍，不是完整流量承诺。
- 流式启用与结束事件按生命周期处理；回退只读取已经完成的响应，不重发网站请求。CDP解压返回的瞬时分配无法由编码长度严格约束。
- 0.5.2将页面采集代次与轨迹查询代次分开：查询刷新不再误丢同页面样本；换页、离开再返回、停止及同ID替换仍淘汰旧结果。
- 发送前再次核对编辑器对象、可写状态和完整已批准文本。用户改稿、DOM替换或按钮不可用时不发送、不覆盖用户新稿、不自动重试。
- “参考”不意味着随意跨请求聚合。槽位a/b、旧缓存、同一模型名称都不能证明属于同一次运行。扩大采样覆盖也不能夸大准确率。

## 当前关闭条件

自动测试、发行包与CI只覆盖代码和合成场景。真实Chrome权限、Arena DOM和目录、模型计数/计费、VSCode持久化、手机步骤11.3仍按阶段8和线性清单实机验证。不用重复的“最新补充”掩盖未验收项，也不把未验收功能改成永久排除项。
