# 启动脚本说明

## 当前Windows入口（2026-09-11）

五个CMD入口统一调用installer/launch.js，classic/vscode/app/admin/extension分别对应run-webagent、run-webagent-vscode、run-webagent-appwindow、run-admin、install-vscode-extension。保持调用者cwd解析相对工作区，文件参数取父目录；显式不存在路径报错，缺省路径才创建；不去掉盘符根分隔符。

安装版只写用户LocalAppData/WebAgent下的运行时和数据；源码版仍在checkout工作。app模式探测healthz等待最多120秒，下载日志在用户数据startup.log。详细布局、迁移/卸载保留策略见installer/README.md。

下面是其余入口及历史实现说明；旧CMD逐行编号和固定5秒等待描述已由上面的统一Node入口替代，不作为当前实现契约。

# 仓库根启动脚本说明书

当前处理目标：仓库根目录下的 `.cmd` / `.sh` / `.gitignore` / `.gitattributes`。

> **为什么不写进根 `README.md`？**  
> GitHub 打开仓库首页就是根 `README.md`（产品说明、两种运行方式、Windows CMD）。第一阶段模板要求「在该文件夹内部建 README」，但根 README 已经是给人看的产品页，**覆盖它会毁掉首页**。根目录也不是一个「只含脚本的子文件夹」（还有 `webagent-core/`、`docs/`、`组件说明.md`），所以启动脚本单独放本文件。

无 Python / 无 HTML。脚本本身就是要说明的代码。行号按当前文件。

---

## 1. 模块概述

- **定位：** Windows 双击入口 + 同功能 bash；环境检查；跑 agent-host 测试；可选管理页。不实现 MCP，只设环境变量并 `node`。
- **依赖：** `webagent-core/agent-host/src/index.js`、`webagent-core/scripts/run-code-oss.js`、`webagent-core/admin-host/index.js`、`agent-host/package.json` 的 `npm test`。
- **谁调用：** 用户在资源管理器双击，或 CMD 里执行。

端口以脚本里 **echo 的为准**（也与 `agent-host/src/config.js` 默认一致）：

| 环境变量 | 默认 | 用途 |
|---|---|---|
| `WORKSPACE_ROOT` | 仓库根下的 `workspace\` | 要改的本机目录 |
| `AGENT_HOST_PORT` | **48271** | MCP / API |
| `WORKBENCH_PORT` | **3000** | 本机工作台 UI |
| `WEBAGENT_ADMIN_PORT` | **4174** | 可选使用统计（`run-admin.cmd`，不是主进程） |

---

## 2. 文件级详细说明书

### 📄 文件名：`run-webagent.cmd`

- **职责：** 经典工作台（**第四阶段起为备用入口**）：只起 Bridge（工作台 + MCP），不下载 VS Code；推荐入口/安装器默认壳已是 `run-webagent-vscode.cmd`。
- **步骤（91 行）：**
  1. L1–L5：`@echo off`；`chcp 65001`；`setlocal EnableExtensions`；窗口标题 `Web Agent`；`cd /d "%~dp0"`。
  2. L7–L8：若有第 1 个参数则 `WORKSPACE_ROOT=%~1`；否则 `%~dp0workspace`。
  3. L10–L11：`AGENT_HOST_PORT=48271`，`WORKBENCH_PORT=3000`。
  4. L18–L26：`where node` 失败 → 提示安装 LTS、`pause`、`exit /b 1`。
  5. L28–L33：有 node 无 npm → 提示重装并勾选 npm。
  6. L35–L47：工作区目录不存在时：无参数则 `mkdir`；**有参数则报错退出**（不替用户建任意路径）。
  7. L49–L62：打印 Node/npm 版本；Git / cloudflared 未安装只提示，不退出（Git 缺则 git 工具不可用；cloudflared 缺则本地 Chat 仍可用）。
  8. L65–L69：打印工作区、`http://127.0.0.1:3000`、MCP 端口 48271、Ctrl+C 停止。
  9. L72–L81：`cd webagent-core\agent-host`；没有 `node_modules\express` 则 `npm install --no-audit --no-fund`。
  10. L83：`node src\index.js`（继承上面的环境变量）。
  11. L84–L91：非 0 退出则提示端口占用 3000/48271，`pause`，`exit /b %ERR%`。成功路径不 pause。

### 📄 文件名：`run-webagent.sh`

- **职责：** 与 `.cmd` 同入口的 bash（指南主体仍是 CMD）。**43** 行。
- L3–L9：有 `$1` 则 `WORKSPACE_ROOT=$1`；否则沿用环境变量或 `$ROOT/workspace`；端口默认 48271 / 3000。
- L15–L22：`command -v node` / `npm` 失败则 stderr 提示并 `exit 1`。
- L24–L31：工作区不存在时：有 `$1` 则报错退出（不替用户建任意路径）；无参数则 `mkdir -p` 默认 `workspace/`。
- L33–L35：打印工作区、UI、MCP 端口。
- L37–L41：进 agent-host；没有 `node_modules/express` 则 `npm install --no-audit --no-fund`。
- L43：`exec node src/index.js`。

### 📄 文件名：`run-webagent-vscode.cmd`

- **职责：** 备选：网页 VS Code。不在本文件里 npm install。
- L7–L8：工作区参数规则同主脚本。
- L15–L26：检查 node / npm。
- L28–L36：工作区**必须已存在**（不 mkdir），否则打印用法 `run-webagent-vscode.cmd D:\code\my-app`；**参数是文件时自动取其目录**当工作区（P5-1，上下文菜单「用 Web Agent 打开」文件项依赖此行为）。
- L36–L38：提示第一次会从 npm 下载 code-server **4.135.0**；**不要和 `run-webagent.cmd` 同时开**（都要用 3000）；Ctrl+C 停止。
- **第四阶段起为安装器默认壳与文档推荐入口**（`installer/webagent.iss` 桌面/开始菜单主图标指向它；用户点名「vscode 复刻的编辑器」当门面）。
- L41：`node "%~dp0webagent-core\scripts\run-code-oss.js" "%WORKSPACE_ROOT%"`。
- L42–L49：非 0 则指向 `网页VSCode使用指南.md`。
- **不**出现 `code-server-dist` 字符串（由 `codeServerNotRunnable.test.js` 锁住）。

### 📄 文件名：`run-webagent-appwindow.cmd`（P5-1 新增）
- **职责：** 「app 窗口」入口：探 3000 口，无主程序则后台最小化拉起 `run-webagent-vscode.cmd`，再用 Edge/Chrome `--app=http://127.0.0.1:3000` 开无边框独立窗口（回退默认浏览器普通窗）。安装器 `appwindow` 任务把它放桌面。
- **边界：** 不开新服务端进程（复用/拉起 VS Code 壳）；浏览器仅作窗口载体，产品逻辑仍在 agent-host。

### 📄 文件名：`run-webagent-vscode.sh`

- 与 `.cmd` 同规则：**不** mkdir。缺 Node/npm 退出；工作区必须已存在。
- 有 `$1` 则写成 `WORKSPACE_ROOT`，否则环境变量或 `$ROOT/workspace`。
- 端口默认 48271 / 3000。最后 `exec node …/run-code-oss.js`。

### 📄 文件名：`check-env.cmd`

- **职责：** 只读检查 PATH，不启动服务、不看仓库文件是否存在。
- L6：Node 版本或「未安装」+ nodejs.org。
- L7：npm 版本或未安装。
- L8：Git 版本或 git-scm 下载页。
- L9：cloudflared 已在 PATH，否则提示 `winget install --id Cloudflare.cloudflared`。
- L10：ngrok 已在 PATH，否则提示 `winget install Ngrok.Ngrok`。
- L11–L13：说明缺什么按链接装，然后新开 CMD；`pause`。

### 📄 文件名：`run-admin.cmd`

- **职责：** 独立进程打开使用统计页，默认端口 4174。主工作台不会自动打开。
- L1–L3：`@echo off`；`setlocal`；`cd /d "%~dp0"`。
- L4：未设 `WEBAGENT_ADMIN_PORT` 则 4174。
- L5–L6：打印「独立进程」说明。
- L7：`node webagent-core\admin-host\index.js`。

### 📄 文件名：`run-admin.sh`

- L1–L3：`set -euo pipefail`；`cd` 脚本目录。
- L4：`WEBAGENT_ADMIN_PORT` 默认 4174。
- L5–L7：打印说明后 `exec node webagent-core/admin-host/index.js`。

### 📄 文件名：`install-vscode-extension.cmd`

- **职责：** 把 `webagent-core/extension` 拷进本机桌面 VS Code 的 `%USERPROFILE%\.vscode\extensions\webagent.webagent-core-<version>`（有 Insiders 再拷一份）。**不**启动 code-server，**不**占用 3000。
- L1–L4：`@echo off`；`chcp 65001`；`setlocal`；`cd /d "%~dp0"`。
- L6–L11：无 node → 提示先 `check-env.cmd`。
- L13–L16：说明是桌面插件安装。
- L17：`node webagent-core\scripts\install-desktop-extension.js`。
- L18–L25：非 0 则 pause 退出；成功提示下一步 `run-webagent.cmd` 与打开同一工作区，指向 [使用指南.md](./使用指南.md) 第 5 节。
- **实现：** `webagent-core/scripts/install-desktop-extension.js`（`copyTree` 跳过 README；`pruneOld` 摘掉其它 `webagent.webagent-core-*`）。测试可设 `WEBAGENT_VSCODE_EXTENSIONS` 指向临时目录。

### 📄 文件名：`run-tests.cmd`

- **职责：** 一键跑现行产品测试（不是 `webagent-repro/tests`）。
- L12–L17：无 node 则让用户先跑 `check-env.cmd`。
- L19–L28：进 `webagent-core\agent-host`；没有 express 则 npm install。
- L31–L33：打印测试目录后 `call npm test`。
- L36–L40：失败 → 指向仓库根 `测试说明.md`，pause，exit 该码。
- L42–L46：成功打印「仓库根不必再单独建 tests\」，pause，exit 0。

### 📄 文件名：`installer/webagent.iss` + `installer/build-installer.cmd`

- **定位：** 第四阶段 S4-1 的 Windows 安装器（Inno Setup 6，**非 Electron**）。`build-installer.cmd` 找 ISCC.exe（两个默认安装路径 + PATH）编译 `webagent.iss`，产物 `installer/output/webagent-setup-<版本>.exe`（output 已 gitignore）。
- **装什么：** 仓库全部源文件与文档；排除 `.git`、`node_modules`、各级缓存、`code-server-app`、`bin/code-server-runtime/*`（保留其 package.json）、日志、`workspace/*`（只带骨架 README）、`manager/privacy.md`。**不捆绑 Node**：安装时检测 PATH/注册表，缺则提示先装 Node LTS 或跑 `check-env.cmd`（不拦安装）；code-server 首跑自下载（既定规则）。
- **默认壳：** 桌面/开始菜单主图标 → `run-webagent-vscode.cmd`（VS Code 壳）；开始菜单「经典工作台（备用）」→ `run-webagent.cmd`。卸载清运行时产物，**不碰 workspace**。
- **沙箱边界：** Linux 沙箱编译不了 .iss；真机编译与安装冒烟在 Windows 验收清单（`review/REPORT_SHUNCODE_S4.md` 收尾时写）。

### 📄 文件名：`.gitignore`

- **职责：** 不提交依赖、运行时、本机密钥、截图垃圾。
- **规则（按行，不是函数）：**
  - L2–L3：`node_modules/`、`.npm/`
  - L6–L12：`code-server-app/`；`bin/code-server-runtime/*` 但 **保留** `bin/code-server-runtime/package.json`；`.cache/`、`.local/`、`*.tar.gz`、`*.tmp.*`
  - L15–L18：`dist/`、`build/`、`out/`、`*.log`
  - L21–L22：`.DS_Store`、`Thumbs.db`
  - L25–L27：`**/.webagent/config.json`、`**/.webagent/read-hashes.json`、`**/.webagent/usage.json`（任意工作区路径；不是整个 `.webagent/`，技能仍可提交）
  - L29–L30：`webagent-core/admin-host/data/`（管理页令牌与上报）
  - L33：`webagent-core/extensions-installed/extensions.json`（启动时会写成绝对路径）
  - L36：`image-search/`

### 📄 文件名：`.gitattributes`

- **职责：** 强制 Windows 脚本在 Git 里用 CRLF，避免检出成 LF 后 CMD 解析异常。
- **规则（L1–L3，无函数）：**
  - L1：`* text=auto` — 文本文件按平台转换换行。
  - L2：`*.cmd text eol=crlf`
  - L3：`*.bat text eol=crlf`

根 `README.md`、`架构导读.md`、`组件说明.md`、`技术实现.md`、`测试说明.md` 是产品文档，不属于本「启动脚本」范围。

---

## 3. 执行逻辑流（仅这些脚本）

```text
用户双击 run-webagent.cmd [可选工作区路径]
  → 有 Node 和 npm？否则退出
  → 默认工作区不存在则 mkdir；自定义路径必须已存在
  → 打印 UI :3000 与 MCP :48271
  → cd agent-host，必要时 npm install
  → node src/index.js   （实现不在本目录）

用户双击 run-webagent-vscode.cmd <已存在的工作区>
  → 检查 node/npm；工作区必须存在
  → node webagent-core/scripts/run-code-oss.js
      → 可能下载 gitignore 掉的 code-server-runtime

用户双击 check-env.cmd → 只打印 PATH 里的 node/npm/git/cloudflared
用户双击 run-tests.cmd → agent-host npm test
用户双击 run-admin.cmd → 独立进程 :4174（主 index.js 不会开这个口）
用户双击 install-vscode-extension.cmd → 拷插件到 ~/.vscode/extensions（不起服务）
```

bash 文件与对应 `.cmd` 同流（工作区参数、缺 Node/npm 退出）。主入口默认工作区可 mkdir；`run-webagent-vscode.sh` 与 `.cmd` 一样不 mkdir。没有 `pause`。

<!-- docs-inventory:start -->
## 自动源码导航

此区块由工具生成；登记和AST提取不等于语义审查通过。不要手改。

| 源码 | 定位证据 |
|---|---|
| [check-env.cmd](check-env.cmd) | 文件级登记；未做符号完整性证明 |
| [install-vscode-extension.cmd](install-vscode-extension.cmd) | 文件级登记；未做符号完整性证明 |
| [run-admin.cmd](run-admin.cmd) | 文件级登记；未做符号完整性证明 |
| [run-admin.sh](run-admin.sh) | 文件级登记；未做符号完整性证明 |
| [run-tests.cmd](run-tests.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-appwindow.cmd](run-webagent-appwindow.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-vscode.cmd](run-webagent-vscode.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-vscode.sh](run-webagent-vscode.sh) | 文件级登记；未做符号完整性证明 |
| [run-webagent.cmd](run-webagent.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent.sh](run-webagent.sh) | 文件级登记；未做符号完整性证明 |
<!-- docs-inventory:end -->
