# 启动入口：选择界面、工作区与运行模式

精细复盘：[逐句语义、参数、目录与CI](docs/development/平台启动与CI详解.md)。


## 职责
根脚本只负责启动、环境检查或测试，不实现MCP业务。本文件是根脚本的归属说明；安装发行布局见[安装器说明](installer/README.md)。旧的几十行CMD逐行转译已经移除，以下按当前薄CMD＋Node启动器解释。

## Windows入口
| 脚本 | launch模式 | 结果 |
|---|---|---|
| run-webagent.cmd | classic | 自绘工作台＋agent-host |
| run-webagent-vscode.cmd | vscode | agent-host＋code-server网页VS Code |
| run-webagent-appwindow.cmd | app | 等待网页VS Code健康后打开App样式浏览器窗口 |
| run-admin.cmd | admin | 独立统计后台，不是工作台管理面板 |
| install-vscode-extension.cmd | extension | 安装桌面扩展，不自动安装VS Code或登录模型 |

这些CMD检查Node，保持调用者cwd，将首个工作区参数交给installer/launch.js并传播退出码。不是先cd到脚本目录再解释相对工作区。

示例（已有目录，含空格时加引号）：

```bat
run-webagent.cmd "D:\projects\my app"
run-webagent-vscode.cmd "D:\projects\my app"
```

## 工作区解析与安装流程
参数优先于WORKSPACE_ROOT；都未提供才选默认目录。显式文件参数取父目录，显式不存在路径报错；默认目录可以创建。源码模式用仓库根目录，安装版用用户数据workspace。

安装版有installation.json，准备可写runtime后运行，配置/依赖不写Program Files。app等待当前3000/healthz最多120秒，失败看startup.log；不能把healthz通过当作整个MCP或桌面PTY已经可用。

可选vscode编排的主机health轮询另有15秒总期限，支持停止；配置/启动失败和主机退出会清理本次直接子进程，未确认退出会明确报失败。只对本次保留的子进程对象操作，不按进程名/端口/旧PID补杀；不保证npm脚本、编辑器worker、PTY或隧道等所有后代退出，仍须本机核对。下载/依赖准备不计入这15秒，已有磁盘配置不回滚。

## macOS/Linux入口及差异
- run-webagent.sh：检查Node/npm，准备工作区和缺失依赖，然后exec agent-host。
- run-webagent-vscode.sh：验证工作区，调用scripts/run-code-oss.js。
- run-admin.sh：启动独立统计后台。
- webagent-core/start-webagent.sh：转发到根run-webagent.sh。

这些shell脚本没有走Windows launch.js，不应宣称拥有完全相同的文件参数或安装版迁移行为。相对工作区路径已在切换cwd前转为调用者PWD下的绝对路径，并用真实Bash＋Node替身验证含空格路径；安装版迁移仍是不同入口。

## 检查与测试入口
check-env.cmd检查Node/npm和环境信息，不自动修复所有问题。run-tests.cmd进入agent-host，检查express及Acorn的package.json，任一缺失则用锁文件执行npm ci --include=dev，再运行npm test；其他文档校验入口仍需事先安装开发依赖。测试失败/缺依赖不能当成功。

## 端口与配置
| 设置 | 默认 | 用途 |
|---|---|---|
| AGENT_HOST_PORT | 48271 | MCP与本机API |
| WORKBENCH_PORT | 3000 | 自绘工作台 |
| CODE_SERVER_PORT | 3000 | 网页VS Code；不同时占用同一UI端口 |
| WEBAGENT_ADMIN_PORT | 4174 | 独立统计后台 |
| DOCS_PORT | 4173 | 文档站，由docs-site/serve.js启动 |
| WEBAGENT_BIND | 127.0.0.1 | 主机监听地址，改变它不等于开放受支持远程UI |

## 失败与验收
端口占用、Node/npm缺失、工作区错误、依赖下载失败和隧道认证失败分别排查。启动器退出码及健康接口比打印的URL更可靠；日志可能含本机连接信息，不公开粘贴。

installerPackaging、skipWorkbench、codeServerAuth及httpSmoke覆盖部分行为，Windows CI提供编译证据。真实安装/升级/卸载、浏览器窗口和VS Code终端不能由静态脚本检查代替；Shell相对路径现另有动态回归。

<!-- docs-inventory:start -->
## 自动源码导航

此区块由工具生成；登记和AST提取不等于语义审查通过。不要手改。

| 源码 | 定位证据 |
|---|---|
| [check-env.cmd](check-env.cmd) | 文件级登记；未做符号完整性证明 |
| [install-vscode-extension.cmd](install-vscode-extension.cmd) | 文件级登记；未做符号完整性证明 |
| [run-admin.cmd](run-admin.cmd) | 文件级登记；未做符号完整性证明 |
| [run-admin.sh](run-admin.sh) | 文件级登记；未做符号完整性证明 |
| [run-tests.cmd](run-tests.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-appwindow.cmd](run-webagent-appwindow.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-vscode.cmd](run-webagent-vscode.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent-vscode.sh](run-webagent-vscode.sh) | 文件级登记；未做符号完整性证明 |
| [run-webagent.cmd](run-webagent.cmd) | 文件级登记；未做符号完整性证明 |
| [run-webagent.sh](run-webagent.sh) | 文件级登记；未做符号完整性证明 |
<!-- docs-inventory:end -->

## 根文件与相对路径补充

.gitattributes固定.sh为LF、CMD/BAT为CRLF、生成的docs-site/content.js为LF，避免Windows检出制造漂移。.gitignore排除依赖/输出/本机敏感运行数据，不自动删除已跟踪内容。

两个Bash入口在切换工作目录前把WORKSPACE_ROOT相对路径转为调用者PWD下的绝对路径，包含空格仍用引号保留；显式不存在目录依然拒绝。不是按agent-host的后续cwd解析。

## 0.7.1：缺少工作区时不启动Bridge

| 入口 | 未指定路径时 | 路径不存在/不是目录时 |
|---|---|---|
| 源码CMD三种UI、根npm start/start:vscode | 仓库根 | 拒绝；CMD显式文件取父目录 |
| 源码两个Shell、run-code-oss、直接agent-host | 仓库根 | 拒绝，不隐式创建错误目录 |
| 安装版launch | 用户数据workspace，首次可创建 | 显式错误路径拒绝，保留Program Files只读布局 |
| 独立桌面VSCode空窗口 | 不替用户选择文件夹 | 点击Bridge启动或发送工作区任务时模态提醒，不发送操作 |

扩展要求可信的首个本地文件夹与主机一致；不是只显示警告后继续执行。GET status前后重查，再由POST校验目录和实例；旧主机、错误项目或已删除目录返回409。多根项目建议单独打开目标，首根规则与PTY/编辑器揭示文件一致。主机不会跟随IDE切目录自动改绑；先停止Bridge/旧主机，再用目标路径启动主机和IDE，刷新后重新配对。

经典工作台没有独立IDE文件夹：页面所属项目由主机启动参数决定，启动Bridge时实时状态必须与页面显示的项目/实例一致，否则提醒刷新核对。工作区绑定只是防误操作，不替代鉴权、审批或OS权限限制。

旧根workspace已改为examples/calculator（可选），根Skills用于产品自检。用户git pull后旧目录若还剩忽略的config/usage等，应先备份再决定清理，不能运行强制删除命令。新根不会自动继承示例里的账号/模型密钥。
