# 证据检查表

- 主机：hostInstanceId与工作台相同，重启后ID可能变化。
- 工作区：与用户指定目录一致，而不是只看到一个同名文件。
- 文件：read_files确实返回目标内容；同时记录hash和读取范围。
- 任务：taskId/callId关联到这次调用，waiting-approval不是完成。
- 结果：外部服务自报成功不等于独立核验；未确认时不要写“已完成”。
- 记录：不向聊天、Git或公开截图复制令牌、完整MCP密钥URL或个人文件正文。
