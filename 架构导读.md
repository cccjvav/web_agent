# 架构导读（给完全没写过这种程序的人）

这篇是「为什么电脑要这样装、不这样会出什么事」。按键步骤看 [使用指南.md](./使用指南.md)；文件夹点名看 [组件说明.md](./组件说明.md)；函数逐步直译看 [技术实现.md](./技术实现.md)。

每一节都按四层写：

1. **人话** — 不先甩缩写。
2. **比喻** — 换一个日常场景，帮助记住「为什么」。
3. **落在仓库哪** — 磁盘上哪个文件在干这件事（以当前源码为准，不写没实现的）。
4. **行业里管这叫什么** — 你以后读别人文档时能对上号。

---

## 1. 这个项目在解决什么麻烦

**人话**  
云上的聊天 AI（Arena、DeepSeek、ChatGPT…）跑在别人的机房里。它默认只能看见你打的字，**进不了你硬盘上的 `D:\code`**。这个仓库把「动手改文件、跑测试」放在**你自己的电脑**上；网页那边只负责想，想完填一张工单过来。

**比喻**  
远方的师傅再聪明，也摸不到你家车库里的车。你不会把整台车寄去机房，而是在自家院子开一间小车间：师傅打电话下工序（「把这个螺丝拧紧，再打火试一下」），车间里的人动手。车间始终在你家。

**落在仓库哪**

- 车间本体：`webagent-core/agent-host/`（进程入口 `src/index.js`）
- 院子墙上的按钮面板：`webagent-core/workbench/`（浏览器 http://127.0.0.1:3000）
- 真正改磁盘的扳手：`webagent-core/agent-host/src/tools/`
- 网页师傅填的工单入口：`src/mcp/server.js`（端口 48271）

**行业里管这叫什么**  
本地工具宿主（agent-host）+ 远端模型。网页侧走 **MCP**（Model Context Protocol，模型调用工具的一套填表规矩）。同类产品介绍可参考 [docs.shuncode.top](https://docs.shuncode.top/docs/intro/)。本仓库是本机工作台 + agent-host，不是 exe 安装包。

不这样会怎样：若把「改文件」也放到云上，你就得把**整份仓库**上传给别人的服务器。本设计刻意反过来——**仓库不整包上传**，网页只填工单。但点了 Bridge / 开了隧道之后，拿到 MCP 地址的人可以读、改、在你电脑跑命令（见第 6、7 节）。不是「工单进门以后代码绝对出不了门」。

---

## 2. 三个绝不能混的东西

**人话**

| 东西 | 它是谁 |
|---|---|
| Git 仓库 `web_agent` | Web Agent **这套程序**的源码（工具箱） |
| 工作区 | Agent **允许读写的那一个目录**（默认 `workspace/`，也可以是 `D:\code\my-app`） |
| 网页上的 AI | 只会聊天、下工单，看不见你的硬盘 |

双击启动时，工作区是仓库里那个带测试的小计算器。**它不是引擎。** 你平时干活的项目，要用 `run-webagent.cmd D:\code\my-app` 指过去。

**比喻**  
工具箱、正在修的那台车、远方师傅，是三样东西。把工具箱本身当「正在修的车」，扳手可能改到启动脚本和说明书上。

**落在仓库哪**  
工作区根写在进程配置 `webagent-core/agent-host/src/config.js` 的 `workspaceRoot`（环境变量 `WORKSPACE_ROOT`）。所有读/写/命令都先经过 `src/tools/patchEngine.js` 的 `resolveSafePath`：路径逃出工作区就拒绝。

**行业里管这叫什么**  
工作区沙箱（workspace sandbox）。和编辑器里「打开了一个文件夹」是同一类边界。

---

## 3. 你电脑上同时活着谁

**人话**  
黑色 CMD 窗口必须一直开着。关了，网页和工作台一起停。同一套程序开了**两扇门**，而且**两套菜单**（两套 Express，不是两个进程）：

- **3000**：给人点的页面（工作台）、本机 Chat、WebSocket。这扇门**不**办临时公网门牌。
- **48271**：给网页工单用的门（`/mcp` + 登录发现文档）。本机的 VS Code 插件也会敲这扇门的 `/api`。**云上经隧道进来时，`/api` 会当成没这道菜（404）**，免得把车间钥匙和改文件按钮送到公网。

只有「让云上的网页改你家文件」时，才再请隧道程序（默认 cloudflared Quick Tunnel，也可以 Named Tunnel 或 ngrok），把 48271 映射成一张公网 https 门牌。本机右侧 Chat **不需要**这个。

另外还有一扇**可选**的统计窗：`run-admin.cmd` 单独起进程，默认 **4174**。主窗口 `index.js` **不会**自动打开它。没配上报地址时，工作台照常，只是本机记一份当天的工具调用次数。

**比喻**  
饭店的**店堂大门**（3000，客人进来点菜）和**后厨送货口**（48271，供应商送原料）不是同一扇门。把两扇门焊成一扇，装修店堂时供应商就进不来——第一代原型 `webagent-repro/` 就是把网页和工单挤在同一个 3000 上，后来换「网页里真 VS Code」会抢门。所以拆开。临时门牌只贴在后厨口；后厨口对外只收工单，不把收银台密码贴到门上。

**落在仓库哪**

```text
run-webagent.cmd
    → node webagent-core/agent-host/src/index.js
        uiApp  :3000  静态 workbench/ + /api + /ws（无 /mcp）
        mcpApp :48271 /mcp + OAuth；/api 先过 src/utils/localControl.js
        不 listen 4174

run-admin.cmd（可选、另一进程）
    → node webagent-core/admin-host/index.js   :4174
```

网页 VS Code 那种跑法会设 `WEBAGENT_SKIP_WORKBENCH=1`：本进程不再占 3000，把店堂让给真 VS Code，后厨 48271 照常。插件打的是本机 `127.0.0.1`，没有 Cloudflare 头，所以 `/api` 仍通。

**行业里管这叫什么**  
工作台是双端口、同一进程。管理页是**第三个、可选的独立进程**。不是两套微服务，只是一扇给人、一扇给机器；统计窗要另开才有。

---

## 4. 三条路，一把扳手

**人话**  
谁下指令可以不同，**改文件的引擎只有一套**（`src/tools/index.js` 的 `callTool`）。

| 路径 | 谁在下指令 | 要不要隧道 |
|---|---|---|
| A 本机 Chat | 你在 3000 右侧说话 | 不要 |
| B 网页 Agent | 云上聊天栏填工单 | 要（Quick Tunnel / Named Tunnel / ngrok 任选） |
| C 网页 VS Code | 侧栏 / `@webagent`，其实还是打本机 `/api/chat` | 网页改磁盘仍走 B |

**比喻**

- A：你自己站在车间里喊「拧那颗螺丝」。
- B：师傅打电话，电话线必须先接到你家门口的**临时门牌**（公网 https），否则他拨 `127.0.0.1` 拨到的是**他自己那台电脑的客厅**。
- C：把店堂装修成更像 VS Code 的样子，后厨还是那一间。

**落在仓库哪**

- A：`workbench/app.js` → `js/chat.js` `sendChat` → **3000** 的 `POST /api/chat` → `src/agent/runChat.js`（认第二参或 `payload.emit`）→ `callTool(..., ask|plan|code)`
- B：云上 HTTPS → 只进 `/mcp`（`src/mcp/server.js`）→ `callTool(..., remoteToolMode)`（默认 code）。隧道请求打 `/api` 得 404。
- C：`run-webagent-vscode.cmd` → `scripts/run-code-oss.js` 拉 code-server；插件 `extension/extension.js` 仍 POST 本机 `127.0.0.1:48271/api/chat`

**行业里管这叫什么**  
同一工具层、多种前端（local chat / MCP client / editor extension）。编辑器只是载体，推理和工具在独立 host。

不这样会怎样：每换一个界面就重写一遍补丁引擎，哈希预检和路径沙箱会各写各的、必漏。

---

## 5. 「工单」长什么样（MCP）

**人话**  
网页 AI 不能直接 `fs.writeFile`。它只能发一种填好的表格：「请用某某工具、参数是这些」。本机核对钥匙后执行，把结果（或失败原因）写回同一张表。

失败通常**不是断线**。工具说「缺哈希」「命令太危险」会作为表格里的错误正文返回，让模型改参数再试。

**比喻**  
不是让师傅闯进厨房乱翻，而是给一本**点菜单**：只能点菜单上的菜。点到「红烧狮子头」厨房没有，不会把整家店的电话挂断，而是回一句「没有这道菜，有宫保鸡丁、鱼香肉丝…」。网页端看到的 `isError` 就是这张回条，不是店关门。

**落在仓库哪**

- 收表：`src/mcp/server.js` 的 `handleRpc`（`initialize` / `tools/list` / `tools/call`）
- 钥匙：URL 里的 `/mcp/<密钥>`，或 ChatGPT 自制 MCP 插件的登录令牌（`src/mcp/oauth.js`）
- 工具失败：`tools/call` **始终**回 MCP `isError:true` 文本；只有「根本不是一张合法工单」（坏协议、未知方法）才走协议级错误
- 第一句连接语（测试锁死原文）：`src/mcp/instructions.js` 的 `CONNECT_LINE`

**行业里管这叫什么**  
MCP、Streamable HTTP、JSON-RPC 2.0。Paste-URL 客户端把密钥放在路径里；OAuth 客户端用 Bearer 打规范地址 `/mcp`。

---

## 6. 大门钥匙：长期门卡 vs 一次性访客码

**人话**

- Arena 一类：地址里带着一串密钥，等于门卡。拿到完整地址的人可以读文件、在你电脑跑命令。**不要发到群里。**
- ChatGPT 自制 MCP 插件：给它看**不带密钥**的 `/mcp`，本机 Bridge 页出现一串几分钟有效的配对码，用过作废。聊天栏贴链接不会走这条。
- 点「重置 MCP 地址」：换新门卡，**写进**工作区 `.webagent/config.json`。旧链接立刻失效，重启也不会把旧卡读回来。

**比喻**  
小区门卡可以一直刷；快递柜取件码用一次就作废。给「只会贴网址、没有登录框」的客户端发门卡；给「自制插件表单、能弹登录」的发取件码，避免长期密钥出现在聊天记录里。

**落在仓库哪**

- 门卡：`config.secretKey`，启动时 `persistIdentity` 从 `config.json` 读回
- 换卡：`POST /api/bridge/reset-secret` → `generateNewSecret()`（会 `store.patch`）
- 取件码：`src/mcp/oauth.js` 的 pairing（内存，进程关了就没）

**行业里管这叫什么**  
URL secret、OAuth 2.1 授权码 + PKCE。本仓库只实现自制插件 OAuth 要用的那一小截，不是完整账号系统。

---

## 7. 临时门牌（隧道）

**人话**  
`127.0.0.1` 的意思永远是「**这台正在执行的电脑自己**」。云上的 ChatGPT 去连 127.0.0.1，连的是机房那台机器，不是你家。所以要一条公网 https，把请求转发进 48271。

点「启动 Bridge」（默认 Cloudflare）会拉起 Quick Tunnel，最多等约 25 秒。成功则 MCP 变成 `https://….trycloudflare.com/mcp/…`。本机没装 cloudflared 时按钮仍成功，地址停在当前页面——本机预览可用，云上连不上。选 **Named Tunnel** 并填主机名 + Tunnel Token 时，会跑 `cloudflared tunnel run --token`，MCP 用你填的 `https://主机名`（Cloudflare 仪表盘里 Public Hostname 要指到 `http://127.0.0.1:48271`）。选 **ngrok** 并填 Authtoken（也可环境变量 `NGROK_AUTHTOKEN`）时，会 spawn PATH 上的 `ngrok http`；可选填预留域名。缺字段或连不上时按钮仍成功，地址停在当前页面，**不会**偷偷改走另一种隧道。逐步填法见 [隧道使用指南.md](./隧道使用指南.md)。

Quick Tunnel（以及未预留域名的 ngrok）的域名**每次启动都可能变**，必须重新复制。Named 和预留 ngrok 域名固定，重启后还是那个。

**比喻**  
在信封上写「送到客厅」——每个房子都有客厅。你要的是「送到北京市某某门牌」。Quick Tunnel 像物业每天换一次的临时门牌：方便，但朋友得拿新地址。Named Tunnel 像办好的固定门牌：要先在物业（Cloudflare）登记，并把你家后厨口告诉他们。ngrok 是另一家快递：用 Authtoken 开隧道，也可以预留固定门牌。不要同时开两家——工作台换一种再启动会先停掉旧的。

**落在仓库哪**

- 拉隧道：`src/api/routes.js` 的 `POST /bridge/start` → `src/tunnel/cloudflared.js` 的 `startQuickTunnel` / `startNamedTunnel`，或 `src/tunnel/ngrok.js` 的 `startNgrokTunnel`
- 停：`/bridge/stop` 与登出都 `stopTunnel`（会停 ngrok）
- 工作台只负责按钮和 toast：`workbench/js/bridge.js` 的 `startBridge` / `paintBridge`（入口仍是 `workbench/app.js`）。Named 带 `#named-domain` / `#named-token`；ngrok 带 `#ngrok-domain` / `#ngrok-token`；Token **不**出现在 `/status`

**行业里管这叫什么**  
Cloudflare Quick Tunnel（无需账号的临时 HTTPS 反代）、Named Tunnel（Zero Trust 里绑好的固定主机名）和 ngrok（Authtoken + 可选预留域名）。不是 VPN，也不是把整个电脑暴露成远程桌面，只转发你指定的那一个本地端口。本仓库再加一层：**看 Cloudflare 头和 Host 名**（含 `*.ngrok*` 与当前 `publicTunnelUrl` 的自定义域名），公网来的请求不能用未鉴权的 `/api`（`src/utils/localControl.js`）。不能只看「是不是 127.0.0.1」——cloudflared / ngrok 连的就是本机回环。

---

## 8. 扳手箱：能干什么、故意不让干什么

**人话**  
大约 25 个正式工具名（读目录、搜、读文件、打补丁、跑命令…）。网页若喊 `bash` / `cat` / `ls`，主机会翻译成正式名，但 **Windows 上命令字符串仍走 PowerShell**。

三档工牌：

- **Ask / Plan**：只能看。喊「改文件」会被拒。
- **Code**（界面上 Agent 默认就是它）：可以补丁、可以跑测试。

本机 Chat 看你点的 Agent ▾。网页工单默认按 Code；若对方在工单里带 `params._meta.mode=ask`，也可以改成只读。

改已有文件优先「只换这一段」（补丁），并核对该段是否还是刚才读到的版本（哈希）。从未读过会要你带哈希再试；读完磁盘又被别人改了会说过期。读过的哈希记在 `.webagent/read-hashes.json`，关掉程序再开还在；点「清除本轮统计」会删掉。

`.env`、密钥、SSH、`.webagent/config.json` 读不出来。`rm -rf` 一类要额外确认。

**比喻**

- Ask 是参观证，Code 是施工证。
- 哈希像你复印文件时盖的「此页指纹」：回去改的时候先对指纹，对不上说明有人动过这一页，禁止凭记忆整页覆盖。
- 敏感文件像保险柜：参观证、施工证都打不开。

**落在仓库哪**

| 事 | 文件 |
|---|---|
| 名单、工牌锁、危险命令闸 | `src/tools/index.js` |
| 别名 `bash`→`run_command` | `src/tools/normalize.js` |
| 补丁 + 路径沙箱 | `src/tools/patchEngine.js` |
| 读哈希落盘 | `src/tools/readCache.js` |
| 敏感路径 | `src/tools/sensitive.js` |
| 跑命令（Windows=PowerShell） | `src/tools/executor.js` |
| 网页默认 Code | `src/mcp/server.js` 的 `remoteToolMode` |

**行业里管这叫什么**  
工具调用（tool calling）、内容寻址预检（hash / optimistic concurrency）、密钥扫描（secret redaction）。本机 Chat 的 Plan 是**串行多模型分支**：有 API Key 时按你选的模型作答，没 Key 时写本机草案；总结要 ≥2 支。没有假的 97% 投票。

---

## 9. 为什么文件夹要拆开（不是为了好看）

**人话 + 隐患**

| 拆法 | 人话 | 不拆的隐患 |
|---|---|---|
| 面板 `workbench/` vs 车间 `agent-host/` | 按钮改不了磁盘，所有动手都进 host | 换皮（真 VS Code）时要把补丁引擎再写一遍 |
| 插件 `extension/` 只打 HTTP | VS Code 侧栏是另一块面板，扳手还在 host | 把工具写进编辑器内核，换皮时就要重写扳手 |
| 默认工作区不是仓库根 | 计算器 ≠ Web Agent 程序 | Agent 可能改掉 `run-webagent.cmd` 和说明书 |
| `.webagent` 在工作区里 | 密钥、技能属于「正在修的那台车」 | 本仓库 gitignore 用 `**/.webagent/config.json`；挂别人的仓库时启动会给那份 git 补 ignore，技能仍可提交 |
| `webagent-repro/` 冻结 | 最早那版 Bridge，对照用 | `npm start` 端口和工具都对不上现在的指南 |
| 不 vendor DeepSeek++ | 扩展有自己的商店更新 | 拷源码进仓库会过期，还多一个无关的本机 Shell |
| 不 vendor Chat Plus | GPL v3；从 GitHub 自己编译 | 拷进本仓库会传染许可证；也不要为连 Bridge 再装 MCP-Gateway |

**落在仓库哪**  
对照表见 [组件说明.md](./组件说明.md) 第 5–6 节。启动脚本**不会**进入 `webagent-repro/`。

**行业里管这叫什么**  
关注点分离、可替换 UI、工作区与工具链隔离。

---

## 10. 一次「网页让你修测试」在机器里怎么走

以 DeepSeek 网页为例（Arena 把提示词当第一句，后面同一套扳手）。

**人话**  
你说话 → 扩展替模型填工单 → 公网门牌转到你家 48271 → 核对钥匙 → 读文件拿指纹 → 打补丁 → 跑测试 → 工作台右侧刷出卡片 → 磁盘已经变了。

**比喻**  
师傅口述「刹车异响」→ 监工（DeepSeek++）写成标准工序单 → 快递按临时门牌送到你家后厨 → 核对门卡 → 复印相关页并盖指纹 → 只改那一段 → 打火试车 → 店堂屏幕上出现「刚干了哪一步」。

**落地闭环（源码路径）**

1. 浏览器 `chat.deepseek.com`（扩展不是本仓库的代码）。
2. HTTPS `https://….trycloudflare.com/mcp/<密钥>`。
3. `src/mcp/server.js`：`extractToken` → 与 `config.secretKey` 比对。
4. `initialize` 把 `src/mcp/instructions.js` 的规则塞给对方。
5. `tools/call` → `callTool(name, args, 'code')`（除非 `_meta.mode` 另写）。
6. `read_files` → sha256 写入 `read-hashes.json`。
7. `apply_patch`：对指纹；写 `.tmp.*` 再改名，避免写到一半停电留下半份文件。
8. `run_command`：工作区里跑，Windows 是 PowerShell。
9. `src/utils/eventBus.js` 经 **3000** 的 `/ws` 推到 `workbench/app.js`，BRIDGE 出卡片。48271 不挂 WebSocket。

本机 Chat 从第 3 步换成 **3000** 的 `POST /api/chat` → `runChat.js`，后面相同。

**行业里管这叫什么**  
远程 tool-calling 闭环、原子替换（temp + rename）、事件总线刷新 UI。

---

## 11. 已经补上的隐患，和仍不要写成「已经接上」的

下面表格里的做法都已经在源码里，不是愿望清单。

| 隐患（不这样会怎样） | 现在的做法 | 文件 |
|---|---|---|
| 点「启动 Bridge」只亮灯、云上仍连 127.0.0.1 | Cloudflare 时 `await startQuickTunnel`；Named 时 `await startNamedTunnel`；ngrok 时 `await startNgrokTunnel`；失败仍 200，本机预览走当前页面 | `src/api/routes.js`、`src/tunnel/cloudflared.js`、`ngrok.js` |
| 重置密钥后重启，旧门卡又活了 | `generateNewSecret` 写入 `config.json` | `src/config.js` |
| 网页 Agent 断线再来，不带哈希就打补丁失败 | 哈希落盘 `read-hashes.json`（给 `apply_patch`）；「清除本轮统计」删除。`write_file` 覆盖只认本进程读过的 hash | `src/tools/readCache.js`、`fileOps.js` |
| 取消命令后仍显示跑完 | 先把状态写成 cancelled 再杀进程；close 不能改回 done | `src/tools/executor.js` |
| 工作台日志里出现 PAT / API Key | `eventBus` 写入 logs / WebSocket 前脱敏截断；WS 最多 32 路 | `src/utils/eventBus.js` |
| MCP 每次请求换新 Session-Id、SSE 后续 POST 丢密钥 | initialize 发并记住 id；未知 id 404；DELETE 结束；SSE endpoint 带回 `/mcp/<secret>` | `src/mcp/server.js`、`session.js` |
| grep 扫到巨大文件把进程拖死 | 跳过 >1.5MB 和含 NUL 的文件；最多 800 个文件、约 8MB；拒绝嵌套量词 | `src/tools/fileOps.js` |
| 命令序号重启后撞车、命令表无限涨 | `execId` 随机 hex；同时 running 最多 8；已结束最多留 40 | `src/tools/executor.js` |
| 用过的 refresh token 再刷一次仍让新 token 活着 | 重放则吊销该 client 全部 token；注册客户端最多 80 | `src/mcp/oauth.js` |
| 新建文件把 unified diff 写进磁盘 | 像 diff 的正文拒绝；空 SEARCH 或纯正文才建新文件 | `src/tools/patchEngine.js` |
| 网页工单完全不上工牌锁 | 远程默认 Code；可 `_meta.mode` | `src/mcp/server.js` |
| 隧道把未鉴权 `/api` 送到公网 | mcp/工作台 `/api` 见隧道头或公网 Host 则 404；`/ws` 非本机直接关掉 | `src/utils/localControl.js`、`src/index.js` |
| 默认听所有网卡，局域网可打 `/api` | 默认 `WEBAGENT_BIND=127.0.0.1` | `src/config.js` |
| 符号链接指到工作区外 | `realpath` 后必须仍在工作区；列目录不跟随外链 | `src/tools/patchEngine.js` |
| 远程 `run_command` 几乎任意 Shell | 危险命令本机要 `confirm_dangerous`；**远程 MCP 即使带该标志也 `E_FORBIDDEN`**。子进程环境去掉 API Key / token。Unix 杀进程组 | `src/tools/index.js`、`executor.js` |
| 两个 Agent 同时改同一文件 | 按路径排队写锁；后到的用旧哈希会 `STALE_FILE` | `src/tools/patchEngine.js` `withWriteLock` |
| UNC / 盘符路径绕过相对路径检查 | `resolveSafePath` 先拒 `//server/share`、`C:\`、`\\?\` | `src/tools/patchEngine.js` |
| 网页 VS Code 关掉工作区信任 | 默认不再传 `--disable-workspace-trust` | `scripts/run-code-oss.js` |
| 任意网页跨域打本机未鉴权 `/api` | MCP CORS 白名单；`/api` 非本机 Origin/Referer 则 404。密钥仍管 `/mcp` | `src/utils/corsAllow.js` |
| 网页 VS Code 无登录 | 默认 password；口令在 `.local/share/code-server/webagent-password`；trusted-origins 不再是 `*` | `scripts/codeServerAuth.js` |
| 工作台保存绕过补丁/敏感闸 | `PUT /api/files/content` 与新建 Skill 走 `write_file`（原子替换）；`.env` 写不进去 | `src/api/routes.js`、`fileOps.js` |
| 挂 `D:\\code\\my-app` 时密钥被 Git 提交 | 启动写入 `.webagent/.gitignore`；若该目录是 git 仓库再补仓库根 ignore；`config.json` chmod 0600 | `src/models/store.js` |
| Plan 无 Key 时仍要诚实 | 本机草案 `simulated:true`，`agreementRate:null`；有 Key 才 `runOpenAI`。界面写「本机拼接 / 合并主模型」，**没有**假 97% | `planRound.js`、`consensusEngine.js`、`runChat.js`、工作台 |
| Windows 文件被补丁改成 LF | 匹配在 LF 上进行，写回用原文件的 CRLF/LF | `src/tools/patchEngine.js` |
| SEARCH 命中多处悄悄改第一处 | 默认必须唯一；可传 `occurrence`（从 1 数） | `src/tools/patchEngine.js` |
| 界面写成 GitHub 登录 / 「永久顺」 | 演示钮仍写「本机演示授权」，**不是**「使用 GitHub 登录」。可选 PAT / 设备码只校验用户名，**令牌不进** `config.json`。旧盘假 github（无 `githubId`）迁成 `local-demo`；带 `githubId` 的留下 | `src/auth/github.js`、`store.js`、`routes.js`、工作台 Bridge 页 |
| Bridge 工具次数没处记 | 仅 MCP `tools/call` 写入当天 `.webagent/usage.json`；「清除本轮统计」不清它。配了 URL+令牌才 POST 到 4174 | `src/usage/tracker.js`、`mcp/server.js`、`admin-host/` |
| Codex / 挂钩 / 插件 / 语音看起来能用 | 界面标明未实现或不会执行；探测模型不用 id 猜视觉/1.3M。Named Tunnel / ngrok **会** spawn（要 Token / Authtoken） | `workbench/index.html`、`providers.js`、`cloudflared.js`、`ngrok.js` |
| `review` Skill 只有半句话，模型不知道怎么审 | 完整只读审查流程 | `workspace/.webagent/skills/review/SKILL.md` |
| GET `/status` 把整段工具结果塞给插件 | `recentLogs` 只留 `tool_call_end` 的 tool / success / durationMs；`tools` 不含 inputSchema；全文仍在本机 `GET /logs` | `src/api/routes.js` |
| 远程 `get_logs` 带回补丁/命令输出 | 工具只返回 type / 工具名 / 成败 / 耗时 | `src/tools/index.js` |
| `recall` 的 `limit` 把标题行也算进去 | 按 `- ` 条目计数，标题不算 | `src/models/memory.js` |

仍不要写成已经接上的：Codex OAuth 不读写 `~/.codex/auth.json`；挂钩保存后不会自动跑命令；没有插件市场；没有语音；没配 Key 的 Plan 分支不会假装已经调了远程大模型；没有浏览器自动点选测试（Playwright）；主进程不会打开 4174。Named Tunnel 和 ngrok 已经接上：缺 Token/Authtoken/主机名会明确报错，不会偷偷改走 Quick Tunnel。故意不做的**理由**见下一节，不是漏修。

---

## 12. 故意不做的取舍（不是漏修）

**人话**  
产品的主路径是：**给网页上的 AI 一座 MCP 桥**，让它当 Agent 改**你这台电脑上的仓库**。桥是门；门后面的扳手仍是读文件、打补丁、跑测试（常见就是 `npm test`、装依赖）。没有后面那些工具，桥接过去也改不了磁盘。

隧道为**一次任务**开着是正常的——网页 Agent 修一个功能可能几十分钟到几小时，这段时间门牌必须还在。这里说的「不要按 24 小时金库来设计」，指的是：**通宵无人看管、把地址发给别人、或永久挂在公网**。那是另一种产品。一次任务挂着，靠的是「拿到完整 MCP 地址 = 施工证」+ 远程危险命令拒绝 + 公网打 `/api` 得 404。任务做完点 **停止 Bridge**。

「两路客户端并行」不是默认用法。例子：Arena 和 DeepSeek++ **同时**连同一把密钥；或者工作台右侧 Chat 正在跑，网页 Agent 也在改同一仓库；或者两个人拿到了同一条 MCP 地址。今天进程里的读哈希、Plan 回合、命令表、点「清除本轮统计」是**一份**。一个人、一个网页、一条隧道时遇不到。真要两路同时改，后到的补丁会 `STALE_FILE`，但点清除会误伤另一路。

下面这些**故意不做**（做了会弄坏现有贴 URL 客户端，或把攻击面换一种方式放大，或变成另一种产品）：

| 不做 | 为什么现在不必 |
|---|---|
| 拆成多把 secret（审计清单里的 P1-5） | Arena / DeepSeek++ / Chat Plus 都靠一行 `/mcp/密钥`。拆开现有客户端全要改。拿到地址已经等于施工证，多把钥匙缩小不了这次任务的权限。 |
| 操作系统级命令沙箱（和「路径不能跑出工作区」不是一回事） | 网页 Agent 正经就要在工作区里跑测试、装依赖。沙箱一紧主路径先坏。路径 `realpath`、远程危险命令 `E_FORBIDDEN`、子进程清 Key 已经挡住「翻出文件夹 / 把密钥带进命令」。 |
| 系统钥匙串 | 密钥跟着工作区（每台「车」一把），gitignore + `chmod 0600`。启动时若 Git **已经跟踪** `config.json`，会警告 `git rm --cached`（gitignore 挡不住已提交的文件）。搬进钥匙串要 native 依赖，换电脑要重填，URL 密钥还是得给网页端。 |
| 按客户端隔离全部全局状态 | 一个人一条隧道用不到。隔离等于重写哈希/任务/命令表。 |
| OpenAI Codex OAuth | 不绑死 ChatGPT。本机 Chat 填 Endpoint + Key 即可。 |
| OAuth 落盘 | ChatGPT 自制插件的 token 只在内存；关进程要重新配对。磁盘被拷走也拿不到活 token。主客户端走 URL 密钥，密钥已经在 `config.json`。 |
| 交互式 PTY | 远程 Agent 能交互输入，比「一条命令跑完」危险。主路径不需要密码提示或 REPL。 |
| Playwright 点工作台 | HTML 测锁定 `bind()` 的 id，HTTP 烟测走真进程。浏览器点选贵且脆。 |
| 假 97%、演示钮改回「使用 GitHub 登录」、改 `webagent-repro` JS | 把已经拆掉的假装再装回去。 |

**比喻**  
工地大门白天开着给师傅进，下班上锁——不是把大门焊死，也不是改造成银行金库。给师傅的是一张施工证，不是把保险柜密码拆成五把再发五张证。

**落在仓库哪**

- 施工证 = URL 密钥：`config.secretKey`，工作台 `/api/status` **仍带** `secretKey`（本机拼 MCP 地址要用）
- 路径沙箱 / 远程危险命令：`src/tools/patchEngine.js`、`src/tools/index.js`
- 密钥不要进 Git：`src/models/store.js` 的 `protectWorkspaceSecrets`；已跟踪则 `warnTrackedSecrets`（启动 `persistIdentity` 会调）
- 无 PTY：`executor.js` `sendCommandInput` 恒定失败
- 无 Codex OAuth：工作台 Codex 页写明未实现

**行业里管这叫什么**  
威胁模型（threat model）：信任「这台电脑的主人」和「一次任务期间拿着施工证的网页 Agent」；不信任「把地址发到群里的路人」和「通宵无人值守的公网」。工作区沙箱 ≠ OS 沙箱。Paste-URL secret 是能力凭证，不是登录会话。

---

## 13. 术语对照（先人话）

读到缩写时回这里。解释以本仓库行为为准。

| 你听到的词 | 人话 |
|---|---|
| Agent | 会自己决定下一步、去调工具的助手，不是只会聊天 |
| MCP | 网页 AI 给本机填的标准工单格式 |
| Bridge | 工作台上「开门让网页进来」的那一页 |
| 隧道 / Quick Tunnel | 给家里 48271 办的临时公网门牌 |
| Named Tunnel | 在 Cloudflare 登记的固定主机名，启动时要填 Token |
| ngrok | 用 Authtoken spawn `ngrok http`；可选预留域名 |
| `127.0.0.1` | 「这台电脑自己」，每台机器都有一个，不能当你家地址给云用 |
| 工作区 | Agent 能改的那一个文件夹 |
| sha256 / 哈希 | 文件内容的指纹，用来发现「你读完之后有人改过」 |
| Ask / Plan / Code | 参观证 / 开会不许动手 / 施工证 |
| OAuth / 配对码 | 给会弹登录的客户端用的一次性访客码 |
| Streamable HTTP | 工单用普通网页那种请求来回传，不是另接一套专用线 |
| WebSocket `/ws` | 车间往店堂屏幕推「刚干完哪一步」 |
| Skill | 放在工作区里的一篇作业指导书，模型用 `load_skill` 读 |
| CORS | 浏览器问「那个网页能不能看这次回答」。本仓库只给扩展和名单里的聊天站点头；**不是**门卡 |

---

## 14. 读完这篇之后

| 你想做的事 | 去哪 |
|---|---|
| 从安装点到改文件 | [使用指南.md](./使用指南.md) |
| 隧道逐步（CMD） | [隧道使用指南.md](./隧道使用指南.md) |
| DeepSeek 网页 + 扩展 | [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md) |
| 多个网页 AI + Chat Plus | [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md) |
| 浏览器里真 VS Code | [网页VSCode使用指南.md](./网页VSCode使用指南.md) |
| GitHub 上每个目录是谁 | [组件说明.md](./组件说明.md) |
| 故意不做的取舍 | 本文件第 12 节；[SECURITY.md](./SECURITY.md) |
| 函数逐步、分支不漏 | [技术实现.md](./技术实现.md) |
| 导读/直译做成可点的图 | [docs-site/](./docs-site/README.md) |
| 子文件夹说明书索引 | [总览.md](./总览.md) |
| 测了什么 | [测试说明.md](./测试说明.md) |

以后若架构、数据流或「为什么这样设计」变了，应改**本文件对应小节**（约定在 `workspace/.webagent/skills/docs-sync/SKILL.md`），不要只改代码不改这篇。
