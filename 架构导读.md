# 架构导读（给完全没写过这种程序的人）

这篇解释“为什么这样设计”，按**人话 → 比喻 → 源码落点 → 行业叫法**阅读。按键步骤见[使用指南](使用指南.md)，目录职责见[组件说明](组件说明.md)，函数细节见[技术实现](技术实现.md)及对应模块详解。本篇不是全部源码的安全认证。

## 1. 这个项目在解决什么麻烦

**人话**
外部AI不能凭一条普通聊天消息直接修改你的硬盘。WebAgent在本机提供工具；兼容MCP客户端经过认证后提出工具请求，由本机执行并反馈。另有本机Chat，使用你明确选择的模型或内置探索器。

源码可直接运行，也有Inno Setup Windows安装器源码和构建链；不是Electron。不能再把“不是Electron”误写为“没有安装包”。安装/升级/卸载的可用性仍须看对应产物与实机验收。

**比喻**
师傅在远处提出工序，本机车间实际动手。不把整台车默认寄出去，不等于师傅永远收不到车间资料：读文件、截图与模型请求都可能发送数据。

**落在仓库哪**
`webagent-core/agent-host/src/index.js`启动工具宿主；`src/mcp/server.js`接收外部工单；`src/agent/runChat.js`处理本机Chat；`installer/`维护Windows载荷与启动布局。

**行业里管这叫什么**
本地工具宿主、远程工具调用、数据流边界。详见[安全说明](SECURITY.md)和[安装器](installer/README.md)。

## 2. 三个绝不能混的东西

**人话**
源码仓库是工具箱；工作区是这次允许文件工具操作的项目；模型是提出任务的调用方。源码启动默认工作区**就是仓库根**，便于自检；`examples/calculator`仅显式选择才使用。安装版默认工作区在用户可写目录，不向Program Files写运行数据。

**比喻**
工具箱也可以是本次维修对象，但必须明确知道自己正在修工具箱，而不是另一台机器。错绑时应拒绝派工，不能只有黄色提醒。

**落在仓库哪**
`src/config.js`、根启动脚本和安装器确定默认根；扩展`workspaceBinding`及`src/utils/workspaceBinding.js`核对当前工作区/hostInstanceId。空窗口、错误首文件夹或过期主机身份会阻止相关启动/操作。

**行业里管这叫什么**
显式上下文绑定与失效检测，不是OS沙箱。文件工具有真实路径约束，任意命令仍能以当前系统用户访问工作区之外。

## 3. 你电脑上同时活着谁

**人话**
经典入口通常由一个agent-host进程提供两个端口：3000给本机工作台、`/api`、`/ws`；48271给MCP/OAuth和受保护本机`/api`。公网隧道只应指向MCP端口，本机管理接口继续拒绝公网/跨站访问。

```text
run-webagent.cmd
  └─ agent-host
       ├─ :3000  经典工作台 + 本机API + WebSocket
       └─ :48271 MCP/OAuth + 受保护本机API

run-admin.cmd（可选，独立进程）
  └─ :4174 统计管理，不由主机自动启动
```

code-server入口设置`WEBAGENT_SKIP_WORKBENCH=1`，让出3000给code-server；同默认端口的经典/网页VSCode入口不要同时启动。桌面VSCode本身不抢3000，主机仍可提供经典页面。端口可配置，以启动日志为准。

**比喻**
同一车间分出人用面板和机器工单入口；可选统计窗口不是自动营业的另一间车间。

**落在仓库哪**
`src/index.js`的uiApp/mcpApp、`src/utils/localControl.js`、`src/utils/corsAllow.js`；`webagent-core/scripts/run-code-oss.js`和`webagent-core/admin-host/`。

**行业里管这叫什么**
控制面与协议入口分离。仅绑定回环还不够：本机隧道代理也从回环转发，需要同时检查Host/转发头及浏览器跨站来源。

## 4. 四条路，共享工具但不同时执行两种模式

**人话**
经典Chat、code-server扩展Chat、桌面扩展Chat都调用本机`/api/chat`；Arena等外部MCP客户端经Bridge调用工具。**Chat与Bridge是主机级互斥执行模式**，不是随意并行的两个Agent入口。Chat内部可多模型，Bridge内部可多个Arena Agent同类型并发；人工编辑和批准不等于启动另一类模型。

**比喻**
同一班次可以有多位同类型师傅协作，但不能让两套互斥的调度同时指挥机器。界面换页并不等于班次已经切换。

**落在仓库哪**
`src/utils/executionControl.js`维护idle/chat/bridge、在途租约及后台/审批屏障；API、MCP、PTY和受控执行接入同一规则。扩展`extension.js`先绑定工作区，再发任务。

**行业里管这叫什么**
主机级模式仲裁、同类型并发。任务板的会话归属不等于所有状态已按客户端完全隔离；读哈希缓存等仍有共享状态，不能冒充多租户OS隔离。详见[权限与模式](Bridge权限与工作模式.md)。

## 5. 工单长什么样，以及出站MCP为何不是另一种登录

**人话**
MCP客户端发送initialize、tools/list、tools/call等请求。WebAgent作为server处理Arena请求；如果你另行接入第三方MCP，WebAgent又作为client请求那一端。

```text
Arena client ──认证入站──→ WebAgent server → 本机工具
                              │
                              └─ 可选出站client ──HTTPS──→ 第三方server
```

第二段不是第一段的前提，也不是控制Arena网页或调用Arena模型API。登记第三方时就发送初始化/目录发现；工具调用还需逐次本机批准。两套认证凭据不自动相互转发。

**比喻**
车间接师傅工单，同时可以经主人同意向另一家专业工坊下单。自己既能接单也能下单，不矛盾。

**落在仓库哪**
入站`src/mcp/server.js`，出站`externalClient.js`/`publicHttps.js`；本机登记后，外部AI通过external_servers→external_request→等待批准→operation_result取结果。HTTP、RPC、业务和verification状态必须分别检查，不能拿HTTP200当工具完成。

**行业里管这叫什么**
连接相对的client/server角色、受控工具委托。第三方结果是external-reported，不是独立副作用验证；地址、DNS、TLS及凭据限制见[公网详解](webagent-core/agent-host/src/mcp/公网出站详解.md)。

## 6. 大门钥匙与权限不是一回事

**人话**
本机连接密钥和OAuth令牌用于认证；Read/Edit/Execute/Capture是主人给Bridge的权限。Ask/Plan/Code又是工具执行模式，不能用Code抬高主人关闭的权限。Edit依赖Read；任意Execute能读写和截图，当前要求四项都允许。取得凭据不是无限越权许可，但仍应当作秘密妥善保管。

OAuth支持公共PKCE及机密客户端认证；具体连接器是否支持要逐客户端验证。授权状态在内存，主机重启须重新配对。密钥轮换保存失败必须明确失败，不虚报成功。GitHub用户名验证与MCP授权也不是同一个协议。

**比喻**
门卡证明能进门，工作许可决定能做什么；不能把“持卡”直接理解成保险柜、所有机器和后台管理权全部开放。

**落在仓库哪**
`src/mcp/oauth.js`、`src/config.js`、`src/models/store.js`、`src/utils/executionControl.js`；GitHub可选验证在`src/auth/github.js`。

**行业里管这叫什么**
认证与授权分离、能力凭证、PKCE。CORS只是浏览器来源规则，不是凭据验证；权限控制也不是OS安全沙箱。

## 7. 临时门牌与启动失败

**人话**
127.0.0.1是正在发请求的那台机器自己，云端不能用它指到你家。Quick Tunnel、Named Tunnel或ngrok可提供公网HTTPS路由。Quick/非预留域名可能变化；Named/预留域名还要配置相应服务。只让本机Chat访问本机host不需要入站隧道，但选择远程模型时Chat仍会联网发送模型请求。

**比喻**
公网门牌解决快递能不能到达，不证明快递员有门卡，更不证明工单已经完成。

**落在仓库哪**
`src/tunnel/cloudflared.js`、`ngrok.js`，`src/api/routes.js`的Bridge启动/停止，前端`bridge.js`。必须检查启动业务结果与实际公网地址，不把HTTP200当成功；失败不自动改投另一隧道。具体命令、25秒等待及Token填写见[隧道指南](隧道使用指南.md)。

**行业里管这叫什么**
反向隧道/代理，不是VPN或远程工作台。完成任务应停止Bridge；持有合法凭据的命令执行风险不会因为“只开几小时”自然消失。

## 8. 文件、命令、图片各自的边界

**人话**
工具目录按权限动态筛选，不在导读硬编码已经过时的“正好30个”。文件工具经过敏感路径、真实路径、预算和版本检查；命令是当前系统用户级执行，工作目录检查不限制命令全部访问范围。Windows工具命令走PowerShell，不因工具别名叫bash就自动有Bash环境。

**比喻**
文件工具像有尺寸规矩的专用扳手；任意Shell像交出一间工具房，不能说它被同一把文件锁完全限制。

**落在仓库哪**
`src/tools/index.js`、`normalize.js`、`patchEngine.js`、`sensitive.js`、`executor.js`；图片经`src/agent/computerUse.js`校验，Chat与MCP分别附图。PTY由核心扩展本机承接，远程send_command_input继续拒绝。

**行业里管这叫什么**
最小权限、路径约束、词法危险检测与资源预算。词法检测、环境过滤、Windows Job Object都不是完整OS沙箱。日志脱敏也不保证任意stdout绝无秘密。

## 9. 预览、回退、检查点为什么分三种

**人话**
经典单次保存回退针对一次已验证保存；原生VSCode草稿恢复针对未保存缓冲区；跨文件检查点则必须在任务前选择已有文件并暂存原文。三者都需要显式操作，不会在模型失败后自动回滚。

| 入口 | 恢复什么 | 不保证什么 |
|---|---|---|
| 经典文件菜单 | 最近有句柄的一次保存前正文 | 任意工具完整历史、持久redo |
| 原生命令面板 | 当前草稿到所审阅磁盘文本，可用原生撤销 | 已保存Agent改动、跨文件事务 |
| 工具接入页检查点 | 任务前选定的1–12个已有文件正文 | 删除/重命名/新文件/外部副作用、原子事务 |

**比喻**
撤销一笔、恢复草稿、按清单留底是三种档案；不能拿一张留底纸说整个工地都能倒带。

**落在仓库哪**
`src/utils/editorUndo.js`、`webagent-core/extension/editorReview.js`、`src/utils/fileCheckpoints.js`。检查点先全部预检，再逐文件hash保护写入；中途失败可能部分完成，保留逐文件结果并消费，不自动补偿。

**行业里管这叫什么**
乐观并发控制、有界内容快照、一次性恢复授权。每次hash匹配只证明当前内容，不检测ABA历史；原子替换单文件也不等于断电fsync或跨文件事务。操作见[使用指南](使用指南.md)。

## 10. 一次修测试任务怎样形成证据

**人话**
请求进入对应模式→认证/权限→读文件和hash→预览或提交修改→写入与读回→运行用户允许的测试→记录工具结果。工具错误不一定是网络断线；缺配置/模型服务失败必须停止，由用户决定后续模型，不自动改builtin或重放修改。STALE_FILE需要重读并协调内容，不能拿报错里的currentHash直接强行再写。

**比喻**
“师傅说修好了”“工具确实动了”“机器现在通过测试”是三份不同证据，不应只盖一个成功章。

**落在仓库哪**
`src/agent/runChat.js`/`openai.js`、`src/tools/patchEngine.js`、`src/utils/toolTrace.js`、`eventBus.js`。Bridge统计和Tasks从主机快照恢复，WS只是实时更新渠道；Agent的Tasks自报不等于工具执行或质量验证。

**行业里管这叫什么**
执行追踪、读回验证、结果分层。测试只覆盖声明场景；模型循环的工具失败反馈和模型API失败停止不能混为一条规则。

## 11. 为什么文件夹和运行数据要分开

**人话**
主机、前端、核心扩展、探针、安装器和测试各有职责。核心扩展不再“只打HTTP”：它也负责本地PTY和明确的原生草稿编辑；模型调度/共享工具仍在host。用户配置跟工作区，源码根本身作为工作区时`.webagent`自然就在根下。

**比喻**
工具箱、仪表板、诊断仪和工作记录分开存，不表示所有仪表只能显示不能操作，也不表示工作记录能随程序升级一起删掉。

**落在仓库哪**
[组件说明](组件说明.md)列现行目录；`src/models/store.js`保护配置与ignore，安装器使用Program Files程序载荷和用户可写runtime/config。Skills可按审阅后的共享策略提交；凭据和私人运行记录不应提交，ignore也挡不住已跟踪文件。

**行业里管这叫什么**
职责分离、可替换界面、用户数据生命周期。每份配置原子替换不代表四文件事务；第三方代码必须按实际许可证和分发方式审阅，不能笼统称GPL“传染整个仓库”。

## 12. 已实现、待验与延期怎样区分

**人话**
已有真实Chromium工作台回归、Windows/Ubuntu Node矩阵和安装器构建检查，不再把Playwright列为“故意不做”。但这些不证明真实桌面VSCode窗口、所有公网服务和用户账户已经通过。手机Arena连接按用户实测记录通过，不扩大成所有工具/网络条件通过。

持久登录、Chat API确切后台身份延期；经典/code-server探针入口按用户决定不施工，保留桌面探针。OCR、发布签名等采用候选与实际施工分开；不为复制别人的产品而悄悄申请密钥、安装扩展或扩大权限。

**比喻**
装好零件、台架测试、交到车主手上路试是三个阶段。不能把“测试台绿灯”写成车主亲自开过。

**落在仓库哪**
`webagent-core/agent-host/tests/`、`.github/workflows/`、[阶段10](manager/stages/s10-upstream-adoption.md)、[人工清单](review/CHECKLIST_WINDOWS.md)、[探针入口矩阵](探针入口与实际可用范围.md)。原三报告和精确失败证据按审查目录保留，不作为现行待办。

**行业里管这叫什么**
自动回归、分层验收、范围管理。全文逐句审查仍未覆盖全仓；清单/hash/AST关联不能替代语义复核。

## 13. 术语对照（先人话）

| 你听到的词 | 人话 |
|---|---|
| Agent | 能按自身策略调用工具的助手；本机builtin只是有限规则式探索 |
| MCP | 客户端向工具服务发标准请求的协议，不等于模型API |
| Bridge | 外部AI认证入站并使用本机工具的路线 |
| 出站MCP | 本机另作client，调用你登记的第三方工具服务 |
| 127.0.0.1 | 当前执行请求的机器自己，不是你电脑的公网地址 |
| 工作区 | 文件工具约束的项目根，不是任意命令的OS沙箱 |
| hash | 当前内容的指纹，不是永不变化的锁 |
| Ask / Plan / Code | 工具模式；不能越过Bridge所有者权限 |
| OAuth / PKCE | 授权码和令牌协议/兑换保护，不是永久登录保证 |
| WebSocket | 实时事件通道，不自动提供历史可靠重放 |
| Skill | 可按需读取的指导内容，不是自动安装/执行授权 |
| 检查点 | 显式选定文件的短期原文备份，不是全机时光倒流 |
| CORS | 浏览器跨站来源规则，不是身份认证 |

## 14. 读完这篇之后

按[使用指南](使用指南.md)操作；目录定位读[组件说明](组件说明.md)，函数学习读[技术实现](技术实现.md)、[总览](总览.md)及模块详解。隧道、Skills、原生扩展和安装器各有专门说明，不在多份导读重复维护易过期的步骤。

修改架构时直接修改对应小节，不能只在旧正文后追加相反的“最新说明”。
