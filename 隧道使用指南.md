# 隧道使用指南（Windows + CMD）

网页上的 AI 跑在别人机房里。它访问 `127.0.0.1` 时，连的是**机房那台机器自己**，不是你家电脑。要把「改你硬盘上的仓库」这件事交给网页 Agent，必须给本机 MCP 口（默认 **48271**）办一张 **https 公网门牌**。这张门牌就是隧道。

本机右侧 **CHAT 不需要隧道**。只有点 **启动 Bridge**、让云上客户端打进来时才要。

这份说明只写 **Windows + CMD**（不要用 Git Bash / WSL 当主路径）。点按钮之前，请先按 [使用指南.md](./使用指南.md) 用 `run-webagent.cmd` 把工作台跑起来。

---

## 1. 三种门牌，选一个

工作台：左下角齿轮 → **智能体自定义设置** → 左侧 **Bridge** → **隧道模式**。

| 模式 | 要不要账号 | 地址会不会变 | 要装的程序 | 适合 |
|---|---|---|---|---|
| **Cloudflare Quick Tunnel**（默认） | 不要 | **每次启动 Bridge 都变** | cloudflared | 先跑通、临时用 |
| **Cloudflare Named Tunnel** | 要 Cloudflare 账号，还要一个域名（可在 Cloudflare 买，或把已有域名 NS 指过去） | **固定** | cloudflared | 经常连、不想每次换地址 |
| **ngrok** | 要 ngrok 账号 | 不填域名则每次变；填了预留域名则固定 | ngrok | 已经在用 ngrok，或不想配 Cloudflare 域名 |

三种都会 `spawn` 本机程序，把请求转到 `http://127.0.0.1:48271`。同一时刻只跑一条：换模式再点启动，会先停掉上一条。

**不要**再开一个 CMD 自己跑 `cloudflared tunnel` 或 `ngrok http`，会和 Bridge 抢同一条隧道。

失败时按钮仍会成功（HTTP 200），toast 会说明原因，MCP 地址停在当前页面（例如 `http://127.0.0.1:3000/mcp/…`）。那种地址**只给本机预览**；云上 Agent 连过去改的是它自己那台机器。

Token / Authtoken **不要**发到聊天、Issue、截图。工作台 `/status` 和日志里不会带原文。

逐步把 MCP 地址交给 Arena / DeepSeek++ / Chat Plus / ChatGPT 自制插件，仍看 [使用指南.md](./使用指南.md) 第 6 节。ChatGPT **聊天栏贴链接不行**。

---

## 2. 公共准备（三种都要）

1. 装 [Node.js LTS](https://nodejs.org/)（勾选 npm）。
2. 仓库根新开 **CMD**：

```bat
cd /d D:\code\web_agent
check-env.cmd
run-webagent.cmd
```

把 `D:\code\web_agent` 换成你的路径。挂自己的仓库：`run-webagent.cmd D:\code\my-app`。

3. 浏览器打开 **http://127.0.0.1:3000**。这个黑色窗口要一直开着。
4. 齿轮 → **Bridge**。默认已是「本机演示授权」。若被关掉，点 **本机演示授权**（**不是**「使用 GitHub 登录」）。
5. 在「怎么连到本机仓库」里点选客户端（默认 Arena）。
6. 选一种隧道，按下面第 3 / 4 / 5 节填完，再点 **启动 Bridge**。最多等约 25 秒。
7. 成功后 MCP 地址变成 `https://……/mcp/一串密钥`。按客户端复制提示词或只复制这一行 URL。

第一次启动 Node，Windows 防火墙可能问「是否允许访问网络」。**专用网络请勾选允许**。

---

## 3. Cloudflare Quick Tunnel（零配置）

不需要 Cloudflare 账号，也不需要域名。每次启动都会拿到一个新的 `*.trycloudflare.com`。

### 3.1 安装 cloudflared

管理员 CMD：

```bat
winget install --id Cloudflare.cloudflared
```

装完**关掉旧 CMD，新开一个**，再：

```bat
cd /d D:\code\web_agent
check-env.cmd
```

应看到 `cloudflared 已在 PATH`。

winget 不可用时：到 https://github.com/cloudflare/cloudflared/releases 下载 `cloudflared-windows-amd64.exe`，改名为 `cloudflared.exe`，放到已在 PATH 的目录（例如 `C:\Windows\System32`），或启动前：

```bat
set CLOUDFLARED_PATH=C:\工具\cloudflared.exe
```

### 3.2 在工作台启动

1. 隧道模式保持 **Cloudflare Quick Tunnel（无需账号或域名）**。
2. 点 **启动 Bridge**。
3. 成功 toast 类似「Quick Tunnel 已就绪：https://随机词.trycloudflare.com」。
4. MCP 地址形如：

   `https://随机词.trycloudflare.com/mcp/一串密钥`

源码实际执行的是：

```text
cloudflared tunnel --url http://127.0.0.1:48271 --no-autoupdate
```

从日志里抠 `*.trycloudflare.com`。你不必在 CMD 里手打这一行。

### 3.3 每次重启

域名**每次点「启动 Bridge」都会变**。必须重新复制，并在网页 Agent / DeepSeek++ / Chat Plus 里换成新地址。旧地址作废。

---

## 4. Cloudflare Named Tunnel（固定域名）

要：Cloudflare 账号 + 已经放在该账号下的域名 + 本机 cloudflared（安装同第 3.1 节）。

仪表盘菜单偶尔会改名，认这几个词即可：**Zero Trust / 网络 / 隧道 / Public Hostname / Tunnel Token**。

### 4.1 在浏览器里建隧道（只做一次）

1. 打开 https://one.dash.cloudflare.com 并登录。
2. 进入 **Zero Trust**（有的账号要先免费开通一次 Zero Trust）。
3. 左侧 **网络（Networks）→ 隧道（Tunnels）**。
4. **创建隧道**。类型选 **Cloudflared**（不要选 WARP）。
5. 起一个名字，例如 `web-agent`，保存。
6. 到「安装连接器」一步时，页面会给出一长串 **Token**（常以 `eyJ` 开头）。  
   - **只复制 Token**，贴到记事本先留着。  
   - **不要**在 Windows 上再执行页面里的 `cloudflared.exe service install …`。Web Agent 会自己 `spawn cloudflared tunnel run --token`。本机再装一份 Windows 服务，会和 Bridge 抢同一条隧道。
7. 下一步 **Public Hostname**（公共主机名）：
   - **子域**：例如 `mcp`
   - **域名**：选你在 Cloudflare 里的那个域，例如 `example.com`
   - **类型**：`HTTP`
   - **URL**：必须填 **`http://127.0.0.1:48271`**（端口以启动窗口打印的 MCP 端口为准；改过 `AGENT_HOST_PORT` 就填那个数）
8. 保存。完整主机名就是 `mcp.example.com`。

源码**不会**把主机名传给 cloudflared。主机名只写在 Cloudflare 仪表盘里。本机进程只拿 Token 去登记。

### 4.2 在工作台填并启动

1. 隧道模式选 **Cloudflare Named Tunnel**。
2. 第一框填主机名，例如 `mcp.example.com`（可以带 `https://`，程序会去掉）。
3. 第二框把 Tunnel Token 贴进去。
4. 点 **启动 Bridge**。
5. 成功 toast：「Named Tunnel 已就绪：https://mcp.example.com」。
6. MCP 地址：

   `https://mcp.example.com/mcp/一串密钥`

缺主机名或 Token 时 toast 会写明，**不会**偷偷改走 Quick Tunnel。

Token 会写入工作区 `.webagent\config.json`（已 gitignore）。下次启动只要主机名还在，可以不重贴 Token。不要把这份文件提交到 Git。

源码实际执行的是：

```text
cloudflared tunnel --no-autoupdate run --token <你的 Token>
```

就绪看日志里的 `Registered tunnel connection`（大约 25 秒）。你不必在 CMD 里手打。

### 4.3 改端口或换电脑

Public Hostname 的 URL 必须仍是本机 MCP 口。换了 `AGENT_HOST_PORT`，回到 Cloudflare 把 HTTP URL 改成新端口。换电脑：Token 仍可用，只要这台电脑能上网、cloudflared 在 PATH。

---

## 5. ngrok

要 ngrok 账号。免费套餐一般就能把 48271 映成 `https://….ngrok-free.app`。固定域名需要在控制台预留（各套餐规则以 ngrok 网站为准）。

### 5.1 安装 ngrok

管理员 CMD：

```bat
winget install Ngrok.Ngrok
```

装完**新开 CMD**，再 `check-env.cmd`，应看到 `ngrok 已在 PATH`。

或从 https://ngrok.com/download 下载 `ngrok.exe` 放到 PATH，或：

```bat
set NGROK_PATH=C:\工具\ngrok.exe
```

不要用 npm 里的 `ngrok` 包装包当产品路径。Web Agent 找的是 PATH 上的 **ngrok 二进制**。

### 5.2 拿到 Authtoken

1. 打开 https://dashboard.ngrok.com 注册/登录。
2. 打开 https://dashboard.ngrok.com/get-started/your-authtoken 复制 Authtoken。
3. 也可以在本机 CMD 先写入 ngrok 自己的配置（可选，不是必须）：

```bat
ngrok config add-authtoken 你的Authtoken
```

工作台仍建议把同一串贴进密码框。不贴的话，程序会再看环境变量 `NGROK_AUTHTOKEN`。两者都空，toast 会要你填 Authtoken，不会改走 Quick Tunnel。

### 5.3 可选：固定域名

在 https://dashboard.ngrok.com/domains 预留一个域名，例如 `yourname.ngrok-free.app` 或付费域 `xxx.ngrok.dev`。工作台「可选」框填这个主机名。留空则每次随机。

### 5.4 在工作台填并启动

1. 隧道模式选 **ngrok**。
2. 第一框：可留空，或填预留主机名（如 `mcp.ngrok-free.app`）。
3. 第二框：Authtoken。
4. 点 **启动 Bridge**。
5. 成功 toast：「ngrok 已就绪：https://……」。
6. MCP 地址：

   `https://……/mcp/一串密钥`

源码实际执行的是（Authtoken 走环境变量，**不会**出现在命令行参数或 `/status` 里）：

```text
ngrok http 127.0.0.1:48271 --log=stdout --log-format=term
```

若填了域名，会再加 `--url https://你的主机名`。公网 URL 从日志的 `started tunnel` / `Forwarding` / `"url"` 解析。你不必另开 CMD 手打。

随机 ngrok 地址和 Quick Tunnel 一样，**每次启动可能变**，要重新复制。预留域名则固定。

---

## 6. 启动成功之后

把 MCP 地址交给网页端，规则与 [使用指南.md](./使用指南.md) 第 6 节相同：

| 客户端 | 复制什么 |
|---|---|
| Arena 等「贴 URL」 | **复制提示词**（两段：地址 + 那句连接说明），整段当新对话第一句 |
| DeepSeek 网页 | 只复制那一行 `https://…/mcp/…`，填进 DeepSeek++ 侧边栏，传输选 Streamable HTTP。见 [网页DeepSeek使用指南.md](./网页DeepSeek使用指南.md) |
| Chat Plus | 同样只填那一行 URL。见 [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md) |
| ChatGPT 自制 MCP 插件 | 服务器 URL 填 **规范地址**（`https://…/mcp`，**不要**带密钥），身份验证 OAuth，配对码在 Bridge 页。不要把地址贴进聊天栏 |

回到工作台右侧 **BRIDGE**，应能看到工具调用。磁盘上的文件会变。

隧道只转发 **MCP 工单**到 48271。带 Cloudflare / 公网 Host 的人打 `/api/status`、`/api/chat` 会看到「没有这个地址」（404），拿不到密钥。本机右侧 Chat 走 3000，不经过隧道。

---

## 7. CMD 备忘

在仓库根（能看见 `run-webagent.cmd` 的那一层）：

```bat
check-env.cmd
winget install --id Cloudflare.cloudflared
winget install Ngrok.Ngrok
set CLOUDFLARED_PATH=C:\工具\cloudflared.exe
set NGROK_PATH=C:\工具\ngrok.exe
set NGROK_AUTHTOKEN=你的Authtoken
run-webagent.cmd
run-webagent.cmd D:\code\my-app
```

装完任何程序都要**新开 CMD**，旧窗口的 PATH 不会自动更新。

---

## 8. 故障排除

**check-env 说未安装，但刚才 winget 成功了**  
没新开 CMD。关掉黑色窗口，新开后再 `check-env.cmd`、`run-webagent.cmd`。

**启动 Bridge 报未找到 cloudflared / ngrok**  
按第 3.1 / 5.1 节安装，或设 `CLOUDFLARED_PATH` / `NGROK_PATH` 指向 exe。

**25 秒没有 trycloudflare.com**  
外网不通，或公司网拦隧道。看 toast。MCP 会停在当前页面源，云上连不上你家。

**Named Tunnel 超时**  
- Token 复制不完整（少了字符、多了空格）  
- Cloudflare 里 Public Hostname 不是 `http://127.0.0.1:48271`  
- 本机没上网，或 Token 属于另一条隧道  
toast 会说明，不会改成 Quick Tunnel。

**ngrok 要 Authtoken / 退出 code 非 0**  
Token 错、账号限制、或预留域名不属于这个账号。免费域常见 `*.ngrok-free.app`。不要在另一个窗口同时跑 `ngrok http`。

**MCP 仍是 127.0.0.1**  
隧道没就绪。看 toast，不要把本机地址发给云上 Agent。

**网页 Agent 没改到文件**  
地址必须是 `https://` 公网门牌，不是 localhost。ChatGPT 聊天栏贴链接任何档位都不行。Quick / 随机 ngrok 重启后要换新地址。确认启动窗口打印的 `Workspace` 是你以为的那个仓库。

**不要做的事**

- 不要把 `…/mcp/密钥` 发到群里。拿到的人可以改你的文件、在你电脑上跑命令。
- 不要为了「手机连电脑」去设 `WEBAGENT_BIND=0.0.0.0`，除非你清楚局域网里谁都能碰到这些端口。
- 不要把 Named Token / ngrok Authtoken 贴进聊天或 README。
- 不要在本机再装 cloudflared Windows 服务，也不要另开 ngrok 窗口。

---

## 9. 和源码的对应关系（给改本仓库的人）

| 现象 | 文件 |
|---|---|
| 点启动后拉 Quick / Named | `webagent-core/agent-host/src/api/routes.js` → `src/tunnel/cloudflared.js` |
| 点启动后拉 ngrok | 同上 → `src/tunnel/ngrok.js` |
| 工作台三个 radio | `webagent-core/workbench/index.html`、`js/bridge.js` |
| 公网 Host 打 `/api` 得 404 | `src/utils/localControl.js` |

按键总流程仍以 [使用指南.md](./使用指南.md) 为准。本文件只把三种隧道写细。
