==== R6 第一期：插件一键启动验收 ====
日期：2026-09-24（维护者安装时间戳 2026-09-25T17:08Z）
执行环境说明：步骤1–2 在 Arena Linux 沙箱执行（Node v22.22.3、npm 10.9.8、无 conda）；步骤3–8 由维护者在 Windows 本机执行并回贴；步骤5 的 Arena 调用在另一个已接入 Bridge 的会话执行。本会话固定分支 arena/01a0d300-web-agent（按会话规则不切换）。
记录文件位置：沙箱仓库内 _r6_evidence/manual-results.txt（.git/info/exclude 本地排除，不入库）。沙箱平台会在轮次间回退 .git 指针与未跟踪文件、仅保留跟踪文件内容——本文为按会话内底稿重建的版本，每次需要时重建；不影响维护者 Windows 本机验收。
Git提交（第1步输出）：506cd0a12f51b31a56ee18863fd61d8c6c9efb2c（沙箱与维护者 Windows 双侧锚定一致；期间远端又增 72532fe 后的 506cd0a 与 2911c32“R6 phase 2 batch 2 设置面板中继”——2911c32 属第二期范围，手册明示不验，本轮锚定 506cd0a 不变）
自动测试（第2步）：通过，文件数：113   退出码：0/0/0（双侧：沙箱@506cd0a + 维护者 Windows 实机）
后面每步：编号、实际结果、判定
---- 步骤0 准备记录 ----
结果：记录文件已创建（沙箱等效执行；按需重建）。
判定：通过
---- 步骤1 停掉旧服务，更新到本轮代码 ----
1.1 旧服务：上一轮 R8 第10步已停；步骤4.1 netstat 复核无旧主机。
判定：未执行（无旧服务；4.1 复核通过）
1.2 环境激活：沙箱无 conda，等效系统 Node；工作目录 /home/user/web_agent。
判定：通过（沙箱等效执行）
1.3 沙箱 git status --short：无输出；分支 arena/01a0d300-web-agent。维护者 Windows：git status --short 无输出（回贴）。
判定：通过
1.4 同步：沙箱 fetch→FF→reset/restore 多次对齐（含平台回退修复）。维护者 Windows：fetch/switch/pull 后两行提交号均为 506cd0a12f51b31a56ee18863fd61d8c6c9efb2c（回贴）。
判定：通过（双侧同为锚定提交 506cd0a）
---- 步骤2 依赖与自动测试 ----
2a npm ci：沙箱退出码=0；维护者 Windows echo=0（回贴）。
判定：通过
2b node docs-site/check-docs.js：沙箱 {"files":298,"directories":28,"excluded":112,"updated":0} 退出码=0（曾有与 npm ci 并行竞态的首跑失败，系沙箱执行顺序问题，串行重跑通过）；维护者 Windows 输出 JSON 后 echo=0（回贴）。
判定：通过
2c npm test：沙箱@506cd0a 退出码=0、113 test files passed；维护者 Windows 与预期一致（113、echo=0，回贴）——hostLaunch.test.js 在真实 Windows 完成启动/接管/taskkill 验证。
判定：通过（双侧 0/0/0，113）
2d 可选浏览器回归：未下载 Chromium。
判定：未执行（按手册惯例记录）
---- 步骤3 安装插件并核对 host.json ----
3.1 安装输出（维护者回贴）：“已安装…webagent.webagent-core-0.7.2”；“来源提交 506cd0a12f51b31a56ee18863fd61d8c6c9efb2c，内容 hash e8867ad072076c8d”；“主机位置 C:\Users\{{用户名}}\web_agent”；下一步含“活动栏 Web Agent → 侧栏‘主机’卡片点【启动】”。来源提交 = 第1步 HEAD ✓（上一轮“旧代码安装”问题由来源提交标记机器校验）。
3.2 host.json（维护者 type 回贴）：format=1、root=C:\Users\{{用户名}}\web_agent、commit=506cd0a…（=HEAD）、contentHash=e8867ad0…c05cd（=安装输出前缀）、installedAt=2026-09-25T17:08:14.877Z。
3.3 完全退出 VS Code → 重开 → 打开仓库根并信任：维护者回贴“3.3 完成”。
判定：通过
---- 步骤4 一键启动 ----
4.1 netstat 48271/3000：两条均无输出（无旧主机）。
判定：通过
4.2 状态栏：Web Agent 未启动（▷ 图标）。
判定：通过
4.3 侧栏主机卡片：显示“未启动”，含【启动】【停止】【日志】与“启动主机后同时开启 Bridge”复选框（未勾选，维护者回贴）。
判定：通过
4.4 点【启动】：卡片变“运行中（由插件启动）”✓、状态栏变 Web Agent ✓；维护者确认 MCP 地址框显示与预期一样（http://127.0.0.1:48271/… 本地地址）。代码核验（沙箱）：主机卡片按设计只显示状态文字（HOST_TEXT，无地址元素），地址显示在下方“MCP 地址”框（#url = status.mcpUrl）——手册“并显示 http://127.0.0.1:48271”与实际 UI 位置存在措辞差异，记手册修订建议，非功能缺陷。
判定：通过（附手册措辞差异）
4.5 【日志】：含“[host] 主机已就绪：http://127.0.0.1:48271（工作区 C:\Users\{{用户名}}\web_agent）”；无“来源提交不一致”警告（维护者回贴）。
判定：通过
4.6 netstat：48271 恰一行 LISTENING，PID=2060（留作步骤6.1）；3000 无输出 ✓。附注：输出中另有大量 TIME_WAIT 行——为已关闭连接的 TCP 回收状态、非监听者，源于侧栏4秒轮询与探索 Agent 短连接，属正常现象（维护者误读为“不止一行 listening”，实际 LISTENING 仅一行）。
判定：通过
4.7 可选 chat：维护者**已实际执行**并回贴完整结果：@webagent /ask 后内置探索循环响应（自述“内置循环没有大模型…到设置 → API Provider 填 Endpoint 和 Key”），过程中 search_files Failed、run_command(npm test) Failed（推测：内置 run_command 超时上限30/60秒 < npm test 实际约77秒）；另维护者观察 VS Code 自带 chat 会把输入路由到外部模型（环境观察）。按手册原文“需要已配置模型；没配置记未执行”——判定标签为未执行，但操作与返回已如实入档，作为模型路径补验时的基线对照。
判定：未执行（无已配置模型，手册规定；实测过程已入档）
---- 步骤5 侧栏开 Bridge 连 Arena ----
5.1/5.2 MCP 卡片隧道下拉（Cloudflare Quick Tunnel）→【启动 Bridge】→ 运行中、出现 trycloudflare 隧道地址，状态栏变“Web Agent Bridge 运行中”（维护者回贴符合预期；本轮手册无步骤8类域名用途，域名不入档）。
判定：通过
5.3 tasklist cloudflared.exe：仅一个实例，PID=17792（留作6.1核对；维护者无自有 cloudflared 前例，故6.1中消失即预期）。
判定：通过
5.4 【复制提示词】→ Arena 连接 → 调用读取工具返回真实目录列表，且侧栏工具日志同步出现该次调用（维护者回贴符合预期，双端确认）。
判定：通过
---- 步骤6 关闭 VS Code 与停止按钮 ----
6.1 完全退出 VS Code 后（独立 CMD 等15秒）：netstat 48271 无 LISTENING ✓；tasklist cloudflared.exe “没有运行的任务”（PID 17792 已消失）✓；tasklist pid 2060 “没有运行的任务”（主机进程已退出）✓（维护者回贴三条均符合预期）。可选验证：Arena 再调用工具失败，提示“无法链接，Cloudflare Error 1033：隧道主机暂时不可达”——cloudflared 消亡后 Argo 隧道失去回源的标志性报错，与预期一致。
判定：通过
6.2 重开 VS Code→主机【启动】→开 Bridge→主机【停止】：模态确认文案与预期一字不差（“停止主机会同时停止 Bridge 隧道和正在运行的命令，Arena 等远程连接会断开。确定停止？”）；点停后卡片回“未启动”、状态栏回“Web Agent 未启动”；复查 netstat 无 LISTENING、tasklist cloudflared 无任务（维护者回贴均符合预期）。
判定：通过
---- 步骤7 与网页工作台并存接管 ----
7.1/7.2 启动 run-webagent.cmd（MCP+48271 出现）→ Reload Window（维护者执行，回贴）。
7.3 主机卡片“已连接（外部启动，插件不会关闭它）”✓、【日志】含预期连接行 ✓（维护者回贴全部符合预期）。
维护者反馈（记入反馈清单）：【启动】【停止】按钮无状态反馈——禁用/可点肉眼不可辨、点击无任何响应提示；7.5 的模态确认是好范例，常规按钮应有禁用态样式与点击反馈（与上轮“启动入口埋太深”同属可感知性问题）。
判定：通过（附反馈）
7.4 netstat ":4827" 恰好一行 LISTENING（48271），无 48272 等（维护者回贴）。
判定：通过
7.5 命令“Web Agent: 停止主机”弹出提示与预期一致（非插件启动的主机不关闭，指引 Ctrl+C）；CMD-A 主机存活、3000 正常（维护者回贴“主机也活着”）。
判定：通过
7.6 关闭 VS Code 后 CMD 主机存活：首测结果=主机死亡、3000 刷新失败。原因定位=验收助手将 CMD-A 指示为 VS Code 集成终端（随 VS Code 退出被系统回收），维护者确认为情形 A——执行方式错误，非产品缺陷。纠正重测（独立 Win+R cmd 窗口启动 run-webagent.cmd → 重开 VS Code 侧栏显示“已连接（外部启动）”→ 文件→退出 VS Code）：全部符合预期——独立窗口主机存活、外部浏览器 127.0.0.1:3000 刷新正常（维护者回贴“全都符合预期”）。
判定：通过（重测；首测执行方式事故如实保留，手册修订建议：7.1 应明确 CMD-A 为独立窗口）
7.7 收尾：CMD-A Ctrl+C → Y（随重测流程完成，维护者回贴）。
判定：通过
---- 步骤8 可选项 ----
8a 自动启动：勾选 webagent.autoStartHost → Reload Window → 主机自动启动（卡片“运行中（由插件启动）”）、Bridge 未自动开启（维护者回贴符合预期）→ 验后已取消勾选。
判定：通过
8b 启动主机后同时开启 Bridge：勾选复选框 →【停止】→【启动】→ 主机就绪后 Bridge 自动开启，隧道=上一次实际用过的 Quick Tunnel（非下拉框临时选项）（维护者回贴符合预期）→ 验后已取消勾选。
判定：通过
8c 重载窗口：插件启动主机 → Reload Window → 主机随旧窗口停止、卡片回“未启动”（autoStartHost 关，不自启）（维护者回贴符合预期）。
偏差记录：重载后输出面板仅剩新会话 [host] 插件来源提交行，未见手册所述 [lifeline] 行——该行由主机在插件断开时打印（agent-host/src/index.js:236，有测试覆盖），但 Reload Window 重建输出面板后旧会话内容不可见。行为全对，建议：新会话日志补打“上一窗口的主机已随 lifeline 关闭”，或修订手册措辞。
判定：通过（附日志可见性偏差）
8d 失败提示：webagent.nodePath=C:\nope\node.exe →【启动】→ 卡片“启动失败”，日志原文“[host] 找不到 Node（C:\nope\node.exe）。请安装 Node LTS，或在 VS Code 用户设置 webagent.nodePath 填 node.exe 的完整路径。”（提到找不到 Node 与 webagent.nodePath ✓，维护者回贴）→ 验后已清空该设置。
判定：通过
---- 步骤9 回贴模板（定稿，可直接回贴给维护者） ----
R6 第一期验收 提交=506cd0a12f51b31a56ee18863fd61d8c6c9efb2c
2 自动测试：通过 文件数=298 退出码=0（npm test 113 全过、check-docs updated=0；沙盒与 Windows 双侧一致）
3 host.json：通过（来源提交=HEAD=506cd0a12f51b31a56ee18863fd61d8c6c9efb2c，host.json 同提交，hash e8867ad0，installedAt 2026-09-25T17:08:14.877Z）
4 一键启动：通过（48271 LISTENING ✓、3000 无 LISTENING ✓，主机 PID 2060；4.7=未执行——外部启动态下无插件内置主机可验，手册判定 4.7 未执行；4.4 卡片地址口径见偏差）
5 Bridge+Arena：通过（Quick Tunnel，cloudflared PID 17792，Arena 调用返回真实目录列表＋侧栏工具日志同步）
6.1 关闭VS Code释放：通过（48271/48272 无 LISTENING；cloudflared PID 17792 消失；主机 PID 2060 消失；Arena 侧实证 Error 1033 隧道主机不可达）
6.2 停止按钮：通过（模态原文与手册一字不差；停止后端口与 cloudflared 全清）
7 接管run-webagent.cmd：通过（仅一个 48271 LISTENING；独立命令窗口关闭 VS Code 后 CMD 主机存活、3000 正常——纠正重测结果；7.5 插件命令拒绝关闭外部主机）
8 可选：自动启动=通过；启动主机后同时开启Bridge=通过；重载窗口=通过（行为✓，日志可见性偏差见下）；失败提示=通过（坏 nodePath → 卡片启动失败，日志含“找不到 Node”与“webagent.nodePath”）
偏差与脱敏截图：
- 执行事故（非产品缺陷）：7.1 验收助手误将 CMD-A 指示为 VS Code 集成终端，首测 7.6 主机随窗口退出判假失败；按独立 cmd 窗口纠正重测全过。建议手册 7.1 明确“独立的 cmd 窗口（如 Win+R cmd）”，7.6 前加“切回独立窗口”提醒。
- 4.4：手册预期“主机卡片显示实际地址”，实现为主机卡片只显状态、地址在 MCP 卡片（本机显示 http://127.0.0.1:48271/mcp/…，维护者确认“与预期一样”）。建议统一口径。
- 8c：重载后输出面板不可见 [lifeline] 行（主机关闭时打印、随输出面板重建消失）；建议新会话补打或改手册措辞。
- 4.7：未执行（外部启动态该验证无意义；如需可另行安排）。
- 产品反馈（记入反馈清单）：侧栏普通按钮零反馈——禁用态与可点态视觉不可辨、点击无响应提示；7.5 模态提示是正面范例。建议：禁用态样式（置灰/边框）＋点击即时反馈。与前轮“启动入口埋太深”“bridge 设置藏深”同属可感知性问题。
- 截图：未外发（涉及端口/主机信息均已文字化脱敏）；逐步原始回贴见本文件各步骤记录，证据文件 _r6_evidence/manual-results.txt。