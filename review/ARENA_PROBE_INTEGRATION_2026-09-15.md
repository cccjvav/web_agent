# 用户模型探针的接入审阅与边界

来源：用户提交73b46ae，分支arena/01a08d85-web-agent；快进合入，无改分支。144个新增文件是上传规模，不是完整审计结论。

## 已检查范围

读取原型README前110行、package.json、main.js前115行、runmodel.js前90行，以及目录结构；recon只做模式计数，不输出匹配内容。未运行原型npm脚本、Python、CDP、令牌探测、主动提问、后台轨迹API或上游网络请求。原型分类器、网络钩子完整实现及所有Python脚本仍未深审。

已确认的接入障碍：
- README把请求model、响应字段等赋固定权重，且有“零侵入”“真实模型”等承诺；这不是经校准的身份可信度，不能升级成本机认证依据。
- main会装钩子、请求模型映射、启用运行解析；runmodel持有访问令牌并拟查询运行轨迹。不能当成一个无副作用纯函数直接随工作台启动。
- recon 103个文件中两个含JWT形态字符串、31个含登录/令牌相关字段（可能也含源码文字，不代表31份有效凭据）。没有验证或使用凭据。移出跟踪保留本地和历史；需要用户自行按平台能力处理仍有效凭据。
- 下载的网站bundle及原始抓包不纳入产品源码文档快照/安装器。研究原型整目录从产品语义覆盖统计排除，明确不是全审通过。

## 已实现的适配层

新增独立用户脚本webagent-connection.user.js：仅用户点击后对pathname做SHA-256并输出五字段JSON；不拦截网络、不读登录存储、不询问模型、不自动发送、无CDP依赖。schema固定为webagent-browser-observation/v1，不读原型dump。

本机connectionCheck模块：严格字段/字节/来源/时间校验，2分钟随机挑战、最多8项、消费一次、返回摘要hash和主机身份，仅已认证且初始化的远程MCP会话可确认。单独本机API创建/查询/清除；跨站/非本机沿用现有拒绝策略。UI没有直接确认按钮，必须经外部MCP工具。

信任结论：browser-reported + authenticated-session-echo；不是平台签名、浏览器证明、模型身份或账户身份。传递挑战的操作者必须自己选对会话；任何持有挑战且有有效认证的会话都可能先回传，不提供抗转交证明。成功不放宽执行权限。

## 用户复验记录

本次用户明确反馈：刷新后Bridge计数与工具调用记录仍在。仅此问题记为用户实机复验通过，不代签11.3或新增连接核对。

## 验证范围

connectionCheck隔离测试涵盖原始额外字段/错误来源/过期拒绝、会话限制、一次消费、到期回收、八项上限、清除、返回数据隔离，以及新用户脚本不自动采集/不联网/不导出查询令牌。浏览器回归添加本机导入→已认证MCP回传→本机查询与清除、跨站拒绝。它不访问真实Arena网页，也不证明真实浏览器扩展兼容；最终结果以对应提交CI为准。

## 中断后续审
用户明确允许审查原型bug。新增采集层完整阅读、Response兼容/读取预算/XHR复用/socket构造器修复和离线probeTransport回归；详见arena-model-probe/TRANSPORT_REVIEW.md。原始模型鉴定、真实站点兼容、整个main销毁链仍未认证。上一接入浏览器回归失败定位为测试误把本机跨站拒绝断言为403；现有rejectCrossSiteApi按安全策略返回404且不执行，改断言为精确404/not found，未放宽服务端。

## 已验证的精确提交

- b5d7fab3da3ffa58d34d2baf68d0865c84da7e66：本机61个测试文件通过；GitHub Actions https://github.com/cccjvav/web_agent/actions/runs/34974399282 全9任务成功，包括真实浏览器连接核对/跨站拒绝/已初始化MCP回传、七个Linux/Windows Node组合和Windows安装器。
- 浏览器原失败0ad3693的annotation明确指向workbench.browser.js:253，404 !== 403，修正的是测试契约而不是放宽接口。
- 后续仅补充未核验标签/API显式标志及记录，也需按后续提交CI状态区分，不冒用上面的SHA结果。
- 仍需用户真实Windows11.3及第三方包验收；上面不是实际账号/模型身份或整份原型全审证明。

## 原型生命周期续修

上一提交8f12e24的全部9项CI也已通过（https://github.com/cccjvav/web_agent/actions/runs/34974802592）。本轮继续修复BUS可退订/异步拒绝隔离、HUD拖动清理、重复启动预约与拒绝旧版热叠加，详见原型TRANSPORT_REVIEW。HUD关闭不等于全局停止；未测试真实账号，也未改动原型轮询读取能力。

## VS Code配套扩展首版

用户授权做可并装的VS Code版本。新增webagent-core/probe-extension独立ID与VSIX白名单构建，复用既有本机HTTP核对接口和WebAgent Bridge命令，不新增host工具/执行权限，不自动启动浏览器原型。模型推断、原始流量、令牌和后台轨迹未移植。验证分为隔离HTTP/VSCode API桩、VSIX包结构、真实桌面人工验收；最后一项仍待用户执行。原型的SSE多行/证据混用等未完成项仍保留。

首版91337db的本机62文件测试和全部9项CI通过（https://github.com/cccjvav/web_agent/actions/runs/34978616519），含Windows独立VSIX构建/ZIP校验。后续补充忙状态防重复、配置变化绑定旧地址、主机重启丢弃记录、过期不请求、卸载取消在途请求测试；失败清空旧显示，避免把旧成功误当新结论。真实VS Code安装/API及两扩展同时运行仍待桌面验收。

## 对能力损失的明确纠正

用户指出未移植功能是否损害探测能力：答案是会。当前WebAgent连接版和VS Code版未实现原型模型线索探测；原型有界采样也可能漏掉线索，不能称等效迁移或无损优化。新增根目录《探针能力对照与迁移边界》，区分代码保留、产品接入和未验证能力。同步修复完成观测重复计数/绕过BUS上限/追加晚于分类快照的问题；不执行真实账号或行为探针题库。

4c8768d CI34980700912为8/9通过：仅Windows/Node20的旧mcpProtocol截图echo在第157行isError=true失败，缺少工具结果尚不能判定原因。补充有界/脱敏诊断后继续验证，不将它宣称全绿或推测为某项已修根因。

## 用户确认完整参考能力与优先级（本轮替代缩减范围决定）

用户明确需要完整原型能力作为参考，不要求绝对真实；允许修改文档决定。故不再把“不认证模型身份”当作排除采集/分析/映射/自动化的理由。仍遵守授权访问、隐私和未核验提示。Chat模式API模型确切验证排在完整整合之后。

0.2.0开始实际复用原型五个模块：Worker中调用SSETap/classify/protocolFingerprint/runCanaries/parseCodename/fingerprintVector/matchTokenizer，按单次文件隔离证据与映射。未调用浏览器钩子安装/main/CDP/后台轮询，未访问真实账号或向模型发送题库。它是实际分析移植的一步，不是完整目标完成。完整待办见根目录《探针完整整合实施与验收》。
