user: 好的，已经重新连接。你说的新规范适配，有必要吗，工作量大吗，对仓库改动多大？
assistent: 先推送上次没推上去的提交，再回答你的问题。

已推送 `1ae9975`。我一边等它的 CI，一边查新规范对服务器到底有哪些要求、你用的客户端现在支持哪一版，并统计仓库里有多少代码依赖会话。

Now a key fact check: what does a modern-only client (like the new OpenAI tunnel client from the search results) actually see from this host today? I'll test it directly against the real host:

Before answering, I'll check one more claim against the spec. How our host negotiates versions is part of the adaptation question.

`1ae9975` 已经推送，CI 9 个 job 全部通过（含 Windows）。

直接回答：**新规范适配现在不是必须的。工作量中等，大约要分 2–3 批做。** 推荐做成新旧两代并存：同一个 `/mcp` 地址按版本号分流，旧协议路径完全不动。

**有没有必要**
- 旧版本协议继续有效，被弃用的功能至少还保留 12 个月 [2](https://stacktr.ee/blog/mcp-2026-spec-changes)。
- 官方 SDK 默认仍然用旧协议，新协议要显式开启 [4](https://betterstack.com/community/guides/ai/mcp-stateless/)。SDK 的默认模式和自动模式我都实测过，能连上本主机。
- Arena 在新规范发布之后（9 月 16 日）已经实测能连。
- 但已经出现只讲新规范的客户端。比如 OpenAI 的 Secure MCP Tunnel，它连接时先问服务器支持哪些版本（`server/discover`），旧服务器回 400 就发现不了工具 [1](https://github.com/54yyyu/zotero-mcp/issues/573)。本主机现在对这个请求正是回 400；官方 SDK 固定用新规范时也连不上，我都实测过。
- 结论：等你要用的某个客户端只支持新规范时，再做不迟。

**工作量和改动面（估算）**
- 协议层的改动比较机械：
  - 新增 `server/discover`；
  - 每个请求自带版本号，并校验请求头和正文一致；
  - 给返回结果补上规范要求的新字段；
  - 新协议下不再发会话 ID。
- 难点在身份识别。现在有 6 个工具（任务板 3 个、外部工具审批、工作流审批、连接确认）要求先建立会话，靠会话区分不同对话。新规范没有会话，只能按登录凭据区分，同一个凭据开的多个对话会被当成同一个调用者。要继续按对话区分，得改成由服务器发放标识、每次调用时传回来，这部分设计量最大。
- 规模：约 8–10 个源码文件，400–700 行代码加测试，约 8 份文档。旧协议的测试基本不动，只新增新协议的测试。
- 不建议改成只支持新规范：那样 SDK 1.x 这类主流旧客户端就连不上了。

**评估时发现的一个现有问题（还没修）**
- 不带会话的远程调用，是按"客户端名@来源 IP"识别调用者的。隧道进程从本机连进来，来源 IP 永远是 127.0.0.1，所以两个不同的凭据会被当成同一个人。
- 我用两个不同凭据在真实主机上复现了：OAuth 客户端 B 查询命令输出，拿到的是用 URL 密钥的 A 的输出；B 查日志也能看到 A 的调用。这和文档写的"远程调用只能看到自己的"不符。
- 实际风险低：两个凭据都是你本机授权的，本来都能执行命令；正常先建立会话的客户端不受影响。
- 但适配新规范后，所有调用都没有会话，这条就成了主路径，所以应该先修，大约 1 批。

评估结论和这个问题都已记进 s10 和 CONTEXT，提交 `4a868ae` 的 CI 也是 9 个 job 全部通过。

我建议的顺序是：先修这个调用者识别问题，然后你本地用 Arena 验收第七批的改动（ping 和旧版 SSE 的处理改了），适配等真正需要时再做。按这个顺序可以吗？