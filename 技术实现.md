# 代码级技术实现

[组件说明.md](./组件说明.md) 讲「谁改谁的盘、怎么点」。本文对着源码讲：**这个文件在链条上干什么、关键几行为什么这样写、跑完产生什么结果。**

读法：先看第 1 节总图，再按你关心的路径跳到第 3 节调用栈，最后用第 2 节当文件索引。行号会随提交微调，以函数名为准。

---

## 1. 总链条（文件名穿起来）

```
run-shuncode.cmd
  └─ node shuncode-core/agent-host/src/index.js
        │  persistIdentity(store)     从工作区 .shuncode/config.json 拿回 secret
        │  同一套 Express app 听两个端口
        │
        ├─ :3000  静态 shuncode-core/workbench/ + /api + /ws
        │     workbench/app.js  ──POST /api/chat──►  agent/runChat.js
        │     workbench/app.js  ──POST /api/bridge/start──►  api/routes.js
        │
        └─ :48271  /mcp + /api + /.well-known/oauth-*
              网页 Agent / DeepSeek++
                │  HTTPS（若有隧道）或本机
                ▼
              mcp/server.js  requireAuth → handleRpc
                │  tools/call
                ▼
              tools/index.js  callTool（模式锁）
                │
                ├─ fileOps / patchEngine / executor / gitOps / skills …
                └─ eventBus.broadcast → 工作台右侧 BRIDGE
```

三条「谁下指令」最终都进 **同一套 `callTool`**：

| 入口 | 第一个文件 | 不经过 |
|---|---|---|
| 本机 CHAT | `workbench/app.js` → `POST /api/chat` → `runChat.js` | 隧道、MCP |
| 网页 VS Code 侧栏 / `@shuncode` | `extension/extension.js` → 同一个 `POST /api/chat` | 工作台 HTML |
| Arena / DeepSeek++ / ChatGPT 连接器 | `POST /mcp/<secret>` 或 `POST /mcp` + Bearer → `mcp/server.js` | `/api/chat` |

---

## 2. 文件目录（用途 → 结果 → 在链上的作用）

每一小节格式相同：**用途**、**结果**、**关键行（干什么 / 为什么）**、**链条位置**。

### 2.1 启动

#### `run-shuncode.cmd`

- **用途：** Windows 唯一主入口。不启动 code-server，不进 `shuncode-repro`。
- **结果：** 一个一直开着的 Node 进程；控制台打印 UI / MCP / Workspace。
- **关键行：**
  - `if not "%~1"=="" set "WORKSPACE_ROOT=%~1"` — 第一个参数就是被改的项目；不传则 `%~dp0workspace`。
  - `set AGENT_HOST_PORT=48271` / `WORKBENCH_PORT=3000` — 两个端口写死默认值，避免和旧原型抢同一个 3000。
  - `cd /d "%~dp0shuncode-core\agent-host"` 然后 `node src\index.js` — **全部产品逻辑从这里进 Node**。
  - 找不到 `node_modules\express` 才 `npm install` — 第一次慢，以后直接起。
- **链条位置：** 用户手 → 操作系统进程 → `index.js`。

`run-shuncode.sh` 是 Linux/macOS 等价物。`run-shuncode-vscode.cmd` 走 `shuncode-core/scripts/run-code-oss.js`，给 agent-host 加 `SHUNCODE_SKIP_WORKBENCH=1`，自己再拉 code-server 占 3000。

#### `shuncode-core/agent-host/src/index.js`

- **用途：** 组装 HTTP 应用，开两个 `http.Server`（同一套 `app`）。
- **结果：** `:3000` 给人看，`:48271` 给 MCP/API；可选只开 48271。
- **关键行：**
  - `persistIdentity(store)` — 启动瞬间用磁盘上的 secret 覆盖内存随机值，否则每次重启 MCP URL 里的密钥都变。
  - `app.use(express.static(workbenchDir))` — 工作台是静态文件，没有服务端渲染。
  - `app.use(oauth.router)` 在 `/mcp` 之前 — OAuth 发现文档和配对页必须匿名可访问。
  - `app.use('/mcp', mcpRouter)` — 所有 MCP JSON-RPC 挂在这里。
  - `const uiServer = http.createServer(app); const mcpServer = http.createServer(app);` — **两套 listen、一份路由**。网页 VS Code 要占用 3000 时，`SHUNCODE_SKIP_WORKBENCH=1` 跳过 `uiServer.listen`。
  - `host: '0.0.0.0'`（来自 config）— 预览环境和隧道要能从非 localhost 进来；只绑 127.0.0.1 时 cloudflared 仍可转，但沙箱预览会挂。
- **链条位置：** 进程心脏。后面所有请求都先到这里。

#### `shuncode-core/agent-host/src/config.js`

- **用途：** 进程内单例：端口、工作区、secret、隧道 URL。
- **结果：** 任何模块 `require('../config')` 拿到的是**同一块对象**。
- **关键行：**
  - `workspaceRoot = path.resolve(process.env.WORKSPACE_ROOT || …/workspace)` — 工具禁止写到这之外，见 `resolveSafePath`。
  - `secretKey: crypto.randomBytes(12).toString('hex')` — 先随机，随即被 `persistIdentity` 换成磁盘值。
  - `generateNewSecret()` — **只改内存**。`POST /api/bridge/reset-secret` 调用它后，当前进程旧 URL 立刻 401；**没有** `store.patch({ secretKey })`，所以进程重启会从旧 `config.json` 把密钥读回来。这是实现现状。
- **链条位置：** 被 index / mcp / tools / tunnel 同时读。

#### `src/models/store.js`

- **用途：** 读写 `<工作区>/.shuncode/config.json`。
- **结果：** 模型列表、Bridge 登录标记、secret、installId 跨重启还在。
- **关键行：**
  - `defaults().bridge.loggedIn: true`、`deviceAuthorized: true` — 复现环境默认已「登录」，否则 `/api/bridge/start` 会 403。本机 Chat 不读这个开关。
  - `load()` 用 try/catch，文件不存在就返回 defaults，不抛 — 第一次启动不必先手工建目录。
- **链条位置：** 启动时 persistIdentity；工作台改 API Provider / 多模型时 save。

---

### 2.2 MCP 层（网页 Agent 进门）

#### `src/mcp/server.js`

- **用途：** Streamable HTTP JSON-RPC 2.0。`GET/POST /mcp` 与 `GET/POST /mcp/:secret`。
- **结果：** 外部客户端能 `initialize`、`tools/list`、`tools/call`。
- **关键行：**
  - `extractToken` — Bearer、路径参数、`x-mcp-secret`、query 四种都认。贴 URL 的客户端把密钥放在路径里；OAuth 客户端放 Header。
  - `oauth.verifyAccessToken` — URL 密钥和 OAuth access token **走同一扇门**（见 oauth.js：`token === config.secretKey` 也算过）。
  - `rejectUnauthorized` 带 `WWW-Authenticate` — ChatGPT 连接器靠这个去发现 OAuth 元数据。
  - `initialize` 返回 `instructions: getInstructions()` — **规则的主通道**。很多网页客户端会丢掉 system prompt，所以后面还有 `shuncode://instructions`。
  - `tools/call` 成功：`clipJson` 后再包成 MCP `content: [{type:'text'}]`；失败分两层：协议错误抛 JSON-RPC `error`，执行错误返回 `isError: true` 文本。原因：模型把「文件不存在」当成协议失败会乱改参数，把「你调用方式不对」当成磁盘失败会空转。
  - `wantsSse` / `sendJsonRpc` — `Accept: text/event-stream` 时改 SSE。DeepSeek++ 文档写了 SSE 备选；主路径仍是普通 JSON POST。
  - `notifications/*` 返回 HTTP 204 — 通知没有 id，不能回 result。
- **链条位置：** 云上客户端 →（隧道）→ 这里 → `callTool`。

#### `src/mcp/oauth.js`

- **用途：** OAuth 2.1 子集（动态注册 + 授权码 + PKCE S256 + 配对码）。给 ChatGPT Plus 连接器。
- **结果：** 客户端拿到 Bearer，调用 `POST /mcp`（路径里**没有**长期密钥）。
- **关键行：**
  - `randomPairingCode()` 用易读字母表，去掉 0/O/1/I — 人要在本机打字。
  - `consumePairing` 最多 5 次、用过即废、5 分钟过期 — 配对码出现在工作台，不能当长期密码。
  - `s256(verifier)` 对 `code_verifier` 做 SHA-256 base64url — 没有 PKCE 的客户端会被拒。
  - 内存 `Map` 存 client/code/token — 进程一关全没。这是故意的：桌面复现不引入数据库。
  - `/.well-known/oauth-authorization-server` 匿名 GET — 连接器的第一步。
- **链条位置：** 仅 Plus 连接器。Arena / DeepSeek++ **不走这里**。

#### `src/mcp/clients.js`

- **用途：** 工作台 Bridge 页的「怎么连」卡片数据，不是 MCP 协议的一部分。
- **结果：** `/api/status.clients[]`，每张卡有 `connectMode` 和剪贴板 `prompt`。
- **关键行：**
  - `paste-url` → `getBootstrapPrompt(mcpUrl)` — URL + 固定那句连接说明（不要加长，网页栏当第一句）。
  - `extension-http`（DeepSeek）→ `prompt = mcpUrl` **只有一行** — 扩展的 URL 框不能吃两段说明。
  - `oauth-connector` → 规范地址 `/mcp`，不含密钥。
  - `unsupported-mcp`（免费 ChatGPT）→ 空 prompt，步骤里写「通常调不了」。
- **链条位置：** 只服务 UI / 文档。真正鉴权仍是 server.js。

#### `src/mcp/instructions.js`

- **用途：** 服务端规则文本 + 剪贴板那一句。
- **结果：** `initialize.instructions`、资源 `shuncode://instructions`、Chat 的 system 也会拼工作区指令。
- **关键行：**
  - `CONNECT_LINE` 是测试锁死的原文 — 改一个字 `mcpProtocol.test.js` 会红。
  - `getInstructions()` 追加 `custom.instructions` 和 `formatWorkspaceContext` — 用户在设置里写的「提交用中文」会进网页 Agent，不只进本机 Chat。
- **链条位置：** 模型「应该怎么调用工具」的说明书。

#### `src/mcp/resources.js` / `session.js` / `budget.js` / `errors.js`

| 文件 | 用途 | 结果 | 为什么 |
|---|---|---|---|
| `resources.js` | `resources/list|read` | `shuncode://protocol` 等 | 网页客户端丢掉 initialize 文本时的第二条通道 |
| `session.js` | `touch` / `snapshot` | 10 秒无调用标 stale | 工作台显示「对面还在不在」，避免盲目重试 initialize |
| `budget.js` | `clipJson` ~16k 字符 | 工具结果带 `_truncated` | 网页上下文会被一次 `npm test` 日志撑爆 |
| `errors.js` | 协议 vs 执行 | `{layer,code,msg}` | 见 server.js 两种 MCP 错误形态 |

---

### 2.3 工具层（真正改磁盘）

#### `src/tools/index.js`

- **用途：** 工具注册表 + 模式锁 + 危险命令闸。
- **结果：** `getToolList()` 给 MCP/Chat；`callTool(name, args, mode)` 执行。
- **关键行：**
  - 每个工具 `mode: ['ask','plan','code']` 或只有 `'code'` — Ask 调 `apply_patch` 抛 `E_BAD_ARGS`，文案让用户切 Code。
  - `aliases`（`read_file`→`read_files`）— 旧客户端/模型爱用短名。
  - `DANGEROUS_RE` + `confirm_dangerous` — 默认拒绝 `rm -rf` 一类，防止模型「顺手清盘」。
  - 返回值一律 `clipJson` — 即使内部忘了截断，出口还有一道。
  - **现在登记 25 个名字**（测试锁死长度）。没有 `lsp` / `get_diagnostics`。
- **链条位置：** MCP 和 Chat 的唯一执行入口。

#### `src/tools/patchEngine.js`

- **用途：** 工作区路径沙箱 + 哈希 + SEARCH/REPLACE 补丁。
- **结果：** 文件被原子替换（写 `.tmp.` 再 `rename`）；广播 `file_patched`。
- **关键行：**
  - `resolveSafePath`：`path.relative` 后若以 `..` 开头或绝对路径，抛 Security error — **所有文件工具的根防线**。
  - `assertNotSensitive(posix)` — `.env`、密钥、`.shuncode/config.json` 不能当补丁目标。
  - `expectedHash` 对不上 → `STALE_FILE` — 逼模型重新 `read_files`，避免按过期记忆覆盖。
  - 换行统一成 `\n` 再匹配 — Windows CRLF 否则 SEARCH 永远对不上。
  - 找不到精确 SEARCH 时 trim 再试一次 — 模型常多带空行；再找不到才 conflict。
  - 无 SEARCH 块且像 unified diff → `jsdiff.applyPatch`；否则整段当新文件内容。
- **链条位置：** Code 模式改文件的首选，而不是 `write_file`。

#### `src/tools/fileOps.js`

- **用途：** 读 / 写 / 删 / 改名 / 列表 / grep。
- **结果：** 带行号的窗口、sha256、分页 hits。
- **关键行：**
  - `readFile` 默认 `limit=400`，内容是 `12: line` — 模型引用行号；整文件默认不塞进上下文。
  - `hash` 来自**全文**不是窗口 — `apply_patch` 的 expectedHash 必须是整文件哈希。
  - `listDir` / `grepSearch` 跳过 `isHidden`（`node_modules`、`.git`、敏感文件）— 否则一次 list 就能把密钥文件名暴露出来。
  - `deleteFile` 拒绝删工作区根、拒绝非空目录。
- **链条位置：** Ask 的主工具；Code 的读前奏。

#### `src/tools/sensitive.js`

- **用途：** 敏感路径和噪声目录名单。
- **结果：** `isHidden` / `assertNotSensitive`。
- **关键行：** `.shuncodeignore` 可追加规则，`!` 前缀当例外 — 用户比默认名单更严或放行某文件。
- **链条位置：** `resolveSafePath` 和 list/grep 过滤器。

#### `src/tools/executor.js`

- **用途：** 在工作区跑命令。
- **结果：** `run_command` 等到结束；`start_command` 立刻返回 `execId`。
- **关键行：**
  - Windows：`powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command` — 用户主路径是 Windows，不能假设 bash。
  - `workingDirFrom` 再次检查 cwd 不出工作区。
  - `taskkill /pid /t /f` — PowerShell 子进程树，SIGTERM 杀不干净。
  - stdout 上限 200KB，环形截尾 — 防测试日志撑爆内存。
  - `start_command` 不 await `done` — 给网页 Agent 一条「先走、再 poll」的路，避免 HTTP 超时。
- **链条位置：** Code 模式跑测试 / 构建。

#### 其它工具文件

| 文件 | 用途 | 链条上 |
|---|---|---|
| `gitOps.js` | 只读 `git status` / `git diff` | 避免模型用 shell 调 git |
| `findFiles.js` | glob，默认 cap 40 | 比整树 list 便宜 |
| `skills.js` | `.shuncode/skills/<名>/SKILL.md` | `load_skill` |
| `workspaceInfo.js` | 一次返回根、分支、技术栈、顶层名 | 网页 Agent 的「朝向」 |
| `progressTracker.js` | todos / 进度 | 工作台任务条 |
| `consensusEngine.js` | Plan 三分支再合并 | **只被本机 Chat 的 Plan 调用**，MCP 远程默认不会进这里 |

---

### 2.4 本机 Chat

#### `src/agent/runChat.js`

- **用途：** `/api/chat` 的实现。有 API Key 走模型循环，否则走内置探索。
- **结果：** 通过 `emit(type, data)` 往 HTTP 响应写一行行 NDJSON。
- **关键行：**
  - `runChat` 先看 `store.load().activeModelId` 对应模型有没有 `apiKey+baseUrl+modelId` — 没配 Key 时**仍然会搜、读、跑测试**，不是空壳。
  - `explore()`：list → git_status → find_files → 用用户词 grep → 读 README/package.json。不写死 `calculator.js`。
  - `mode === 'ask'` 只 `summarizeAsk`，不 `apply_patch`。
  - `mode === 'plan'` 调 `runMultiModelConsensus`。
  - `mode === 'code'` 无模型时：若消息里有 SEARCH/REPLACE 或「写入 file + 代码块」才改文件，然后 `detectTestCommand()` 跑测试。
- **链条位置：** 工作台 CHAT、VS Code 插件 Chat 的后端。

#### `src/agent/openai.js`

- **用途：** OpenAI 兼容 `/chat/completions` 工具循环，最多 10 步。
- **结果：** 模型自己决定调哪些工具；每步 `emit('tool')`。
- **关键行：**
  - `getToolList(mode)` 做成 `tools` 参数 — Ask 时模型根本看不到 `apply_patch`。
  - `systemPrompt` 拼工作区根、中文回复、Windows PowerShell、用户 instructions。
  - 工具结果 `JSON.stringify(result).slice(0, 12000)` 再塞回 `role: tool` — 与 MCP budget 同一目的。
- **链条位置：** 仅本机 Chat（填了 Provider 之后）。网页 Agent 用的是它自己云上的模型 + 我们的 MCP。

#### `src/api/routes.js`

- **用途：** 工作台 / 插件的 REST。
- **结果：** 状态、Chat 流、文件树、自定义设置、启停 Bridge。
- **关键行：**
  - `POST /chat`：`Content-Type: application/x-ndjson`，`X-Accel-Buffering: no` — 让 nginx/预览不要把流攒成一块。
  - `mcpOrigin`：有 `config.publicTunnelUrl` 用隧道域名，否则用当前请求的 Host — 预览环境可以把 MCP 挂在同一预览域名上。
  - `POST /bridge/start`：检查 store 里 loggedIn；`config.bridgeRunning = true`；`oauth.ensurePairing()`。**这里目前没有调用 `tunnel.startQuickTunnel`。** 因此桌面上要出现 `trycloudflare.com`，还需要后续把 `startQuickTunnel` 接到这个 handler（实现缺口，见第 4 节）。返回的 `note` 写明了预览环境可以不靠 cloudflared。
  - `POST /bridge/reset-secret`：内存换密钥 + `oauth.revokeAll()`。
- **链条位置：** 所有「给人点的按钮」的后端。

#### `src/utils/eventBus.js`

- **用途：** 进程内事件 + WebSocket 扇出。
- **结果：** 工作台 `/ws` 收到 `tool_call_end`、`command_output`、`file_patched`。
- **关键行：** `readyState === 1` 才 send；日志最多 500 条。单例 export，MCP 和 Chat 打到同一块屏幕。

---

### 2.5 隧道

#### `src/tunnel/cloudflared.js`

- **用途：** 找 `cloudflared` 二进制、拉起 Quick Tunnel、从日志解析 `https://….trycloudflare.com`。
- **结果：** 成功则 `config.publicTunnelUrl = url`。
- **关键行：**
  - `CLOUDFLARED_PATH` → `where`/`which` → 常见安装目录 — Windows 装完不新开 CMD 时 PATH 里没有，所以要猜路径。
  - `spawn(..., { shell: needShell })` 仅当二进制是 `.cmd/.bat` — Node 在 Windows 上直接 spawn `.cmd` 会失败。
  - stdout **和** stderr 都扫 — cloudflared 把 URL 打在 stderr。
  - 25 秒超时杀进程 — 避免 UI 一直转圈。
- **链条位置：** **设计上**是「启动 Bridge」之后、网页 Agent 之前。**当前 routes.js 尚未调用它**（见第 4 节）。

---

### 2.6 工作台 UI

#### `shuncode-core/workbench/app.js` + `index.html`

- **用途：** 浏览器里的壳。不碰磁盘。
- **结果：** CHAT 流、BRIDGE 卡片、复制 MCP、打开各网站。
- **关键函数：**
  - `sendChat` → `fetch('/api/chat')` 按行 `JSON.parse` NDJSON → `handleEvent`。
  - `startBridge` → `POST /api/bridge/start`，然后 `refreshStatus` 画 MCP URL。
  - `promptText()` 读当前选中客户端的 `c.prompt` — DeepSeek 卡只复制 URL。
  - `openSite('deepseek')` 设 `selectedClient`，内置页说明「假浏览器跑不了扩展」，外链 `chat.deepseek.com`。
  - `arenaConnect` 会对本机 `/mcp/${secret}` 发 initialize/tools/list — **演示用**，真正 Arena 在云上自己连隧道。
- **链条位置：** 路径 A 的前端；路径 B 的「复制地址 / 看工具卡片」。

---

### 2.7 网页 VS Code 插件

#### `shuncode-core/extension/extension.js`

- **用途：** code-server 侧栏 + 原生 Chat `@shuncode`。
- **结果：** 同样打 `http://127.0.0.1:48271/api/chat`。
- **关键行：**
  - `agentHostUrl()` 默认 48271，可被配置覆盖。
  - `modeFromChatRequest`：无 slash 就是 `code`；`/ask` `/plan` `/code` 切换 — 对应 Copilot 的 Agent 默认动手。
  - `apply_patch` 成功后 `revealWorkspaceFile` — 编辑器里打开被改文件。
  - 插件 **不实现工具**。没有第二套 patch 引擎。
- **链条位置：** 路径 C 的 UI。`scripts/ensure-code-server.js` 把本文件复制到 `extensions-installed/…` 并重写带绝对路径的 `extensions.json`（不进 Git）。

---

### 2.8 工作区侧数据

| 文件 | 写到磁盘 | 谁读 |
|---|---|---|
| `models/customizations.js` | `.shuncode/customizations.json` + `instructions.md` + `preference.md` + `tech-stack.md` | Chat system、MCP initialize、工作台表单 |
| `models/profile.js` | 不写；探测 OS/Shell/`package.json` | `formatWorkspaceContext` 拼进指令 |
| `models/memory.js` | `.shuncode/memory/` | 工具 `remember` / `recall` |

`.shuncode` 在 **工作区根**，因为密钥和指令属于「正在修的项目」，不属于 GitHub 上的 `web_agent` 工具箱。

---

## 3. 三条路径的函数调用栈

### 3.1 本机 Chat，无 API Key，Code 模式

```
用户点发送
workbench/app.js sendChat()
  fetch POST /api/chat  { mode:'code', message, history }
api/routes.js  打开 NDJSON，emit = res.write
agent/runChat.js runChat()
  store 无 apiKey → runBuiltin()
    timedTool(set_todos)
    explore()
      callTool('list_directory', …, 'code')   → fileOps.listDir → resolveSafePath
      callTool('git_status')                  → gitOps
      callTool('find_files') / search_files / read_files
    若消息含 SEARCH/REPLACE
      read_files 取 hash
      callTool('apply_patch', { expectedHash, patch }, 'code')
        patchEngine.applyPatch → 写磁盘 → eventBus file_patched
    detectTestCommand() → callTool('run_command', { command: 'npm test' })
      executor: powershell.exe …
    emit('message', 摘要)
workbench handleEvent 画工具卡 / 打开 diff 页
```

### 3.2 DeepSeek++ 改磁盘（设计链）

```
chat.deepseek.com 上模型打出工具标记
DeepSeek++（浏览器扩展，本仓库无源码）
  POST https://xxxx.trycloudflare.com/mcp/<secret>
    jsonrpc initialize / tools/list / tools/call
cloudflared → 127.0.0.1:48271
mcp/server.js
  extractToken 从路径取出 secret
  oauth.verifyAccessToken 认作 url-secret
  handleRpc tools/call
    callTool(name, args)          // 远程默认不传 mode，工具按注册表执行
      apply_patch / run_command …
    clipJson → MCP content
    eventBus tool_call_end
workbench /ws → 右侧 BRIDGE
扩展把 JSON 文本塞回同一条 DeepSeek 对话
```

`clients.js` 的 DeepSeek 卡片只负责告诉人「把哪一行填进扩展」。扩展不读我们的 `prompt` 字段。

### 3.3 ChatGPT Plus 连接器

```
连接器 GET /.well-known/oauth-authorization-server
POST /oauth/register  → client_id
浏览器打开 /oauth/authorize
  用户输入工作台配对码  → consumePairing
  302 带回 authorization code
POST /oauth/token + PKCE verifier → Bearer
之后 POST /mcp   Authorization: Bearer …   （路径无密钥）
  与 3.2 相同的 handleRpc
```

---

## 4. 代码现状里和「文档愿望」不一致的地方

技术文档必须写实际代码，避免后人按愿望改错文件。

1. **`POST /api/bridge/start` 没有调用 `startQuickTunnel`。**  
   `cloudflared.js` 是完整实现，也有单测解析 URL。但按钮目前只置 `bridgeRunning` 和配对码。`mcpOrigin()` 在 `publicTunnelUrl` 为空时退回当前页面 Host。预览/沙箱能靠这一招让 MCP 和 UI 同域；**用户自己的 Windows 桌面要 `trycloudflare.com`，需要把 `await tunnel.startQuickTunnel()` 接到这个 handler，成功后再 `mcpInfo(req)`。**

2. **`generateNewSecret` 不写 `config.json`。**  
   当前进程旧链接会 401；重启后又用文件里的旧密钥。若产品语义是「重置永久生效」，应在 reset 路由里 `store.patch({ secretKey: config.secretKey })`。

3. **远程 `tools/call` 不传 Ask/Plan/Code。**  
   `callTool(name, args)` 第三参是 `null`，模式锁不生效。网页 Agent 被当成 Code。锁只约束本机 Chat。这是有意降低远程握手复杂度，代价是远程没有只读档。

4. **`read_files` 的敏感拦截靠 `resolveSafePath` 里的 `assertNotSensitive`；列表靠 `isHidden`。** 两条路径都要留着，删一个就会在「读」或「列」上漏。

---

## 5. 改代码时建议从哪下手

| 你想改的行为 | 先打开 |
|---|---|
| 新 MCP 工具 | `tools/index.js` 注册 + 新文件；`mcpProtocol.test.js` 的 25 改数量 |
| 补丁匹配 / 哈希 | `patchEngine.js` + `tests/patchEngine.test.js` |
| 网页第一句提示词 | `instructions.js` 的 `CONNECT_LINE`（有测试锁原文） |
| DeepSeek / Arena 卡片步骤 | `mcp/clients.js` |
| 本机无 Key 时的探索策略 | `agent/runChat.js` `explore` / `runBuiltin` |
| 有 Key 时模型怎么被管 | `agent/openai.js` `systemPrompt` |
| 工作台按钮 | `workbench/app.js` + `index.html` |
| VS Code Chat 默认 Agent | `extension/extension.js` `modeFromChatRequest` |
| 隧道真正拉起 | `api/routes.js` 的 `/bridge/start` ← `tunnel/cloudflared.js` |
| 密钥持久化 | `config.js` + `store.js` |

测试入口永远是 `shuncode-core/agent-host/tests/`，根目录 `run-tests.cmd` 只是调用它。说明见 [测试说明.md](./测试说明.md)。
