# 代码直译技术实现

本文只描述**当前仓库里读到的源码**。没有写进代码的行为标「源码未实现」，不补愿望、不杜撰调用。

读法：第 0 节总链 → 按文件跳到对应小节。每个函数拆成「步骤 1、2、3…」；`if` / `try` 分支全部列出。**闭区间行号以各子文件夹 README / [总览.md](./总览.md) 为准**（四阶段文档）；本文偏行为直译，会随提交微调。

小白工作流、怎么点按钮，仍看 [组件说明.md](./组件说明.md)。人话架构看 [架构导读.md](./架构导读.md)。本文是对着源码的直译。

---

## 0. 总链条（文件名穿起来）

```
run-webagent.cmd
  └─ node webagent-core/agent-host/src/index.js
        persistIdentity(store)
        两套 Express（同一进程）
        ├─ uiApp :3000  静态 workbench/ + /api + /ws + SPA 回退（无 /mcp）
        │     workbench/app.js  POST /api/chat → agent/runChat.js
        │     workbench/app.js  POST /api/bridge/start → api/routes.js
        └─ mcpApp :48271  /mcp + OAuth；/api 先过 utils/localControl.js
              隧道头或公网 Host → /api 404；本机回环仍通
              mcp/server.js requireAuth → handleRpc
                tools/call → tools/index.js callTool
                  fileOps / patchEngine / executor / gitOps …
                  eventBus.broadcast → 工作台 :3000 /ws（mcp 口不挂 WSS）
```

三条「谁下指令」最终都进 **同一套 `callTool`**：

| 入口 | 第一个文件 | 源码里**不经过** |
|---|---|---|
| 本机 CHAT | `workbench/app.js` → **3000** `POST /api/chat` → `runChat.js` | 隧道、`/mcp` |
| 网页 VS Code 侧栏 / `@webagent` | `extension/extension.js` → 本机 48271 `POST /api/chat`（无 CF 头） | 工作台 HTML |
| Arena / DeepSeek++ / Chat Plus / ChatGPT 连接器 | `POST /mcp/<secret>` 或 `POST /mcp` + Bearer → `mcp/server.js` | `/api/chat` |

---

## 1. 启动脚本

### 1.1 `run-webagent.cmd`

**用途：** Windows 主入口。不启动 code-server，不进 `webagent-repro`。  
**结果：** 一个一直开着的 Node 进程（`node src\index.js`）。  
**链条位置：** 用户手 → 操作系统进程 → `index.js`。

步骤：

1. `@echo off`、`chcp 65001`、`setlocal EnableExtensions`、窗口标题 `Web Agent`、`cd /d "%~dp0"` 进仓库根。
2. `if not "%~1"==""` → 把第一个参数写成 `WORKSPACE_ROOT`；未定义则 `%~dp0workspace`。
3. 写死 `AGENT_HOST_PORT=48271`、`WORKBENCH_PORT=3000`。
4. `where node`：`errorlevel 1` 则打印安装提示、`pause`、`exit /b 1`。
5. `where npm`：失败同样退出。
6. 工作区不存在时：
   - `if "%~1"==""`（没传参数）→ `mkdir` 默认 `workspace`。
   - **否则**打印「工作区不存在」并退出（不会替你建 `D:\code\my-app`）。
7. 打印 `node -v`、`npm -v`。`where git` 失败只打印「未安装」，**不退出**。`where cloudflared` 失败只打印提示，**不退出**。
8. `cd /d "%~dp0webagent-core\agent-host"`。若不存在 `node_modules\express` 则 `call npm install --no-audit --no-fund`；失败则退出。
9. `node src\index.js`。若 `ERRORLEVEL` 非 0：打印端口占用提示、`pause`，再 `exit /b %ERR%`。

### 1.2 `run-webagent.sh`

**用途：** Linux/macOS 等价入口。  
**结果：** `exec node src/index.js` 替换当前 shell。

步骤：`set -e`；`ROOT` 为脚本所在目录；`WORKSPACE_ROOT` 默认 `$ROOT/workspace`；端口默认 48271 / 3000；`cd agent-host`；没有 `node_modules/express` 就 `npm install`；`exec node src/index.js`。没有 Windows 那套 `where` / `pause`。

### 1.3 `run-webagent-vscode.cmd`

**用途：** 第二种跑法：网页 VS Code。  
**结果：** 调用 `node webagent-core\scripts\run-code-oss.js`，把工作区当参数传进去。

步骤：同样设 `WORKSPACE_ROOT`（默认 `%~dp0workspace`）。缺 Node/npm 退出。工作区不存在时**直接退出**（即使没传参数也不 `mkdir`，与 `run-webagent.cmd` 不同）。成功则 `node …\run-code-oss.js "%WORKSPACE_ROOT%"`。

### 1.4 `webagent-core/scripts/ensure-code-server.js`

**用途：** 第一次把 `code-server@4.135.0` 装到 `bin/code-server-runtime/`，并把插件拷到 `extensions-installed/`。  
**结果：** 返回 `out/node/entry.js` 的绝对路径。  
**链条位置：** 只被 `run-code-oss.js`（以及本文件作为 CLI 时）调用。`run-webagent.cmd` **不经过**这里。

#### `npmCmd`

`win32` → `npm.cmd`，否则 `npm`。

#### `runNpm(args, cwd)`

步骤 1：`spawnSync` 跑 npm，`stdio: inherit`，Windows 上 `shell: true`。  
步骤 2：`env` 里加 `FORCE_NODE_VERSION` 为当前 Node 主版本号。  
步骤 3：`if (r.status !== 0)` 抛 `Error`。

#### `findEntry`

按顺序查三个候选路径，`find` 第一个 `existsSync` 的；都没有返回 `null`。

#### `ensureVscodeDeps(entry)`

步骤 1：从 entry 推出 `lib/vscode`。  
步骤 2：若已有 `node_modules/@microsoft/1ds-core-js` → **直接 return**。  
步骤 3：若该目录没有 `package.json` → 抛「包不完整」。  
步骤 4：否则 `npm install --omit=dev --ignore-scripts`。

#### `ensure`

步骤 1：`mkdirSync(runtimeRoot, { recursive: true })`。  
步骤 2：`findEntry()`。若没有：
   - 没有 `package.json` 则写入一个只依赖 `code-server` 的私有包。
   - `npm install code-server@4.135.0 --omit=dev --ignore-scripts`。
   - 再 `findEntry()`。
步骤 3：仍没有 entry → 抛错。  
步骤 4：`ensureVscodeDeps(entry)`，**返回 entry**。

#### `syncExtension`

步骤 1：把 `extension/package.json`、`extension.js` 拷到 `extensions-installed/webagent.webagent-core-0.6.9/`。  
步骤 2：若存在 `resources/icon.svg` 也拷。  
步骤 3：写 `extensions.json`，`location.path` 是**本机绝对路径**（正斜杠）。

文件末尾：`require.main === module` 时自己跑 `ensure` + `syncExtension`；失败 `process.exit(1)`。

### 1.5 `webagent-core/scripts/run-code-oss.js`

**用途：** 先起 agent-host（跳过 3000），再起 code-server 占 3000。  
**结果：** 两个子进程；Ctrl+C 时 Windows 用 `taskkill /t /f`。

#### 顶层

`workspace` = `argv[2]` 或 `WORKSPACE_ROOT` 或仓库 `workspace`。不存在则 `console.error` + `process.exit(1)`。  
`mcpPort` 默认 48271，`codePort` 默认 3000。

#### `waitHealth(url, timeoutMs)`

循环 `http.get`：状态码 200 则 resolve；超时 reject「agent-host 未在时限内就绪」；`error` 事件同样按超时判断，否则 200ms 后再试。

#### `run(command, args, opts)`

`spawn`，`stdio: inherit`，`windowsHide: true`。`child.on('error')` 打印后 `process.exit(1)`。返回 child。

#### `main`（async）

步骤 1：`ensure()` 得到 code-server entry，`syncExtension()`。  
步骤 2：agent-host 没有 `node_modules/express` 则在该目录 `npm install`；子进程 exit 非 0 则 reject。  
步骤 3：注册 `SIGINT`/`SIGTERM`：对每个 child，Windows `taskkill /pid /t /f`，否则 `SIGTERM`。  
步骤 4：`run(process.execPath, ['src/index.js'], { cwd: agentHostDir, env: { WORKSPACE_ROOT, AGENT_HOST_PORT, WEBAGENT_SKIP_WORKBENCH: '1' } })`。agent `exit` 若 `code` 真值：stop 后 `process.exit(code)`。  
步骤 5：`await waitHealth(http://127.0.0.1:${mcpPort}/health, 15000)`。  
步骤 6：确保 `.local/share/code-server` 目录存在。`resolveAuth`：默认 `--auth password`，口令来自 `CODE_SERVER_PASSWORD` 或该目录 `webagent-password`（没有则生成并写入）；`CODE_SERVER_AUTH=none` 才无登录。拼参数：`--auth`、`--bind-addr ${WEBAGENT_BIND||127.0.0.1}:${codePort}`、关遥测/更新/workspace-trust、`--trusted-origins` 仅本机 http 源、`--app-name Web Agent`、user-data-dir、extensions-dir、config yaml、最后一项是 **workspace 路径**。  
步骤 7：`run(process.execPath, csArgs, { cwd: dirname(entry), env: password 时 PASSWORD })`。code-server 退出则 stop 并 `process.exit(code || 0)`。

`main().catch`：打印 `err.message`，`exit(1)`。

---

## 2. 进程心脏

### 2.1 `src/config.js`（47 行）

**用途：** 进程内单例：端口、工作区、secret、隧道 URL。  
**结果：** 任何 `require('../config')` 拿到**同一块对象**。  
**链条位置：** index / mcp / tools / tunnel / routes 都读它。

#### 模块加载时

步骤 1：`workspaceRoot = path.resolve(process.env.WORKSPACE_ROOT || path.join(__dirname, '../../../workspace'))`。  
步骤 2：构造 `config`：
- `port`：`parseInt(process.env.AGENT_HOST_PORT || '48271', 10)`
- `workbenchPort`：`parseInt(process.env.WORKBENCH_PORT || '3000', 10)`
- `host`：`process.env.WEBAGENT_BIND || '127.0.0.1'`
- `secretKey`：`crypto.randomBytes(12).toString('hex')`（先随机）
- `version: '0.6.9'`，`serverName: 'WebAgent-AgentHost'`，`productName: 'Web Agent'`
- `tunnelProvider: 'cloudflare'`，`publicTunnelUrl: null`，`bridgeRunning: false`
- `installId`：8 字节 hex

#### `generateNewSecret`

步骤 1：`config.secretKey = crypto.randomBytes(12).toString('hex')`。  
步骤 2：lazy `require('./models/store').patch({ secretKey })` 写入工作区 `.webagent/config.json`；失败 catch 空。  
步骤 3：返回新值。

#### `persistIdentity(store)`

步骤 1：`saved = store.load()`。  
步骤 2：`if (saved.secretKey)` → 用磁盘值覆盖内存；**else** `store.patch({ secretKey: config.secretKey })` 把随机值落盘。  
步骤 3：`installId` 同样：有则覆盖，无则 patch。  
步骤 4：若 `store.protectWorkspaceSecrets` 是函数则调用（gitignore + chmod）；失败 catch 空。

### 2.2 `src/index.js`（136 行）

**用途：** 组装 **uiApp / mcpApp** 两套 Express，开两个 `http.Server`。  
**结果：** 默认 `:3000` 给人看（工作台 + `/api` + `/ws`），`:48271` 给 MCP/OAuth；mcp 口 `/api` 只对本机回环开放。`WEBAGENT_SKIP_WORKBENCH=1` 时只开 48271。

步骤 1：`persistIdentity(store)`。  
步骤 2：工作区目录：
- 不存在 **且** 设了 `process.env.WORKSPACE_ROOT` → 打印错误、`process.exit(1)`（不替你建用户指定的路径）。
- 不存在 **且没有** 该环境变量 → `mkdirSync(..., { recursive: true })`。
步骤 3：`applyCommon(app)`：关 x-powered-by；所有响应 `Cache-Control: no-store`；`express.json({ limit: '20mb' })`；`urlencoded({ extended: false })`。**不**在这里挂 cors。  
步骤 4：`mountHealth`：`GET /health` 返回 `{ ok, product, version }`。  
步骤 5：`mountWorkbench`：静态目录 `path.resolve(__dirname, '../../workbench')`；SPA 回退：
- `method !== 'GET'` → `next()`。
- path 以 `/api` `/mcp` `/ws` `/oauth` `/.well-known` 开头，或恰好是 `/register` → `next()`。
- `path.extname(req.path)` 真值（带扩展名）→ `next()`。
- 否则 `sendFile(workbench/index.html)`。
步骤 6：`uiApp`：applyCommon、health、`/api` 先 `rejectUnlessLocalControl` 再 `rejectCrossSiteApi`、工作台静态 + SPA。无 cors、无 `/mcp`。  
步骤 7：`mcpApp`：applyCommon、**`mcpCors()`**（白名单，不是无条件 `cors()`）、health、`oauth.router`、`/mcp`、`/api` 先 `rejectUnlessLocalControl` 再 `rejectCrossSiteApi`。无静态工作台。  
步骤 8：`attachWss(server)`：`WebSocketServer({ server, path: '/ws' })`；**非本机控制面 `ws.close(1008)`**；否则 `eventBus.addWsClient(ws)`，并立刻 `ws.send` 一条 `type:'connected'`，payload 只含 `serverName`、`version`（**不含 secretKey**）。  
步骤 9：`uiServer = createServer(uiApp)`，`mcpServer = createServer(mcpApp)`；**只**给 uiServer attachWss。  
步骤 10：`listenOrExit`：`error` 且 `err.code === 'EADDRINUSE'` 打印占用并 `exit(1)`；其它 error 也 `exit(1)`；`server.listen(port, config.host)`。  
步骤 11：`skipWorkbench = process.env.WEBAGENT_SKIP_WORKBENCH === '1'`。
- **不是** skip：`listenOrExit(uiServer, workbenchPort)`，listening 时打印 UI / MCP / Workspace，以及「公网只收 /mcp 与 OAuth」。
- **是** skip：打印「本进程不占用 3000」，并说明 `/api` 仅本机回环。
步骤 12：无论 skip 与否，都 `listenOrExit(mcpServer, config.port)`。

导出 `{ uiApp, mcpApp, uiServer, mcpServer }`。

### 2.3 `src/utils/localControl.js`（55 行）

**用途：** 判断请求是不是本机控制面。  
**结果：** 隧道/公网 Host 打 48271 的 `/api` 得 404 `{ error:'not found' }`。

- `isLoopbackAddress`：`127.0.0.1` / `::1` / `::ffff:127.0.0.1` / `localhost`。
- `isTunnelRequest`：头 `cf-ray` / `cf-connecting-ip` / `cf-visitor` / `cf-ew-via` / `cdn-loop`。
- `isPublicHost`：Host 为 `*.trycloudflare.com` 或含 `ngrok`。
- `isLocalControlPlane`：隧道头或公网 Host → 假；否则看 `socket.remoteAddress` 是否回环。
- `rejectUnlessLocalControl`：本机 `next()`，否则 404。不能只看「连的是 127.0.0.1」——cloudflared 连的就是回环。

### 2.4 `src/utils/corsAllow.js`（115 行）

**用途：** MCP 口浏览器跨域白名单；`/api` 拒绝外站 Origin。  
**结果：** `https://evil.example` 预检拿不到 `Access-Control-Allow-Origin`；打 `/api` 得 404。DeepSeek++ / Chat Plus 后台是 `chrome-extension://…`，仍放行。无 Origin 的 curl / 云上服务器 / VS Code 插件仍通。  
**不是认证：** `/mcp` 仍要 URL 密钥或 Bearer。

- 无 Origin → MCP CORS 放行。
- 本机 `http(s)://127.0.0.1|localhost|::1` → 放行。
- `chrome-extension:` / `moz-extension:` / `safari-web-extension:` → 放行。
- 文档点名的页面：`chat.deepseek.com`、`chatgpt.com`、`chat.openai.com`、`gemini.google.com`、`aistudio.google.com`、`arena.ai`。
- `WEBAGENT_CORS_ORIGINS` 逗号追加。
- `/api` 只接受无 Origin 或本机 Origin；外站 Origin / Referer → 404。

---

## 3. 磁盘上的工作区状态

### 3.1 `src/models/store.js`

**用途：** 读写 `<工作区>/.webagent/config.json`。  
**结果：** 模型列表、Bridge 登录标记、secret、installId 跨重启还在。  
**链条位置：** 启动 persistIdentity；工作台改 Provider / 多模型时 save。

#### `dir` / `storePath`

`.webagent` 目录；其下 `config.json`。

#### `defaults`

返回对象，要点：
- `activeModelId: 'builtin'`，一条 builtin 模型。
- `multiModel.enabled: true`，`maxBranches: 4`（clamp 2–8），`mergeAllowsRead: true`，`mergeModel:'auto'`，`thinkLevel:'high'`。
- `bridge.loggedIn: true`，`deviceAuthorized: true`，`provider/username/license: 'local-demo'/'local'/'local-demo'`（**不是** GitHub），`tunnelProvider: 'cloudflare'`，`persistentMode: false`。

#### `normalizeBridge`

与 defaults 合并后：旧盘 `license==='永久顺'` → `local-demo`；`provider==='github'` → `local-demo`；`username==='demo'` 且已是 local-demo → `'local'`。

#### `clampBranches` / `normalizeMultiModel`

非有限 → 4；否则 round 后夹到 2–8。`normalizeMultiModel` 与 defaults 合并后 clamp；布尔字段非布尔则 true。

#### `load`

`try`：读 JSON，与 defaults 浅合并；`models` 若不是非空数组则用 defaults.models；`bridge` 走 `normalizeBridge`；`multiModel` 走 `normalizeMultiModel`。  
`catch`（文件不存在或坏 JSON）：**返回 defaults，不抛**。

#### `save(next)`

先 `normalizeBridge` + `normalizeMultiModel`，再 `mkdirSync`、写美化 JSON、`chmod 0600`、`protectWorkspaceSecrets()`，返回规范化后的 cfg。

#### `protectWorkspaceSecrets`

写 `.webagent/.gitignore`（`config.json`、`read-hashes.json`）。工作区根有 `.git` 时，若仓库 `.gitignore` 还没有这两行则追加。不是 git 仓库不改用户根 gitignore。已有 `config.json` 再 chmod。失败 catch 空。密钥**仍在工作区**（每台「车」一把），不搬到 APPDATA。

#### `patch(partial)`

`load` → 浅合并；`bridge` 再深一层后走 `normalizeBridge`；`multiModel` 再深一层；`models` 用 `partial.models || current.models`（传了才换）。然后 `save`。

### 3.2 `src/models/customizations.js`

**用途：** `<工作区>/.webagent/customizations.json`，并同步写出 md。  
**结果：** 指令、环境、技术栈、agents/prompts 等。  
**链条位置：** MCP `initialize.instructions`、本机 Chat system、工作台设置页。

#### `file`

`…/.webagent/customizations.json`。

#### `defaults`

含 `preference: ''`；`environment` 全 `auto` / `zh-CN`；`techStack` 空字符串；默认 `instructions` 文案；一个 default agent；一个 diagnose prompt；空数组 hooks/mcpServers/plugins/quickLinks；`codex: { loggedIn: false, account: '' }`。

#### `loadCustom`

`try` 读 JSON，与 defaults 合并 environment/techStack。  
`catch` 返回 defaults。

#### `saveCustom(next)`

步骤 1：`merged = { ...defaults(), ...next }`。  
步骤 2：`if (next && next.environment)` 再合并 environment。  
步骤 3：`if (next && next.techStack)` 再合并 techStack。  
步骤 4：确保目录存在，写 `customizations.json`。  
步骤 5：同目录再写 `instructions.md`（`merged.instructions || ''`）、`preference.md`（`markdownPreference(merged)`）、`tech-stack.md`（`markdownTechStack(merged)`）。  
步骤 6：返回 merged。

#### `patchCustom(partial)`

`saveCustom({ ...loadCustom(), ...partial })`。

### 3.3 `src/models/profile.js`

**用途：** 探测 OS/技术栈，拼进给模型的上下文；生成两份 markdown。  
**结果：** 纯函数，**不写磁盘**（写磁盘是 customizations.saveCustom）。

#### `detectEnvironment`

`process.platform`：`win32` → os `windows`；`darwin` → `macos`；否则 `linux`。  
shell：windows → `powershell`，否则 `bash`。  
回复/提交语言写死 `zh-CN`。`notes: ''`。

#### `readJson(file)`

`try` `JSON.parse(readFileSync)`；`catch` 返回 `null`。

#### `exists(root, rel)`

`fs.existsSync(path.join(root, rel))`。

#### `detectTechStack(workspaceRoot)`

步骤 1：languages / frameworks 数组，packageManager / testCommand 空串，notes 空数组（后面 `notes.join` 但代码从未 `notes.push`，所以 notes 恒为空串）。  
步骤 2：有 `package.json`：
- languages 加 `JavaScript`。
- 有 `tsconfig.json` 或 `jsconfig.json` 再加 `TypeScript`。
- deps 合并 dependencies + devDependencies：`next` → Next.js，否则 `react` → React；`nuxt` → Nuxt，否则 `vue` → Vue；`express` → Express。
- lock：`pnpm-lock.yaml` → pnpm；else `yarn.lock` → yarn；else `npm`。
- `pkg.scripts.test` 存在 → `testCommand = ${packageManager} test`。
步骤 3：有 `pyproject.toml` 或 `requirements.txt` 或 `pytest.ini`：加 Python；若还没有 packageManager → `pip`；若还没有 testCommand → `python -m pytest -q`。  
步骤 4：`Cargo.toml`：Rust / cargo / `cargo test`（仅当 packageManager / testCommand 仍空）。  
步骤 5：`go.mod`：Go / go / `go test ./...`（同样仅当仍空）。  
步骤 6：有 `index.html` 且 languages 不含 HTML → 加 HTML。  
步骤 7：`uniq` 去重后 join。

#### `resolveEnvironment(custom)`

探测值作底。`env.os && env.os !== 'auto'` 才用用户值；shell 同理。`replyLanguage` / `commitLanguage` 用 `||`（空串会回落到探测值）。`notes` 用 `env.notes || ''`。

#### `resolveTechStack(custom, workspaceRoot)`

各字段 `stack.xxx || detected.xxx`。

#### `languageLabel(code)`

`zh-CN` 或 `zh` → 中文；`en` → English；`follow-user` → 跟随用户提问的语言；否则原样或空。

#### `osLabel(os)`

windows / macos / linux 映射；其它原样。

#### `formatWorkspaceContext(custom, skills)`

拼 markdown 三段：Environment preference、Tech stack、Skills。  
tech stack 若 languages 和 testCommand 都空，加一行 `(not declared; inspect the workspace)`。  
skills 空：提示放到 `.webagent/skills/`。非空：对每个 skill 取 preview 里第一行非空且不以 `#` 开头的，截 80 字。

#### `markdownPreference` / `markdownTechStack`

生成中文 md。`filter((l) => l !== null)`：空字符串会保留（用来空行）。

### 3.4 `src/models/memory.js`

**用途：** `.webagent/memory/YYYY-MM-DD.md` 追加备忘。  
**结果：** 工具 `remember` / `recall`。

#### `memoryDir` / `dayFile`

目录；`day` 缺省用 `toISOString().slice(0, 10)`。

#### `remember({ text, day })`

步骤 1：`body = String(text || '').trim()`；空则 `{ ok: false, error: 'text required' }`。  
步骤 2：`mkdirSync`。  
步骤 3：一行 `- <ISO时间> <正文换行压成空格>`。  
步骤 4：文件已存在 → `appendFileSync(line)`；否则先写 `# 日期` 再写 line。  
步骤 5：返回 `{ ok: true, path: 相对工作区 }`。

#### `recall({ limit = 40, day })`

步骤 1：确保目录。  
步骤 2：若传了 `day`，只看那一个文件；否则读目录所有 `.md`，`sort().reverse()`（新日期在前）。  
步骤 3：逐文件：不存在 `continue`；push `## 文件名\n内容`；若拼接长度 `> 8000` **break**。  
步骤 4：无内容则 `'(empty memory)'`。  
步骤 5：按行 `slice(0, Math.max(5, limit))`。  
返回 `{ files: 相对路径数组, text }`。

---

## 4. MCP 层（网页 Agent 进门）

### 4.1 `src/mcp/errors.js`

#### 类 `ProtocolError` / `ExecutionError`

都 `super(message)`；设 `name`、`layer`（`protocol` / `execution`）、`code`、`detail || {}`。

#### `classifyToolError(err)`

步骤 1：已经是上述两类 → 原样返回。  
步骤 2：取 `err.message` 或 `err` 转字符串，按正则**从上到下**第一次命中：
- `Unknown tool` → `ProtocolError E_UNKNOWN_CMD`
- `locked in` 或 `Ask/Plan are read-only` → `E_BAD_ARGS`
- `requires ` 或 `required` → `E_BAD_ARGS`
- `HASH_REQUIRED` → `E_BAD_ARGS`
- `STALE_FILE` → `ExecutionError E_STALE_FILE`
- `Patch conflict` → `E_CONFLICT`
- `GIT_UNAVAILABLE` 或 `not a git repository` → `E_NOT_READY`（现行 `git_status`/`git_diff` 多数已收成 `available:false`，不一定抛到这里）
- `not found` 或 `No such file` → `E_NOT_FOUND`
- `timeout` 或 `isTimeout` → `E_TIMEOUT`
- `ACCESS_DENIED_SENSITIVE_FILE` 或 `E_FORBIDDEN` → `E_FORBIDDEN`
- `confirm_dangerous` / `confirm_overwrite` / `confirm=true` → `ProtocolError E_BAD_ARGS`
- 其它 → `ExecutionError E_INTERNAL`

#### `publicError(err)`

classify 后返回 `{ layer, code, msg: classified.message, detail }`。

### 4.2 `src/mcp/session.js`

内存 `Map`，进程关了就没。

#### `sessionKey(req)`

ip：`req.ip` 或 `x-forwarded-for`，都没有 `'local'`。  
client：`req.body.params.clientInfo.name`，没有 `'mcp'`。  
返回 `` `${client}@${ip}` ``。

#### `touch(req, extra = {})`

步骤 1：key = `extra.key || sessionKey(req)`。  
步骤 2：已有记录或新建 `{ key, connectedAt, calls:0, fail:0 }`。  
步骤 3：合并 extra；`lastSeen` 现时 ISO；`calls` / `fail` 仅当 `extra.incCall` / `incFail` 为真才 +1；`busy = Boolean(extra.busy)`。  
步骤 4：写回 Map，返回 next。

#### `snapshot`

按 `lastSeen` 字符串降序。`alive`：最新一条的年龄 `< 10000` ms。返回 `clients` 数、`staleAfterMs: 10000`、`ageMs`、`latest`、最多 8 条 `sessions`。

#### `reset`

`sessions.clear()` 后返回 `snapshot()`。被 `POST /api/bridge/reset-round` 调用。

### 4.3 `src/mcp/budget.js`

常量 `MAX_CHARS = 16000`（注释：约 4k tokens）。

#### `estimateTokens(text)`

`ceil(length / 4)`。

#### `clipText(text, maxChars)`

空当 `''`。长度 ≤ max → `{ text, truncated: false }`。  
否则切 `maxChars-80`，后缀说明 truncated 字数，并提示用 offset/limit/cursor/`get_command_output`。

#### `clipJson(value, maxChars)`

- `null/undefined` 原样。
- **字符串**：clipText；截断则 `{ text, _truncated, originalChars }`，否则原字符串。
- 先 `JSON.stringify`；长度 ≤ max → 原对象。
- 否则：对象（非数组）浅拷贝，数组则包成 `{ value }`。
- 对键 `stdout|stderr|content|preview|text|diff`：若字符串长度 > 2000，clipText 到 `min(4000, maxChars/3)`。
- `matches` 数组长度 > 20：留下 20，设 `nextCursor=20`，补 `totalMatches`。
- `items` 数组长度 > 40：留下 40，`nextCursor=40`。
- 再 stringify：仍超长 → `{ _truncated, summary: clipText(...), originalChars, hint }`。
- 否则给 copy 打 `_truncated` 和 `originalChars` 后返回 copy。

### 4.4 `src/mcp/instructions.js`

#### 常量 `CONNECT_LINE`

固定一句：`快速连接这个 MCP（URL），明确使用规则，熟悉可用工具，做好处理接下来一系列工作的准备。`  
（测试锁原文，改一字会红。）

#### `getBootstrapPrompt(mcpUrl)`

`` `${mcpUrl || ''}\n\n${CONNECT_LINE}` ``。

#### `SERVER_INSTRUCTIONS`

大段 markdown：工具改磁盘、不要杜撰、Ask/Plan 只读、工作流 1–7、输出预算、错误分层、安全、memory。源码里就是这一整段字符串。

#### `getInstructions`

步骤 1：`loadCustom()`。  
步骤 2：若 `custom.instructions` 真值，push `## Workspace instructions\n…`。  
步骤 3：push `formatWorkspaceContext(custom, listSkills())`。  
步骤 4：push `## Workspace root\n${config.workspaceRoot}`。  
步骤 5：`[SERVER_INSTRUCTIONS.trim(), ...extra].join('\n\n')`。

### 4.5 `src/mcp/clients.js`

**用途：** 工作台 Bridge 卡片数据。**不是** MCP 协议的一部分。  
**结果：** `/api/status.clients[]`。

`CLIENTS` 数组 **7** 项，字段以源码为准：

| id | connectMode | needsPlus | needsTunnel | supportsMcp |
|---|---|---|---|---|
| `chat` | `local-chat` | false | false | false |
| `arena` | `paste-url` | false | true | true |
| `deepseek` | `extension-http` | false | true | true |
| `chat-plus` | `extension-http` | false | true | true |
| `generic` | `paste-url` | false | true | true |
| `chatgpt-free` | `unsupported-mcp` | false | true | false |
| `chatgpt-plus` | `oauth-connector` | true | true | true |

DeepSeek 项还带 `extensionId`、`storeUrl`。Chat Plus 项带 `repoUrl`（GitHub，无商店 ID）。每项有 `summary` 和 `steps[]`（UI 步骤文案）。

#### `hydrateClient(client, urls)`

步骤 1：`mcpUrl = urls.mcpUrl || ''`。  
步骤 2：`canonical = urls.mcpCanonicalUrl || mcpUrl.replace(/\/mcp\/[^/]+$/, '/mcp')`。  
步骤 3：按 `connectMode` 设 `prompt`：
- `paste-url` → `getBootstrapPrompt(mcpUrl)`（URL + 空行 + CONNECT_LINE）
- `oauth-connector` → 两行：规范地址 + 「认证：OAuth…」
- `extension-http` → **只有** `mcpUrl`（一行，给扩展 URL 框）
- `unsupported-mcp` → `''`
- 其它（含 `local-chat`）→ `prompt` 保持 `''`
步骤 4：返回展开后的 client；`mcpUrl` 在 oauth 模式下改成 canonical；附 `connectLine`。

#### `listClients(urls)`

`CLIENTS.map(hydrateClient)`。

#### `getClient(id, urls)`

`find` 按 id；找不到则 **`CLIENTS[1]`（arena）**。再 hydrate。

### 4.6 `src/mcp/resources.js`

`RESOURCE_DEFS` 8 个 URI：instructions / protocol / capabilities / config / workspace / memory / profile / clients。

#### `listResources`

直接返回 `RESOURCE_DEFS`。

#### `readResource(uri)` — `switch`

- `webagent://instructions` → `getInstructions()`，markdown。
- `webagent://profile` → `formatWorkspaceContext(loadCustom(), listSkills())`。
- `webagent://protocol` → 源码里写死的一组要点（传输、`tools/call` 失败是 `isError`、心跳 10s、apply_patch 可复用上次 read 的 sha256、`bash`/`cat`/`path` 别名）。
- `webagent://capabilities` → `tools N` + 每行 `- name: description`。
- `webagent://config` → server/version/workspace/`bridgeRunning`/tunnel/`installId`/mcpClients；**明文写 `secret omitted`**，不输出密钥。
- `webagent://workspace` → root、instructions、task 状态、最近 5 条 event 的 type。
- `webagent://memory` → `recall({ limit: 80 }).text`。
- `webagent://clients` → 用占位 `(mcp url)` hydrate 后列出每张卡的 summary。
- **default** → `null`。

### 4.7 `src/mcp/oauth.js`（379 行）

**用途：** OAuth 2.1 子集：动态注册 + 授权码 + PKCE S256 + 本机配对码。  
**结果：** Bearer，给 `POST /mcp`（路径无密钥）。  
**链条位置：** 仅 ChatGPT Plus 连接器。Arena / DeepSeek++ / Chat Plus **不走这里**。  
存储：四个内存 `Map` + 模块级 `pairing`。进程退出全丢。

TTL：配对 5 分钟、授权码 5 分钟、access 1 小时、refresh 7 天。

#### `now` / `randomToken(prefix, bytes=24)` / `randomPairingCode`

配对码字母表去掉 0/O/1/I：`ABCDEFGHJKLMNPQRSTUVWXYZ23456789`，8 字节映射成长度 8 的码。

#### `requestOrigin(req)`

若 `config.publicTunnelUrl` 真值 → 去掉末尾 `/` 后返回。  
否则 proto：`x-forwarded-proto` 或 `req.protocol` 或 `http`，取逗号前第一段。  
host：`x-forwarded-host` 或 `host` 或 `127.0.0.1:${config.port}`。  
返回 `` `${proto}://${host}` ``。

#### `issuePairing` / `snapshotPairing` / `ensurePairing`

issue：写新 pairing（attempts:0）。  
snapshot：没有或过期 → `{ code:null, expiresInSec:0, expired:true }`。  
ensure：过期或无 code 则 issue，否则返回当前 snap。

#### `consumePairing(code)`

步骤 1：过期或无 pairing → 抛，`status=400`，「配对码已过期…」。  
步骤 2：`attempts += 1`；`> 5` → `pairing=null`，抛 429「尝试次数过多」。  
步骤 3：trim + toUpperCase 不等于 `pairing.code` → 抛 400「不正确」。  
步骤 4：`pairing=null`（用过即废），返回 `true`。

#### `authorizationServerMetadata(origin)` / `protectedResourceMetadata(origin)` / `wwwAuthenticate(origin)`

返回发现文档对象；WWW-Authenticate 指向 `/.well-known/oauth-protected-resource`。

#### `registerClient(body)`

步骤 1：`redirect_uris` 必须是非空数组，否则 400。  
步骤 2：生成 `sccid_` / `sccsec_`，存入 `clients` Map。  
步骤 3：返回 client_id/secret、grant_types、response_types。

#### `s256(verifier)`

SHA-256，`digest('base64url')`。

#### `issueAccess(clientId)`

生成 `scat_` / `scrt_`，写入两个 Map，返回 rec。

#### `verifyAccessToken(token)`

步骤 1：假值 → `null`。  
步骤 2：`token === config.secretKey` → `{ kind:'secret', clientId:'url-secret' }`（**URL 密钥和 OAuth 走同一扇门**）。  
步骤 3：Map 里没有 → `null`。  
步骤 4：`accessExp < now()` → delete，返回 `null`。  
步骤 5：`{ kind:'oauth', clientId }`。

#### `revokeAll`

四个 Map `clear()`，`pairing=null`。

#### `escapeHtml` / `authorizeHtml(query, error)`

HTML 转义；返回配对页 HTML（含隐藏字段 client_id / redirect_uri / state / code_challenge）。

#### `completeAuthorize(body)`

步骤 1：未知 client_id → 400。  
步骤 2：`redirect_uri` 不在注册列表 → 400。  
步骤 3：`code_challenge_method` 缺省 S256；不是 S256 → 400。  
步骤 4：`consumePairing(body.pairing_code)`。  
步骤 5：发 `sccode_`，存 challenge 与过期。  
步骤 6：拼 redirect URL，带 `code`；若有 `state` 也带上。返回该字符串。

#### `handleToken(body)`

- `grant_type === 'authorization_code'`：
  1. code 不存在或过期 → 400，并不会发 token。
  2. **先 delete 该 code**（一次性）。
  3. client_id 不符 → 400。
  4. redirect_uri 不符 → 400。
  5. 无 `code_verifier` 或 `s256(verifier) !== rec.challenge` → 400「PKCE verification failed」。
  6. `issueAccess`，`tokenResponse`。
- `grant_type === 'refresh_token'`：
  1. 无效或 refresh 过期 → 400。
  2. 删旧 access 和 refresh。
  3. 再 `issueAccess`。
- 其它 grant → 400 `unsupported grant_type`。

#### `tokenResponse` / `sendError`

标准 token JSON；`err.status || 500`，400 时 `error: invalid_request`，否则 `server_error`。

#### 路由（全部挂在 oauth.router 上，由 index 挂到 app 根）

| 方法 | 路径 | 行为 |
|---|---|---|
| GET | `/.well-known/oauth-authorization-server` | metadata |
| GET | `/.well-known/oauth-protected-resource` | 资源元数据 |
| GET | `/.well-known/oauth-protected-resource/mcp` | 同上 |
| POST | `/oauth/register` 与 `/register` | `registerClient`，201；catch → sendError |
| GET | `/oauth/authorize` | `ensurePairing()`，返回 HTML |
| POST | `/oauth/authorize` | try `completeAuthorize` → 302；catch 用错误信息重绘 HTML |
| POST | `/oauth/token` | `handleToken` |
| POST | `/oauth/revoke` | 从两个 token Map delete `body.token || body.access_token`，始终 200 `{ revoked:true }` |

### 4.8 `src/mcp/server.js`（277 行）

**用途：** Streamable HTTP JSON-RPC 2.0。  
**结果：** `GET/POST /mcp` 与 `GET/POST /mcp/:secret`。  
**链条位置：** 云上客户端 →（隧道，若已有 publicTunnelUrl）→ 这里 → `callTool`。

常量 `SUPPORTED_PROTOCOL = ['2024-11-05','2025-03-26','2025-06-18']`。

#### `extractToken(req)`

顺序：
1. `Authorization` 匹配 `/^Bearer\s+/i` → 去掉前缀 trim。
2. `req.params.secret`。
3. 头 `x-mcp-secret`。
4. `req.query.secret`。
5. 否则 `''`。

#### `isAuthorized` / `rejectUnauthorized` / `requireAuth`

`oauth.verifyAccessToken(extractToken(req))` 真值才过。  
拒绝：设 `WWW-Authenticate`，401 JSON-RPC `error.code = -32000`。

#### `wantsSse(req)`

`Accept` 头包含 `text/event-stream`。

#### `sessionIdFor(req)`

头 `mcp-session-id`，否则随机 8 字节 hex。

#### `sendJsonRpc(req, res, payload, httpStatus=200)`

设 `Mcp-Session-Id`。若 SSE：写 `event: message\ndata: …\n\n` 后 end。否则 `res.status().json`。

#### `builtinPrompts` / `promptsFromCustom`

内置一个 `connect`。再拼 `custom.prompts` 的 id/name/description（content 截 120）。

#### `pickProtocol(params)`

`protocolVersion` 在支持列表里就用它，否则 `'2025-03-26'`。

#### `handleRpc(req)` — `switch (method)`

从 `req.body` 取 `id, method, params`。

**`initialize`**
1. `clientInfo = params.clientInfo || { name:'External-Agent' }`。
2. `touch(req, { clientInfo, key: name@ip })`。
3. `eventBus.broadcast('agent_connected', { clientInfo, ip })`。
4. 返回 `protocolVersion`、capabilities（tools/resources/prompts listChanged、logging 空对象）、`serverInfo`、**`instructions: getInstructions()`**。

**`notifications/initialized` / `notifications/cancelled` / `logging/setLevel`**  
返回 `{}`。

**`ping`**  
`touch(..., { incCall:true })`，返回 ok/ts/`busy:false`/session 摘要/host snapshot。

**`tools/list`**  
`touch(req)`，返回 `{ tools: getToolList() }`（**不传 mode**，列表含全部工具含 Code-only）。

**`tools/call`**
1. 无 `params.name` → 抛 `ProtocolError E_BAD_ARGS`。
2. broadcast `tool_call_start`，source `'Bridge-Remote'`。
3. `try`：`callTool(name, toolArgs || {}, remoteToolMode(params))` —— 默认 `'code'`；`params._meta.mode` 为 ask|plan|code 时用之。
4. 再 `clipJson`；`touch incCall`；broadcast 成功。
5. 把结果变成 MCP `content: [{ type:'text', text: clipText(...).text }]`，`isError:false`。
6. `catch`：`publicError`；`touch incCall+incFail`；broadcast 失败。**始终** `return { content:[{type:'text', text: JSON.stringify(info)}], isError:true }`。未知工具名、HASH_REQUIRED、STALE_FILE 都走这条。JSON-RPC `error` 只留给坏 jsonrpc / 未知 **method** / 缺 `params.name`。

**`resources/list`** → `listResources()`。  
**`resources/read`** → 未知 uri 抛 `E_NOT_FOUND`。  
**`prompts/list`** → `promptsFromCustom()`。  
**`prompts/get`**
- `name === 'connect'`：返回一条 user 消息，含 `getBootstrapPrompt('(this MCP server)')` + 让它读 instructions。
- 否则在 custom.prompts 里按 id 或 name find；没有抛 `E_NOT_FOUND`；有则把 `prompt.content` 当 user 文本。

**default** → `ProtocolError E_UNKNOWN_CMD`。

#### `hostStatus`

给 GET 非 SSE 用：online、tools 名列表、resources URI、完整 instructions、session、transports `['streamable-http','sse']`、auth `['url-secret','bearer','oauth']`。

#### `handlePost`

步骤 1：`jsonrpc !== '2.0'` → 400，RPC `-32600`。  
步骤 2：`try handleRpc`。若 method 以 `notifications/` 开头 → **HTTP 204 无 body**。否则 `sendJsonRpc` result。  
步骤 3：`catch`：`E_UNKNOWN_CMD` → HTTP 404 且 rpc `-32601`；其它协议 → `-32602`；否则 `-32603`（或 `err.rpcCode`）。HTTP 默认 200。

#### `handleGet`

若 SSE：写 `event: endpoint\ndata: /mcp\n\n`，每 15s 写 `: ping\n\n`，`req.on('close')` 清 interval。  
否则 `res.json(hostStatus())`。

路由：`GET/POST /` 与 `GET/POST /:secret` 都 `requireAuth` 后进上述 handler。

---

## 5. 工具层（真正改磁盘）

### 5.1 `src/utils/eventBus.js`

单例 `BridgeEventBus extends EventEmitter`。

- `addWsClient(ws)`：加入 Set；`close` 时 delete。
- `broadcast(type, payload={})`：
  1. 造 `{ type, timestamp ISO, payload }`。
  2. `logs.unshift`；超过 `maxLogs=500` 则 `pop`。
  3. 对每个 ws，`readyState === 1` 才 `try ws.send`，catch 空。
  4. `this.emit(type, payload)`。
- `getRecentLogs(limit=50)`：`logs.slice(0, limit)`。

### 5.2 `src/utils/diff.js`

`createUnifiedDiff(filePath, oldContent, newContent)`：
1. `jsdiff.createTwoFilesPatch` 路径 `a/` `b/`。
2. `diffLines` 累计 added/removed 行数（`split('\n').filter(Boolean).length`）。
3. 返回 `{ patch, additions, deletions, changes }`。

### 5.3 `src/tools/sensitive.js`

常量：`SENSITIVE_PATTERNS`（`.env`、密钥、`.ssh/`、`.webagent/config.json` 等）、`SENSITIVE_EXCEPTIONS`（`.env.example` 等）、`NOISE_NAMES`（`node_modules`、`.git`、`dist`…）。

#### `toPosix` / `globMatch`

路径统一 `/`，去掉前导 `./`。  
pattern 以 `/` 结尾：匹配该目录或前缀。  
否则把 `*` / `**` 编成正则，对整路径或 basename 测试。

#### `loadCustomPatterns`

读工作区 `.webagentignore`。不存在或 catch → `[]`。按行 trim，空行和 `#` 开头丢掉。

#### `isSensitive(relPath)`

basename 在例外列表 → **false**。  
先扫内置 patterns，命中 true。  
再扫自定义：以 `!` 开头命中则 **false**（放行）；否则命中 true。  
都没有 → false。

#### `isNoise` / `isHidden` / `assertNotSensitive`

noise：路径任一段在 `NOISE_NAMES`。空或 `.` 不是 noise。  
hidden = sensitive **或** noise。  
assert：sensitive 则抛 `ACCESS_DENIED_SENSITIVE_FILE`，`err.code='E_FORBIDDEN'`。

### 5.4 `src/tools/patchEngine.js`

**用途：** 路径沙箱 + sha256 + SEARCH/REPLACE。  
**链条位置：** Code 模式改文件的首选。

#### `computeHash(content)`

sha256 hex，utf8。

#### `toPosixRel` / `isInsideWorkspace` / `resolveSafePath(relPath)`

步骤 1：root 绝对化；incoming 把 `/` `\` 换成 `path.sep`；`path.resolve(root, incoming)`。  
步骤 2：`rel = path.relative(root, resolved)`，再 posix。  
步骤 3：若 posix 是 `..`、以 `../` 开头、或 `path.isAbsolute(rel)` → 抛 `Security error: path "…" is outside workspace root.`  
步骤 4：对 resolved 做 `realpath`（缺文件则对已存在的祖先 realpath 再拼回去）。真实路径仍必须落在工作区 realpath 之内，否则同样抛。这样 **symlink / junction** 指到工作区外会被拦住。  
步骤 5：posix 非空且不是 `.` → `assertNotSensitive(posix)`。  
步骤 6：返回逻辑绝对路径。

#### `parseSearchReplaceBlocks(patchText)`

正则 `<<<<< SEARCH` … `=====` … `>>>>> REPLACE`（5 个或更多 `<`/`=`/`>`），全局 exec，收集 `{ search, replace }`。

#### `applyPatch({ filePath, patch, expectedHash=null, dryRun=false })`

步骤 1：`fullPath = resolveSafePath(filePath)`。

**文件还不存在：**
2. 解析 blocks。默认 `newContent = patch`。若有 block 且 **第一个 search trim 为空** → `newContent = blocks[0].replace`（空 SEARCH 当新建）。
3. `dryRun` → 返回成功、`isNewFile:true`，不写盘。
4. `mkdirSync` 父目录，`writeFileSync`。
5. 对空旧内容做 unified diff，broadcast `file_patched`（含 newHash）；`rememberHash`。
6. 返回 success / isNewFile / newHash / `diffSummary: +N -0`。

**文件已存在：**
7. 读全文，算 `currentHash`。没传 `expectedHash` 则用 `recalledHash(filePath)`（上次 `read_files` / 成功补丁 / 写入记下的 sha256）。
8. 仍无 hash 且非 `dryRun` → `ProtocolError E_BAD_ARGS`，消息 `HASH_REQUIRED`，`detail.currentHash`。
9. 若有 `expectedHash`，且既不等于也不当前缀 → `ExecutionError E_STALE_FILE`，detail 含 currentHash。
10. 解析 blocks。
11. **有 blocks：** 对每个 block：
    - 当前内容、search、replace 都把 `\r\n` 换成 `\n`。
    - 若 `includes(normalizedSearch)` → `replace` 一次（String.replace 默认只换第一处）。
    - **否则** search `trim` 后非空且 includes(trimmed) → 用 trimmed 去换 `normalizedReplace.trim()`。
    - 再否则抛 `Patch conflict: SEARCH block #i could not be found`。
12. **无 blocks：**
    - 若 `patch.startsWith('--- ')` 且 `includes('@@')` → `jsdiff.applyPatch`；`applied === false` 抛 unified diff 失败。
    - 否则 **整段 patch 当作新文件内容**（覆盖）。
13. 算 diff。`dryRun` → 返回 summary + diff，不写。
14. 写 `${fullPath}.tmp.${Date.now()}`，再 `renameSync` 到目标（同盘原子替换）。
15. `rememberHash`；broadcast `file_patched`，返回 newHash、diffSummary、diff。

### 5.5 `src/tools/fileOps.js`

#### `readFiles({ filePath, paths, offset=1, limit=400 })`

步骤 1：paths 数组先推进 list，再 push 单个 filePath。  
步骤 2：list 空 → 抛 `read_files requires filePath or paths[]`。  
步骤 3：恰好 1 个 → 直接 `readFile`（单对象，不是 `{ files:[] }`）。  
步骤 4：多个：逐个 try `readFile`；catch 成 `{ filePath, error: err.message }`。返回 `{ files: [...] }`。

#### `readFile({ filePath, offset=1, limit=400 })`

步骤 1：resolveSafePath（含敏感拦截）。  
步骤 2：不存在 → 抛 File not found。  
步骤 3：是目录 → 抛去用 list_dir。  
步骤 4：读 utf8，hash **全文**，按 `\r?\n` 分行。  
步骤 5：窗口：`startIdx = max(0, offset-1)`，`endIdx = min(len, start+limit)`。  
步骤 6：每行格式 `` `${行号}: ${内容}` ``（行号 1-based）。  
步骤 7：broadcast `file_read`；`rememberHash(filePath, hash)`。返回 filePath/totalLines/offset/limit/hash/content。

#### `deleteFile({ filePath, confirm=false })`

无 filePath 抛。resolve。相对路径空或 `.` → 拒绝删工作区根。`confirm` 假 → `E_BAD_ARGS`（须 `confirm=true`）。不存在抛。  
是目录：`readdir` 非空 → 抛「not empty」；空则 `rmdirSync`。  
是文件：`unlinkSync`。  
broadcast `file_deleted`；`forgetHash`。返回 type directory/file。

#### `renameFile({ from, to, filePath, dest })`

`srcRel = from || filePath`，`destRel = to || dest`。缺一抛。  
两边都 resolveSafePath。源不存在抛。目标已存在抛。  
`mkdirSync` 目标父目录，`renameSync`。broadcast `file_renamed`。

#### `writeFile({ filePath, content, expectedHash, confirmOverwrite=false, confirm_overwrite=false })`

已存在且未 `confirm_overwrite`：若 `expectedHash` 或 `recalledHash` 仍等于当前 sha256 则放行；否则 `E_BAD_ARGS`（detail.currentHash）。若传了 expectedHash 但不匹配 → `E_STALE_FILE`。然后 mkdir，写 `.tmp.${Date.now()}` 再 `renameSync`（失败则删 tmp）；broadcast `file_written`；`rememberHash`。

#### `listDir({ dirPath='.', recursive=false, maxDepth=3 })`

不存在抛。内嵌 `scan(currentPath, currentDepth)`：
- `currentDepth > maxDepth` → `[]`。
- 每个 entry：hidden 则 `continue`。
- 目录：若 `recursive && currentDepth < maxDepth` 才扫 children。
- 文件：带 size、mtime。  
从 depth=1 开始 scan。返回 `{ dirPath, items }`。

#### `grepSearch({ query, searchPath='.', isRegex=false, caseSensitive=false, limit=20, cursor=0 })`

步骤 1：resolve 起点。  
步骤 2：编正则：`caseSensitive` 则 `'g'` 否则 `'gi'`；`isRegex` 假则先转义 query。`catch` → 抛 Invalid regex。  
步骤 3：目录则递归 `searchInDir`：hidden skip；文件 try 读，按行 `pattern.test`，命中 push `{ file, line, content: trim }`，并 `pattern.lastIndex=0`；读失败 catch 空（跳过二进制等）。  
步骤 4：起点是文件则只搜该文件。  
步骤 5：分页：`pageSize = min(100, max(1, Number(limit)||20))`，`start = max(0, Number(cursor)||0)`，`nextCursor` 还有剩余则为 `start+page.length` 否则 `null`。  
返回 `{ query, totalMatches, matches: page, cursor, nextCursor }`。

### 5.6 `src/tools/executor.js`

模块状态：`commandSequence`、`commandStore` Map、`children` Map、`MAX_CAPTURE = 200*1024`。

#### `killChild(child, force=false)`

无 pid return。  
win32：`taskkill /pid /t /f`。catch 空。  
非 Windows：先 `process.kill(-pid)` 杀进程组，失败再 `child.kill`。

#### `workingDirFrom(cwd)`

走 `resolveSafePath`（含真实路径）。逃出工作区抛 cwd outside。

#### `publicRecord(rec, tail)`

stdout/stderr 各 `slice(-limit)`，limit 在 500 与 MAX_CAPTURE 之间，默认 8000。  
running 时 `suggestedWaitMs` 保留，hint 让去 poll；否则 suggestedWaitMs=0，hint undefined。

#### `startProcess({ command, cwd='.', timeoutSec=30 })`

步骤 1：`execId = ++commandSequence`，timeout 至少 1000ms。  
步骤 2：rec 入 store，status `running`，`suggestedWaitMs = min(4000, max(800, round(timeoutMs/8)))`。  
步骤 3：broadcast `command_started`。  
步骤 4：win32 → `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command`；否则 `/bin/bash -c`。非 Windows `detached:true`。env 加 `CI=true`、`TERM`、`FORCE_COLOR=1`。  
步骤 5：timer：到点 `isTimeout=true`，`killChild`；再 2s 后若还在 children 里 `killChild(force true)`。  
步骤 6：stdout/stderr `data`：追加，超 MAX_CAPTURE 截尾，broadcast `command_output`。  
步骤 7：`error`：清 timer，status `error`，reject `Failed to start command`。  
步骤 8：`close`：清 timer；若当前不是 `cancelled` 则 status 为 timeout 或 `done`；broadcast `command_finished`；resolve `publicRecord`。  
返回 `{ rec, done }`（done 是 Promise）。

#### `executeCommand` / `startCommand`

execute：返回 `done`（等到结束）。  
start：`done.catch` 把 status 设 error 并拼 stderr；**立即**返回 `{ execId, status:'running', command, suggestedWaitMs, hint }`。

#### `getCommandOutput({ execId, commandId, tail })`

id = `String(execId || commandId || commandSequence)`。没有 rec → `{ found:false, message }`。否则 publicRecord。

#### `cancelCommand({ execId })`

没有 rec → found false。  
`status !== 'running'` 或没有 child → cancelled false。  
否则 force kill，status `cancelled`。

#### `sendCommandInput`

**恒定**返回 `{ ok:false, message: 'Interactive PTY is not enabled…' }`，不写 stdin。

#### `wait({ ms=800 })`

delay clamp 到 0–15000，`setTimeout` 后 `{ waitedMs }`。

### 5.7 `src/tools/gitOps.js`

#### `notGitResult(extra={})`

返回 `{ ok:true, available:false, git:false, ... hint 不要擅自 git init }`。

#### `git(args, timeoutMs=8000)`

`spawnSync('git', ['-c','color.ui=never', ...args], { cwd: workspaceRoot, encoding utf8, timeout, maxBuffer 512KB })`。  
启动失败：ENOENT/`not found` → `Error` `code='GIT_UNAVAILABLE'`，其它 `E_INTERNAL`。  
`status !== 0`：127 / not a git repository / command not found → `GIT_UNAVAILABLE`；否则 `E_INTERNAL` 截 800 字。  
成功返回 stdout。

#### `gitStatus`

try porcelain v1 -b；成功 `{ ok, available:true, git:true, branch, dirty, summary, files≤80, truncated }`。catch 仅 `GIT_UNAVAILABLE` → `notGitResult()`，其它再抛。

#### `gitDiff({ filePath, staged=false, stat=false })`

args 从 `diff` 起；staged 加 `--cached`；stat 加 `--stat`；然后 `--`；有 filePath 则 `resolveSafePath` 后相对路径（空则 `.`）。  
输出行数 cap：stat 80 否则 200。同样把 `GIT_UNAVAILABLE` 收成 `notGitResult`。

### 5.8 `src/tools/findFiles.js`

#### `globToRegExp(glob)`

默认 `**/*`。反斜杠改 `/`。先转义正则特殊字符，再 `**`→`.*`，`*`→`[^/]*`，`?`→`[^/]`。整串 `^…$`。

#### `findFiles({ glob='**/*', searchPath='.', maxResults=40 })`

起点不存在抛。内嵌 `walk`：readdir 失败 catch 直接 return。真实路径在工作区外或是符号链接则 skip。hidden skip。目录递归。文件：`re.test(rel) || re.test(name) || glob==='**/*'` 再套一层相同条件才 push。满 maxResults 停止。  
起点是文件则只 push 它自己。返回 `{ glob, searchPath, total, files }`。

### 5.9 `src/tools/skills.js`

#### `skillRoots`

两个：`.webagent/skills` 与工作区根下 `skills/`。

#### `listSkills`

每个 root 不存在 continue。对每个名字：必须是目录且有 `SKILL.md` 才 push `{ name, path 相对, preview: 前 240 字 }`。

#### `loadSkill({ name })`

无 name → `{ skills, hint }`（有/无 skill 两套 hint）。  
有 name 找不到 → `{ found:false, available, hint }`。  
找到 → 读 SKILL.md **最多 8000 字**，`{ found:true, name, path, content }`。  
演示工作区有 `fix-tests` / `review` / `docs-sync`（约定：改功能必须同步改说明书；动到设计理由时改 `架构导读.md`）。

### 5.10 `src/tools/workspaceInfo.js`

#### `workspaceInfo`

1. loadCustom，resolveEnvironment / resolveTechStack。  
2. `try gitStatus` → `{ available:true, ... }`；`catch` → `{ available:false, error: err.message }`。  
3. `try listDir({ dirPath:'.', recursive:false, maxDepth:1 })` 映射 name/type/path；catch → `[]`。  
4. `try` 读工作区 `package.json` 的 `name`；不存在或坏 JSON → `null`。  
5. 返回 root、packageName、git（只把 available/branch/error 放出）、environment、techStack、skills 名列表、topLevel、hint 不要 dump 整树。

### 5.11 `src/tools/progressTracker.js`

模块级 `currentTaskState` 初始 idle。

#### `reportProgress({ message, percentage=0, stepName='' })`

`status`：percentage≥100 → `completed` 否则 `in_progress`。progress clamp 0–100。stepName 空则保留旧值。broadcast `progress_updated`。返回 success/progress/stepName/message。

#### `setTodos({ todos=[] })`

每项补 id（缺省 `todo-N`）、title（`title||text||description||'Task item'`）、status 默认 pending。broadcast `todos_updated`。

#### `getTaskState` / `resetTaskState`

直接返回；reset 回到 idle 并 broadcast。

### 5.12 `src/tools/planRound.js`

**用途：** 进程内存里的一次 Plan 多模型回合（不落盘）。  
**源码事实：** 串行分支；满额拒绝；≥2 才能总结。不调 HTTP。

- `clampMax`：非有限 → 4；否则 2–8。
- `start`：空任务抛 `E_PLAN_NO_TASK`。
- `addBranch`：无回合 `E_PLAN_NO_ROUND`；已合并 `E_PLAN_MERGED`；已满 `E_PLAN_FULL`。
- `markMerged`：少于 2 支 `E_PLAN_NEED_TWO`。
- `snapshot`：给 `/api/status.planRound` 与 NDJSON `planRound` 事件。

### 5.12b `src/tools/consensusEngine.js`

**用途：** **没有 API Key** 时的本机单支草案 + 原文拼接。  
**源码事实：** 不调用任何 LLM API。`simulated: true`、`consensusReached: false`、`agreementRate: null`。summary 写明「不是假的 97% 投票」。有 Key 的真分支在 `runChat.js` 调 `runOpenAI`。

#### `draftLocalBranch` / `mergeLocalBranches` / `runMultiModelConsensus`

`draftLocalBranch` 用 facts 拼中文草案。`mergeLocalBranches` 把各支 `answer` 原文拼进 canonical。`runMultiModelConsensus` 无活回合时出一份草案再拼接（给 `POST /consensus/run`）。

### 5.13 `src/tools/index.js`（435 行）

**用途：** 注册表 + 模式锁 + 危险命令闸。  
**结果：** `getToolList` / `callTool`。  
**链条位置：** MCP 与 Chat 的唯一执行入口。

`tool(def)` 原样返回。另有 `normalize.js`（`bash`→`run_command`、`cat`→`read_files`、`path`→`filePath`、`"true"`→布尔）和 `readCache.js`（路径→sha256，落盘 `.webagent/read-hashes.json`；`reset-round` 会 `resetHashes`）。`DANGEROUS_RE` 还匹配 `git push` / `git reset --hard` / `git checkout --` / `git clean -f` / `format x:` / `del /s` / `rd /s` / `Remove-Item -Recurse` / `drop database` / `iex` / `iwr` / `curl|wget` 管道进 shell / `powershell -enc` / `certutil -urlcache` / `bitsadmin` / `reg add` / `net user` / `schtasks`。

`TOOLS` 现登记 **25** 个名字（测试锁长度）。摘要：

| name | aliases | mode | handler |
|---|---|---|---|
| ping | | ask,plan,code | pingHost |
| workspace_info | | 同上 | workspaceInfo |
| get_capabilities | | 同上 | getCapabilities |
| get_logs | | 同上 | getLogs（maxLines clamp 1–200） |
| get_task_status | | 同上 | getTaskStatus |
| remember | | 同上 | remember |
| recall | | 同上 | recall |
| list_directory | list_dir | 同上 | listDir |
| find_files | | 同上 | findFiles |
| search_files | grep_search | 同上 | grepSearch |
| read_files | read_file | 同上 | readFiles |
| git_status | | 同上 | gitStatus |
| git_diff | | 同上 | gitDiff |
| load_skill | | 同上 | loadSkill |
| apply_patch | | **code** | applyPatch |
| write_file | | **code** | writeFile |
| delete_file | | **code** | deleteFile |
| rename_file | move_file | **code** | renameFile |
| run_command | execute_command | **code** | executeCommand |
| start_command | | **code** | startCommand |
| get_command_output | | ask,plan,code | getCommandOutput |
| cancel_command | | **code** | cancelCommand |
| wait | | ask,plan,code | wait |
| report_progress | | **plan,code** | reportProgress |
| set_todos | | ask,plan,code | setTodos |

加载时把 name 和 aliases 都放进 `toolRegistry` Map。

#### `getToolList(currentMode=null)`

`currentMode` 假 → 全部；真则 `t.mode.includes(currentMode)`。映射为 `{ name, description, inputSchema }`（不含 handler/mode）。

#### `callTool(name, args={}, currentMode=null)`

步骤 1：`resolveToolName(name)`；registry 先查 resolved 再查原名。没有 → `E_UNKNOWN_CMD`，消息含 `Available:` 列表。  
步骤 2：`currentMode` 真 **且** 该工具 mode 列表不含它 → `E_BAD_ARGS`，文案让切 CODE。远程 MCP 默认传入 `'code'`；仅当第三参为 `null`/`''` 才跳过锁。  
步骤 3：`normalizeToolArgs`。若工具名是 `run_command` 或 `start_command`，且命令匹配 `DANGEROUS_RE`，且没有 `confirm_dangerous` → `E_BAD_ARGS`。  
步骤 4：`await toolDef.handler(input)`。  
步骤 5：若 `result && result.isTimeout`：打 `code='E_TIMEOUT'`，`suggestedWaitMs=0`。  
步骤 6：`return clipJson(result)`。

`getTaskStatus`：running 则 suggestedWaitMs=2000，`etaSeconds = max(1, round((100-progress)/10))`；否则 0。

---

## 6. 本机 Chat 与 REST

### 6.1 `src/agent/providers.js`

#### `normalizeBase(url)`

trim，去掉末尾 `/`，再去掉末尾 `/chat/completions`（i 标志）。

#### `guessCaps` / `guessContext`

id 小写。caps 恒含 `'工具'`；匹配 vision|image|gpt-4o|flash|pro 加视觉；video 加视频。  
context：video|image → `1.3M`；mini|haiku|lite → `128K`；否则 `1.3M`。

#### `listRemoteModels(baseUrl, apiKey)`

无 base → 抛「API Endpoint URL 不能为空」。无 key → 「API Key 不能为空」。  
`GET ${base}/models` Bearer。`!resp.ok` 抛 HTTP 状态 + 正文前 200。  
JSON.parse 失败 → 「不是 JSON」。  
`data.data` 数组，否则 `data` 本身是数组，否则 `[]`；空则抛。  
hostname 去掉 `api.` 取第一段当 group。每项 `{ id, name, group, contextSize, caps, pricing }`。

### 6.2 `src/agent/openai.js`

#### `toolLabel` / `systemPrompt(mode)`

Ask/Plan：只读锁文案。Code：允许 patch/命令。  
回复语言：follow-user / en / 默认中文。  
Windows 或 powershell → 提示 Prefer PowerShell。  
plan 再加一句不要改仓库。  
拼 custom.instructions 与 `formatWorkspaceContext`。

#### `temperatureFor(level)`

`low→0.1`，`medium→0.4`，其它 `0.7`。不盲发未知厂商字段。

#### `runOpenAI({ mode, message, history=[], emit, model, thinkLevel, allowTools=true, extraSystem })`

步骤 1：baseUrl 去尾 `/`，空则抛。`emit` 缺省空函数。  
步骤 2：`allowTools` 真才把 `getToolList(mode)` 转成 function tools（Ask/Plan 列表里**没有** apply_patch）。  
步骤 3：messages = system（可拼 extraSystem）+ history 最后 12 条 + 当前 user。  
步骤 4：最多 **10** 轮：
- emit status。
- `POST ${base}/chat/completions`，`temperature: temperatureFor(thinkLevel)`；有 tools 才带 `tool_choice:'auto'`。
- `!resp.ok` 抛 HTTP + 正文前 240。
- JSON 失败抛。无 `choices[0].message` 抛。
- 把 msg push 进 messages。
- **若有 tool_calls**：最多 8 个；arguments JSON.parse 失败当 `{}`；`try callTool(name, args, mode)` 成功则 role tool 内容 `JSON.stringify(result).slice(0,12000)`；catch 则 `ERROR: message`。然后 `continue` 下一轮。
- **无 tool_calls**：emit `message`（content 空则「（无文本输出）」），**`return { text }`**。
步骤 5：10 轮用尽：emit「已达到最大工具轮次。」并 `return { text }`。

### 6.3 `src/agent/runChat.js`（597 行）

**用途：** `POST /api/chat` 的实现。  
**结果：** 通过 `emit(type, data)` 写 NDJSON。

#### 辅助

- `flattenDir` / `countFiles`：递归摊平。
- `toolLabel`：给 UI 的短标签（Explored / Found N files / Read path / …）。失败时 list/read 也给固定英文标签。
- `timedTool(emit, mode, name, args)`：计时 `callTool`；成功/失败都 emit `tool`；返回 `{ ok, result|error, durationMs }`。**不把异常抛出**。
- `keywordsFrom(message)`：按空白和中文标点切，去掉停用词，长度≥2，最多 6 个。
- `pickExisting(relPaths)`：工作区里存在且是文件的留下。
- `detectTestCommand`：先 `resolveTechStack(loadCustom()).testCommand`；否则 package.json scripts.test → `npm test`（这里**写死 npm**，不用探测到的包管理器）；再 pytest / cargo / go；再若存在 `tests/` 目录 → `{ cmd:'npm test', kind:'guess' }`；都没有 → `null`。
- `extractPatch`：消息里第一段 `<<<<<<< SEARCH` … `>>>>>>> REPLACE`。
- `extractWriteIntent`：同时有代码围栏和「写入|创建|write|create + 带扩展名路径」才返回 `{ filePath, content }`。
- `clip` / `stripLineNumbers`：去 `^\d+:\s?`。

#### `explore(emit, mode, message)`

1. `list_directory` recursive maxDepth 3，最多收 80 个文件路径。  
2. `git_status`。  
3. `find_files **/*` maxResults 100，合并进 files。  
4. 若 keywords 非空：`search_files` query 为前 3 个 keyword 用 `|` 拼，`isRegex:true`，limit 30。  
5. 候选 README/package.json/pyproject/Cargo/go.mod 里存在的先读；再从 files 补到最多 6 个（跳过 `SKIP_DIRS` 顶层）。  
6. `read_files` limit 120；readme / package.json 填进 facts。  
7. `detectTestCommand` 写入 facts.testCmd（默认已是 `npm test`）。  
返回 facts。**explore 不写文件。**

#### `summarizeAsk(message, facts)`

拼工作区路径、用户问题、文件列表（最多 30）、README/pkg 摘要、测试命令，结尾声明这是只读 Ask。

#### `runBuiltin(payload, emit)`

步骤 1：mode 默认 `ask`。先 `set_todos` 三条。  
步骤 2：`facts = await explore(...)`。再更新 todos。  
步骤 3：`mode === 'plan'`（仅当误入 builtin）：emit 一段「请走 Plan 入口」摘要，**return**。真正的分支在 `runPlanRound`。  
步骤 4：`mode === 'ask'`：todos 全 completed，emit summarizeAsk，**return**。  
步骤 5：到这里视为 **code**：
- 若 `extractWriteIntent` 有值 → `write_file`。
- 若 `extractPatch` 有值：从消息匹配 `file|文件` 路径，否则 `facts.files[0]`；有 target 则先 `read_files` 取 hash，成功才 `apply_patch`。
- `detectTestCommand()` 有则 `run_command` timeoutSec 60，stdout+stderr 进 facts.testOutput。
- 再 set_todos completed。
- emit 摘要：是否写入、是否尝试补丁、测试输出 clip 1200；并写明「内置循环没有大模型」。

#### `resolvePlanAction` / `runPlanBranch` / `runPlanRound`

- 显式 `planAction` 优先；`multiModel.enabled===false` → `single`；空消息且未合并的活回合 → `branch`；否则 `start`。
- 能调模型：`runOpenAI` mode plan，`simulated:false`。否则复用 `live.facts` 或 `explore`，`draftLocalBranch`，`simulated:true`。
- `merge`：少于 2 支 emit error；合并主模型 `auto`→active；能调则 `runOpenAI` 读各支原文（`mergeAllowsRead===false` 则 `allowTools:false`），`agreementRate:null`；否则 `mergeLocalBranches`。然后 `markMerged`、todos、emit consensus。
- 空任务 `start` 抛 `E_PLAN_NO_TASK`（catch 成 error 事件）。

#### `runChat(payload = {}, emit)`

步骤 1：`send = typeof emit === 'function' ? emit : payload.emit`。  
步骤 2：`mode==='plan'` → `runPlanRound`。  
步骤 3：否则能调 `payload.modelId` 或 active（同时有 apiKey、baseUrl、modelId，且 protocol 不是 builtin）→ `runOpenAI`（失败改 builtin）。  
步骤 4：**否则** `runBuiltin(payload, send)`。导出 `{ runChat, planRound }`。

### 6.4 `src/api/routes.js`（378 行）

**用途：** 工作台 / 插件 REST，挂在 `/api`。

#### `publicOrigin` / `mcpOrigin` / `mcpInfo`

publicOrigin：跟 oauth.requestOrigin 类似，但 fallback host 用 `workbenchPort`。  
mcpOrigin：有 `config.publicTunnelUrl` 用它，否则 publicOrigin。  
mcpInfo：拼 `/mcp/${secretKey}`、canonical `/mcp`、prompt、clients、pairing、`tunnel.snapshot()`。

#### 各路由（逐步）

**GET `/status`**  
拼 online、端口、workspace、tools、taskState、recentLogs 40 条、bridgeRunning、mcpInfo 展开、models（apiKey 变成 `hasKey` 布尔）、activeModelId、multiModel、**`planRound: planRound.snapshot()`**、bridgeAccount、mcpSession。

**POST `/bridge/reset-secret`**  
记下 oldSecret → `generateNewSecret()`（内存 + `config.json`）→ `oauth.revokeAll()` → broadcast `secret_rotated` → json success + mcpInfo。

**POST `/bridge/start`**（async）
1. `store.load()`；`!loggedIn || !deviceAuthorized` → **403**，Chat 不受影响。
2. provider = body.tunnelProvider 或 cfg 或 `'cloudflare'`。
3. `store.patch({ bridge: { tunnelProvider: provider } })`。
4. `config.bridgeRunning = true`，`config.tunnelProvider = provider`。
5. `oauth.ensurePairing()`。
6. `provider === 'cloudflare'` 时 **`await tunnel.startQuickTunnel({ port: config.port })`**。catch 记下 `tunnelError`（无二进制、超时、spawn 错），**不** 500。
7. `mcpInfo`：有 `tunnel.url` 则 note「Quick Tunnel 已就绪」；有 `tunnelError` 则 note 带错误 +「MCP 暂走当前页面源」；否则 note 写未启动 Quick Tunnel（Named / ngrok 不 spawn）。
8. broadcast `bridge_started`（含 tunnelUrl / tunnelError），json success、running、provider、tunnelError、note、mcpInfo。

**POST `/bridge/stop`**  
**`tunnel.stopTunnel()`**；`config.bridgeRunning = false`；broadcast；json 带 mcpInfo。

**POST `/bridge/reset-round`**  
`session.reset()` + `resetHashes()` + broadcast `bridge_round_reset`。

**POST `/consensus/run`**  
`runMultiModelConsensus({ taskDescription })`；失败 500。

**POST `/tool/call`**  
body `{ name, arguments, mode='code' }`。broadcast start source `Chat-${mode}` → `callTool(name, toolArgs, mode)` → 成功 200；catch 400 `{ error: err.message }`。

**POST `/chat`**  
设 NDJSON、`X-Accel-Buffering: no`，flushHeaders。  
`emit` = `res.write(JSON.stringify({ type, ...data })+'\n')`。  
try `runChat({ mode, message, history, modelId, thinkLevel, planAction, emit })`（emit 在对象里，由 `runChat` 取出）然后 emit `done`；catch emit `error`。最后 `res.end()`。

**POST `/tasks/reset`** → `resetTaskState()`。

**GET `/files/tree`** → `callTool('list_directory', { dirPath:'.', recursive:true, maxDepth:5 }, 'ask')`。

**GET `/files/content`**  
query.path → resolveSafePath。不存在或是目录 → 404。否则全文 + hash + size，并 `rememberHash`。

**PUT `/files/content`**  
body.path 与 content 必须；content 必须是 string。`callTool('write_file', { confirm_overwrite:true, expectedHash }, 'code')`。敏感/越界 400；`STALE_FILE` 409。工作台保存走这一条，不再直接 `writeFileSync`。

**GET `/skills`**  
扫两个 skills 根目录，有 SKILL.md 则 preview 前 400 字。  
（与 `tools/skills.listSkills` 类似，但这里不要求 `statSync(dir).isDirectory()`。）

**POST `/providers/probe`** → `listRemoteModels`，失败 400。

**GET `/models`** → models 的 apiKey 显示成 `••••` 或 `''`。

**POST `/models`**  
可改 activeModelId；可整表替换 models；可 upsert 单个 `body.model`；可合并 multiModel。然后 `store.save(cfg)`（**整份 cfg**，不是 patch）。

**GET `/logs`** → 80 条。  
**GET `/profile/detect`** → detectEnvironment + detectTechStack + listSkills。  
**GET/PUT `/customizations`** → load / patchCustom。

**POST `/skills`**  
name trim，非单词字符变 `-`，去首尾 `-`，最长 40；空则 400。content 默认一个中文模板。`callTool('write_file', { confirm_overwrite:true }, 'code')` 写 `.webagent/skills/<name>/SKILL.md`。

**POST `/bridge/login`**  
本机演示授权：patch `loggedIn`/`deviceAuthorized` true，`provider`/`license`=`local-demo`，`username`=`local`。**不**请求 GitHub，body 里的 provider 也不再写入。

**POST `/bridge/logout`**  
patch loggedIn false；**`tunnel.stopTunnel()`**；`config.bridgeRunning=false`。

---

## 7. 隧道 `src/tunnel/cloudflared.js`

**用途：** 找二进制、拉 Quick Tunnel、从日志解析 `https://….trycloudflare.com`。  
**结果：** 成功则 `config.publicTunnelUrl = url`。  
**链条位置：** 模块被 `routes.js` require。`/api/status` 暴露 `snapshot()`；**`POST /bridge/start`（cloudflare）`await startQuickTunnel`**；`/bridge/stop` 与 `/bridge/logout` 调 `stopTunnel`。Named / ngrok 不进本文件。

模块状态：`child`、`quickUrl`。`URL_RE` 匹配 trycloudflare.com。

#### `parseTunnelUrl(chunk)`

正则命中则去尾 `/`，否则 null。

#### `findCloudflared`

1. `CLOUDFLARED_PATH` 存在则用。  
2. `where`（win）或 `which`，stdout 按行找 `existsSync`。  
3. 再猜常见安装路径（Windows LOCALAPPDATA / Program Files；Unix brew/usr）。  
4. 都没有 `null`。

#### `installHint`

按平台返回安装说明字符串（含 winget / GitHub releases）。

#### `stopTunnel`

child 未 killed → SIGTERM；1.5s 后再 SIGKILL。清空 child、quickUrl、`config.publicTunnelUrl`。

#### `startQuickTunnel({ port=config.port, timeoutMs=25000 })`

1. 先 `stopTunnel()`。  
2. 找不到二进制 → **Promise.reject**，`err.code='E_NO_CLOUDFLARED'`，message=installHint。  
3. spawn `cloudflared tunnel --url http://127.0.0.1:${port} --no-autoupdate`。Windows 上若二进制是 `.cmd/.bat` 才 `shell:true`。  
4. 25s 内未解析到 URL：stopTunnel，reject 超时文案。  
5. stdout **和** stderr 都进 `onData`：broadcast `tunnel_log`（chunk 截 400），`parseTunnelUrl(buf)` 成功则 settled、设 quickUrl 与 `config.publicTunnelUrl`、broadcast `tunnel_ready`、resolve `{ url, binary, target }`。  
6. `error`：未 settled 则 reject「无法启动」。  
7. `exit`：未 settled 则 reject 退出码 + installHint。

#### `snapshot`

`{ binary: findCloudflared(), url: quickUrl || config.publicTunnelUrl, running: Boolean(child && !child.killed) }`。

进程 `exit`/`SIGINT`/`SIGTERM` 会 stopTunnel（SIGINT/TERM 再 `process.exit(0)`）。

---

## 8. 工作台 UI `webagent-core/workbench/`

**用途：** 浏览器里的壳。**不直接 fs。**  
**结果：** CHAT 流、BRIDGE 卡片、复制 MCP、内置假浏览器页。  
**链条位置：** 路径 A 前端；路径 B 的「复制地址 / 看工具卡片」。

入口 `app.js`（ES module）import `js/state.js`、`js/dom.js`、`js/tabs.js`、`js/chat.js`、`js/bridge.js`、`js/settings.js`、`js/bind.js`。跨文件调用走 `ui` 袋，行为与拆开前同一套函数。行号以 [workbench/README.md](./webagent-core/workbench/README.md) 为准。

`$` / `$$` 是 querySelector。  
`SITES`：chatgpt / arena / deepseek / workbuddy / trae / qwen / manus / shunova 的外链。  
`state` 初值：`mode:'code'`，一条 welcome tab，`loggedIn:true`，`selectedClient:'arena'`，`stayOnBridge:false`，`planRound:null`。

下面按函数直译（HTML 模板不逐字抄，分支写全）。函数现分文件，名称与分支未改。

#### `toast` / `escapeHtml` / `renderMd`

toast 2.2s 隐藏。escape 五字符。renderMd：先 escape 再极简替换 code fence / inline / bold / 标题 / `- ` 列表 / 换行变 `<br>`（不是完整 markdown）。

#### `termLine` / `openModal` / `closeModal` / `showPage` / `setRight`

终端追加一行。模态显示某设置页。右侧切 chat/bridge（CSS class `hidden` / `on`）。

#### `paintTabs` / `activateTab` / `closeTab`

画标签。activate：按 kind 显示 welcome / browser / agent / diff / file（Monaco 或 fallback textarea）。只剩 1 个 tab 时 close 直接 return。

#### `openAgentWindow` / `openDiff` / `paintDiff` / `ensureWelcome`

没有 agent tab 就加。diff：`+` 且非 `+++` 绿色，`-` 且非 `---` 红色，`@@` hunk。

#### `openFile(filePath)`（async）

已有 tab 则激活。否则 `GET /api/files/content?path=`；`!res.ok` toast 后 return；否则新 tab。

#### `langFor` / `applyEditor`

按扩展名；有 monaco 则 createModel，否则写 textarea。

#### `treeHtml` / `loadTree`

递归 HTML。点击目录折叠（chev ▾/▸）；点击文件 `openFile`。recent-list 最多 6 个文件。

#### `emptyChat` / `paintChat` / `summarizeTool` / `renderMsg` / `pushMsg`

无消息显示空态四个设置链接。  
renderMsg 分支：`user` / `status` / `tool`（点 header 切换 pre 显示）/ `consensus`（标题「多模型总结」；合并 + 各分支 tab；「按总结切到 Code 执行」会 `setAgentMode('code')` 并 `sendChat('按已对齐方案执行：…')`）/ 默认 assistant（renderMd）。

#### `paintPlanComposer` / `sendChat(text, opts={})`（async）

Plan 时显示 `分支 n/max`；`canMerge` 才显示总结钮。

1. `state.sending` 真 → return。  
2. text 为 null/undefined 才从输入框取值并清空。  
3. 若智能体窗可见则用 `#agent-mode`，否则 `#mode-select`。  
4. Ask/Code 空输入直接 return；Plan 空输入仅 `canBranch` 时当 `planAction:'branch'`；`planAction:'merge'` 不重打任务。  
5. `stayOnBridge = !!opts.stayOnBridge`；非 stay 则切到 chat。  
6. history 取最后 12。  
7. `POST /api/chat` JSON `{ mode, message, history, modelId, thinkLevel, planAction }`。  
8. `ReadableStream` 按 `\n` 拆；`JSON.parse` 失败 `continue`；`handleEvent`；累积 assistantText。  
9. catch → 推「请求失败」。  
10. finally：`sending=false`，`refreshStatus()`，`loadTree()`。

#### `handleEvent(ev)`

- status → 状态行。
- tool → 工具卡；stayOnBridge 则 `logBridgeTool`；`set_todos` 画任务；`run_command`/`execute_command` 把 stdout/stderr 进终端；`apply_patch` 成功则刷新已打开 tab，有 diff 则 `openDiff`。
- `planRound` 写入 `state.planRound` 并 `paintPlanComposer`。
- consensus / message（可附分支徽章） / error。

#### `logBridgeTool` / `paintTodos` / `agentLabel` / `setAgentMode` / `paintStats`

统计 calls/fail/totalMs（成功率 = `calls ? round((1-fail/calls)*100) : 100`）。`resetRound` POST `/api/bridge/reset-round` 后清本地统计和 bridge-log。todos 同时画 chat 与 bridge 两处。

#### `selectedClientInfo` / `promptText` / `paintClients`

选中卡的 `c.prompt`；没有则 status.prompt 或自己拼 CONNECT_LINE。  
配对码：`pair.code && bridgeRunning` 才显示码，否则说明文案。

#### `renderBrowser(tab)`

- `arena`：内置仿 Arena 页，发送走 `arenaConnect`。
- `chatgpt`：仿 ChatGPT，发送同样 `arenaConnect`。
- `deepseek`：**不**调 MCP。展示商店链接、说明内置浏览器跑不了扩展、只显示 mcpUrl。
- 其它：外链 + prompt 文本。

#### `arenaConnect(text)`（async）

1. 切到 bridge，toast「已用提示词连接本机 MCP」。  
2. try：对 `/mcp/${secret}` 连发 initialize、tools/list、resources/read instructions（clientInfo name `'Arena'`）。失败 toast。  
3. 从 text 去掉 `promptText()` 后的剩余当任务；空则默认「搜相关文件…」。  
4. `setAgentMode('code')`，`sendChat(task, { stayOnBridge:true })`。  
**这是工作台内置演示，不是云上 Arena。** 云上 Arena 自己对隧道发 MCP。

#### `openSite(key)`（async）

未知 key return。bridge 未运行则 `await startBridge()`。try 复制 promptText。打开 browser tab，切右侧 bridge。

#### `startBridge` / `stopBridge` / `paintBridge` / `refreshStatus`

start：读 radio `tunnel`，默认 cloudflare；`POST /api/bridge/start`；`!data.success` toast 返回 false；有 note toast 前 180 字；refresh；复制 mcpUrl。  
stop：POST stop，去 sess-dot。  
paintBridge：按 `s.tunnel.url` 显示 Quick Tunnel 或「未找到 cloudflared 时，MCP 走当前页面源」。若 `s.bridgeAccount.loggedIn` 是布尔则同步 `state.loggedIn`。已授权文案写「本机演示授权（不是 GitHub 登录）」，不是 GitHub @demo / 永久顺。  
refreshStatus：GET /status，重画模型下拉、同步 `state.planRound` 并 `ui.paintPlanComposer`、未触摸过的思考下拉跟 `multiModel.thinkLevel`、provider 表、todos。

#### `paintCustom` / `paintProviderTable` / `loadCustomizations` / `saveCustom` / `loadSkills`

设置页表单。provider 表过滤掉 protocol===builtin。点 radio → POST `/api/models` `{ activeModelId }`。

#### `bind`

先 `onClick(id, handler)`：节点不存在则跳过。绑定活动栏、菜单、发送、`#btn-plan-merge`（`planAction:'merge'`）、`#model-select` onchange POST `/api/models` `{ activeModelId }`、Enter（Shift+Enter 不发送）、Bridge 启停、复制 URL/提示词（extension-http 时 toast 不同文案）、重置密钥、本机演示授权（POST `/bridge/login`，不是 GitHub）、各种 saveCustom、探测 env/stack（HTML 有 `#page-env` / `#page-stack`）、保存多模型博弈（`maxBranches` 默认 4）、Add API（probe 失败且填了手动 id 则造一条 custom 模型；排除 modelId 匹配 video|image 的当默认）、终端 `POST /api/tool/call` run_command mode code、搜索 `search_files` mode ask、Ctrl/Cmd+S 保存。

`skillMarkdown` / `SKILL_TPL`（fix-tests / review / release）只在 bind 闭包内。`fillSkillPreview`：body 已 dirty 则不覆盖。

#### `saveActive` / `connectWs` / `loadMonaco` / `boot`

仅 file tab 才保存 PUT `/api/files/content`。  
WS：`wss` 或 `ws` + `location.host` + `/ws`。消息：command_output 进终端；file_patched 刷新树；todos_updated；tool_call_end → logBridgeTool。parse 失败 return。  
Monaco 从 jsDelivr 拉 0.52.2；7s 超时或 onerror → resolve false/是否已有 editor。  
boot：`try ui.bind()`（失败 `console.error`，后面仍跑）、默认 code、并行 refresh/tree/skills/custom/monaco，再 WS，激活 welcome。`boot().catch(console.error)`。

---

## 9. 网页 VS Code 插件 `webagent-core/extension/extension.js`（492 行）

**用途：** code-server 侧栏 + 原生 Chat `@webagent`。  
**结果：** 同样打 `agentHostUrl()/api/chat`。  
**链条位置：** 路径 C 的 UI。插件**不实现** patch 引擎。

#### `agentHostUrl`

配置 `webagent.agentHostUrl` 或 env `WEBAGENT_AGENT_HOST_URL` 或 `'http://127.0.0.1:48271'`，去尾 `/`。

#### `requestJson(method, url, body)`

http/https 按协议。body 有则 JSON。结束时 try parse；失败 `{ json:null, raw }`。`req.on('error', reject)`。

#### `postNdjson(url, body, onEvent)`

**只用 `http.request`**（https URL 不会走 https 模块）。按行 JSON.parse，失败忽略。结束时残余 buf 再试一次。

#### `modeFromChatRequest(request)`

`request.command` 小写是 ask/plan/code 则用。否则 prompt 以 `/ask` `/plan` `/code` 开头。**都不匹配 → `'code'`**。

#### `historyFromChatContext` / `revealWorkspaceFile`

history 最多 12 轮。reveal：没有 workspaceFolder 或 rel 则 return；打开失败 catch 空。

#### `registerChatParticipant`

没有 `vscode.chat.createChatParticipant` → return。  
handler：去掉 slash 后 message 空 → 输出模式说明 markdown，return。  
否则 progress 文案，`postNdjson /api/chat`。事件：status→progress；tool→markdown 一行，apply_patch 成功则 reveal + `stream.reference`；message；error；consensus。  
catch：提示连不上 48271。  
外层 try/catch：注册失败 `console.warn`，不抛给 activate。

#### `activate(context)`

注册 ChatView、BridgeView。`registerChatParticipant`。状态栏每 5s GET `/api/status`：bridgeRunning 则「Bridge 运行中」，失败「未连接 48271」。  
命令：`openBridge` 打开侧栏；`openAgentChat` 试 `workbench.action.chat.open` query `@webagent `，失败则侧栏；`resetSecret` POST reset-secret。

#### `ChatView`

webview 脚本开。消息 `openNative` → 命令。`send`：本地 history 12，`postNdjson`，事件转给 webview；apply_patch reveal；assistantText 非空才推进 history。catch 发 error 事件。

webview 内脚本：默认 `mode='code'`；Agent 菜单切 ask/plan/code；Enter 发送；set_todos 画任务。

#### `BridgeView`

start → POST `/api/bridge/start` `{ tunnelProvider:'cloudflare' }`。stop / copy / reset。`refresh` GET status。catch 弹 ErrorMessage。  
webview：4s 一次 refresh；copy 用 `status.prompt` 或 mcpUrl+CONNECT。

`deactivate` 空函数。导出 `{ activate, deactivate, modeFromChatRequest }`。

---

## 10. 三条路径的函数调用栈（逐步，对照源码）

### 10.1 本机 Chat，无 API Key，Code 模式

1. 用户点发送 → `app.js sendChat`。  
2. `fetch POST` **3000** `/api/chat` `{ mode:'code', message, history, modelId, thinkLevel }`。  
3. `routes.js` 打开 NDJSON，`emit = res.write`，调用 `runChat({ mode, message, history, modelId, thinkLevel, planAction, emit })`。  
4. `runChat` 取出 `send`；`mode==='plan'` 走 `runPlanRound`；否则能调模型走 `runOpenAI`，否则 `runBuiltin`。  
5. `timedTool(set_todos)` → `callTool(..., 'code')`。  
6. `explore`：list_directory → git_status → find_files → 或有 search_files → read_files。均 `callTool`，失败不抛。  
7. 若消息含围栏+「写入文件」→ `write_file`。  
8. 若含 SEARCH/REPLACE → 读 hash → `apply_patch`（resolveSafePath → 写 tmp → rename）。  
9. `detectTestCommand` 有则 `run_command`（Windows powershell.exe）。  
10. emit message 摘要。`handleEvent` 画卡；有 diff 开 diff tab。

### 10.2 DeepSeek++ 改磁盘（源码能证明的部分）

本仓库**没有** DeepSeek++ 源码。能直译的是我们这一侧：

1. 工作台 `clients.js` hydrate：`connectMode==='extension-http'` → `prompt = mcpUrl` 一行。  
2. 用户把这一行填进扩展（文档步骤，不是本仓库代码）。  
3. 扩展应对 `POST /mcp/<secret>`（或隧道 URL 同路径）。  
4. `server.js` `extractToken` 从 `params.secret` 取出 → `verifyAccessToken` 与 `config.secretKey` 相等 → 过。  
5. `handleRpc` `tools/call` → `callTool(name, args, remoteToolMode)`：默认 `'code'`；`params._meta.mode` 为 ask|plan|code 时用之。  
6. `clipJson` 后 MCP text；broadcast `tool_call_end`。  
7. 工作台 `connectWs` 收到后 `logBridgeTool`。

隧道：`POST /bridge/start` 在 cloudflare 下 `await startQuickTunnel`，成功则 `publicTunnelUrl` 为 trycloudflare.com。无二进制或超时 catch 后 `mcpOrigin` 退回当前页面 Host（本机预览可用）。Named / ngrok 不 spawn。

### 10.3 Chat Plus 改磁盘（源码能证明的部分）

本仓库**没有** Chat Plus 源码（GPL，不 vendor）。能直译的是我们这一侧，与 10.2 同一条 MCP：

1. `clients.js` 里 `id:'chat-plus'` 同样 `connectMode==='extension-http'` → `prompt = mcpUrl` 一行。  
2. 用户按 [网页ChatPlus使用指南.md](./网页ChatPlus使用指南.md) 从 GitHub 编译扩展，把这一行填进侧边栏（传输 Streamable HTTP）。**不要**再跑 `aiguicai/MCP-Gateway`。  
3. 之后 `POST /mcp/<secret>` 与 10.2 第 3–7 步相同。

### 10.4 ChatGPT Plus 连接器

1. GET `/.well-known/oauth-authorization-server`（匿名）。  
2. POST `/oauth/register` → client_id/secret。  
3. 浏览器 GET `/oauth/authorize` → `ensurePairing` 出码（码在工作台 `/api/status.pairing`）。  
4. POST 表单 → `completeAuthorize` → `consumePairing` → 302 带 code。  
5. POST `/oauth/token` + PKCE → Bearer。  
6. 之后 `POST /mcp`（无密钥路径）`Authorization: Bearer`。  
7. `handleRpc` 与 10.2 相同。

---

## 11. 源码现状里不要写成「已经接上」的点

技术文档必须写实际代码。

1. **Named / ngrok 不 spawn。** `/bridge/start` 只在 `provider==='cloudflare'` 时 `await startQuickTunnel`。另外两个下拉不会拉起对应隧道。无 cloudflared 时 start 仍 200，MCP 走当前 Host。

2. **读哈希文件不要提交。** `.webagent/read-hashes.json` 是运行时缓存（最多 400 条）；「清除本轮统计」会删。文件在进程外被改仍会 `STALE_FILE`。

3. **远程 `tools/call` 默认 Code。** `remoteToolMode` 读 `params._meta.mode`（ask|plan|code），缺省 `'code'`。本机 Chat 仍由 `/api/chat` 传入 UI 模式。

4. **没 Key 时 `consensusEngine` 不请求任何模型 HTTP。** 本机草案 `simulated:true`，`agreementRate:null`。有 Key 的 Plan 分支走 `runOpenAI`。关闭「多模型博弈」则 `planAction:'single'`，不出回合。

4b. **`POST /bridge/login` 不是 GitHub OAuth。** 只把本机 `loggedIn` 写成 true，`license` 是 `local-demo`。旧盘上的「永久顺」/ `github` 在 `normalizeBridge` 里收掉。默认 `loggedIn:true`，所以没点按钮也能启动 Bridge。

5. **`sendCommandInput` 恒定 ok:false。** 没有交互式 PTY。

6. **工作台 `arenaConnect` 打的是本机 `/mcp/${secret}`**，再转去本机 `/api/chat`。不是 Cloudflare 上的 Arena 产品。

7. **`read_files` 的敏感拦截在 `resolveSafePath`→`assertNotSensitive`；列表/grep 靠 `isHidden`。** 两条都在，删一条会在「读」或「列」上漏。

8. **隧道上的 `/api` 不是「有密钥就过」。** mcp 口见 Cloudflare 头或 `*.trycloudflare.com` Host 则 404。本机 Chat 走 3000；VS Code 插件走无 CF 头的 `127.0.0.1:48271`。WebSocket 只挂 3000，非本机连接直接关掉，连接消息不下发 secretKey。默认 `WEBAGENT_BIND=127.0.0.1`。符号链接必须 `realpath` 仍在工作区。`git push` / `curl|sh` 等要 `confirm_dangerous`。mcp 口不再 `cors()` 全开：只给扩展和已知聊天站回 CORS 头。带外站 Origin 的 `/api` 404。CORS **不是**认证。

---

## 12. 改代码时建议从哪下手

| 你想改的行为 | 先打开 |
|---|---|
| 新 MCP 工具 | `tools/index.js` 注册 + 新文件；`mcpProtocol.test.js` 的 25 改数量 |
| 补丁匹配 / 哈希 | `patchEngine.js` + `tests/patchEngine.test.js` |
| 网页第一句提示词 | `instructions.js` 的 `CONNECT_LINE`（有测试锁原文） |
| DeepSeek / Chat Plus / Arena 卡片步骤 | `mcp/clients.js` |
| 本机无 Key 时的探索策略 | `agent/runChat.js` `explore` / `runBuiltin` |
| Plan 多模型回合 | `tools/planRound.js` + `runChat.js` `runPlanRound` |
| 有 Key 时模型怎么被管 | `agent/openai.js` `systemPrompt` / `temperatureFor` |
| 工作台按钮 | `workbench/js/bind.js` + `index.html` |
| 隧道上未鉴权 `/api` | `src/utils/localControl.js` + `src/index.js` |
| MCP CORS / 外站 Origin 打 `/api` | `src/utils/corsAllow.js` |
| VS Code Chat 默认 Agent | `extension/extension.js` `modeFromChatRequest` |
| 网页 VS Code 登录口令 | `scripts/codeServerAuth.js` + `run-code-oss.js` |
| 外部工作区密钥进 Git | `models/store.js` `protectWorkspaceSecrets` |
| 工作台保存 / 新建 Skill | `api/routes.js` PUT `/files/content`、POST `/skills`（均 `write_file`） |
| Named / ngrok 隧道 | `api/routes.js` `/bridge/start`（目前只接 cloudflare Quick Tunnel） |
| 密钥持久化 | `config.js` + `store.js` + reset-secret 路由 |

测试入口：`webagent-core/agent-host/tests/`，根目录 `run-tests.cmd` 只是调用它。说明见 [测试说明.md](./测试说明.md)。
