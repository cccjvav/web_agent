为了帮助你的 Agent 精准、无遗漏且高质量地完成这项“代码级文档工程”，我为你设计了一套**结构化分步提示词（Prompt Chain）**。

由于项目代码量通常较大，单一的提示词容易导致 Agent 遗忘上下文或超出 Token 限制。这套提示词分为 **4个阶段**，建议你按顺序发给 Agent（或作为系统指令一次性植入，让它自行规划）。

---

### 🚀 第一阶段：项目扫描与架构占位（初始化）

**提示词目标**：让 Agent 建立全局认知，生成主目录的总览骨架。

```markdown
# Role: 高级代码文档架构师

# Task: 初始化项目文档体系
请扫描当前根目录下的所有一级子文件夹（排除 __pycache__, .git, .idea, node_modules, venv, .env 等环境/缓存目录）。

请执行以下操作：
1. 生成一个完整的项目目录树（Tree Structure），标记出所有包含 `.py`, `.json`, `.html`, `.js`, `.css`, `.yaml`, `.xml` 文件的文件夹。
2. 在根目录下创建一个 `总览.md` 文件。
3. 在 `总览.md` 中写入以下骨架（仅骨架，不要填充具体代码解释）：
   - **项目名称与一句话简介**
   - **技术栈列表**（根据文件后缀和内容初步推断，如 FastAPI, Flask, Vue, PyTorch 等）
   - **核心架构分层**（例如：API层、业务逻辑层、数据层、静态资源层）
   - **子模块索引表格**（列名：子文件夹名称 | 核心职责 | 详细文档链接），链接暂时留空，待第二阶段填充。
```

---

### 🧠 第二阶段：深入子文件夹——生成细颗粒度文档（核心循环）

**提示词目标**：这是最核心的循环指令。让 Agent **逐个**处理子文件夹，生成包含行级/函数级解释的 `README.md`。

> **建议**：请 Agent 一次只处理 **1个** 子文件夹，处理完并写入文件后，再处理下一个，防止上下文爆炸。

```markdown
# Role: 代码逆向解析专家

# Task: 深度解析子文件夹 [请替换为具体文件夹名，如 /src/core]
当前处理目标：`./[子文件夹名]/`

请读取该文件夹下的 **所有代码文件**（优先处理 .py, .js, .html，其次处理 .json, .yaml 等配置文件）。

针对该子文件夹，请在 **该文件夹内部** 创建一个 `README.md` 文件。该文件必须包含以下严格的格式化内容：

## 1. 模块概述
- 该模块在项目中的定位（如：数据预处理模块、支付接口封装、UI 组件库）。
- 模块间的依赖关系（它调用了哪些其他兄弟模块？）。

## 2. 文件级详细说明书（按文件逐个列出）
对于每个代码文件，请按以下模板生成解释：

### 📄 文件名：`xxx.py` (或 .js, .html)
- **文件职责**：用一两句话概括该文件的宏观作用。
- **核心类/函数清单（代码行级注释）**：
  - 按逻辑顺序列出该文件中的所有 Class 和 Function。
  - 对于 **函数/方法**，请解释：
    - 输入参数（类型与含义）。
    - 返回值（类型与含义）。
    - **核心逻辑拆解**：不要只抄代码，要解释第几行到第几行在做什么（例如：*L15-L20: 校验输入参数的合法性；L25-L30: 调用外部API并处理重试机制*）。
    - 如果是 HTML 文件，请解释 DOM 结构区块的功能（如：*L10-L25: 用户登录表单区域*）。
- **关键变量/常量说明**：解释文件顶部定义的全局变量或配置项的作用。

## 3. 执行逻辑流（仅针对该子文件夹）
用文字描述数据进入该文件夹后，是如何在各个文件之间流转的（例如：先由 `main.py` 接收请求，实例化 `handler.py` 中的类，最后调用 `utils.py` 格式化输出）。

---
**重要约束**：
1. 必须引用具体的行号（例如 `L12-L35`），实现“行至行”的映射。
2. 如果是 JSON 配置文件，请解释每一个 Key 的用途以及 Value 的取值范围。
3. 完成该文件夹的 README.md 后，请 **暂停**，等待我输入下一个文件夹名称，或如果你已处理完所有文件夹，请进入第三阶段。
```

---

### 🔗 第三阶段：回填主目录与交叉链接

**提示词目标**：收集所有子文件夹的 README，完善根目录的 `总览.md`，并建立跳转链接。

```markdown
# Role: 文档整合官

# Task: 汇总并建立知识图谱
请回顾所有已生成的子文件夹 `README.md`。

现在，请回到根目录，更新 `总览.md` 文件：

1. **填充子模块索引表格**：在第二阶段的表格中，将“详细文档链接”替换为正确的相对路径（例如 `[查看详情](./src/core/README.md)`）。
2. **绘制数据流向图（文字版）**：根据各个子文件夹 README 中的“执行逻辑流”，整理出一份从用户请求到服务器响应的全局调用链路图（使用 Markdown 的 ```mermaid 或 纯文本缩进流程图）。
3. **环境与配置总览**：汇总根目录下的 `requirements.txt`、`package.json` 或 `docker-compose.yml`，在 `总览.md` 中解释该项目的启动步骤（Install -> Build -> Run）。
4. **全局索引**：在 `总览.md` 的顶部生成一个“快速导航”区域，列出所有核心类或入口函数，并链接到对应子文件夹的 README 中的锚点（例如：`[核心认证类](./src/auth/README.md#核心类)`）。
```

---

### 🛠️ 第四阶段：质量校验与补漏（收尾）

**提示词目标**：检查有没有遗漏的文件，并修正格式。

```markdown
# Role: QA 文档测试员

# Task: 查漏补缺与格式规范化
请对所有生成的 `.md` 文件执行最终审查：

1. **覆盖率检查**：对照第一阶段的目录树，检查是否根目录下的每一个代码文件都被至少一个 `README.md` 提及。如果有遗漏，单独补充对应文件夹的文档。
2. **链接有效性检查**：点击思维中的链接路径，确认 `总览.md` 指向子文件夹的链接地址无误（确保 `./` 和 `../` 使用正确）。
3. **格式美化**：
   - 确保所有代码块（```python, ```json, ```html）标记了正确的语言以支持高亮。
   - 确保所有函数名、类名使用反引号（`）包裹。
4. **生成报告**：在根目录下创建一个 `DOCUMENTATION_SUMMARY.md`，简要列出总共解析了多少个文件夹、多少个代码文件，以及文档的覆盖范围。
```

---

### 💡 给 Agent 的额外执行建议（写在提示词末尾）

为了确保 Agent 不“偷懒”或“幻觉”，建议你在第一段提示词开头加上这句话：

> **执行原则**：请不要杜撰代码逻辑。所有的解释必须严格基于读取到的实际代码内容。如果某个函数逻辑过于复杂，将其拆解为“步骤1, 2, 3...”进行直译。对于行级解释，宁愿详尽，不可遗漏关键分支（如 if-else 或 try-except）。