# bind.js：逐个交互回调与提交数据

[bind.js](bind.js)是页面事件连接层。**confirmedJson(response,action)**先解析JSON，再要求HTTP成功及非数组对象，失败抛带动作上下文的错误；它不替代每个调用点的业务字段检查。**onClick(id,handler)**找不到节点就返回，找到赋onclick；**bind()**执行一次大批绑定，最后ui.bind引用它。多数其他直接`$('#id').onclick`要求HTML节点存在，不能把onClick的容错推广到整个文件。

模块级**detectProfile()**以profileDetection单飞，10秒AbortController后调用confirmedJson；局部**validStrings(value,keys)**要求环境/技术栈必需字段均为字符串，完整候选通过才返回，finally释放单飞。它不把探测自动保存。当前已审的认证、画像、新建、终端和搜索消费者使用HTTP+业务/形状合同；其它回调仍按各节说明，不能把一个帮助函数推广为全部API的统一授权边界。

## 1. 显示与导航回调

| 元素/事件 | 回调输入与动作 | 状态/边界 |
|---|---|---|
| btn-theme | 读dataset切applyTheme | bind起始先initTheme |
| btn-text-smaller / btn-text-larger | 各调stepTextScale(-1/+1) | bind起始先initTextScale恢复上次选择；到上下限时按钮自身disabled。只改本页字号，不动文件或Monaco设置 |
| btn-sess-toggle | toggle collapsed、aria-expanded | 不断开MCP |
| btn-bridge-health | checkBridgeHealth若存在 | 本源检查 |
| activitybar [data-left] | forEach绑定；重复当前→closeSidebar，否则切explorer/search | 收起统一移active、把aria-pressed归false；若焦点原在侧栏则恢复至可见活动按钮 |
| btn-manage / document click | 阻冒泡打开管理并同步aria-expanded；外部点击关管理/文件/Agent菜单并复位 | document listener不自动去重 |
| btn-agent-pick / agent-pick-menu | 显示后positionPopover定位；closest data-mode选模式 | aria-expanded同步，打开聚焦首按钮；关闭旧模型弹层；resize关闭 |
| menu-custom-from-agent | 关模式菜单，openModal agents | 不新建模型 |
| menu-custom/api/bridge | 打开overview/api/bridge设置 | 仅导航 |
| btn-agent-window、walk-local-chat | openAgentWindow | 本机Agent页 |
| walk-basics、walk-bridge | 打开对应设置 | 连接步骤不自动执行 |
| data-menu=file / file-menu | 切菜单；closest data-act委托new/open/save/welcome/custom | save调用带hash编辑器流程 |
| modal-close / modal背景 | closeModal；仅点背景自身才关闭 | 不自动取消在途请求 |
| nav-item/card-grid data-page | forEach绑定showPage | 切页不写盘 |
| rb-chat-tab/rb-bridge-tab | 点击setRight；左右/Home/End按键切换、循环并聚焦活动页签 | 同步工作面与tabpanel、aria-selected/tabindex，不启动服务 |
| menu-term/menu-help/btn-clear-term | 切终端区/打开help帮助页/清terminal HTML | 清输出不kill进程 |
| 全局keydown | 模态打开时Tab/Shift+Tab把焦点约束在可见可用的按钮、链接、折叠摘要及表单控件，并排除未展开details里的后代；Escape优先关模式菜单并恢复按钮焦点，否则关模型弹层及实际打开的modal；Ctrl/Meta+S阻默认并saveActive | 输入法、辅助技术和浏览器行为仍需人工验证 |

**closeSidebar()**是bind内复用闭包：仅在侧栏展开时收起，清除全部活动按钮的active/aria-pressed；若当前焦点会随侧栏隐藏，则恢复到原活动按钮。窗口宽度首次从大于700px跨入抽屉断点时调用它，避免桌面展开态在窄屏覆盖Agent菜单；只改变高度或一直处于窄屏不会自动关闭用户刚打开的抽屉。

**closeAgentMenu()**是bind内复用闭包：隐藏模式菜单并将按钮aria-expanded设为false；外部click、resize、选模式、进入自定义和Escape共用，不在外部点击时抢焦点。resize同时记录是否跨入700px抽屉断点。

walk-start是原先遗漏绑定的首张欢迎卡，现在用onClick打开help，与顶部帮助一致；walk-basics仍打开overview设置。

## 2. 模型与Chat回调

- model-pick-btn→openModelPicker；**onPick(id)**把候选ID交给model-select.onchange，不乐观修改按钮标签。btn-mm-pick→带mergeMark的picker，onPick仅改待保存merge/display值；btn-mm-active回active，不立即写后端。
- model-select.onchange捕获非空候选ID，立即恢复隐藏select为最近确认的activeModelId，再调用saveModelSettings；等待期间Chat不消费未确认候选。HTTP/业务/网络失败不修改确认快照，成功后的有效刷新统一更新select和按钮。think-select.onchange设dataset.touched，避免refreshStatus覆盖手选。
- btn-send→sendChat()；btn-plan-merge→sendChat('',planAction merge)；btn-agent-send取agent-input和模式再sendChat显式text（不会走sendChat无text清输入分支）。两输入keydown Enter无Shift触发发送，mode-select.onchange更新模式，chips.onclick取data-mode/data-text快捷发送。发送中再次sendChat是取消，不排队。

## 3. Bridge与身份回调

| 控件 | API/动作 | 错误及限制 |
|---|---|---|
| btn-bridge-toggle / btn-stop-bridge-rb | 启动在途优先stop，否则按status选择；右栏直接stop | 启停独立guard、代次与绑定结果消费见Bridge说明；页面锁非跨页幂等 |
| btn-reset-round | resetRound | 清统计不重置全部授权 |
| btn-copy-url/prompt/rules | clipboard.writeText | URL含secret；rules无内容仅提示；提示按客户端适配，没有“已连上”证明 |
| btn-reset-secret | 委托ui.resetSecret | 主逻辑在Bridge说明：确认、绑定/旧密钥复查、在途互斥、坏响应未知；不再无条件toast成功 |
| open-site各按钮 | openSite(data-site) | 本地连接指引，不真实浏览器自动登录 |
| btn-gh-login | 先取消设备轮询，POST bridge/login；要求HTTP成功及success/demo/provider完整确认，再设loggedIn并刷新 | 失败不改本地授权；写确认但状态刷新失败单独提示，明确不是GitHub |
| btn-gh-token | 先取消设备轮询；空白不发；捕获token后POST并要求success/provider/username | 失败保留输入，成功且输入未变化才清；写确认与状态刷新分开提示 |
| btn-gh-device | 取消旧轮询并开新代次/AbortController；POST device要求码和URL，按服务端interval安排tick | 不自动填写授权；重复身份操作淘汰旧回包，错误停止自动轮询 |
| btn-gh-clear | 先取消轮询，POST github/clear并要求local-demo/local完整确认，再刷新 | 失败不假清除；写确认但刷新失败单独提示 |
| btn-refresh-auth | paintBridge+提示 | **不fetch新状态**，只是用当前state重绘 |

身份区闭包**cancelDevicePoll()**递增devicePollGeneration、清timer并abort当前controller；演示、PAT、设备新尝试和清除都先调用。**refreshAfterConfirmedAuth()**把refreshStatus的false/reject统一为false，不重放写请求。设备码闭包 **tick()** 每次先比代次，POST device/poll并用confirmedJson；done还要求success和非空username，pending严格要求pending=true/done=false并采用返回interval（至少5秒）。HTTP/业务/JSON/网络失败停止自动轮询；旧代次/AbortError静默退出。它是当前页面生命周期协调，不是跨标签页或服务端永久锁。

## 4. Skill与定制配置回调

**skillMarkdown(name,when,steps)**trim名称或untitled，description替换换行截200，组装front matter、何时使用/步骤并join；这是普通文本拼接，不是YAML通用安全序列化。SKILL_TPL三个固定模板fix-tests/review/release只产生说明，不自行运行命令。

**fillSkillPreview()**不存在正文框或dataset.dirty=1就返回，否则用三个字段生成。name/when/steps.forEach注册input；正文input标dirty保护手改；skill-templates.onclick找data-tpl、填三个字段、清dirty再生成。

| 控件 | 收集字段与动作 | 副作用 |
|---|---|---|
| btn-add-agent | 旧agents追加Date.now ID/name/role，saveCustom | 登记智能体描述，不启动独立Agent |
| btn-add-skill | name及正文或生成正文POST skills，用confirmedJson并要求success/path后loadSkills | 写确认才报已创建；目录刷新false单独提示手动刷新，业务/网络/解析失败报状态未知、不自动重试；后端拒绝同名覆盖 |
| btn-detect-env | 记录os/shell草稿→共享detectProfile→完整验证后填入 | 等待期间字段变化则保留草稿并提示重新探测；失败不发布；只填表不保存、不激活Conda |
| btn-save-env | os/shell/replyLanguage/commitLanguage/notes→saveCustom environment | 后端决定偏好文件写法 |
| btn-detect-stack | 记录四字段草稿→共享detectProfile→完整验证后填入languages/frameworks/packageManager/testCommand | 等待期间编辑不被旧结果覆盖；探测后仍需用户保存，不自动跑testCommand |
| btn-save-stack | 五个技术栈字段→saveCustom techStack | 不安装依赖 |
| btn-save-instr | instructions→saveCustom | 写指令配置 |
| btn-add-prompt | 旧prompts追加ID/name/content | 不直接发送 |
| btn-add-hook | 旧hooks追加event/command | 登记项，不在此执行shell |
| btn-add-mcp | 旧mcpServers追加name/url | 登记而非已经建立连接 |
| btn-add-plugin | 旧plugins追加name | 不下载插件 |
| btn-add-link | 旧quickLinks追加name/url | 后续settings渲染 |
| btn-codex | 仅toast未实现 | 不碰Codex OAuth凭据 |
| btn-save-mm | enabled/mergeModel/thinkLevel/mergeAllowsRead/maxBranches→POST models | maxBranches Number或4；真正范围校验在后端 |
| btn-save-pref | preference以及instructions（空时用偏好）→saveCustom | 可能同时改两项，不是只显示文字 |

追加数组都基于当前state.custom快照；快速并发保存没有前端事务锁。

## 5. Provider发现与添加

**providerDraft()**在点击时trim读取m-base、m-key、m-id及m-vision.checked，返回本次四字段快照。btn-test-api/btn-save-model分别调用ui.configureProvider(draft,true/default false)，具体期限、共享guard、失败处理和保存契约的唯一主说明见[Bridge与设置详解](Bridge与设置详解.md)。bind不再直接探测后拼builtin+models整表覆盖。

Test只读取列表，不保存。Add在手动ID非空时明确跳过发现，否则发现成功才提交；不因网络/HTTP失败自动转手动或改用内置。后端仅追加、冲突拒绝，保留旧模型/Key及活动模型，不宣称列表成功等于Chat兼容。btn-use-builtin仍须用户明确点击，调用saveModelSettings；false显示未确认，true只确认已保存并提醒刷新失败需核对，不自动重放。

## 6. 文件、终端、搜索与站点栏

- lnk-new-file：fileCreatePending使页内单飞，prompt名称→PUT files/content(path,content:'',createOnly:true)，要求HTTP成功、success/path回显和64位hash；后端以exclusive发布拒绝已有路径。创建确认后再分别刷新树/openFile，刷新失败不会重发创建。任何失败明确“未确认”且不主动覆写旧内容。open-file/open-folder只切explorer，并非操作系统选目录对话框。
- term-form.onsubmit：preventDefault、trim非空、清输入/回显，POST tool/call，name run_command、mode code、arguments.command；confirmedJson后还要求success严格true及对象result，stdout/stderr只接受字符串，无输出明确显示完成。HTTP/业务/形状失败写错误行并返回false；此回调不提供危险确认字段，真正权限仍由后端决定。
- btn-search：query trim后POST search_files/ask，只有HTTP成功、success严格true且matches逐项含字符串file/content和整数line才替换结果；动态命中是可键盘激活的button并转义文本。失败清除旧点击处理并显示错误，不能把坏响应当“没有命中”。没有分页UI和总结果数提示，也不跳到具体行。
- br-go：读地址，只有当前browser tab才更新url并renderBrowser；br-reload同样重绘。它们不创建真实外部会话、不执行本机Agent任务。

## 7. 验证

```bat
npm test --prefix webagent-core/agent-host -- --filter=workbenchRuntime
npm test --prefix webagent-core/agent-host -- --filter=workbenchHtml
```

两个fixture方向分别关注交互运行与HTML绑定，但不能把它们推广成所有未命名回调的逐分支全覆盖。本讲解按控件列出匿名回调，避免只列bind一个函数造成“函数都有名字便算全量”的误导。

## 主机核对入口
btn-host-diagnostics打开diagnostics页并refreshDiagnostics；btn-refresh-diagnostics只读刷新；btn-compare-host比较外部工具返回UUID。均用onClick容错绑定，不变更服务权限。


## 受控执行增量

bind新增ui.initOperations绑定本机工具接入与审批；按钮和确认流程逐项见agent-host/src/utils/受控工具与工作流详解.md。

btn-refresh-activity通过可选onClick绑定调用refreshBridgeActivity，只重新同步，不调用reset-round。

文件菜单data-act=preview调用ui.previewActive；保存仍调用ui.saveActive，二者不同。确认预览按钮由tabs.initEditorSafety绑定，不用把所有diff当成可写草稿。验证editorRuntime与workbench.browser的菜单点击流程。

文件菜单data-act=undo-save调用ui.previewUndo，仅预览最近保存的受保护回退；实际确认由tabs的同一diff按钮执行，不绑定自动回退快捷键。

定制设置调用统一检查saveCustom返回值：false立即退出，环境/技术栈状态、指令/偏好/智能体成功toast只在确认成功后更新。失败细节与草稿保留由settings处理，不自动重试；该约束不表示所有其他fetch按钮已审完。

多模型保存按钮通过saveModelSettings统一检查HTTP/业务失败与未知状态，移除无条件“已保存”提示；失败保留输入。模型表格选择使用同一页面内互斥；其他模型新增/状态刷新调用仍单独审查。

第三方候选的复制地址toast提醒先核对版本、认证与兼容性；复制仍只是本机剪贴板操作，不自动建立会话、注入规则或授权工具。

复制提示词先检查promptText结果，空值不写剪贴板、不显示成功；OAuth连接器的菜单/账户支持需按版本核对，不依旧ChatGPT菜单文案操作。

F54第六批：workspace-switcher三个data-workspace-view按钮只切编辑器/Chat/Bridge展示，不切主机执行控制的chat/bridge模式。点击先closeSidebar；ui.closeSidebar供tabs在窄屏打开文件时收起遮挡抽屉。document的focusin仅宽屏时记住center/rightbar中正在操作的工作面，使缩窄后焦点所在内容仍可见；仅界面dataset与按钮选择态，不写磁盘/调用工具。
