# 工作台CSS：变量、所有组件规则组与级联

[styles.css](styles.css)配合[页面结构](页面结构详解.md)及js生成DOM。它不会执行工具或改变服务器权限；on/hidden/failed等视觉类来自JS状态。

## 1. 基础与尺寸

:root的bg/bg0/bg2/bg3、hover/border、text/text2/mute建立背景/文本层次；link/focus/green/warn/warnbg/danger是语义色；font/mono字体回退；title30px/status22px/activity48px/right356px定义主框架；sess-bg/border/card/num单独会话区主题。部分声明可能暂无消费者，不能据变量名推断有功能。

通配border-box；html/body满高、零margin、overflow:hidden，**整个页面不滚动，由内层滚动**；表单继承字体/颜色，button手形；.hidden display:none!important高优先级，u去下划线只是菜单外观，不注册Alt快捷键。

## 2. 框架与浏览指引规则组

| 选择器组 | 关键属性与协作 |
|---|---|
| #titlebar/.tb-menu/button/hover/.tb-center/.tb-search/.tb-right/.tb-theme | 三列grid（中间220–480px），menu flex，标题框最大420px，主题按钮右对齐；user-select:none禁止标题栏选字 |
| #workbench | flex，高度减title/status，避免底栏重叠 |
| #activitybar/.ab-btn/hover/active/active::before/.ab-spacer | 固定活动宽，48px按钮居中，active前伪元素画左线；spacer弹性推管理到底 |
| #sidebar/.collapsed/.side-title/.side-workspace/.side-pad | 240px侧栏、min160，collapsed彻底不占布局；标题/内距 |
| .file-tree/.tree-item/hover/dir/.tree-item .chev/.tree-dir .tree-kids | 树滚动，行flex与悬停；chev宽12；tree-kids空规则本身无折叠效果，折叠由JS改display |
| #center/#tabs/.tab/.tab.on/.tab .x | center flex:1/min-width0防被长内容撑开，tabs固定35高横向滚动，活跃背景、关闭标记 |
| #editor-wrap/#editor/#editor-fallback | wrap flex/min-height0/relative，编辑器absolute inset0；fallback同尺寸、等宽字体且resize none |
| #welcome/.welcome-grid/h1/sub/h3 | 欢迎页absolute自滚动、双列gap48，标题42px细字；与编辑器互斥显示 |
| .start-links/button/hover/.codicon/.recent/button/.path | 链接外观按钮、图标宽16，最近路径弱化，不提供文件访问权限 |
| .walk-card/hover/.walk-ico/title/desc/.welcome-bottom/.agent-window-btn/hover/.chk | 演练卡flex、按钮/勾选排版；cursor不证明已绑定行为 |
| #browser/.hidden/.browser-bar/button/#br-url/#browser-page | absolute flex，URL flex填宽，页面滚动；浏览指引本地容器不是iframe |
| .generic-site/h2/.prompt-box | 通用外链说明使用主题底色、预格式prompt；height100%可能仍依赖父滚动 |

## 3. 消息、任务、编辑和终端规则组

| 选择器组 | 关键属性与协作 |
|---|---|
| #panel/.hidden/.panel-head/.terminal/.info/.err/.ok/#term-form/.prompt/#term-input | panel160px，terminal flex滚动等宽字体，输出分类色；表单flex，无交互PTY模拟逻辑 |
| #rightbar/.rb-head/button/on/.rb-body | 固定right变量、min280px；body flex/min-height0确保消息区滚动 |
| .chat-stream/.chat-empty/.bubble/h3/p/a | 流flex滚动、空状态圆图和设置链接样式 |
| .msg/.user/.assistant/code/pre/.status-line | 用户保留换行，助手边框/代码滚动，状态蓝色；内容净化由JS承担 |
| .tool-card/header/.dur/.fail/header/.dur/pre | 工具卡点击头，失败红边，pre默认display none、max-height120滚动；JS click才展开 |
| .task-box/.task-h/ul/li/.completed/.box | 去列表圆点，计数左右分布，completed删线/灰色；真实状态由服务端返回 |
| .agent-pick/.model-table/th/td/.model-group/h4/.cap-pill | 模型表边线/能力胶囊，group间距；能力pill只是声明 |
| .consensus/h3/.branch-tabs/button/on/.adopt | 总结卡、分支按钮换行，选中focus色、全宽执行按钮；执行需用户点击 |
| .chat-compose/.chips/button/.composer-box/:focus-within | 输入底区、快捷chips横滚，框内聚焦时边框强调 |
| #chat-input/.composer-tools/.icon-btn/.mini-sel/.send-arr | 输入全宽、取消resize/outline，工具flex、发送margin-left:auto推右；样式主要给chat-input，不自动覆盖agent-input所有属性 |
| .plan-badge/#btn-plan-merge | 固定不压缩胶囊、总结小按钮，与JS隐藏逻辑配合 |
| #agent-pane/#diff-pane/.hidden/.agent-head/.diff-head/.diff-body/.diff-add/del/hunk | absolute占满、头部边线、diff主体滚动；增绿删红hunk蓝，不修改磁盘 |
| .branch-pill-row/.branch-pill/.composer-chips/.chip-info | 分支标签右对齐、本地/审批小标签；纯信息 |

## 4. Bridge、弹层与表单规则组

| 选择器组 | 关键属性与协作 |
|---|---|
| .bridge-wait/.wait-ico/h3/p/.bridge-log | 空状态居中与输出列表flex滚动 |
| .mcp-session/.sess-head/.sess-head-left/.chev/.collapsed .sess-body/.collapsed .chev | 卡使用sess变量，头整行按钮；collapsed隐藏body并旋转箭头，aria-expanded由bind另更新 |
| .sess-actions/.sess-btn/.sess-head .dot/.dot.on | 按钮换行、状态灯灰/绿；视觉灯不是网络证明 |
| .sess-note/.sess-grid/div/span/label/.sess-foot/#sess-meta | 2列统计数字、卡片边框、脚注；meta break-all应对长URL |
| #statusbar/.sb-left/.sb-right/#sb-ws | 固定status高度、左右flex，WS重连warn色 |
| #modal/.modal-card/.modal-nav/.modal-nav-title/.modal-main/.modal-close | fixed全屏遮罩z30，卡最大1080/96vw×720/90vh，导航220，正文自滚动；不实现焦点捕获 |
| .nav-item/hover/on/em/.page h2/.lead/.hint/.field-label | 设置选中状态、计数、标题层次与标签，不生成表单字段 |
| .ms-input/:focus/.tall/.pref-row | 全宽输入、focus边框、tall最小140可竖直拉伸，pref并排 |
| .card-grid/article/h4/p | 设置概览3列、卡内容边距，描述min-height48对齐按钮 |
| .vs-btn/hover/primary/block/.ghost/.row-btns/.tiny/.mute | 普通/主/全宽按钮变体，行按钮换行、弱字通用类 |
| .br-head/.status-pill/stop/run/ok/.btn-wrap/.warn-line | Bridge头flex、运行/成功绿，stop仍蓝；pill颜色不校验授权 |
| .mcp-row/.url-box/.banner | URL等宽长字断行、复制并列、警告带；secret显示来自JS |
| .block/.block-h/.tunnel-title/.tunnel-card/on/.t-name/em/ul/details.block summary | 区块标题布局、隧道选中边、说明列表、原生details手形 |
| #manage-menu/button/hover/.dropdown/button/hover/hr | 固定弹出菜单最终z100；管理左下，通用下拉左上；Agent菜单JS动态覆写位置 |
| #toast | 固定右下、z50；hidden属性由JS切，不等同.hidden类 |
| .guide-steps/li/.client-cards/.client-card/on/strong/p/.badge/ok/warn/.client-detail/ol/.list-row/button | Skill步骤、2列客户端卡、等级标识、登记项行；CSS不执行hook/MCP |
| .model-picker/.mp-search/.mp-list/.mp-row/hover/.mp-name/sub/caps/mark/.mm-pick-row/.ms-input | picker最终z100、宽min(330px,视口减16px)，maxHeight由positionPopover按上下剩余空间限制；列表min-height0与overflow保证滚动；文字/背景使用主题变量；合并行输入flex填宽 |

## 5. 滚动、响应式与浅色覆盖

WebKit scrollbar宽/高10px、thumb灰、track透明；非WebKit未必同外观。max-width980px时welcome-grid/card-grid单列，modal-card改纵向92vh，modal-nav全宽横滚；导航按钮不压缩，legacy-settings为折叠备注入口，正文min-width/min-height0保留滚动。max-width700px隐藏中间标题栏，rightbar限于视口减活动栏，工具条允许换行。主框架不是手机单列IDE；手机任务仍走外部MCP，不开放远程工作台。

深浅色共用input-bg/code-text与语义文字变量；color-scheme同步原生表单/选项。原灰色背景组件改用bg/hover/input-bg，浅色不再继承深底淡字；强化mute、link、green、danger，补全编辑fallback、终端、模型菜单、设置卡片、外链指引和diff增删配色。Arena/GPT仿站样式已随仿站DOM移除。

末尾共享规则仍参与级联：菜单max-width/max-height与overflow-y/overscroll，模型列表flex/min-height0、搜索框不压缩；focus-visible轮廓与placeholder文字；状态栏省略/不换行/裁剪防止遮挡输入；chat-compose不压缩，任务列表限高且可滚。边框和信息标签不是权限证明。CSS不能代替JS定位，也不声称整个产品已做完整无障碍认证。

## 6. 验证

`npm test --prefix webagent-core/agent-host -- --filter=workbenchRuntime`

CI回归验证主题函数、模型选择/监听清理和几何钳制（DOM fixture，不是浏览器）。2026-09-14额外在Linux Chromium153做了真实页面/内置读取、深浅主题×4种尺寸、滚动/键盘/resize及可见文字对比度抽查；不等同用户Windows/DPI或全部页面状态认证。人工重点：宽窄屏、未保存编辑切主题、Monaco迟到升级、长日志/表格、弹层层级、键盘焦点、200%缩放。当前正文解释实现，不把这些未执行检查标为通过。
