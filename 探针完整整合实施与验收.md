# 探针完整整合实施与验收

## 当前决定（替代“只做连接版”的缩减范围）

用户已明确：需要原型完整能力作为模型参考，不要求绝对真实。完整整合是当前目标，不能以“不具备身份认证”作为排除分析、采集、映射或自动化功能的理由。必须展示来源、冲突、未知与采样局限，但这些提示不是取消功能的替代品。

旧0.1.x仅有连接核对，是已经发生的交付缺口，不是最终范围。0.2.0开始交付真正的原型分析能力，**仍不是完整整合完成版**。

“Chat模式API背后的模型确切验证”排在本项目完整探针整合之后，不在本轮开展。本轮只使用合成样本，不访问真实账号或验证任何真实token。

## 0.3新增Inspector合作路线

已接入用户上传的arena-trace-inspector 0.9.0（a8f7d86），浏览器以其显式CDP采集、scope校验、run/span统计与历史为基础，加入原Probe家族/代号参考；VS Code现直接接收原生单run证据并用两套引擎分析。原型浏览器不再需要同时启动两套钩子。构建/安装/现有限制见webagent-core/probe-extension/浏览器整合说明.md。下表0.2记录保留作为前阶段，0.3只推进了浏览器单采集器、trace证据、使用量与手动文件互通，不代表表内全部能力已完成。

## 分工

- 浏览器端：网页环境采集、原始请求关联、显式启停、授权内的自动化。不是让VS Code假装拥有网页网络权限。
- 共享分析内核：原型模型字段解析、协议指纹、行为/分词参考、命名规则、映射及后续历史聚类；不另造一套不兼容算法。
- VS Code：控制、离线分析、来源与候选呈现、后续历史/导出和浏览器端接入。
- WebAgent：任务执行/审批、Bridge、既有连接核对；分析结果不自动扩大执行权限。

## 必须完成的能力表

| 能力 | 本轮落地 | 完整验收还欠什么 |
|---|---|---|
| 模型字段/流内容解析 | 0.2复用原SSETap，离线解析用户选定的模型观测JSON | 标准SSE、多帧/多请求归属、浏览器实际采集验收 |
| 协议指纹与候选排序 | 0.2直接使用原classify/registry，不另复制算法 | 站点/网关样本回归、可理解的冲突展示 |
| 行为与tokenizer参考 | 0.2离线调用原runCanaries/matchTokenizer及指纹向量 | 主动题组的明确选择、发送/取消、计数口径匹配；不是自动向模型发问 |
| UUID公开映射 | 0.2支持观测内提供的映射，冲突不强选一个 | 授权范围内刷新公开目录、缓存版本及映射来源记录 |
| 未知命名/历史/聚类 | 0.2复用parseCodename及fingerprintVector | 持久化、导入导出、历史对比、聚类、回填及隐私清理 |
| 浏览器采集与控制 | 原型代码保留，0.2不自动启动浏览器原型 | 浏览器→VSCode联动、明确权限/启停、工作区与请求归属、采样配置 |
| 运行标签/轨迹链 | 0.2可分析用户提供的run.trace.model线索，不读取凭据或拉取后台 | 明确授权/接口范围、令牌不落日志、并发归属、过期/取消、可见操作与实测 |
| CDP/自动化驱动 | 原型文件保留，不在本轮执行 | 独立授权连接、目标校验、任务/断开/失败生命周期及真实浏览器验收 |
| 连接核对/WebAgent配合 | 既有MCP挑战、共享配置和Bridge命令继续可用 | 与完整浏览器端/分析历史串联，不自我确认 |
| 安全停止/资源预算 | Worker计算可取消，原型有预算与重复启动防护 | 所有钩子/定时器/轮询完整卸载，未采/截断明确可见；可配置而非静默丢线索 |

本表是剩余实施与验收清单，不是“未移植功能永不提供”的排除表。涉及凭据/后台的功能仍需合法授权与访问范围；不以“页面包含token”推定任意后台资源均获授权。

## 本轮验证标准

- 不是只测试UI文案：实际运行原SSETap/classify/probe/learned命名函数，对合成观测产出模型候选、协议、向量、映射冲突。
- 单次分析单独Worker，重复/并发输入不继承上一次BUS或映射，不读取原型localStorage。
- 不执行原main、自动fetch/CDP/轨迹/主动提问；输入由用户明确选文件，主机和模型API收到零次分析请求。
- VSIX按白名单打入同份原源码，并记录SHA256；解包后没有仓库源码目录也能分析。
- 必须记录完整测试/CI和包结构结果；桌面VS Code安装、浏览器联动和完整能力验收不能由这些离线结果代替。
