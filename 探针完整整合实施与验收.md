# 探针完整整合实施与验收

## 当前状态：0.5.2生命周期复核与兼容接入，阶段8继续验收

本页替换旧0.2/0.3/0.4阶段性表格；旧时间线保留在 manager/stages/probe-dual-integration-2026-09-15.md。目标仍是完整参考能力，不是只做连接诊断；身份不可认证不是删功能的理由。Chat模式 API 后端确切验证仍后置。

Chat与Bridge互斥，同类型可并行；手机Arena固定属于Bridge。远端读取probe_links/probe_report需Read，probe_request属于不透明操作需四项权限全开，仍须逐次本机审批；Code不是所有者权限。将参考填入Chat草稿不执行模型，实际发送前须结束Bridge工作并明确切Chat。

分工：浏览器拥有唯一采集器/页面交互；VSCode负责分析、历史及操作入口；WebAgent承接认证客户端、逐次审批与结果。分析不扩大执行权限。

| 能力 | 0.5代码路径 | 验收边界 |
|---|---|---|
| trace/run/span/用量 | Inspector原采集器、traceInput、analysisWorker | 同型号多调用分别保留；缺字段/历史不伪装新观测 |
| 字段/协议/行为 | browserReference + genericCapture，原Probe内核 | 同源Fetch/XHR/EventSource/文本WS被动采样；真站流格式待验 |
| 实时跨端 | browserBridge → /probe-link → liveCommands | 显式15分钟配对、单tab/工作区、2秒轮询；MV3休眠重配 |
| 统一远控审批 | probe_request/operatorQueue/browserActions | 一次动作、不可变文本、页面确认、取消/未知不重放 |
| 主动题组/tokenizer | 浏览器题目选择与发送；离线matchTokenizer | 安全替代拒答题；只有完整基准和可比prompt计数才计算 |
| 公开UUID映射 | 固定公开目录、缓存/版本/冲突、当前分析应用 | 无Cookie/跳转；真实公开页面与大小待验 |
| 历史与跨端迁移 | history/historyTransfer | 50条/1MiB摘要；浏览器批量迁移；新文件导出，不是云同步 |
| 未知样本/聚类/回填 | 指纹、complete-link簇、精确UUID派生记录 | 不靠相似度继承名字；原记录保留，不认证身份 |
| 原连接挑战/Chat参考 | 既有五字段挑战、/ask局部草稿 | 两个契约独立；需外部客户端完成挑战；草稿人工发送 |
| 资源/取消/卸载 | Worker、读体预算、TTL、信号、队列撤销 | 不承诺已发生副作用回滚或跨进程事务 |

## Windows/实际Arena线性验收

使用桌面VSCode集成CMD、已有Conda、系统Node，不新建venv。详细安装/配对步骤见 webagent-core/probe-extension/README.md。

1. 更新并重启主机；构建安装0.5.2 VSIX，解压加载0.5.2浏览器包；停用单独旧Probe/Inspector。确认扩展日志无缺模块错误。
2. 在已保存Arena会话显式监听、正常发送有权测试的消息。核对HUD run/span、两次同模型调用、用量缺失标记和原始标签时间；切历史不贴当前响应参考。
3. 按README配对到同一工作区；检查断网、错误扩展ID、错误tab/会话被拒绝。分别订阅trace/response；保存参考后重载VSCode再查看。
4. 申请一次rename，**未审批时网页不得修改**。核对本机文本再审批，网页确认后核对真实标题。取消/换页/撤销/过期后不得自动补做；结果未知不重复提交。
5. 用可丢弃会话测试archive：本机和网页确认后核对原生归档、浏览器本地记录删除；VSCode历史不自动消失。
6. 选择安全题，全文确认后提交；核对只发送一次、不覆盖已有草稿。发送成功不是模型回答完成；实际账户计费自行核对。
7. 测试公开目录刷新成功或明确失败且旧缓存仍在；检查来源日期和冲突。tokenizer只在完整基准与正确计数可得时测，不用总token冒充。
8. 用旧Probe“导出证据”保存JSON，选择离线分析入口，核对导出时间来源/历史/截断标记；全局旧模型不得混入新结论。浏览器导出批量历史→VSCode导入→整库导出到新文件→另一工作区导入。重复导入不重复，损坏/冲突/超额不覆盖旧库。查看指纹簇；用公开目录追加UUID参考，旧记录仍在。
9. 关闭页面、重载扩展、等待TTL、停止主机；确认订阅停止、旧凭据失效、重新配对必须明确操作。清理测试历史与导出文件，手动覆盖配对剪贴板。
10. 在已认证手机Arena→MCP/Bridge路径读取probe_links/probe_report，核对主机已处于Bridge且所有者已允许相应权限后，以Code模式请求操作并在本机批准；这是探针跨端功能验证，与已关闭的11.3协作说明不同，不能由本机HTTP替代。

记录每一步实际结果、版本、时间及可公开错误；不上传令牌、原始私密响应。当前自动测试与打包检查不代表上述真实桌面/账户步骤已通过。

## 仍须追踪，不能隐藏的差距

- 真实Chrome权限、DOM选择器、公开目录解析、实际账户流及探针跨端功能尚需对应实机记录。
- 当前版本支持旧Probe buildDump当前观测转换；全局证据/缓存判定不混入，缺失当前观测会拒绝。旧导出已丢失的4000字符以外正文无法恢复。无已保存agent会话不能远控，其他Arena页面仍可本地被动采样。
- 普通采样可选择标准/轻量预设（只影响新请求，重启恢复标准）；原trace预算保持安全上限，不保证所有站点/传输完整捕获。
- 聚类不自动按相似度回填姓名，明确UUID映射采用追加派生记录。完整分析的所有字段不是历史备份：保存的是白名单参考摘要。
- 代码完成与阶段关闭分开：以阶段8表和精确SHA CI为依据；不把“有模块”写成真实网站已成功。
