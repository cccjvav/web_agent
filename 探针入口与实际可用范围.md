# 探针到底在哪里用：入口与实际接线

核对日期：2026-09-16。这是代码接线说明，不代签用户机器安装或真实网站兼容。

## 不是一个扩展，而是三个部分

1. **整合浏览器Inspector**：加载在你访问Arena的Chrome/Edge中；它拥有唯一CDP/debugger连接，采集trace与响应参考。启动WebAgent不会自动开始监听。
2. **agent-host**：接收明确配对的共享报告，提供认证MCP工具probe_links/probe_report/probe_request及本机/api/probe接口，复用operatorQueue审批。不是浏览器采集器，也不会自动鉴定本机Chat API背后的模型。
3. **Probe Companion 0.5.2**：独立VSIX，提供桌面VSCode分析、历史、配对、订阅、请求/审批命令。核心WebAgent 0.7.1扩展提供Chat/Bridge/PTY，不能把这两者混为一个。

## 启动方式对照

| 启动方式 | 实际已接入 | 尚未接入/不会自动发生 |
|---|---|---|
| 桌面VSCode + run-webagent.cmd | 主机接口；另安装Companion后使用命令面板的探针功能 | install-vscode-extension.cmd只安装核心扩展，不安装Companion；仍需浏览器扩展与显式配对 |
| 仅run-webagent.cmd经典工作台 | 同一主机探针API/MCP工具；通用受控操作页面可审批已提交的操作 | 没有与Companion等价的探针配对/分析/历史/订阅面板；不会自动采集 |
| run-webagent-vscode.cmd / code-server / App窗口 | 同一主机接口；自动同步核心WebAgent扩展 | 未自动安装Companion；Companion的allowed明确要求Desktop且无remoteName，即使手工装VSIX也不能说功能可用 |

## 当前桌面使用入口

在可信本地项目中，按[Companion说明](webagent-core/probe-extension/README.md)构建、安装独立VSIX；按[浏览器整合说明](webagent-core/probe-extension/浏览器整合说明.md)加载整合Inspector。停用旧的单独采集器，不创建第二个CDP连接。然后使用命令面板“WebAgent Probe: 配对浏览器到当前主机工作区”，确认项目并把一次性JSON粘贴到浏览器扩展弹窗。凭据不发给AI、不记日志。订阅是读取，保存历史另行确认，浏览器写操作仍需审批。

## 不扩展的实现边界（用户已确认不要求补齐）

用户2026-09-16说明此前只是询问，不要求施工经典工作台/code-server探针入口。因此以下仅为将来另有需求时的设计边界，不是当前待办：经典工作台若要扩展，需要配对/撤销、会话参考、状态失效、操作审批的明确入口；完整离线分析/历史也不能靠展示原始JSON冒充。code-server需要设计宿主与浏览器位置、来源认证、信任/工作区生命周期及Worker/文件URI支持，再接入测试。不能直接删掉Desktop保护、把localhost误当用户浏览器所在电脑，或宣称已经兼容SSH/WSL。

源码定位：probe-extension/extension.js的allowed；scripts/ensure-code-server.js的syncExtension；agent-host/src/api/routes.js的/probe路由；src/utils/probeBridge.js的pair/report/request；workbench/js/operations.js的通用审批。主机接线测试通过不等于上述缺失界面完成。
