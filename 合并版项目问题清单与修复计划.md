# Web Agent 合并版项目问题清单与修复计划

> 这是两轮检查结果的合并版：
> - 本次仓库检查发现的问题
> - 负责项目助手自查发现的问题
>
> 本文件只整理问题和建议，不代表已经修复。
>
> 检查日期：2026-09-05

---

## 一、先澄清分支和版本问题

当前沙盒仓库实际状态为：

```text
当前本地分支：arena/01a07192-web-agent
HEAD：4b681e9 feat: add visual HTML guide for architecture and code docs
origin/main：5837ed2 feat: ShunCode core repro
```

当前本地没有看到：

```text
arena/01a05d84-web-agent
origin/arena/01a05d84-web-agent
```

因此，负责项目助手提示词中的：

```text
你在仓库 cccjvav/web_agent 的分支 arena/01a05d84-web-agent 上工作
```

与本次沙盒实际检出的分支不同。

这不是我凭空使用的分支名：`arena/01a07192-web-agent` 是本次 Arena 会话预先检出的工作分支，当前 Git 也确认 HEAD 位于该分支。

同时，GitHub 远程当前只显示了 `origin/main`，没有在本地远程引用中看到 `origin/arena/01a05d84-web-agent`。因此不能仅凭助手提示词断定 GitHub 上确实存在或已经更新了 `01a05d84` 分支；需要在 GitHub 网页或执行以下命令后确认：

```bash
git fetch --all --prune
git branch -r
git ls-remote --heads origin
```

注意：本会话分支由 Arena 固定，不能切换到其他分支。若 `01a05d84` 是 GitHub 上另一个分支，应先确认它与当前 `01a07192` 是否为不同提交、不同阶段或不同任务快照，再决定后续操作。

---

## 二、P0：必须优先处理

### P0-1：工作台 boot 可能因不存在的 DOM 节点直接崩溃

负责助手指出：

- `webagent-core/workbench/js/bind.js` 访问 `#btn-detect-env`
- 还访问 `#page-env`、`#page-stack`
- 当前 `index.html` 可能没有这些节点
- `boot()` 第一行执行 `ui.bind()`
- 如果绑定代码直接对 `null` 赋值，后续状态刷新、文件树、WebSocket、终端、搜索、新建文件等都不会执行

建议：

1. 检查上述节点是否真的缺失。
2. 所有可选 DOM 节点使用空值守卫。
3. 决定是补齐环境/技术栈页面，还是删除无效导航和文档步骤。
4. 增加 `bind()` 在当前 HTML 上不抛异常的测试。

这是用户打开工作台后可能完全不可用的 P0 功能问题。

---

### P0-2：Cloudflare Tunnel 可能把未认证 `/api` 整套暴露到公网

双方都发现：

- agent-host 同时监听 UI 和 MCP
- MCP 端口监听 `0.0.0.0`
- Cloudflare Tunnel 转发整个 `48271`
- `/api` 路由没有统一认证
- `cors()` 全开放
- `/ws` 的连接消息还可能携带 secret

高风险接口包括：

```text
GET  /api/status
POST /api/tool/call
POST /api/chat
PUT  /api/files/content
POST /api/consensus/run
POST /api/bridge/reset-secret
POST /api/bridge/start
POST /api/bridge/stop
POST /api/models
PUT  /api/customizations
POST /api/skills
```

可能造成：

- 远程读取工作区
- 远程写入文件
- 远程运行工具和命令
- 修改模型设置
- 修改自定义提示词
- 重置 secret
- 触发本地 Agent

建议优先采用以下架构：

```text
本机 UI/API：仅监听 127.0.0.1
远程 MCP：独立监听远程端口
Cloudflare Tunnel：只转发 MCP 和必要 OAuth 发现接口
```

如果暂时不拆服务，至少：

- `/api/*` 增加认证和 scope
- 本机 UI 使用单独本地 token
- 远程 MCP 使用 Bearer/OAuth
- 禁止远程直接访问文件写入和命令执行 API
- 对 `/ws` 进行认证并禁止发送 secret

---

### P0-3：远程 Agent 可以间接获得任意 Shell / PowerShell 执行能力

`src/tools/executor.js` 使用 bash 或 PowerShell 执行命令：

```js
spawn('/bin/bash', ['-c', command])
```

Windows 使用 `ExecutionPolicy Bypass`。

工作目录限制不等于命令权限限制。命令仍可能：

- 读取 workspace 外部文件
- 访问环境变量和凭证
- 访问网络
- 安装软件
- 删除数据
- 启动后台进程
- 修改系统
- 创建无法被正常清理的子进程

建议：

- 命令分为只读、测试、写入、高风险等级
- 高风险命令人工确认
- 禁止或警告删除、格式化、关机、`git push`、下载后执行等命令
- 限制网络、CPU、内存、子进程和文件系统
- 正确清理整个进程树
- 对远程 MCP 与本机 Agent 使用不同权限

---

## 三、P1：高优先级安全问题

### P1-1：CORS 全开放与未认证 API 组合风险

当前存在：

```js
app.use(cors());
```

结合无认证写 API，恶意网页可能尝试调用本机 Agent 服务。

建议：

- 删除无条件 `cors()`
- 只允许本地工作台和可信来源
- 写 API 增加认证
- 增加 Origin/Referer 检查和 CSRF 防护
- 不把 CORS 当作认证机制

---

### P1-2：`code-server` 默认无认证

`run-code-oss.js` 使用：

```text
--auth none
--trusted-origins *
--disable-workspace-trust
```

如果端口被局域网或公网访问，可能任何人都能进入 VS Code。

建议：

- 默认绑定 `127.0.0.1`
- 需要公网预览时单独启用安全配置
- 默认使用 token 或密码
- 不要默认信任所有 Origin
- 不要默认关闭 workspace trust

---

### P1-3：路径沙箱可能被 symlink/junction 绕过

`resolveSafePath()` 主要检查逻辑路径，未充分检查真实路径。

示例：

```text
workspace/link -> /home/user/private
link/secrets.txt
```

建议使用真实路径检查，并增加：

- Linux symlink 测试
- Windows junction 测试
- UNC 路径测试
- 外部目录链接测试
- 新建文件时父目录真实路径测试

---

### P1-4：API 直接写文件，绕过工具层保护

API 中直接使用：

```js
fs.writeFileSync(full, content, 'utf8');
```

可能绕过工具层的：

- expectedHash
- 覆盖确认
- 敏感路径保护
- 原子写入
- 冲突检查

建议所有 API 文件写入统一调用工具层，并使用原子写入。

---

### P1-5：Secret 同时承担 URL、Bearer 和管理身份

同一个 secret 可能用于：

- `/mcp/<secret>` URL
- Bearer token
- MCP 访问
- 某些管理行为

建议分离：

- UI session token
- MCP read-only token
- MCP write token
- 管理 token
- OAuth access token

并增加 scope、audience、过期、撤销和来源记录。

---

### P1-6：OAuth 缺少完整的限速和生命周期设计

当前 OAuth 状态主要存在内存 Map 中，进程重启后 token 和注册客户端会丢失。

需要补充：

- 动态 client 注册限速
- IP/client 维度限制
- client 清理
- refresh token 轮换
- refresh token 重放检测
- 明确进程重启后的行为
- 必要时加密持久化

---

### P1-7：默认登录状态与实际 UI 文案矛盾

负责助手指出：

- 默认 `bridge.loggedIn=true`
- UI 又提示“需要先登录并确认设备授权”
- GitHub 登录可能只是 demo

需要明确选择：

- 实现真实登录流程
- 默认未登录并阻止 Bridge
- 或明确是演示模式，不伪装成真实 GitHub 登录

---

### P1-8：API Key 文案声称 secure storage，但实际可能明文保存

如果 key 保存于：

```text
.webagent/config.json
```

则不应直接称为 secure storage。

建议：

- 文案改为“保存到本地配置文件”；或
- 接入 Windows Credential Manager、macOS Keychain、Linux Secret Service；或
- 至少设置文件权限、脱敏日志和状态输出。

---

## 四、P1：功能真实性和产品一致性问题

负责助手指出以下功能可能只是 UI 示意：

- GitHub 登录
- license“永久顺”旧品牌文案
- Codex 按钮仅写 JSON
- Named/ngrok 没有真正 spawn
- 自定义智能体
- Hooks
- Chat 外接 MCP
- 插件
- Voice

处理原则：

- 已实现的功能接到真实逻辑
- 未实现的按钮删除，或标注“示意功能，当前源码未实现”
- 文案不能暗示不存在的功能已经可用
- 旧品牌和 license 文案必须统一

---

## 五、P1：全局状态和并发隔离问题

当前很多状态是进程级全局：

- task state
- read hash cache
- MCP session
- Bridge 状态
- secret
- command store
- OAuth pairing
- model 配置

多个客户端同时使用时可能互相影响：

- 一个客户端 reset round，清空其他客户端 hash
- 一个客户端 reset secret，其他客户端全部失效
- 一个客户端 reset task，覆盖其他客户端任务
- 多个 Agent 同时修改同一文件

建议引入：

```text
workspaceId
clientId
sessionId
taskId
```

至少将 task、hash、命令、聊天和取消状态按 session 隔离。

---

## 六、P1/P2：文件、补丁和稳定性问题

### 1. CRLF/LF 可能被补丁系统改变

补丁匹配阶段将 CRLF 标准化为 LF，写回时可能导致整个 Windows 文件被转换成 LF。

建议保留原始换行风格并增加测试。

### 2. SEARCH/REPLACE 多匹配时可能静默替换第一个

建议多匹配默认拒绝，要求唯一匹配或显式 occurrence。

### 3. 新文件补丁格式可能误把 Unified Diff 当成文件正文

建议严格区分：

- 新文件内容
- SEARCH/REPLACE
- Unified Diff

### 4. `cancelCommand()` 状态竞争

取消后 `close` 事件可能将状态重新写成 `done`。

建议使用只能进入一次终态的状态机。

### 5. 命令 ID 和命令状态只存在内存

重启后状态丢失，命令 ID 可能重复。建议使用 UUID，并明确重启语义。

### 6. 大文件、二进制和 grep 资源限制不足

建议增加：

- 单文件大小限制
- 总扫描字节数限制
- 最大文件数
- 二进制检测
- 超时和取消
- 恶意正则防护

### 7. SSE/WebSocket 缺少资源上限

建议增加：

- 最大连接数
- 每 IP 限制
- 空闲超时
- 单消息大小上限
- 广播背压

---

## 七、P1/P2：文档、品牌和工程一致性问题

### 1. 旧的 `workbench/app.js` 引用

当前工作台已经拆分为多个 `workbench/js/*.js` 文件，但 README 或生成文档可能仍引用旧的 `app.js`。

### 2. 技术文档固定行号漂移

例如文档写 `tools/index.js` 某函数在 L435，但源码已经移动到 L451。

建议改为“按函数名定位”，不要人工维护闭区间行号；如果必须显示行号，应自动生成。

### 3. docs-site 根目录 Markdown 链接可能 404

`docs-site/serve.js` 只服务 `docs-site/`，但侧栏可能链接到 `../架构导读.md` 等仓库根文件。

建议：

- docs-site 内部链接统一使用站内 hash 路由；或
- 明确配置安全的仓库根静态文件服务；或
- 构建时将所有文档内容嵌入页面

### 4. docs-site 生成文件可能与源 Markdown 漂移

应增加构建一致性检查：

```bash
npm run docs:build
git diff --exit-code docs-site/content.js
```

### 5. `bin/code-server-runtime/package.json` 仍有旧品牌

需要统一 ShunCode、Web Agent 和当前项目名，区分历史来源与当前产品品牌。

### 6. `origin/main` 仍是旧产品内容

不能擅自修改 main，但 README 应明确当前有效分支或正式发布入口。正式用户不应依赖不稳定的 Arena 内部工作分支。

### 7. `webagent-repro` 容易被误启动

建议：

- 标明 `ARCHIVED`
- 不提供默认生产启动入口
- 文档明确“历史原型，不是现行主程序”

### 8. “代码不出门，工单进门”表述过于绝对

启用 Tunnel 后，远程客户端可以调用本机开放接口。文档应明确说明认证、授权和公网暴露风险。

---

## 八、测试缺口

现有主项目测试在依赖安装后可以通过，但需要新增：

### 安全测试

- 未认证访问 `/api`
- Tunnel 暴露后访问 `/api`
- CORS 和 CSRF
- OAuth scope、过期和撤销
- secret 泄露防护
- `/ws` 认证
- code-server 认证

### 文件系统测试

- symlink/junction 逃逸
- UNC 路径
- 大小写路径
- API 写入是否经过 hash 保护
- CRLF/LF 保留
- SEARCH 多匹配

### 工作台测试

- 当前 HTML 调用 `bind()` 不抛异常
- 所有可选按钮缺失时仍能启动
- boot 失败时能显示错误
- WebSocket 连接和断线处理

### 并发和资源测试

- 两个 Agent 同时 patch
- 同时 write/delete
- 同时 reset secret
- 多个 MCP session
- command cancel 状态
- 大文件和超长输出
- SSE/WebSocket 连接上限
- 恶意 grep 正则

### 启动和跨平台测试

- Windows CMD
- PowerShell
- code-server 启动失败清理
- cloudflared 崩溃恢复
- npm install 中断
- 端口释放
- 子进程清理

---

## 九、推荐修复顺序

### 第一阶段：阻断高风险问题

1. 修复工作台 boot 崩溃
2. 阻止 Tunnel 访问未认证 `/api`
3. 给命令执行增加权限和人工确认
4. 移除全开放 CORS
5. 修复 code-server 无认证
6. 修复 symlink/junction 路径逃逸

### 第二阶段：统一安全和数据操作

1. API 写文件统一走工具层
2. 分离 secret、Bearer、OAuth 和管理权限
3. 增加 session/client 隔离
4. 增加 OAuth 限速和生命周期处理
5. 加入资源限制和进程树清理
6. 修复 CRLF、补丁多匹配和取消状态问题

### 第三阶段：清理产品和文档

1. 清理假 UI 或明确标注未实现
2. 修正登录状态和 API Key 文案
3. 清理旧品牌和旧 license
4. 修正旧文件名和固定行号
5. 修复 docs-site 404 和构建漂移
6. 标记并隔离 `webagent-repro`
7. 更新安全模型文档

### 第四阶段：补齐自动化测试

优先新增安全边界、工作台 boot、路径逃逸、并发、资源限制和文档一致性测试。

---

## 十、最终判断

两份检查结果应合并使用：

- 负责助手的检查更偏向：工作台可用性、假 UI、品牌、文档和当前分支状态。
- 本次检查更偏向：远程攻击面、命令执行、文件系统安全、并发、资源限制和数据一致性。

综合后，当前最关键的三件事是：

```text
1. 修复工作台 boot 崩溃，确保基本界面真正可用。
2. 阻止 Cloudflare Tunnel 绕过 MCP/OAuth 访问整个 /api。
3. 限制 Agent 的 Shell/PowerShell 执行能力，并修复路径沙箱逃逸风险。
```

在这些问题解决前，不建议把 Cloudflare Tunnel 地址长期暴露，也不建议把项目部署到不受信任的局域网或公网环境。

---

# 十一、第三轮审查新增问题（web-agent-review.md）

以下内容来自第三轮审查。该审查基线写作 `arena/01a07189-web-agent`、基线提交 `4b681e9`；当前 Arena 沙盒分支仍是 `arena/01a07192-web-agent`，因此修复前应以实际工作树重新确认。

## 1. 本机 Chat 事件流签名错误：新增 P0/P1

`runChat.js` 的函数签名可能是：

```js
async function runChat(payload, emit) { ... }
```

但 API 路由这样调用：

```js
await runChat({ mode, message, history, emit });
```

如果当前源码确实如此，则 `emit` 被放进第一个参数，第二个参数为空，导致内置 Agent 的 `status`、`tool`、`message` 事件被静默跳过。

现有单元测试使用两参数调用，因此可能全部通过，却没有覆盖真实 HTTP 路径。

建议：

- 统一使用 `runChat({ mode, message, history, emit })`
- 同步修改所有调用方和测试
- 新增真实 `POST /api/chat` 集成测试
- 解析 NDJSON，验证至少出现 `tool`、`message`、`done`
- 分别测试无 API Key 的内置 Agent 路径和 OpenAI Provider 路径

## 2. “多模型博弈”可能是硬编码模拟：新增 P1

`consensusEngine.js` 可能硬编码：

- planA / planB / planC
- `agreementRate: '97%'`
- `confidence: 0.94/0.96/0.95`
- `consensusReached: true`
- `sleep(200)` 模拟等待

如果没有实际模型调用，不能在 UI 和 README 中作为真实多模型能力宣传。

建议：

- 未实现时改名为“内置静态检查清单”
- 删除伪造的一致率和置信度
- 返回 `simulated: true`
- UI 和文档明确“不调用多个模型”
- 或真正实现多模型调用、失败处理、成本限制和隐私说明

## 3. 模型能力标签可能是猜测或编造：新增 P1/P2

`agent/providers.js` 中的 `guessContext()`、`guessCaps()` 可能通过模型名称猜测：

- 上下文长度
- 视觉能力
- 视频能力

未知能力应显示为空或 `—`，不能把猜测值作为真实模型元数据展示，否则可能导致错误的模型选择和超限请求。

## 4. 持久化 read hash 可能削弱覆盖保护：新增 P1

`read-hashes.json` 跨进程保存 read hash，`write_file` 可能据此认为文件已被确认读取。

这会使“几天前读过的文件”在今天仍可被无额外确认覆盖。

建议区分：

- 持久 hash：用于冲突检测
- 当前 session 已读 hash：用于覆盖确认

覆盖保护应优先只接受当前 session、当前 client 的读取记录，或始终要求显式 `confirm_overwrite=true`。

## 5. 外部工作区可能保存明文 secret/API key：新增 P1

当用户运行：

```bat
run-webagent.cmd D:\code\my-app
```

配置可能被写入：

```text
D:\code\my-app\.webagent\config.json
```

仓库根的 `.gitignore` 只保护项目内固定的 `workspace/.webagent/config.json`，无法保护用户传入的外部仓库。

建议优先把敏感配置放到用户级目录：

- Windows：`%APPDATA%\WebAgent\`
- Linux：`~/.config/webagent/`
- macOS：`~/Library/Application Support/WebAgent/`

如果暂时仍写入工作区：

- 自动提醒用户
- 检测 Git 仓库并提示风险
- 自动建议或写入 `.gitignore`
- 使用文件权限
- 增加 `**/.webagent/config.json` 等规则
- 但不要只依赖本项目根 `.gitignore`

## 6. MCP session/SSE 实现可能不完整：新增 P1/P2

需要复核：

- `Mcp-Session-Id` 是否真正维护和校验
- 是否支持 session 生命周期和 DELETE
- SSE endpoint 返回的后续 POST 地址是否包含正确认证信息
- Streamable HTTP 和旧式 SSE 客户端是否都能继续调用
- JSON-RPC 错误与 HTTP 状态码是否一致
- Origin 校验是否完善

建议增加真实 MCP 客户端兼容测试，而不只测试单次请求。

## 7. eventBus、`/api/logs`、`/api/status` 和 `/ws` 可能泄露大量敏感信息：新增细化

可能泄露：

- 完整工具参数
- 补丁全文
- 命令字符串
- 命令输出
- 文件路径
- 文件读取事件
- 哈希和最近日志

建议：

- `/api/logs` 单独认证
- `/api/status` 只返回摘要
- 不记录完整 patch、命令参数和文件正文
- token、key、password 统一脱敏
- WebSocket 按客户端权限过滤事件
- 不在 WebSocket 初始消息中发送 secret

## 8. 测试全绿但未覆盖真实 HTTP 路径：新增测试结论

当前 `npm test` 通过不代表 `/api/chat`、浏览器工作台和公网接口正确。

需要增加：

- `/api/chat` 端到端测试
- `/api` 鉴权测试
- CORS/Origin 测试
- `/ws` 认证和数据脱敏测试
- 工作台 `bind()`/`boot()` 测试
- 内置 Agent 无 API Key 测试

---

# 十二、第三轮修复方案中的兼容性注意事项

## 1. 拆分 UI/MCP 服务后必须同步 VS Code 扩展

建议把 UI API 仅放到 UI server、MCP 只放到 MCP server，但当前 VS Code 扩展可能默认访问：

```text
http://127.0.0.1:48271/api/chat
```

拆分后必须同步修改：

- extension 的 `agentHostUrl`
- VS Code 启动逻辑
- `run-code-oss.js`
- 工作台 API 地址
- Bridge UI
- README 和使用指南
- 集成测试

可选方案：

- 分别配置 `agentHostUiUrl` 和 `agentHostMcpUrl`
- 或提供本机安全反向代理

不能只拆服务而不更新扩展，否则会修复安全问题却破坏 VS Code Chat。

## 2. `/api/bridge/secret` 不能只是换一个路径

如果新增 `/api/bridge/secret` 但它仍然无认证，只是把泄露从 `/api/status` 移到另一个接口，问题没有解决。

该接口至少应：

- 仅允许本机 UI Origin
- 要求本机 token
- 不通过 WebSocket 广播
- 不写入日志
- 不返回给远程 MCP 客户端

## 3. 不要直接删除 `extensions-installed`

建议先建立单一来源：

- 以 `extension/` 为源
- 启动时校验/同步到运行时目录
- 确保离线环境和首次启动仍可用
- 确认安装失败时的提示和回滚

## 4. 不要机械把测试改成 `node --test`

当前测试可能是直接执行脚本的自定义测试，而不是 `node:test` 格式。

改 runner 前需先确认测试结构，否则可能把现有全绿测试改坏。可以先保留现有 runner，再增加失败汇总、覆盖率和集成测试。

## 5. CI 需要处理跨平台和外部依赖

Linux/Windows CI 应避免：

- 真实 cloudflared
- 外部模型服务
- 固定端口
- `/bin/bash` 假设
- 固定 HOME 路径
- code-server 网络下载

测试应使用 mock、端口 0 和平台条件分支。

---

# 十三、综合后的最终优先级

## P0

1. Bridge 暴露未认证 `/api`
2. CORS 全开放
3. `/ws` 无鉴权且下发 secret/敏感事件
4. `/api/chat` 的 `runChat` 事件流签名错误
5. 远程 API 可触发任意命令执行
6. 工作台 `boot()` 可能因缺少 DOM 节点崩溃

## P1

1. 符号链接/junction 逃逸
2. 命令超时无法杀完整进程组
3. 假登录和假设备授权
4. 假多模型博弈和伪造模型能力标签
5. secret/API key 写入外部工作区
6. 持久 hash 缓存削弱覆盖确认
7. MCP session/SSE 兼容性问题
8. 日志和 eventBus 泄露命令、文件和补丁
9. code-server 默认无认证
10. API 文件写入绕过工具层保护
11. 全局状态未按 session 隔离
12. API/WS/状态接口之间的认证边界不一致

## P2

1. 版本号散落
2. 手工扩展副本
3. 重复的 `toolLabel`
4. `webagent-repro` 旧原型
5. docs-site 生成物漂移
6. 测试串行长链和随机端口
7. profile 语言硬编码
8. memory limit 语义混乱
9. findFiles 静默截断
10. 缺少跨平台测试
11. 启动脚本行为不对等
12. 旧品牌、旧 license 和假 UI 文案

## P3

1. 缺 CI
2. 缺 LICENSE
3. 缺 SECURITY.md
4. 缺 CONTRIBUTING.md
5. 缺 lint/format 规范
6. 缺英文 README
7. 文档固定行号维护成本高
8. 文档未清晰警告 Bridge 会把本机能力暴露到公网
9. 外部工作区配置文件缺少自动忽略提示

---

# 十四、合并后的最终判断

三轮检查互相补充：

- 第一轮主要发现了远程攻击面、命令执行、文件系统、并发和资源限制问题。
- 第二轮补充了工作台 boot、假 UI、品牌、文档和分支状态问题。
- 第三轮进一步通过黑盒 HTTP 验证确认了 `/api` 公网执行命令、CORS、WebSocket、`runChat` 事件流和测试盲区，并补充了 MCP 协议、持久 hash、外部工作区密钥、假多模型和模型能力标签问题。

当前最重要的修复顺序是：

```text
1. 修复工作台 boot 和 /api/chat 事件流，保证本机基本功能真实可用。
2. 阻止 Tunnel 绕过 MCP/OAuth 访问 /api、/ws 和敏感状态。
3. 限制 Agent 的 Shell/PowerShell 执行能力并修复真实路径沙箱。
4. 防止 secret/API key 写入外部项目并被 Git 提交。
5. 清除假登录、假多模型、伪造模型能力和旧品牌文案。
6. 补充真实 HTTP、MCP、WebSocket、安全、并发和跨平台测试。
```

在完成前 3 项之前，不建议把 Cloudflare Tunnel 地址长期暴露，也不建议把项目部署到不受信任的局域网或公网环境。
